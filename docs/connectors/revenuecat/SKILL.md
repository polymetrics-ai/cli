---
name: pm-revenuecat
description: RevenueCat connector knowledge and safe action guide.
---

# pm-revenuecat

## Purpose

Reads and writes RevenueCat v2 project configuration, customer, product, offering, subscription, purchase, paywall, virtual currency, integration, and metrics resources through the REST API.

## Icon

- id: revenuecat
- asset: icons/revenuecat.svg
- source: official
- review_status: official_verified
- review_url: https://www.revenuecat.com/docs/api-v1

## Capabilities

- check=true catalog=true read=true write=true query=false
- Integration type: api

## Authentication

- Use pm credentials add with --from-env or --value-stdin for secret fields.

## Configuration

- app_id
- base_url
- chart_name
- created_after
- customer_id
- entitlement_id
- invoice_id
- offering_id
- package_id
- paywall_id
- product_id
- project_id
- purchase_id
- starting_after
- store_purchase_identifier
- store_subscription_identifier
- subscription_id
- updated_after
- virtual_currency_code
- webhook_integration_id
- api_key (secret)

## ETL Streams

- projects:
  - primary key: id
  - fields: created_at(string), icon_url(string), icon_url_large(string), id(string), name(string), object(string), stream(string)
- apps:
  - primary key: id
  - fields: created_at(string), id(string), name(string), object(string), project_id(string), stream(string), type(string)
- products:
  - primary key: id
  - fields: app_id(string), created_at(string), display_name(string), id(string), object(string), state(string), store_identifier(string), stream(string), type(string)
- offerings:
  - primary key: id
  - fields: created_at(string), description(string), display_name(string), id(string), identifier(string), lookup_key(string), object(string), project_id(string), state(string), stream(string)
- customers:
  - primary key: id
  - fields: app_user_id(string), created_at(string), first_seen_at(string), id(string), last_seen_at(string), object(string), project_id(string), stream(string), updated_at(string)
- app:
  - primary key: id
  - fields: created_at(string), id(string), name(string), object(string), project_id(string), stream(string), type(string)
- app_public_api_keys:
  - primary key: id
  - fields: app_id(string), created_at(string), environment(string), id(string), key(string), object(string), stream(string)
- app_store_kit_config:
  - primary key: app_id
  - fields: app_id(string), contents(string), object(string), stream(string)
- audit_logs:
  - primary key: id
  - fields: action_type(string), actor_identifier(string), actor_type(string), additional_data(string), id(string), object(string), occurred_at(string), project_id(string), stream(string), target_identifier(string), target_type(string)
- chart_data:
  - primary key: chart_name
  - fields: category(string), chart_name(string), description(string), display_name(string), display_type(string), object(string), resolution(string), stream(string), summary(string), values(string)
- chart_options:
  - primary key: chart_name
  - fields: chart_name(string), filters(string), object(string), resolutions(string), segments(string), stream(string), user_selectors(string)
- collaborators:
  - primary key: id
  - fields: accepted_at(string), email(string), has_mfa(string), id(string), name(string), object(string), role(string), stream(string)
- customer:
  - primary key: id
  - fields: active_entitlements(string), attributes(string), first_seen_at(string), id(string), last_seen_at(string), object(string), project_id(string), stream(string)
- customer_active_entitlements:
  - primary key: entitlement_id
  - fields: entitlement_id(string), expires_at(string), object(string), stream(string)
- customer_aliases:
  - primary key: id
  - fields: created_at(string), id(string), object(string), stream(string)
- customer_attributes:
  - primary key: name
  - fields: name(string), object(string), stream(string), updated_at(string), value(string)
- customer_center:
  - primary key: customer_id
  - fields: customer_center(string), customer_id(string), object(string), stream(string)
- customer_invoices:
  - primary key: id
  - fields: id(string), invoice_url(string), issued_at(string), line_items(string), object(string), paid_at(string), stream(string), total_amount(string)
- customer_purchases:
  - primary key: id
  - fields: customer_id(string), id(string), object(string), product_id(string), purchased_at(string), status(string), store_purchase_identifier(string), stream(string)
- customer_subscriptions:
  - primary key: id
  - fields: customer_id(string), id(string), object(string), product_id(string), starts_at(string), status(string), store_subscription_identifier(string), stream(string)
- customer_virtual_currencies:
  - primary key: currency_code
  - fields: balance(string), currency_code(string), description(string), name(string), object(string), stream(string)
- entitlements:
  - primary key: id
  - fields: created_at(string), display_name(string), id(string), lookup_key(string), object(string), products(string), project_id(string), state(string), stream(string)
- entitlement:
  - primary key: id
  - fields: created_at(string), display_name(string), id(string), lookup_key(string), object(string), products(string), project_id(string), state(string), stream(string)
- entitlement_products:
  - primary key: id
  - fields: app_id(string), created_at(string), display_name(string), id(string), object(string), state(string), store_identifier(string), stream(string), type(string)
- webhook_integrations:
  - primary key: id
  - fields: app_id(string), created_at(string), environment(string), event_types(string), id(string), name(string), object(string), project_id(string), stream(string), url(string)
- webhook_integration:
  - primary key: id
  - fields: app_id(string), created_at(string), environment(string), event_types(string), id(string), name(string), object(string), project_id(string), stream(string), url(string)
- overview_metrics:
  - primary key: project_id
  - fields: currency(string), metrics(string), object(string), project_id(string), stream(string)
- revenue_metric:
  - primary key: project_id
  - fields: currency(string), end_date(string), object(string), project_id(string), revenue_type(string), start_date(string), stream(string), value(string)
- offering:
  - primary key: id
  - fields: created_at(string), display_name(string), id(string), is_current(string), lookup_key(string), metadata(string), object(string), packages(string), paywall_id(string), project_id(string), stream(string)
- offering_packages:
  - primary key: id
  - fields: created_at(string), display_name(string), id(string), lookup_key(string), object(string), position(string), products(string), stream(string)
- package:
  - primary key: id
  - fields: created_at(string), display_name(string), id(string), lookup_key(string), object(string), position(string), products(string), stream(string)
- package_products:
  - primary key: id
  - fields: eligibility_criteria(string), id(string), object(string), product(string), stream(string)
- paywalls:
  - primary key: id
  - fields: automatically_scale_font_size(string), created_at(string), id(string), name(string), object(string), offering_id(string), published_at(string), stream(string)
- paywall:
  - primary key: id
  - fields: automatically_scale_font_size(string), components(string), created_at(string), id(string), name(string), object(string), offering_id(string), published_at(string), stream(string)
- product:
  - primary key: id
  - fields: app_id(string), created_at(string), display_name(string), id(string), object(string), state(string), store_identifier(string), stream(string), type(string)
- purchases:
  - primary key: id
  - fields: customer_id(string), id(string), object(string), product_id(string), purchased_at(string), status(string), store_purchase_identifier(string), stream(string)
- purchase:
  - primary key: id
  - fields: customer_id(string), id(string), object(string), product_id(string), purchased_at(string), status(string), store_purchase_identifier(string), stream(string)
- purchase_entitlements:
  - primary key: id
  - fields: created_at(string), display_name(string), id(string), lookup_key(string), object(string), project_id(string), state(string), stream(string)
- subscriptions:
  - primary key: id
  - fields: customer_id(string), id(string), object(string), product_id(string), starts_at(string), status(string), store_subscription_identifier(string), stream(string)
- subscription:
  - primary key: id
  - fields: customer_id(string), id(string), management_url(string), object(string), product_id(string), starts_at(string), status(string), store_subscription_identifier(string), stream(string)
- subscription_authenticated_management_url:
  - primary key: subscription_id
  - fields: management_url(string), object(string), stream(string), subscription_id(string)
- subscription_entitlements:
  - primary key: id
  - fields: created_at(string), display_name(string), id(string), lookup_key(string), object(string), project_id(string), state(string), stream(string)
- subscription_transactions:
  - primary key: id
  - fields: expiration_date(string), id(string), object(string), product_store_identifier(string), purchased_at(string), revenue_in_usd(string), stream(string)
- virtual_currencies:
  - primary key: code
  - fields: code(string), created_at(string), description(string), name(string), object(string), product_grants(string), project_id(string), state(string), stream(string)
- virtual_currency:
  - primary key: code
  - fields: code(string), created_at(string), description(string), name(string), object(string), product_grants(string), project_id(string), state(string), stream(string)

## Sync Modes

- ETL sync modes: full_refresh_append, full_refresh_overwrite, full_refresh_overwrite_deduped

## Reverse ETL Actions

- create_project:
  - endpoint: POST /projects
  - required fields: name
  - risk: creates a RevenueCat project
- create_app:
  - endpoint: POST /projects/{{ config.project_id }}/apps
  - required fields: name, type
  - risk: creates a RevenueCat app in the configured project
- update_app:
  - endpoint: POST /projects/{{ config.project_id }}/apps/{{ record.app_id }}
  - required fields: app_id
  - risk: updates an existing RevenueCat app
- delete_app:
  - endpoint: DELETE /projects/{{ config.project_id }}/apps/{{ record.app_id }}
  - required fields: app_id
  - risk: permanently deletes a RevenueCat app and its configuration
- create_customer:
  - endpoint: POST /projects/{{ config.project_id }}/customers
  - required fields: id
  - risk: creates a RevenueCat customer
- delete_customer:
  - endpoint: DELETE /projects/{{ config.project_id }}/customers/{{ record.customer_id }}
  - required fields: customer_id
  - risk: permanently deletes a RevenueCat customer
- assign_customer_offering:
  - endpoint: POST /projects/{{ config.project_id }}/customers/{{ record.customer_id }}/actions/assign_offering
  - required fields: customer_id
  - risk: assigns or clears a customer offering override
- grant_customer_entitlement:
  - endpoint: POST /projects/{{ config.project_id }}/customers/{{ record.customer_id }}/actions/grant_entitlement
  - required fields: customer_id, entitlement_id
  - risk: grants an entitlement to a customer
- restore_purchase_by_order_id:
  - endpoint: POST /projects/{{ config.project_id }}/customers/{{ record.customer_id }}/actions/restore_purchase_by_order_id
  - required fields: customer_id, order_id
  - risk: restores a Google Play purchase by order id
- revoke_customer_granted_entitlement:
  - endpoint: POST /projects/{{ config.project_id }}/customers/{{ record.customer_id }}/actions/revoke_granted_entitlement
  - required fields: customer_id, entitlement_id
  - risk: revokes a granted customer entitlement
- transfer_customer_data:
  - endpoint: POST /projects/{{ config.project_id }}/customers/{{ record.customer_id }}/actions/transfer
  - required fields: customer_id, destination_customer_id
  - risk: transfers subscriptions and purchases to another customer
- set_customer_attributes:
  - endpoint: POST /projects/{{ config.project_id }}/customers/{{ record.customer_id }}/attributes
  - required fields: customer_id, attributes
  - risk: sets customer attributes
- create_virtual_currencies_transaction:
  - endpoint: POST /projects/{{ config.project_id }}/customers/{{ record.customer_id }}/virtual_currencies/transactions
  - required fields: customer_id, currency_code, amount
  - risk: creates a virtual currency transaction for a customer
- update_virtual_currencies_balance:
  - endpoint: POST /projects/{{ config.project_id }}/customers/{{ record.customer_id }}/virtual_currencies/update_balance
  - required fields: customer_id, currency_code, balance
  - risk: updates a customer virtual currency balance without creating a transaction
- create_entitlement:
  - endpoint: POST /projects/{{ config.project_id }}/entitlements
  - required fields: lookup_key, display_name
  - risk: creates an entitlement
- update_entitlement:
  - endpoint: POST /projects/{{ config.project_id }}/entitlements/{{ record.entitlement_id }}
  - required fields: entitlement_id
  - risk: updates an entitlement
- delete_entitlement:
  - endpoint: DELETE /projects/{{ config.project_id }}/entitlements/{{ record.entitlement_id }}
  - required fields: entitlement_id
  - risk: deletes an entitlement
- archive_entitlement:
  - endpoint: POST /projects/{{ config.project_id }}/entitlements/{{ record.entitlement_id }}/actions/archive
  - required fields: entitlement_id
  - risk: archives an entitlement
- unarchive_entitlement:
  - endpoint: POST /projects/{{ config.project_id }}/entitlements/{{ record.entitlement_id }}/actions/unarchive
  - required fields: entitlement_id
  - risk: unarchives an entitlement
- attach_products_to_entitlement:
  - endpoint: POST /projects/{{ config.project_id }}/entitlements/{{ record.entitlement_id }}/actions/attach_products
  - required fields: entitlement_id, product_ids
  - risk: attaches products to an entitlement
- detach_products_from_entitlement:
  - endpoint: POST /projects/{{ config.project_id }}/entitlements/{{ record.entitlement_id }}/actions/detach_products
  - required fields: entitlement_id, product_ids
  - risk: detaches products from an entitlement
- create_webhook_integration:
  - endpoint: POST /projects/{{ config.project_id }}/integrations/webhooks
  - required fields: name, url
  - risk: creates a webhook integration that sends events to the configured URL
- update_webhook_integration:
  - endpoint: POST /projects/{{ config.project_id }}/integrations/webhooks/{{ record.webhook_integration_id }}
  - required fields: webhook_integration_id
  - risk: updates a webhook integration
- delete_webhook_integration:
  - endpoint: DELETE /projects/{{ config.project_id }}/integrations/webhooks/{{ record.webhook_integration_id }}
  - required fields: webhook_integration_id
  - risk: deletes a webhook integration
- create_offering:
  - endpoint: POST /projects/{{ config.project_id }}/offerings
  - required fields: lookup_key, display_name
  - risk: creates an offering
- update_offering:
  - endpoint: POST /projects/{{ config.project_id }}/offerings/{{ record.offering_id }}
  - required fields: offering_id
  - risk: updates an offering
- delete_offering:
  - endpoint: DELETE /projects/{{ config.project_id }}/offerings/{{ record.offering_id }}
  - required fields: offering_id
  - risk: deletes an offering and attached packages
- archive_offering:
  - endpoint: POST /projects/{{ config.project_id }}/offerings/{{ record.offering_id }}/actions/archive
  - required fields: offering_id
  - risk: archives an offering
- unarchive_offering:
  - endpoint: POST /projects/{{ config.project_id }}/offerings/{{ record.offering_id }}/actions/unarchive
  - required fields: offering_id
  - risk: unarchives an offering
- create_package:
  - endpoint: POST /projects/{{ config.project_id }}/offerings/{{ record.offering_id }}/packages
  - required fields: offering_id, lookup_key, display_name
  - risk: creates a package in an offering
- update_package:
  - endpoint: POST /projects/{{ config.project_id }}/packages/{{ record.package_id }}
  - required fields: package_id
  - risk: updates a package
- delete_package:
  - endpoint: DELETE /projects/{{ config.project_id }}/packages/{{ record.package_id }}
  - required fields: package_id
  - risk: deletes a package
- attach_products_to_package:
  - endpoint: POST /projects/{{ config.project_id }}/packages/{{ record.package_id }}/actions/attach_products
  - required fields: package_id, product_ids
  - risk: attaches products to a package
- detach_products_from_package:
  - endpoint: POST /projects/{{ config.project_id }}/packages/{{ record.package_id }}/actions/detach_products
  - required fields: package_id, product_ids
  - risk: detaches products from a package
- create_paywall:
  - endpoint: POST /projects/{{ config.project_id }}/paywalls
  - required fields: name, offering_id
  - risk: creates a paywall
- update_paywall:
  - endpoint: PATCH /projects/{{ config.project_id }}/paywalls/{{ record.paywall_id }}
  - required fields: paywall_id
  - risk: updates a paywall draft
- delete_paywall:
  - endpoint: DELETE /projects/{{ config.project_id }}/paywalls/{{ record.paywall_id }}
  - required fields: paywall_id
  - risk: deletes a paywall
- create_paywall_version:
  - endpoint: POST /projects/{{ config.project_id }}/paywalls/{{ record.paywall_id }}/versions
  - required fields: paywall_id
  - risk: creates a paywall version
- create_product:
  - endpoint: POST /projects/{{ config.project_id }}/products
  - required fields: store_identifier, type, app_id, display_name
  - risk: creates a product
- update_product:
  - endpoint: POST /projects/{{ config.project_id }}/products/{{ record.product_id }}
  - required fields: product_id
  - risk: updates a product
- delete_product:
  - endpoint: DELETE /projects/{{ config.project_id }}/products/{{ record.product_id }}
  - required fields: product_id
  - risk: deletes a product
- archive_product:
  - endpoint: POST /projects/{{ config.project_id }}/products/{{ record.product_id }}/actions/archive
  - required fields: product_id
  - risk: archives a product
- unarchive_product:
  - endpoint: POST /projects/{{ config.project_id }}/products/{{ record.product_id }}/actions/unarchive
  - required fields: product_id
  - risk: unarchives a product
- create_product_in_store:
  - endpoint: POST /projects/{{ config.project_id }}/products/{{ record.product_id }}/create_in_store
  - required fields: product_id
  - risk: pushes a product to the configured store
- refund_purchase:
  - endpoint: POST /projects/{{ config.project_id }}/purchases/{{ record.purchase_id }}/actions/refund
  - required fields: purchase_id
  - risk: refunds a Web Billing purchase
- cancel_subscription:
  - endpoint: POST /projects/{{ config.project_id }}/subscriptions/{{ record.subscription_id }}/actions/cancel
  - required fields: subscription_id
  - risk: cancels an active Web Billing subscription
- extend_subscription:
  - endpoint: POST /projects/{{ config.project_id }}/subscriptions/{{ record.subscription_id }}/actions/extend
  - required fields: subscription_id
  - risk: extends the current billing period of a subscription
- refund_subscription:
  - endpoint: POST /projects/{{ config.project_id }}/subscriptions/{{ record.subscription_id }}/actions/refund
  - required fields: subscription_id
  - risk: refunds an active Web Billing subscription
- refund_subscription_transaction:
  - endpoint: POST /projects/{{ config.project_id }}/subscriptions/{{ record.subscription_id }}/transactions/{{ record.transaction_id }}/actions/refund
  - required fields: subscription_id, transaction_id
  - risk: refunds a Play Store or Galaxy subscription transaction
- create_virtual_currency:
  - endpoint: POST /projects/{{ config.project_id }}/virtual_currencies
  - required fields: code, name
  - risk: creates a virtual currency
- update_virtual_currency:
  - endpoint: POST /projects/{{ config.project_id }}/virtual_currencies/{{ record.virtual_currency_code }}
  - required fields: virtual_currency_code
  - risk: updates a virtual currency
- delete_virtual_currency:
  - endpoint: DELETE /projects/{{ config.project_id }}/virtual_currencies/{{ record.virtual_currency_code }}
  - required fields: virtual_currency_code
  - risk: deletes a virtual currency
- archive_virtual_currency:
  - endpoint: POST /projects/{{ config.project_id }}/virtual_currencies/{{ record.virtual_currency_code }}/actions/archive
  - required fields: virtual_currency_code
  - risk: archives a virtual currency
- unarchive_virtual_currency:
  - endpoint: POST /projects/{{ config.project_id }}/virtual_currencies/{{ record.virtual_currency_code }}/actions/unarchive
  - required fields: virtual_currency_code
  - risk: unarchives a virtual currency

## Security

- read risk: external RevenueCat API reads of project, app, product, offering, customer, subscription, purchase, paywall, virtual currency, integration, metrics, and audit-log data
- write risk: external RevenueCat API mutations that create, update, archive, delete, refund, cancel, transfer, grant, revoke, or otherwise alter project and customer resources
- approval: reverse ETL writes require plan preview and approval token; destructive deletes/refunds/cancellations are flagged as destructive
- Never pass secret values in chat, shell arguments, logs, docs, or JSON output.

## Command Surface

- Run RevenueCat's declared streams and reverse-ETL actions.
- Usage: pm revenuecat <command> [flags]
- Read streams
- Reverse ETL writes
- Other Commands
  - api get projects project-id customers customer-id events - Documented GET /projects/{project_id}/customers/{customer_id}/events (not implemented) [intent=direct_read availability=not_implemented operation=revenuecat.get.projects-project-id-customers-customer-id-events]; approval: not implemented: the direct-read executor has no non-redacting output policy for this provider operation; risk: low; notes: named_dependency=engine.direct_read_executor: the direct-read executor has no non-redacting output policy for this provider operation; flags: --page, --page-cursor
  - api get projects project-id customers customer-id invoices invoice-id file - Documented GET /projects/{project_id}/customers/{customer_id}/invoices/{invoice_id}/file (not implemented) [intent=direct_read availability=not_implemented operation=revenuecat.get.projects-project-id-customers-customer-id-invoices-invoice-id-file]; approval: not implemented: the direct-read executor has no non-redacting output policy for this provider operation; risk: medium; notes: named_dependency=engine.direct_read_executor: the direct-read executor has no non-redacting output policy for this provider operation; flags: --page, --page-cursor
  - api get projects project-id paywalls paywall-id versions version-id - Documented GET /projects/{project_id}/paywalls/{paywall_id}/versions/{version_id} (not implemented) [intent=direct_read availability=not_implemented operation=revenuecat.get.projects-project-id-paywalls-paywall-id-versions-version-id]; approval: not implemented: the direct-read executor has no non-redacting output policy for this provider operation; risk: low; notes: named_dependency=engine.direct_read_executor: the direct-read executor has no non-redacting output policy for this provider operation; flags: --page, --page-cursor
  - api post projects project-id media-assets - Documented POST /projects/{project_id}/media_assets (not implemented) [intent=direct_write availability=not_implemented operation=revenuecat.post.projects-project-id-media-assets]; approval: not implemented: the REST write executor lacks a reviewed operation-specific body, risk, approval, and execution contract; risk: high; notes: named_dependency=engine.rest_write_operation_contract: the REST write executor lacks a reviewed operation-specific body, risk, approval, and execution contract
  - api post projects project-id paywalls paywall-id actions publish - Documented POST /projects/{project_id}/paywalls/{paywall_id}/actions/publish (not implemented) [intent=direct_write availability=not_implemented operation=revenuecat.post.projects-project-id-paywalls-paywall-id-actions-publish]; approval: not implemented: the REST write executor lacks a reviewed operation-specific body, risk, approval, and execution contract; risk: high; notes: named_dependency=engine.rest_write_operation_contract: the REST write executor lacks a reviewed operation-specific body, risk, approval, and execution contract
  - api post projects project-id paywalls paywall-id actions unpublish - Documented POST /projects/{project_id}/paywalls/{paywall_id}/actions/unpublish (not implemented) [intent=direct_write availability=not_implemented operation=revenuecat.post.projects-project-id-paywalls-paywall-id-actions-unpublish]; approval: not implemented: the REST write executor lacks a reviewed operation-specific body, risk, approval, and execution contract; risk: high; notes: named_dependency=engine.rest_write_operation_contract: the REST write executor lacks a reviewed operation-specific body, risk, approval, and execution contract
  - app list - Run the app ETL stream [intent=etl availability=implemented stream=app]
  - app public api keys list - Run the app public api keys ETL stream [intent=etl availability=implemented stream=app_public_api_keys]
  - app store kit config list - Run the app store kit config ETL stream [intent=etl availability=implemented stream=app_store_kit_config]
  - apps list - Run the apps ETL stream [intent=etl availability=implemented stream=apps]
  - archive entitlement apply - Plan and execute the archive entitlement reverse-ETL action [intent=reverse_etl availability=not_implemented write=archive_entitlement]; approval: requires plan, preview, approval, and execute; risk: archives an entitlement; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - archive offering apply - Plan and execute the archive offering reverse-ETL action [intent=reverse_etl availability=not_implemented write=archive_offering]; approval: requires plan, preview, approval, and execute; risk: archives an offering; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - archive product apply - Plan and execute the archive product reverse-ETL action [intent=reverse_etl availability=not_implemented write=archive_product]; approval: requires plan, preview, approval, and execute; risk: archives a product; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - archive virtual currency apply - Plan and execute the archive virtual currency reverse-ETL action [intent=reverse_etl availability=not_implemented write=archive_virtual_currency]; approval: requires plan, preview, approval, and execute; risk: archives a virtual currency; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - assign customer offering apply - Plan and execute the assign customer offering reverse-ETL action [intent=reverse_etl availability=not_implemented write=assign_customer_offering]; approval: requires plan, preview, approval, and execute; risk: assigns or clears a customer offering override; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - attach products to entitlement apply - Plan and execute the attach products to entitlement reverse-ETL action [intent=reverse_etl availability=not_implemented write=attach_products_to_entitlement]; approval: requires plan, preview, approval, and execute; risk: attaches products to an entitlement; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - attach products to package apply - Plan and execute the attach products to package reverse-ETL action [intent=reverse_etl availability=not_implemented write=attach_products_to_package]; approval: requires plan, preview, approval, and execute; risk: attaches products to a package; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - audit logs list - Run the audit logs ETL stream [intent=etl availability=implemented stream=audit_logs]
  - cancel subscription apply - Plan and execute the cancel subscription reverse-ETL action [intent=reverse_etl availability=not_implemented write=cancel_subscription]; approval: requires plan, preview, approval, and execute; risk: cancels an active Web Billing subscription; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - chart data list - Run the chart data ETL stream [intent=etl availability=implemented stream=chart_data]
  - chart options list - Run the chart options ETL stream [intent=etl availability=implemented stream=chart_options]
  - collaborators list - Run the collaborators ETL stream [intent=etl availability=implemented stream=collaborators]
  - create app apply - Plan and execute the create app reverse-ETL action [intent=reverse_etl availability=not_implemented write=create_app]; approval: requires plan, preview, approval, and execute; risk: creates a RevenueCat app in the configured project; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - create customer apply - Plan and execute the create customer reverse-ETL action [intent=reverse_etl availability=not_implemented write=create_customer]; approval: requires plan, preview, approval, and execute; risk: creates a RevenueCat customer; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - create entitlement apply - Plan and execute the create entitlement reverse-ETL action [intent=reverse_etl availability=not_implemented write=create_entitlement]; approval: requires plan, preview, approval, and execute; risk: creates an entitlement; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - create offering apply - Plan and execute the create offering reverse-ETL action [intent=reverse_etl availability=not_implemented write=create_offering]; approval: requires plan, preview, approval, and execute; risk: creates an offering; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - create package apply - Plan and execute the create package reverse-ETL action [intent=reverse_etl availability=not_implemented write=create_package]; approval: requires plan, preview, approval, and execute; risk: creates a package in an offering; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - create paywall apply - Plan and execute the create paywall reverse-ETL action [intent=reverse_etl availability=not_implemented write=create_paywall]; approval: requires plan, preview, approval, and execute; risk: creates a paywall; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - create paywall version apply - Plan and execute the create paywall version reverse-ETL action [intent=reverse_etl availability=not_implemented write=create_paywall_version]; approval: requires plan, preview, approval, and execute; risk: creates a paywall version; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - create product apply - Plan and execute the create product reverse-ETL action [intent=reverse_etl availability=not_implemented write=create_product]; approval: requires plan, preview, approval, and execute; risk: creates a product; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - create product in store apply - Plan and execute the create product in store reverse-ETL action [intent=reverse_etl availability=not_implemented write=create_product_in_store]; approval: requires plan, preview, approval, and execute; risk: pushes a product to the configured store; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - create project apply - Plan and execute the create project reverse-ETL action [intent=reverse_etl availability=not_implemented write=create_project]; approval: requires plan, preview, approval, and execute; risk: creates a RevenueCat project; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - create virtual currencies transaction apply - Plan and execute the create virtual currencies transaction reverse-ETL action [intent=reverse_etl availability=not_implemented write=create_virtual_currencies_transaction]; approval: requires plan, preview, approval, and execute; risk: creates a virtual currency transaction for a customer; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - create virtual currency apply - Plan and execute the create virtual currency reverse-ETL action [intent=reverse_etl availability=not_implemented write=create_virtual_currency]; approval: requires plan, preview, approval, and execute; risk: creates a virtual currency; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - create webhook integration apply - Plan and execute the create webhook integration reverse-ETL action [intent=reverse_etl availability=not_implemented write=create_webhook_integration]; approval: requires plan, preview, approval, and execute; risk: creates a webhook integration that sends events to the configured URL; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - customer active entitlements list - Run the customer active entitlements ETL stream [intent=etl availability=implemented stream=customer_active_entitlements]
  - customer aliases list - Run the customer aliases ETL stream [intent=etl availability=implemented stream=customer_aliases]
  - customer attributes list - Run the customer attributes ETL stream [intent=etl availability=implemented stream=customer_attributes]
  - customer center list - Run the customer center ETL stream [intent=etl availability=implemented stream=customer_center]
  - customer invoices list - Run the customer invoices ETL stream [intent=etl availability=implemented stream=customer_invoices]
  - customer list - Run the customer ETL stream [intent=etl availability=implemented stream=customer]
  - customer purchases list - Run the customer purchases ETL stream [intent=etl availability=implemented stream=customer_purchases]
  - customer subscriptions list - Run the customer subscriptions ETL stream [intent=etl availability=implemented stream=customer_subscriptions]
  - customer virtual currencies list - Run the customer virtual currencies ETL stream [intent=etl availability=implemented stream=customer_virtual_currencies]
  - customers list - Run the customers ETL stream [intent=etl availability=implemented stream=customers]
  - delete app apply - Plan and execute the delete app reverse-ETL action [intent=reverse_etl availability=not_implemented write=delete_app]; approval: requires plan, preview, approval, and execute; risk: permanently deletes a RevenueCat app and its configuration; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - delete customer apply - Plan and execute the delete customer reverse-ETL action [intent=reverse_etl availability=not_implemented write=delete_customer]; approval: requires plan, preview, approval, and execute; risk: permanently deletes a RevenueCat customer; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - delete entitlement apply - Plan and execute the delete entitlement reverse-ETL action [intent=reverse_etl availability=not_implemented write=delete_entitlement]; approval: requires plan, preview, approval, and execute; risk: deletes an entitlement; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - delete offering apply - Plan and execute the delete offering reverse-ETL action [intent=reverse_etl availability=not_implemented write=delete_offering]; approval: requires plan, preview, approval, and execute; risk: deletes an offering and attached packages; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - delete package apply - Plan and execute the delete package reverse-ETL action [intent=reverse_etl availability=not_implemented write=delete_package]; approval: requires plan, preview, approval, and execute; risk: deletes a package; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - delete paywall apply - Plan and execute the delete paywall reverse-ETL action [intent=reverse_etl availability=not_implemented write=delete_paywall]; approval: requires plan, preview, approval, and execute; risk: deletes a paywall; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - delete product apply - Plan and execute the delete product reverse-ETL action [intent=reverse_etl availability=not_implemented write=delete_product]; approval: requires plan, preview, approval, and execute; risk: deletes a product; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - delete virtual currency apply - Plan and execute the delete virtual currency reverse-ETL action [intent=reverse_etl availability=not_implemented write=delete_virtual_currency]; approval: requires plan, preview, approval, and execute; risk: deletes a virtual currency; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - delete webhook integration apply - Plan and execute the delete webhook integration reverse-ETL action [intent=reverse_etl availability=not_implemented write=delete_webhook_integration]; approval: requires plan, preview, approval, and execute; risk: deletes a webhook integration; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - detach products from entitlement apply - Plan and execute the detach products from entitlement reverse-ETL action [intent=reverse_etl availability=not_implemented write=detach_products_from_entitlement]; approval: requires plan, preview, approval, and execute; risk: detaches products from an entitlement; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - detach products from package apply - Plan and execute the detach products from package reverse-ETL action [intent=reverse_etl availability=not_implemented write=detach_products_from_package]; approval: requires plan, preview, approval, and execute; risk: detaches products from a package; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - entitlement list - Run the entitlement ETL stream [intent=etl availability=implemented stream=entitlement]
  - entitlement products list - Run the entitlement products ETL stream [intent=etl availability=implemented stream=entitlement_products]
  - entitlements list - Run the entitlements ETL stream [intent=etl availability=implemented stream=entitlements]
  - extend subscription apply - Plan and execute the extend subscription reverse-ETL action [intent=reverse_etl availability=not_implemented write=extend_subscription]; approval: requires plan, preview, approval, and execute; risk: extends the current billing period of a subscription; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - grant customer entitlement apply - Plan and execute the grant customer entitlement reverse-ETL action [intent=reverse_etl availability=not_implemented write=grant_customer_entitlement]; approval: requires plan, preview, approval, and execute; risk: grants an entitlement to a customer; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - offering list - Run the offering ETL stream [intent=etl availability=implemented stream=offering]
  - offering packages list - Run the offering packages ETL stream [intent=etl availability=implemented stream=offering_packages]
  - offerings list - Run the offerings ETL stream [intent=etl availability=implemented stream=offerings]
  - overview metrics list - Run the overview metrics ETL stream [intent=etl availability=implemented stream=overview_metrics]
  - package list - Run the package ETL stream [intent=etl availability=implemented stream=package]
  - package products list - Run the package products ETL stream [intent=etl availability=implemented stream=package_products]
  - paywall list - Run the paywall ETL stream [intent=etl availability=implemented stream=paywall]
  - paywalls list - Run the paywalls ETL stream [intent=etl availability=implemented stream=paywalls]
  - product list - Run the product ETL stream [intent=etl availability=implemented stream=product]
  - products list - Run the products ETL stream [intent=etl availability=implemented stream=products]
  - projects list - Run the projects ETL stream [intent=etl availability=implemented stream=projects]
  - purchase entitlements list - Run the purchase entitlements ETL stream [intent=etl availability=implemented stream=purchase_entitlements]
  - purchase list - Run the purchase ETL stream [intent=etl availability=implemented stream=purchase]
  - purchases list - Run the purchases ETL stream [intent=etl availability=implemented stream=purchases]
  - refund purchase apply - Plan and execute the refund purchase reverse-ETL action [intent=reverse_etl availability=not_implemented write=refund_purchase]; approval: requires plan, preview, approval, and execute; risk: refunds a Web Billing purchase; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - refund subscription apply - Plan and execute the refund subscription reverse-ETL action [intent=reverse_etl availability=not_implemented write=refund_subscription]; approval: requires plan, preview, approval, and execute; risk: refunds an active Web Billing subscription; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - refund subscription transaction apply - Plan and execute the refund subscription transaction reverse-ETL action [intent=reverse_etl availability=not_implemented write=refund_subscription_transaction]; approval: requires plan, preview, approval, and execute; risk: refunds a Play Store or Galaxy subscription transaction; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - restore purchase by order id apply - Plan and execute the restore purchase by order id reverse-ETL action [intent=reverse_etl availability=not_implemented write=restore_purchase_by_order_id]; approval: requires plan, preview, approval, and execute; risk: restores a Google Play purchase by order id; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - revenue metric list - Run the revenue metric ETL stream [intent=etl availability=implemented stream=revenue_metric]
  - revoke customer granted entitlement apply - Plan and execute the revoke customer granted entitlement reverse-ETL action [intent=reverse_etl availability=not_implemented write=revoke_customer_granted_entitlement]; approval: requires plan, preview, approval, and execute; risk: revokes a granted customer entitlement; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - set customer attributes apply - Plan and execute the set customer attributes reverse-ETL action [intent=reverse_etl availability=not_implemented write=set_customer_attributes]; approval: requires plan, preview, approval, and execute; risk: sets customer attributes; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - subscription authenticated management url list - Run the subscription authenticated management url ETL stream [intent=etl availability=implemented stream=subscription_authenticated_management_url]
  - subscription entitlements list - Run the subscription entitlements ETL stream [intent=etl availability=implemented stream=subscription_entitlements]
  - subscription list - Run the subscription ETL stream [intent=etl availability=implemented stream=subscription]
  - subscription transactions list - Run the subscription transactions ETL stream [intent=etl availability=implemented stream=subscription_transactions]
  - subscriptions list - Run the subscriptions ETL stream [intent=etl availability=implemented stream=subscriptions]
  - transfer customer data apply - Plan and execute the transfer customer data reverse-ETL action [intent=reverse_etl availability=not_implemented write=transfer_customer_data]; approval: requires plan, preview, approval, and execute; risk: transfers subscriptions and purchases to another customer; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - unarchive entitlement apply - Plan and execute the unarchive entitlement reverse-ETL action [intent=reverse_etl availability=not_implemented write=unarchive_entitlement]; approval: requires plan, preview, approval, and execute; risk: unarchives an entitlement; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - unarchive offering apply - Plan and execute the unarchive offering reverse-ETL action [intent=reverse_etl availability=not_implemented write=unarchive_offering]; approval: requires plan, preview, approval, and execute; risk: unarchives an offering; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - unarchive product apply - Plan and execute the unarchive product reverse-ETL action [intent=reverse_etl availability=not_implemented write=unarchive_product]; approval: requires plan, preview, approval, and execute; risk: unarchives a product; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - unarchive virtual currency apply - Plan and execute the unarchive virtual currency reverse-ETL action [intent=reverse_etl availability=not_implemented write=unarchive_virtual_currency]; approval: requires plan, preview, approval, and execute; risk: unarchives a virtual currency; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - update app apply - Plan and execute the update app reverse-ETL action [intent=reverse_etl availability=not_implemented write=update_app]; approval: requires plan, preview, approval, and execute; risk: updates an existing RevenueCat app; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - update entitlement apply - Plan and execute the update entitlement reverse-ETL action [intent=reverse_etl availability=not_implemented write=update_entitlement]; approval: requires plan, preview, approval, and execute; risk: updates an entitlement; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - update offering apply - Plan and execute the update offering reverse-ETL action [intent=reverse_etl availability=not_implemented write=update_offering]; approval: requires plan, preview, approval, and execute; risk: updates an offering; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - update package apply - Plan and execute the update package reverse-ETL action [intent=reverse_etl availability=not_implemented write=update_package]; approval: requires plan, preview, approval, and execute; risk: updates a package; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - update paywall apply - Plan and execute the update paywall reverse-ETL action [intent=reverse_etl availability=not_implemented write=update_paywall]; approval: requires plan, preview, approval, and execute; risk: updates a paywall draft; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - update product apply - Plan and execute the update product reverse-ETL action [intent=reverse_etl availability=not_implemented write=update_product]; approval: requires plan, preview, approval, and execute; risk: updates a product; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - update virtual currencies balance apply - Plan and execute the update virtual currencies balance reverse-ETL action [intent=reverse_etl availability=not_implemented write=update_virtual_currencies_balance]; approval: requires plan, preview, approval, and execute; risk: updates a customer virtual currency balance without creating a transaction; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - update virtual currency apply - Plan and execute the update virtual currency reverse-ETL action [intent=reverse_etl availability=not_implemented write=update_virtual_currency]; approval: requires plan, preview, approval, and execute; risk: updates a virtual currency; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - update webhook integration apply - Plan and execute the update webhook integration reverse-ETL action [intent=reverse_etl availability=not_implemented write=update_webhook_integration]; approval: requires plan, preview, approval, and execute; risk: updates a webhook integration; notes: named_dependency=engine.reverse_etl_scalar_flag_contract: the reverse-ETL command surface cannot faithfully expose this action's required object or array record fields as scalar flags
  - virtual currencies list - Run the virtual currencies ETL stream [intent=etl availability=implemented stream=virtual_currencies]
  - virtual currency list - Run the virtual currency ETL stream [intent=etl availability=implemented stream=virtual_currency]
  - webhook integration list - Run the webhook integration ETL stream [intent=etl availability=implemented stream=webhook_integration]
  - webhook integrations list - Run the webhook integrations ETL stream [intent=etl availability=implemented stream=webhook_integrations]

## Commands

### Inspect as a manual

```bash
pm connectors inspect revenuecat
```

### Inspect as structured JSON

```bash
pm connectors inspect revenuecat --json
```

## Agent Rules

- Run pm connectors inspect revenuecat before creating credentials or plans.
- Use --json only when the caller needs structured output; use the manual for human-readable guidance.
- Never ask the user to paste secret values into chat.
- For reverse ETL writes, create a plan, show the preview, wait for explicit approval, then run with the approval token.
