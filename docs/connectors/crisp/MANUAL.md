# pm connectors inspect crisp

```text
NAME
  pm connectors inspect crisp - Crisp connector manual

SYNOPSIS
  pm connectors inspect crisp
  pm connectors inspect crisp --json
  pm credentials add <name> --connector crisp [--config key=value] [--from-env field=ENV] [--value-stdin field]

DESCRIPTION
  Inventory scaffold for the Crisp REST API v1 connector. The official 220-operation surface is recorded in api_surface.json and cli_surface.json; executable streams, direct reads, binary policy, and reverse-ETL writes land in follow-up #204 subissues before this parent is ready.

ICON
  asset: icons/pm-sample.svg
  source: polymetrics
  review_status: polymetrics
  review_url: https://github.com/polymetrics-ai/cli

CAPABILITIES
  check=true catalog=true read=false write=false query=false
  Integration type: api

AUTHENTICATION
  Use pm credentials add with --from-env or --value-stdin for secret fields.

CONFIGURATION
  base_url
  max_pages
  page_size
  start_date
  website_id
  identifier (secret)
  key (secret)

SECURITY
  read risk: Crisp REST API reads may include website visitor, conversation, people, campaign, and helpdesk data; executable reads are not exposed by #205.
  write risk: Crisp REST API mutations can create, update, delete, import, export, or administer website resources; all writes remain blocked until typed reverse-ETL actions and approvals are declared.
  approval: Future reverse ETL writes require plan, preview, approval token, and typed confirmation for destructive/admin operations.
  Never pass secret values in chat, shell arguments, logs, docs, or JSON output.

COMMAND SURFACE
  Work with Crisp websites, conversations, people, helpdesk, campaigns, plugins, and subscription metadata from the command line.
  Usage: pm crisp <command> [flags]
  Source CLI: Crisp REST API (https://docs.crisp.chat/references/rest-api/v1/)
  Global flags:
    --json (boolean): Write machine-readable JSON output.
    --connection (string): Use a saved Crisp connector credential and website scope.: maps_to=connection
  Website And Settings
  Conversations, Messages, And Calls
  People, Events, Segments, And Campaign Links
  Helpdesk And Articles
  Campaigns
  Plugins, Connect, Plans, Bucket, And Media
  Other Commands
    create website - Creates a new website. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    get website - Resolves an existing website information. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    delete website - Deletes an existing website. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    abort website deletion - Aborts scheduled deletion for an existing website. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    list conversations - Lists conversations for website. [intent=etl availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    list suggested conversation segments - Lists suggested conversation segments for website. [intent=etl availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    delete suggested conversation segment - Deletes a suggested conversation segment for website. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    list suggested conversation data keys - Lists suggested conversation data keys for website. [intent=etl availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    delete suggested conversation data key - Deletes a suggested conversation data key for website. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    list spam conversations - Lists spam conversations in website. Those are not conversation entities, but can be converted to conversations if a spam is later accepted as ham. [intent=etl availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    resolve spam conversation content - Resolves full content for spam conversation. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    submit spam conversation decision - Submits decision on spam conversation (this allows to accepts or rejects a spam). If rejected, a spam message will be gone forever. If accepted, it will get transformed into a regular conversation. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    create new conversation - Creates a new conversation. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    get conversation - Resolves conversation information. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    remove conversation - Removes a conversation in website. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    initiate conversation existing session - Initiates a conversation from an existing session. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    get messages conversation - Resolves messages in an existing conversation. Returns the last batch of messages if there are many messages in the conversation. Then, messages can be paged towards older messages using the timestamp_before parameter, towards more recent messages using the timestamp_after parameter, or centered around a given timestamp using the timestamp_around parameter. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    send message conversation - Sends a message in an existing conversation. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    get message conversation - Resolves an existing message in an existing conversation. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    update message conversation - Edits an existing message in an existing conversation. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    remove message conversation - Removes an existing message in an existing conversation. Used to retract a previously-sent message. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    compose message conversation - Starts or stop composing a message in an existing conversation. This route is used for MagicType preview. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    mark messages as read conversation - Marks messages as read in conversation. Either using given message fingerprints, or all messages. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    mark conversation as unread - Marks whole conversation as unread. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    mark messages as delivered conversation - Marks messages as delivered in conversation. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    update conversation open state - Updates conversation open state for authenticated operator user. Lets other operators see which conversation people are on at any given moment. This state automatically expires after a while, if not renewed. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    get conversation routing assign - Resolves assigned operator for conversation routing. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    assign conversation routing - Assigns conversation routing to an operator, or unassign. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    update conversation inbox - Updates inbox used for conversation. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    get conversation metas - Resolves conversation meta information. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    update conversation metas - Updates conversation meta information. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    get original message conversation - Resolves an existing original message in an existing conversation. This route is mainly used to retrieve original HTML content and headers for emails. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    list conversation pages - Lists browsed pages in conversation. [intent=etl availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    list conversation events - Lists stacked events in conversation. [intent=etl availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    list conversation files - Lists files in conversation (extracted from messages). [intent=etl availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    get conversation state - Resolves conversation state (resolved, unresolved or pending). [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    change conversation state - Updates conversation state (resolved, unresolved or pending). [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    get conversation relations - Resolves conversation relations (related conversations: parents and children, if any). [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    get conversation participants - Resolves conversation participants (external participants, eg. emails to be set in CC). [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    save conversation participants - Saves conversation participants (external participants, eg. emails to be set in CC). [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    get block status conversation - Resolves conversation block status. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    block incoming messages conversation - Blocks further incoming messages from a conversation. Messages from visitor will be silently dropped, but you can still send messages to visitor. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    get verify status conversation - Resolves conversation verify status. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    update verify status conversation - Updates conversation verify status. When a conversation user is verified, a verified badge shows in the Crisp apps. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    request identity verification conversation - Requests a verification of an identity channel for conversation. Verifying a channel automatically marks the whole conversation as verified, showing a verified badge in the Crisp apps. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    redeem identity verification link conversation - Redeems a verification link to verify an identity channel for conversation. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    request email transcript conversation - Requests an email transcript for a conversation. The transcript is emailed to the authenticated user, or to the indicated email. It contains all conversation history and may be kept for as an external record. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    request chatbox binding purge conversation - Requests a chatbox binding purge for conversation. The visitor must be online for the purge to be proceeded. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    request user feedback conversation - Requests feedback from user for conversation. The visitor must be online for the feedback request to be delivered. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    list browsing sessions conversation - Lists available browsing sessions for conversation. A browsing session can be initiated to stream user screen content to operators. [intent=etl availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    initiate browsing session conversation - Initiates browsing session for conversation. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    send action existing browsing session - Sends an action to an existing browsing session. Used to send stream actions to a browsing session (eg. to start or stop the stream session). [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    assist existing browsing session - Assists an existing browsing session. Used by LiveAssist™ to start or stop an assist session, used by operators to take control on the screen of user browser (mouse move and scroll). [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    initiate new call session conversation - Initiates a new audio/video call session for conversation. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    get ongoing call session conversation - Gets the ongoing audio/video call session for conversation. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    abort ongoing call session conversation - Aborts the ongoing audio/video call session for conversation. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    transmit signaling ongoing call session - Transmits a signaling payload for the ongoing audio/video call session for conversation. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    request tool call conversation - Requests a new WebMCP tool call on the chatbox widget for conversation. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    deliver widget button action conversation - Delivers a button action on plugin widget for conversation, used to perform an action on the plugin, eg. refund a user. A hook will be sent to the plugin widget receiver. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    deliver widget data action conversation - Delivers a data action on plugin widget for conversation, used to either fetch or edit a data value from the plugin, eg. fetch a user unpaid balance. A hook will be sent to the plugin widget receiver. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    schedule reminder conversation - Schedules a reminder in the future for conversation, with a note to your future-self. A push notification and an email will be sent, as well as a note in the conversation. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    report conversation - Reports a conversation to Crisp and remove it from the inbox. This can be used to move an entire conversation to spams. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    get people statistics - Resolves the amount of people stored in the CRM. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    list suggested people segments - Lists suggested segments for people. [intent=etl availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    delete suggested people segment - Deletes a suggested segment for people. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    list suggested people data keys - Lists suggested data keys for people. [intent=etl availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    delete suggested people data key - Deletes a suggested data key for people. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    list suggested people events - Lists suggested events for people. [intent=etl availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    delete suggested people event - Deletes a suggested event for people. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    list people profiles - Lists people profiles for website. [intent=etl availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    add new people profile - Adds a new people profile. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    get people profile - Resolves people profile. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    save people profile - Saves people profile, and overwrite all previous information. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    update people profile - Updates people profile, and save only changed fields on the previous profile revision. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    remove people profile - Removes people profile in website. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    list people conversations - Lists conversations linked to people. [intent=etl availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    list people campaigns - Lists campaigns linked to people. [intent=etl availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    add people event - Stacks an event for people. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    list people events - Lists stacked events for people. [intent=etl availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    get people data - Gets stored data for people. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    save people data - Saves stored data for people. This replaces any already-set data with the new data keys. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    update people data - Updates stored data for people. This merges the new data keys with already-set data. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    get people subscription status - Resolves subscription status for people (subscribed or unsubscribed). [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    update people subscription status - Updates current subscription status for people (subscribe or unsubscribe). [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    export people profiles - Exports people profiles. The exported data is sent back to email to the requester user. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned file/export/import command only. #210 must declare bounded binary/file policy before implementation.
    import people profiles - Imports people profiles. The imported data, eg. a CSV file containing user emails and full names is used to populate website user database. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned file/export/import command only. #210 must declare bounded binary/file policy before implementation.
    count visitors - Counts visitors currently on website. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    list visitors - Lists visitors currently on website. [intent=etl availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    pinpoint visitors map - Maps visitors in a geographical area, given a geographical center and a map radius (defaults to whole Earth if none given). Visitors are grouped in geographical points. There is a static number of points per area (the area is composed of sub-divisions of equal size). Only points which contain visitors are returned. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    get session identifier token - Transforms a token to a session identifier, if any session is bound to the token. The token can be bound to a session from the Crisp chatbox. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    count blocked visitors - Counts all blocked visitors on website, by rule list. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    count blocked visitors rule - Counts all blocked visitors in rule list on website. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    clear blocked visitors rule - Clears all blocked visitors in rule list on website. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    get website availability status - Resolves the website availability status. This tells whether the chatbox is seen as online or away by visitors. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    list website operator availabilities - Lists the availabilities for website operators. This maps the availability of each operator in the website. [intent=etl availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    list website operators - Lists all operator members of website. [intent=etl availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    list last active website operators - Lists last active website operators, ie. operators who last replied to a visitor message. [intent=etl availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    flush last active website operators - Flushes the list of last active website operators, ie. operators who last replied to a visitor message. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    send email website operators - Sends an email to target website operators. The email will be delivered to each operator account email address. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    get website operator - Resolves a given website operator. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    invite website operator - Invites an email to join website as operator. The target email doesn't need to be a valid Crisp account, since Crisp will send an invite email to that email. The receiver is then prompted to click on a link to either join the website with an existing account, or create a new account on the fly. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    change operator membership - Changes the membership of an existing operator. Useful to downgrade or upgrade an operator from/to owner role, or change their title in the team. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    unlink operator website - Unlinks given operator from website. Note that the last operator in the website cannot be unlinked. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    resolve helpdesk - Resolves helpdesk information for website. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    initialize helpdesk - Initializes a new helpdesk for website. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    delete helpdesk - Deletes helpdesk for website. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    list helpdesk locales - Lists locales for helpdesk in website. [intent=etl availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    add helpdesk locale - Adds a locale for helpdesk in website. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    resolve helpdesk locale - Resolves a locale for helpdesk in website. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    delete helpdesk locale - Deletes a locale for helpdesk in website. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    list helpdesk locale articles - Lists articles for a helpdesk locale in website. [intent=etl availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    add new helpdesk locale article - Adds a new locale article for helpdesk in website. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    resolve helpdesk locale article - Resolves a locale article for helpdesk in website. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    save helpdesk locale article - Saves a locale article for helpdesk in website. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    update helpdesk locale article - Updates a locale article for helpdesk in website. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    delete helpdesk locale article - Deletes a locale article for helpdesk in website. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    resolve helpdesk locale article page - Resolves a locale article page information for helpdesk in website. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    resolve helpdesk locale article category - Resolves a locale article category for helpdesk in website. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    update helpdesk locale article category - Updates a locale article category for helpdesk in website. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    list helpdesk locale article alternates - Lists alternate locales on a locale article for helpdesk in website. [intent=etl availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    resolve helpdesk locale article alternate - Resolves alternate locale on a locale article for helpdesk in website. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    save helpdesk locale article alternate - Saves alternate locale on a locale article for helpdesk in website. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    delete helpdesk locale article alternate - Deletes alternate locale on a locale article for helpdesk in website. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    publish helpdesk locale article - Publishes a locale article for helpdesk in website. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    unpublish helpdesk locale article - Unpublishes a locale article for helpdesk in website. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    list helpdesk locale categories - Lists locale categories for helpdesk in website. [intent=etl availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    add helpdesk locale category - Adds a locale category for helpdesk in website. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    resolve helpdesk locale category - Resolves a locale category for helpdesk in website. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    save helpdesk locale category - Saves a locale category for helpdesk in website. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    update helpdesk locale category - Updates a locale category for helpdesk in website. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    delete helpdesk locale category - Deletes a locale category for helpdesk in website. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    list helpdesk locale sections - Lists locale sections for helpdesk in website and category. [intent=etl availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    add helpdesk locale section - Adds a locale section for helpdesk in website and category. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    resolve helpdesk locale section - Resolves a locale section for helpdesk in website and category. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    save helpdesk locale section - Saves a locale section for helpdesk in website and category. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    update helpdesk locale section - Updates a locale section for helpdesk in website and category. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    delete helpdesk locale section - Deletes a locale section for helpdesk in website and category. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    map helpdesk locale feedback ratings - Map locale feedback ratings for helpdesk in website. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    list helpdesk locale feedbacks - Lists locale feedbacks for helpdesk in website. [intent=etl availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    import external helpdesk locale - Imports a whole external helpdesk to Crisp, as a Crisp Helpdesk. The import crawler pipeline is asynchronous. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned file/export/import command only. #210 must declare bounded binary/file policy before implementation.
    export helpdesk locale articles - Exports helpdesk articles for locale. The exported data is sent back to email to the requester user. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned file/export/import command only. #210 must declare bounded binary/file policy before implementation.
    list helpdesk redirections - Lists redirections for helpdesk in website. [intent=etl availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    add helpdesk redirection - Adds a redirection for helpdesk in website. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    resolve helpdesk redirection - Resolves a redirection for helpdesk in website. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    delete helpdesk redirection - Deletes a redirection for helpdesk in website. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    resolve helpdesk settings - Resolves settings for helpdesk in website. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    save helpdesk settings - Saves settings for helpdesk in website. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    resolve helpdesk domain - Resolves domain for helpdesk in website. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    request helpdesk domain change - Requests a change in the domain used for helpdesk. The change will be propagated once all setup steps are fulfilled (see the setup route). [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    generate helpdesk domain setup flow - Retrieves the domain setup flow for helpdesk. This will return the DNS records required for helpdesk domain setup. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    list campaigns - Lists campaigns for website. [intent=etl availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    list campaign tags - Lists campaign tags for website. [intent=etl availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    list campaign templates - Lists campaign templates for website. [intent=etl availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    create new campaign template - Creates a new campaign template. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    get campaign template - Resolves campaign template information. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    save campaign template - Saves a campaign template in website, and overwrite previous template information. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    update campaign template - Updates a campaign template in website, and save only changed fields. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    remove campaign template - Removes a campaign template in website. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    create new campaign - Creates a new campaign. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    get campaign - Resolves campaign information. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    save campaign - Saves a campaign in website, and overwrite previous campaign information. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    update campaign - Updates a campaign in website, and save only changed fields. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    remove campaign - Removes a campaign in website. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    dispatch campaign - Dispatches a ready campaign. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    resume campaign - Resumes a paused and dispatched campaign. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    pause campaign - Pauses a running and dispatched campaign. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    test campaign - Tests a ready campaign. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    list campaign recipients - Lists campaigns recipients on a non-dispatched one-shot campaign for website. This can be used to preview the recipients the campaign will get delivered to. [intent=etl availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    list campaign statistics - Lists campaigns statistics by type for website. This can be used to see who opened a sent campaign, clicked on a link or unsubscribed from the campaign. [intent=etl availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    generate analytics - Generates analytics for given type and metric in website. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    batch resolve items - Resolves given (or all) items in website (conversations). [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    batch unresolve items - Unresolves given items in website (conversations). [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    batch read items - Marks given (or all) items as read in website (conversations). [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    batch unread items - Marks given items as unread in website (conversations). [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    batch report items - Reports given items in website (conversations). [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    batch block items - Blocks or unblocks given items in website (conversations). [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    batch routing items - Assigns or unassigns operator for given items in website (conversations). [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    batch inbox items - Moves given items to a different inbox in website (conversations). [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    batch segments items - Assigns segments to given items in website (conversations or people). [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    batch data items - Assigns data to given items in website (conversations or people). [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    batch remove items - Removes given items in website (conversations or people). [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    list website inboxes - Lists all inboxes for website. [intent=etl availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    batch order website inboxes - Changes order for multiple matching inboxes on website. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    create new website inbox - Creates a new inbox for website. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    get website inbox - Resolves inbox for website. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    save website inbox - Saves inbox for website. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    delete website inbox - Deletes inbox for website. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    get website settings - Resolves the current settings for a website. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    update website settings - Updates the current settings for a website. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    get verify settings - Resolves verify settings. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    update verify settings - Updates verify settings. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    get verify key - Resolves verify key. This key is to be used in the user backend to sign user data (eg. email) with an HMAC-SHA256 function. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    roll verify key - Rolls verify key. This requests a new keypair to be generated. Previous key will be invalidated. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned reverse-ETL command only. #211 must declare an explicit record schema and approval gate before execution.
    get website connect endpoints - Resolves the website endpoints information. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    list all active subscriptions - Lists all active plugin subscriptions on all websites, linked to payment methods owned by the user, or from websites the user is member of. [intent=etl availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    list subscriptions website - Lists plugin subscriptions for given website. [intent=etl availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    get subscription details - Resolves details on a given subscription. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    subscribe website plugin - Subscribes a given website to a given plugin. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    unsubscribe plugin website - Unsubscribes a given plugin from a given website. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    get subscription settings - Resolves plugin subscription settings. Used to read plugin configuration on a given website. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    save subscription settings - Saves plugin subscription settings (overwrites existing settings). Used to configure a given plugin on a given website. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    update subscription settings - Updates plugin subscription settings (merges with existing settings). Used to configure a given plugin on a given website. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    get plugin usage bills - Acquires all non-settled (ie. open) usage bills for a subscribed plugin. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    report plugin usage bill - Reports a billable usage for a website using a subscribed plugin. This billable usage will be invoiced to the user whenever they get charged for other Crisp subscriptions. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    get plugin attest provenance - Attests for plugin user provenance, using the plugin signature secret. This provenance payload can be provided to the plugin eg. Web configuration UI, so that the plugin can independently authenticate the Crisp user and verify eg. their role in the website. This lets plugins implement some weak ACLs. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    forward plugin payload channel - Forwards generic payload given generic namespace to plugin channel. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    dispatch plugin event - Dispatches a generic data event for plugin, for use with Web Hooks. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    get connect account - Resolves the current plugin account information. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    list all connect websites - Lists all websites linked to connected plugin. [intent=etl availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    list connect websites since - Lists the websites linked or unlinked or updated for connected plugin, since given date. This is basically a differential that allows you to either add, remove or update websites in use for the plugin. [intent=etl availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    get connect endpoints - Resolves the current plugin endpoints information. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    list all active plan subscriptions - Lists all active plan subscriptions on all websites, linked to payment methods owned by the user, or from websites the user is member of. [intent=etl availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    get plan subscription website - Resolves plan subscription for given website. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    subscribe website plan - Subscribes a given website to a given plan. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    unsubscribe plan website - Unsubscribes a given plan from a given website. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    change bill period website plan subscription - Changes how often the website subscription is paid for. The payment method will be debited at selected interval for the due amount. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    check coupon availability website plan subscription - Resolves a coupon for a website subscription. This helps check for coupon availability and returns information on the coupon. [intent=direct_read availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
    redeem coupon website plan subscription - Redeems a coupon for a website subscription. This applies coupon rebates and upgrades immediately to the subscription. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned administrative/destructive reverse-ETL command only. #211 must require plan, preview, approval, and typed confirmation before execution.
    generate bucket url - Generates a bucket URL. The URL is signed, and lets you upload a file directly to it. [intent=reverse_etl availability=planned]; approval: reverse ETL commands must use plan → preview → approval → execute; no direct write dispatch is exposed by #205.; risk: External Crisp API mutation; exact risk tier and destructive/admin confirmation are finalized in #211 before implementation.; notes: Planned file/export/import command only. #210 must declare bounded binary/file policy before implementation.
    list animation medias - Lists animation medias, given the search query and paging. [intent=etl availability=planned]; notes: Planned read command only. #207/#209 must choose stream vs direct-read execution and declare bounded schemas/output policy before implementation.
  Help topics:
    crisp auth - Configure Crisp REST API Basic credentials from environment variables or stdin; never pass secret values in prompt text.
    crisp safety - Crisp write commands are blocked by default until typed reverse-ETL schemas, risk text, preview, approval, and confirmation gates are implemented.

EXAMPLES
  # Inspect as a manual
  pm connectors inspect crisp

  # Inspect as structured JSON
  pm connectors inspect crisp --json

AGENT WORKFLOW
  - Run pm connectors inspect crisp before creating credentials or plans.
  - Use --json only when the caller needs structured output; use the manual for human-readable guidance.
  - Never ask the user to paste secret values into chat.

EXIT STATUS
  0 success
  1 runtime error
  2 usage error

```
