---
name: pm-gong
description: Gong connector knowledge and safe action guide.
---

# pm-gong

## Purpose

Reads Gong users, calls, scorecards, settings, flows, and related public API resources; executes selected typed POST read-query commands; models Gong mutations, multipart uploads, and top-level array uploads as typed reverse-ETL actions.

## Icon

- id: gong
- asset: icons/gong.svg
- source: upstream_registry
- review_status: upstream_seeded
- review_url: https://us-66463.app.gong.io/settings/api/documentation

## Capabilities

- check=true catalog=true read=true write=true query=false
- Integration type: api

## Authentication

- Use pm credentials add with --from-env or --value-stdin for secret fields.

## Configuration

- base_url
- max_pages
- mode
- page_size
- start_date
- access_key (secret) (required)
- access_key_secret (secret) (required)

## ETL Streams

- users:
  - primary key: id
  - cursor: created
  - fields: active(boolean), created(string), email_address(string), first_name(string), id(string), last_name(string), manager_id(string), phone_number(string), title(string)
- calls:
  - primary key: id
  - cursor: started
  - fields: direction(string), duration(integer), id(string), is_private(boolean), language(string), media(string), scheduled(string), scope(string), started(string), system(string), title(string), url(string)
- scorecards:
  - primary key: scorecardId
  - cursor: updated
  - fields: created(string), enabled(boolean), scorecardId(string), scorecardName(string), updated(string), workspaceId(string)
- crm_integrations:
  - fields: created(string), id(string), name(string), title(string), updated(string)
- workspaces:
  - fields: created(string), id(string), name(string), title(string), updated(string)
- trackers:
  - fields: created(string), id(string), name(string), title(string), updated(string)
- briefs:
  - fields: created(string), id(string), name(string), title(string), updated(string)
- library_folders:
  - fields: created(string), id(string), name(string), title(string), updated(string)
- flows:
  - fields: created(string), id(string), name(string), title(string), updated(string)
- flow_folders:
  - fields: created(string), id(string), name(string), title(string), updated(string)
- call_outcomes:
  - fields: created(string), id(string), name(string), title(string), updated(string)
- permission_profiles:
  - fields: created(string), id(string), name(string), title(string), updated(string)

## Sync Modes

- ETL sync modes: full_refresh_append, full_refresh_overwrite, full_refresh_overwrite_deduped, incremental_append, incremental_append_deduped

## Reverse ETL Actions

- add_call:
  - endpoint: POST /calls
  - required fields: actualStart, clientUniqueId, direction, parties, primaryUser
  - risk: high: sends call, engagement, or digital interaction content to Gong; requires reverse ETL approval
- create_permission_profile:
  - endpoint: POST /permission-profile?workspaceId={{ record.workspaceId }}
  - required fields: workspaceId
  - risk: high: administrative Gong settings or permissions mutation; requires reverse ETL approval and destructive confirmation
- update_permission_profile:
  - endpoint: PUT /permission-profile?profileId={{ record.profileId }}
  - required fields: profileId
  - risk: high: administrative Gong settings or permissions mutation; requires reverse ETL approval and destructive confirmation
- update_meeting:
  - endpoint: PUT /meetings/{meetingId}
  - required fields: meetingId, endTime, invitees, organizerEmail, startTime
  - risk: medium: mutates Gong API state; requires reverse ETL approval
- delete_meeting:
  - endpoint: DELETE /meetings/{meetingId}
  - required fields: meetingId
  - risk: high: removes Gong access, integration, meeting, or flow assignment state; requires reverse ETL approval and destructive confirmation
- content_viewed:
  - endpoint: PUT /customer-engagement/content/viewed
  - required fields: contentId, contentTitle, contentUrl, eventTimestamp, reportingSystem
  - risk: high: sends call, engagement, or digital interaction content to Gong; requires reverse ETL approval
- content_shared:
  - endpoint: PUT /customer-engagement/content/shared
  - required fields: contentId, contentTitle, contentUrl, eventTimestamp, reportingSystem
  - risk: high: sends call, engagement, or digital interaction content to Gong; requires reverse ETL approval
- custom_action:
  - endpoint: PUT /customer-engagement/action
  - required fields: eventTimestamp, reportingSystem
  - risk: high: sends call, engagement, or digital interaction content to Gong; requires reverse ETL approval
- register_crm_integration:
  - endpoint: PUT /crm/integrations
  - required fields: name, ownerEmail
  - risk: medium: mutates Gong API state; requires reverse ETL approval
- delete_crm_integration:
  - endpoint: DELETE /crm/integrations?integrationId={{ record.integrationId }}&clientRequestId={{ record.clientRequestId }}
  - required fields: integrationId, clientRequestId
  - risk: high: removes Gong access, integration, meeting, or flow assignment state; requires reverse ETL approval and destructive confirmation
- add_calls_users_access:
  - endpoint: PUT /calls/users-access
  - risk: medium: mutates Gong API state; requires reverse ETL approval
- delete_calls_users_access:
  - endpoint: DELETE /calls/users-access
  - risk: high: removes Gong access, integration, meeting, or flow assignment state; requires reverse ETL approval and destructive confirmation
- create_meeting:
  - endpoint: POST /meetings
  - required fields: endTime, invitees, organizerEmail, startTime
  - risk: medium: mutates Gong API state; requires reverse ETL approval
- integration_settings:
  - endpoint: POST /integration-settings
  - required fields: integrationTypeSettings
  - risk: high: administrative Gong settings or permissions mutation; requires reverse ETL approval and destructive confirmation
- unassign_flows_by_instance_id:
  - endpoint: POST /flows/prospects/unassign-flows-by-instance-id
  - required fields: flowInstanceIds
  - risk: high: removes Gong access, integration, meeting, or flow assignment state; requires reverse ETL approval and destructive confirmation
- unassign_flows_by_crm_id:
  - endpoint: POST /flows/prospects/unassign-flows-by-crm-id
  - required fields: crmProspectId
  - risk: high: removes Gong access, integration, meeting, or flow assignment state; requires reverse ETL approval and destructive confirmation
- submit_flow_prospects_bulk_assignment:
  - endpoint: POST /flows/prospects/bulk-assignments
  - required fields: flowId, flowInstanceOwnerEmail, prospects
  - risk: medium: mutates Gong API state; requires reverse ETL approval
- assign_prospects:
  - endpoint: POST /flows/prospects/assign
  - required fields: crmProspectsIds, flowId, flowInstanceOwnerEmail
  - risk: medium: mutates Gong API state; requires reverse ETL approval
- assign_prospects_cool_off_override:
  - endpoint: POST /flows/prospects/assign/cool-off-override
  - required fields: crmProspectsIds, flowId, flowInstanceOwnerEmail
  - risk: medium: mutates Gong API state; requires reverse ETL approval
- add_digital_interaction:
  - endpoint: POST /digital-interaction
  - required fields: content, eventId, eventType, timestamp
  - risk: high: sends call, engagement, or digital interaction content to Gong; requires reverse ETL approval
- purge_phone_number:
  - endpoint: POST /data-privacy/erase-data-for-phone-number?phoneNumber={{ record.phoneNumber }}
  - required fields: phoneNumber
  - risk: critical: destructive Gong data privacy erasure; requires reverse ETL plan, preview, approval, and destructive confirmation
- purge_email_address:
  - endpoint: POST /data-privacy/erase-data-for-email-address?emailAddress={{ record.emailAddress }}
  - required fields: emailAddress
  - risk: critical: destructive Gong data privacy erasure; requires reverse ETL plan, preview, approval, and destructive confirmation
- update_task:
  - endpoint: PATCH /tasks/{taskId}
  - required fields: taskId, userId
  - risk: medium: mutates Gong API state; requires reverse ETL approval
- upload_call_media:
  - endpoint: PUT /v2/calls/{{ record.id }}/media
  - required fields: id, media_file_path
  - risk: high: uploads call media from a bounded local file path to Gong; file path/content are redacted in plans and require reverse ETL approval
- upload_crm_entities:
  - endpoint: POST /v2/crm/entities
  - required fields: data_file_path
  - risk: high: uploads CRM data from a bounded local file path to Gong; file path/content are redacted in plans and require reverse ETL approval
- upload_crm_entity_schema:
  - endpoint: POST /v2/crm/entity-schema
  - required fields: selected_fields
  - risk: high: uploads CRM entity schema as a bounded top-level JSON array; use only reviewed table mappings and reverse ETL approval
- upload_target_assignments:
  - endpoint: POST /v2/targets/{{ record.targetId }}/assignments
  - required fields: targetId, workspaceId, assignments_file_path
  - risk: high: uploads target assignments from a bounded local file path to Gong; file path/content are redacted in plans and require reverse ETL approval

## Security

- read risk: external Gong API read of call, user, CRM, settings, flow, and activity data; direct reads are bounded and redacted
- write risk: typed Gong reverse ETL mutations for calls, meetings, CRM, permissions, flows, engagement, multipart uploads, top-level array CRM schema upload, and data privacy erasure
- approval: reverse ETL writes require plan, preview, approval, execute; destructive/admin actions require --confirm destructive; local file upload plans bind a SHA-256 payload identity that is snapshot-verified before network send
- Never pass secret values in chat, shell arguments, logs, docs, or JSON output.

## Command Surface

- Inspect, read, and safely plan typed Gong operations.
- Usage: pm gong <command> [flags]
- Source CLI: Gong API (Public OpenAPI 3.0.1)
- Global flags:
  - --credential (string): Credential name to use for the Gong request.
  - --connection (string): Alias for --credential.
  - --config (string_array): Connector config override as key=value.
  - --json (boolean): Emit machine-readable JSON output.
  - --limit (integer): Maximum PM ETL records to emit; does not control Gong page size or total provider-side results.
  - --max-bytes (integer): Maximum direct-read response bytes; typed operations are capped at 16 MiB and each operation may declare a lower limit.
  - --plan (string): Execute an approved reverse-ETL plan by id.
  - --preview (boolean): Preview a reverse-ETL write command without making a network mutation.
  - --approval-token-stdin (boolean): Read the approval token as one bounded line from standard input.
  - --confirm (string): Typed confirmation challenge for destructive reverse-ETL writes.
- Calls
  - calls list - List Gong calls as ETL records. [intent=etl availability=implemented stream=calls]; notes: Use --from/--to for provider-side Gong call time bounds. --to is an exclusive upper bound. Global --limit caps PM output records only; Gong request page size remains the connector page_size config for compatibility.; flags: --from (non-empty, max 4096 bytes, format=date-time) (string): Inclusive ISO-8601 lower bound mapped to Gong fromDateTime.: maps_to=query.fromDateTime, --to (non-empty, max 4096 bytes, format=date-time) (string): Exclusive ISO-8601 upper bound mapped to Gong toDateTime.: maps_to=query.toDateTime
  - calls get - Retrieve data for a specific call (/v2/calls/{id}) [intent=direct_read availability=implemented]; risk: bounded Gong JSON read; response is limited to 1 MiB and secret/download/content-shaped fields are redacted; flags: --id (max 4096 bytes) (string): Gong's unique numeric identifier for the call (up to 20 digits).: maps_to=path.id, --page (integer): Page number to return. Only for connectors whose declared pagination addresses pages by number (page_number, offset_limit); defaults to the first page., --page-cursor (string): Continuation token to return the next page. Use the next_cursor value from a previous page, for connectors whose declared pagination addresses pages by token (cursor, next_url, link_header).
  - calls create - sends call, engagement, or digital interaction content to Gong; requires reverse ETL approval [intent=reverse_etl availability=partial write=add_call]; approval: Use reverse ETL plan -> preview -> approval -> execute. Connector command execution is metadata-only for complex object/array records; use typed reverse ETL records.; risk: high: sends call, engagement, or digital interaction content to Gong; requires reverse ETL approval; notes: No raw HTTP body is accepted. Object and array payloads must come from typed reverse-ETL records validated by writes.json.; flags: --actualStart (string): The actual date and time when the call started in the ISO-8601 format (e.g., '2018-02-18T02:30:00-07:00' or '2018-02-18T08:00:00Z', where Z stands for UTC);: maps_to=record.actualStart, --clientUniqueId (string): A call's unique identifier in the PBX or the recording system. Gong uses this identifier to prevent repeated attempts to upload the same recording.: maps_to=record.clientUniqueId, --direction (string): Whether the call is inbound (someone called the company), outbound (a rep dialed someone outside the company), or a conference call.: maps_to=record.direction, --parties (string): A list of the call's participants. A party must be provided for the primaryUser.: maps_to=record.parties, --primaryUser (string): The Gong internal user ID of the team member who hosted the call.: maps_to=record.primaryUser
  - calls users-access add - mutates Gong API state; requires reverse ETL approval [intent=reverse_etl availability=partial write=add_calls_users_access]; approval: Use reverse ETL plan -> preview -> approval -> execute. Connector command execution is metadata-only for complex object/array records; use typed reverse ETL records.; risk: medium: mutates Gong API state; requires reverse ETL approval; notes: No raw HTTP body is accepted. Object and array payloads must come from typed reverse-ETL records validated by writes.json.
  - calls users-access delete - removes Gong access, integration, meeting, or flow assignment state; requires reverse ETL approval and destructive confirmation [intent=reverse_etl availability=partial write=delete_calls_users_access]; approval: Use reverse ETL plan -> preview -> approval -> execute. Connector command execution is metadata-only for complex object/array records; use typed reverse ETL records.; risk: high: removes Gong access, integration, meeting, or flow assignment state; requires reverse ETL approval and destructive confirmation; notes: No raw HTTP body is accepted. Object and array payloads must come from typed reverse-ETL records validated by writes.json.
  - calls extensive - Retrieve detailed call data by various filters (/v2/calls/extensive) [intent=direct_read availability=implemented operation=gong.calls_extensive]; approval: none: read-only POST query with schema-gated body; risk: bounded typed POST read-query; response size is capped and secret/download/content-shaped fields are redacted; notes: Executes through the typed operation direct-read engine; no raw body or generic HTTP flag is exposed.; flags: --call-id (required) (string_array): Gong call ID; repeat or comma-separate.: maps_to=body.filter.callIds, --from (string): Inclusive ISO-8601 call start lower bound.: maps_to=body.filter.fromDateTime, --to (string): Exclusive ISO-8601 call start upper bound.: maps_to=body.filter.toDateTime, --primary-user-id (string_array): Primary Gong user ID; repeat or comma-separate.: maps_to=body.filter.primaryUserIds, --workspace-id (string): Gong workspace ID.: maps_to=body.filter.workspaceId, --context (enum): Context expansion level.: values=None|Basic|Extended: maps_to=body.contentSelector.context, --context-timing (string_array): Context timing: Now or TimeOfCall; repeat or comma-separate.: maps_to=body.contentSelector.contextTiming, --include-parties (boolean): Include call parties.: maps_to=body.contentSelector.exposedFields.parties, --include-media (boolean): Include temporary media metadata; URL fields are redacted.: maps_to=body.contentSelector.exposedFields.media, --include-public-comments (boolean): Include public comments.: maps_to=body.contentSelector.exposedFields.collaboration.publicComments, --include-brief (boolean): Include call brief.: maps_to=body.contentSelector.exposedFields.content.brief, --include-call-outcome (boolean): Include call outcome.: maps_to=body.contentSelector.exposedFields.content.callOutcome, --include-highlights (boolean): Include highlights.: maps_to=body.contentSelector.exposedFields.content.highlights, --include-key-points (boolean): Include key points.: maps_to=body.contentSelector.exposedFields.content.keyPoints, --include-outline (boolean): Include outline.: maps_to=body.contentSelector.exposedFields.content.outline, --include-structure (boolean): Include structure.: maps_to=body.contentSelector.exposedFields.content.structure, --include-topics (boolean): Include topics.: maps_to=body.contentSelector.exposedFields.content.topics, --include-trackers (boolean): Include trackers.: maps_to=body.contentSelector.exposedFields.content.trackers, --include-tracker-occurrences (boolean): Include tracker occurrences.: maps_to=body.contentSelector.exposedFields.content.trackerOccurrences, --include-speakers (boolean): Include speaker statistics.: maps_to=body.contentSelector.exposedFields.interaction.speakers, --include-questions (boolean): Include question counts.: maps_to=body.contentSelector.exposedFields.interaction.questions, --include-person-stats (boolean): Include host interaction statistics.: maps_to=body.contentSelector.exposedFields.interaction.personInteractionStats, --include-video-stats (boolean): Include video statistics.: maps_to=body.contentSelector.exposedFields.interaction.video, --page (integer): Page number to return. Only for connectors whose declared pagination addresses pages by number (page_number, offset_limit); defaults to the first page., --page-cursor (string): Continuation token to return the next page. Use the next_cursor value from a previous page, for connectors whose declared pagination addresses pages by token (cursor, next_url, link_header).
  - calls users-access get - Retrieve users that have specific individual access to calls (/v2/calls/users-access) [intent=direct_read availability=implemented operation=gong.calls_users_access_get]; approval: none: read-only POST query with schema-gated body; risk: bounded typed POST read-query; response size is capped and secret/download/content-shaped fields are redacted; notes: Executes through the typed operation direct-read engine; no raw body or generic HTTP flag is exposed.; flags: --call-id (required) (string_array): Gong call ID; repeat or comma-separate.: maps_to=body.filter.callIds, --page (integer): Page number to return. Only for connectors whose declared pagination addresses pages by number (page_number, offset_limit); defaults to the first page., --page-cursor (string): Continuation token to return the next page. Use the next_cursor value from a previous page, for connectors whose declared pagination addresses pages by token (cursor, next_url, link_header).
  - calls transcript - Retrieve transcripts of calls (/v2/calls/transcript) [intent=direct_read availability=implemented operation=gong.calls_transcript]; approval: none: read-only POST query with schema-gated body; risk: bounded typed POST read-query; response size is capped and secret/download/content-shaped fields are redacted; notes: Executes through the typed operation direct-read engine; no raw body or generic HTTP flag is exposed.; flags: --call-id (required) (string_array): Gong call ID; repeat or comma-separate.: maps_to=body.filter.callIds, --from (string): Inclusive ISO-8601 call start lower bound.: maps_to=body.filter.fromDateTime, --to (string): Exclusive ISO-8601 call start upper bound.: maps_to=body.filter.toDateTime, --workspace-id (string): Gong workspace ID.: maps_to=body.filter.workspaceId, --page (integer): Page number to return. Only for connectors whose declared pagination addresses pages by number (page_number, offset_limit); defaults to the first page., --page-cursor (string): Continuation token to return the next page. Use the next_cursor value from a previous page, for connectors whose declared pagination addresses pages by token (cursor, next_url, link_header).
  - calls upload-media - Add call media (/v2/calls/{id}/media) [intent=reverse_etl availability=implemented write=upload_call_media]; approval: reverse ETL plan -> preview -> approval -> execute; destructive confirmation required; risk: high: uploads call media from a bounded project-local file path; file path/content are redacted in plans; notes: Uses typed multipart write support; no generic upload command is exposed.; flags: --id (string): Gong call ID whose media should be uploaded.: maps_to=record.id, --media-file-path (string): Project-relative media file path to upload.: maps_to=record.media_file_path
- Users
  - users list - List Gong users as ETL records. [intent=etl availability=implemented stream=users]
  - users get - Retrieve user (/v2/users/{id}) [intent=direct_read availability=implemented]; risk: bounded Gong JSON read; response is limited to 1 MiB and secret/download/content-shaped fields are redacted; flags: --id (max 4096 bytes) (string): Gong's unique numeric identifier for the user (up to 20 digits).: maps_to=path.id, --page (integer): Page number to return. Only for connectors whose declared pagination addresses pages by number (page_number, offset_limit); defaults to the first page., --page-cursor (string): Continuation token to return the next page. Use the next_cursor value from a previous page, for connectors whose declared pagination addresses pages by token (cursor, next_url, link_header).
  - users settings-history - Retrieve user settings history (/v2/users/{id}/settings-history) [intent=direct_read availability=implemented]; risk: bounded Gong JSON read; response is limited to 1 MiB and secret/download/content-shaped fields are redacted; flags: --id (max 4096 bytes) (string): Gong's unique numeric identifier for the user (up to 20 digits).: maps_to=path.id, --page (integer): Page number to return. Only for connectors whose declared pagination addresses pages by number (page_number, offset_limit); defaults to the first page., --page-cursor (string): Continuation token to return the next page. Use the next_cursor value from a previous page, for connectors whose declared pagination addresses pages by token (cursor, next_url, link_header).
  - users extensive - List users by filter (/v2/users/extensive) [intent=direct_read availability=implemented operation=gong.users_extensive]; approval: none: read-only POST query with schema-gated body; risk: bounded typed POST read-query; response size is capped and secret/download/content-shaped fields are redacted; notes: Executes through the typed operation direct-read engine; no raw body or generic HTTP flag is exposed.; flags: --user-id (string_array): Gong user ID; repeat or comma-separate.: maps_to=body.filter.userIds, --created-from (string): Inclusive ISO-8601 creation lower bound.: maps_to=body.filter.createdFromDateTime, --created-to (string): Exclusive ISO-8601 creation upper bound.: maps_to=body.filter.createdToDateTime, --include-avatars (boolean): Include Gong avatar users.: maps_to=body.filter.includeAvatars, --page (integer): Page number to return. Only for connectors whose declared pagination addresses pages by number (page_number, offset_limit); defaults to the first page., --page-cursor (string): Continuation token to return the next page. Use the next_cursor value from a previous page, for connectors whose declared pagination addresses pages by token (cursor, next_url, link_header).
- Scorecards
  - scorecards list - List Gong scorecards as ETL records. [intent=etl availability=implemented stream=scorecards]
- Workspaces
  - workspaces list - List Gong workspaces as ETL records. [intent=etl availability=implemented stream=workspaces]
- Settings
  - settings trackers list - List Gong tracker settings as ETL records. [intent=etl availability=implemented stream=trackers]
  - settings briefs list - List Gong AI brief settings as ETL records. [intent=etl availability=implemented stream=briefs]
- Permissions
  - permissions profiles list - List Gong permission profiles as ETL records. [intent=etl availability=implemented stream=permission_profiles]
  - permissions profile get - Permission profile for a given profile Id (/v2/permission-profile) [intent=direct_read availability=implemented]; risk: bounded Gong JSON read; response is limited to 1 MiB and secret/download/content-shaped fields are redacted; flags: --profileId (max 4096 bytes) (string): Permission profile identifier.: maps_to=query.profileId, --page (integer): Page number to return. Only for connectors whose declared pagination addresses pages by number (page_number, offset_limit); defaults to the first page., --page-cursor (string): Continuation token to return the next page. Use the next_cursor value from a previous page, for connectors whose declared pagination addresses pages by token (cursor, next_url, link_header).
  - permissions profile-users list - List all users attached to a given permission profile (/v2/permission-profile/users) [intent=direct_read availability=implemented]; risk: bounded Gong JSON read; response is limited to 1 MiB and secret/download/content-shaped fields are redacted; flags: --profileId (max 4096 bytes) (string): Permission profile identifier.: maps_to=query.profileId, --page (integer): Page number to return. Only for connectors whose declared pagination addresses pages by number (page_number, offset_limit); defaults to the first page., --page-cursor (string): Continuation token to return the next page. Use the next_cursor value from a previous page, for connectors whose declared pagination addresses pages by token (cursor, next_url, link_header).
  - permissions profile create - administrative Gong settings or permissions mutation; requires reverse ETL approval and destructive confirmation [intent=reverse_etl availability=partial write=create_permission_profile]; approval: Use reverse ETL plan -> preview -> approval -> execute. Connector command execution is metadata-only for complex object/array records; use typed reverse ETL records.; risk: high: administrative Gong settings or permissions mutation; requires reverse ETL approval and destructive confirmation; notes: No raw HTTP body is accepted. Object and array payloads must come from typed reverse-ETL records validated by writes.json.; flags: --workspaceId (string): workspaceId value: maps_to=record.workspaceId
  - permissions profile update - administrative Gong settings or permissions mutation; requires reverse ETL approval and destructive confirmation [intent=reverse_etl availability=partial write=update_permission_profile]; approval: Use reverse ETL plan -> preview -> approval -> execute. Connector command execution is metadata-only for complex object/array records; use typed reverse ETL records.; risk: high: administrative Gong settings or permissions mutation; requires reverse ETL approval and destructive confirmation; notes: No raw HTTP body is accepted. Object and array payloads must come from typed reverse-ETL records validated by writes.json.; flags: --profileId (string): profileId value: maps_to=record.profileId
- CRM
  - crm integrations list - List Generic CRM integrations as ETL records. [intent=etl availability=implemented stream=crm_integrations]
  - crm entity-schema get - List Schema Fields (/v2/crm/entity-schema) [intent=direct_read availability=implemented]; risk: bounded Gong JSON read; response is limited to 1 MiB and secret/download/content-shaped fields are redacted; flags: --integrationId (max 4096 bytes) (integer): Integration ID generated when creating the integration: maps_to=query.integrationId, --objectType (max 4096 bytes) (string): Type of object to retrieve the schema fields for (case-sensitive). <br>Omitting this parameter returns the schema for all object types.: maps_to=query.objectType, --page (integer): Page number to return. Only for connectors whose declared pagination addresses pages by number (page_number, offset_limit); defaults to the first page., --page-cursor (string): Continuation token to return the next page. Use the next_cursor value from a previous page, for connectors whose declared pagination addresses pages by token (cursor, next_url, link_header).
  - crm entities get - Get CRM objects (/v2/crm/entities) [intent=direct_read availability=implemented]; risk: bounded Gong JSON read; response is limited to 1 MiB and secret/download/content-shaped fields are redacted; flags: --integrationId (max 4096 bytes) (integer): Integration ID generated when creating the integration: maps_to=query.integrationId, --objectType (max 4096 bytes) (string): Requested objects type: maps_to=query.objectType, --objectsCrmIds (max 4096 bytes) (string): <b>Request Body:</b> The requested objects crm ids (should be sent in the request body): maps_to=query.objectsCrmIds, --page (integer): Page number to return. Only for connectors whose declared pagination addresses pages by number (page_number, offset_limit); defaults to the first page., --page-cursor (string): Continuation token to return the next page. Use the next_cursor value from a previous page, for connectors whose declared pagination addresses pages by token (cursor, next_url, link_header).
  - crm request-status get - Get Request Status (/v2/crm/request-status) [intent=direct_read availability=implemented]; risk: bounded Gong JSON read; response is limited to 1 MiB and secret/download/content-shaped fields are redacted; flags: --integrationId (max 4096 bytes) (integer): Integration ID generated when creating the integration: maps_to=query.integrationId, --clientRequestId (max 4096 bytes) (string): The client request ID used in the asynchronous endpoint you want to get a status for: maps_to=query.clientRequestId, --page (integer): Page number to return. Only for connectors whose declared pagination addresses pages by number (page_number, offset_limit); defaults to the first page., --page-cursor (string): Continuation token to return the next page. Use the next_cursor value from a previous page, for connectors whose declared pagination addresses pages by token (cursor, next_url, link_header).
  - crm integrations register - mutates Gong API state; requires reverse ETL approval [intent=reverse_etl availability=partial write=register_crm_integration]; approval: Use reverse ETL plan -> preview -> approval -> execute. Connector command execution is metadata-only for complex object/array records; use typed reverse ETL records.; risk: medium: mutates Gong API state; requires reverse ETL approval; notes: No raw HTTP body is accepted. Object and array payloads must come from typed reverse-ETL records validated by writes.json.; flags: --name (string): The name of the integration: maps_to=record.name, --ownerEmail (string): Email address of the person responsible for the integration: maps_to=record.ownerEmail
  - crm integrations delete - removes Gong access, integration, meeting, or flow assignment state; requires reverse ETL approval and destructive confirmation [intent=reverse_etl availability=partial write=delete_crm_integration]; approval: Use reverse ETL plan -> preview -> approval -> execute. Connector command execution is metadata-only for complex object/array records; use typed reverse ETL records.; risk: high: removes Gong access, integration, meeting, or flow assignment state; requires reverse ETL approval and destructive confirmation; notes: No raw HTTP body is accepted. Object and array payloads must come from typed reverse-ETL records validated by writes.json.; flags: --clientRequestId (string): clientRequestId value: maps_to=record.clientRequestId, --integrationId (string): integrationId value: maps_to=record.integrationId
  - crm upload-entities - Upload CRM objects (/v2/crm/entities) [intent=reverse_etl availability=implemented write=upload_crm_entities]; approval: reverse ETL plan -> preview -> approval -> execute; destructive confirmation required; risk: high: uploads CRM data from a bounded project-local file path; file path/content are redacted in plans; notes: Uses typed multipart write support; no generic upload command is exposed.; flags: --data-file-path (string): Project-relative CRM data file path to upload.: maps_to=record.data_file_path
  - crm upload-entity-schema - Upload Object Schema (/v2/crm/entity-schema) [intent=reverse_etl availability=partial write=upload_crm_entity_schema]; approval: reverse ETL plan -> preview -> approval -> execute; destructive confirmation required; risk: high: uploads CRM entity schema as a top-level JSON array; no raw CLI body is exposed; notes: Write action supports schema-gated top-level array bodies for table/reverse-ETL mappings; provider-style raw JSON CLI flags remain intentionally unavailable.
- Meetings
  - meetings update - mutates Gong API state; requires reverse ETL approval [intent=reverse_etl availability=partial write=update_meeting]; approval: Use reverse ETL plan -> preview -> approval -> execute. Connector command execution is metadata-only for complex object/array records; use typed reverse ETL records.; risk: medium: mutates Gong API state; requires reverse ETL approval; notes: No raw HTTP body is accepted. Object and array payloads must come from typed reverse-ETL records validated by writes.json.; flags: --endTime (string): The meeting end time in ISO-8601 format (e.g., '2018-02-18T02:30:00-07:00' or '2018-02-18T08:00:00Z', where Z stands for UTC).: maps_to=record.endTime, --invitees (string): A list of email addresses of invitees to the event (not including the organizer).: maps_to=record.invitees, --meetingId (string): meetingId value: maps_to=record.meetingId, --organizerEmail (string): The email address of the user who created the meeting.: maps_to=record.organizerEmail, --startTime (string): The meeting start time in ISO-8601 format (e.g., '2018-02-18T02:30:00-07:00' or '2018-02-18T08:00:00Z', where Z stands for UTC).: maps_to=record.startTime
  - meetings delete - removes Gong access, integration, meeting, or flow assignment state; requires reverse ETL approval and destructive confirmation [intent=reverse_etl availability=partial write=delete_meeting]; approval: Use reverse ETL plan -> preview -> approval -> execute. Connector command execution is metadata-only for complex object/array records; use typed reverse ETL records.; risk: high: removes Gong access, integration, meeting, or flow assignment state; requires reverse ETL approval and destructive confirmation; notes: No raw HTTP body is accepted. Object and array payloads must come from typed reverse-ETL records validated by writes.json.; flags: --meetingId (string): meetingId value: maps_to=record.meetingId
  - meetings create - mutates Gong API state; requires reverse ETL approval [intent=reverse_etl availability=partial write=create_meeting]; approval: Use reverse ETL plan -> preview -> approval -> execute. Connector command execution is metadata-only for complex object/array records; use typed reverse ETL records.; risk: medium: mutates Gong API state; requires reverse ETL approval; notes: No raw HTTP body is accepted. Object and array payloads must come from typed reverse-ETL records validated by writes.json.; flags: --endTime (string): The meeting end time in ISO-8601 format (e.g., '2018-02-18T02:30:00-07:00' or '2018-02-18T08:00:00Z', where Z stands for UTC).: maps_to=record.endTime, --invitees (string): A list of email addresses of invitees to the event (not including the organizer).: maps_to=record.invitees, --organizerEmail (string): The email address of the user creating the meeting, the Gong consent page link will be used according to the settings of this user.: maps_to=record.organizerEmail, --startTime (string): The meeting start time in ISO-8601 format (e.g., '2018-02-18T02:30:00-07:00' or '2018-02-18T08:00:00Z', where Z stands for UTC).: maps_to=record.startTime
  - meetings integration-status - Validate Gong meeting Integration (/v2/meetings/integration/status) [intent=direct_read availability=implemented operation=gong.meetings_integration_status]; approval: none: read-only POST query with schema-gated body; risk: bounded POST read-query; sends only typed email array body fields and redacts sensitive response fields; notes: Executes through the typed operation direct-read engine; no raw body flag is exposed.; flags: --email (required) (string_array): Email address to validate. Repeat or comma-separate; max 100 per Gong API.: maps_to=body.emails, --page (integer): Page number to return. Only for connectors whose declared pagination addresses pages by number (page_number, offset_limit); defaults to the first page., --page-cursor (string): Continuation token to return the next page. Use the next_cursor value from a previous page, for connectors whose declared pagination addresses pages by token (cursor, next_url, link_header).
- Engagement
  - engagement content-viewed - sends call, engagement, or digital interaction content to Gong; requires reverse ETL approval [intent=reverse_etl availability=partial write=content_viewed]; approval: Use reverse ETL plan -> preview -> approval -> execute. Connector command execution is metadata-only for complex object/array records; use typed reverse ETL records.; risk: high: sends call, engagement, or digital interaction content to Gong; requires reverse ETL approval; notes: No raw HTTP body is accepted. Object and array payloads must come from typed reverse-ETL records validated by writes.json.; flags: --contentId (string): The id of the content that was viewed in the reporting system.: maps_to=record.contentId, --contentTitle (string): Human readable title of the content.: maps_to=record.contentTitle, --contentUrl (string): The url of the content that was viewed in the reporting system. This is the url that is was accessed by the viewer.: maps_to=record.contentUrl, --eventTimestamp (string): The date and time when the event happened in the ISO-8601 format (e.g., '2021-08-01T02:30:00+05:00' or '2021-08-01T08:00:00Z', where Z stands for UTC);: maps_to=record.eventTimestamp, --reportingSystem (string): The unique identifier of the reporting system. It is the same value in all events originating from the same system.: maps_to=record.reportingSystem
  - engagement content-shared - sends call, engagement, or digital interaction content to Gong; requires reverse ETL approval [intent=reverse_etl availability=partial write=content_shared]; approval: Use reverse ETL plan -> preview -> approval -> execute. Connector command execution is metadata-only for complex object/array records; use typed reverse ETL records.; risk: high: sends call, engagement, or digital interaction content to Gong; requires reverse ETL approval; notes: No raw HTTP body is accepted. Object and array payloads must come from typed reverse-ETL records validated by writes.json.; flags: --contentId (string): The id of the content that was shared in the reporting system.: maps_to=record.contentId, --contentTitle (string): Human readable title of the content.: maps_to=record.contentTitle, --contentUrl (string): The url of the content that was shared in the reporting system. This is the url that is was accessed by the viewer.: maps_to=record.contentUrl, --eventTimestamp (string): The date and time when the event happened in the ISO-8601 format (e.g., '2021-08-01T02:30:00+05:00' or '2021-08-01T08:00:00Z', where Z stands for UTC);: maps_to=record.eventTimestamp, --reportingSystem (string): The unique identifier of the reporting system. It is the same value in all events originating from the same system.: maps_to=record.reportingSystem
  - engagement custom-action - sends call, engagement, or digital interaction content to Gong; requires reverse ETL approval [intent=reverse_etl availability=partial write=custom_action]; approval: Use reverse ETL plan -> preview -> approval -> execute. Connector command execution is metadata-only for complex object/array records; use typed reverse ETL records.; risk: high: sends call, engagement, or digital interaction content to Gong; requires reverse ETL approval; notes: No raw HTTP body is accepted. Object and array payloads must come from typed reverse-ETL records validated by writes.json.; flags: --eventTimestamp (string): The date and time when the event happened in the ISO-8601 format (e.g., '2021-08-01T02:30:00+05:00' or '2021-08-01T08:00:00Z', where Z stands for UTC);: maps_to=record.eventTimestamp, --reportingSystem (string): The unique identifier of the reporting system. It is the same value in all events originating from the same system.: maps_to=record.reportingSystem
- Stats
  - stats interaction - Retrieve interaction stats for applicable users by date (/v2/stats/interaction) [intent=direct_read availability=implemented operation=gong.stats_interaction]; approval: none: read-only POST query with schema-gated body; risk: bounded typed POST read-query; response size is capped and secret/download/content-shaped fields are redacted; notes: Executes through the typed operation direct-read engine; no raw body or generic HTTP flag is exposed.; flags: --from-date (required) (string): Inclusive date, YYYY-MM-DD.: maps_to=body.filter.fromDate, --to-date (required) (string): Exclusive date, YYYY-MM-DD.: maps_to=body.filter.toDate, --user-id (string_array): Gong user ID; repeat or comma-separate.: maps_to=body.filter.userIds, --created-from (string): Optional ISO-8601 creation lower bound.: maps_to=body.filter.createdFromDateTime, --created-to (string): Optional ISO-8601 creation upper bound.: maps_to=body.filter.createdToDateTime, --page (integer): Page number to return. Only for connectors whose declared pagination addresses pages by number (page_number, offset_limit); defaults to the first page., --page-cursor (string): Continuation token to return the next page. Use the next_cursor value from a previous page, for connectors whose declared pagination addresses pages by token (cursor, next_url, link_header).
  - stats activity-scorecards - Retrieve answered scorecards for applicable reviewed users or scorecards for a date range (/v2/stats/activity/scorecards) [intent=direct_read availability=implemented operation=gong.stats_activity_scorecards]; approval: none: read-only POST query with schema-gated body; risk: bounded typed POST read-query; response size is capped and secret/download/content-shaped fields are redacted; notes: Executes through the typed operation direct-read engine; no raw body or generic HTTP flag is exposed.; flags: --call-from-date (string): Inclusive call date, YYYY-MM-DD.: maps_to=body.filter.callFromDate, --call-to-date (string): Exclusive call date, YYYY-MM-DD.: maps_to=body.filter.callToDate, --review-from-date (string): Inclusive review date, YYYY-MM-DD.: maps_to=body.filter.reviewFromDate, --review-to-date (string): Exclusive review date, YYYY-MM-DD.: maps_to=body.filter.reviewToDate, --review-method (enum): Review method.: values=AUTOMATIC|MANUAL|BOTH: maps_to=body.filter.reviewMethod, --reviewed-user-id (string_array): Reviewed Gong user ID; repeat or comma-separate.: maps_to=body.filter.reviewedUserIds, --scorecard-id (required) (string_array): Scorecard ID; repeat or comma-separate.: maps_to=body.filter.scorecardIds, --page (integer): Page number to return. Only for connectors whose declared pagination addresses pages by number (page_number, offset_limit); defaults to the first page., --page-cursor (string): Continuation token to return the next page. Use the next_cursor value from a previous page, for connectors whose declared pagination addresses pages by token (cursor, next_url, link_header).
  - stats activity-day-by-day - Retrieve daily activity for applicable users for a date range (/v2/stats/activity/day-by-day) [intent=direct_read availability=implemented operation=gong.stats_activity_day_by_day]; approval: none: read-only POST query with schema-gated body; risk: bounded typed POST read-query; response size is capped and secret/download/content-shaped fields are redacted; notes: Executes through the typed operation direct-read engine; no raw body or generic HTTP flag is exposed.; flags: --from-date (required) (string): Inclusive date, YYYY-MM-DD.: maps_to=body.filter.fromDate, --to-date (required) (string): Exclusive date, YYYY-MM-DD.: maps_to=body.filter.toDate, --user-id (string_array): Gong user ID; repeat or comma-separate.: maps_to=body.filter.userIds, --created-from (string): Optional ISO-8601 creation lower bound.: maps_to=body.filter.createdFromDateTime, --created-to (string): Optional ISO-8601 creation upper bound.: maps_to=body.filter.createdToDateTime, --page (integer): Page number to return. Only for connectors whose declared pagination addresses pages by number (page_number, offset_limit); defaults to the first page., --page-cursor (string): Continuation token to return the next page. Use the next_cursor value from a previous page, for connectors whose declared pagination addresses pages by token (cursor, next_url, link_header).
  - stats activity-aggregate - Retrieve aggregated activity for defined users by date (/v2/stats/activity/aggregate) [intent=direct_read availability=implemented operation=gong.stats_activity_aggregate]; approval: none: read-only POST query with schema-gated body; risk: bounded typed POST read-query; response size is capped and secret/download/content-shaped fields are redacted; notes: Executes through the typed operation direct-read engine; no raw body or generic HTTP flag is exposed.; flags: --from-date (required) (string): Inclusive date, YYYY-MM-DD.: maps_to=body.filter.fromDate, --to-date (required) (string): Exclusive date, YYYY-MM-DD.: maps_to=body.filter.toDate, --user-id (string_array): Gong user ID; repeat or comma-separate.: maps_to=body.filter.userIds, --created-from (string): Optional ISO-8601 creation lower bound.: maps_to=body.filter.createdFromDateTime, --created-to (string): Optional ISO-8601 creation upper bound.: maps_to=body.filter.createdToDateTime, --page (integer): Page number to return. Only for connectors whose declared pagination addresses pages by number (page_number, offset_limit); defaults to the first page., --page-cursor (string): Continuation token to return the next page. Use the next_cursor value from a previous page, for connectors whose declared pagination addresses pages by token (cursor, next_url, link_header).
  - stats activity-aggregate-by-period - Retrieve aggregated activity for defined users by a date range with grouping in time periods (/v2/stats/activity/aggregate-by-period) [intent=direct_read availability=implemented operation=gong.stats_activity_aggregate_by_period]; approval: none: read-only POST query with schema-gated body; risk: bounded typed POST read-query; response size is capped and secret/download/content-shaped fields are redacted; notes: Executes through the typed operation direct-read engine; no raw body or generic HTTP flag is exposed.; flags: --from-date (required) (string): Inclusive date, YYYY-MM-DD.: maps_to=body.filter.fromDate, --to-date (required) (string): Exclusive date, YYYY-MM-DD.: maps_to=body.filter.toDate, --user-id (string_array): Gong user ID; repeat or comma-separate.: maps_to=body.filter.userIds, --created-from (string): Optional ISO-8601 creation lower bound.: maps_to=body.filter.createdFromDateTime, --created-to (string): Optional ISO-8601 creation upper bound.: maps_to=body.filter.createdToDateTime, --aggregation-period (required) (enum): Aggregation period.: values=DAY|WEEK|MONTH|QUARTER|YEAR: maps_to=body.aggregationPeriod, --page (integer): Page number to return. Only for connectors whose declared pagination addresses pages by number (page_number, offset_limit); defaults to the first page., --page-cursor (string): Continuation token to return the next page. Use the next_cursor value from a previous page, for connectors whose declared pagination addresses pages by token (cursor, next_url, link_header).
- Engage Flows
  - flows list - List Gong Engage flows as ETL records. [intent=etl availability=implemented stream=flows]
  - flows folders list - List Gong Engage flow folders as ETL records. [intent=etl availability=implemented stream=flow_folders]
  - flows bulk-assignment get - Get the results of a bulk assignment of prospects to a flow (/v2/flows/prospects/bulk-assignments/{id}) [intent=direct_read availability=implemented]; risk: bounded Gong JSON read; response is limited to 1 MiB and secret/download/content-shaped fields are redacted; flags: --id (max 4096 bytes) (string): maps_to=path.id, --page (integer): Page number to return. Only for connectors whose declared pagination addresses pages by number (page_number, offset_limit); defaults to the first page., --page-cursor (string): Continuation token to return the next page. Use the next_cursor value from a previous page, for connectors whose declared pagination addresses pages by token (cursor, next_url, link_header).
  - flows prospects unassign-by-instance - removes Gong access, integration, meeting, or flow assignment state; requires reverse ETL approval and destructive confirmation [intent=reverse_etl availability=partial write=unassign_flows_by_instance_id]; approval: Use reverse ETL plan -> preview -> approval -> execute. Connector command execution is metadata-only for complex object/array records; use typed reverse ETL records.; risk: high: removes Gong access, integration, meeting, or flow assignment state; requires reverse ETL approval and destructive confirmation; notes: No raw HTTP body is accepted. Object and array payloads must come from typed reverse-ETL records validated by writes.json.; flags: --flowInstanceIds (string_array): The unique identifiers of the flows that prospects should be removed from.: maps_to=record.flowInstanceIds
  - flows prospects unassign-by-crm - removes Gong access, integration, meeting, or flow assignment state; requires reverse ETL approval and destructive confirmation [intent=reverse_etl availability=partial write=unassign_flows_by_crm_id]; approval: Use reverse ETL plan -> preview -> approval -> execute. Connector command execution is metadata-only for complex object/array records; use typed reverse ETL records.; risk: high: removes Gong access, integration, meeting, or flow assignment state; requires reverse ETL approval and destructive confirmation; notes: No raw HTTP body is accepted. Object and array payloads must come from typed reverse-ETL records validated by writes.json.; flags: --crmProspectId (string): The CRM ID of the prospect to unassign.: maps_to=record.crmProspectId
  - flows prospects bulk-assign - mutates Gong API state; requires reverse ETL approval [intent=reverse_etl availability=partial write=submit_flow_prospects_bulk_assignment]; approval: Use reverse ETL plan -> preview -> approval -> execute. Connector command execution is metadata-only for complex object/array records; use typed reverse ETL records.; risk: medium: mutates Gong API state; requires reverse ETL approval; notes: No raw HTTP body is accepted. Object and array payloads must come from typed reverse-ETL records validated by writes.json.; flags: --flowId (string): The Flow ID to assign the prospects to.: maps_to=record.flowId, --flowInstanceOwnerEmail (string): The email of the Gong user who will own the flow instance.: maps_to=record.flowInstanceOwnerEmail, --prospects (string): The prospects that should be assigned to the flow.: maps_to=record.prospects
  - flows prospects assign - mutates Gong API state; requires reverse ETL approval [intent=reverse_etl availability=partial write=assign_prospects]; approval: Use reverse ETL plan -> preview -> approval -> execute. Connector command execution is metadata-only for complex object/array records; use typed reverse ETL records.; risk: medium: mutates Gong API state; requires reverse ETL approval; notes: No raw HTTP body is accepted. Object and array payloads must come from typed reverse-ETL records validated by writes.json.; flags: --crmProspectsIds (string_array): The CRM IDs of the contact or lead that should be assigned to a flow.: maps_to=record.crmProspectsIds, --flowId (string): The Engage Flow ID to assign the contacts to.: maps_to=record.flowId, --flowInstanceOwnerEmail (string): The email of the Gong user who owns the flow to-dos.: maps_to=record.flowInstanceOwnerEmail
  - flows prospects assign-cool-off-override - mutates Gong API state; requires reverse ETL approval [intent=reverse_etl availability=partial write=assign_prospects_cool_off_override]; approval: Use reverse ETL plan -> preview -> approval -> execute. Connector command execution is metadata-only for complex object/array records; use typed reverse ETL records.; risk: medium: mutates Gong API state; requires reverse ETL approval; notes: No raw HTTP body is accepted. Object and array payloads must come from typed reverse-ETL records validated by writes.json.; flags: --crmProspectsIds (string_array): The CRM IDs of the contact or lead that should be assigned to a flow.: maps_to=record.crmProspectsIds, --flowId (string): The Engage Flow ID to assign the contacts to.: maps_to=record.flowId, --flowInstanceOwnerEmail (string): The email of the Gong user who owns the flow to-dos.: maps_to=record.flowInstanceOwnerEmail
  - flows steps - Get flow details and steps for one or more Engage flows (/v2/flows/steps) [intent=direct_read availability=implemented operation=gong.flows_steps]; approval: none: read-only POST query with schema-gated body; risk: bounded POST read-query; sends only typed flow ID array body fields and redacts sensitive response fields; notes: Executes through the typed operation direct-read engine; no raw body flag is exposed.; flags: --flow-id (required) (string_array): Engage flow ID to retrieve steps for. Repeat or comma-separate; max 20 per Gong API.: maps_to=body.flowIds, --page (integer): Page number to return. Only for connectors whose declared pagination addresses pages by number (page_number, offset_limit); defaults to the first page., --page-cursor (string): Continuation token to return the next page. Use the next_cursor value from a previous page, for connectors whose declared pagination addresses pages by token (cursor, next_url, link_header).
  - flows prospects - List prospects assigned flows (/v2/flows/prospects) [intent=direct_read availability=implemented operation=gong.flows_prospects]; approval: none: read-only POST query with schema-gated body; risk: bounded POST read-query; sends only typed CRM prospect or flow instance ID arrays and redacts sensitive response fields; notes: Executes through the typed operation direct-read engine; no raw body flag is exposed.; flags: --crm-prospect-id (string_array): CRM prospect ID to query. Repeat or comma-separate.: maps_to=body.crmProspectsIds, --flow-instance-id (string_array): Assigned flow instance ID to query. Repeat or comma-separate.: maps_to=body.flowInstanceIds, --page (integer): Page number to return. Only for connectors whose declared pagination addresses pages by number (page_number, offset_limit); defaults to the first page., --page-cursor (string): Continuation token to return the next page. Use the next_cursor value from a previous page, for connectors whose declared pagination addresses pages by token (cursor, next_url, link_header).
- Library
  - library folders list - List Gong library folders as ETL records. [intent=etl availability=implemented stream=library_folders]
  - library folder-content list - List of calls in a specific folder (/v2/library/folder-content) [intent=direct_read availability=implemented]; risk: bounded Gong JSON read; response is limited to 1 MiB and secret/download/content-shaped fields are redacted; flags: --folderId (max 4096 bytes) (string): Gong's unique numeric identifier for the folder (up to 20 digits).: maps_to=query.folderId, --page (integer): Page number to return. Only for connectors whose declared pagination addresses pages by number (page_number, offset_limit); defaults to the first page., --page-cursor (string): Continuation token to return the next page. Use the next_cursor value from a previous page, for connectors whose declared pagination addresses pages by token (cursor, next_url, link_header).
- Entities
  - entities get-brief - Generate a brief for a CRM entity (/v2/entities/get-brief) [intent=direct_read availability=implemented]; risk: bounded Gong JSON read; response is limited to 1 MiB and secret/download/content-shaped fields are redacted; flags: --workspaceId (max 4096 bytes) (integer): Workspace identifier. The workspace the brief is defined in.: maps_to=query.workspaceId, --briefName (max 4096 bytes) (string): The name of the brief to generate. Briefs are configured in the Agent Studio > AI Briefer.: maps_to=query.briefName, --crmEntityType (max 4096 bytes) (string): The object type of the entity in the CRM.: maps_to=query.crmEntityType, --crmEntityId (max 4096 bytes) (string): The CRM Id of the entity the question is asked about.: maps_to=query.crmEntityId, --timePeriod (max 4096 bytes) (string): Specifies the time period during which associated calls and emails must have occurred to be used to generate the answer. The calls and emails must be associated with the specified : maps_to=query.timePeriod, --fromDateTime (max 4096 bytes) (string): Required when timePeriod is CUSTOM_RANGE
Specifies the date and time (UTC) on or after which calls (based on call start time) and emails must have occurred to be included when gene: maps_to=query.fromDateTime, --toDateTime (max 4096 bytes) (string): Relevant only when timePeriod is CUSTOM_RANGE
Specifies the date and time (UTC) before which calls (based on call start time) and emails must have occurred to be included when gene: maps_to=query.toDateTime, --page (integer): Page number to return. Only for connectors whose declared pagination addresses pages by number (page_number, offset_limit); defaults to the first page., --page-cursor (string): Continuation token to return the next page. Use the next_cursor value from a previous page, for connectors whose declared pagination addresses pages by token (cursor, next_url, link_header).
  - entities ask - Ask about an entity (/v2/entities/ask-entity) [intent=direct_read availability=implemented]; risk: bounded Gong JSON read; response is limited to 1 MiB and secret/download/content-shaped fields are redacted; flags: --workspaceId (max 4096 bytes) (integer): Workspace identifier. The calls and emails used to answer the question must be associated with this workspace.: maps_to=query.workspaceId, --crmEntityType (max 4096 bytes) (string): The object type of the entity in the CRM.: maps_to=query.crmEntityType, --crmEntityId (max 4096 bytes) (string): The CRM Id of the entity the question is asked about.: maps_to=query.crmEntityId, --timePeriod (max 4096 bytes) (string): Specifies the time period during which associated calls and emails must have occurred to be used to generate the answer. The calls and emails must be associated with the specified : maps_to=query.timePeriod, --fromDateTime (max 4096 bytes) (string): Required when timePeriod is CUSTOM_RANGE
Specifies the date and time (UTC) on or after which calls (based on call start time) and emails must have occurred to be included when gene: maps_to=query.fromDateTime, --toDateTime (max 4096 bytes) (string): Relevant only when timePeriod is CUSTOM_RANGE
Specifies the date and time (UTC) before which calls (based on call start time) and emails must have occurred to be included when gene: maps_to=query.toDateTime, --question (max 4096 bytes) (string): The question to answer.: maps_to=query.question, --page (integer): Page number to return. Only for connectors whose declared pagination addresses pages by number (page_number, offset_limit); defaults to the first page., --page-cursor (string): Continuation token to return the next page. Use the next_cursor value from a previous page, for connectors whose declared pagination addresses pages by token (cursor, next_url, link_header).
- Data Privacy
  - privacy find-phone - Retrieve all references to a phone number. (/v2/data-privacy/data-for-phone-number) [intent=direct_read availability=implemented]; risk: bounded Gong JSON read; response is limited to 1 MiB and secret/download/content-shaped fields are redacted; flags: --phoneNumber (max 4096 bytes) (string): The phone number.

This number must start with a + (plus) sign followed by the country code, area code, and local phone number. All other non-digits are ignored.

The following are: maps_to=query.phoneNumber, --page (integer): Page number to return. Only for connectors whose declared pagination addresses pages by number (page_number, offset_limit); defaults to the first page., --page-cursor (string): Continuation token to return the next page. Use the next_cursor value from a previous page, for connectors whose declared pagination addresses pages by token (cursor, next_url, link_header).
  - privacy find-email - Retrieve all references to an email address. (/v2/data-privacy/data-for-email-address) [intent=direct_read availability=implemented]; risk: bounded Gong JSON read; response is limited to 1 MiB and secret/download/content-shaped fields are redacted; flags: --emailAddress (max 4096 bytes) (string): The email address.: maps_to=query.emailAddress, --page (integer): Page number to return. Only for connectors whose declared pagination addresses pages by number (page_number, offset_limit); defaults to the first page., --page-cursor (string): Continuation token to return the next page. Use the next_cursor value from a previous page, for connectors whose declared pagination addresses pages by token (cursor, next_url, link_header).
  - privacy erase-phone - destructive Gong data privacy erasure; requires reverse ETL plan, preview, approval, and destructive confirmation [intent=reverse_etl availability=partial write=purge_phone_number]; approval: Use reverse ETL plan -> preview -> approval -> execute. Connector command execution is metadata-only for complex object/array records; use typed reverse ETL records.; risk: critical: destructive Gong data privacy erasure; requires reverse ETL plan, preview, approval, and destructive confirmation; notes: No raw HTTP body is accepted. Object and array payloads must come from typed reverse-ETL records validated by writes.json.; flags: --phoneNumber (string): phoneNumber value: maps_to=record.phoneNumber
  - privacy erase-email - destructive Gong data privacy erasure; requires reverse ETL plan, preview, approval, and destructive confirmation [intent=reverse_etl availability=partial write=purge_email_address]; approval: Use reverse ETL plan -> preview -> approval -> execute. Connector command execution is metadata-only for complex object/array records; use typed reverse ETL records.; risk: critical: destructive Gong data privacy erasure; requires reverse ETL plan, preview, approval, and destructive confirmation; notes: No raw HTTP body is accepted. Object and array payloads must come from typed reverse-ETL records validated by writes.json.; flags: --emailAddress (string): emailAddress value: maps_to=record.emailAddress
- Tasks
  - tasks update - mutates Gong API state; requires reverse ETL approval [intent=reverse_etl availability=partial write=update_task]; approval: Use reverse ETL plan -> preview -> approval -> execute. Connector command execution is metadata-only for complex object/array records; use typed reverse ETL records.; risk: medium: mutates Gong API state; requires reverse ETL approval; notes: No raw HTTP body is accepted. Object and array payloads must come from typed reverse-ETL records validated by writes.json.; flags: --taskId (string): taskId value: maps_to=record.taskId, --userId (string): The id of the Gong user who owns the task: maps_to=record.userId
  - tasks list - List user's tasks (/v2/tasks) [intent=direct_read availability=implemented operation=gong.tasks_list]; approval: none: read-only POST query with schema-gated body; risk: bounded typed POST read-query; response size is capped and secret/download/content-shaped fields are redacted; notes: Executes through the typed operation direct-read engine; no raw body or generic HTTP flag is exposed.; flags: --user-id (required) (string): Owning Gong user ID.: maps_to=body.filter.userId, --status (required) (string_array): Task status: OPEN, DONE, or DISMISSED; repeat or comma-separate.: maps_to=body.filter.status, --task-action (required) (string_array): Task action; Gong currently supports CALL.: maps_to=body.filter.taskAction, --task-type (required) (string_array): Task type: FLOW or MANUAL; repeat or comma-separate.: maps_to=body.filter.types, --workspace-id (string): Optional Gong workspace ID.: maps_to=body.filter.workspaceId, --page (integer): Page number to return. Only for connectors whose declared pagination addresses pages by number (page_number, offset_limit); defaults to the first page., --page-cursor (string): Continuation token to return the next page. Use the next_cursor value from a previous page, for connectors whose declared pagination addresses pages by token (cursor, next_url, link_header).
- Digital Interactions
  - digital-interaction create - sends call, engagement, or digital interaction content to Gong; requires reverse ETL approval [intent=reverse_etl availability=partial write=add_digital_interaction]; approval: Use reverse ETL plan -> preview -> approval -> execute. Connector command execution is metadata-only for complex object/array records; use typed reverse ETL records.; risk: high: sends call, engagement, or digital interaction content to Gong; requires reverse ETL approval; notes: No raw HTTP body is accepted. Object and array payloads must come from typed reverse-ETL records validated by writes.json.; flags: --content (string): The content the person looked at: maps_to=record.content, --eventId (string): The provider's unique identifier for the event used for deduplication: maps_to=record.eventId, --eventType (string): The type of the event, explaining the content and action performed. Values may be partner-aligned, for example, "link clicked", "page viewed": maps_to=record.eventType, --timestamp (string): The date and time of the event in ISO 8601 format: maps_to=record.timestamp
- Logs
  - logs list - Retrieve logs data by type and time range (/v2/logs) [intent=direct_read availability=implemented]; risk: bounded Gong JSON read; response is limited to 1 MiB and secret/download/content-shaped fields are redacted; flags: --logType (max 4096 bytes) (string): Type of logs requested.: maps_to=query.logType, --fromDateTime (max 4096 bytes) (string): The time from which to retrieve log records, in the ISO-8601 format (e.g., '2018-02-18T02:30:00-07:00' or '2018-02-18T08:00:00Z', where Z stands for UTC).: maps_to=query.fromDateTime, --toDateTime (max 4096 bytes) (string): The time until which to retrieve log records, in the ISO-8601 format (e.g., '2018-02-18T02:30:00-07:00' or '2018-02-18T08:00:00Z', where Z stands for UTC); if not specified, the lo: maps_to=query.toDateTime, --page (integer): Page number to return. Only for connectors whose declared pagination addresses pages by number (page_number, offset_limit); defaults to the first page., --page-cursor (string): Continuation token to return the next page. Use the next_cursor value from a previous page, for connectors whose declared pagination addresses pages by token (cursor, next_url, link_header).
- Coaching
  - coaching list - List all coaching metrics (/v2/coaching) [intent=direct_read availability=implemented]; risk: bounded Gong JSON read; response is limited to 1 MiB and secret/download/content-shaped fields are redacted; flags: --workspace-id (max 4096 bytes) (integer): maps_to=query.workspace-id, --manager-id (max 4096 bytes) (integer): maps_to=query.manager-id, --from (max 4096 bytes) (string): Association time filter - only the associations made after that time will be listed. The time is in the ISO-8601 format (e.g., '2018-02-18T02:30:00-07:00' or '2018-02-18T08:00:00Z': maps_to=query.from, --to (max 4096 bytes) (string): Association time filter - only the associations made after that time will be listed. The time is in the ISO-8601 format (e.g., '2018-02-18T02:30:00-07:00' or '2018-02-18T08:00:00Z': maps_to=query.to, --page (integer): Page number to return. Only for connectors whose declared pagination addresses pages by number (page_number, offset_limit); defaults to the first page., --page-cursor (string): Continuation token to return the next page. Use the next_cursor value from a previous page, for connectors whose declared pagination addresses pages by token (cursor, next_url, link_header).
- Outcomes
  - call-outcomes list - List Gong call outcomes as ETL records. [intent=etl availability=implemented stream=call_outcomes]
- Integration Settings
  - integration-settings update - administrative Gong settings or permissions mutation; requires reverse ETL approval and destructive confirmation [intent=reverse_etl availability=partial write=integration_settings]; approval: Use reverse ETL plan -> preview -> approval -> execute. Connector command execution is metadata-only for complex object/array records; use typed reverse ETL records.; risk: high: administrative Gong settings or permissions mutation; requires reverse ETL approval and destructive confirmation; notes: No raw HTTP body is accepted. Object and array payloads must come from typed reverse-ETL records validated by writes.json.; flags: --integrationTypeSettings (string): Set a list of dynamic origins for a company: maps_to=record.integrationTypeSettings
- Targets
  - targets list - List target definitions (/v2/targets) [intent=direct_read availability=implemented]; risk: bounded Gong JSON read; response is limited to 1 MiB and secret/download/content-shaped fields are redacted; flags: --workspaceId (max 4096 bytes) (string): The ID of the workspace to retrieve target definitions for. You can retrieve workspace IDs using the Workspaces API.: maps_to=query.workspaceId, --page (integer): Page number to return. Only for connectors whose declared pagination addresses pages by number (page_number, offset_limit); defaults to the first page., --page-cursor (string): Continuation token to return the next page. Use the next_cursor value from a previous page, for connectors whose declared pagination addresses pages by token (cursor, next_url, link_header).
  - targets upload-assignments - Upload target assignments (/v2/targets/{targetId}/assignments) [intent=reverse_etl availability=implemented write=upload_target_assignments]; approval: reverse ETL plan -> preview -> approval -> execute; destructive confirmation required; risk: high: uploads target assignments from a bounded project-local file path; file path/content are redacted in plans; notes: Uses typed multipart write support; no generic upload command is exposed.; flags: --targetId (string): Unique identifier for the target (up to 20 digits). Retrieve this value using the List targets API.: maps_to=record.targetId, --workspaceId (string): The ID of the workspace the target belongs to. You can retrieve workspace IDs using the Workspaces API.: maps_to=record.workspaceId, --validate-only (string): (Default: false) When true, the CSV file is fully validated but no data is uploaded.: maps_to=record.validateOnly, --assignments-file-path (string): Project-relative target assignments CSV file path to upload.: maps_to=record.assignments_file_path
- Help topics:
  - gong-auth - Use Gong access key and access key secret via credentials; never pass secrets in command text.
  - gong-writes - Gong mutations are typed reverse-ETL actions with plan, preview, approval, execute gates.
  - gong-direct-read - Gong direct reads are bounded JSON GET or typed POST operations with redaction for secret/download/content-shaped fields; call transcripts support a 16 MiB cap.

## Commands

### Inspect as a manual

```bash
pm connectors inspect gong
```

### Inspect as structured JSON

```bash
pm connectors inspect gong --json
```

## Agent Rules

- Run pm connectors inspect gong before creating credentials or plans.
- Use --json only when the caller needs structured output; use the manual for human-readable guidance.
- Never ask the user to paste secret values into chat.
- For reverse ETL writes, create a plan, show the preview, wait for explicit approval, then run with the approval token.
