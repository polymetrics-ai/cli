# Overview

Loader test fixture bundle; not a real connector.

## Auth setup

Optional bearer token via `secrets.token`.

## Streams notes

Single `widgets` stream, no pagination.

## Write actions & risks

None.

## Known limits

Fixture only; used by engine loader tests (`bundle_test.go`).
