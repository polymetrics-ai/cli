{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "PandaDoc Connection Specification",
  "type": "object",
  "required": ["api_key"],
  "properties": {
    "api_key": {
      "type": "string",
      "x-secret": true,
      "description": "PandaDoc API key, sent as the Authorization header with an 'API-Key ' prefix. Never logged."
    },
    "base_url": {
      "type": "string",
      "format": "uri",
      "default": "https://api.pandadoc.com/public/v1",
      "description": "PandaDoc API base URL override for tests or proxies."
    },
    "count": {
      "type": "string",
      "default": "100",
      "description": "Records per page (1-100)."
    },
    "max_pages": {
      "type": "string",
      "default": "0",
      "description": "Maximum pages; use 0, all, or unlimited to exhaust the stream."
    },
    "mode": {
      "type": "string",
      "description": "Runtime mode: live (default) or fixture for credential-free conformance."
    }
  }
}
