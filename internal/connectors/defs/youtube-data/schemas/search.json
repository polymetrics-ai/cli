{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "search",
  "type": "object",
  "x-primary-key": ["id"],
  "x-cursor-field": "published_at",
  "required": ["id"],
  "properties": {
    "id": { "type": "string", "description": "The matched video's id (id.videoId); this stream is scoped to type=video results only." },
    "title": { "type": ["string", "null"] },
    "published_at": { "type": ["string", "null"] }
  }
}
