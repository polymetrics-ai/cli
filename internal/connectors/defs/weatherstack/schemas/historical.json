{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "historical",
  "type": "object",
  "x-primary-key": ["id"],
  "required": ["id"],
  "properties": {
    "id": { "type": "string", "description": "Synthetic id computed as 'historical:{query}', matching legacy's fallback id (the raw API response carries no id field of its own)." },
    "location": { "type": ["object", "null"] },
    "historical": { "type": ["object", "null"] }
  }
}
