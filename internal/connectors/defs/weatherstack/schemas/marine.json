{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "marine",
  "type": "object",
  "x-primary-key": ["id"],
  "required": ["id"],
  "properties": {
    "id": { "type": "string", "description": "Synthetic id computed as 'marine:{latitude},{longitude}' (the raw API response carries no id field of its own), matching this bundle's current/historical/forecast synthetic-id convention." },
    "location": { "type": ["object", "null"] },
    "current": { "type": ["object", "null"] }
  }
}
