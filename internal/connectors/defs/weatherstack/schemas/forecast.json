{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "forecast",
  "type": "object",
  "x-primary-key": ["id"],
  "required": ["id"],
  "properties": {
    "id": { "type": "string", "description": "Synthetic id computed as 'forecast:{query}', matching legacy's fallback id (the raw API response carries no id field of its own)." },
    "location": { "type": ["object", "null"] },
    "forecast": { "type": ["object", "null"] }
  }
}
