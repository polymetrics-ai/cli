{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "reservations",
  "type": "object",
  "x-primary-key": ["id"],
  "x-cursor-field": "date",
  "required": ["id"],
  "properties": {
    "id": { "type": "string" },
    "guest_name": { "type": ["string", "null"], "description": "Legacy field, copied from guest_name when present or computed from the nested real-API guest.name field." },
    "date": { "type": ["string", "null"], "description": "YYYY-MM-DD booking date." },
    "status": { "type": ["string", "null"] }
  }
}
