{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Nebius AI Connection Specification",
  "type": "object",
  "required": ["api_key"],
  "properties": {
    "api_key": {
      "type": "string",
      "x-secret": true,
      "description": "Nebius AI Studio API key. Sent as a Bearer token on every request; never logged."
    },
    "base_url": {
      "type": "string",
      "format": "uri",
      "default": "https://api.studio.nebius.com",
      "description": "Nebius AI Studio API base URL override; defaults to production."
    },
    "limit": {
      "type": "integer",
      "default": 20,
      "description": "Page size for list endpoints (models/files/batches), sent as the `limit` query parameter. Must be between 1 and 100 (Nebius AI Studio's own accepted range)."
    }
  }
}
