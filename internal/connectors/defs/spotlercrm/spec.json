{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Spotler CRM Connection Specification",
  "type": "object",
  "required": ["api_key"],
  "properties": {
    "api_key": {
      "type": "string",
      "x-secret": true,
      "description": "Spotler CRM API key, sent as the X-API-Key header value. Never logged."
    },
    "base_url": {
      "type": "string",
      "format": "uri",
      "default": "https://api.spotlercrm.com/api/v1",
      "description": "Spotler CRM API base URL override for tests or proxies."
    },
    "mode": {
      "type": "string",
      "description": "Runtime mode: live (default) or fixture for credential-free conformance."
    }
  }
}
