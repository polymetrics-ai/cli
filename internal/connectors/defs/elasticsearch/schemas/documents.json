{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "documents",
  "type": "object",
  "x-primary-key": ["id"],
  "required": ["id"],
  "properties": {
    "id": { "type": "string", "description": "Stamped from the raw hit's _id field by hooks/elasticsearch's RecordHook; matches legacy's mapHit exactly." }
  },
  "additionalProperties": true
}
