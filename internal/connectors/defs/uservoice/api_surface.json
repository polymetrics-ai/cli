{
  "api": "UserVoice API v2 (https://api.uservoice.com)",
  "docs": "https://developer.uservoice.com/",
  "reviewed_at": "2026-07-03",
  "scope": "wave2 fan-out migration: the 1 legacy-parity read stream only (suggestions); minimal-honest api_surface per docs/migration/conventions.md section 2 / DECISIONS.md #4 - full UserVoice API-surface research (every documented endpoint) is Pass B (wave5), not this fan-out.",
  "endpoints": [
    { "method": "GET", "path": "/api/v2/suggestions", "covered_by": { "stream": "suggestions" } },
    { "method": "POST", "path": "/api/v2/suggestions", "excluded": { "category": "out_of_scope", "reason": "Pass B capability expansion; legacy implements no write path (Write returns ErrUnsupportedOperation)" } },
    { "method": "GET", "path": "/api/v2/forums", "excluded": { "category": "out_of_scope", "reason": "Pass B capability expansion; legacy does not implement a forums stream" } },
    { "method": "GET", "path": "/api/v2/users", "excluded": { "category": "out_of_scope", "reason": "Pass B capability expansion; legacy does not implement a users stream" } },
    { "method": "GET", "path": "/api/v2/comments", "excluded": { "category": "out_of_scope", "reason": "Pass B capability expansion; legacy does not implement a comments stream" } }
  ]
}
