{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "pipelines",
  "type": "object",
  "x-primary-key": ["id"],
  "required": ["id"],
  "properties": {
    "id": { "type": "string", "description": "Computed from the raw API's `_id` field, falling back to `id` to match legacy firstString." },
    "name": { "type": ["string", "null"] }
  }
}
