{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "categories",
  "type": "object",
  "x-primary-key": ["id"],
  "required": ["id"],
  "properties": {
    "id": { "type": "string", "description": "Computed from the raw API's `_id` field (computed_fields rename)." },
    "name": { "type": ["string", "null"] }
  }
}
