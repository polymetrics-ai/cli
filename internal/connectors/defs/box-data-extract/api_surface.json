{
  "api": "Box REST API 2.0 (https://api.box.com/2.0)",
  "docs": "https://developer.box.com/reference/",
  "reviewed_at": "2026-07-03",
  "scope": "the 1 legacy-parity read stream with a real live implementation (files); legacy's second catalog stream (file_text) has no live implementation in legacy either, see excluded entry below",
  "endpoints": [
    { "method": "GET", "path": "/2.0/folders/{folder_id}/items", "covered_by": { "stream": "files" } },
    { "method": "POST", "path": "/oauth2/token", "excluded": { "category": "non_data_endpoint", "reason": "OAuth2 client-credentials token exchange, not a syncable object stream" } },
    { "method": "GET", "path": "(no real endpoint)", "excluded": { "category": "out_of_scope", "reason": "legacy's file_text stream has NO live HTTP implementation at all — Read() unconditionally returns an error outside fixture mode ('box-data-extract file_text live read requires fixture mode or a safe extraction service'); there is no real Box endpoint being called to port. Omitting it from this bundle's streams.json changes no real behavior (legacy never emitted live file_text records either); see docs.md's Known limits." } }
  ]
}
