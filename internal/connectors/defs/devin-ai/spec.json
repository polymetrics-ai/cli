{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Devin AI Connection Specification",
  "type": "object",
  "required": ["org_id", "api_token"],
  "properties": {
    "api_token": {
      "type": "string",
      "x-secret": true,
      "description": "Devin service-user API key (cog_...). Used only for Bearer auth; never logged."
    },
    "org_id": {
      "type": "string",
      "description": "Devin organization id; every stream reads /v3/organizations/{org_id}/... scoped to this org."
    },
    "base_url": {
      "type": "string",
      "format": "uri",
      "default": "https://api.devin.ai",
      "description": "Devin API base URL override for tests or proxies."
    },
    "start_date": {
      "type": "string",
      "format": "date-time",
      "description": "RFC3339 lower bound; only session-derived objects created at or after this time are read."
    },
    "page_size": {
      "type": "string",
      "default": "100",
      "description": "Records per page (1-200), sent as the first query param on every stream."
    },
    "mode": {
      "type": "string",
      "description": "Runtime mode: live (default) or fixture for credential-free conformance."
    }
  }
}
