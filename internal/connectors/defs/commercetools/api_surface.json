{
  "api": "commercetools Composable Commerce HTTP API",
  "docs": "https://docs.commercetools.com/api/",
  "reviewed_at": "2026-07-03",
  "scope": "Full legacy capability-parity migration: all 3 legacy streams (customers, orders, products). Legacy is read-only with no reverse-ETL write target; this bundle stays read-only too.",
  "endpoints": [
    { "method": "GET", "path": "/{projectKey}/customers", "covered_by": { "stream": "customers" } },
    { "method": "GET", "path": "/{projectKey}/orders", "covered_by": { "stream": "orders" } },
    { "method": "GET", "path": "/{projectKey}/products", "covered_by": { "stream": "products" } }
  ]
}
