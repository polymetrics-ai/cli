{
  "base": {
    "url": "{{ config.endpoint }}",
    "user_agent": "polymetrics-go-cli",
    "auth": [
      {
        "mode": "none"
      }
    ],
    "pagination": {
      "type": "none"
    },
    "check": {
      "method": "POST",
      "path": "/"
    },
    "error_map": [
      {
        "status": 400,
        "hint": "dynamodb request rejected; check table_name/region and that the table exists"
      },
      {
        "status": 403,
        "hint": "dynamodb SigV4 signature invalid or access_key_id/secret_access_key lack access; re-run pm credentials add dynamodb"
      },
      {
        "status": 429,
        "class": "rate_limited",
        "hint": "dynamodb provisioned/on-demand throughput exceeded; the connector will retry with backoff"
      }
    ]
  },
  "streams": [
    {
      "name": "items",
      "method": "POST",
      "path": "/",
      "records": {
        "path": "Items"
      },
      "schema": "schemas/items.json"
    }
  ]
}
