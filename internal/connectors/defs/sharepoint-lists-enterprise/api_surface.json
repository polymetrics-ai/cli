{
  "api": "Microsoft Graph API v1.0 (SharePoint lists resource)",
  "docs": "https://learn.microsoft.com/en-us/graph/api/resources/list?view=graph-rest-1.0",
  "reviewed_at": "2026-07-03",
  "scope": "wave2 fan-out migration: the 2 legacy-parity read streams only (lists, list_items); legacy is read-only, so there are no write endpoints in or out of scope",
  "endpoints": [
    { "method": "GET", "path": "/sites/{site-id}/lists", "covered_by": { "stream": "lists" } },
    { "method": "GET", "path": "/sites/{site-id}/lists/{list-id}/items", "covered_by": { "stream": "list_items" } },
    { "method": "GET", "path": "/sites/{site-id}/lists/{list-id}", "excluded": { "category": "out_of_scope", "reason": "Pass B capability expansion; single-list fetch not implemented by legacy" } },
    { "method": "POST", "path": "/sites/{site-id}/lists", "excluded": { "category": "out_of_scope", "reason": "Pass B capability expansion; legacy is read-only" } },
    { "method": "GET", "path": "/sites/{site-id}/lists/{list-id}/columns", "excluded": { "category": "out_of_scope", "reason": "Pass B capability expansion; not implemented by legacy" } }
  ]
}
