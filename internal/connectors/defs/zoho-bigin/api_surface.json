{
  "api": "Zoho Bigin API v2 (https://www.zohoapis.com/bigin/v2)",
  "docs": "https://www.bigin.com/developer/docs/apis/v2/",
  "reviewed_at": "2026-07-03",
  "scope": "Tier-2 AUTH_COMPLEX repair (docs/migration/quarantine.json): the 3 legacy-parity read streams only (pipelines, records, fields); legacy is read-only, so there are no write endpoints in or out of scope",
  "endpoints": [
    { "method": "GET", "path": "/Pipelines", "covered_by": { "stream": "pipelines" } },
    { "method": "GET", "path": "/{module_name}", "covered_by": { "stream": "records" } },
    { "method": "GET", "path": "/settings/fields", "covered_by": { "stream": "fields" } },
    { "method": "GET", "path": "/Contacts", "excluded": { "category": "out_of_scope", "reason": "reachable via the generic records stream with module_name=Contacts; a dedicated stream is Pass B capability expansion" } },
    { "method": "GET", "path": "/Accounts", "excluded": { "category": "out_of_scope", "reason": "reachable via the generic records stream with module_name=Accounts; a dedicated stream is Pass B capability expansion" } },
    { "method": "POST", "path": "/{module_name}", "excluded": { "category": "out_of_scope", "reason": "legacy zoho-bigin connector is read-only; write support is Pass B capability expansion" } },
    { "method": "PUT", "path": "/{module_name}/{id}", "excluded": { "category": "out_of_scope", "reason": "legacy zoho-bigin connector is read-only; write support is Pass B capability expansion" } },
    { "method": "DELETE", "path": "/{module_name}/{id}", "excluded": { "category": "out_of_scope", "reason": "legacy zoho-bigin connector is read-only; write support is Pass B capability expansion" } },
    { "method": "GET", "path": "/org", "excluded": { "category": "non_data_endpoint", "reason": "organization metadata listing, not a syncable object stream" } }
  ]
}
