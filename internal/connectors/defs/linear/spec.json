{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Linear Connection Specification",
  "type": "object",
  "required": [],
  "properties": {
    "base_url": {
      "type": "string",
      "format": "uri",
      "default": "https://api.linear.app",
      "description": "Linear API origin URL override for tests or proxies. Defaults to https://api.linear.app; streams append /graphql with fixed GraphQL documents."
    },
    "api_key": {
      "type": "string",
      "x-secret": true,
      "description": "Linear personal API key, sent as a bare Authorization header (no Bearer prefix). Provide either api_key or access_token."
    },
    "access_token": {
      "type": "string",
      "x-secret": true,
      "description": "Linear OAuth access token, sent as a Bearer Authorization header. Provide either api_key or access_token; access_token takes priority when both are set."
    },
    "auth_type": {
      "type": "string",
      "enum": [
        "",
        "oauth",
        "oauth2.0"
      ],
      "default": "",
      "description": "Optional. Set to oauth or oauth2.0 to send api_key with a Bearer prefix instead of the default bare personal-API-key header (legacy's isOAuth config-forced-Bearer path). Not needed when access_token is set (access_token always uses Bearer regardless of this value)."
    },
    "max_pages": {
      "type": "string",
      "description": "Reserved for future configured page caps; current Linear GraphQL streams rely on command --limit and cursor exhaustion."
    }
  }
}
