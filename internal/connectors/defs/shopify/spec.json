{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Shopify Connection Specification",
  "type": "object",
  "required": [
    "shop_domain",
    "access_token"
  ],
  "properties": {
    "shop_domain": {
      "type": "string",
      "description": "Canonical Shopify Admin API host only, for example fixture-shop.myshopify.com. Custom storefront domains, schemes, paths, ports, and trailing dots are not accepted.",
      "pattern": "^[a-z0-9][a-z0-9-]*\\.myshopify\\.com$"
    },
    "access_token": {
      "type": "string",
      "description": "Shopify Admin API access token supplied through the credential store or environment, never inline in docs or fixtures.",
      "x-secret": true
    }
  }
}
