{
  "api": "Phyllo API (https://docs.getphyllo.com/)",
  "docs": "https://docs.getphyllo.com/",
  "reviewed_at": "2026-07-02",
  "scope": "wave2 fan-out migration: the 4 legacy-parity read streams only (users, accounts, profiles, social_contents); legacy conservatively reads only stable list endpoints (Phyllo's public docs are JS-backed) and ships no writes.",
  "endpoints": [
    { "method": "GET", "path": "/v1/users", "covered_by": { "stream": "users" } },
    { "method": "GET", "path": "/v1/accounts", "covered_by": { "stream": "accounts" } },
    { "method": "GET", "path": "/v1/profiles", "covered_by": { "stream": "profiles" } },
    { "method": "GET", "path": "/v1/social/contents", "covered_by": { "stream": "social_contents" } },
    { "method": "POST", "path": "/v1/users", "excluded": { "category": "out_of_scope", "reason": "legacy connector never implemented user creation; Pass B capability expansion" } },
    { "method": "GET", "path": "/v1/social/comments", "excluded": { "category": "out_of_scope", "reason": "not modeled by legacy; Pass B capability expansion" } }
  ]
}
