{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Phyllo Connection Specification",
  "type": "object",
  "required": ["client_id", "client_secret"],
  "properties": {
    "client_id": {
      "type": "string",
      "x-secret": true,
      "description": "Phyllo client ID, sent as the HTTP Basic auth username. Never logged."
    },
    "client_secret": {
      "type": "string",
      "x-secret": true,
      "description": "Phyllo client secret, sent as the HTTP Basic auth password. Never logged."
    },
    "base_url": {
      "type": "string",
      "format": "uri",
      "default": "https://api.getphyllo.com",
      "description": "Phyllo API base URL. Defaults to the production host; set explicitly to https://api.sandbox.getphyllo.com or https://api.staging.getphyllo.com to target a non-production environment."
    },
    "page_size": {
      "type": "string",
      "default": "50",
      "description": "Records per page (1-100)."
    },
    "max_pages": {
      "type": "string",
      "default": "0",
      "description": "Maximum pages; use 0, all, or unlimited to exhaust the stream."
    },
    "mode": {
      "type": "string",
      "description": "Runtime mode: live (default) or fixture for credential-free conformance."
    }
  }
}
