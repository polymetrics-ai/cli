{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Rollbar Connection Specification",
  "type": "object",
  "required": ["access_token"],
  "properties": {
    "access_token": {
      "type": "string",
      "x-secret": true,
      "description": "Rollbar project access token, sent as the X-Rollbar-Access-Token header. Never logged."
    },
    "base_url": {
      "type": "string",
      "format": "uri",
      "default": "https://api.rollbar.com",
      "description": "Rollbar API base URL override for tests or proxies."
    }
  }
}
