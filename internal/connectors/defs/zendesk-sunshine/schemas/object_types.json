{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "object_types",
  "type": "object",
  "x-primary-key": ["id"],
  "required": ["id"],
  "properties": {
    "id": { "type": "string", "description": "Mapped from the raw API's key field via a computed_fields rename." },
    "schema": { "type": ["object", "null"] },
    "created_at": { "type": ["string", "null"] },
    "updated_at": { "type": ["string", "null"] }
  }
}
