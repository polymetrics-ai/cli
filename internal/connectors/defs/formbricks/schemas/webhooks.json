{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "webhooks",
  "type": "object",
  "x-primary-key": ["id"],
  "required": ["id"],
  "properties": {
    "id": { "type": "string" },
    "url": { "type": ["string", "null"] },
    "source": { "type": ["string", "null"] },
    "environment_id": { "type": ["string", "null"], "description": "Mapped from the raw API's environmentId field." },
    "surveyIds": { "type": ["array", "null"], "description": "Raw API array of survey ids; legacy emits this key verbatim (camelCase, unrenamed) despite the rest of the schema being snake_case." },
    "triggers": { "type": ["array", "null"] },
    "created_at": { "type": ["string", "null"], "description": "Mapped from the raw API's createdAt field." },
    "updated_at": { "type": ["string", "null"], "description": "Mapped from the raw API's updatedAt field." }
  }
}
