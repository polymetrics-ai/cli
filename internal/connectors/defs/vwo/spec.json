{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "VWO Connection Specification",
  "type": "object",
  "required": ["api_key"],
  "properties": {
    "base_url": {
      "type": "string",
      "format": "uri",
      "default": "https://app.vwo.com/api/v2",
      "description": "VWO API base URL. Defaults to the public VWO API v2 host."
    },
    "api_key": {
      "type": "string",
      "x-secret": true,
      "description": "VWO API token, sent as an Authorization: Token <api_key> header."
    },
    "start_date": {
      "type": "string",
      "format": "date-time",
      "description": "Optional start_date query parameter passed through to the campaigns endpoint verbatim (RFC3339). Not used as a stateful incremental cursor by legacy."
    }
  }
}
