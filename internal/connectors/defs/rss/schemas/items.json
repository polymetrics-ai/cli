{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "items",
  "type": "object",
  "x-primary-key": ["id"],
  "x-cursor-field": "published_at",
  "required": ["id"],
  "properties": {
    "id": { "type": "string", "description": "guid, falling back to link, then title (hooks/rss/hooks.go's itemRecord, matching legacy's rss.go itemRecord fallback chain)." },
    "title": { "type": ["string", "null"] },
    "link": { "type": ["string", "null"] },
    "description": { "type": ["string", "null"] },
    "published_at": { "type": ["string", "null"], "description": "Raw <pubDate> text, verbatim (not reformatted), matching legacy exactly." }
  }
}
