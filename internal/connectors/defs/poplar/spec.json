{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Poplar Connection Specification",
  "type": "object",
  "required": ["api_token"],
  "properties": {
    "api_token": {
      "type": "string",
      "x-secret": true,
      "description": "Poplar API token, sent as a Bearer token (Authorization: Bearer <api_token>). Never logged."
    },
    "base_url": {
      "type": "string",
      "format": "uri",
      "default": "https://api.heypoplar.com/v1",
      "description": "Poplar API base URL override for tests or proxies."
    }
  }
}
