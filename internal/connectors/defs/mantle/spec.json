{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Mantle Connection Specification",
  "type": "object",
  "required": ["api_key"],
  "properties": {
    "api_key": {
      "type": "string",
      "x-secret": true,
      "description": "Mantle API key, sent as a Bearer token (Authorization: Bearer <api_key>). Never logged."
    },
    "base_url": {
      "type": "string",
      "format": "uri",
      "default": "https://api.heymantle.com",
      "description": "Mantle API base URL override for tests or proxies."
    }
  }
}
