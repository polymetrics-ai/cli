{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Employment Hero Connection Specification",
  "type": "object",
  "required": ["api_key"],
  "properties": {
    "api_key": {
      "type": "string",
      "x-secret": true,
      "description": "Employment Hero API token, sent as a Bearer token (Authorization: Bearer <api_key>). Never logged."
    },
    "base_url": {
      "type": "string",
      "format": "uri",
      "default": "https://api.employmenthero.com/api/v1",
      "description": "Employment Hero API base URL override for tests or proxies."
    },
    "organization_id": {
      "type": "string",
      "description": "Organisation id the org-scoped streams (employees, leave_requests, teams) are read from. Read the organisations stream first to discover ids."
    }
  }
}
