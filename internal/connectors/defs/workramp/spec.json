{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "WorkRamp Connection Specification",
  "type": "object",
  "required": ["api_key"],
  "properties": {
    "api_key": {
      "type": "string",
      "x-secret": true,
      "description": "WorkRamp API key, sent as a Bearer token on every request. Never logged."
    },
    "base_url": {
      "type": "string",
      "format": "uri",
      "default": "https://api.workramp.com",
      "description": "WorkRamp API base URL. Defaults to the production endpoint; override for test proxies."
    },
    "page_size": {
      "type": "string",
      "default": "100",
      "description": "Records per page (1-500)."
    },
    "max_pages": {
      "type": "string",
      "default": "1",
      "description": "Maximum pages to read; matches legacy's default of 1 page when unset."
    },
    "mode": {
      "type": "string",
      "description": "Runtime mode: live (default) or fixture for credential-free conformance."
    }
  }
}
