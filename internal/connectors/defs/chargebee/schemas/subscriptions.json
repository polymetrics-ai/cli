{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "subscriptions",
  "type": "object",
  "x-primary-key": ["id"],
  "x-cursor-field": "updated_at",
  "required": ["id"],
  "properties": {
    "id": { "type": "string" },
    "customer_id": { "type": ["string", "null"] },
    "plan_id": { "type": ["string", "null"] },
    "status": { "type": ["string", "null"] },
    "currency_code": { "type": ["string", "null"] },
    "plan_quantity": { "type": ["string", "null"], "description": "Integer, stringified by computed_fields (see docs.md Known limits)." },
    "plan_amount": { "type": ["string", "null"], "description": "Integer, stringified by computed_fields (see docs.md Known limits)." },
    "current_term_start": { "type": ["string", "null"], "description": "Unix seconds, stringified by computed_fields (see docs.md Known limits)." },
    "current_term_end": { "type": ["string", "null"], "description": "Unix seconds, stringified by computed_fields (see docs.md Known limits)." },
    "created_at": { "type": ["string", "null"], "description": "Unix seconds, stringified by computed_fields (see docs.md Known limits)." },
    "started_at": { "type": ["string", "null"], "description": "Unix seconds, stringified by computed_fields (see docs.md Known limits)." },
    "updated_at": { "type": ["string", "null"], "description": "Unix seconds, stringified by computed_fields (see docs.md Known limits)." },
    "deleted": { "type": ["string", "null"], "description": "Boolean, stringified by computed_fields (see docs.md Known limits)." }
  }
}
