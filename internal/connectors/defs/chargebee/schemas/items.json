{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "items",
  "type": "object",
  "x-primary-key": ["id"],
  "x-cursor-field": "updated_at",
  "required": ["id"],
  "properties": {
    "id": { "type": "string" },
    "name": { "type": ["string", "null"] },
    "type": { "type": ["string", "null"] },
    "item_family_id": { "type": ["string", "null"] },
    "status": { "type": ["string", "null"] },
    "is_shippable": { "type": ["string", "null"], "description": "Boolean, stringified by computed_fields (see docs.md Known limits)." },
    "enabled_for_checkout": { "type": ["string", "null"], "description": "Boolean, stringified by computed_fields (see docs.md Known limits)." },
    "created_at": { "type": ["string", "null"], "description": "Unix seconds, stringified by computed_fields (see docs.md Known limits)." },
    "updated_at": { "type": ["string", "null"], "description": "Unix seconds, stringified by computed_fields (see docs.md Known limits)." }
  }
}
