{
  "base": {
    "url": "{{ config.base_url }}",
    "user_agent": "polymetrics-go-cli",
    "auth": [
      {
        "mode": "basic",
        "username": "{{ secrets.identifier }}",
        "password": "{{ secrets.key }}"
      }
    ],
    "check": {
      "method": "GET",
      "path": "/v1/website/{{ config.website_id }}"
    },
    "error_map": [
      {
        "status": 401,
        "class": "auth",
        "hint": "Crisp credentials are missing or invalid; run pm credentials add <name> --connector crisp with identifier/key from --from-env or --value-stdin and website_id from --config."
      },
      {
        "status": 403,
        "class": "auth",
        "hint": "Crisp credential scope does not allow this operation."
      },
      {
        "status": 404,
        "class": "not_found",
        "hint": "Crisp website or resource was not found."
      },
      {
        "status": 429,
        "class": "rate_limited",
        "hint": "Crisp API rate limit exceeded; retry after the provider window."
      }
    ]
  },
  "streams": []
}
