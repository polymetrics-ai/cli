{
  "api": "Ticket Tailor REST API v1",
  "docs": "https://developers.tickettailor.com/",
  "reviewed_at": "2026-07-03",
  "scope": "wave2 fan-out migration: the 3 legacy-parity read streams only (events, orders, issued_tickets); Ticket Tailor's write/mutation endpoints and other resources (venues, ticket_types, discount_codes, box_office) are out of scope (legacy itself ships no writes; capabilities.write is false).",
  "endpoints": [
    { "method": "GET", "path": "/v1/events", "covered_by": { "stream": "events" } },
    { "method": "GET", "path": "/v1/orders", "covered_by": { "stream": "orders" } },
    { "method": "GET", "path": "/v1/issued_tickets", "covered_by": { "stream": "issued_tickets" } },
    { "method": "GET", "path": "/v1/venues", "excluded": { "category": "out_of_scope", "reason": "not modeled by legacy; Pass B capability expansion" } },
    { "method": "GET", "path": "/v1/ticket_types", "excluded": { "category": "out_of_scope", "reason": "not modeled by legacy; Pass B capability expansion" } },
    { "method": "GET", "path": "/v1/discount_codes", "excluded": { "category": "out_of_scope", "reason": "not modeled by legacy; Pass B capability expansion" } },
    { "method": "POST", "path": "/v1/box_office/checkin", "excluded": { "category": "out_of_scope", "reason": "legacy connector never implemented writes; Pass B capability expansion" } }
  ]
}
