{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
    "api_key": {
      "description": "Ashby API key. Provide from an environment variable or stdin; never inline in prompts or docs.",
      "type": "string",
      "x-secret": true
    },
    "base_url": {
      "default": "https://api.ashbyhq.com",
      "description": "Ashby API base URL; override only for local tests.",
      "type": "string"
    },
    "max_pages": {
      "default": "0",
      "description": "Maximum pages to read per stream. Defaults to 0 for an exhaustive read; 0, all, and unlimited are equivalent.",
      "type": "string"
    },
    "mode": {
      "description": "Set to fixture for credential-free native tests.",
      "type": "string"
    },
    "page_size": {
      "default": "100",
      "description": "Per-page body limit for Ashby list endpoints; bounded to 1..100 by the native connector.",
      "type": "string"
    }
  },
  "required": [
    "api_key"
  ],
  "title": "Ashby Connection Specification",
  "type": "object"
}
