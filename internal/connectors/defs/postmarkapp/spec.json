{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Postmark App Connection Specification",
  "type": "object",
  "required": ["X-Postmark-Server-Token"],
  "properties": {
    "X-Postmark-Server-Token": {
      "type": "string",
      "x-secret": true,
      "description": "Postmark server token, sent as the X-Postmark-Server-Token header for message-resource streams (outbound_messages, inbound_messages). Never logged."
    },
    "base_url": {
      "type": "string",
      "format": "uri",
      "default": "https://api.postmarkapp.com",
      "description": "Postmark API base URL override for tests or proxies."
    },
    "mode": {
      "type": "string",
      "description": "Runtime mode: live (default) or fixture for credential-free conformance."
    }
  }
}
