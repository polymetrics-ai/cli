{
  "api": "signNow REST API (https://api.signnow.com)",
  "docs": "https://docs.signnow.com/",
  "reviewed_at": "2026-07-03",
  "scope": "wave2 fan-out migration: the 3 legacy-parity read streams only; the full signNow surface (folders, invites, embedded signing, teams) is out of scope until Pass B",
  "endpoints": [
    {
      "method": "GET",
      "path": "/document",
      "covered_by": {
        "stream": "documents"
      }
    },
    {
      "method": "GET",
      "path": "/template",
      "covered_by": {
        "stream": "templates"
      }
    },
    {
      "method": "GET",
      "path": "/user",
      "covered_by": {
        "stream": "users"
      }
    },
    {
      "method": "POST",
      "path": "/document",
      "excluded": {
        "category": "out_of_scope",
        "reason": "/document is a mutation endpoint; this bundle has no reviewed write record schema, approval risk, or legacy write contract for that operation"
      }
    },
    {
      "method": "GET",
      "path": "/folder",
      "excluded": {
        "category": "out_of_scope",
        "reason": "/folder is an additional documented read resource not implemented by legacy; this bundle does not declare its stream schema, pagination contract, or fixtures"
      }
    },
    {
      "method": "POST",
      "path": "/document/{id}/invite",
      "excluded": {
        "category": "out_of_scope",
        "reason": "/document/{id}/invite is a mutation endpoint; this bundle has no reviewed write record schema, approval risk, or legacy write contract for that operation"
      }
    }
  ]
}
