{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "problematic_urls",
  "type": "object",
  "x-primary-key": ["scraping_job_id", "url"],
  "required": ["url"],
  "properties": {
    "url": { "type": "string" },
    "type": { "type": ["string", "null"], "description": "Either 'empty' or 'failed'." },
    "scraping_job_id": { "type": ["string", "null"], "description": "Stamped by fan_out with the scraping_job_ids config entry that produced this record." }
  }
}
