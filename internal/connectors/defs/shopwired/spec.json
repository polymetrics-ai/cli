{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ShopWired Connection Specification",
  "type": "object",
  "required": ["api_key"],
  "properties": {
    "api_key": {
      "type": "string",
      "x-secret": true,
      "description": "ShopWired API key, sent as the X-API-Key header. Never logged."
    },
    "base_url": {
      "type": "string",
      "format": "uri",
      "default": "https://api.shopwired.co.uk",
      "description": "ShopWired API base URL override for tests or proxies."
    }
  }
}
