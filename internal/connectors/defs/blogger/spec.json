{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Blogger Connection Specification",
  "type": "object",
  "required": ["client_id", "client_secret", "client_refresh_token", "blog_id"],
  "properties": {
    "client_id": {
      "type": "string",
      "x-secret": true,
      "description": "Google OAuth 2.0 client ID for the refresh-token grant. Used only in the token-request form; never logged."
    },
    "client_secret": {
      "type": "string",
      "x-secret": true,
      "description": "Google OAuth 2.0 client secret. Used only in the token-request form; never logged."
    },
    "client_refresh_token": {
      "type": "string",
      "x-secret": true,
      "description": "Long-lived Google OAuth 2.0 refresh token. Exchanged for a short-lived access token at token_url; never logged. The 3-legged consent/acquisition dance is out of scope for this connector (credentials layer already owns it)."
    },
    "blog_id": {
      "type": "string",
      "description": "The Blogger blog id to read (blogs/{blog_id}/... resources)."
    },
    "base_url": {
      "type": "string",
      "format": "uri",
      "default": "https://www.googleapis.com/blogger/v3",
      "description": "Blogger API base URL override for tests or proxies."
    },
    "token_url": {
      "type": "string",
      "format": "uri",
      "default": "https://oauth2.googleapis.com/token",
      "description": "Google OAuth 2.0 token endpoint override. MUST be https in production; the hook fails closed on a non-https or unparseable value to prevent exfiltrating the refresh token to an attacker-chosen endpoint."
    },
    "page_size": {
      "type": "string",
      "default": "100",
      "description": "Records per page (1-500, maxResults) for paginated streams (posts, pages, comments)."
    }
  }
}
