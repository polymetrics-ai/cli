{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "delegates",
  "type": "object",
  "x-primary-key": ["delegate_email"],
  "required": ["delegate_email"],
  "properties": {
    "delegate_email": { "type": "string", "description": "Renamed from raw delegateEmail; also this stream's primary key (Gmail's Delegate resource has no separate id field)." },
    "verification_status": { "type": ["string", "null"] }
  }
}
