{
  "name": "gmail",
  "display_name": "Gmail",
  "description": "Reads Gmail messages, threads, drafts, and labels via the Google OAuth 2.0 refresh-token grant.",
  "integration_type": "api",
  "docs_url": "https://developers.google.com/gmail/api/reference/rest",
  "release_stage": "ga",
  "capabilities": {
    "check": true,
    "read": true,
    "write": false,
    "query": false,
    "cdc": false,
    "dynamic_schema": false
  },
  "batch": {
    "read_page_size": 100
  },
  "risk": {
    "read": "external Gmail API read of message/thread/draft/label metadata",
    "approval": "none; read-only, no reverse-ETL write surface"
  },
  "conformance": {
    "skip_dynamic": true,
    "reason": "custom-auth-only (mode:custom AuthHook, no when-gated non-custom fallback); hook-covered, proven live by internal/connectors/paritytest/gmail (the AuthHook's real OAuth2 refresh-grant path) -- conformance's synthetic config has no real token_url and cannot exercise mode:custom auth at all, so every auth-resolving dynamic check would otherwise fail identically and uninformatively"
  }
}
