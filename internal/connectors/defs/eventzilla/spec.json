{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Eventzilla Connection Specification",
  "type": "object",
  "required": ["api_key"],
  "properties": {
    "api_key": {
      "type": "string",
      "x-secret": true,
      "description": "Eventzilla API key, sent as the x-api-key request header. Never logged."
    },
    "base_url": {
      "type": "string",
      "format": "uri",
      "default": "https://www.eventzillaapi.net/api/v2",
      "description": "Eventzilla API base URL override for tests or proxies."
    },
    "mode": {
      "type": "string",
      "description": "Runtime mode: live (default) or fixture for credential-free conformance."
    }
  }
}
