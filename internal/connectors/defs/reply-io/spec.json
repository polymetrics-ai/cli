{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Reply.io Connection Specification",
  "type": "object",
  "required": ["api_key"],
  "properties": {
    "api_key": {
      "type": "string",
      "x-secret": true,
      "description": "Reply.io API key, sent as the X-Api-Key header. Never logged."
    },
    "base_url": {
      "type": "string",
      "format": "uri",
      "default": "https://api.reply.io/v1",
      "description": "Reply.io API base URL override for tests or proxies."
    },
    "updated_after": {
      "type": "string",
      "description": "Optional updated-after filter applied to every stream's request, matching legacy's config-driven query filter."
    },
    "created_after": {
      "type": "string",
      "description": "Optional created-after filter applied to every stream's request."
    },
    "email": {
      "type": "string",
      "description": "Optional email filter applied to every stream's request."
    },
    "status": {
      "type": "string",
      "description": "Optional status filter applied to every stream's request."
    }
  }
}
