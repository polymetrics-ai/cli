{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "New York Times Connection Specification",
  "type": "object",
  "required": ["api_key"],
  "properties": {
    "api_key": {
      "type": "string",
      "x-secret": true,
      "description": "NYTimes Developer API key. Sent as the api-key query parameter; never logged."
    },
    "base_url": {
      "type": "string",
      "format": "uri",
      "default": "https://api.nytimes.com/svc",
      "description": "NYTimes API base URL override for tests or proxies."
    },
    "period": {
      "type": "string",
      "default": "7",
      "description": "Most Popular period in days: 1, 7, or 30."
    },
    "mode": {
      "type": "string",
      "description": "Runtime mode: live (default) or fixture for credential-free conformance."
    }
  }
}
