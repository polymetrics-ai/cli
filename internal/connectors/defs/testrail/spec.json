{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "TestRail Connection Specification",
  "type": "object",
  "required": ["username", "password"],
  "properties": {
    "username": {
      "type": "string",
      "description": "TestRail username (email), sent as the Basic auth username."
    },
    "password": {
      "type": "string",
      "x-secret": true,
      "description": "TestRail password or API key, sent as the Basic auth password. Never logged."
    },
    "base_url": {
      "type": "string",
      "format": "uri",
      "default": "https://example.testrail.io",
      "description": "TestRail instance base URL override for tests or proxies."
    }
  }
}
