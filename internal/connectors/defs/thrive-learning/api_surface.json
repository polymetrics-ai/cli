{
  "api": "Thrive Learning API (https://developers.thrivelearning.com/)",
  "docs": "https://developers.thrivelearning.com/",
  "reviewed_at": "2026-07-03",
  "scope": "wave2 fan-out migration: the 3 legacy-parity read streams only (users, content, completions); Thrive Learning's remaining surface (courses/pathways, groups, certificates) is out of scope for this migration (legacy itself never implemented them)",
  "endpoints": [
    { "method": "GET", "path": "/users", "covered_by": { "stream": "users" } },
    { "method": "GET", "path": "/content", "covered_by": { "stream": "content" } },
    { "method": "GET", "path": "/completions", "covered_by": { "stream": "completions" } },
    { "method": "GET", "path": "/courses", "excluded": { "category": "out_of_scope", "reason": "legacy connector never implemented this stream; Pass B capability expansion" } },
    { "method": "GET", "path": "/groups", "excluded": { "category": "out_of_scope", "reason": "legacy connector never implemented this stream; Pass B capability expansion" } },
    { "method": "GET", "path": "/certificates", "excluded": { "category": "out_of_scope", "reason": "legacy connector never implemented this stream; Pass B capability expansion" } }
  ]
}
