{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "SimFin Connection Specification",
  "type": "object",
  "required": ["api_key"],
  "properties": {
    "api_key": {
      "type": "string",
      "x-secret": true,
      "description": "SimFin API key, sent as the api-key query parameter on every request. Never logged."
    },
    "base_url": {
      "type": "string",
      "format": "uri",
      "default": "https://backend.simfin.com",
      "description": "SimFin API base URL override for tests or proxies."
    }
  }
}
