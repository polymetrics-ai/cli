{
  "record": {
    "schema": "public",
    "table": "customers",
    "values": [
      { "name": "id", "value_int": 42 },
      { "name": "email", "value_string": "customer@example.invalid" }
    ],
    "keys": [
      { "name": "id", "value_int": 42 }
    ]
  },
  "expect": {
    "sql_template": "MERGE INTO \"public\".\"customers\" AS target USING (VALUES ($1, $2)) AS source (\"id\", \"email\") ON target.\"id\" = source.\"id\" WHEN MATCHED THEN UPDATE SET \"email\" = source.\"email\" WHEN NOT MATCHED THEN INSERT (\"id\", \"email\") VALUES (source.\"id\", source.\"email\")",
    "args_redacted": true
  }
}
