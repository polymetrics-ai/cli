{
  "name": "postgres",
  "display_name": "PostgreSQL",
  "description": "Reads PostgreSQL tables: discovers schemas/columns from information_schema, snapshots tables, supports cursor-incremental reads on a configurable cursor column, and consumes logical-replication CDC with source-bound durable LSN checkpoints. Read-only source.",
  "integration_type": "database",
  "release_stage": "ga",
  "capabilities": {
    "check": true,
    "read": true,
    "write": false,
    "query": false,
    "cdc": true,
    "dynamic_schema": true
  },
  "risk": {
    "read": "low",
    "write": "n/a (read-only source)",
    "approval": "none required for read-only sync"
  }
}
