{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Bahmni (bahmni-docker) Connection Specification",
  "type": "object",
  "required": [
    "base_url",
    "username",
    "password"
  ],
  "properties": {
    "base_url": {
      "type": "string",
      "format": "uri",
      "default": "http://localhost/openmrs",
      "description": "Base URL of the local bahmni-docker OpenMRS instance, e.g. http://localhost/openmrs."
    },
    "username": {
      "type": "string",
      "description": "OpenMRS username for HTTP Basic authentication."
    },
    "password": {
      "type": "string",
      "x-secret": true,
      "description": "OpenMRS password for HTTP Basic authentication; never logged or printed."
    },
    "patient_query": {
      "type": "string",
      "description": "Identifier or name search term used to enumerate the patients stream (OpenMRS patient search q)."
    },
    "patient_uuid": {
      "type": "string",
      "description": "Patient UUID context used to scope patient-linked streams (encounters, observations, visits, orders, lab results, diagnoses)."
    }
  }
}
