{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "search",
  "type": "object",
  "x-primary-key": ["url"],
  "x-cursor-field": "published_date",
  "required": ["url"],
  "properties": {
    "url": { "type": "string" },
    "title": { "type": ["string", "null"] },
    "content": { "type": ["string", "null"] },
    "engine": { "type": ["string", "null"] },
    "engines": { "type": ["array", "string", "null"], "description": "The raw API's engines[] array, passed through unjoined (see docs.md Known limits: the engine's declarative dialect has no array-join filter, unlike legacy's comma-joined string)." },
    "score": { "type": ["number", "null"] },
    "category": { "type": ["string", "null"] },
    "published_date": { "type": ["string", "null"], "description": "Mapped from the raw API's publishedDate field; not every result carries it." },
    "thumbnail": { "type": ["string", "null"] }
  }
}
