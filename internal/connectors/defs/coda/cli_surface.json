{
  "tagline": "Run Coda's declared typed write actions.",
  "usage": "pm coda <command> [flags]",
  "groups": [
    {
      "id": "reverse_etl",
      "title": "Reverse ETL writes",
      "commands": [
        "create page apply",
        "delete page apply",
        "delete row apply",
        "delete rows apply",
        "push button apply",
        "update page apply",
        "update row apply",
        "upsert rows apply"
      ]
    }
  ],
  "commands": [
    {
      "path": "create page apply",
      "summary": "Typed action create_page",
      "intent": "reverse_etl",
      "availability": "partial",
      "write": "create_page",
      "flags": [],
      "risk": "creates a new page in the configured doc; requires Doc Maker access in the workspace, queued for async processing (202)",
      "approval": "Blocked pending a faithful CLI record binding: declaration-pending: typed action path uses unrecognized placeholder {{ config.doc_id }}.",
      "notes": "Generated from the connector-owned typed action; declaration-pending: typed action path uses unrecognized placeholder {{ config.doc_id }}.",
      "examples": [
        "pm coda create page apply --plan <plan-name>"
      ]
    },
    {
      "path": "delete page apply",
      "summary": "Typed action delete_page",
      "intent": "reverse_etl",
      "availability": "partial",
      "write": "delete_page",
      "flags": [
        {
          "name": "page-id",
          "type": "string",
          "summary": "Required page_id record field.",
          "maps_to": "record.page_id",
          "required": true
        }
      ],
      "risk": "permanently removes a page (and its subpages/content) from the doc; irreversible, queued for async processing (202)",
      "approval": "Blocked pending a faithful CLI record binding: declaration-pending: typed action path uses unrecognized placeholder {{ config.doc_id }}.",
      "notes": "Generated from the connector-owned typed action; declaration-pending: typed action path uses unrecognized placeholder {{ config.doc_id }}.",
      "examples": [
        "pm coda delete page apply --plan <plan-name>"
      ]
    },
    {
      "path": "delete row apply",
      "summary": "Typed action delete_row",
      "intent": "reverse_etl",
      "availability": "partial",
      "write": "delete_row",
      "flags": [
        {
          "name": "row-id",
          "type": "string",
          "summary": "Required row_id record field.",
          "maps_to": "record.row_id",
          "required": true
        },
        {
          "name": "table-id",
          "type": "string",
          "summary": "Required table_id record field.",
          "maps_to": "record.table_id",
          "required": true
        }
      ],
      "risk": "permanently removes a row from a Coda table; irreversible, queued for async processing (202)",
      "approval": "Blocked pending a faithful CLI record binding: declaration-pending: typed action path uses unrecognized placeholder {{ config.doc_id }}.",
      "notes": "Generated from the connector-owned typed action; declaration-pending: typed action path uses unrecognized placeholder {{ config.doc_id }}.",
      "examples": [
        "pm coda delete row apply --plan <plan-name>"
      ]
    },
    {
      "path": "delete rows apply",
      "summary": "Typed action delete_rows",
      "intent": "reverse_etl",
      "availability": "partial",
      "write": "delete_rows",
      "flags": [
        {
          "name": "row-ids",
          "type": "json",
          "summary": "Required rowIds record field.",
          "maps_to": "record.rowIds",
          "required": true
        },
        {
          "name": "table-id",
          "type": "string",
          "summary": "Required table_id record field.",
          "maps_to": "record.table_id",
          "required": true
        }
      ],
      "risk": "permanently removes multiple rows from a Coda table in one request; irreversible, queued for async processing (202)",
      "approval": "Blocked pending a faithful CLI record binding: declaration-pending: typed action path uses unrecognized placeholder {{ config.doc_id }}.",
      "notes": "Generated from the connector-owned typed action; declaration-pending: typed action path uses unrecognized placeholder {{ config.doc_id }}.",
      "examples": [
        "pm coda delete rows apply --plan <plan-name>"
      ]
    },
    {
      "path": "push button apply",
      "summary": "Typed action push_button",
      "intent": "reverse_etl",
      "availability": "partial",
      "write": "push_button",
      "flags": [
        {
          "name": "column-id",
          "type": "string",
          "summary": "Required column_id record field.",
          "maps_to": "record.column_id",
          "required": true
        },
        {
          "name": "row-id",
          "type": "string",
          "summary": "Required row_id record field.",
          "maps_to": "record.row_id",
          "required": true
        },
        {
          "name": "table-id",
          "type": "string",
          "summary": "Required table_id record field.",
          "maps_to": "record.table_id",
          "required": true
        }
      ],
      "risk": "pushes a button on a row; the underlying button can perform ANY action the doc's formulas define, including writes to other tables and Pack actions outside this connector's declared surface — high blast-radius, approval required",
      "approval": "Blocked pending a faithful CLI record binding: declaration-pending: typed action path uses unrecognized placeholder {{ config.doc_id }}.",
      "notes": "Generated from the connector-owned typed action; declaration-pending: typed action path uses unrecognized placeholder {{ config.doc_id }}.",
      "examples": [
        "pm coda push button apply --plan <plan-name>"
      ]
    },
    {
      "path": "update page apply",
      "summary": "Typed action update_page",
      "intent": "reverse_etl",
      "availability": "partial",
      "write": "update_page",
      "flags": [
        {
          "name": "page-id",
          "type": "string",
          "summary": "Required page_id record field.",
          "maps_to": "record.page_id",
          "required": true
        }
      ],
      "risk": "renames, hides, or restyles an existing page; renaming/re-iconing requires Doc Maker access in the workspace, queued for async processing (202)",
      "approval": "Blocked pending a faithful CLI record binding: declaration-pending: typed action path uses unrecognized placeholder {{ config.doc_id }}.",
      "notes": "Generated from the connector-owned typed action; declaration-pending: typed action path uses unrecognized placeholder {{ config.doc_id }}.",
      "examples": [
        "pm coda update page apply --plan <plan-name>"
      ]
    },
    {
      "path": "update row apply",
      "summary": "Typed action update_row",
      "intent": "reverse_etl",
      "availability": "partial",
      "write": "update_row",
      "flags": [],
      "risk": "overwrites cell values on an existing row; queued for async processing (202) and generally applied within seconds",
      "approval": "Blocked pending a faithful CLI record binding: declaration-pending: the closed CLI flag set cannot faithfully represent required record field row.cells.0.value.",
      "notes": "Generated from the connector-owned typed action; declaration-pending: the closed CLI flag set cannot faithfully represent required record field row.cells.0.value.",
      "examples": [
        "pm coda update row apply --plan <plan-name>"
      ]
    },
    {
      "path": "upsert rows apply",
      "summary": "Typed action upsert_rows",
      "intent": "reverse_etl",
      "availability": "partial",
      "write": "upsert_rows",
      "flags": [],
      "risk": "inserts new rows, or upserts existing ones when keyColumns is set, into a Coda table; queued for async processing (202) and generally applied within seconds",
      "approval": "Blocked pending a faithful CLI record binding: declaration-pending: the closed CLI flag set cannot faithfully represent required record field rows.0.cells.0.value.",
      "notes": "Generated from the connector-owned typed action; declaration-pending: the closed CLI flag set cannot faithfully represent required record field rows.0.cells.0.value.",
      "examples": [
        "pm coda upsert rows apply --plan <plan-name>"
      ]
    }
  ]
}
