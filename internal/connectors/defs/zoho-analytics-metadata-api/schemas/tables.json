{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "tables",
  "type": "object",
  "x-primary-key": ["id"],
  "required": ["id"],
  "properties": {
    "id": { "type": "string" },
    "name": { "type": ["string", "null"] },
    "viewId": { "type": ["string", "null"], "description": "Raw Zoho Analytics view/table id field (legacy's mapView is shared by both the views and tables streams). See docs.md Known limits for the id/name coalesce this bundle does not reproduce." },
    "viewName": { "type": ["string", "null"] },
    "tableId": { "type": ["string", "null"] },
    "tableName": { "type": ["string", "null"] }
  }
}
