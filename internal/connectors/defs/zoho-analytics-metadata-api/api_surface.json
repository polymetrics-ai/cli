{
  "api": "Zoho Analytics Metadata API v2 (https://analyticsapi.zoho.com/restapi/v2)",
  "docs": "https://www.zoho.com/analytics/api/v2/",
  "reviewed_at": "2026-07-03",
  "scope": "Tier-2 AUTH_COMPLEX repair (docs/migration/quarantine.json): the 3 legacy-parity read streams only (workspaces, views, tables); legacy is read-only, so there are no write endpoints in or out of scope",
  "endpoints": [
    { "method": "GET", "path": "/workspaces", "covered_by": { "stream": "workspaces" } },
    { "method": "GET", "path": "/views", "covered_by": { "stream": "views" } },
    { "method": "GET", "path": "/tables", "covered_by": { "stream": "tables" } },
    { "method": "GET", "path": "/workspaces/{workspaceId}/views", "excluded": { "category": "out_of_scope", "reason": "Pass B capability expansion" } },
    { "method": "GET", "path": "/views/{viewId}/columns", "excluded": { "category": "out_of_scope", "reason": "Pass B capability expansion" } },
    { "method": "POST", "path": "/workspaces", "excluded": { "category": "out_of_scope", "reason": "legacy zoho-analytics-metadata-api connector is read-only; write support is Pass B capability expansion" } },
    { "method": "DELETE", "path": "/workspaces/{workspaceId}", "excluded": { "category": "out_of_scope", "reason": "legacy zoho-analytics-metadata-api connector is read-only; write support is Pass B capability expansion" } },
    { "method": "GET", "path": "/orgs", "excluded": { "category": "non_data_endpoint", "reason": "organization metadata listing, not a syncable object stream" } }
  ]
}
