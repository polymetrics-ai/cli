{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Confluence Connection Specification",
  "type": "object",
  "required": ["base_url", "email", "api_token"],
  "properties": {
    "base_url": {
      "type": "string",
      "format": "uri",
      "description": "Your Confluence Cloud site's full API base URL, including the /wiki/api/v2 path (e.g. https://mysite.atlassian.net/wiki/api/v2)."
    },
    "email": {
      "type": "string",
      "description": "Atlassian account email used as the HTTP Basic auth username."
    },
    "api_token": {
      "type": "string",
      "x-secret": true,
      "description": "Atlassian API token, used as the HTTP Basic auth password. Never logged."
    },
    "mode": {
      "type": "string",
      "description": "Runtime mode: live (default) or fixture for credential-free conformance."
    }
  }
}
