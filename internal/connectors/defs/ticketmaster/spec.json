{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Ticketmaster Connection Specification",
  "type": "object",
  "required": ["api_key"],
  "properties": {
    "api_key": {
      "type": "string",
      "x-secret": true,
      "description": "Ticketmaster Discovery API key, sent as the apikey query parameter. Never logged."
    },
    "base_url": {
      "type": "string",
      "format": "uri",
      "default": "https://app.ticketmaster.com/discovery/v2",
      "description": "Ticketmaster Discovery API base URL override for tests or proxies."
    },
    "keyword": {
      "type": "string",
      "description": "Optional keyword search filter applied to the events stream."
    },
    "country_code": {
      "type": "string",
      "description": "Optional ISO country code filter applied to the events stream."
    },
    "locale": {
      "type": "string",
      "description": "Optional locale filter applied to the events stream."
    }
  }
}
