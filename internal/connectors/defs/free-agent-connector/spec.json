{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
    "client_id": {
      "description": "FreeAgent OAuth client ID.",
      "type": "string",
      "x-secret": true
    },
    "client_refresh_token_2": {
      "description": "FreeAgent OAuth refresh token.",
      "type": "string",
      "x-secret": true
    },
    "client_secret": {
      "description": "FreeAgent OAuth client secret.",
      "type": "string",
      "x-secret": true
    },
    "updated_since": {
      "description": "Optional FreeAgent update lower bound.",
      "format": "date-time",
      "type": "string"
    }
  },
  "required": [
    "client_id",
    "client_secret",
    "client_refresh_token_2"
  ],
  "title": "FreeAgent Connection Specification",
  "type": "object",
  "x-reject-unknown-config": true
}
