{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Shippo Connection Specification",
  "type": "object",
  "required": ["api_token"],
  "properties": {
    "api_token": {
      "type": "string",
      "x-secret": true,
      "description": "Shippo API token, sent as an Authorization header (Authorization: ShippoToken <api_token>). Never logged."
    },
    "base_url": {
      "type": "string",
      "format": "uri",
      "default": "https://api.goshippo.com",
      "description": "Shippo API base URL override for tests or proxies."
    }
  }
}
