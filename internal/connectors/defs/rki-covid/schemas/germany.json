{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "germany",
  "type": "object",
  "x-primary-key": ["id"],
  "required": ["id"],
  "properties": {
    "id": { "type": "string", "description": "Static literal 'germany' (computed_fields) — the raw API's whole-object response has no natural id field, matching legacy's mapRecord fallback (id/ags/abbreviation/name/date all absent -> id = stream name)." },
    "cases": { "type": ["integer", "null"] },
    "deaths": { "type": ["integer", "null"] },
    "recovered": { "type": ["integer", "null"] },
    "weekIncidence": { "type": ["number", "null"] },
    "casesPer100k": { "type": ["number", "null"] },
    "casesPerWeek": { "type": ["integer", "null"] },
    "stream": { "type": "string", "description": "Static-literal computed_field naming which stream this record came from, matching legacy's derived marker field." }
  }
}
