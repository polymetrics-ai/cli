{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "SavvyCal Connection Specification",
  "type": "object",
  "required": ["api_key"],
  "properties": {
    "api_key": {
      "type": "string",
      "x-secret": true,
      "description": "SavvyCal API key, sent as a Bearer token (Authorization: Bearer <api_key>). Never logged."
    },
    "base_url": {
      "type": "string",
      "format": "uri",
      "default": "https://api.savvycal.com",
      "description": "SavvyCal API base URL override for tests or proxies."
    },
    "page_size": {
      "type": "string",
      "default": "100",
      "description": "Records requested per page (legacy's per_page query param)."
    }
  }
}
