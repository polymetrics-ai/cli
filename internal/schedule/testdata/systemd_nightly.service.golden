[Unit]
Description=pm schedule nightly-leads (flow: likely-customers)
After=network.target

[Service]
Type=oneshot
ExecStart=/usr/local/bin/pm schedule fire nightly-leads --authorization auth_0123456789abcdef --json

[Install]
WantedBy=default.target
