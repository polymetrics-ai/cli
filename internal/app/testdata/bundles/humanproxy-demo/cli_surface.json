{
  "tagline": "Human Proxy Demo fixture commands.",
  "usage": "pm humanproxy-demo <command> [flags]",
  "groups": [
    {
      "id": "writes",
      "title": "Reverse ETL writes",
      "commands": ["vote", "profile sync"]
    }
  ],
  "global_flags": [
    { "name": "credential", "type": "string", "summary": "Credential name to use." },
    { "name": "config", "type": "string_array", "summary": "Connector config override as key=value." },
    { "name": "json", "type": "boolean", "summary": "Emit machine-readable JSON output." },
    { "name": "preview", "type": "boolean", "summary": "Preview a reverse-ETL write without executing it." },
    { "name": "approval-token-stdin", "type": "boolean", "summary": "Read the approval token as one bounded line from standard input." },
    { "name": "confirm", "type": "string", "summary": "Typed confirmation for destructive reverse-ETL writes." }
  ],
  "commands": [
    {
      "path": "vote",
      "summary": "Cast a single vote as the authenticated human.",
      "intent": "reverse_etl",
      "availability": "implemented",
      "write": "cast_vote",
      "risk": "Casts one vote; never runs from a bulk reverse ETL plan.",
      "approval": "Requires reverse ETL plan -> preview -> explicit approval -> execute.",
      "flags": [
        { "name": "id", "type": "string", "summary": "Target thing id.", "maps_to": "record.id" },
        { "name": "dir", "type": "integer", "summary": "Vote direction.", "maps_to": "record.dir" }
      ],
      "examples": ["pm humanproxy-demo vote --id t3_abc --dir 1 --preview"]
    },
    {
      "path": "profile sync",
      "summary": "Upsert a profile through typed reverse ETL.",
      "intent": "reverse_etl",
      "availability": "implemented",
      "write": "sync_profile",
      "risk": "Updates a profile.",
      "approval": "Requires reverse ETL plan -> preview -> explicit approval -> execute.",
      "flags": [
        { "name": "id", "type": "string", "summary": "Profile id.", "maps_to": "record.id" },
        { "name": "display-name", "type": "string", "summary": "Display name.", "maps_to": "record.display_name" }
      ],
      "examples": ["pm humanproxy-demo profile sync --id u_1 --display-name Ada --preview"]
    }
  ]
}
