# Foundation 0.3.0 release candidate r1 evidence

source_sha: 91c8b6f71c2894848de5271560671b8dfb42a1af

**Implementation SHA I7:** `91c8b6f71c2894848de5271560671b8dfb42a1af`.

The release candidate begins at exact canonical core `041d2ec7ed986aea15d2d3d64f2076b484c3f999`, preserves reverse action binding through merge `50e90fa854635b7c8b295b7090034b82a52e4e03`, and preserves public output hardening through merge `e18f4372f4f65ae5e42265f237abad79473a7425`. `input-manifest.json` is the immutable typed provenance record.

The 38 original blocker categories are historical: both authoritative intake audits found committed repairs, and this RC does not reimplement them. The implementation includes the prior `a5005fa…` test/source-lock alignment, the confirmed Recurly repair (five accidentally removed required path declarations restored through `operations.json` and `surface-sync`), the confirmed GitHub source-projection repair, and the confirmed RC-09 CodeQL repair. RC-09 removes unchecked allocation multiplication and two dead assignments without suppressing or baselining any alert.

RC-10 corrects a release-gate test verdict, not a GitHub declaration. At E2, Verify demanded that fifteen already-implemented GitHub reads become `partial`; the eleven list declarations are identical at `v0.2.1`, `origin/main`, and I3. A fresh `pm` binary now executes all fifteen disputed commands against a local faithful fixture. For each, it initializes a project and fixture credential, emits exactly one authenticated GET whose resolved path matches the declared API-surface template, and validates the emitted direct-read envelope plus declared JSON output. All fifteen passed. The hard-coded partial tests now match that execution evidence. The candidate fixture decoder also reads JSON stdout separately from diagnostics; its prior `CombinedOutput` code path could decode stderr as if it were JSON. This is a local behavioral/fixture result only: it is not a real-provider proof or a certification claim.

RC-11 closes the subsequent Verify-only fixture failure without changing the production rate-limit safety fence. The final declaration witness is GitHub’s seven `certification-*` scopes, each `coordination=require_shared`, unchanged from `origin/main`; an unreachable inherited endpoint must correctly refuse before provider I/O. The fixture now starts a disposable loopback Redis-compatible coordinator and injects only its address into the fresh `pm` child. With `POLYMETRICS_DRAGONFLY_ADDR=127.0.0.1:1` in the parent, all 97 generated candidate stages pass in 70.92s; with `PM_DRAGONFLY_ADDR=127.0.0.1:1`, all pass in 70.48s. Each emitted an authenticated declared request and passed its declared output assertion, and the coordinator retained shared-admission state. The existing production refusal test remains unchanged. This is local behavioral fixture proof, not a shared-coordinator or provider certification claim.

RC-12 is the direct exact-gate lint correction: CI-pinned staticcheck found only a redundant boolean return in the PR’s source projection. The implementation now returns the existing predicate directly; `make lint` reports zero issues, full `cmd/connectorgen` passes in 146.778s, and the fresh-binary 97-stage fixture passes in 70.72s without changing a route, declaration, candidate, or generated artifact. RC-13 is generator-owned manual parity: `make docs-check-no-build` directly found the GitHub manual stale; `pm docs generate` changed only GitHub’s `MANUAL.md` and `SKILL.md`, a second run was idempotent, and docs validation/help checks passed.

`FND-B09` was reproduced before this closure: the inherited schema-2 manifest fails strict decode because `component_inputs` is an object, not the required typed list. The prior closures are superseded after CI directly proved generator-owned website data stale, the current branch reproduced the GitHub reachability regression, CodeQL reproduced the three RC-09 alerts, Verify reproduced the RC-10 test-verdict conflict and RC-11 fixture-environment coupling, and exact local gates reproduced RC-12 lint and RC-13 generated-manual drift. This schema-3 manifest binds I7, its five typed component identities, the current certification subject fingerprint, and closure-artifact SHA-256 digests. The strict evidence gate is run only from the clean evidence commit because it verifies the commit-parent graph.

At I7, the exact release-surface command reports identical results for `v0.2.1` and HEAD: `endpoints=1225`, `blocked=1`, and `direct_read_candidates=120`; endpoint, candidate, and blocked-identity differences are all empty. The sole blocked identity is the same retired route, `POST /user/{user_id}/projectsV2/{project_number}/drafts`, whose operation remains duplicate-of `POST /graphql (github.graphql.mutation.add-project-v2-draft-issue)`.

No credential value was requested, read, printed, or stored. No approved non-secret credential reference was supplied to this isolated lane, so no new provider call was made. Recurly has local fixture proof only and is explicitly implemented-but-uncertified. GitHub has local behavioral proof only: a fresh binary executed all 97 generated candidates under the declared local shared coordinator, all 633 released API-surface targets, and the fifteen RC-10 disputed reads with emitted-request and declared-output assertions. Full `internal/cli` passed in 1066.072s; full `cmd/connectorgen` passed in 146.778s; vet, build, docs/manual, connector boundary, smoke, and release workflow gates pass. Local hermetic behavior proof, historical provider observations, and certification are intentionally separate in `RELEASE-CANDIDATE-R1-REVIEW.md`; the current certification matrix reports zero certified connector shards.
