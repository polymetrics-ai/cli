# GSD programming-loop adapter check

Attempted command before implementation:

```bash
scripts/gsd prompt programming-loop init --phase issue-80-linear-parity --dry-run
```

Result: `scripts/gsd: unknown GSD command: programming-loop`.

Manual GSD/TDD fallback is recorded in `.planning/phases/issue-80-linear-parity-wave01-r1/PLAN.md`.
