# TDD Ledger

Phase: cli-engine-rest-write-executor-r1

Record failing test evidence before production code for every behavior-adding task.

| Slice | Red test | Expected initial result | Green evidence | Status |
| --- | --- | --- | --- | --- |
| Core executor and confirmation gate | `TestOperationDirectWritePreviewsApprovesAndExecutesSingleFormRequest` in `internal/connectors/engine` | `go test ./internal/connectors/engine -run 'TestOperationDirectWrite' -count=1` failed before production code: `OperationSpec.Batchable`, `OperationDirectWriteRequest`, `PreviewOperationDirectWrite`, and `OperationDirectWrite` did not exist. | `go test ./internal/connectors/engine -run 'TestOperationDirectWrite' -count=1` passed after the executor used `PreviewPreparedWrite` and `ExecutePreparedWrite`. | green |
| No-retry policy | `TestOperationDirectWriteNeverRetriesNonIdempotentFailure` in `internal/connectors/engine` | Same red compilation run: no direct-write executor/no-retry transport mode exists. | Same green run proved one HTTP 500 attempt only. Additional requester coverage proves a disabled retry mode suppresses 401 refresh and net/http replay signals. | green |
| Redirect replay refusal | `TestOperationDirectWriteRefusesRedirectReplay` in `internal/connectors/engine` | `go test ./internal/connectors/engine -run '^TestOperationDirectWriteRefusesRedirectReplay$' -count=1` failed: a 307 completed successfully after following the redirected request. | The same test passed after the executor reused the shared prepared-write transport policy for every `rest_write` dispatch. | green |
| CLI command lifecycle | `TestDirectWriteCommandPlanPreviewApprovalAndExecute` in `internal/app` | `go test ./internal/app -run 'TestDirectWriteCommandPlanPreviewApprovalAndExecute' -count=1` failed before app changes: `ReversePlan.ConnectorCommandOperation` and `ReverseRun.OperationDirectWrite` did not exist. | The same command passed against a real loaded test bundle and loopback server: plan preview made 0 HTTP calls, missing confirmation made 0 calls, one confirmed request executed, and replay made 0 additional calls. `TestNonDestructiveDirectWriteStillRequiresPreviewAndSingleUseApproval` adds the safe-write token path. | green |
| Runtime preflight metadata binding | `TestPreflightOperationDirectWriteRejectsMismatchedOperationPolicy` in `internal/connectors/commandrunner` | `Preflight` returned nil for a command whose output policy differed from its declared operation. | The same test passed after preflight asked the metadata provider to bind the operation and policy. | green |
| Executable command validation and sync | `TestValidate_CLISurfaceImplementedDirectWritePasses` and `TestSyncBundleDirectWriteDerivesOperationContract` in `cmd/connectorgen` | Validation reported `direct_write` unsupported; surface-sync reported zero corrections for a stale direct-write command. | Both tests pass: validation admits only supported `rest_write` contracts and surface-sync derives endpoint, path binding, exact output policy, and response bound. | green |
| Captain no-redaction response contract | Existing `TestOperationDirectWritePreviewsApprovesAndExecutesSingleFormRequest`, plus a table test for `json_redacted`, `write_result_redacted`, and `gong_bounded_input_redacted` in `internal/connectors/engine` | `go test ./internal/connectors/engine -run 'TestOperationDirectWrite(PreviewsApprovesAndExecutesSingleFormRequest|RedactingPoliciesKeepResponseBody|NeverRetriesNonIdempotentFailure)' -count=1` failed: every asserted token was absent and the HTTP error body was `[redacted]`. | Same focused engine command passed after the direct-write executor returned decoded content intact and rendered captured HTTP errors without the shared read redactor. | green |
| Complete direct-write errors and preview record | Direct-write commandrunner coverage for a declared redacted field and error value, plus app lifecycle coverage for plan sample and persisted failed-run error content | `go test ./internal/connectors/commandrunner -run 'TestBuildOperationDirectWriteCommand(UsesTypedInputsAndPlanLifecycle|KeepsCompleteInputError)' -count=1` and `go test ./internal/app -run 'TestDirectWriteCommand(PlanPreviewApprovalAndExecute|FailurePreservesErrorContent)' -count=1` failed: plan sample/command error replaced values with `***` and the provider error with `[redacted]`. | Both focused commands passed after direct-write command shaping and direct-write report persistence stopped applying redaction. | green |
