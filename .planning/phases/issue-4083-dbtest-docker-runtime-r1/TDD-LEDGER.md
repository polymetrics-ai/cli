# TDD Ledger — Issue #4083

| Stage | Check | Expected result | Evidence |
| --- | --- | --- | --- |
| CI remediation red | `GOTOOLCHAIN=go1.25.12 go run golang.org/x/vuln/cmd/govulncheck@latest ./...` | Fails because the pinned standard library contains reachable vulnerabilities fixed in Go 1.25.13. | Exited 1 with GO-2026-6218, GO-2026-6091, GO-2026-6090, GO-2026-6089, GO-2026-6088, GO-2026-5972, and GO-2026-5026, all found in Go 1.25.12 and fixed in Go 1.25.13. |
| CI remediation green | `GOTOOLCHAIN=go1.25.13 go run golang.org/x/vuln/cmd/govulncheck@latest ./...` | Passes with no reachable vulnerabilities. | `go version` reported `go1.25.13 darwin/arm64`; the scan exited 0 with `No vulnerabilities found` and `Your code is affected by 0 vulnerabilities.` |
| Docker VM capacity red | `go test -timeout 20m -run '^(TestDockerVMCapacityUsesOnlyAPreCachedLockedDownProbe\|TestDockerVMCapacityRefusesAnUncachedOrMalformedProbe)$' ./internal/connectors/native/dbtest` | Does not compile until `Config` can describe a Docker VM capacity probe and the harness owns its cleanup. | Exited 1: `Config.DockerCapacityProbeImage undefined` and `Harness.capacityProbeName undefined`. |
| Docker VM capacity green | `go test -timeout 20m ./internal/connectors/native/dbtest` | Passes for a pre-cached pinned probe and refuses absent or malformed daemon-side capacity evidence without pulling the probe. | Passed. The focused test asserts `--pull=never`, no network, read-only filesystem, dropped capabilities, no-new-privileges, PID bound, explicit daemon-root bind mount, POSIX `df -P -B1`, and idempotent probe cleanup. |
| Review follow-up Red: ownership and raw environment boundary | Review inspection of the current Docker VM probe and enabled MySQL configuration paths | A pre-existing probe name must be refused before cleanup ownership, and raw whitespace/control characters must remain invalid configuration. | Confirmed as review findings before remediation; the prior code claimed the probe name before inspecting it and trimmed both environment values. |
| Review follow-up Green: ownership and raw environment boundary | `go test -tags=databaseintegration -count=1 -timeout 20m -run '^(TestDockerVMCapacityUsesOnlyAPreCachedLockedDownProbe\|TestDockerVMCapacityRefusesAPreexistingProbeWithoutClaimingItsCleanup\|TestDockerVMCapacityRefusesAnUncachedOrMalformedProbe\|TestNewMySQLContainerHarnessConfigurationGuidance)$' ./internal/connectors/native/dbtest ./internal/connectors/native/mysql` | Refuses a foreign probe without deleting it and rejects raw control-character configuration with the same environment guidance. | Passed: `dbtest` 0.312s; `mysql` 0.728s. |
| Review follow-up Red: atomic probe ownership | Review inspection of cleanup after the Docker VM probe name precheck | A name absence check cannot reserve the generated name; cleanup must prove a per-run label and delete by the inspected immutable container ID. | Confirmed as review finding R6: a foreign container created after the precheck could otherwise be force-removed after `docker run --name` failed. |
| Review follow-up Green: atomic probe ownership | `go test -timeout 20m -run '^(TestDockerVMCapacityUsesOnlyAPreCachedLockedDownProbe\|TestDockerVMCapacityRefusesAPreexistingProbeWithoutClaimingItsCleanup\|TestDockerVMCapacityPreservesAProbeCreatedAfterPrecheck\|TestCloseRemovesOnlyLabelOwnedCapacityProbe\|TestDockerVMCapacityRefusesAnUncachedOrMalformedProbe)$' ./internal/connectors/native/dbtest` | Refuses a foreign probe created after the name check, removes only a matching label-owned probe by its inspected immutable ID, and retains the locked-down pre-cached probe contract. | Passed: `dbtest` 0.350s. |
| Review follow-up Red: atomic database-container ownership | Review inspection of normal database-container creation and cleanup | A generated-name absence check cannot reserve the name; an indeterminate failed create must never force-remove a foreign container that appeared after that check. | Confirmed as review finding R7: normal-container cleanup was armed by name before `run`, then removed that name directly after a creation race. |
| Review follow-up Green: atomic database-container ownership | `go test -timeout 20m -run '^(TestCleanupRemovesOnlyRunOwnedResources\|TestCopyFileFromContainerUsesTheConfiguredEndpoint\|TestStartCleansResourcesAfterIndeterminateEngineOutcomes\|TestStartPreservesAContainerCreatedAfterPrecheck\|TestCloseDuringCreateStillRemovesTheCreatedResource\|TestEveryTargetCommandRechecksTargetIdentity\|TestCleanupFailsClosedWhenTargetIdentityChanges\|TestStartUsesDockerTargetIdentityAndCapacity\|TestStartUsesPodmanMachineDaemonCapacity\|TestDockerVMCapacityUsesOnlyAPreCachedLockedDownProbe\|TestDockerVMCapacityRefusesAPreexistingProbeWithoutClaimingItsCleanup\|TestDockerVMCapacityPreservesAProbeCreatedAfterPrecheck\|TestCloseRemovesOnlyLabelOwnedCapacityProbe\|TestCloseRemovesOnlyLabelOwnedDatabaseContainer)$' ./internal/connectors/native/dbtest` | Normal containers carry a unique owner label, establish a verified immutable ID before use, and clean an indeterminate create only after re-proving that label; a foreign raced container remains untouched. | Passed: `dbtest` 0.499s. |
| Review follow-up Red: volume and generated-image ownership | Review inspection of normal volume and generated image-reference creation and cleanup | A generated volume has no immutable daemon ID, so cleanup must verify a per-run volume label and never force-remove it. A mutable run tag must not choose the startup image or be removed after it changes identity. | Confirmed as review findings R8/R9: a raced volume could be force-removed by name, and a retagged image reference could be run or removed as though it remained harness-owned. |
| Review follow-up Green: volume and generated-image ownership | `go test -timeout 20m ./internal/connectors/native/dbtest` | Volume creation records a per-run label, re-proves it before non-force cleanup, and preserves foreign labels. The generated reference is bound to an inspected immutable source image ID; startup uses that ID, while retagged references are refused and retained. | Passed: `dbtest` 0.448s. |
| Review follow-up Red: atomic volume and tag cleanup | Review inspection of volume and generated-image cleanup after ownership checks | A named volume can be removed and recreated after a separate label inspection, and a mutable tag can change after a separate image-ID inspection; neither name-based deletion is safely conditional. | Confirmed as review findings R10/R11: both cleanup paths could mutate a foreign replacement after a concurrent retag or resource recreation. |
| Review follow-up Green: atomic volume and tag cleanup | `go test -timeout 20m ./internal/connectors/native/dbtest` | The database data volume is anonymous and cleaned only through verified immutable container-ID removal with `--volumes`; the generated tag remains after startup and cleanup, while the immutable source image ID is the only image used to start the database. | Passed: `dbtest` 0.459s. |
| Review follow-up Red: immutable capacity probe and strict `df` schema | Review inspection of Docker VM capacity evidence | A cached probe tag can change after inspection, and a header that merely contains `Available` can misidentify arbitrary output as POSIX `df` capacity evidence. | Confirmed as review findings R12/R13: mutable probe tags and malformed headers could supply unproven capacity before database-image mutation. |
| Review follow-up Green: immutable capacity probe and strict `df` schema | `go test -timeout 20m ./internal/connectors/native/dbtest` | The cache inspection captures an immutable local image ID for the `--pull=never` probe, and parsing requires the exact `Filesystem 1-blocks Used Available Capacity Mounted on` schema before source-image mutation. | Passed: `dbtest` 0.395s. |
| Red | `TestNewRequiresAnExplicitContainerRuntime` | Fails because `New` silently accepts the hard-wired Podman default instead of requiring an explicit runtime. | `go test -timeout 20m -run '^TestNewRequiresAnExplicitContainerRuntime$' ./internal/connectors/native/dbtest` exited 1: `New() accepted a database test configuration without an explicit Docker or Podman runtime`. |
| Green | Focused runtime-selection/endpoint/target tests | Passes for explicit Docker and Podman selections while unknown/unsafe input remains refused. | `go test -timeout 20m -run '^(TestNewRequiresAnExplicitContainerRuntime\|TestNewAcceptsExplicitDockerRuntime\|TestNewRejectsUnknownContainerRuntime\|TestRuntimeCommandPinsTheConfiguredEndpoint\|TestNewRejectsUnsafeEndpoints\|TestStartUsesDockerTargetIdentityAndCapacity\|TestParseDockerTargetIdentity)$' ./internal/connectors/native/dbtest` passed. |
| Regression | dbtest and MySQL non-live package tests | Pass without a container daemon. | `go test -timeout 20m ./internal/connectors/native/dbtest`, `go test -timeout 20m ./internal/connectors/native/mysql`, and the tagged MySQL selector without its opt-in all passed; the tagged selector visibly skipped before startup. |
| Enabled incomplete config | Tagged MySQL proof with opt-in but no runtime variables | Fails clearly instead of reporting a skipped pass. | `POLYMETRICS_DATABASE_INTEGRATION=1 go test -tags=databaseintegration -count=1 -timeout 20m -run '^TestMySQLContainerHarness$' -v ./internal/connectors/native/mysql` exited 1 and named both required variables, `docker`, `podman`, and the direct Unix socket form. |
| Live Docker | Tagged MySQL proof with Colima's exact direct explicit Docker socket | Passes only if actually observed. | **Passed.** Bootstrap, explicitly pinned to the same socket: `docker --host unix:///Users/karthiksivadas/.colima/default/docker.sock pull docker.io/library/busybox:1.37.0` (observed digest `sha256:9db7b59979c38555a39def84a31fb98b5296952f9e3afd4f6f11f05b07adfab0`). Proof: `POLYMETRICS_DATABASE_INTEGRATION=1 POLYMETRICS_CONTAINER_RUNTIME=docker POLYMETRICS_CONTAINER_ENDPOINT=unix:///Users/karthiksivadas/.colima/default/docker.sock go test -tags=databaseintegration -count=1 -timeout 20m -run '^TestMySQLContainerHarness$' -v ./internal/connectors/native/mysql`; `PASS` in 9.01s, including all five TLS-mode subtests; daemon-store report `before=100515446784 after=100515454976`. |
| Live Podman | Tagged MySQL proof with a direct explicit Podman socket | Passes only if actually observed. | Not proved: no local Podman VM is running, hence no explicit direct Unix Podman API endpoint exists for this test. The harness did not read or alter a Podman global default. |
| Refactor | gofmt, vet, diff check, scoped review | No formatting, safety, or scope drift. | `gofmt -w` on changed Go files, `go vet ./internal/connectors/native/dbtest ./internal/connectors/native/mysql`, and `git diff --check` passed; inline review pending final gate sweep. |

## Red rationale

The protected behavior is not merely an alternate binary name. An explicit
runtime must select its own client command while every invocation remains
pinned to the caller-provided local Unix endpoint. The red test therefore
tests the safety contract and the missing Docker dispatch together.
