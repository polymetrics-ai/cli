# Issue #397 PM Orchestrator Extension Summary

Status: REVIEW CORRECTION ROUND 3 IMPLEMENTED — final exact-head verification/re-review, Shepherd, and PR checks pending.

Captain authorized additive canonical orchestration corrections on draft PR #495. The extension starts from reviewed synchronization head `3fd63fbe0f526873fa3adb8a75fa5f20342d52a6`, keeps PR #493 head `e21e56339390c5e1946eb4cfaf276eb80a889f29` path-disjoint, and adds no product behavior or dependency.

The canonical route has one owner: `/pm-orchestrate` runs the universal lifecycle when registry discovery shows `programming-loop` is absent. It preserves durable reconciliation, isolated workers, bounded correction, machine/credential contracts, and human merge authority. Exact-head verification is followed by fresh-context read-only local Codex review with written dispositions, then independent Shepherd validation before integration. Every changed head invalidates prior exact-head gates.

Current PM dependency paths now use PM-specific worker-handoff and review-disposition templates that represent `local_codex`, Shepherd, correction-budget, and human-gate state. Generic hosted-review templates remain only for truthful historical or separately authorized non-PM use. Autonomous state persists one exact-base/candidate-lineage counter across changed heads and replacement PRs, maps the old counter only as read-only migration input, and blocks for a human when the four-round default is exceeded.

The authoritative #397 trace, scoped #408 subissue, machine run state, and summary make the post-Wave1 PR #493 routing reconciliation a hard dependency. After Wave 1 integrates, PR #493 must merge the actual resulting parent, reconcile only its owned routing/skill/Makefile guidance, rerun its gates, and integrate before another CLI Architecture v2 implementation worker starts. #408 remains `not_spawned_dependency_blocked` until both gates complete. Machine state names the current `chore/...` PR #495 branch and retains the old `fm/...` ref only as historical evidence.

Focused RED failed against the old route and against each review round's machine-contract gaps. Focused GREEN passes for the transitive PM dependency graph, every durable PR #493/#408 queue record, review-enum parity, stable correction lineage, cap and parent-ready terminal classification, Pi model routing, Shepherd guards, JSON/YAML parsing, dependency invariance, and PR #493 path disjointness. The full credential-free gate passed at reviewed head `3af7910528d234d1a1d886a6778d7817495e6321`; it must rerun after the correction-round-3 evidence commit.

Initial fresh-context review produced F1–F5. Correction round 1 fixed route/schema/budget/PR evidence and deferred the direct-read Gong defect under the no-product boundary. Captain authorized focused follow-up https://github.com/polymetrics-ai/cli/issues/497. Re-review at `0665ad7aad1ec083f4bb0572a88ac1a38f417a35` produced F6–F8; correction round 2 implemented all three. Re-review at `3af7910528d234d1a1d886a6778d7817495e6321` produced N1–N5; correction round 3 implements N2–N5. N1 is a separate pre-existing Gong write-URL defect and remains deferred pending separate captain authority; no product or PR #493-owned path changed. Final verification and a new exact-head review follow before independent Shepherd validation.
