# Issue #397 PM Orchestrator Extension Summary

Status: CAPTAIN-AUTHORIZED CORRECTION ROUND 5 IMPLEMENTED FOR R1–R3 — exact-version verification/re-review, Shepherd, and required CI pending; no round 6.

Captain authorized additive canonical orchestration corrections on draft PR #495. The extension starts from reviewed synchronization head `3fd63fbe0f526873fa3adb8a75fa5f20342d52a6`, keeps PR #493 head `e21e56339390c5e1946eb4cfaf276eb80a889f29` path-disjoint, and adds no product behavior or dependency.

The canonical route has one owner: `/pm-orchestrate` runs the universal lifecycle when registry discovery shows `programming-loop` is absent. It preserves durable reconciliation, isolated workers, bounded correction, machine/credential contracts, and human merge authority. Exact-head verification is followed by fresh-context read-only local Codex review with written dispositions, then independent Shepherd validation before integration. Every changed head invalidates prior exact-head gates.

Current PM dependency paths use PM-specific worker-handoff and review-disposition templates that represent `local_codex`, Shepherd, correction-budget, and human-gate state. The exact six-value finding-disposition enum is machine-specified and parsed across schema, workflow, prompt, contracts, templates, and every actual disposition row. `pm-gsd-worker` routes through `required-skills-routing.md`, loads only harness-advertised skills, and active dependency traversal rejects missing required references. Generic hosted-review templates remain only for truthful historical or separately authorized non-PM use. Autonomous state persists one exact-base/candidate-lineage counter across changed heads and replacement PRs, maps the old counter only as read-only migration input, and records the captain's single round-5 override without resetting the lineage.

The authoritative #397 trace, scoped #408 subissue, machine run state, and summary make the post-Wave1 PR #493 routing reconciliation a hard dependency. After Wave 1 integrates, PR #493 must merge the actual resulting parent, reconcile only its owned routing/skill/Makefile guidance, rerun its gates, and integrate before another CLI Architecture v2 implementation worker starts. #408 remains `not_spawned_dependency_blocked` until both gates complete. Machine state names the current `chore/...` PR #495 branch and retains the old `fm/...` ref only as historical evidence.

Round-5 RED reproduced exactly R1–R3. Focused GREEN now passes for missing-reference rejection and harness-routed worker skills; actual disposition-row enum validation with F2/N1 canonical `deferred`; explicit unsupported schema, unknown kind, canonical missing kind, and cap fail-closed classification; parent-ready and absent-schema historical readability; Pi routing; syntax/parse; dependency invariance; and PR #493 path disjointness. Full exact-version verification must rerun after the round-5 evidence commit.

After four correction rounds, exact-head review at `32dbda9daf432a5c3f45e7b7753a41eaa96bd915` returned R1–R3 and correctly blocked before Shepherd. Captain decision `pm-correction-budget-exceeded` authorized exactly round 5 on the existing lineage for those three findings. Implementation head `fa6054c07cfd4a43e0c03d262681df37ce8565d4` closes all three without product, dependency, #408, audit-backed review-system, or PR #493-owned changes. The direct-read Gong defect remains at https://github.com/polymetrics-ai/cli/issues/497; N1 remains a separate deferred pre-existing Gong write-URL defect pending authority. Final exact-version verification and fresh Codex review follow; Shepherd runs only if Codex is clean. Any new actionable finding returns to the captain because no round 6 is authorized.
