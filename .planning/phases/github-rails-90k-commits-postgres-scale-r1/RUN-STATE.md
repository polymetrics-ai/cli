# Run state

- 2026-08-16: branch created from `origin/integration/4015-mvp-flat-r1` at `ddfca0bbe6a237d6965894df16d764851651bdf0`; `BASE-OK` verified.
- 2026-08-16: GSD adapter doctor, command-source resolution, and `agentcontractgen check` passed. Inline/manual GSD fallback is recorded in `PLAN.md` because the phase is absent from the roadmap and no compatible isolated Pi roles are available.
- 2026-08-16: host disk check recorded 16 GiB free; shared Docker/Colima endpoint reports Docker 27.4.0, 2 CPUs, and 2,054,631,424 bytes memory. No lifecycle action was taken.
- 2026-08-16: bounded configuration tests were red before `githubCommitTransportScaleConfig` was introduced, then green. The one-page fresh-binary feasibility completed with independently verified `extracted=100`, `warehouse_parquet=100`, `postgres=100` in 22.85 seconds.
- 2026-08-16: the 900-page run failed after 926.02 seconds at the managed PostgreSQL approval gate (`write approval evidence has expired`). Its pre-cleanup phase counts were not persisted and cannot be reconstructed.
- 2026-08-16: the 800-page counterfactual completed in 891.51 seconds with independently verified `extracted=80000`, `warehouse_parquet=80000`, and `postgres=80000`; this isolates the 15-minute run-wide evidence boundary rather than storage or volume.
- 2026-08-16: captain design decision superseded run-scoped approval evidence. Follow-on implementation is tracked by #4181: day-scale durable authorization, per-unit source/apply deadlines, and durable success/failure phase measurements before test cleanup.
- 2026-08-16: red/green two-clock, durable-authorization, CLI lifetime, and durable-measurement slices completed. Focused `internal/app`, `internal/cli`, `internal/synctransport`, `internal/connectors/engine`, and native PostgreSQL tests pass; one-page real binary proof reports durable `100/100/100` counts before cleanup.
- 2026-08-16: exact 900-page production binary run completed after 1,076.989768 seconds: persisted phase counts are `extracted=90000`, `warehouse_parquet=90000`, `postgres=90000`. An independent PostgreSQL connection ran `SELECT count(*)` and returned `90000`; state has an acknowledged checkpoint and one durable delivery receipt. Phase elapsed totals: GitHub page extraction `539.100967784s`, warehouse/Parquet `7.244399840s`, PostgreSQL apply/read-back `517.652168624s`.
- 2026-08-16: resource observations for the 90k run on Docker VM 2 CPU / 2,054,631,424-byte RAM: maximum sampled `pm` RSS was 469,920 KiB; staged Parquet is 16,674,647 logical bytes across 90 artifacts; PostgreSQL business relation is 58,859,520 bytes; warehouse allocated total was 124,836 KiB. Host free disk remained above 14.6 GiB and returned to 14.9 GiB after cleanup.
- 2026-08-16: configured extraction executed 900 `commits` pages at 100 records/page. GitHub's post-run authenticated rate-limit headers reported `limit=5000`, `remaining=5000`, `used=0`; because that provider accounting conflicts with the 900 completed data-page requests, it is recorded as provider-reported headroom rather than a trustworthy consumed-count meter.
- 2026-08-16: execution-environment loss orphaned the test wrapper during the long run but not its `pm` child. After independently collecting terminal state and PostgreSQL evidence, the exact named container and exact temporary test directory were removed; Docker/Colima were neither stopped nor reconfigured.
- Pending: complete full local verification, review, commit, push, and direct PR.
