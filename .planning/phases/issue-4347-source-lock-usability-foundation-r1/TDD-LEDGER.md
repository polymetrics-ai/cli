# TDD ledger — issue 4347 source-lock usability

| Slice | Red | Green | Refactor | Evidence |
| --- | --- | --- | --- | --- |
| Retain-specific validation | **Red:** `go test -timeout 10m ./cmd/connectorgen -run 'TestSourceRetain(RetainsV3ArtifactWithoutImportTimeFormInventory|SupportsParitySourceLock|VerifiesReorderedJSONByCanonicalIdentity|ReportsWrongSourceBeforeDrift)$'` failed: the v3 fixture was rejected for an OpenAPI version outside the import inventory and the parity lock was not discovered. | **Green:** the retain-specific loader accepts the minimal operation/parity lock facts, while the same no-form v3 lock still fails `parseSourceImportLock`. The HTTP retain entry point also accepts an unknown import form pin without relaxing import validation. | No separate refactor required; shared identity validation is consumed by both retain and offline import. | `go test -timeout 10m ./cmd/connectorgen -run '^TestSourceRetain'` passed 2026-08-24. |
| Canonical JSON identity | **Red:** same command failed: strict source-lock JSON decoding rejected `canonical_sha256`, so an equal semantic document with different key order could not be retained. | **Green:** the second, reordered serialisation is retained and the offline retained-artifact fetcher verifies it through `canonical_json`; the manifest holds selected canonical identity and fetched byte provenance separately. | Manifest path selection now uses the fetched-byte SHA-256 so two valid serialisations cannot overwrite one another. | `go test -timeout 10m ./cmd/connectorgen -run '^TestSourceRetain'` passed 2026-08-24. |
| Wrong-source classification | **Red:** same command failed: HTTP 403 remained a generic fetch error and the 20-byte login/landing response was reported as `source-lock refresh required`. | **Green:** the 404 and the non-login, drastic-size-collapse fixture return `wrong source`; neither returns a refresh/drift message. | HTTP redirects/statuses preserve typed source-access errors through the existing safe HTTP client. | `go test -timeout 10m ./cmd/connectorgen -run '^TestSourceRetain'` passed 2026-08-24. |
| Provider automation block | **Red:** `go test -timeout 10m ./cmd/connectorgen -run '^TestSourceRetainReportsBotBlockBeforeWrongSourceOrDrift$'` failed: HTTP 403 was `wrong source` and TLS refusal was a generic fetch failure. | **Green:** 403 and TLS/certificate failures emit the distinct actionable `BOT-BLOCK` verdict, directing the reader to a browser capture or provider-owned repository; they are neither absence nor drift. | The HTTP retain entry point emits a typed bot-block error; generic fetcher seams classify the same response category without broadening the verdict set. | `go test -timeout 10m ./cmd/connectorgen -run '^TestSourceRetain'` passed 2026-08-24. |
| Uppercase lock digest round-trip | **Red:** `go test -timeout 10m ./cmd/connectorgen -run '^TestSourceRetainReadsUppercaseLockDigestAfterRetention$'` failed after successful retention with `retained source artifact provenance does not match source lock`. The manifest normalizes the digest to lowercase while the offline reader compared it case-sensitively. | **Green:** the same command passes after the reader compares the validated hexadecimal lock digest with `strings.EqualFold`; the complete source-retain suite and retained-artifact mismatch regression also pass. | Keep manifest digests normalized for stable filenames and records; hexadecimal identity matching is case-insensitive at the reader boundary. | Audit finding from frozen SHA `f92a16fe6f16772503676af276242100144e6ed9`; red/green commands ran 2026-08-24. |
| Live exact retention | **Red:** initial built-binary evidence found ShipStation's fixed `?download` query was rejected and WooCommerce's valid 4.4 MB HTML documentation was misclassified from incidental login text. | **Green:** the rebuilt binary retained fastly, github, hubspot, pipedrive, shipstation, squarespace, woocommerce, and zendesk-support. The before/after SHA-256 values of all eight lock files are identical. | **Red:** an existing legacy manifest was not enriched with form/version. **Green:** `TestSourceRetainEnrichesExistingManifestWithIdentityAndDetectedForm` now records explicit byte identity, fetched-byte provenance, and detected form/version without changing the lock. | Built binary: `./.task-bin-connectorgen source-retain <connector> --retrieved-at 2026-08-24T13:18:22Z --license undetermined --terms undetermined`, all eight exit 0 on 2026-08-24. |
| R1 F1 — operation-evidence v3 document identity | **Red:** `go test -timeout 10m ./cmd/connectorgen -run '^(TestOperationEvidenceUsesStrictV3DocumentOperationInventory|TestSourceImportVersion3RenderedReferenceCitationNeedsPointerOrVerifiedCaptureBinding|TestSourceRetainHTTPResponseEvidenceClassifiesPlausibleBadSourcesBeforeDrift)$' -count=1` failed: the legal v3 GitHub document inventory produced 305 rows rather than the 1,525 legacy rows, proving `operation-evidence` reads only legacy `rest.operations`. | **Green:** ordinary source locks now decode through `parseSourceImportLock`; the same v3 GitHub workspace preserves every row, all six class lanes, declared/deferred evidence, and GraphQL rows. | Keep skipped/dynamic absence records on their existing narrow evidence-only parser; import does not admit that provider-absence shape. | The exact focused command passed in 2.822s on 2026-08-26. |
| R1 F2 — rendered citation specificity | **Red:** same command failed: a generic same-origin publication citation parsed successfully, while the prospective capture binding was rejected as an unknown field. | **Green:** a rendered operation needs a fragment pointer or exact capture/hash/bytes/location binding; a generic URL and mismatched binding fail before import fetch. | Citation is never a fetch authority; no provider I/O. The read-only Batch 6–7 audit at `origin/fm/cli-map-batch67-r1` / `18248d233e6abd9d7ec03075a225cf35ee2f5399` found 861 generic rendered-operation citations in eight connectors; that is deliberately rejected impact for the lock owners to remedy, not a claim that their pages are operation-specific evidence. | The exact focused command passed in 2.822s on 2026-08-26. |
| R1 F3 — MIME/body wrong-source classification | **Red:** same command failed: both a structured source served as HTML and a plausible-size login form reached `source-lock refresh required: fetched artifact does not match locked bytes and SHA-256`. | **Green:** retain receives MIME/body evidence, reports `wrong source` before identity comparison for structured bad/invalid MIME and credible login/error HTML, and leaves valid documentation HTML and canonical JSON identity unchanged. The adversarial regression includes a same-size HTML `Error 503` page as well as the login form. | Keep byte-only hermetic fetchers source-neutral; MIME is asserted only where the HTTP fixture supplied it. | The exact focused command passed in 2.822s on 2026-08-26. |
| R2 exact-SHA admission repair | **Red:** `go test -timeout 10m ./cmd/connectorgen -run '^(TestOperationEvidenceRejectsV3InventoryStateClaimingAbsence|TestSourceRetainCanonicalJSONRejectsHTMLMIMEBeforeIdentityDrift|TestSourceCanonicalJSONSHA256RejectsDuplicateObjectMembers|TestSourceRetainCanonicalJSONRejectsDuplicateMembersBeforeIdentityDrift|TestSourceRetainVerifiesMinifiedJSONByCanonicalIdentityDespiteRawByteChange|TestSourceImportVersion3RenderedReferenceCitationNeedsPointerOrVerifiedCaptureBinding)$' -count=1` failed: a 1,220-row v3 inventory became absence; HTML/deduplicated JSON reached canonical drift; unrelated fragment and contradictory binding parsed; and 31-byte minified JSON was wrong-source against a 1,063-byte formatted canonical lock. | **Green:** populated v3 inventory enters strict decode rather than absence projection; canonical JSON expects a JSON MIME, rejects duplicate-member ambiguity before hashing, and accepts equivalent formatting by canonical digest alone; citation fragment and binding are independently constrained to locked extraction/capture facts. | No provider I/O or re-pin path. Preserve empty-inventory v3 absence evidence and raw byte provenance. | Focused green command adding help/docs assertions passed in 1.617s; `^TestSourceRetain` passed in 1.409s and operation-evidence/rendered-reference/canonical focused suite in 17.618s, all on 2026-08-26. Exact-SHA audit instructions 015, 016, and 017. |

The initial red run was recorded before production edits. Green evidence asserts retained artifact/manifest state, not merely exit status.
