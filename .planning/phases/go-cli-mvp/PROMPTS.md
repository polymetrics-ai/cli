# Prompt Snapshot

User requested creation of the whole working application using the universal programming loop.

Relevant architecture source:

- `POLYMETRICS_GO_CLI_MONOLITH_PRD_ARCHITECTURE.md`

Execution approach:

- Use `gsd-programming-loop` for local phase/TDD/verification structure.
- Use `go-engineering` for Go package design, context-aware operations, small interfaces, safe credentials, and tests.
- Build a dependency-free MVP before adding production data-store and connector dependencies.

