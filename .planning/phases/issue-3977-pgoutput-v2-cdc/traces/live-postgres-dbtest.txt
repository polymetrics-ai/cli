Command:

    POLYMETRICS_DATABASE_INTEGRATION=1 POLYMETRICS_CONTAINER_RUNTIME=docker \
    POLYMETRICS_CONTAINER_ENDPOINT=unix:///Users/karthiksivadas/.colima/default/docker.sock \
    go test -tags=databaseintegration -count=1 -timeout 20m -v ./internal/connectors/native/postgres

Result: pass (2026-08-15).

The real PostgreSQL 16.10 container uses `wal_level=logical` and
`logical_decoding_work_mem=64kB`. The test writes a 128-row, 1 MiB transaction
to force pgoutput v2 streaming, proves no downstream event/checkpoint is visible
before commit, verifies rollback leaves event/checkpoint/acknowledged LSN unchanged,
then proves durable checkpoint acknowledgement, restart from its LSN, and scoped
slot teardown.
