# TDD LEDGER — caller-supplied identifier sets

| ID | Contract | Red evidence | Green evidence |
| --- | --- | --- | --- |
| R1 | Closed operation declaration requires min/max and a supported shape/wire | The test-only bundle was rejected because `rest.caller_supplied_identifier_sets` was an unknown closed-schema property | `TestCallerSuppliedIdentifierSetDeclarationsRejectUnsafeContracts` and `TestValidate_CallerSuppliedIdentifierSetBundlePasses` pass. |
| R2 | Implemented direct-read command binds exactly one required `string_array` flag to every set | `go test ./internal/connectors/commandrunner -run TestRunOperationDirectReadPreservesIdentifierSetPresence` failed to compile: `OperationDirectReadRequest.IdentifierSets` did not exist | `TestValidate_CallerSuppliedIdentifierSetRequiresExactCLIBinding`, `TestSyncBundleDerivesCallerSuppliedIdentifierSetMapping`, and the commandrunner presence test pass. |
| R3 | Flat identifier lists encode correctly for comma query, repeated query, JSON body, and path segment | `go test ./internal/connectors/engine -run TestCallerSuppliedIdentifierSetsWireEncodings` failed to compile because the typed request had no identifier-set input | `TestRunCallerSuppliedIdentifierSetCommandsEndToEnd` passes each command through engine request shaping; the engine-level wire test also passes. |
| R4 | Cardinality and shape fail before networking without identifier disclosure | The focused engine rejection test could not compile before the typed request surface existed | `TestCallerSuppliedIdentifierSetsRejectBeforeNetworkWithoutValueDisclosure` asserts typed rejections, named limit/position/shape diagnostics, zero server hits, and no leaked fixture identifier. |
| R5 | Explicit blank retains `[]` with zero minimum; absent remains rejected | The focused commandrunner test could not compile before the typed request surface existed | `TestRunOperationDirectReadPreservesIdentifierSetPresence` preserves a non-nil `[]`; `TestCallerSuppliedIdentifierSetsPreserveExplicitEmptyBodyArray` verifies literal `[]` on wire. |
