# TDD Ledger — Issue 4166 Live Write Certification

| Slice | Red | Green | Refactor / evidence |
| --- | --- | --- | --- |
| Scope gate | The prior proof’s 607 `DryRunWrite` preparations were falsely treated as live action passes. | `SCOPE.md` classifies all 607 actions against the confirmed disposable certification identity, Polymetrics-Cert, and its in-progress Enterprise trial. | The captain corrected the old personal-control-plane premise: 28 run now; the other 579 have named fixture, credential, account, App/OAuth, or trial prerequisites. No path is permanently untestable on the disposable boundary. |
| Report truthfulness | `go test -timeout 20m ./internal/connectors/certify ./internal/cli` initially failed because `WriteActionResult` could not express a declared path/risk outcome and full-parity options had no requirement bit. | The sweep executes no `DryRunWrite`; each unexecuted action is `not_live` with its declared path/risk and a non-pass stage. The batch summary stays `not_live`, and text rendering is `[PARTIAL]`, never `[PASS]`. | Focused green: `go test -timeout 20m ./internal/connectors/certify ./internal/cli` (pass). |
| Full parity | `TestCertifyOptionsFullParityEnablesFullWriteAndRejectsWriteSkip` was red before `--full-parity` set full/write; `TestFullParityClaimRefusesNonLiveWriteAction` proves a non-live action blocks the claim. | The CLI enables full/write, rejects `--skip write`, records a `full_parity` stage, and passes `rep.FullParityVerified()` rather than a hard-coded true into the external-proof writer. | Focused green: `go test -timeout 20m ./internal/connectors/certify ./internal/cli` (pass). |
| Durable lifecycle ledger | `TestLedgerPersistsScenarioMutationReadBackAndCleanupState` did not compile before the ledger exposed scenario, resource, state, and read-back checkpoints. `TestWriteStagesLedgerPersistsAtConfiguredLedgerRoot` and `TestRunBatchAssignsDurablePerConnectorLedgerRoot` were red before production callers set a durable ledger root. | Every selected write pairing records `planned`, `mutated`, `read_back`, then `cleaned`; single and batch certification persist their ledger below `.polymetrics/certifications/ledger/<connector>`. The sweeper reads that ledger while driving cleanup against the original project root. | Focused green: `go test -timeout 20m ./internal/connectors/certify -run 'Test(RunBatchAssignsDurablePerConnectorLedgerRoot|WriteStagesLedger|SweeperHarnessRoot)'`, then `go test -timeout 20m ./internal/connectors/certify ./internal/cli` (pass). |
| Concrete non-live classification | `TestGithubWriteActionInventoryClassifiesEveryDeferredActionWithConcretePrerequisite` did not compile before inventory rows exposed a classification; `TestWriteSweepDoesNotTreatExistingNonPassResultAsLiveCoverage` was red because map presence was mistaken for coverage. | All 607 rows have an exhaustive current-boundary class and concrete prerequisite. A stale `not_live` result is replaced, not treated as coverage; only `pass` plus `read_back` suppresses a sweep failure. | Focused green: `go test -timeout 20m ./internal/connectors/certify -run 'Test(GithubWriteActionInventory|WriteSweepDoesNotTreatExistingNonPassResultAsLiveCoverage)'` (pass), then `go test -timeout 20m ./internal/connectors/certify ./internal/cli` (pass). |
| Live scenarios | **Active:** write production-entry scenario tests for all 28 safe repository actions. | Pending implementation. | Every action needs a production-entry-point mutation, independent read-back, ownership-checked cleanup, and resume assertion. |
| Fault proof | **Active acceptance:** inject a test-scoped post-schema request defect; the production certification run must fail and identify its action. | Pending implementation. | The intact control passes only after observable mutation/read-back. |
