# TDD ledger — issue 4325 declaration-admission foundation

| Slice | Red evidence | Green evidence | Refactor / scope evidence |
| --- | --- | --- | --- |
| Admission checker | **Red (2026-08-25):** `go test -timeout 20m ./cmd/connectorgen -run '^TestDeclarationAdmission'` failed to build because the declaration-admission document, finder, and lane/state contract did not exist. A later red subtest proved that an implemented write could otherwise bind an action at a different endpoint. | **Green:** the focused suite passes with cited runnable reads, deferred reverse-ETL/delete/binary rows, an importer/descriptor gap, malformed citation, missing/duplicate/stale/base-path/lane/delete-metadata/false-binding defects, exact write endpoint binding, and a clean on-disk sidecar that has no retained artifact or hash. | No connector-owned Batch-1 definition is modified. |
| Deferred state | **Red:** the pre-existing command surface had no `deferred` availability or foundation metadata to carry the required discoverable command state. | **Green:** `TestPreflightDeferredCommandReturnsNamedFoundationBeforeExecutor` proves the commandrunner returns a typed `missing_foundation` refusal before an executor. `TestCommandSurfaceProjectsDeferredFoundationGap` proves the engine preserves that metadata. `TestDeclarationAdmissionAdmitsGitHubImplementedDeleteControl` proves the inverse remains true for GitHub's implemented delete action. | `implemented` continues to use the existing runtime preflight contract; deferred is a separate terminal refusal, never a generic delete/destructive classification. |
| Certificate separation | **Red:** the absent admission seam could not represent a complete all-deferred connector without treating it as an executable claim. | **Green:** `TestDeclarationAdmissionAcceptsCompleteZeroRunnableConnector` accepts an all-deferred cited bundle, while `TestDeclarationAdmissionRejectsCompletenessAndBindingDefects` rejects a falsely implemented row. | The check reads local declaration files only; it performs no provider I/O and does not call source-lock, runtime, or live certification. |
| Enforceable deferred mapping | **Red (2026-08-25):** `go test -timeout 20m ./cmd/connectorgen -run '^(TestDeclarationAdmissionStripeAccountsDeleteIsDiscoverableDeferred|TestDeclarationAdmissionRejectsPolicyOnlyDeferredFoundation)$'` failed to build because an admission foundation could name only an ID/reason, not a missing implementation component and its evidence. | **Green:** the generic deferred-delete fixture has a cited source row, discoverable command, and `system/missing_foundation` preflight; a `blocked_by_default`-only foundation is rejected. | Captain clarification 007 moves individual provider mappings to `cli-batch1-repair-r1`. Uncommitted Stripe definition paths are preserved but excluded from this PR; no typed action/schema, credential, provider request, or live certification is added. |
