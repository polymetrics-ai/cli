# TDD ledger — issue 4325 declaration-admission foundation

| Slice | Red evidence | Green evidence | Refactor / scope evidence |
| --- | --- | --- | --- |
| Admission checker | **Red (2026-08-25):** `go test -timeout 20m ./cmd/connectorgen -run '^TestDeclarationAdmission'` failed to build because the declaration-admission document, finder, and lane/state contract did not exist. A later red subtest proved that an implemented write could otherwise bind an action at a different endpoint. | **Green:** the focused suite passes with cited runnable reads, deferred reverse-ETL/delete/binary rows, an importer/descriptor gap, malformed citation, missing/duplicate/stale/base-path/lane/delete-metadata/false-binding defects, exact write endpoint binding, and clean on-disk catalogs that contain no retained artifact or hash. | No connector-owned Batch-1 definition is modified. |
| Deferred state | **Red:** the pre-existing command surface had no `deferred` availability or foundation metadata to carry the required discoverable command state. | **Green:** `TestPreflightDeferredCommandReturnsNamedFoundationAfterExactTargetValidation` proves commandrunner returns typed `missing_foundation` only after exact target validation; the fail-closed companion tests reject a connector without that resolver and an invalid target before classification. `TestCommandSurfaceProjectsDeferredFoundationGap` proves the engine preserves the complete metadata. `TestDeclarationAdmissionAdmitsGitHubImplementedDeleteControl` proves the inverse remains true for GitHub's implemented delete action. | `implemented` continues to use the existing runtime preflight contract; deferred is a separate terminal refusal, never a generic delete/destructive classification. |
| Certificate separation | **Red:** the absent admission seam could not represent a complete all-deferred connector without treating it as an executable claim. | **Green:** `TestDeclarationAdmissionAcceptsCompleteZeroRunnableConnector` accepts an all-deferred cited bundle, while `TestDeclarationAdmissionRejectsCompletenessAndBindingDefects` rejects a falsely implemented row. | The check reads local declaration files only. It reuses no-I/O command preflight to prevent resolver drift, but does not run source-lock, credential-bound runtime certification, live certification, or provider I/O. |
| Enforceable deferred mapping | **Red (2026-08-25):** `go test -timeout 20m ./cmd/connectorgen -run '^(TestDeclarationAdmissionStripeAccountsDeleteIsDiscoverableDeferred|TestDeclarationAdmissionRejectsPolicyOnlyDeferredFoundation)$'` failed to build because an admission foundation could name only an ID/reason, not a missing implementation component and its evidence. | **Green:** the generic deferred-delete fixture has a cited source row, discoverable command, and `system/missing_foundation` preflight; a `blocked_by_default`-only foundation is rejected. | Captain clarification 007 moves individual provider mappings to `cli-batch1-repair-r1`. Uncommitted Stripe definition paths are preserved but excluded from this PR; no typed action/schema, credential, provider request, or live certification is added. |
| Audit repair: resolver, citation, and evidence | **Red (2026-08-25):** `go test -count=1 -timeout 20m ./cmd/connectorgen -run '^TestDeclarationAdmissionAuditRepair'` failed as expected. It showed duplicate exact provider identities, HTTP and credential-shaped citations, noncanonical command paths, false POST-delete metadata, and an excluded deferred target all produced no finding; the latter reached typed `missing_foundation` before its target was resolved. `go test -count=1 -timeout 20m ./internal/connectors/commandrunner -run '^TestPreflightDeferredCommand'` also showed a connector without exact-target preflight could receive that typed classification. | **Green (2026-08-25):** `go test -count=1 -timeout 20m ./cmd/connectorgen -run '^(TestDeclarationAdmission\|TestCheckCLISurfaceEndpointCoverageAllowsDeclarationBoundDeferredCommand)$'`, `go test -count=1 -timeout 20m ./internal/connectors/commandrunner -run '^TestPreflightDeferredCommand'`, and `go test -count=1 -timeout 20m ./internal/connectors/engine -run '^TestCommandSurfaceProjectsDeferredFoundationGap$'` pass. The shared resolver now rejects excluded, policy-only, duplicate, mismatched, and stale targets before `missing_foundation`; admission reuses the source-import citation policy and the real no-I/O command preflight. | Inline GSD gap-repair fallback: the Pi role runtime is unavailable and role spawning is forbidden, so the worker performs the generated discuss/plan/execute/verify/review steps directly. No connector-owned Stripe or concurrent Docker Hub file will be staged. |
| Semantic destructive metadata | **Red (2026-08-25):** the focused `destructive_post_lacks_destructive_metadata` and `delete_metadata_on_post` cases failed: admission ignored a source-declared destructive POST and accepted a declaration's self-authored delete label. | **Green:** the same focused cases pass. Applicability comes from the independent source row's `none`, `delete`, or `destructive` semantic; declaration metadata must match it exactly, and implemented rows additionally prove the runtime retains it. | No provider behavior or typed request body is inferred; the source cohort is the semantic authority and runtime is checked only for an implemented binding. |
| Exact-SHA audit repair | **Red (2026-08-26):** the audit reproduced a zero-work pass with no sidecars, source/declaration operation swaps at one endpoint, empty raw operation-ID rejection, false implemented GraphQL/operation-free/templated commands, destructive POST self-labeling, GraphQL deferred staleness, loss of typed `missing_foundation` at App/CLI boundaries, shipped preflight without `api_surface`, and malformed exact targets. The first clean green candidate then rejected 243 valid commands by comparing runtime-relative and provider-facing paths literally. | **Green:** focused suites now require two nonempty catalogs and exact expected counts; compare stable binding and source-owned destructive identities; share the engine's implemented resolver without changing runtime-valid base-path/placeholder aliases; reject GraphQL shared-transport swaps; embed the compact source ledger; preserve typed failures before credentials and through the public CLI; and reject noncanonical deferred/source endpoints. The full 5,149-command implemented-preflight sweep passes. | The green production catalog contains one existing GitHub implemented-delete control in shared root files only. Stripe and Docker Hub handoffs remain byte-identical and unstaged. |
| Exact-SHA R2 gap closure | **Planned Red (2026-08-26):** provenance-only duplicate rows under different bindings; same-binding wrong endpoint cases across stream/write/REST/binary; a locked 243-alias fleet census; GitHub GraphQL ETL canonical/transport separation; stale deferrals of runnable GitHub delete and GraphQL read/write controls across every allowed component; and the unclassified compact production ledger. | **Green:** source and compact-ledger duplicate keys exclude bindings while separately enforcing binding uniqueness; `ResolvedCommandBinding` retains canonical and transport targets with closed named proofs; arbitrary stream/write/REST/binary changes fail; GitHub GraphQL identity and Notion hook routing pass; commandrunner tries the real implemented path before accepting a deferral; idempotency was removed as a non-executor component; response/importer evidence is executor-specific; and inventory attributes the exact root ledger. Detached clean SHA `f97dede07` proves the locked 243 non-GraphQL aliases plus all 4 GraphQL operations, synthetic-discovery compatibility with real Outreach stream/write shapes, and the complete 477-test/example connectorgen inventory. | Inline GSD `plan-phase --gaps` / `execute-phase --gaps-only` fallback; no provider I/O, credential access, connector-definition mutation, dependency, or weakened runtime/live gate. |
| Exact-SHA R2 focused red | **Red (2026-08-26):** `go test -timeout 20m ./cmd/connectorgen ./internal/connectors/engine ./internal/connectors/defs ./internal/cli -run 'TestDeclarationAdmissionRejectsCompletenessAndBindingDefects\|TestDeclarationAdmissionUsesRuntimeResolverForImplementedCommandKinds\|TestResolveImplementedCommandBindingRejectsUnprovenEndpointAliases\|TestResolveImplementedCommandBindingNamesGraphQLStreamOperation\|TestEmbeddedArtifactClassRejectsBuildTimeArtifacts\|TestShippedDeclarationTarget'` failed as expected: a binding-swapped duplicate provenance row was missed; unrelated stream/write/REST/binary endpoints were silently substituted; a GraphQL stream identity was rejected as an HTTP path; and the shipped declaration target ledger was unclassified. The CLI stale-deferral table initially exposed a test import-alias compile error, corrected before production edits. | **Green:** the same focused packages pass; the expanded public CLI regression rejects every component for the runnable GitHub delete and GraphQL read/write controls with the explicit real-preflight stale reason. `TestDeclarationAdmissionOutreachRealBundleResolverCompatibility` passes only for generic admission/resolver preflight over real Outreach stream/write shapes with an in-memory discovery projection; it is not a shipped CLI or credential-boundary proof. | Tests and shared runtime only; no provider I/O or connector-owned definition edits. |
| Exact-SHA R3 canonical citation and claim repair | **Red (2026-08-26):** focused authoring and compact-ledger tests failed exactly as expected. Both consumers accepted uppercase DNS hosts, explicit `:443`, unstable query order, non-normalized escaped paths, trailing-dot hosts, and empty DNS labels. A second authoring row with another binding and `https://PROVIDER.EXAMPLE.TEST:443/v1/reference?b=2&a=1` evaded the canonical first row's duplicate identity and failed only for its deliberately absent declaration. The existing Outreach test also overclaimed actual CLI/credential-boundary coverage even though it synthesizes the missing discovery projection in memory. | **Green:** `internal/safety.CanonicalProviderCitationURL` is shared by authoring admission and the compact runtime ledger. Both require stored URLs to equal the public-HTTPS canonical form and key provenance by the returned identity; focused safety, authoring, and ledger tests pass for host case, `:443`, query order, normalized escaping, dot-segment rejection, trailing-dot/ambiguous hosts, and binding-independent duplicates. The renamed Outreach regression/docs now claim only real-bundle resolver compatibility. | Schema v1 remains unchanged. No Outreach, Stripe, Docker Hub, or other Batch-1 connector definition is edited; final real Outreach reachability stays a combined-head gate after #4350 repair. |
| Exact-SHA R5 reviewed source denominator | **Red (2026-08-26):** `go test -count=1 -timeout 20m ./cmd/connectorgen -run 'TestDeclarationAdmissionBindsSourceRowsToReviewedConnectorOwnedLock\|TestDeclarationAdmissionIndependentInventoryCannotShrinkWithCatalogRows\|TestDeclarationAdmissionAcceptsProviderEvidencedUnsupportedAcrossAllSixLanes'` failed. The current v1 schema demanded adjacent `expected_*` counts and had no inventory/lock seam; unrelated-host, nonexistent-operation, semantic-alias, connector-ownership, and row-deletion fixtures could not be certified by an independent denominator. | **Green (2026-08-26):** `go test -count=1 -timeout 20m ./cmd/connectorgen -run '^TestDeclarationAdmission'` passes. The v1 independent inventory selects exact connector-owned reviewed lock operations; v2 catalogs reject expected counts; URL, location, protocol, provider ID, method, path, ownership, and operation existence must match exactly. The production command reports one clean GitHub operation. | Offline structural admission only: no provider I/O, retained artifact read/hash recomputation, credential, or runtime/live certificate. Existing GitHub lock is referenced but no connector-owned file changes. |
| Exact-SHA R5 provider-evidenced unsupported | **Red (2026-08-26):** the same focused connectorgen run rejected all six `unsupported_with_provider_evidence` lanes at the old source-catalog schema boundary. `go test ... ./internal/connectors/commandrunner` failed to compile because `PreflightRequest` does not exist, and the existing path-only unsupported branch carries no typed provider-evidenced terminal. | **Green (2026-08-26):** all six authoring lanes pass with exact discovery evidence and no executor/foundation target. Commandrunner's focused typed-terminal regression passes with `system/provider_evidenced_unsupported`; malformed dispositions fail closed. | This disposition does not omit the operation, silently change its lane, or claim live/runtime usability. |
| Exact-SHA R5 public input ordering | **Red (2026-08-26):** the focused commandrunner suite failed at the absent `PreflightRequest` API. The focused CLI run let invalid GitHub/synthetic inputs reach `withApp`; the concurrent unstaged Stripe handoff then caused the embedded registry to panic on its intentionally incomplete foundation object. That concurrent failure is not absorbed: it proves invalid input was not terminated before app/credential composition. | **Green (2026-08-26):** `TestPreflightRequestValidatesDeclaredInputsWithoutExecutor` and `TestConnectorCommandInputDefectsFailBeforeWithApp` pass for required, unknown, enum, minimum, env-only, and help-first behavior. `PreflightRequest` and runtime mapping helpers share `validateCommandFlagSet`; resolved env-only values and config are validated before `withApp`. The exact GitHub missing-input/then-missing-credential case is reserved for the clean exact-head checkout because the concurrent Stripe handoff is intentionally not absorbed. | Existing credential resolution, runtime preflight, executor, binary proof, and live certification remain strict and separate. Stripe/Docker Hub bytes remain untouched and will be excluded from a clean exact-head verification checkout. |
