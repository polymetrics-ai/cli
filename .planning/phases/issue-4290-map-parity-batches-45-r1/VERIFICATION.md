# Verification Checklist — Issue #4290

- [x] Every Batch 4 source lock/map inventory is regenerated from a provider-complete source or carries an explicit dynamic/unavailable basis (`materialize-parity-maps.mjs check batch4`).
- [x] Every Batch 5 source lock/map inventory is regenerated from a provider-complete source or carries an explicit dynamic/unavailable basis (`materialize-parity-maps.mjs check batch5`).
- [x] Per connector source report records old `api_surface` operation count, regenerated count, and source basis (`SOURCE-INVENTORY-REPORT.md`).
- [x] Every map has valid six-class parity classification and DELETE coverage (Batch 4 and Batch 5 check assertions).
- [x] Every unauthored operation is `declaration-pending`, not `foundation-gap` (Batch 4 and Batch 5 check assertions). The merged #4304 foundation head closes the stale generic App/CLI dispatch claim; a missing connector-owned destination transport remains declaration-pending.
- [x] Buildkite v1 sensitive-operation provenance recovery: targeted `connectorgen validate internal/connectors/defs/buildkite --json` first reported 100 missing provider citations; after rematerialization through the pinned retrieval-artifact fallback it reports zero findings, and the ledger has 100 cited operations with zero missing sensitive citations. The source lock is correctly named `buildkite-parity-source-lock.json` so the current canonical source-import validator does not parse map evidence as an executable source-import contract.
- [x] Seven-surface audit: `materialize-parity-maps.mjs seven-surface-ledger --check` asserts exactly 20 assigned rows; every existing typed write has one explicit reverse-ETL eligibility disposition. The generic App/CLI dispatch is available from the merged #4304 head; missing connector-owned destination transport/conformance declarations are `declaration-pending`.
- [x] Captain hard-gate artifact: `materialize-parity-maps.mjs hard-premerge-gate --check` deterministically joins 7,301 enumerable provider-source operations to one canonical map row each, preserves source/document trace and separate six-surface disposition, and lists three unenumerable source inventories explicitly. `pending` and `not-asserted` never count as N/A.
- [ ] Captain hard-gate completion: source-bound partial commands are now declared for 6,945 source operations and dispatch fail-closed rather than becoming unknown commands, but no partial command is execution proof. Runtime reachability, generated website row, and executable fixture/conformance evidence remain pending; `HARD-PREMERGE-GATE.json` intentionally reports zero proved values. The map PR remains an evidence-bearing dependency increment, not an assertion that provider operations were exercised.
- [x] Captain foundation-gap artifact: `materialize-parity-maps.mjs foundation-gap-ledger --check` deterministically reports a clear zero-row foundation-gap ledger. Its explicit clear state proves the materializer does not treat missing connector declarations as engine work.
- [x] Foundation dependency: `94e5496ed` from `origin/fm/cli-reverse-etl-destination-r1` is an ancestor of this branch. Its current App/CLI generic-destination dispatch tests are run as a focused merged-tree proof; no connector-specific workaround or fabricated transport declaration is used.
- [x] Final generated and repository gates: `GOFLAGS=-p=3 go run ./cmd/connectorgen validate --json` checked 552 connectors with 0 findings and warnings; `GOFLAGS=-p=3 go run ./cmd/connectorgen surface-sync --check` scanned 552 connectors with 0 fields filled/corrected; detached/polled `GOFLAGS=-p=3 make connector-boundary` exited 0; final serialized `GOFLAGS=-p=3 make verify` exited 0 after transcript, certification-subject, and operation-evidence regenerations.
- [x] Generated root help transcript recovery: the initial full `make verify` failed only `internal/cli:TestGoldenTranscripts` for the nine root-help variants, whose connector-command listing now includes the assigned namespaces. The repository's targeted fixture updater refreshed those nine records; a targeted non-update rerun is green. The full suite remains pending its final rerun.
- [x] Generated certification-subject recovery: the subsequent full gate reached `connectorgen-certification-subject` and correctly identified an out-of-date declaration/CLI fingerprint. `go run ./cmd/connectorgen certification-subject` regenerated only `internal/connectors/certifications/current-subject.json`; the check will be re-exercised by the final full suite.
- [x] Generated operation-evidence recovery: the next full gate reached `connectorgen-operation-evidence` and correctly identified only its stale generated projection. `go run ./cmd/connectorgen operation-evidence --write-fixed-100` refreshed `internal/connectors/operation-evidence.json` (1,525 rows, five rollups); its `--check`, certification artifact checks, GitHub parity checks, connector canon, and release-workflow checks were rerun individually before the final full suite.
- [x] Focused tests are green: `go test -timeout 20m ./cmd/connectorgen`, `go test -timeout 20m ./internal/connectors/engine`, and `go test -timeout 20m ./internal/connectors/defs/zendesk-support`. The Zendesk test drives the newly discovered `DELETE /api/v2/api_tokens/{id}` through the real typed write, plan/preview/approval path, and capture server.
- [ ] Local limitation: `go test -timeout 20m ./internal/cli` timed out after 20m while concurrent full CLI suites were active. It timed out during `TestScheduleCLI_Create_InvalidName` while loading the all-connector registry (`engine.loadOperationEndpointLedgers`); no assertion failure was reported. Log: `/tmp/cli-map-batch45-cli-test.log`. It is left to CI rather than retried concurrently.
- [x] Repository generated/docs and compile checks: final `make verify` passed `fmt`, tidy, vet, all tests, build, docs validation, smoke, lint, agent-contract, connectorgen, certification, connector-boundary/canon, and release workflow gates. `make docs-check` also passed after regenerated connector manuals, skills, catalog, and website catalog data.
- [x] PR #4295 base is `main`: after PR #4304 merged, `gh-axi api PATCH /repos/polymetrics-ai/cli/pulls/4295 --field base=main` completed and the GitHub API read-back confirms `base: main`, `head: fm/cli-map-batch45-r1`, and an open PR.
- [x] Post-main reconciliation: merged `origin/main` normally at `98f7291a0` (no rebase or force-push). Regenerated the conflict-only HubSpot manuals/skills, website catalog, certification subject, and operation evidence from the merged source. Batch/parity checks, 552-connector validate (0 findings/warnings), surface sync (0 changes), detached/polled boundary, and final serialized `GOFLAGS=-p=3 make verify` all pass on the merged tree.
- [x] Post-merge website CI repair: remote `Website checks` initially failed because its Zendesk API-data test still expected the former operation-backed `get_sources_by_target` command. The command is now a source-bound partial direct read with no invented operation contract, so the test asserts its path, partial availability, source URL, and locked-source note. Local `pnpm test:unit -- tests/api/connector-data.test.ts` reports 12 files / 80 tests passing; lint (warnings only), typecheck, and website script tests pass.
