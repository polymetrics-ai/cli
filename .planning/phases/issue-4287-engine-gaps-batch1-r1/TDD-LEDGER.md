# TDD Ledger: Issue #4287

## Planned Red/Green Evidence

| Slice | Red | Green | Edge/Regression |
| --- | --- | --- | --- |
| Operation pagination | An operation cannot select page/page-size or start-index/count independently from global next-url pagination. | Source-derived operation pagination controls its direct-read request and navigation. | A conflicting source declaration rejects before the provider double receives a request; global fallback remains intact. |
| HEAD status | A HEAD operation is rejected as an invalid `rest_read`. | A typed HEAD executor reports bounded status metadata without JSON decoding. | Invalid method/body/output combinations reject with zero sends. |
| JSON-family writes | `application/scim+json` is rejected despite being JSON. | Closed documented JSON-family content type sends the correct bytes. | Unknown media type rejects before I/O; legacy JSON/form behavior remains unchanged. |
| Text export | A text/csv export without binary metadata cannot declare an explicit safe executor. | A capped text export streams only within its declared byte cap. | Missing/zero cap rejects before I/O and an over-cap response leaves no output artifact. |
| Secret response | Live sensitive writes are blocked and no response destination protects a returned credential. | A secret response is persisted through the encrypted credential-store seam while its declared runtime response remains complete. | A missing declared encrypted store or incompatible policy rejects before I/O; the runtime retains the provider response without redaction. |

## Actual Evidence

### 2026-08-19 — pagination red checkpoint

- Red: `go test -timeout 20m ./internal/connectors/engine -run 'TestOperationDirectRead(UsesOperationDeclaredPagination|RejectsPaginationThatDisagreesWithSourceBeforeIO)'` failed to compile because `RESTOperationSpec` has neither `Pagination` nor `PaginationParameters`. The tests establish operation-owned pagination and source/spec mismatch refusal before I/O.
- Green: `gofmt -w internal/connectors/engine/bundle.go internal/connectors/engine/direct_read.go internal/connectors/engine/direct_read_paginate.go internal/connectors/engine/direct_read_pagination_test.go && go test -timeout 20m ./internal/connectors/engine -run 'TestOperationDirectRead(UsesOperationDeclaredPagination|RejectsPaginationThatDisagreesWithSourceBeforeIO|PageOneUsesDeclaredPageSize)'` passed. Existing global pagination remains covered by `TestOperationDirectReadPageOneUsesDeclaredPageSize`.
- Manual GSD fallback: recorded in `CONTEXT.md` because `init.phase-op 4287` reports `phase_found: false` and the task forbids role spawning.

### 2026-08-19 — remaining capability slices

- HEAD status Red: `go test -timeout 20m ./internal/connectors/engine -run 'TestOperationStatusCheck'` failed with undefined `OperationStatusCheck` and `connectors.OperationStatusCheckRequest`; Green: the same command passed after adding the distinct `rest_status`/`status_check` executor and preflight contract.
- JSON-family writes Red: `go test -timeout 20m ./internal/connectors/engine -run 'TestOperationDirectWriteContentType'` failed because `application/scim+json` was not supported; Green: `gofmt -w internal/connectors/connsdk/http.go internal/connectors/engine/direct_write.go internal/connectors/engine/direct_write_test.go && go test -timeout 20m ./internal/connectors/connsdk ./internal/connectors/engine -run 'TestOperationDirectWriteContentType'` passed. The unknown media-type case uses `PreviewOperationDirectWrite` and proves zero sends.
- Text export Red: `go test -timeout 20m ./internal/connectors/engine -run 'TestOperationTextExport'` rejected the new declaration as not `binary_download`; Green: `gofmt -w internal/connectors/engine/binary_read.go internal/connectors/engine/bundle.go internal/connectors/engine/text_export_test.go && go test -timeout 20m ./internal/connectors/engine -run 'TestOperationTextExport'` passed, including zero-I/O unbounded refusal and no partial artifact on cap overflow.
- Secret response Red: manual source verification found `SensitivePolicySpec` documented as schema/validator-only with live writes blocked, with no response sink. Green: `gofmt -w internal/connectors/engine/bundle.go internal/connectors/engine/direct_write.go internal/connectors/engine/direct_write_test.go && go test -timeout 20m ./internal/connectors/engine -run 'TestOperationDirectWrite(StoresReturnedSecretAndRetainsRuntimeResponse|RejectsSecretResponseWithoutEncryptedStoreBeforeIO|ContentType)'` passed. The test uses a canary, asserts it reaches the declared credential `SecretStore` for encrypted-at-rest persistence, and asserts the complete provider response remains available to the runtime; a missing storage destination is refused before I/O.
- Captain-policy correction Red: the existing `TestOperationDirectWriteRedactsDeclaredGraphQLInputFromProviderErrors` proved the old GraphQL path removed a provider error. Green: `TestOperationDirectWriteSecretOperationRetainsProviderErrors` now proves a secret operation retains its complete provider error, including a token-shaped canary, while the encrypted-store response path remains mandatory. Non-secret legacy redaction behavior remains unchanged.
- Contract-mirror Red: review found `secret_stored`, `application/scim+json`, `status_check`, and `text_export` missing from one or more generated-surface mirrors despite the engine admitting them. Green: `go test -timeout 20m ./internal/connectors/engine ./internal/connectors/commandrunner ./internal/connectors/connsdk ./cmd/connectorgen` passed after adding the closed schema, generator, adapter, and command preflight mirrors. The status cap and bodyless contract, text export cap/media type, and unknown write type remain refusal-before-I/O checks.
- Cross-slice Green: `go test -timeout 20m ./internal/connectors/engine ./internal/connectors/commandrunner ./cmd/connectorgen` passed after operation-source parameter import and command-surface synchronization support.
