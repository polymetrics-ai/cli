# Discussion log — CI runner routing

The worker brief fixes the routing policy, trusted identities, runner label,
non-PR policy, Windows exception, website deployment routing, and required
PR-body follow-ups. No product decision remains open. The requested
`discuss-phase` prompt was generated and completed inline because this is an
issue-scoped worker task, not a numbered ROADMAP phase, and the worker brief
forbids role spawning.
