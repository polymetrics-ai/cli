# #4067 TDD ledger — acknowledged transport completion rebase

**Issue:** #4067
**Starting candidate:** `883a86cf0040d559edcd4777413d1c2de20cd94a` (immutable rejected baseline)
**R2 rejected candidate:** `3f84693bfbc128523a66e22653db7227fb9c0869`
**Correction ledger:** 3/5 consumed by prior #4067 delivery validation; r3 may submit only loop 4/5, never `--yes`.
**Safety:** deterministic fake-backed JSON-state tests are the primary evidence. Existing-connector proof is separately bounded to current definition/preflight/inspection tests; any credentialed read-only smoke may run only when an approved credential is already available through its sanctioned channel, with no token disclosure, provider mutation, warehouse mutation, container, or certification claim.

| ID | Requirement | RED command / expected failure | GREEN evidence | Status |
|---|---|---|---|---|
| C1 | An acknowledged run interrupted by an unrelated post-checkpoint writer returns zero/non-terminal and is durably `running` after reopen on the rejected baseline. | **Observed:** `go test -json -count=1 -timeout 20m ./internal/app -run '^TestRunETLTransportAcknowledgedCompletionRebasesUnrelatedStateForAllModes$'` exited `1`. Each canonical mode first retained the acknowledged target stream and unrelated writer state, then observed zero returned `Run`, durable `running` target run, and zero completion timestamp. | The current r2 combined focused suite exits `0` in every canonical mode. Returned and reopened runs are matching terminal `completed` records; acknowledged checkpoint identity and unrelated writer data remain intact. | Green |
| C2 | Latest-state completion is eligible only for a matching `running` target run and exact acknowledged target stream. | `TestRunETLTransportAcknowledgedCompletionFailsClosedWhenTargetChanges` drives changed, missing, and terminal targets. | The current r2 combined focused suite exits `0`: all three leave their current target state intact, return `Run{}`, and retain `errors.Is(err, errStateRevisionConflict)`. | Green |
| C3 | Eligible completion changes only target terminal fields and its own final metadata; winner/acknowledged checkpoint and unrelated state survive. | Snapshot before/after state around a second unrelated writer. | C1's current r2 all-seven witness proves target checkpoint connection/stream/generation/timestamp/opaque identity stays unchanged while only final stream/run metadata advances; unrelated stream/checkpoint/run survives. | Green |
| C4 | Returned run and reopened durable run agree; ordinary revision-conflict/error chain remains detectable; definite/committed/indeterminate persistence outcomes stay truthful. | State-store outcome fixtures fail before rule exists or expose a speculative return. | The current r2 focused rerun of `TestRunETLTransportAcknowledgedCompletionReturnsTruthfulPersistenceOutcome` exits `0`: definite pre-commit returns zero/running; committed unlock and indeterminate directory-sync outcomes return matching terminal runs and retain `CommitOutcomeError`; fail-closed C2 retains revision-conflict detection. | Green |
| C5 | Cancellation after acknowledgement and before completion remains truthful. | Deterministic cancellation fixture. | The current r2 combined focused suite exits `0`: each mode retains the acknowledged checkpoint, returns/durably stores a failed run, and retains `errors.Is(err, context.Canceled)`. | Green |
| C6 | Same interleaving is correct in all canonical modes. | Table-driven `full_overwrite`, `full_append`, `incremental_append`, `incremental_upsert`, `incremental_dedupe`, `incremental_dedupe_history`, `change_capture`. | The current r2 focused normal suite exits `0` across all seven modes for C1, C5, C8, and C9. `go test -race -count=1 -timeout 20m ./internal/app -run '^(TestRunETLTransportAcknowledgedFailureAfterUnrelatedRevisionForAllModes|TestRunETLTransportAcknowledgedFailurePreservesSourceError|TestRunETLTransportAcknowledgedFailureFailsClosedWhenTargetChanges|TestRunETLTransportAcknowledgedCompletionMissingRunIsTypedConflictForAllModes|TestRunETLTransportAcknowledgedFailureReturnsTruthfulPersistenceOutcome|TestRunETLTransportStaleWriterFinalizesLosingRun)$'` exits `0` with no Go race report. | Green |
| C7 | #4046 typed-conflict-only behavior and R7/R8 per-stream CAS/source identity remain unchanged. | Existing focused regression commands. | The current r2 #4046 repeated/reopen/cancellation and all-seven selectors exit `0`; the R7/R8 source-identity/per-stream-CAS selector exits `0`. The narrowed r2 error finalizer does not change ordinary `failRun`, checkpoint CAS, or destination replay. | Green |
| C8 | After acknowledgement, an unrelated persisted revision plus cancellation leaves the rejected r2 candidate with a zero returned run and a durable `running` record; GREEN returns a matching durable failed run while retaining `context.Canceled`, one apply, and exact acknowledged/unrelated state. | **Observed:** `go test -json -count=1 -timeout 20m ./internal/app -run '^TestRunETLTransportAcknowledgedFailureAfterUnrelatedRevisionForAllModes$'` exited `1`. Every canonical mode first preserved the exact acknowledged target stream, unrelated stream/checkpoint/run, and one destination apply; then it observed `Run{}` and the matching durable target run still `running` while `errors.Is(err, context.Canceled)` remained true. | **Observed:** the exact selector exits `0` after the minimal r2 change. Every mode returns a matching reopened `failed` run, retains `errors.Is(err, context.Canceled)`, preserves acknowledged/unrelated state, and applies exactly once. | Green |
| C9 | A representative post-ack source error follows the same guarded finalizer, preserving the source sentinel chain and one apply; a missing exact target run in every mode returns `Run{}` with `errStateRevisionConflict` detectable and no state mutation. | **Observed:** `go test -json -count=1 -timeout 20m ./internal/app -run '^(TestRunETLTransportAcknowledgedFailurePreservesSourceError|TestRunETLTransportAcknowledgedCompletionMissingRunIsTypedConflictForAllModes)$'` exited `1`. The source sentinel stayed detectable but returned `Run{}`/durable `running`; every missing-run case preserved state and one apply but returned plain `run \"...\" not found`, not `errStateRevisionConflict`. | **Observed:** the exact selector exits `0`. The source sentinel reaches a matching durable failed run with one apply; all seven missing-run cases return `Run{}`, preserve reopened state, apply once, and retain `errors.Is(err, errStateRevisionConflict)`. | Green |
| C10 | The new acknowledged-error finalizer preserves state-store outcome truth and the initiating source-error chain. | Coverage expansion after the committed C8/C9 behavioral RED: definite non-commit must not return a speculative failed run; committed/indeterminate writes must return the matching durable failed run. | **Observed:** `go test -count=1 -timeout 20m ./internal/app -run '^TestRunETLTransportAcknowledgedFailureReturnsTruthfulPersistenceOutcome$' -v` exits `0`. It retains the source sentinel in all outcomes; definite pre-commit preserves the complete reopened state and returns `Run{}`, while committed unlock and indeterminate directory-sync outcomes return matching durable failed runs with the typed commit outcome. | Green |
| C11 | The acknowledged-error finalizer fails closed if its exact target stream changes/disappears or its target run becomes terminal/disappears after acknowledgement. | Review finding CR-001: no direct r2 error-path guard witness existed after C8/C9. | **Observed:** `go test -count=1 -timeout 20m ./internal/app -run '^TestRunETLTransportAcknowledgedFailureFailsClosedWhenTargetChanges$' -v` exits `0`. All four cases apply once, preserve the latest persisted state, return `Run{}`, and retain both the original post-ack source error and `errStateRevisionConflict`. | Green |
| C12 | After page one is durably acknowledged, a real winner advances the target stream; the loser applies page two and loses its per-stream CAS. In all seven modes, the typed conflict must return a non-zero failed loser matching reopen without retry/replay. | **Observed RED:** `go test -count=1 -timeout 20m ./internal/app -run '^TestRunETLTransportAcknowledgedPageThenStaleSecondPageFinalizesLosingRunForAllModes$' -v` exited `1`. Every mode first proved the durable page-one checkpoint, separate real winner checkpoint/run, unrelated stream/checkpoint/run, typed conflict, and exactly two loser applies; it then observed `Run{}` and the reopened loser still `running`. | **Observed GREEN:** after the exact typed-conflict early branch in `failAcknowledgedTransportRun`, the same command exits `0` across all seven modes. It returns/reopens a matching failed loser, retains `errors.Is(err, errTransportStreamStateConflict)`, preserves winner/unrelated state, and keeps the loser apply count at two. | Green |
| C13 | Existing-connector evidence is distinct from core proof and production registration is reported truthfully. | **Observed:** built the real `pm`; hook, definition, command-runner runtime-preflight, connector/CLI, and bounded GitHub-proof-harness tests pass. Binary inspection reports both GitHub and PostgreSQL source/destination transport roles `unsupported` and both connectors `COMMUNITY BUILD, UNCERTIFIED`. | Verified locally with a downstream limitation: no approved credential/name or sanctioned secret channel was supplied in this custody, so no credentialed provider smoke was attempted, no credential was probed/created, and no live warehouse/certification claim is made. This cannot replace C12 core proof or the downstream #4015 wiring/certification gate. | Green with declared downstream limitation |

## Commit gates

1. R2 planning evidence — committed before production paths.
2. R2 RED — test/evidence only, committed before production mutation; C8/C9 commands exited non-zero for the durable symptom and missing typed chain.
3. R2 GREEN — this implementation checkpoint contains the smallest acknowledged-error implementation, passing the matching C8/C9 commands and C10 outcome coverage.
4. Focused coverage/review fixes only after their corresponding checks pass; C6/C7/C11, affected-package, repository, website, and manual GSD verification/review gates are complete. Generated artifacts remain out of the r2 production scope because canonical checks found no r2-owned drift.
5. Fresh no-mistakes loop 3/5 passed locally without `--yes`: review, the focused persisted-state/race suite, documentation, and lint completed with no finding; push/PR/CI were skipped because current help has no safe existing-#4059 route. Its document agent nevertheless committed an unrelated warehouse-architecture rewrite. The follow-up scope-restoration commit cancels that unrelated diff while retaining the completed run in history. Stop for Firstmate direction before any push or PR mutation.
6. R3 planning evidence — completed in `0d448ea10`; #4067 was amended/read back, GSD prompts resolved, and C12/C13 were recorded before a production edit.
7. R3 RED — this test/evidence checkpoint is committed before `internal/app/app.go` mutation; C12 exits non-zero for the observable durable-running leak.
8. R3 GREEN and verification — smallest typed-conflict early branch, matching all-seven green/race/core/connector evidence, manual GSD verify/review, then only local no-mistakes loop 4/5 with push/PR/CI/document skipped.
