# Test plan

| Behavior | First red evidence | Green evidence |
| --- | --- | --- |
| `media.download` is command-reachable through `binary_download` | Focused commandrunner/CLI test or preflight failure before metadata is promoted | Targeted test plus `TestEveryImplementedCommandPassesRuntimePreflight` |
| `reports.query` remains honestly planned | Surface inspection verifies it has no executable `provider_search`/`rest_write` mapping | Surface validator and command help preserve planned availability without claiming reachability |
| Declared request fields have evidence | Citation matrix review detects every missing field | Connector validation and matrix accounting show all fields cited or tier-5 deferred |
| Analytics selectors fail closed before requests | Review reproduces the optional/absent `groups.list` and `groupItems.list` selectors | Required typed CLI flags plus non-optional stream query interpolation reject missing selectors |
| Documented mutations keep executable contracts | Review compares all seven provider mutations against CLI and write action mappings | Per-mutation research ledger records seven specific `reverse_etl` actions and no `rest_write` |
| Generated surface stays in parity | `surface-sync --check` fails before regeneration when input drift exists | `surface-sync`, `--check`, and website data generation succeed |

No credentialed provider execution or reverse-ETL execution is in scope.
