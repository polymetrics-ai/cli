# TDD ledger — #4354

| Slice | Red evidence | Green evidence | Refactor / notes |
| --- | --- | --- | --- |
| Source inventory and schema-v3 evidence | `go run ./cmd/connectorgen validate internal/connectors/defs/outreach --json` exits 1: `parse source lock: json: unknown field "source_kind"`. `source-import` repeats the strict-parser failure; scoped `surface-sync --check` reaches `canonical source descriptor is missing`. | Intentionally unavailable locally: Firstmate directs PR #4350 to own shared source-evidence compatibility. The Outreach candidate has no retained source-artifact manifest or canonical descriptor, so a local green would require unapproved source refetch/reconstruction or a competing shared change. | The imported 259 source rows remain intact. `outreach-six-lane-evidence.json` records the unadmitted inventory and explicit foundation dependency rather than downgrading commands or accepting missing citations. |
| Command preflight boundary | Before adding the regression test, direct fixture-free invocations of `outreach prospects list`, `create account note apply`, and `delete account apply` established the required observable boundary: `missing --credential`. | `go test -timeout 20m -v ./internal/cli -run 'TestPMBinaryOutreach(RepresentativeCommandsReachCredentialBoundary|FixtureBindsDeclaredMethodAndPath)$' -count=1` passes. The new binary tests cover ETL, typed write, destructive delete, local-fixture GET route, and caller-supplied method/path/source-url rejection. | Fixture-only; no provider I/O, real credential, or reverse execution. |
| Artifact currentness | The first full `go test -timeout 20m ./internal/cli` run exposed two generated user-surface drifts caused by the new command namespace: `TestSkillsGenerateMatchesTrackedSkills` and `TestGoldenTranscripts/root_bare_manual`. | `./pm skills generate --dir docs/skills --json`, `TestSkillsGenerateMatchesTrackedSkills`, and the supported targeted `POLYMETRICS_UPDATE_GOLDEN_TRANSCRIPTS=1` golden update all pass. A complete `go test -timeout 20m ./internal/cli` process then completed; its follow-up cache read returned `ok   polymetrics.ai/internal/cli (cached)`. The all-implemented runtime preflight sweep and full `cmd/connectorgen` package also pass. | The correction is generated user-facing evidence only: `docs/skills/pm-outreach/SKILL.md` and the root-help golden transcript. No generator, reader, validator, or schema implementation is changed. |
