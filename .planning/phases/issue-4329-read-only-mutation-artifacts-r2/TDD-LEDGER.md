# TDD ledger — issue #4329, r2

| Slice | Red evidence | Green evidence | Refactor / scope evidence |
| --- | --- | --- |
| Write-disabled source mutation artifact | **Red:** `go test -count=1 -timeout 20m ./cmd/connectorgen -run '^(TestSourceProjectionWriteDisabledLockedSourcesRetainMutationArtifacts|TestSourceProjectionWriteDisabledMutationArtifactsPreserveExecutableDeletes)$'` failed to compile: `sourceProjectionApplyWriteDisabledMutationArtifacts` was undefined. | **Green:** the focused command passes after the source-import annotation derives the existing named non-executable mutation artifact. | No connector-specific branch or count waiver; exact source citation is required. |
| Read/ETL preservation | **Red:** the source-shaped fixture could not apply a mutation artifact before the shared helper existed. | **Green:** Sentry- and Vercel-shaped retained-source imports preserve their implemented GET command, return zero executable-coverage findings, and leave writes/CLI bytes unchanged. | No action, request schema, transport, command, or partial declaration is generated. |
| Sentry/Vercel acceptance | **Red:** covered by the missing shared helper compilation failure. | **Green:** exact preserved source-lock citations exercise Sentry `createOrganizationDashboard` (SHA `b712166…bd435`) and Vercel `deleteStorageStoresBlobById` (SHA `74cb7f…3da28`), including byte counts, URLs, locations, method/path, and source IDs. | Vectors prove source-lock/citation semantics, not a connector-name or count bypass; no provider document is copied or changed. |
| Executable-write boundary | **Red:** covered by the missing helper compilation failure. | **Green:** complete delete/reverse-ETL stays implemented and receives no artifact; a write-capable Vercel-shaped bundle still refuses an absent action. | Real complete/implemented actions stay implemented; no safety suppression. |
| Source-import command integration | **Red:** helper did not exist. | **Green:** `runSourceImportWithFetcher` reads explicit write-disabled metadata, writes the generated source descriptor, and then succeeds in `--check` mode with the same cited artifact. | The descriptor is regenerated from the retained lock; no local source repair or importer bypass is introduced. |
| Audit: write-capable artifact bypass | **Red:** with the validator policy check temporarily absent, `TestSourceProjectionWriteCapableBundlesDoNotAutoDeferMutations` failed: `write-capable automatic artifact coverage findings = [], want policy refusal`. | **Green:** the same focused test passes after both projection and `validateSourceExecutableCoverage` require explicit `metadata.capabilities.write=false` for the automatic reason. | Manual source-cited dispositions retain their existing policy; only the exact automatic reason is constrained. |
| Preserved full source-lock inventory | **Red:** the independent audit correctly found that hand-constructed operation values did not prove the actual source locks. | **Green:** immutable Sentry/Vercel source-lock fixtures are byte-identical to the preserved locks; test loads all 223/400 operations, retains all 103/237 classified mutations, then validates each selected real read/mutation pair. | Provider documents and preserved worktrees remain untouched; only read-only test fixtures were copied byte-for-byte. |
| Current-main integration (`1324c52…`) | **Red:** no new production behavior was introduced by the integration; the original missing-helper red is recorded above. The incoming declaration-admission changes exposed the need for a stronger source-lock executable-delete regression. | **Green:** after the clean no-force merge of current `origin/main`, `TestSourceProjectionWriteDisabledMutationArtifactsPreserveSourceLockedExecutableDeletes` loads the actual Sentry `deleteOrganizationDashboard` and Vercel `deleteStorageStoresBlobById` entries, binds each to a declared destructive delete/reverse-ETL action, and proves that automatic artifact retention returns zero while coverage remains clean. | Test-only strengthening: it neither edits a source lock nor manufactures a production action, schema, transport, command, or partial route. The action-first rule remains connector-neutral. |
