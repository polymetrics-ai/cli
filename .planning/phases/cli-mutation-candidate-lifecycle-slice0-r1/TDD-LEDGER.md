# Slice 0 TDD ledger

| Step | State | Required observable | Evidence |
| --- | --- | --- | --- |
| Mutation inventory projects all eligible declared commands | RED | A test fails until it sees exactly 279 `direct_write` and 577 `reverse_etl` fixture-required candidates derived from declarations. | `go test -timeout 20m ./cmd/connectorgen` failed as intended: `CertificationMutationCandidateGeneration`, the mutation candidate types, and `buildGeneratedMutationCandidates` are undefined. |
| Generic projection emits execution identity without I/O | GREEN | The preceding test passes; each candidate has a declaration binding and derived invocation fields, with no provider request. | `go test -count=1 -timeout 20m ./cmd/connectorgen` passed. `TestBuildGeneratedMutationCandidatesDerivesDeclaredExecutionContract` proves tokens, `--credential`, flags, slots, declaration, executor, and address come from synthetic declarations. |
| Classification is exhaustive and exclusive | GREEN | A test fails on zero, duplicate, or unknown classification and on a bucket total other than 856. | The same package test passed. `TestGitHubFixtureRequiredMutationCohortGeneratesEveryCandidate` derives 279 + 577 = 856, compares all committed rows with the projection, and sums classification buckets. Multiple family matches return an error. |
| Unassessed fails closed | GREEN | A test fails if an unmatched candidate is classified `contained`; it must emit `unassessed` with evidence. | `TestBuildGeneratedMutationCandidatesClassifiesEscapeAndFailsClosed` passed: its unmatched declaration receives `unassessed`, never `contained`; it also rejects a selector outside the cohort. |
| Escape classifier refuses unsafe declarations | GREEN | Paid-seat, outside-invitation, public-publication, and third-party test declarations must not be contained. | The synthetic test passed for `real_money`, `real_people`, `public_visibility`, and `third_party_scope`; none is `contained`. |
| Contained declaration is accepted | GREEN | A disposable-scope declaration produces `contained` with the connector-owned evidence path. | The synthetic disposable widget declaration passed with `contained` and its connector-owned evidence. |
| Derived REST collection cycle is mechanical | RED → GREEN | A terminal item path joins the POST/PUT sibling collection, parent collections sort first, and no-creator paths stay named exceptions. | RED: changed the synthetic child collection expectation to depth 4; `go test -count=1 -timeout 20m -run TestBuildGeneratedMutationCandidatesDerivesRESTCollectionCycleInURLDepthOrder -v ./cmd/connectorgen` failed with `collection depth = 5, want 4`. GREEN: restored the declared depth and the same command passed. |
| Generated artifacts are canonical | GREEN | Generator rerun produces no diff and the sweep remains 1,571. | Candidate SHA `7f1608b713b08da357a9e7a5e6a894c93bfdd67442df6aa83d7cc6478d6f23e1` and sweep SHA `494421bdc4438b23e013fe3c1c7cd9175ad605c652eed02a26e10656c32cda3e` were each stable on the second run; both `--check` commands passed and `make verify` passed. See `VERIFICATION.md`. |
