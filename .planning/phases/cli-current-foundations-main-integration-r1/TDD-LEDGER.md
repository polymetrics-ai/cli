# TDD Ledger — Current Foundations Main Integration r1

Every code or conflict correction begins with a focused production-shaped failing test. The component merge itself does not replace this rule.

| ID | Requirement | Red evidence | Green evidence | Refactor evidence | Status |
| --- | --- | --- | --- | --- | --- |
| INT-01 | Structured body + typed header + exact query/path binding | `TestDirectWriteCommandBindsStructuredBodyHeadersQueryAndPath` failed before I/O: a declaration-fixed `payload.fixed` field was treated as missing when the caller supplied only `payload.name`. | Passed after structured composition: the persisted plan bound `PATCH /api/widgets/{id}`, `dry_run=true`, exact `X-Change-Reason`, and the static-plus-caller structured JSON body before its one approved request. | Existing direct-write/scalar/form/SCIM regressions plus `go test -timeout 20m ./internal/connectors/engine` | green |
| INT-02 | Terminal status-only 4xx/5xx versus binary/text GET errors | `TestOperationStatusCheckPreservesTerminalNon2xxMetadataAndDeclaredHeaders` failed: ordinary requester returned `http 404` as an error before the final declared header/result could be retained. | `OperationStatusCheck` now uses the closed `DoStatusCheck` response path while retaining declaration-owned request/response headers; focused engine, connsdk, commandrunner, and CLI regressions pass. | Loader, retry, bounded download, no-output-file, and deterministic no-header CLI status-output regressions | green |
| INT-03 | Source-imported declaration reaches installed generated surface | Pending combined #4306/#4307 head | Pending | Source-lock hash/count, malformed/unknown/oversized source regressions | ready after qualified merge inputs |
| INT-04 | Multiple reverse-ETL actions through persisted App/installed CLI | `TestFoundationRollupPreservesMultiActionReverseETLComposition` failed to compile before the exact reverse head was composed: `undefined: TestPersistedConnectionSelectsDeclarativeTypedDestinationAction`. This names the missing production-shaped persisted App plan/preview/approval/apply/acknowledgement behavior rather than substituting an adapter call. | Passed from composite `808896a28873c5f0479fa10e2f798da56f885b5e`; the persisted App route exercises independently selectable declaration-bound destinations through plan, approval, apply, durable acknowledgement, and complete provider-result retention. | Full focused `internal/app` package run in progress; typed destination and GitHub regressions remain the follow-on guard. | green |
| INT-05 | Direct-write receipt preservation must not leak through diagnostics | Composed `internal/connectors/engine` first failed: multipart/non-idempotent terminal results retained provider bytes but printable errors echoed them; malformed JSON lost its typed provider cause; GraphQL incorrectly treated explicit text/binary as a JSON envelope. | `go test -timeout 20m ./internal/connectors/connsdk ./internal/connectors/engine` passed after the resolution: typed result/cause preserve complete receipts, diagnostics stay masked, declaration-owned response headers overlay the receipt, omitted/JSON GraphQL parses, and explicit text/binary stays byte-exact. | The focused App/CLI and generated surface checks run from the same composed SHA. | green |
| INT-06 | Cross-bound and raw-authority rejection happens before I/O | Pending exact combined head | Pending | Existing no-generic-HTTP/action authority regressions | pending focused commandrunner/CLI checks |

No `Green` entry may be added solely from a component PR report: the test must pass from the final composed SHA.

## Release-candidate r1 closure ledger

| ID | Requirement | Red | Green / required evidence | Status |
| --- | --- | --- | --- | --- |
| RC-01 | Core + reverse conflict preservation | The three known textual seams must not silently drop either invariant family. A focused failure or direct diff witness is required before any correction. | Full App, Postgres race, sync transport, commandrunner, and connector cohorts pass from I; review records both fence and action-owned receipt/readback invariants. | green |
| RC-02 | Core/reverse + public conflict preservation | The five public seams must not mask ordinary values or reopen operation authority. A focused failure or direct diff witness is required before any correction. | Full App/CLI plus engine, connector, commandrunner, SQS, and output cohorts pass from I; review records exact masking and raw-byte/key authority invariants. | green |
| FND-B09 / RC-03 | Exact schema-3 evidence closure | The inherited schema-2 manifest failed strict decode with `component_inputs` object-to-typed-list refusal. | This dedicated evidence-only commit names I; the strict graph/artifact/ledger/review gate is executed immediately after it is committed cleanly. | ready for post-commit gate |
| FND-W01 / RC-04 | Publication ordering warning disposition | Inspected source-import writes and ran the hermetic `TestSourceImport.*` cohort; no deterministic externally observable release failure resulted. | Deferred/non-reproducing in the review record; no speculative transactional publication change. | deferred |
| RC-05 | Align certification-sweep expectation and embedded lock identity with the composed declarations | Exact-SHA release gate: `TestCertificationSweepDropsProviderRefusalObservationWhenTheCommandIsPartial` expected the command to be `partial` while `connectorgen surface-sync --check` directly proved the lock-derived declaration is `implemented`; `TestProductionEmbedPreservesGithubSourceLockBytes` failed because its expected lock SHA was the public predecessor (`59e9…`), not the composed core lock (`281b…`). | Renamed the stale test to assert the implemented command retains its observed 422 refusal, restored declaration/sweep/skill outputs with the authoritative `surface-sync`, and updated only the test’s immutable source-lock digest. The two deterministic tests and generated checks pass. | green |
| RC-06 | Recurly binary-download path parameters remain declaration-owned and executable | At `7c3d856445f68695e2886c4dfca0a2662dcedc7c`, `go test -v -count=1 ./internal/connectors/defs/recurly` failed `TestBinaryDownloadCommandsExecute`: `invoice pdf get` rejected `--invoice-id` and `export files get` rejected `--export-date`. Direct history shows `d3bf5da0` removed the five path flags while the declared paths and examples remained. | Each affected operation now declares its exact required `path` parameter in `operations.json`; `connectorgen surface-sync` generated five required flags. The unchanged Recurly binary execution test, `connectorgen surface-sync --check`, Recurly validation, full `cmd/connectorgen` test suite, regenerated docs/skills, and initialized-project binary invocations all pass. The two binary commands now parse their flags and stop only at `missing --credential`. | Preserve the direct execution test unchanged; no live Recurly provider claim is made. | green |
| RC-07 | GitHub source-locked generated direct reads remain executable | At exact `596c90c112067d5b532fd0d27311dd528ba5b6d6`, `TestDirectReadCandidatesForGitHub` failed with 43 candidates versus 120 (23 manual plus 97 generated); 77 declared generated candidate commands were `partial`. The original fresh-binary fixture first failed on `enterprise-team-organizations get-assignments`: unresolved `{enterprise-team}` template, before its fixture server received an output. | Narrow cohort-only source projection restores 97 generated candidates. The split `TestPMBinaryExecutesGitHubGeneratedDirectReadCandidatesAgainstFixture` now directly executes all 97 declaration-generated commands with local admission and proves each request, direct-read envelope, and declaration-owned output assertion (29.56s); the three-stage certification shared-admission proof is recorded in RC-11. `source-import --check`, `surface-sync --check`, and `certification-candidates --check` pass. | Superseded only as to its former statement that the remaining GitHub surface loss was an external follow-up: captain's exact `origin/main`/candidate comparison proved the 370 endpoint loss is the same Foundation projection regression. No count relaxation, provider request, or live-certification claim. | green; broadened by RC-08 and RC-15 |
| RC-08 | GitHub API-surface read coverage must remain executable from declaration-owned routes | Red at `a750e2c4e6db21510f2710843aafc625fbe09e9f`: `TestSurfaceInventoryForGitHubAccountsForAllReviewedEndpoints` reports `Covered = 854, want 1224`; exact artifact comparison shows 1,225 endpoints at both refs but candidate has 371 blocked vs released `v0.2.1`/`origin/main` one retired POST duplicate. The 370 added blocked GET endpoints exactly match the deficit and cite absent field-complete declaration-owned executable routes. The fresh-binary fixture then found an additional direct witness in the restored set: `repo read-file` was promoted while its declared `path.path` flag remained optional, so execution failed before provider I/O with `missing path variable "path"`. | `sourceProjectionRestoreReachableRead*` now restores every field-complete source-bound read route without certification-cohort authority; API coverage uses operation-owned plural aliases only where declared. The path-flag projection marks an existing required source path flag required for every restored route, including non-candidates. `source-import` regeneration produces endpoints=1,225, blocked=1, candidates=120; focused projection tests and `TestSurfaceInventoryForGitHubAccountsForAllReviewedEndpoints` pass. `TestPMBinaryExecutesGitHubReleasedReadSurfaceAgainstFixture` builds a fresh `pm` and executes all 633 unique `v0.2.1` API-surface targets (a strict superset of the 370 restored endpoints) against a local fixture: 633 emitted resolved requests, declared method checks, and JSON/text/status-only/repository-content/binary output assertions passed with each coordinator alias poisoned (182.73s and 188.48s). | Exact final tag-vs-head identity comparison remains a post-commit gate. This is hermetic local behavioral proof, not a live-provider certification claim; no expected count or endpoint is weakened/excluded. | green |
| RC-09 | Remove exact PR-introduced CodeQL high and warnings without suppressions | CodeQL at `25b2f8447…` reports high `go/allocation-size-overflow` for `len(values)*4` in `writeRedactionLiterals`, plus dead assignments of reconciliation output in parked resume and terminal HTTP error output. These are direct deterministic static failures, not historical reviewer suggestions. | `writeRedactionLiterals` now avoids capacity preallocation from input arithmetic; parked resume still invokes durable reconciliation while discarding its non-actionable returned error; final HTTP status still returns its typed status error directly. Focused redaction, post-commit claim reconciliation, and terminal-retry receipt tests pass; full `cmd/connectorgen` passes cold in 143.231s. `TestPMBinaryExecutesGitHubReleasedReadSurfaceAgainstFixture` also passes in 193.93s with 633 emitted request/output assertions. Fresh CodeQL remains the final external confirmation. | No suppression, baseline, annotation, JavaScript, or `ledger.go` change is in scope. GSD inline/manual fallback: no compatible isolated Pi role runtime is available; skills loaded are golang-how-to, golang-security, golang-safety, golang-error-handling, golang-testing, and golang-lint. | green pending fresh CodeQL |
| RC-10 | Settle disputed GitHub partial verdicts by real CLI behavior | CI `Verify` on E2 `8ed5ab93…` failed two hard-coded partial expectation guards covering 15 implemented commands. Captain artifact comparison proves the 11 list declarations are byte-identical on `v0.2.1`, `origin/main`, and E2, including valid API-surface bindings; this makes the failure a test-vs-behavior question, not authority to downgrade a command. An external report also names line 110 of the generated-candidate fixture test with `invalid character r looking for beginning of value`. | `TestPMBinaryExecutesGitHubDisputedPartialVerdictsAgainstFixture` builds a fresh `pm`, initializes a project/fixture credential, and executes all 15 commands (the eleven lists plus `issue status`, `pr checks`, `ruleset check`, and `search prs`). Every invocation made exactly one authenticated GET, whose resolved path matches the declared API-surface template; every emitted direct-read envelope and declared JSON result assertion passed (39.16s). The two verdict tests then pass (2.841s). The candidate test's local rerun did not reproduce the external decode fault, but its direct code witness showed JSON was decoded from `CombinedOutput`; it now decodes stdout only and reports independently redacted stderr. Its fresh-binary 97-stage direct-read proof remains green. Full `internal/cli` passes (1061.223s), full `commandrunner` passes (21.816s), full `cmd/connectorgen` passes cold (143.786s), and `go vet ./...`, build, and surface sync pass. | Fixture proof is local behavioral evidence, not real-provider certification. Preserve exact source artifacts and parity; no command failed provider execution, so none is downgraded or escalated. | green |
| RC-11 | Prove GitHub certification's declared shared-admission path without turning execution coverage into a rate-limit soak | Verify `32589551092` and `32592490944` failed the fresh child at `secret-scanning scan-history view` with `shared_coordinator_unavailable` before fixture provider I/O. The final declaration witness is `defs/github/rate_limits.json`: all seven `certification-*` policies require shared coordination, identically on `origin/main`; `applyCertificationSourceDefaults` forcibly selects `tier=certification`. The subsequent exact Verify `32597572602` showed that applying those production budgets to the 633-stage fixture makes its real `Wait(...)` calls exceed the fixed 20-minute test deadline. | `TestPMBinaryProvesGitHubSharedAdmissionForGeneratedDirectReadFixture` passed in 17.27s: its fresh-binary certification cohort of three generated stages owns a disposable loopback Redis-compatible coordinator, asserts all three real direct-read output assertions pass, and verifies nonzero shared-admission state. It exercises the required production coordination path without changing the policy or masking its pacing. | The embedded certification contract has no fixture budget override: `rate_limit_runtime.go` uses only declaration-loaded policy budgets, while `certification.go` overwrites a caller's tier. This is local behavioral coordination evidence, not Dragonfly or live-provider certification. | green |
| RC-12 | Keep the exact Foundation lint gate green without changing projection behavior | `make lint` at I5 failed CI-pinned `golangci-lint v2.12.2` staticcheck S1008 in the projection source: a boolean predicate result was expanded into a redundant `if` plus `return false`. `git blame` ties that code to PR projection commit `f9f279cbd`; I5 did not modify the source. | The function now returns the exact existing predicate directly. `make lint` reports zero issues; `surface-sync --check` reports 552 connectors and zero corrections; `certification-candidates --connector github --check` reports current; full `go test -timeout 20m -count=1 ./cmd/connectorgen` passes in 146.778s; and the real fresh-binary 97-stage fixture passes in 70.72s with the parent primary coordinator alias poisoned. | No source projection, declaration, generated artifact, candidate set, or connector runtime behavior changed. | green |
| RC-13 | Keep generated GitHub manual parity exact | At I6, exact `make docs-check-no-build` failed: `connector github manual is stale; run pm docs generate`. The current tree was clean before the validation, so this is a deterministic generated-output witness. | The owning `./pm docs generate --dir docs/cli --connectors-dir docs/connectors` changed only `docs/connectors/github/{MANUAL,SKILL}.md`; a second run produced no further changes, `make docs-check-no-build` passed, and `./pm help github` plus `./pm github --help` showed the generated manual and namespace help. | No manually authored manual text, declaration, or runtime behavior changed. | green |
| RC-14 | Keep the 633-command GitHub released-read fixture isolated from ambient shared coordination | Verify `32597572602` at E5 timed out `TestPMBinaryExecutesGitHubReleasedReadSurfaceAgainstFixture` after 20m. The exact log records children inheriting `POLYMETRICS_DRAGONFLY_ADDR=127.0.0.1:2`, correctly refusing shared admission before provider I/O; replacing that inherited address with a real coordinator then correctly paced the large fixture against production budgets. | Every fresh command child removes both coordinator aliases and uses its own process-local declared normal-tier admission, while preserving all 633 real request, resolved-path, method, and declaration-owned output assertions. Exact tests with `POLYMETRICS_DRAGONFLY_ADDR=127.0.0.1:1` and `PM_DRAGONFLY_ADDR=127.0.0.1:1` each passed (182.73s and 188.48s). The focused shared test separately covers the policy that requires shared admission. | No production rate-limit declaration, generated surface, candidate count, endpoint, or provider behavior changes; this is local fixture isolation only. | green |
| RC-15 | Execute all 97 restored generated candidates without making a count an execution claim | The former 97-stage certification fixture coupled candidate execution to `tier=certification`, so its shared declaration budgets made it unsuitable as the exhaustive fast command proof. Candidate count 97 alone cannot establish provider dispatch or output conformance. | `TestPMBinaryExecutesGitHubGeneratedDirectReadCandidatesAgainstFixture` directly invokes every one of the 97 declaration-generated candidate arg vectors against an authenticated local fixture. For every candidate it asserts one resolved GET against the command's exact declared API-surface template, a successful direct-read envelope, fixture request/output agreement, and the candidate's own declared JSON output assertion. | There is no fixture-scoped certification budget selector in the production runtime; this uses the captain-authorized local-admission fallback only for the exhaustive fixture sweep. It is local behavioral evidence, not a live-provider certification claim. | moved to dedicated CI gate RC-16 |
| RC-16 | Retain exhaustive 97-command proof without consuming the shared `internal/cli` deadline | Fresh Verify at I8 timed out its fixed 20-minute `make verify` package gate while `TestPMBinaryExecutesGitHubReleasedReadSurfaceAgainstFixture` was active; captain confirms CI runners are materially slower and 18 other checks passed. The newly added 97-command fresh-binary sweep is individually fast locally but makes no sense inside the package-level wall-clock budget. Raising that shared timeout, dropping stages, or weakening the declared shared policy is forbidden. | `TestPMBinaryExecutesGitHubGeneratedDirectReadCandidatesAgainstFixture` is build-tagged `github_fixture_sweep` and runs only in a new `github-generated-direct-read-fixture` CI job with `timeout-minutes: 45` and test timeout `35m`. The default package retains the small three-stage shared-admission proof and its fixed deadline; local proof passes: shared-admission 16.70s, tagged exhaustive 97-stage proof 46.92s, and the untagged package reports no such test to run. The dedicated CI job executes every one of the 97 candidates with the same request/envelope/output assertions. | Option A selected by captain direction. No candidate is omitted, no representative cohort claim is made, the default package timeout remains 20m, and `rate_limits.json` is untouched. | local green; CI pending |
| RC-17 | Keep every hermetic fresh-binary GitHub fixture proof outside the shared `internal/cli` deadline | After RC-16, `github-generated-direct-read-fixture` passed all 97 stages, but Verify still timed out at 20m while `TestPMBinaryExecutesGitHubReleasedReadSurfaceAgainstFixture` ran. The released-surface fixture starts a fresh `pm` per command, so its 633 complete route/output assertions have the same CI cost profile. Raising the shared timeout, shrinking coverage, omitting aliases, or changing `rate_limits.json` is forbidden. | The tagged dedicated job runs the complete original bodies of six hermetic tests: 3-stage shared admission (5.58s); all 97 generated candidates (47.09s); all 15 disputed aliases (28.45s); all 633 released read-surface commands (198.43s); the GitHub issue-label transport lifecycle (29.25s); and the faithful GitHub warehouse-flow round trip (30.69s). The combined tagged command passed in 341.779s. Every test is absent from the normal package test list; `go test -timeout 20m -count=1 ./internal/cli` passed in 765.837s, while the dedicated job retains its 45-minute limit and the Go test retains its 35-minute limit. The small shared admission proof brings up its own Redis-compatible coordinator in that job. | No production/runtime/declaration/generated-surface change; wrappers only relocate existing test execution. The credential-gated live GitHub flow proof stays opt-in and is neither run nor claimed. GSD inline/manual fallback: isolated Pi worker roles are unavailable under the single-worker contract; the resolved discuss/plan/execute/verify/review prompts, Go skills, and TDD evidence are recorded here. | local green; CI pending |
| RC-18 | Measure the complete ordinary `internal/cli` suite on actual CI hardware before changing its deadline | Hosted Verify `32605370643` failed at the fixed 20-minute test alarm after the six fresh-binary GitHub fixture proofs had been moved and passed independently; the panic names `TestSkillsGenerateMatchesTrackedSkills` after 2m08s, proving expiration is package-wide rather than a remaining GitHub fixture. `buildTransportPM` is directly inspected as a package-wide `sync.Once` cache, so the suite does not rebuild the `pm` binary once per test. | A 45-minute, no-tag `internal-cli-wall-clock-measurement` job runs the exact ordinary package on an Ubuntu GitHub runner with a 35-minute Go alarm and publishes `internal_cli_wall_seconds`. Its result will select either measured-headroom timeout or further isolation; neither test count nor assertion changes. | This changes CI measurement only, not connector/runtime/declaration/generated behavior. | red recorded; CI measurement pending |
