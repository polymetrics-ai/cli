# Issue 4166 TDD Ledger

| Slice | Red | Green | Refactor / evidence |
| --- | --- | --- | --- |
| Lifecycle / delivery contract | The base branch lacks the newly referenced task-header template, so the template was read from the detached launch commit `b3df1c1c7`; no durable issue-4166 header existed. | `4166-CONTEXT.md` records the exact base, landing path, working branch, acceptance assertions, and live/fake reasons before production edits. | `scripts/gsd doctor`, all lifecycle `sources` commands, and `go run ./cmd/agentcontractgen check` passed. GSD prompts are executed inline because role spawning is forbidden. |
| Gap 1 write-action coverage | `go test -timeout 20m ./internal/connectors/certify -run 'TestFullWriteSweepFailsForDeliberatelyBroken' -count=1` initially failed to compile because the per-run probe seam did not exist. The final RED corrupts the selected embedded action's actual `record_schema`; the real engine compiler returns `compile record_schema` for both `create_label` and formerly blocked `update_issue`, and the tests fail if certification ignores that result. | `TestFullWriteSweepFailsForDeliberatelyBrokenDeclaredAction`, `TestFullWriteSweepFailsForDeliberatelyBrokenPreviouslyBlockedAction`, and `TestFullWriteSweepExercisesAll607DeclaredGitHubWriteActions` pass. The intact sweep reaches the real engine dry-run preparation for all 607 actions with zero records/provider sends; each corrupted action produces a named failed stage, `Report.Passed=false`, and exit 2. | The seam is scoped to one `runContext`; production `certificationWriteActionProbe` loads the definition once and prepares every action with fixture-scoped, content-free approval identity. Unsafe live mutations retain an explicit non-live reason instead of being counted as an unexercised passing block. |
| Gap 2 declared transport | With a deliberate mutation that ignored `RegisterDeclaredTransports`' missing-factory error, `TestCertificationDeclaredTransportPairFailsWhenRegistrationIsMissing` failed because the report incorrectly passed. The observed real error was `declared source transport executor "issue_label_source" is not registered`. A second negative control removes the actual GitHub bundle's `SyncTransport` descriptor; `TestDeclaredTransportCertificationFailsWhenDeclarationIsMissing` requires the production applicability guard to reject it as absent/incomplete. | `go test -timeout 20m ./internal/app -run 'TestDeclaredTransportCertificationFailsWhenDeclarationIsMissing' -count=1` and `go test -timeout 20m ./internal/connectors/certify -run 'TestCertificationDeclaredTransportPair' -count=1` cover missing declaration, intact execution, missing registration, and the explicit non-applicable skip. The intact control observes 3 provider reads (source pages plus readback), 1 write, 1 record read/loaded, one committed checkpoint, and one durable stage manifest/Parquet file; both inverses fail before provider execution. | Certification now runs `declared_transport_pair` during full GitHub certification through production `app.Open` composition and `RunETL`. The only fake is the bounded GitHub-shaped HTTP boundary. |
| Gap 3 flow round trip | With a deliberate manifest mutation from authorized `comment_issue` to `create_label`, `go test -timeout 20m ./internal/cli -run 'TestFreshBinaryDeclarativeGitHubWarehouseFlowRoundTrip' -count=1` failed at `flow run` with authorization scope changed (`write_action requires re-approval`) before a provider write. Baseline certification had no disposable repository or connector-backed flow action. | The hermetic command passes after restoring `comment_issue`. The live command `GOMAXPROCS=2 go test -p=1 -timeout 20m ./internal/cli -run '^TestLiveFreshBinaryGitHubWarehouseFlowRoundTrip$' -count=1 -v` also passes with a process-local certification credential: a fresh 151,259,538-byte binary (`sha256:0849ee30f2b782684080ac1222837b5c76f19a5c46a5332cc85b36b93bb110c3`) runs a real issues ETL job plus a real reverse-ETL job composed as one flow; flow sync/action each write 1 record, GitHub comments advance to 2, a separate binary query reads 1 Parquet row, and the reopened checkpoint/flow receipt exist. Replay/unapproved/auth refusals add zero comments; auth leaves the checkpoint unchanged and returns the currently observed `internal/internal_error`, whose correction belongs to #4169. | The test creates only `pm-cert-flow-4166-<10 hex>` under `Polymetrics-Cert`, independently reads the comment from GitHub, deletes only that run-owned repository, and requires a post-delete 404. The live evidence reports `zero_provider_residue=true`. Runbook resource-owner labels `polymetrics-cli-cert` and `polymetrics-cli-cert-1` are stale; token 1 works when the 93-character token substring is extracted from its labeled line, and the actual certification organization is `Polymetrics-Cert`. No credential, repository identifier, response body, or rendered rate scope is retained. |

## Red / Green Rule

Each gap needs an intact control and a negative control. The proof is GREEN only when the intact control has an observable execution/state transition and the deliberately broken condition fails at the claimed boundary.
