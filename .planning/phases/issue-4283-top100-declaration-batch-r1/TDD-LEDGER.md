# TDD Ledger: Issue #4283 Batch-1 source-rigidity R2

## Status

No provider-facing production test has been added or executed. Captain instruction `006.msg` replaces the obsolete scope rule with a bounded-cohort policy; the policy correction itself is the first red-green-refactor slice.

## Reserved evidence rows

| Area | Required red evidence | Required green evidence | Current state |
| --- | --- | --- | --- |
| Delivery policy | The three authoritative direct clauses in required-skills routing, task-skill matrix, and issue-agent contract require one target connector and a separate foundation PR. | Those three clauses require bounded cohort ledger + Atlas disposition and preserve source evidence, TDD, review, CI, safety, and human merge gates. | GREEN: correct focused static regression passed with the three policy paths as separate positional arguments (`exit_status=0`); AGENTS.md contains no direct obsolete clause, non-authoritative copies were restored, YAML/JSON parsing and agent-contract checks pass (`exit_status=0`). The earlier space-joined-path invocation is excluded from evidence. No dedicated policy-regression harness exists. |
| F1/F2 | Locked source fixtures for aliases, finite numeric spellings, defaults/presence, duplicate semantic statuses, ranges, `default`, and raw status facts. | Canonical semantic status plus raw fact retention with source-cited disposition. | Not started: authorized shared foundation review pending. |
| P1–P3 | v2/v3 source-contract bridge, cited-only reference, cross-document and zero-operation document fixtures. | Deterministic source retention/mapping while certification bytes/hashes remain non-binding evidence. | **P1 GREEN.** Red: `TestParseSourceImportLockRetainsBatchOneLegacyProviderFacts` rejected Bitbucket `headers`, CircleCI `allowReserved`, Docker Hub `$ref`, GitLab/Jira `example`, Sentry `x-learn-more`, Stripe `encoding`, and Vercel `x-vercel-cli`; singular media examples also failed. Green: exact `source_operation`/`source_contract` object fragments survive the v1/v2 canonical wire while outer source-lock fields and non-object fragments still fail. **P2 mapping-bridge GREEN:** Red: `operation-evidence` rejected `--connector` as an unexpected argument; once selection existed, a mixed `asana`/`missing` selector incorrectly exited 0. Green: `TestOperationEvidenceConnectorFilterIsBounded` emits exactly Asana's 249 source identities, excludes every unselected connector, requires an explicit non-global output path, and rejects duplicate or any missing selection. The GitLab proof (`TestOperationEvidenceGitLabSourceLockBridge`) additionally emits exactly its 1,752 locked identities, rejects foreign rows/duplicate IDs/incomplete citations, and preserves an explicit CLI gap where a source identity has no materialized command. GitLab's closed eligible-lane materialization is recorded separately below; the old v4-only `source-materialize` guard remains a closed refusal path, not the GitLab canonical-evidence import path. P3 remains unimplemented. |
| P2 non-binding evidence resilience | Remove retained-source evidence from valid GitHub v2 and Asana v3 locks, then remove certification-subject and generated-website artifacts. | The same source identities remain in operation evidence with `source.retention-import.v1`, `verification.conformance-certification.v1`, or `source.projection-admission.v1` typed gaps; source-import evidence failure cannot leave a row runtime-enabled. | **GREEN.** Red: `TestOperationEvidenceRetainsV2AndV3RowsWhenSourceImportEvidenceIsUnavailable` failed because the GitHub row had no source-retention gap; `TestOperationEvidenceRetainsRowsWhenCertificationAndWebsiteArtifactsAreUnavailable` failed because missing `current-subject.json` aborted operation evidence. Green: the two tests pass after generic per-row non-binding gap projection. Fresh final-worktree `go test -count=1 -timeout 20m ./cmd/connectorgen` passed in 325.246s; scoped Asana/GitLab acceptance passed in 4.394s. The stale pre-final package run is excluded. |
| GitLab canonical-evidence projection | GitLab’s complete schema-v2 1,752-operation lock retains a closed `source_contract` and all per-operation provider facts/citations but has no connector-owned raw-artifact manifest. | A generic source-lock projection parses the retained contract without a fetcher, checks every identity/citation/method/provider route/mapped route against the lock, and emits the canonical descriptor. Hash/bytes remain non-binding provenance. | **GREEN:** red first attempted the artifact reader, then failed because the retained root had no copied `paths`, then rejected fragments without `operationId`. `TestSourceImportGitLabCanonicalEvidenceProjectsLockedInventoryWithoutFetcher` now proves all 1,752 rows (GET=746, POST=435, PUT=329, DELETE=212, PATCH=27, HEAD=3), the source URL/location trace, original provider path plus mapped connector path, exact existing stream reachability, strict descriptor admission, and zero fetches. `TestSourceImportCanonicalEvidenceRejectsInsufficientContract` rejects a malformed provider fragment without falling back to fetching. `go run ./cmd/connectorgen source-import gitlab --check` is green. **Next RED:** full eligible-lane projection must materialize the source-complete JSON GET and no-body reverse-ETL subset, and leave every other identity in a per-row cited typed outcome. |
| GitLab eligible-lane projection and report | A source-complete mapped route is missed by action coverage; an unsafe provider query spelling becomes a raw/silent direct read; live operation evidence omits the descriptor’s typed alias candidate. | Existing closed JSON GET/direct-read and no-body reverse-ETL lanes materialize against mapped connector routes, refresh derived finite request limits, and retain unsafe provider spelling as a source-contract gap in the command/report surface. | **GREEN, pending scoped commit:** Red: `TestSourceProjectionMaterializesEligibleSourceLockLanesWithMappedRoute`, `TestSourceProjectionGeneratedDirectReadRefusesUnsafeProviderQueryParameter`, and `TestOperationEvidenceGitLabSourceLockBridge` respectively exposed raw `/api/v4` matching, unsafe direct-read admission, and zero alias gaps. Green: their focused suite passes; `source-import gitlab --materialize-eligible-lanes --check` and GitLab connector validation pass without a fetcher. Refreshed operation evidence has 1,752 rows: 733 runtime-enabled (direct-read 582, ETL 4, reverse-ETL 147) and 1,019 blocked rows, all with named foundations; evidence-defect rows 0. The 15 request-phase provider-key alias gaps remain `runtime-provider-parameter-alias-investigating-r1`; no shared runtime or CLI alias implementation is claimed. |
| P3 source-projection document isolation | A v3 lock with one unavailable source document and one closed available operation returns only the unavailable-document finding before checking the available operation. | The available descriptor row validates against its own source-lock identity despite the unavailable sibling; the unavailable document stays source-import/operation-evidence-only. | **RED pending:** add one mixed-document fixture with a valid available operation and assert `checkSourceProjection` accepts it; preserve refusal for any mismatch in the available operation. |
| P4–P6 | Existing direct/binary/form/multipart/no-body/fixed-GraphQL lanes and one source denominator fixtures. | Cited lane selection plus generated command/help/docs reachability for implemented/blocked/unsupported rows. | Not started. |
| R1–R4 | Parameter/cookie serialization, media encoders, recursive/union schemas, and response-variant fixtures from the locked cohort. | Exact bounded encoding/selection and refusal-before-I/O for unrepresented contracts. | Not started. |
| Cohort reconciliation | Per-connector source identity matrix and built-binary credential-boundary tests. | Each cited identity is implemented, named-foundation blocked, or provider-evidenced unsupported. | Discovery denominator committed in `data/connector-canon/batch1-source-rigidity-r2-cohort-ledger.json`: ten source locks, SHA-256-pinned, 4,341 REST identities, zero projected cells. Red source-operation-to-lane reconciliation test is next; the ledger makes no usability claim. |

## GitLab endpoint-ledger propagation — Atlas-first red/green

- Classification before repair: `source.projection-admission.v1` is an available foundation with the owner `cmd/connectorgen/surfacesync.go#syncRuntimeOperationEndpointLedger`; `runtime.direct-execution.v1` already owns the closed `rest_read` executor. The failure was a constrained extension of the projection selector, not a planned/new runtime capability. `internal/connectors/engine/operation_endpoint_ledger.go#OperationDirectReadEndpointLedgerEntries` is the runtime consumer.
- Red: `TestGitLabGeneratedDirectReadReachesCredentialBoundary` invoked the generated `GET /admin/active_context/connections` command in an initialized project with no credential. It was blocked before credential acquisition because `operation_endpoint_ledger.json` had no `GET /admin/active_context/connections` `rest_read` row at `max_bytes=67108864`.
- Green: `surface-sync --connector gitlab` projects only GitLab's 582 direct-read endpoint rows; `TestSurfaceSyncConnectorNamesSelectsOnlyRequestedDefinition` and `TestSyncRuntimeOperationEndpointLedgerConnectorPreservesUnselectedEntries` prove selected-only generation and preservation of unselected ledger rows. `TestGitLabSourceLockedCommandsPassRuntimePreflight` exercises all 582 direct reads, four ETL commands, and 147 reverse-ETL actions through the real preflight path. The credential-boundary test and a rebuilt `pm` both reach exactly `error: missing --credential`, with zero provider requests. No engine executor, provider alias, request encoding, approval, credential, or provider-specific runtime branch was added.

No claimed Red or Green result is backfilled from a different branch, historical test, certification label, or provider-live execution.
