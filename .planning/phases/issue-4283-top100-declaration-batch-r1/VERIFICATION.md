# Verification: Issue #4283 Batch-1 source-rigidity R2

## Completed planning checks

- [x] Fetched `origin/fm/cli-top100-declaration-batch-r1` and checked out SHA `6ec34964fda7a78a1736a5cd8933a46418346261` detached in the assigned task worktree.
- [x] Verified the required ancestor with `git merge-base --is-ancestor`.
- [x] Verified PR #4294 API base/head/SHA: `main`, `fm/cli-top100-declaration-batch-r1`, `6ec34964fda7a78a1736a5cd8933a46418346261`.
- [x] Ran `scripts/gsd doctor`, resolved all required lifecycle command sources, generated `discuss-phase` and `plan-phase --tdd` prompts, and ran `go run ./cmd/agentcontractgen check`.
- [x] Read the required routing, GSD adapter, issue contract, CLI parity, connector lane, and source-rigidity audit materials.
- [x] Processed Firstmate inbox messages `001.msg` and `002.msg` as historical scope context.
- [x] Captured RED policy evidence: the three authoritative direct clauses in required-skills routing, task-skill matrix, and issue-agent contract had the obsolete mandatory split.
- [x] Captured GREEN policy evidence: the correct `focused-policy-regression` passes with the three authoritative paths as separate positional arguments (`exit_status=0`); `AGENTS.md` has no direct obsolete clause; all 17 non-authoritative copies were restored exactly; YAML/JSON parsing and `go run ./cmd/agentcontractgen check` pass (`exit_status=0`). The earlier space-joined-path invocation is excluded from evidence. The repository has no dedicated delivery-policy regression harness.
- [x] Processed Firstmate inbox message `006.msg`: commit `924197b49adc516ec7bdb3b6d928c9701cfab946` was normal-fast-forward pushed and remote verified, then `006.msg` was moved to `handled/`.
- [x] Processed Firstmate inbox messages `007.msg` through `010.msg`: narrowed policy scope to the three named direct clauses, completed focused validation, and moved each message to `handled/`. Message `010.msg` additionally prohibits installing ad hoc tooling or scripts.
- [x] Committed immutable Batch-1 discovery denominator: `data/connector-canon/batch1-source-rigidity-r2-cohort-ledger.json` pins all ten source locks, schema forms, identity locations, source-lock SHA-256 values, and the 4,341-identity total. It expressly has zero projected cells and makes no reachability claim.
- [x] P1 red/green: `go test -timeout 20m ./cmd/connectorgen -run 'TestParseSourceImportLock(RetainsBatchOneLegacyProviderFacts|RetainsSingularMediaExamples)$' -count=1` first failed on retained provider fields, then the focused parser suite passed after source-only object-fragment retention. The tests cover the named representative Batch-1 locks (Asana, Bitbucket, GitLab, Jira, Stripe, and Vercel among the ten), happy provider facts, malformed fragment rejection, explicit `null` media example preservation, and strict outer-lock rejection.
- [x] P1 scoped green checks: `go test -timeout 20m ./cmd/connectorgen` passed (`195.891s`); `go vet ./cmd/connectorgen`, `make docs-check`, Ruby JSON parsing of the Atlas and cohort ledger, and `git diff --check` passed. The Atlas was updated as the `source.retention-import.v1` constrained extension, including owner symbols and proof tests.
- [x] P2 red remains explicit: `go run ./cmd/connectorgen source-materialize gitlab --check` now reaches the documented v4-only bridge guard and exits 1. It makes no provider request and does not alter the P1 source-evidence result.
- [x] P2 mapping bridge red/green: `operation-evidence` first rejected `--connector`; its first selector implementation then let a mixed valid/missing connector set exit successfully. `TestOperationEvidenceConnectorFilterIsBounded` now verifies exact Asana-only identity coverage (249 rows), no unrelated connector rows, explicit non-global output requirement, and duplicate/missing-selector rejection. The scoped ten-connector run emits 4,341 rows; it is a mapping evidence matrix only, not a materialization or runtime-usability claim.
- [x] P2 GitLab bridge proof: `TestOperationEvidenceGitLabSourceLockBridge` reads only GitLab's locked source evidence and asserts its 1,752 unique source identities have complete lock citations, no foreign rows, and an explicit command gap whenever no generated command exists. It makes no provider request. This does not remove the v4 materialization or retained-artifact blockers.
- [x] P2 generic non-binding evidence resilience: Atlas reuse of `source.retention-import.v1`, `source.projection-admission.v1`, and `verification.conformance-certification.v1` keeps mapping independent of retained-source, certification-subject, and generated-website evidence availability. Red: the v2/v3 retained-source test found no source-retention gap and the certification/website test aborted at the missing certification artifact. Green: valid GitHub v2 and Asana v3 identities remain in operation evidence with typed named foundation gaps; source-import evidence disables only operation-evidence runtime classification, while certification/website evidence remains overlay-only. `TestOperationEvidenceGitLabSourceLockBridge` proves all 1,752 GitLab identities retain this non-executable source-retention state. Fresh final-worktree `go test -count=1 -timeout 20m ./cmd/connectorgen` passed in 325.246s, followed by the scoped Asana/GitLab acceptance suite in 4.394s. No provider request, credential resolution, approval path, or runtime request execution occurred.
- [x] Pre-GitLab-slice local checks: `gofmt -d` for changed Go files, `go vet ./cmd/connectorgen`, Foundation Atlas JSON parsing, `make docs-check`, `go run ./cmd/connectorgen declaration-admission --json`, and `git diff --check` passed. The final GitLab-slice checks are recorded below and must be repeated immediately before the exact-SHA review.
- [x] Global generator gate recorded, not masked: the fresh final `go run ./cmd/connectorgen surface-sync --check` stops at `bitbucket source projection: canonical source descriptor is missing` (exit 1). `git cat-file -e HEAD:internal/connectors/defs/bitbucket/sources/bitbucket-operation-descriptor.json` confirms that descriptor is absent from the pre-slice HEAD. GitLab's descriptor is now intentionally present and its scoped `source-import --check` plus declared-contract validation pass; this GitLab-only slice neither causes nor masks the remaining Bitbucket failure.
- [ ] Captain instruction `022.msg` P3 isolation slice: RED pending. CodeGraph and direct execution established that `checkSourceProjection` returns its first unavailable-document finding before it validates independent available-document descriptor rows. The focused regression will contain one unavailable v3 document and one retained available operation; it must prove that the latter maps while the former stays an explicit source-import/operation-evidence gap. No connector definition or source-lock edit is in scope.
- [x] Captain instruction `023.msg` starting-point comparison: current `56cd817d616a423a3f4b84fc9ce2fd0216bf5929` and parent `013373a68a940bff7b74b08375fb2a129fd235dd` have byte-identical source-projection code/tests, Asana generator/conformance code and definitions, and tracked Asana skill. Therefore the reproduced `TestSkillsGenerateMatchesTrackedSkills`, Asana `create_batch_request` conformance failure, and nine-missing-descriptor source-projection result predate the current evidence-resilience commit. `origin/main` is `813f457a925f7ee3fe3bea101a43e445992c8552`: it has neither GitLab's source lock nor an equivalent GitLab source-projection input, while the parent branch adds GitLab's schema-v2 1,752-operation lock and the source-projection implementation. The failures are recorded separately as existing branch starting points, not a green result or a reason to mask them.
- [x] Captain instructions `027.msg`–`032.msg`, GitLab current slice: Atlas-first review recorded `runtime.provider-parameter-alias.v1` as `investigating`; no shared engine/CLI alias was added. The red report test saw zero retained candidate rows; the green focused suite (`TestOperationEvidenceGitLabSourceLockBridge`, mapped-route eligible materialization, unsafe-query refusal/pruning, and canonical no-fetch import) passes. Fresh no-fetch commands pass: `source-import gitlab --materialize-eligible-lanes`, the matching `--check`, and `validate internal/connectors/defs --connector gitlab --require-operational-contract declared`. Fresh connector-only operation evidence is the count authority: 1,752 source rows; 733 enabled (direct-read 582, ETL 4, reverse-ETL 147); 1,019 blocked rows with named foundations; 0 blocked rows without one; and 0 source/certification/website evidence-defect rows. Foundation entry counts overlap by operation: malformed path 2, request encoding 47, request schema 1,243, closed operation execution 143, provider-key alias candidate 15, and non-executable mutation 856. The earlier provisional shape arithmetic is superseded because it counted overlapping gap entries and all 16 punctuated-key routes; the existing `getApiV4Issues` `issues list` ETL lane is source-backed with no exposed unsafe optional key, leaving 15 new direct-read alias candidates. The full per-row machine artifact is reproducible by `operation-evidence --connector gitlab --output <temp>/gitlab-operation-evidence.json`; it retains each citation, mapping, lane, CLI reachability, and typed gap.
- [x] Captain instruction `033.msg`, fresh full-package gate: the first fresh `go test -count=1 -timeout 20m ./cmd/connectorgen` failed because operation evidence attempted to reconcile a GitHub descriptor row absent from that source lock; the descriptor overlay was too broad. The repaired intersection then failed the fixed-100 gate on Asana because descriptor gaps changed a fixed source row. The root cause was the same: canonical-descriptor gaps were being attached to ordinary v2/v3 locks. The repair restricts descriptor-runtime-gap overlay to locks that explicitly set `rest.canonical_evidence: true`; GitLab is that source-complete lock. The required fresh rerun passed: `go test -count=1 -timeout 20m ./cmd/connectorgen` (`288.041s`).
- [x] Captain instruction `034.msg`, documentation scope: `pm docs connector generate --connector gitlab` refreshed only GitLab's manual/skill. A temporary full-document generation showed that the required catalog change is GitLab-only (read/write capability and 147 source-backed write actions); the retained generated files are GitLab's manual/skill plus `docs/connectors/catalog/all-connectors.{json,md}`. No non-GitLab connector manual, skill, icon, or catalog entry is retained.
- [x] Captain instructions `035.msg`–`036.msg`, generated-skill scope: the only retained generated skill is `docs/skills/pm-gitlab/SKILL.md`. The temporary generator's newly-created `docs/skills/pm-asana/SKILL.md` delta was restored exactly to `HEAD`; `git diff --exit-code -- docs/skills/pm-asana/SKILL.md` passes. A temporary generated-skill directory confirms GitLab matches its regenerated skill while Asana remains the pre-existing tracked-skill mismatch, so no Asana definition/source artifact was changed or committed in this slice.
- [x] Captain instructions `037.msg`–`038.msg`, Atlas-first endpoint-ledger classification: available `source.projection-admission.v1` owns `cmd/connectorgen/surfacesync.go#syncRuntimeOperationEndpointLedger`; available `runtime.direct-execution.v1` owns the closed `rest_read` consumer `internal/connectors/engine/operation_endpoint_ledger.go#OperationDirectReadEndpointLedgerEntries`. The generated GitLab direct read was red because the compact runtime ledger omitted `GET /admin/active_context/connections` at `max_bytes=67108864`, not because a runtime executor was missing. The constrained projection extension adds selected-only `surface-sync --connector gitlab` ledger propagation and preserves unselected rows; no engine executor, request encoder, alias, credential, approval, provider-specific branch, or provider I/O was added. New red/green proofs are `TestGitLabGeneratedDirectReadReachesCredentialBoundary`, `TestGitLabSourceLockedCommandsPassRuntimePreflight`, `TestSurfaceSyncConnectorNamesSelectsOnlyRequestedDefinition`, and `TestSyncRuntimeOperationEndpointLedgerConnectorPreservesUnselectedEntries`; the all-command preflight proof covers 582 direct reads, four ETL commands, and 147 reverse-ETL actions. A rebuilt `pm` reaches exactly `error: missing --credential` for the generated direct read, with zero provider requests.
- [x] Fresh current GitLab checks: focused source-projection/endpoint-ledger tests passed in 5.508s; focused GitLab CLI command-surface and credential-boundary tests passed in 4.034s; `source-import gitlab --materialize-eligible-lanes`, matching `--check`, selected `surface-sync --connector gitlab --check`, and GitLab declared-contract validation pass. The refreshed GitLab operation-evidence artifact remains the source-count authority: 1,752 rows; 733 runtime-enabled (582 direct-read, 4 ETL, 147 reverse-ETL); 1,019 blocked with named foundation; 0 blocked without foundation; and 0 evidence-defect rows.
- [ ] Captain instruction `039.msg`, normal full-package baseline blocker: two fresh ordinary-parallel `go test -count=1 -timeout 20m ./cmd/connectorgen` runs fail after 259.446s and 243.968s respectively in unchanged `TestSourceMaterialize_EdgeUsesExactRetainedSourceURLs`, with a Go `reflect.DeepEqual` nil-pointer panic at `cmd/connectorgen/sourcematerialize_test.go:67`. The test and its four-worker v3 importer predate this slice (`git diff HEAD -- cmd/connectorgen/sourcematerialize_test.go` is empty); its isolated run, `-race` run, and `-count=20` run pass. The full suite with `-parallel 1` passes in 287.043s, confirming the parallel baseline/test-fixture interaction without masking it. Per `039.msg`, this GitLab slice does not modify that unrelated fixture or claim the ordinary parallel suite green.
- [ ] Scoped generated-transcript baseline blocker: the selected GitLab root-help transcript update passes while Asana is temporarily held at its committed tracked output; normal `TestGoldenTranscripts` remains red only because current unmodified Asana `cli_surface.json` says 118 direct reads/96 writes while its tracked transcript says 106/94. This is recorded, not repaired, under the same scope boundary. The retained golden diff changes only GitLab's root-help description.
- [x] Final no-I/O generation/docs checks: `source-import gitlab --materialize-eligible-lanes`, its `--check`, `validate internal/connectors/defs --connector gitlab --require-operational-contract declared`, `go vet ./cmd/connectorgen`, `make docs-check`, `go run ./cmd/agentcontractgen check`, Atlas JSON parsing, and `git diff --check` all pass. No provider request, credential lookup, reverse-ETL dispatch, or destructive action was invoked.

## Not run by design

- [ ] Provider-live requests, credentials, reverse-ETL dispatch, or destructive operations — excluded by the task.
- [ ] P3 document-isolation, F1/F2, P4–P6, R1–R4, and the remaining nine connector slices — outside this sequential GitLab delivery; they retain their named foundation/evidence state and are not claimed complete.
- [ ] Exact-SHA Codex review, remote PR-check diagnosis, commit, or push — pending the final scoped verification and review. The ordinary-parallel generator-suite baseline blocker and unrelated Asana generated-artifact drift prevent a zero-blocker/merge-ready claim.

## Current constraint

The captain has changed the relevant repository policy, not reduced a quality gate. This is a bounded GitLab slice of the all-Batch-1 program; it must pass its final gates and exact-SHA review before a normal fast-forward push. It does not claim completion for the remaining nine connectors or unapproved shared foundations.
