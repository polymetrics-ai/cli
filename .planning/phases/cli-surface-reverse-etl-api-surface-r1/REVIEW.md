# Code review — reverse-ETL API-surface derivation r1

## Scope reviewed

- The generic synchronization path in `cmd/connectorgen/surfacesync.go`.
- The all-bundle regression invariant and focused source-bound derivation tests.
- The regenerated GitHub `cli_surface.json` and certification sweep.

## Findings

No actionable finding.

- The new derivation runs before intent-specific operation policy so an endpoint summary is handled for every implemented intent; it contains no connector identifier or allowlist.
- It binds an endpoint only when the parsed summary exactly joins the bundle's already-declared `api_surface.json` endpoint. This prevents the generator from treating punctuated prose as a provider address.
- Operation-backed commands retain their operation endpoint as the authoritative correction source.
- The invariant covers every definition bundle and rejects an implemented command with an empty `api_surface` when its endpoint summary resolves to a canonical endpoint; friendly aliases remain outside that predicate.
- Generated artifacts were regenerated by their owning command and checked for a stable second pass.

## Review route

The PR targets the non-default integration branch. Per the automated-review routing contract, opening a non-draft PR as the trusted author selects `claude_auto`; no manual Claude or Copilot request is warranted before the automatic trigger is observed.
