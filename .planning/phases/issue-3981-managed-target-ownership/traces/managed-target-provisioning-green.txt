# GREEN — TestManagedTargetProvisioningTruthTable

The RED contract test is green after adding the driver-neutral owner/ref/control
values and create-or-assert state machine.

```text
go test -timeout 20m -count=1 ./internal/connectors/database -run '^TestManagedTargetProvisioningTruthTable$'
ok  polymetrics.ai/internal/connectors/database

go test -race -timeout 20m -count=1 ./internal/connectors/database -run '^TestManagedTargetProvisioningTruthTable$'
ok  polymetrics.ai/internal/connectors/database
```

The table asserts state transitions and fake mutation counts for absent
namespace, idempotent repeat, exact owner, missing/foreign/unreadable owner,
occupied namespace/name collision, native replacement, schema drift, orphaned
control record, invalid plan, post-create reassertion, cancellation, and
concurrent callers. No driver, SQL, DDL, CLI, or capability implementation is
needed for the proof.
