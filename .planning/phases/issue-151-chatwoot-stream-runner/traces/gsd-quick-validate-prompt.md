# GSD quick-validate prompt attempt

Command:

```bash
scripts/gsd prompt quick-validate "Issue #151 Chatwoot stream runner"
```

Result:

```text
scripts/gsd: unknown GSD command: quick-validate
```

Manual verification checklist recorded in `VERIFICATION.md`.
