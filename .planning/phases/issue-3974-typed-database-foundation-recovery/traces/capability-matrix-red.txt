Correction round: 1/5
Correction child: #4026 (sub-issue of #3974)
Gate: make connectorgen-certification-matrix

RED command:
  make connectorgen-certification-matrix

RED output:
  go run ./cmd/connectorgen certification-matrix --check
  connectorgen certification-matrix: generated artifact "/Users/karthiksivadas/.treehouse/cli-83d592/44/cli/internal/connectors/certifications/capability-matrix.json" has drift; run `go run ./cmd/connectorgen certification-matrix`
  exit status 1
  make: *** [Makefile:120: connectorgen-certification-matrix] Error 1

Branch-versus-parent attribution (completed before committing any generated output):
  - The matrix is unchanged between origin/feat/3972-postgres-parity and HEAD
    before generation, so parent ancestry does not already carry the expected
    current source locations.
  - Parent artifact locations exactly match parent bundle.go (for example
    expectedOperationBlock begins at line 2080 and rest_read is line 2082).
  - The #3974 replay adds the optional database definition loader and field to
    internal/connectors/engine/bundle.go, moving the same function to line 2104
    and rest_read to 2106. The generated diff contains exactly 15 corresponding
    discovery_source line-location updates and no PostgreSQL capability change.
  - Therefore #3974 owns the derived-artifact synchronization. This is not an
    inherited-parent blocker and does not promote any capability.

Chronology note:
  A generator invocation began immediately before the explicit correction-child
  instruction arrived and left an uncommitted projection. It was frozen and not
  committed while the attribution above was completed. #4026 was then created
  and attached to #3974 before the formal regeneration/green verification.

GREEN regeneration:
  go run ./cmd/connectorgen certification-matrix
  generated certification matrices: connectors=556 capability_complete=0 certified=0

GREEN check:
  make connectorgen-certification-matrix
  certification matrices are current: connectors=556 capability_complete=0 certified=0

Capability-false verification after regeneration:
  - metadata.json: write=false, query=false, cdc=false.
  - database.json: admitted_modes=[] (policy-only; no native execution mode admitted).
  - `./pm connectors inspect postgres --json`: write=false, query=false;
    changefeed_status=planned.
  - `./pm connectors catalog --capability cdc --json`: count=0 and no postgres entry.
