# TDD LEDGER — issue #3716 clean project-local Claude workers

| ID | Enforcement | RED evidence | GREEN evidence | Refactor/verification |
|---|---|---|---|---|
| R1 | Both Claude workers have generated, parseable frontmatter with bounded skill access and no `Agent` | `go test ./internal/agentcontract -run TestClaudeProjectWorkersBlockAmbientAgentDelegation -count=1` failed because both project projections were absent | The same test verifies `name`, `description`, base tools, every scoped `Skill(name)` rule, `permissionMode`, the `Agent`/`Task` denylist, and the absence of `Agent` and bare `Skill`. | Focused review-fix verification remains `go test ./internal/agentcontract ./cmd/agentcontractgen -count=1`. |
| R2 | A generated worker cannot delegate to an ambient user/plugin agent | The executed RED test reported: `pm-delivery-worker can resolve to an ambient same-name agent: missing project projection .claude/agents/pm-delivery-worker.md leaves no project-local tools allowlist that omits Agent` (and the equivalent connector failure). | The generated frontmatter omits `Agent`, denies `Agent` and `Task`, and scopes Skill invocation to the repository-required Go/design names; the drift test proves sync restores that policy. | The prior trusted-home Claude Code v2.1.222 smoke received exactly `AGENT_UNAVAILABLE` when told to invoke ambient agent fixtures. It proves direct `Agent` omission for the selected project worker, not clean-home discovery or arbitrary same-name skill overrides. |
| R3 | Missing required Claude projections are generated and any frontmatter/body drift fails `check` | Not applicable before the renderer existed. | `TestProjectionDriftCheckAndSync` creates both absent required files, injects `Agent` into one, observes `CheckProjections` fail, then observes sync restore the safe file. | `go run ./cmd/agentcontractgen check` passed after sync. |
| R4 | Installed CLI discovers project workers by name while ambient fixtures remain non-delegable | Not applicable before generated project roles existed. | In this trusted checkout, `claude --agent pm-delivery-worker` and `claude --agent pm-connector-worker` each returned their selected role name using Claude Code v2.1.222. | **NOT PERFORMED:** a real clean trusted-home runtime selection with unrelated global definitions. The unauthenticated fixture is not runtime evidence; official precedence plus generated full-file drift enforcement are static evidence only. |
| R5 | Skill access cannot bypass the single-worker boundary | Bare `Skill` would make project, user, plugin, and bundled skills reachable, including skills that can use `context: fork`. | The canonical renderer emits only the approved `Skill(name)` rules and rejects bare `Skill`, missing required names, or a weakened `Agent`/`Task` denylist. | Static policy evidence only; the clean-home runtime smoke remains **NOT PERFORMED**. |
| R6 | The agent-contract implementation remains outside the connector provider-policy lexicon | PR #3728 CI `connector-boundary` failed on `HarnessPolicyFor` in `internal/agentcontract`: `harness` is a connector token and the `...PolicyFor` suffix resembles connector-specific policy. | Renamed the neutral lookup to `ProjectionFor` and updated its focused callers; no contract behavior changes. | `go test ./internal/agentcontract ./cmd/agentcontractgen -count=1`, `go run ./cmd/agentcontractgen check`, `go vet ./internal/agentcontract ./cmd/agentcontractgen`, and `go run ./cmd/connectorgen boundary . --json` passed (`outcome: clean`); a fresh no-mistakes child pipeline follows. |
