# TDD LEDGER — issue #3716 clean project-local Claude workers

| ID | Enforcement | RED evidence | GREEN evidence | Refactor/verification |
|---|---|---|---|---|
| R1 | Both Claude workers have generated, parseable frontmatter with a minimal allowlist and no `Agent` | `go test ./internal/agentcontract -run TestClaudeProjectWorkersBlockAmbientAgentDelegation -count=1` failed because both project projections were absent | The same test now passes against both generated complete files and verifies `name`, `description`, exact `tools`, `permissionMode`, and the absence of `Agent`. | `go test ./internal/agentcontract ./cmd/agentcontractgen -count=1` passed. |
| R2 | A generated worker cannot delegate to an ambient user/plugin agent | The executed RED test reported: `pm-delivery-worker can resolve to an ambient same-name agent: missing project projection .claude/agents/pm-delivery-worker.md leaves no project-local tools allowlist that omits Agent` (and the equivalent connector failure). | The generated frontmatter's explicit allowlist is `Bash, Edit, Glob, Grep, Read, Write`; the test rejects `Agent`, and the drift test proves sync restores that list. | Live Claude Code v2.1.222 smoke exposed an ambient CLI fixture and local plugin fixture, told `pm-delivery-worker` to invoke them through `Agent`, and received exactly `AGENT_UNAVAILABLE` with no tool use. This proves the selected project worker cannot delegate; it does not prove a managed or CLI same-name override cannot replace the selected definition. |
| R3 | Missing required Claude projections are generated and any frontmatter/body drift fails `check` | Not applicable before the renderer existed. | `TestProjectionDriftCheckAndSync` creates both absent required files, injects `Agent` into one, observes `CheckProjections` fail, then observes sync restore the safe file. | `go run ./cmd/agentcontractgen check` passed after sync. |
| R4 | Installed CLI discovers project workers by name while ambient fixtures remain non-delegable | Not applicable before generated project roles existed. | In this trusted checkout, `claude --agent pm-delivery-worker` and `claude --agent pm-connector-worker` each returned their selected role name using Claude Code v2.1.222. | A clean-home same-name user fixture could not complete a model run because the isolated home correctly had no login; no credentials were copied or inspected. Official precedence documentation supplies the user/plugin ordering claim, while the live ambient CLI/plugin smoke supplies the delegation claim. |
