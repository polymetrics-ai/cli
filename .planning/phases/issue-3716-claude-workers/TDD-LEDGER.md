# TDD LEDGER — issue #3716 clean project-local Claude workers

| ID | Enforcement | RED evidence | GREEN evidence | Refactor/verification |
|---|---|---|---|---|
| R1 | Both Claude workers have generated, parseable frontmatter with bounded skill access and no `Agent` | `go test ./internal/agentcontract -run TestClaudeProjectWorkersBlockAmbientAgentDelegation -count=1` failed because both project projections were absent | The same test verifies `name`, `description`, base tools, plugin-qualified `skills` preloads, `permissionMode`, the `Agent`/`Task`/`Skill` denylist, and the absence of runtime `Agent` and `Skill`. | Focused review-fix verification remains `go test ./internal/agentcontract ./cmd/agentcontractgen -count=1`. |
| R2 | A generated worker cannot delegate to an ambient user/plugin agent | The executed RED test reported: `pm-delivery-worker can resolve to an ambient same-name agent: missing project projection .claude/agents/pm-delivery-worker.md leaves no project-local tools allowlist that omits Agent` (and the equivalent connector failure). | The generated frontmatter omits `Agent` and `Skill`, denies `Agent`, `Task`, and `Skill`, and preloads only trusted plugin-qualified Go/frontend names; drift sync restores that policy. | The prior trusted-home Claude Code v2.1.222 smoke received exactly `AGENT_UNAVAILABLE` when told to invoke ambient agent fixtures. It proves direct `Agent` omission for the selected project worker, not clean-home discovery, runtime preload resolution, or managed/CLI overrides. |
| R3 | Missing required Claude projections are generated and any inventory/frontmatter/body drift fails `check` | Not applicable before the renderer existed. | `TestProjectionDriftCheckAndSync` creates both absent required files, injects `Agent` into one, observes `CheckProjections` fail, then observes sync restore the safe file. `TestCheckProjectionsRejectsClaudeAgentInventoryDrift` covers extra and duplicate-name definitions. | The final focused package run covers the actual inventory and drift paths. |
| R4 | Installed CLI discovers project workers by name while ambient fixtures remain non-delegable | Not applicable before generated project roles existed. | In this trusted checkout, `claude --agent pm-delivery-worker` and `claude --agent pm-connector-worker` each returned their selected role name using Claude Code v2.1.222. | **NOT PERFORMED:** a real clean trusted-home runtime selection with unrelated global definitions. The unauthenticated fixture is not runtime evidence; official precedence plus generated full-file drift enforcement are static evidence only. |
| R5 | Skill access cannot bypass the single-worker boundary | Review found that the first scoped-rule design retained unqualified names that personal skills could shadow and use with `context: fork`. | The canonical renderer uses Claude's `skills` preload field only for plugin-qualified sources, omits runtime `Skill`, and rejects missing/extra preloads or a weakened `Agent`/`Task`/`Skill` denylist. | Official docs demonstrate plugin namespaces cannot collide with personal/project names. Generated tests demonstrate configuration only; the clean-home runtime smoke remains **NOT PERFORMED**. |
| R6 | The agent-contract implementation remains outside the connector provider-policy lexicon | PR #3728 CI `connector-boundary` failed on `HarnessPolicyFor` in `internal/agentcontract`: `harness` is a connector token and the `...PolicyFor` suffix resembles connector-specific policy. | Renamed the neutral lookup to `ProjectionFor` and updated its focused callers; no contract behavior changes. | `go test ./internal/agentcontract ./cmd/agentcontractgen -count=1`, `go run ./cmd/agentcontractgen check`, `go vet ./internal/agentcontract ./cmd/agentcontractgen`, and `go run ./cmd/connectorgen boundary . --json` passed (`outcome: clean`); a fresh no-mistakes child pipeline follows. |
| R7 | Claude's recursive project-agent inventory cannot hide an extra or duplicate canonical name | Review demonstrated that checking only registered paths left an extra `.claude/agents/**.md` definition outside drift enforcement. | `checkClaudeAgentInventory` rejects symlinks, malformed or unexpected Markdown definitions, duplicate names, name/path mismatches, and missing canonical files before content comparison. | `TestCheckProjectionsRejectsClaudeAgentInventoryDrift` exercises both an extra unique definition and a nested duplicate canonical name. |
| R8 | Contract validation and GSD command resolution are host-portable | Review traced Windows failures to `filepath.Clean` on slash-based JSON paths and direct execution of the extensionless Node script; the fixture also required `/bin/sh`. | Contract paths use `fs.ValidPath` plus `path.Clean`; the checker resolves Node and supplies `scripts/gsd` as its script; the fixture is JavaScript. | The final focused package run exercises Node-mediated execution on the current host. Windows runtime execution remains not performed. |
| R9 | Every repository-local Claude agent scope is part of canonical inventory | Review traced a concrete bypass through `website/.claude/agents/pm-delivery-worker.md`: the root-only walk never sees it, while Claude selects the closest nested same-name definition when launched below `website`. | The shared inventory walk recognizes `.claude/agents` at every repository depth and rejects nested duplicate or unexpected definitions before projection comparison. | Focused table cases cover both nested duplicate-name and unique unexpected definitions while preserving recursive root-tree coverage. |
| R10 | Windows line endings do not create false projection drift | Review showed that CRLF bytes fail the LF-only frontmatter delimiter and raw whole-file comparison even when all generated content is semantically canonical. | Claude parsing, checking, and whole-file sync comparison normalize CRLF to canonical LF while retaining exact comparison for every other byte. | The focused package run covers direct CRLF parsing, accepted CRLF projections, and no-op sync. Windows runtime execution remains not performed. |
| R11 | Git metadata pruning cannot hide a discoverable case-variant project scope | Review traced `.GIT/.claude/agents/pm-delivery-worker.md` through the case-folded directory exclusion: it is ordinary content on a case-sensitive host but never reaches inventory. | The walk skips only the exact root `.git` metadata directory; `.GIT`, `.Git`, and nested paths are inventoried normally. | Focused cases reject duplicate and unexpected definitions beneath case-variant Git-like directories and retain an exact `.git` metadata-pruning case. |
| R12 | Canonical CRLF values cannot cause repeated projection rewrites | Review traced canonical JSON CRLF through raw renderer output: actual bytes normalize to LF while expected bytes remain CRLF, so check fails and every sync rewrites. | The Claude renderer emits canonical LF bytes, and check/sync normalize both whole-file operands before exact comparison or writing. | The focused package run covers CRLF injected through canonical discovery text, LF-only rendering, successful check, and a zero-update second sync. |
| R13 | Claude EOL normalization reaches its canonical form in one pass | Review traced `\r\r\r\n` through the pairwise replacement as `\r\r\n`, then `\r\n`, then `\n`; rendering, checking, and syncing therefore applied different intermediate forms and could rewrite repeatedly. | The shared normalizer collapses an entire carriage-return run followed by LF to one LF in a linear pass while preserving bare carriage returns, making every subsequent call byte-identical. | Direct table cases cover repeated and multiple runs plus preserved bare carriage returns; canonical-source render/check/sync coverage injects `\r\r\r\n` and requires LF-only output and a zero-update second sync. |
