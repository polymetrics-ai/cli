# Ledger233

Red: admission-red at unchanged f84ff59a: healthy nonpaging POST loader passed; paging actual loader rejected body role fields, direct consumer refused body-only cursor as missing source query inventory. All new test bytes snapshotted before execution.

Green: direct-matrix-03 native1.26.6 count1 passed47/47 parent/leaf events: real loader, exact five-ID/three-body one-page direct flow, declaration negatives, input/default/size/query/body/capsule drift controls. No saved implementation/admission claimed yet.

Retained attempts: direct-green was compiler setup (HTTPBase field name); direct-green-02 physical direct passed while actual metaschema remained RED. direct-matrix caught numeric raw cursor coercion causing one send; validate supplied typed body roles before converting paginator state fixes that behavioral defect. direct-matrix-02 was fixture field-name setup; source declaration inspected and corrected, direct-matrix-03 GREEN. No setup failure counted as behavior.

Next: merge selected inert234 types, wire saved prepared body/schema; extensive boundary/oracle/race/compatibility and final lint/vet remain.

## Saved and final groups
Red: saved-red reaches exported Read/local HTTP and proves missing prepared body at merged234 interface checkpoint. Green: saved-green-02 passes48/48. First saved-green was a compile-only insertion in the separate fan-out discovery loop; concrete function source located and hunk moved into readOneSequence, no fan-out change retained.

Red: edge-red catches invalid schema defaults admitted and raw cursor loop unseeded. Green: edge-green64/64 validates defaults at admission and seeds Resume. Addressability-red separately proves a body-size-only next_url descriptor admitted despite no typed body URL navigation; unsupported body strategy now refuses at admission. This slice admits body cursor/page_number/offset_limit roles, not new URL/Link POST machinery. Legacy absent-request_inputs saved behavior and GET URL/Link remain intact.

Green: final-normal410/410; final-body-race80/80; earlier final-race151/151 includes72 NextLink events and79 then-current BodyPaging events. Counts overlap and are not additive. Source-compatibility18/18, original Requester6/6, vet pass. Scoped final lint reports7 preexisting/inherited findings:4 old229 and3 inert234 unused fields; no233-owned warning. All runs source-before/after stable, fresh count1, native1.26.6/cgo, focused20m.

Boundary controls: actual REST loader, safe POST/default1000 vs caller2, three exact bodies/five identities, one page per direct invocation, saved2/2/1/cap/resume, auth-frontier healthy+invalid, required/type/bounds/raw/schema/variant/conflict refusals, direct/saved changed body/query/schema/origin identity, malformed later token and raw/repeated cursor. Independent same-count-wrong/duplicate/omitted IDs and wrong body location falsify the literal oracle.

The five-ID fixture narrows cursor length to64 and uses a synthetic default2 for bounded multi-page controls. Exact retained Jira default1000 is separately tested; neither fixture is a claim of full Jira source projection or original provider-chain GREEN.

## Final mixed placement gap closure
Red: mixed-size-red was fixture admission/setup (caller size lacked rest.parameters) and reached no wire; not a behavioral RED. After inspecting the actual parameter contract, mixed-size-red-02 reaches both physical consumers: direct sends query limit3/body position2; saved overwrites caller query with limit2. Independent expected bodies require position3 and size3.
Green: final-mixed-normal412/412, final-mixed-race82/82, final-mixed-vet pass; final-mixed-lint retains exactly the7 independently pinned baseline/inherited findings. Preparations resolve effective query size before body placement/paginator construction; declared body size still has its exclusive destination and caller precedence. Source18/18 and CLI14/14/Requester6/6 compatibility receipts retained; no source/CLI/Requester implementation changed by this helper-only correction.

## Resolved path closure
Red: resolved-path-red and resolved-saved-path-red each preserve a healthy same-path resume, then prove changed caller path values incorrectly send twice. Green: final-identity-normal418/418, final-identity-race88/88 and final-identity-vet pass. The direct helper now takes resolved baseURL/requestPath and binds them together with full original operation/HTTP declaration and selected input schema; saved caller-known paths bind through existing InterpolatePath plus original stream/HTTP declaration.

Latest scoped lint6 findings:4 original229 plus2 parent234 compiled schema/bindings fields awaiting population. REST inputPlan is now consumed for capsule identity, so its old unused finding disappeared through actual use. No warning suppression or unrelated change.
