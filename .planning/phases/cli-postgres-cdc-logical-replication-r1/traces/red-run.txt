Command:

    go test -count=1 ./internal/connectors/native/postgres ./internal/cli

Result (expected before implementation):

    FAIL: TestConnectorSatisfiesCoreInterfaces
    native postgres connector must implement ChangefeedExecutor for logical replication

    FAIL: TestCDCIsNotAnUnsupportedStub
    postgres connector must implement a logical-replication ChangefeedExecutor

The parent fail-closed capability test remains intact: no bundle declaration or
metadata bit is sufficient without the matching runtime executor.
