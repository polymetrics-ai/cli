# TDD ledger — issue #4183

| ID | Class | Behavior and observable assertion | Red | Green |
| --- | --- | --- | --- | --- |
| R1 | Happy/live regression | Two-plus-page `full_overwrite` publishes rows from every page, not only the last. | Red: `TestPMBinaryPostgresFullOverwriteRetainsEverySourcePage` was added with batch size 2 / 3 rows; the historical per-page `TRUNCATE` left only the final page. Static trace located that shipped route. | Green: live production binary run with the direct Docker socket passes and asserts the durable target is exactly `[1,2,3]`; `TestOrchestratorFullOverwritePublishesAllBoundedPagesBeforeOneCheckpoint` also proves one publish/read-back/checkpoint. |
| R1b | Edge/live | Empty/single/multi-page fixtures preserve the exact expected final row set. | Red: the final-content assertion is intentionally the regression detector, not relaxed to an exit-status assertion. | Green: `TestOrchestratorArrowFullOverwritePublishesZeroSourceWithNoCreditLeak`, the two-page live binary test, and the one-row transformation fixture cover empty, multi-page, and single-record outcomes. |
| CI1 | Happy/edge regression repair | The existing all-mode App transport cases must exercise the same run-scoped full-overwrite contract as production while preserving equivalent stale-writer, cancellation, and missing-run coverage. | Red: CI's `full_overwrite` subtests first failed before their source/page hooks because `appTransportDestinationExecutor` did not implement `BeginFullOverwrite`; after the double was corrected, the seven runs showed that their legacy per-page checkpoint assertions are incompatible with the published-once contract. | The accepted acknowledgement-contract decision keeps production publish-then-checkpoint unchanged and splits the test rows by the acknowledgement they can honestly observe; no existing test is skipped or disabled. |
| CI2 | Happy/edge decision repair | App-level coverage reflects each mode's real acknowledgement cardinality: ordinary page modes retain per-page races, while `full_overwrite` proves pre-publish abort/no-checkpoint, post-read-back stale CAS, and post-final-checkpoint terminal rebase/missing-run behavior. | Red: the new ordinary callback test observed acknowledgement count `0`, proving `afterApply` ran before acknowledgement construction; the old full-overwrite stale-writer wait timed out at 21.28 s because its page-apply signal preceded publication; and the new full-overwrite source/cancellation inverse initially observed `apply/abort/publish/readback = 1/0/0/0`. | Green: `afterApply` now follows ordinary acknowledgement construction, and separate page/publish/read-back/abort hooks plus a durable-store locker prove the decided contracts. The named full-overwrite source-failure, cancellation-before-publish, post-read-back stale-writer, final-checkpoint rebase, and final-checkpoint missing-run tests pass; the stale-writer test passes 20 consecutive executions at the unchanged 20-second timeout. The two end-to-end post-final-checkpoint cancellation/failure rows remain explicitly inapplicable for `full_overwrite` because no real controller observation point exists; no test is deleted or skipped. |
| R2 | Happy | A neutral typed batch becomes a versioned segment and manifest entry under byte-credit bounds. | Red: manifest and byte-credit tests were added before their implementations. | Green: `TestSegmentManifestV1PublishesAndReopensImmutableSegment` and `TestByteCreditControllerBoundsConcurrentRetainedBytes` pass. |
| R2b | Bad | An over-credit/corrupt-manifest input returns its typed refusal before a bulk applier call. | Red: tests named typed refusal requirements first. | Green: `TestSegmentManifestV1RefusesInvalidIdentityBeforePublishing`, `TestSegmentManifestV1RejectsReplayAndCorruption`, and `TestByteCreditControllerRefusesOversizedUnitBeforeWaiting` pass. |
| R2c | Edge | Zero rows, a full 512 MiB boundary, and one byte over retain bounded accounting. | Red: controller/credit fixtures were written before admission implementation. | Green: `TestOrchestratorArrowFullOverwritePublishesZeroSourceWithNoCreditLeak`, `TestByteCreditControllerBoundsConcurrentRetainedBytes`, `TestByteCreditControllerRefusesOversizedUnitBeforeWaiting`, and cancellation/release coverage pass. |
| R3 | Happy | A normalized projection/type/expression/filter plan has stable canonical bytes and hash. | Red: plan fixture added before parser; direct timestamp identity projection was later added and initially refused. | Green: `TestTransformPlanV1NormalizesAndValidatesTypedProjectedFilter`, `TestTransformPlanV1PreservesTypedTimestampDirectly`, and `TestReadTransformPlanFileReturnsNormalizedClosedPlan` pass. |
| R3b | Bad | Invalid expression/type/hash drift fails pre-I/O with exact typed reason. | Red: unsafe SQL-like expression fixture added before parser. | Green: `TestTransformPlanV1RefusesUnknownOperationBeforeConnectionIO` and `TestReadTransformPlanFileRefusesNonRegularPathBeforeRead` pass. |
| R3c | Edge | Empty/oversized files, duplicate/missing identifiers, normalized replay, timestamp units, and null/zero rows stay deterministic. | Red: the microsecond Arrow timestamp COPY source initially decoded as nanoseconds. | Green: `TestReadTransformPlanFileRefusesEmptyAndOversizedInput`, `TestTransformPlanV1RejectsDuplicateTargetAndSchemaDrift`, `TestPostgresArrowCopyFromSourceReusesTypedRowVector`, and zero/null Arrow tests pass. |
| R4 | Happy/live | Binary `CopyFrom` shadow publish produces transformed target rows, one receipt, then checkpoint. | Red: the transformed production-binary test was introduced before Arrow range/COPY routing; no route existed. | Green: `TestPMBinaryPostgresTransformedFullOverwriteUsesArrowCOPY` passes live: exact projected/filtered rows, one receipt, durable metrics, then checkpoint. |
| R4b | Bad/live | A malformed fast path is refused before target mutation. | Red: missing-segment/invalid Arrow input tests preceded routing. | Green: `TestOrchestratorArrowFullOverwriteRefusesMissingSegmentStoreBeforeExtractorIO`, `TestPostgresArrowRangeCheckpointRefusesNonPostgresSourceBeforeQueryIO`, and `TestPostgresArrowCopyFromSourceRefusesUnsupportedOutputType` assert typed pre-I/O refusals. |
| R4c | Edge/live | Receipt identity is deterministic and a full-overwrite run has one publish lifecycle. | Red: receipt tests preceded its closed contract. | Green: `TestFullOverwriteReceiptV1IsDeterministicAndPayloadFree`, replay/corrupt-manifest tests, and the live one-receipt assertion pass; checkpoint follows receipt read-back in the controller. |
| R5 | Happy | Success persists source/transform/Parquet/COPY/index/publish/checkpoint phase data before cleanup. | Red: phase-measurement assertion preceded fields. | Green: `TestTransportPhaseMeasurementReportsLogicalInputThroughputAndEveryFastPathPhase` plus the live binary transformed test pass. |
| R5b | Bad | Invalid authorization/plan/checkpoint rejects before extractor or applier I/O. | Red: transform-hash drift and missing-fast-store fixtures preceded enforcement. | Green: `TestCreateConnectionRefusesTransformHashDriftBeforePersistence`, approval binding tests, and the fast-path refusal test pass. |
| R5c | Edge | Cancellation, stale checkpoint, constrained credits, and interleaved receipt lifecycle remain bounded. | Red: credit cancellation and per-unit timeout fixtures preceded controller behavior. | Green: `TestByteCreditControllerCancelledWaitDoesNotLeakCredit` and the established per-unit timeout tests pass; no run-wide deadline was added. |
| R6 | Happy/live binary | The tagged binary fixture proves exact transformed content and reports MB/s + MiB/s from measured logical bytes. | Red: 5 GB harness was absent. | Honest result: the direct Docker-host harness moved 5,368,947,776 logical source bytes and completed the realistic mapping at 111.78 decimal MB/s / 106.60 MiB/s in 48.03 s. It fails the 200 MB/s/25 s gate as designed; JSON report is committed. |
| R6b | Bad | Missing performance opt-in visibly skips; low disk hard-stops before a segment. | Red: harness/space guard fixtures preceded implementation. | Green: tagged harness skip is observed without opt-in; `TestRequireMinimumFastPathFreeSpaceRefusesBeforeSegmentWrite` asserts typed 3 GiB refusal before a write. |
| R6c | Edge | Identity and transformed runs on the same source report transformation tax without scoring identity as the feature result. | Red: two-run report was absent. | Honest result: identity was 105.68 MB/s / 100.79 MiB/s in 50.80 s; realistic was 111.78 MB/s / 106.60 MiB/s in 48.03 s. The report records both, but only the realistic gate is scored. |
