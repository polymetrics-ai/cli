# Eval Plan

- `source-postgres` plan mentions logical replication setup and CDC state.
- `source-mysql` plan mentions row-based binlog and GTID/file-position state.
- MongoDB source plan mentions change streams and resume tokens.
- `source-github` plan remains enabled and wave 0.
- Planned destinations show write work as planned, not enabled reverse ETL.
