# Context: Issue #3990

The issue body, verified-state brief, and connector canon fully determine the implementation choices for this bounded corrective slice. No product choice is left open: GraphQL must account from its own `rateLimit` body fields; certification must require the already-landed shared registry; scopes must be provider-declared and opaque; and a refusal must prove zero sends.

The task is executed non-interactively under the Firstmate launch brief. The manual discussion conclusion is therefore to adopt the issue's specified defaults and preserve its stated exclusions.
