# UAT — seven connector extraction r1

## Inline/manual fallback

The repository's canonical parent contract forbids spawning GSD verification roles for this
single-worker captain assignment. The `verify-work` prompt was generated with the project adapter;
the following acceptance checks were performed inline from its required evidence.

| Deliverable | Evidence | Result |
| --- | --- | --- |
| Seven completed connector command surfaces | Compiled real binary reached every 1,984 implemented path and its own `NAME` line; per-connector totals are 911, 584, 139, 127, 100, 63, and 60. | pass |
| Shared plural-write foundation | Focused red/green tests plus validator loaded all 551 bundles with 0 findings. | pass |
| Generated CLI/docs/website parity | `surface-sync --check`, docs validation, website generation, full CLI golden tests, and bare/help namespace checks pass. | pass |
| Scope and safety truthfulness | Source trees match `c28bc75a3` for only the seven; no github/zendesk input; no credential or live-service operation occurred. | pass |

No human judgment is requested for these automated/local checks. Certification and live-service
exercise are deliberately not claimed; the PR body wording is fixed in `SUMMARY.md`.
