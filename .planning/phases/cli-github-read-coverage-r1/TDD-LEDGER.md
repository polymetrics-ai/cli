# TDD ledger — GitHub live read coverage r1

| Slice | Red evidence | Green evidence | Refactor / result |
| --- | --- | --- | --- |
| Credential-rendering guard | `node --test scripts/tests/github-live-artifact-guard.test.mjs` was red: synthetic `password=`/`token:` assignments reached `redactPersistedText` unchanged. | The same test is green: recognized sensitive assignments become `<redacted>` and unrenderable unsafe text throws before any return value. | The guard now preserves ordinary line breaks while rejecting other unsafe controls, PEM/token/JWT-like residual material. No real credential was used. |
| Scope classification | Existing profile declares only two direct reads and one binary candidate despite the declared 1,571-command / 37-stream surface; therefore coverage is not representative. | `SCOPE.md` reconciles every command and stream into mutually exclusive, non-pass-aware buckets before candidate changes. | Discovery found 127 directly targetable commands and 29 streams, but no schema/runtime representation for a produced-value assertion.  The required shared foundation is explicitly split rather than hidden in GitHub code. |
| Declaration-owned direct-read assertion foundation (#4191) | `go test ./internal/connectors/certify ./internal/connectors/engine` was red because neither the generic assertion type nor the direct-read evaluator existed. | The same targeted packages are green after schema, bundle validation, and the generic direct-read assertion evaluator were added. The false-value test fails without rendering provider values; the matching synthetic value passes. | Existing connectors may omit assertions unchanged. Assertions can only address `/response/...`, never the envelope kind or exit status, and duplicate/invalid JSON Pointers are rejected at bundle load. |
| Candidate execution | After `go run ./cmd/connectorgen validate internal/connectors/defs` passed, a scratch-only assertion changed the first GitHub candidate to an impossible value. The compiled binary returned certification exit 2 at that candidate while the command itself retained the correct result kind. | The scratch declaration was restored and revalidated. The final compiled `--direct-read-only` live run passed all 23 candidates, each after a real direct read produced its declared value/type. | The failure reports only the stage and JSON Pointer, never the provider value. Scratch artifacts were discarded after the proof. |
| Boundary roll-up | The plain-text report previously omitted the direct-read capability, so it could not state the real boundary even though JSON carried it. | `TestRenderCertifyReportTextStatesDirectReadBoundary` is green: text shows the candidate count and explicitly says it is not a whole command/stream surface claim. | `SCOPE.md` has 23 passes; every other read bucket stays concrete non-pass or out-of-scope and the command/stream totals reconcile. |
| Resumable rate-limited execution | An interrupted sweep previously had no declaration/configuration-bound record of completed reads. | `TestDirectReadCheckpointResumesOnlyMatchingCandidates` and checkpoint persistence tests are green; the final live run recorded 72 bounded rate-limit admission events while serially completing all 23 candidates. | A resumed row is explicitly marked `resumed` with `CertificationCheckpoint`, never represented as a new provider call. |

No existing test will be weakened, skipped, or mode-excluded.
