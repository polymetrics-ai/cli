---
status: complete
phase: issue-3856-polling-watermark-conformance-corpus-r1
mode: automated-contract
fallback: inline/manual GSD verify-work under --auto; the canonical single-worker contract forbids GSD-role spawning and this corpus has no human-facing provider flow
---

# UAT — #3856 polling-watermark conformance corpus

## Automated checkpoints

### 1. No-skip corpus execution

Expected: one registered lane factory executes the complete immutable fixture
set without accepting a filter, skip list, or lane-owned fixture list.

Result: pass — `TestPollingWatermarkConformanceSuiteRunsEveryMandatoryFixture`
asserts all eleven IDs, the two-argument non-variadic runner signature, equal
watermark page splitting, acknowledgement-before-persistence failure, restart
from the prior committed envelope, and no stable-identity loss.

### 2. Unsafe watermark inputs and ordering

Expected: NULL and float coercion are refused; nanosecond timestamps and large
JSON numeric tie breakers remain exact; non-unique tuples and non-advancing
pages are rejected; bounded overlap never regresses durable state.

Result: pass — the corpus carries raw JSON samples and duplicate tuples. The
#4074 correction derives the lower overlap request from the source record and
requires it to precede the durable checkpoint.

### 3. Recovery and deletion state

Expected: source-generation/schema mismatch yields typed rebootstrap state,
acknowledgement persistence failure replays safely, soft deletes tombstone and
close history, and hard deletes remain invisible.

Result: pass — the runner validates `CheckpointEnvelope` validity/commit state
and fixture-matched source, generation, schema, and mechanism provenance,
typed `RebootstrapRequiredError` outcomes, replay positions, tombstone/history
identities, and hard-delete invisibility.

### 4. Admission fail-closed

Expected: an unregistered lane, unsafe registration, or stale/incomplete
evidence cannot execute the corpus.

Result: pass — registration is checked before every fixture; explicit tests
cover zero-value registration, unsafe keyset/cursor/overlap/commit-lag
declarations, and incomplete evidence.

## Summary

total: 4
passed: 4
issues: 0
human_judgment: not applicable — this child is an executable deterministic
test/corpus contract, not a credential-backed or user-facing provider flow.

No live credential, provider, GitHub-provider, PostgreSQL, or database mutation
was performed. This is not live Transport certification.
