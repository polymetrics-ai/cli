# TDD ledger — issue 4336

| Slice | Red evidence | Green evidence | Refactor / scope evidence |
| --- | --- | --- | --- |
| Real provider dialects and bounds | `go test -timeout 20m ./cmd/connectorgen -run 'TestSourceImportProviderDialectContracts\|TestSourceImportKeepsDepthBoundFiniteAfterProviderIncrease' -count=1` exited 1: Bitbucket and Notion exceeded schema depth; Stripe exceeded reference depth. The reduced operation fixtures retain each provider path, operation ID, and source construct from the pinned 2026-08-23 artifacts recorded in issue #4325. | The same command exits 0 after the 64-level finite bound. The test imports retained Bitbucket and Notion response nests, follows Stripe's 30-reference source-shaped chain, and refuses a 65-level pathological document. | The real provider artifacts cannot yet be imported end-to-end because of the request-contract findings recorded in `REQUEST-CONTRACT-INVENTORY.md`; this lane neither suppresses nor treats those as green. |
| Supported schema constructs | `go test -timeout 20m ./internal/connectors/engine -run 'TestSchemaCompileKeywordMatrix\|TestSchemaValidateInstances' -count=1` exited 1: `example` and `patternProperties` were unknown keywords. The importer red suite also rejected Vercel's response `patternProperties`. | `go test -timeout 20m ./internal/connectors/engine -run 'TestSchemaCompileKeywordMatrix\|TestSchemaValidateInstances' -count=1` exits 0: `example` remains an annotation and `patternProperties` validates matching keys without admitting unmatched keys. | Unknown and dynamic schema keywords remain rejected. |
| Retained malformed provider contracts | The importer red suite exited 1: Docker Hub's `#/components/responses/team_repo` resolved to response rather than schema; GitLab's `epic_issue_id` had no required path parameter. | `TestSourceImportProviderDialectContracts` exits 0 and proves each affected operation remains in the result, merge-blocked by a source-traced gap naming the pointer or path placeholder. | Only response-schema wrong-target/dangling local references and path-contract errors are retained; other malformed positions still fail. |
| Regression and pathological controls | `TestSourceImportKeepsDepthBoundFiniteAfterProviderIncrease` is present before production edits; it will prove a document above the new finite bound remains rejected. The first full `go test -timeout 20m ./cmd/connectorgen` run exposed stale expectations that required malformed response-schema/path contracts to abort. | The targeted importer suite exits 0 with the 65-level refusal; after the expectations were strengthened to require a merge-blocked source-traced gap, `go test -timeout 20m ./cmd/connectorgen` exits 0. `TestSourceImportPreservesFrozenGitHubArtifacts` remains the byte-identical existing-importable projection control. | No connector definitions change. |
