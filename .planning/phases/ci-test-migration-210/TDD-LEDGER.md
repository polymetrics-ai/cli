# TDD ledger
Baseline6f9366b4. Original203 log is historical (45c235dd), never substituted for current runs. Every attempted command/output retained under private ci-test-migration-210 receipts.
Red: baseline receipt records original assertions and separately labeled setup failures. C Linux historical inode-reuse RED remains original203 evidence.
Green: scoped normal and race receipts below; Linux post-fix remains pending.

## A/C/D/E slice
Red: private baseline receipt at initial planned source1d1e87b2 (production6f9366b4),32.26s, exit1, retains original A/D/E assertions plus F/G setup failures; C/139 macOS healthy. Hosted C inode-reuse failure remains original203 Linux RED, not falsely reproduced here.
Green: acde-green30.96s exit0,142 test lifecycle events, no skips. Complete execution Entry projection zeros only independent SourceVisibility; both stage/index/hook checks and unrelated/malformed diagnostic counterfactuals retained. C renames A into retained sibling, checks its original identity and bytes, complete A/B device/inode/type witness with deliberately wrong-state controls. D expected connector hash b7cc34c4… → c8a06a68… after exact intentional0b214b79 canonical overlay transition; base hash and state assertions retained. E original invalid_cli_surface member now asserts scanner independence; separately actual loader refuses malformed execution and historical citation JSON with healthy execution surface control. No production edits. Linux C/139 post-fix remains pending hosted verification.

## F/G slice
Red: baseline records all four Recurly missing-input failures and both stale native source-path failures. These are setup migration RED, not claimed provider behavior failures.
Green: fg-green15.33s exit0,360 lifecycle events, no skips, full Recurly and credential packages. Restored exact97 historical inputs from2008509f40c90de5d041aa944b19fedfecc15533 with path/size/SHA ledger; all93 stream first pages plus second-site/two-write/retry closure, outside defs.FS. Hash oracle rejects modified bytes and embedded absence is asserted for every input. Raw OAS is not retained; operation/parameter coordinates in historical evidence are preserved, current provider accuracy unclaimed. G now uses external credential_test package to reach real registry without import cycle; SafetyCulture/Canny Check and Read assert exact transport bytes and expected records. Absent versus explicit-empty typed distinctions and unsafe SafetyCulture header no-send assertions pass; fake observer rejects one-/two-edge trimming mutations. amazon-ads original AST coverage remains.

Additional A oracle control enumerates every Entry field: each non-diagnostic field mutation must break execution equality, diagnostic-only mutation must not. Full projection cannot silently omit future fields (an unseeded new field fails the counterexample).

Final Green: scoped-race exit0,170.30s,474 lifecycle events,no skips; scoped vet and agentcontractgen check pass. Lint remains failed on20 base-identical findings in8 unowned files, documented in VERIFICATION. Independent review is parent-owned; no self-review performed.
