# Verification evidence

Inline/manual GSD verification: compatible isolated role delegation is forbidden by this assignment and the canonical contract. Discuss → plan --tdd → execute → verify prompts were resolved/read and executed inline. Code-review prompt was resolved/read; independent review is handed to parent, not performed by the implementer.

| Contract / owner | Desired RED / healthy control | Observed evidence / effects |
| --- | --- | --- |
| A connectorgen admission test | SourceVisibility may differ; every execution Entry field must agree | baseline assertion failed; acde-green and projection-control pass; scoped-race passes. Both staged/indexed projections plus GitHub hooks retained; no production writes. |
| C publication observation test | Retained live A differs from replacement B; invalid identity/type rejected | Historical Linux RED in203; macOS acde-green/scoped-race pass all fifo/symlink/directory and139 schedules. Renamed A retained and bytes/identity re-read; bounded subprocess cleanup unchanged. Linux post-fix remains unverified. |
| D agentcontract test | Exact intentional source-lock overlay render; base and inheritance stable | baseline golden failed; new exact connector SHA plus existing base/state assertions pass; agent-contract check passes. No generated output edits. |
| E boundary and engine tests | Scanner ignores CLI citation artifact; actual execution loader refuses malformed input | baseline scanner expectation failed; scanner independent and actual loader healthy/malformed controls pass normal/race. No provider I/O. |
| F Recurly tests and testdata | Complete exact historical closure and original query/retry/stream/legacy assertions | Baseline missing-input setup failures; full package green, race selected all Review/Recovered/new provenance roots green.97 source-hashed records; mutated bytes rejected; all inputs absent runtime embed. |
| G credential tests | Actual registered Check/Read preserve edge bytes; invalid credentials never send | Baseline deleted-path setup failures; full package green, race all RegisteredCredential roots green. Healthy record and request counts, typed errors, wrong-byte oracle controls; amazon-ads AST member retained. Fake transport only. |

Receipts under private ci-test-migration-210 retain complete argv, JSON membership, raw output, source HEAD/diff/untracked hashes, dependency hashes, exit and elapsed. acde-green30.96s; fg-green15.33s; projection-control14.47s; scoped-race170.30s; all exit0 and no skips. Scoped vet6.85s exit0. Agent-contract4.58s exit0. gofmt touched only owned files; git diff --check passes.

Scoped lint20.92s exit1:20 findings across8 unmodified/unowned files; lint-unowned-source.json binds every file to exact base6f9366b4 bytes. No suppression or out-of-scope correction. Full Verify/full CLI corpora not run or claimed. No build/result cache substitution (-count=1 throughout).

Required remaining evidence: Linux run exact scoped publication/139 selections with -count=1 -timeout20m and retained A/B identity witnesses; parent integration and independent review; baseline lint disposition. Historical Recurly raw OAS source is absent in Git; retained retry record has23 operation and3 parameter coordinates with its original source hash. This proves historical fixture/runtime assertions, not renewed provider accuracy.

Firstmate215 procedure read fully: preserve failed setup attempts, distinguish setup from behavioral RED, diagnose exact tooling after two failed approaches. No unresolved repeated setup approach remains.

CLI help/manual/docs/website parity is not applicable: test/evidence migration changes no command, flag, help, runtime output or generated surface. Original203 artifacts and parent B diagnostic work remain untouched. No acceptance or review claim.
