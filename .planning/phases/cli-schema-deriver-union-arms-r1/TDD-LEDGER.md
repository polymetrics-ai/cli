# TDD Ledger

Phase: cli-schema-deriver-union-arms-r1

Record failing test evidence before production code for every behavior-adding task.

| Slice | Red command/evidence | Green command/evidence | Status |
| --- | --- | --- | --- |
| Union-rooted record schema is never promoted hollow | The initial validator fixture failed on 2026-08-05 with `findings=[]`, reproducing the union-rooted no-flag promotion. The final regression moved to the real gate because connectorgen must not copy runtime rules. | `go test ./internal/connectors/engine ./internal/connectors/commandrunner -run 'Test(InspectRecordSchema|ValidatePromotableRecordSchema|PreflightRejectsHollowDeclarativeRecordSchemas)' -count=1` passed: oneOf/anyOf arms are measured before promotion, root unions require named actions, and a derived empty closed object fails real runtime preflight. | green |
| Zendesk FOUNDATION actions, OAS contract, and bounds | `go test ./internal/connectors/defs/zendesk-support -run '^TestZendeskFoundationOperationRequestFieldsCitePinnedOAS$' -count=1` failed: the four permission-action wrapper fields lacked citations. `go test ./internal/connectors/defs/zendesk-support -run '^TestZendeskFoundationMergeUserAcceptsOASIdentifierVariants$' -count=1` failed because the authored merge arm invented an `external_id` requirement. | Recursive source-citation and operation/action parity tests pass; five fixture-backed actions execute; id/email merge identifiers pass; each documented 100-user bound rejects item 101. | green |
| CodeQL capacity-overflow remediation | GitHub Advanced Security check run `92293450431` failed with four `Size computation for allocation may overflow` findings on the two `len(baseRequired)+len(armRequired)` preallocation expressions in `mergeRecordSchemaRequired`. During the rewrite, `go test ./internal/connectors/engine -run '^TestMergeRecordSchemaRequiredKeepsAnEmptyRequiredArray$' -count=1` failed with `merged required = null, want []`, protecting the existing empty-array contract. | `go test ./internal/connectors/engine -run '^Test(MergeRecordSchemaRequired|InspectRecordSchema|ValidatePromotableRecordSchema)' -count=1`, commandrunner preflight tests, and `go vet ./internal/connectors/engine ./internal/connectors/commandrunner` pass after removing the length addition while retaining order and `[]` serialization. | green locally; CodeQL rerun pending |
