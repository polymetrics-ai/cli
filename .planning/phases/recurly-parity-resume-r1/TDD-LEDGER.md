# TDD ledger — Recurly parity-resume r1

| Slice | Red evidence | Green evidence | Status |
| --- | --- | --- | --- |
| Recovery | `go run ./cmd/connectorgen validate recurly` exited 1: `validate: read root: open .: no such file or directory`, because current main has no Recurly bundle. | Restored only `internal/connectors/defs/recurly/**` from `6b3224e7f`; current validator loads and reports zero findings. | green |
| Required preview flags | `go run ./cmd/connectorgen validate internal/connectors/defs/recurly --json` found six `cli_surface_safety` failures: invoice `currency`; purchase `currency` and `account.code`; gift-card `product_code`, `unit_amount`, and `currency`. | All six flags are `required: true`; focused validation returned zero findings and warnings. | green |
| Field citations | No approved citation metadata exists on this base; raw provider field matrix required without inventing a local convention. | `RECURLY-PROVIDER-FIELD-RESEARCH.json` records all 197 provider operations and 2,951 provider inputs from Recurly's pinned OpenAPI source. `RECURLY-LOCAL-REQUEST-FIELD-RESEARCH.json` remains the preserved raw local join; shared citation integration is explicitly outside this phase. | raw evidence preserved |
| Binary reads | Recovered binary operations were `planned` and API-surface-blocked despite current binary-download runtime support. | `TestBinaryDownloadCommandsExecute` preflights and executes invoice PDF, export dates, and export files through commandrunner and engine against a local fixture; all three write only below `DestRoot`. | green |
| Full operation parity | Recovered 197 operations are historical evidence only. | Recurly's provider-owned v2021-02-25 OpenAPI (`https://recurly.com/developers/api/spec/v2021-02-25.yaml`) contains exactly 197 method/path operations. Every one has an exact bundle endpoint and executable coverage: 93 streams, 96 typed writes, five JSON direct reads/previews, and three binary downloads; zero are planned or blocked. | green |
| CLI generated parity | Full `go test ./internal/cli/...` first failed only because root help now correctly lists `pm recurly`. | Regenerated `internal/cli/testdata/golden_transcripts.json`; focused golden and tracked-manual tests pass. The complete `go test ./internal/cli/... -count=1` then passed in 389.209s. Website data generation includes Recurly. | green |
| Current-main metadata preservation | `TestRecoveredLegacyStreamMetadataPreserved` failed for all five pre-existing streams: required `id`, cursor metadata, typed fields, `limit=200`, computed projections, and fixture fields had been replaced by generic recovery data. `TestRecoveredConnectionSpecPreservesBaseURLFormat` also failed because `base_url` had lost its `uri` format. | Restored the current-main schemas, stream defaults/projections, and fixture bodies; restored `base_url.format`. The connector-local tests now pass, and a semantic base-versus-current audit confirms zero missing legacy schema properties/required fields, matching primary/cursor metadata, matching stream contract, and matching fixture response bodies. | green |
| Provider OAS reconciliation | Recovered count alone was insufficient evidence. | The current provider reference redirects to `https://recurly.com/developers/api/v2021-02-25/`; its pinned OAS SHA-256 is `b98a3f85d0a1190c2c8e11f57fa5ec13b841665e658596dcb5d7f3ddce70baca`, with 197 operations: 97 GET reads and 100 POST/PUT/DELETE writes. The Recurly API surface has exactly the same 197 method/path pairs, with zero missing and zero extra. | green |
| Review: exact request contracts | `TestReviewWriteContractsMatchProviderShapes` failed: 25 request-body operations remained bodyless, required gift-card objects and credit-policy shape were absent, and banking identifiers were not all redacted. | Pinned-OAS regeneration now models 59 JSON bodies, 49 required bodies, all required nested fields, closed records with documented opaque regions, typed fixtures, and complete billing redaction. The final focused gate passed. | green |
| Review: safe write retries | `TestDeclarativeWriteRetryPolicy` failed to compile because `WriteAction` had no idempotency-header declaration; the local contract test also found all 96 actions undeclared. | Declarative writes now run once without a declared provider key; Recurly actions generate a fresh key per approved record and reuse it across only that record's retries. The final focused gate passed. | green |
| Review: decimal CLI input | `TestCoerceFlagValueNumber` rejected `12.34` as an unsupported flag type, and the Recurly contract test found `unit_amount` still typed as integer. | Provider-neutral finite-number coercion and schema compatibility now carry decimal gift-card amounts through the generated command. The final focused gate passed. | green |
| Review: ETL response contracts | `TestStreamReplayResponseHeadersExpandBaseURL` observed no replayed Link header; the Recurly contract test found generic open schemas, missing durable keys, incorrect performance-obligation extraction, and no second page. | Non-legacy schemas and fixtures now follow OAS response items, performance obligations extract `data`, derived keys cover entitlements/account balances, and replay proves two-page Link pagination. The final focused gate passed. | green |
| Review: limiter boundary | OAS evidence documents 400 requests/minute for sandbox and 1,000 GET requests/minute for production, so the recovered global 1,000/minute runtime limiter is not a faithful boundary. | Removed the enforced stream-runtime limiter, retained informational metadata, and documented the provider-specific sandbox/production boundary. The final focused gate passed. | green |
| Review: explicit delete retry boundary | Current `writeRequester` sets `DisableRetries` for every action without a provider-key header, including actions with `delete.idempotent: true`; 1,120 base actions carry that explicit declaration. | Explicitly idempotent deletes retain retries, unmarked deletes remain single-attempt, and Recurly delete retry evidence is operation-scoped. The final focused gate passed. | green |
| Review: mutation query controls | The pinned OAS exposes exactly three mutation query inputs (`deactivate_account.redact`, `terminate_subscription.refund`, and `terminate_subscription.charge`), while the generated actions, CLI commands, and fixtures expose none. | Generated typed record/query/CLI contracts and query-aware fixtures now expose all three controls while requiring explicit refund and redaction choices. The final focused gate passed. | green |
| Review: explicit termination charge | `terminate_subscription.charge` is optional and uses `omit_when_absent`, but shared query resolution intentionally tolerates absence only for config, secrets, and incremental references; an omitted `record.charge` therefore passes preview and fails before HTTP execution. | The generator now requires charge in schema, query, CLI, and evidence; preview rejects omission, explicit false remains valid, and the final focused gate passed. | green |
