# TDD ledger — Recurly parity-resume r1

| Slice | Red evidence | Green evidence | Status |
| --- | --- | --- | --- |
| Recovery | `go run ./cmd/connectorgen validate recurly` exited 1: `validate: read root: open .: no such file or directory`, because current main has no Recurly bundle. | Restored only `internal/connectors/defs/recurly/**` from `6b3224e7f`; current validator loads and reports zero findings. | green |
| Required preview flags | `go run ./cmd/connectorgen validate internal/connectors/defs/recurly --json` found six `cli_surface_safety` failures: invoice `currency`; purchase `currency` and `account.code`; gift-card `product_code`, `unit_amount`, and `currency`. | All six flags are `required: true`; focused validation returned zero findings and warnings. | green |
| Field citations | No approved citation metadata exists on this base; raw provider field matrix required before applying the landed convention. | `RECURLY-PROVIDER-FIELD-RESEARCH.json` records all 197 provider operations and 2,951 provider inputs from Recurly's pinned OpenAPI source. `RECURLY-LOCAL-REQUEST-FIELD-RESEARCH.json` joins all 538 locally exposed request fields with zero unmatched. Final convention rebase remains pending. | raw evidence complete |
| Binary reads | Recovered binary operations were `planned` and API-surface-blocked despite current binary-download runtime support. | `TestBinaryDownloadCommandsExecute` preflights and executes invoice PDF, export dates, and export files through commandrunner and engine against a local fixture; all three write only below `DestRoot`. | green |
| Full operation parity | Recovered 197 operations are historical evidence only. | Recurly's provider-owned v2021-02-25 OpenAPI (`https://recurly.com/developers/api/spec/v2021-02-25.yaml`) contains exactly 197 method/path operations. Every one has an exact bundle endpoint and executable coverage: 93 streams, 96 typed writes, five JSON direct reads/previews, and three binary downloads; zero are planned or blocked. | green, citation-format integration pending |
| CLI generated parity | Full `go test ./internal/cli/...` first failed only because root help now correctly lists `pm recurly`. | Regenerated `internal/cli/testdata/golden_transcripts.json`; focused golden and tracked-manual tests pass. Website data generation includes Recurly. | green, rerun full CLI suite after final citation rebase |
