# Prompt snapshot — Recurly parity-resume r1

Manual-GSD fallback activated because the repository adapter does not expose `programming-loop`.

Task: resume Recurly to full provider-documented operation parity from preserved snapshot
`6b3224e7f`, repair six required flags, promote three binary reads after runtime execution testing,
and create per-request-field provider research without inventing the in-flight citation convention.

Downstream artifact: this phase's plan, ledger, matrix, verification record, and summary.

Verification evidence: see `VERIFICATION.md`; the outer no-mistakes run owns the final outcome.
