# Issues #3990 and #4091 — TDD ledger

| Slice | Red | Green | Refactor / evidence note |
| --- | --- | --- | --- |
| Lifecycle/manual fallback | `gsd-sdk query init.phase-op issue-3990-4091-github-live-proof-club-r1` returned `phase_found: false`; the official roadmap-phase workflow cannot own this combined captain task. | Manual issue-backed CONTEXT/PLAN/TDD/VERIFICATION artifacts created before production edits; all required command prompts are generated and executed inline. | Reason is compatibility, not a lifecycle exemption. |
| #3990 live proof | A built-binary `--full --write` run completed against real GitHub with `passed=false`: 19 empty-stream flow failures, 8 query attempts after typed live-unavailable reads, and a destructive label-cleanup plan that did not preview before looking for its token. The failed cleanup left one run-tagged label, then an independent PM list found zero matching labels after an emergency plan → preview → approved delete. A later external proof RED rejected 17 distinct targets because a per-target retry bound was incorrectly applied to the whole run. | Focused tests pass after query/flow skips, mandatory cleanup preview and destructive confirmation, and a 16-per-target / 4,096-total external exchange bound. The fresh external child then passed against real GitHub: 1,370 stages, zero failures, 83 fingerprinted exchanges, no 429/abuse response, and zero residue. | The fixed GraphQL query returned actual provider cost data; approved `addStar` / `removeStar` proved the mutation route and independent count changed `0 → 1 → 0`. |
| #4091 live proof | The real-binary preflight exposed that `parseETLRunTransportApproval` rejected the app-supported durable same-plan carriage unless a new token was also supplied. `TestETLRunTransportApprovalAllowsDurablePlanReferenceWithoutTokenCarrier` failed with `approved issue-label transport ETL requires --approval-plan, --approval-token-stdin, and --confirm destructive`. | The focused parser suite passes after making the token stdin marker optional only for the forward route while retaining plan ID and typed destructive confirmation. Cleanup still requires its one-time token. The rebuilt binary completed first and tokenless identical-scope runs for both non-additive modes with exact GitHub read-back. | Old-token replay is asserted as typed `AuthorizationTokenReplayError`, while the live CLI refusal left labels and stream checkpoints unchanged. A real GitHub 401 produced typed `connsdk.HTTPError`, zero loaded records, no checkpoint, and unchanged labels. |
| Proof driver changes | New focused tests failed before production changes: the query test dereferenced the absent harness instead of skipping; the empty-flow and destructive-cleanup protocol tests rejected the old behavior. | The three focused regressions pass through the existing Runner/CLI seam. | Changes are limited to defects observed in the real run; no speculative harness rewrite. |
| CLI documentation parity | A post-generation grep showed that the transport-specific help and golden transcript described durable tokenless reruns, while the embedded `pm etl` manual and website still instructed every forward run to provide a fresh token. | The embedded manual source, generated CLI manual, website ETL page, generated website data, and golden transcripts now describe first approval versus exact-scope durable reuse consistently; `pm docs validate` and all help entry points pass. | This is required parity for the live-exposed parser fix, not a new command or capability. |

## Red/green rule

Any defect discovered live receives a production-path failing regression before source changes. A
refusal regression must assert its typed error and negative side effects; a passing exit code or
absence of panic is never GREEN.
