# TDD LEDGER — issue 3595 icon registry single-source foundation

## Red-first expectations

| Area | Red evidence before production edit | Green evidence |
| --- | --- | --- |
| Canonical key policy | Test/proof fails when registry contains `source-*` / `destination-*` keys or ambiguous collapses | generator/validation rejects prefixed keys and audited collisions; canonical registry uses bare keys |
| Go runtime lookup | Existing exact/source/destination fallback behavior is demonstrated | Go runtime resolves exact bare identifiers only |
| Website mapping authority | Website-only `website/data/icon_overrides.json` masks canonical registry gaps | website scripts read only `internal/connectors/icon_data.json`; override file removed |
| Asset authority | Website-only SVGs can exist without canonical docs source assets | canonical SVGs live under `docs/connectors/icons/**`; website public assets are generated/copied copies |
| Ownership consumer | Ownership cannot resolve examples like `apify-dataset -> icons/apify.svg` and `apple-search-ads -> icons/simple-icons/apple.svg` through one registry | ownership maps source assets and generated website copies to the canonical bare connector; ambiguous/orphan/duplicate paths reject |
| Catalog allowance handoff | PR #3590 still needs exact catalog allowances | handoff records exact `docs/connectors/catalog/all-connectors.json` and `.md` allowance for PR #3590 reconciliation |
| Review F1 canonical coverage | Missing registry rows can be masked by consumer-side fallback | constructed registries reject missing explicit rows and metadata consumers use canonical rows only |
| Review F2 source URL collision | Same ID/path with different source URLs collapses by iteration order | conflicting source URLs reject while identical source URL fixtures still collapse |
| Review F3 generated icon reconciliation | Deleted or renamed canonical icons leave stale website SVGs | a two-generation path-change fixture proves output-only SVGs are removed inside the bounded generated tree |
| Review F4 slash path portability | host `filepath` semantics can reject portable registry paths | nested forward-slash registry paths validate through slash-oriented `path` semantics |
| Review F5 generated subtree containment | `icons/../outside.svg` passes syntax validation and resolves above the generated icon root | non-clean, dot-segment, absolute, and escaped paths reject before any generated-tree mutation |
| Review F6 generated docs metadata | catalog, MANUAL, and SKILL icon paths can disagree with the canonical registry without failing docs validation | all three generated surfaces validate canonical paths; the exact 61 stale mappings are regenerated |
| Review F7 complete rendered metadata | matching paths mask missing review URLs and obsolete provenance in generated catalog, MANUAL, and SKILL icon blocks | serialized catalog icon objects and exact rendered guide blocks must equal their canonical projections, including optional-field omission |
| Review F8 full canonical projection | the runtime/docs projection drops ID from guides and drops title, slug, hex, license/attribution, and match metadata from every generated surface | one projection renders and validates all canonical icon metadata while absent optional fields remain omitted |
| Review F9 generated website propagation | canonical bundle icon updates leave downstream catalog/list objects and output-only SVG copies stale | generated catalog/list icon values match the bundle, the public icon tree exactly matches canonical paths, non-icon values remain byte-equivalent, and a second generation is clean |
| Review F10 exact unique icon section | first-substring extraction accepts `FAVICON`, embedded heading fragments, and duplicate icon sections | only one exact line-boundary heading is accepted; missing and duplicate headings fail deterministically |
| Review F11 heading/spacing separation | skill duplicate detection ignores an exact heading when it lacks the canonical following blank line | every exact heading line counts toward uniqueness independently of the section separator |
| Review F12 complete registry path validation | invalid paths on unimplemented registry rows are filtered out before validation | every registry row is validated before implementation mapping or icon-path collection |
| Review F13 duplicate curated ownership | duplicate curated connector keys overwrite by iteration order | duplicate curated keys reject before precedence merge |
| Review F14 shared asset path ownership | different source URLs for one generated asset path overwrite by iteration order | shared asset paths require one identical source URL identity before asset construction |
| Review F15 curated key silent drop | an empty or legacy-prefixed curated connector key is silently filtered out and backfilled from upstream/fallback data, destroying hand-curated review status, review URL, and source attribution while reporting success | `loadCuratedIconEntries` rejects empty and `source-`/`destination-`-prefixed curated keys with an error naming the key and the curated file; prefixed raw upstream records still collapse to bare identifiers unaffected |
| CodeQL fetch-simple-icons input validation | `icon.slug` reaches `fetch()` and `icon.path` reaches `resolve`/`mkdirSync`/`writeFileSync` in `fetch-simple-icons.mjs` with no validation adjacent to either sink | `resolveSimpleIconRequest` rejects an invalid slug (empty, containing `/`, containing a scheme) or an escaping path (`../`-traversal, absolute) and throws before returning a URL or write path; valid bare slugs and in-tree paths resolve unchanged |
| Review F16 in-tree non-icon write destination | containment alone constrains only *where* the write lands, so an in-tree but non-icon registry path (`index.md`, `icons/github.svg`) still resolves to a writable destination, and a non-string path fails inside `resolve` with an opaque `TypeError` instead of a named validation error | `resolveSimpleIconRequest` also enforces the `icons/simple-icons/<name>.svg` shape after containment and rejects a non-string path, so only the Simple Icons subtree is writable |
| Review F17 repeat icon-coverage validation | layered constructors each re-scan the whole registry through `MustValidateIconCoverage`, and memoizing it would mask a connector registered after validation | validation is memoized on the registry and `Register` clears the memo, so repeat calls are free but post-registration drift still aborts |
| CodeQL alert #93 fetched-content integrity | the fetched SVG response body flows to `writeFileSync` with no validation of the content itself, only of the destination path | `verifyFetchedIconDigest` checksum-verifies fetched content against a per-connector SHA-256 lockfile entry and throws before returning on a missing entry or mismatch; connectors sharing one icon each verify independently and duplicate digests are expected, not deduped |

## Actual evidence log

- 2026-08-02: GSD adapter checked with `scripts/gsd doctor` in isolated worktree.
- 2026-08-02: Required command probe `scripts/gsd prompt programming-loop init --phase connector-guardrail-remediation-r1/workers/issue-3595 --dry-run` failed with `unknown GSD command: programming-loop`; manual GSD fallback uses `.pi/prompts/pm-gsd-loop.md` and records `local_critical_path` because subagents are prohibited for this worker.
- 2026-08-02: GSD trace generated with `scripts/gsd prompt execute-phase connector-guardrail-remediation-r1 --dry-run` in the planning scaffold.
- 2026-08-02: Planning-only scaffold created before any production edits.
- 2026-08-02: Pre-edit audit evidence captured: `internal/connectors/icon_data.json` has 651 entries (590 `source-*`, 56 `destination-*`, 5 bare); 22 base identifiers currently collapse across multiple keys; non-identical collapses include `gcs`, `mssql`, and `file`; `website/data/icon_overrides.json` has 61 curated Simple Icons overrides; `docs/connectors/icons/**` has no Simple Icons while `website/public/connectors/icons/simple-icons/**` has 53 website-only SVG assets.
- 2026-08-02: Red tests added before production code: Go runtime exact bare lookup rejects legacy prefixed fallback; icon registry builder rejects unreviewed source/destination collisions and prefixed emitted keys; website generator/fetcher reject `website/data/icon_overrides.json` authority; ownership/icon path helpers reject ambiguous, orphaned, duplicate, and undeclared canonical/generated icon paths.
- 2026-08-02: RED `go test ./internal/connectors ./cmd/iconregistrygen` failed as expected before implementation: missing `IconSourceSimpleIcons`, `ConnectorIconOwnerForPath`, `ValidateConnectorIconOwnershipPaths`, and `buildOptions`; current generator signature cannot satisfy bare/collision tests.
- 2026-08-02: RED `node --test website/scripts/icon-registry.test.mjs` failed as expected before implementation: `destination-astra` prefixed registry key, `website/data/icon_overrides.json` still exists, and website scripts still reference `icon_overrides`/prefix stripping.
- 2026-08-02: GREEN `go test ./internal/connectors ./cmd/iconregistrygen` passed after implementation: exact bare lookup, Simple Icons mapping, collision rejection, and icon ownership helpers are covered.
- 2026-08-02: GREEN `node --test website/scripts/icon-registry.test.mjs` plus `node --check website/scripts/gen-connector-bundles.mjs` and `node --check website/scripts/fetch-simple-icons.mjs` passed after implementation: website consumers read the canonical registry only and source/generated icon assets exist under docs and website.
- 2026-08-02: Focused CLI docs generation exposed one necessary adjacent docs-generator path: `internal/cli/connector_docs.go` copied only top-level SVGs, so nested canonical `icons/simple-icons/**` assets were missing from temp docs. Implemented recursive SVG copy and verified with `go test ./internal/cli ./cmd/pm` and `make verify`.
- 2026-08-02: Review round 2 rechecked the repo-local GSD adapter; `scripts/gsd doctor` passed and the required `programming-loop` probe remained unavailable, so the recorded manual GSD fallback continues.
- 2026-08-02: Pre-edit F5 proof confirmed `validConnectorIconPath("icons/../outside.svg")` accepts an escaped path and output resolution is anchored at `website/public/connectors`, above the authorized icon subtree.
- 2026-08-02: Pre-edit F6 audit compared catalog, MANUAL, and SKILL path fields with `internal/connectors/icon_data.json` and found the same exact 61 stale connector mappings on every surface.
- 2026-08-02: Review round 3 rechecked the repo-local GSD adapter; `scripts/gsd doctor` passed and the required `programming-loop` probe remained unavailable, so the recorded manual GSD fallback continues.
- 2026-08-02: Pre-edit F7 audit reproduced 220 catalog JSON icon-object mismatches plus 216 MANUAL and 216 SKILL rendered-block mismatches against the canonical registry projection; `100ms` lacks its review URL and `convex` retains obsolete provenance despite matching paths.
- 2026-08-02: Review round 4 rechecked `scripts/gsd doctor`; the adapter passed while the required `programming-loop` command remained unavailable, so the recorded manual GSD fallback and `local_critical_path` execution continue.
- 2026-08-02: Pre-edit F8 audit confirmed all 554 catalog icon objects use the reduced five-field `ConnectorIcon`, all 554 registry-backed MANUAL/SKILL icon blocks omit icon IDs, and all 61 curated Simple Icons entries lose title, slug, hex, license, match, and matched-by metadata before rendering.
- 2026-08-02: F8 regression edits precede production edits; the active review-fix contract permits only one focused verification after the complete fix round, so no interim RED command is run.
- 2026-08-02: The first focused F8 gate exposed two stale path-corruption fixtures whose context still assumed `asset` was the first rendered icon line; the fixtures were narrowed to mutate only the asset field before repeating the focused gate.
- 2026-08-02: Review round 5 rechecked `scripts/gsd doctor`; the adapter passed while `scripts/gsd prompt programming-loop init --phase connector-guardrail-remediation-r1/workers/issue-3595 --dry-run` remained unavailable, so the recorded manual GSD fallback and `local_critical_path` execution continue.
- 2026-08-02: Pre-edit F9 audit reproduced exactly 230 stale icon objects in `website/lib/connectors.catalog.data.generated.json`, 14 stale icons in `website/lib/connectors.generated.ts`, and 61 output-only SVGs under `website/public/connectors/icons/**`; the canonical website bundle itself matches its generator projection.
- 2026-08-02: Pre-edit F10 inspection confirmed `strings.Index(document, heading)` selects an `ICON` substring inside a line such as `FAVICON` and accepts the first of duplicate exact sections.
- 2026-08-02: F10 regression edits precede production edits; the active review-fix contract permits only one focused verification after the complete fix round, so no interim RED command is run.
- 2026-08-02: Review round 6 rechecked `scripts/gsd doctor`; the adapter passed while `scripts/gsd prompt programming-loop init --phase connector-guardrail-remediation-r1/workers/issue-3595 --dry-run` remained unavailable, so the recorded manual GSD fallback and `local_critical_path` execution continue.
- 2026-08-02: Pre-edit inspection reproduced F11-F14: skill heading counting depends on `\n\n`, registry-only paths are silently filtered, curated keys overwrite in a map, and asset paths overwrite source URLs in a map.
- 2026-08-02: F11-F14 regression edits precede production edits; the active review-fix contract permits only one focused verification after the complete fix round, so no interim RED command is run.
- 2026-08-02: GREEN focused Go tests passed for exact heading-line uniqueness, duplicate curated-key rejection, shared-path source-URL conflict rejection, and identical shared-asset acceptance.
- 2026-08-02: GREEN Node icon-registry tests passed, including invalid unimplemented-row rejection; two consecutive bundle generations produced identical data/public-icon hashes and no derived-output diff.
- 2026-08-03: Pre-edit F15 audit confirmed `loadCuratedIconEntries` silently `continue`d past any curated row with an empty or `source-`/`destination-`-prefixed connector key, and that the `--curated` flag defaults to `--out`, so regenerating against a legacy (non-bare) registry would discard those curated rows and refill them from upstream/fallback data while `iconregistrygen` reported success.
- 2026-08-03: RED `go test ./cmd/iconregistrygen -run TestLoadCuratedIconEntries` failed as expected before implementation: empty-key and `source-`/`destination-`-prefixed curated keys were silently accepted (no error) instead of rejected.
- 2026-08-03: F15 fix implemented: `loadCuratedIconEntries` now returns an error naming the offending connector key and the curated registry file path for empty or non-bare curated keys, matching the existing hard-error conventions at connector metadata name validation and duplicate curated key rejection; prefix collapse in `buildIconEntry`/`canonicalConnectorKey` for raw upstream Docker slugs is untouched.
- 2026-08-03: GREEN `go test ./cmd/iconregistrygen -v` passed: empty curated key rejected, `source-`-prefixed curated key rejected, `destination-`-prefixed curated key rejected, valid bare curated key still loads and preserves review URL, and a `destination-`-prefixed raw upstream record still collapses to a bare identifier.
- 2026-08-03: Confirmed the committed `internal/connectors/icon_data.json` (554 entries) contains zero empty/prefixed curated keys, so the stricter F15 check does not regress the current default `--curated`-equals-`--out` regeneration path.
- 2026-08-03: Diagnosed the PR #3596 `Website checks` CI failure (step "Verify generated website data") as run against stale commit `10829e569` (the planning-scaffold-only push), predating the six pipeline `no-mistakes(review)` commits already reconciling website icon data; reproduced with pnpm 11.7.0 (matching CI's `PNPM_VERSION`) at pipeline head `8faa94cc6`: `pnpm run gen:website-data` produced zero diff, and `pnpm run lint`, `pnpm run typecheck`, `pnpm run test:unit` (76/76), and `pnpm run build` all passed locally.
- 2026-08-03: Two CodeQL alerts on PR #3596 (js/request-forgery-like at fetch-simple-icons.mjs:62, js/path-injection-like at :74) required captain-decided `resolveSimpleIconRequest` boundary validation for the registry-authored `simple_icon_slug` (URL) and `path` (filesystem write destination) inputs; decision key `pr3596-codeql-fetch-simple-icons-validation-20260803`.
- 2026-08-03: RED `node --test scripts/icon-registry.test.mjs` (from `website/`) failed as expected before implementation: `Cannot find module '.../website/scripts/lib/simple-icons.mjs'` — the test suite imported `resolveSimpleIconRequest`/`validSimpleIconSlug`, which did not exist yet, and the added source-text assertion (`fetchScript` must match `/resolveSimpleIconRequest/`) also failed against the pre-fix script.
- 2026-08-03: Implemented `website/scripts/lib/simple-icons.mjs` exporting `validSimpleIconSlug` (lowercase-alphanumeric-only, matching every real `simple_icon_slug` already in `internal/connectors/icon_data.json`) and `resolveSimpleIconRequest(docsConnectorsRoot, icon)`, which throws before returning either the CDN URL or the write path on an invalid slug or a path that resolves outside `docsConnectorsRoot` (reusing the existing `assertInside` containment check from `website/scripts/lib/connector-icons.mjs`, now exported for reuse). Rewired `fetch-simple-icons.mjs`'s fetch loop to call `resolveSimpleIconRequest` and use only its returned `url`/`outputPath` before `fetch`/`mkdirSync`/`writeFileSync`; tightened the pre-existing collection-loop slug guard to the same `validSimpleIconSlug` predicate. The pre-existing `validIconPath` shape guard and SVG payload checks (`<svg` prefix, `<script` rejection) were unchanged at this point; the shape guard was later folded into the shared library by review round 9 below.
- 2026-08-03: GREEN `node --test scripts/icon-registry.test.mjs` (equivalently `pnpm run test:scripts`, the suite wired into `.github/workflows/website.yml` this branch) passed 8/8: a `../`-traversing path is rejected before anything is written, a nested `../` path is rejected, an absolute path (`/etc/passwd`) is rejected, a slug containing `/` is rejected before any fetch, a slug containing a scheme (`https://...`) is rejected before any fetch, an empty slug is rejected, and a valid bare slug with an in-tree path resolves to the unchanged expected URL/path. Each throw case is proved to happen strictly before a URL or write path is ever produced, by construction (the function throws before returning either value; the test never calls `fetch` or performs any write).
- 2026-08-03: GREEN `pnpm run lint`, `pnpm run typecheck`, `pnpm run gen:website-data` (zero diff, deterministic) all passed after the fix, confirming no regression to the unrelated generated website data this same branch already reconciled.
- 2026-08-03: Pre-edit F16/F17 inspection of the round-8 fix: `resolveSimpleIconRequest` returned any destination contained by `docsConnectorsRoot`, so registry paths such as `index.md` and `icons/github.svg` still resolved to writable non-Simple-Icons destinations, and a non-string `path` failed inside `resolve` with an opaque `TypeError` instead of a named validation error; `MustValidateIconCoverage` re-scanned every registry row on each layered constructor call.
- 2026-08-03: F16/F17 regression edits precede production edits; the active review-fix contract permits only one focused verification after the complete fix round, so no interim RED command is run. Node coverage extends the existing `Simple Icons request resolution enforces the CodeQL-flagged input boundaries` case with in-tree non-icon (`index.md`), in-tree curated-icon (`icons/github.svg`), and missing-path rejections; Go coverage adds `TestMustValidateIconCoverageRevalidatesAfterRegistration`, which requires a panic when a connector with no registry row is registered after a successful validation.
- 2026-08-03: Review round 9 fix implemented in commit `4b182b1ba`: `website/scripts/lib/simple-icons.mjs` now also exports `validSimpleIconPath`, and `resolveSimpleIconRequest` rejects a non-string path before `resolve` and enforces the `icons/simple-icons/<name>.svg` shape after `assertInside`. `fetch-simple-icons.mjs` dropped its local `validIconPath` copy in favor of that shared predicate for the collection-loop guard and now prefixes a resolver failure with the connector name; `gen-connector-bundles.mjs` dropped its duplicate `assertInside` in favor of the shared export from `website/scripts/lib/connector-icons.mjs`. `Registry` memoizes icon-coverage validation in `iconCoverageValidated`, which `Register` clears so a connector registered after validation still aborts. The SVG payload checks (`<svg` prefix, `<script` rejection) and the duplicate-path guard are unchanged.
- 2026-08-03: Review round 9 focused gate commands are listed under "Review repair round 9" in `VERIFICATION.md`; their results are owned by the enclosing no-mistakes validation run for PR #3596 and are not restated here.
- 2026-08-03: CodeQL alert #93 (`js/http-to-file-access`, "Network data written to file") opened at `fetch-simple-icons.mjs:83` after review round 9's refactor moved the sink line. Traced its actual taint source via the GitHub API (`message.markdown` cited `L72C26-L72C36`, the `fetch(url)` response): the flagged dataflow is the *fetched SVG response body* (`svg`, from `response.text()`) reaching `writeFileSync`, not `icon.path` — a dataflow neither the F15/round-8/round-9 fixes touched, since all three addressed registry-derived `slug`/`path` validation, not fetched-content validation. Decision key `pr3596-codeql-93-svg-content-checksum-20260803`: checksum-pin the fetched content, lockfile keyed by connector so connectors sharing one icon (confirmed for real in the registry: `ebay` shared by `ebay-finance`/`ebay-fulfillment`; `zoho` shared by 8 Zoho product connectors) each get their own entry.
- 2026-08-03: RED `node --test scripts/icon-registry.test.mjs` (from `website/`) failed as expected before implementation: `SyntaxError: The requested module './lib/simple-icons.mjs' does not provide an export named 'readSimpleIconsLockfile'` — the test suite imported `readSimpleIconsLockfile`/`sha256Hex`/`verifyFetchedIconDigest`/`writeSimpleIconsLockfile`, none of which existed yet.
- 2026-08-03: Implemented `website/scripts/lib/simple-icons.mjs` additions: `sha256Hex(content)` (SHA-256 hex digest); `verifyFetchedIconDigest(lockfile, icon, content)`, which throws naming the connector, expected digest, and received digest on a missing lockfile entry or a hash mismatch, before returning — never returns successfully on a bad input, so the caller cannot fall through to `mkdirSync`/`writeFileSync`; `readSimpleIconsLockfile`/`writeSimpleIconsLockfile`, reading a missing lockfile as `{}` (not an error) and writing entries sorted by connector key for a stable, reviewable diff. Wired `fetch-simple-icons.mjs`: added `--update-lockfile` (a deliberate, documented regeneration mode — `node scripts/fetch-simple-icons.mjs --update-lockfile` — not hand-editing) that records a fresh digest per fetched connector instead of verifying; default mode calls `verifyFetchedIconDigest` immediately after the existing `<svg`/`<script` payload checks and before `mkdirSync`/`writeFileSync`, in a try/catch that names the connector and rethrows, matching the statically-terminating style already hardened for the slug/path guard. New lockfile: `website/data/simple-icons.lock.json`.
- 2026-08-03: GREEN `node --test scripts/icon-registry.test.mjs`: 14/14, including matching-content acceptance, tampered-content rejection (proved by construction: `verifyFetchedIconDigest` throws before returning, so the real script's `mkdirSync`/`writeFileSync` are structurally unreachable on that path), missing-lockfile-entry rejection, independent per-connector verification for two connectors sharing one digest (one stale entry fails only that connector, the sibling still passes), and sorted round-trip persistence. Added a source-text assertion that `fetch-simple-icons.mjs` references `verifyFetchedIconDigest`.
- 2026-08-03: Ran the real regeneration end-to-end against the live Simple Icons CDN (network access confirmed: `curl -sS https://cdn.simpleicons.org/github` → 200): `node scripts/fetch-simple-icons.mjs --update-lockfile` fetched all 61 real Simple Icons SVGs and wrote `website/data/simple-icons.lock.json` with 61 connector entries, confirmed duplicate digests present exactly as expected for `ebay-finance`/`ebay-fulfillment` (shared `ebay` slug) and all 8 `zoho-*` connectors (shared `zoho` slug) — not deduped. A second real run in default (verify) mode against that lockfile passed 61/61 with zero deviation.
- 2026-08-03: End-to-end tamper proof against the real filesystem: recorded a real SVG's MD5 as a sentinel, corrupted its lockfile entry's `sha256` field, reran the real script — it exited 1 naming the connector, the expected digest, and the received digest, and the sentinel file's MD5 was unchanged afterward (`FILE_UNCHANGED_AS_EXPECTED`), proving rejection happens before any write reaches disk, not just in the unit test.
- 2026-08-03: GREEN `pnpm run lint`, `pnpm run typecheck`, `pnpm run gen:website-data` (zero diff): pass after the fix. `git status` after the real regeneration shows only the new lockfile and the code changes — every re-fetched SVG under `docs/connectors/icons/simple-icons/**` and its `website/public/connectors/icons/simple-icons/**` copy is byte-identical to what was already committed, proving the happy path is unchanged.
