# Verification Contract

Targeted gates are selected per sub-issue and must include Asana connector validation, relevant Go
tests, CLI help/docs checks, and `git diff --check`. Parent completion additionally requires the full
commands in PLAN.md. Every result records command, exit code, UTC time, and head SHA.

Current status: planning only; no implementation gate has been claimed.

