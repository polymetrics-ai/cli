# TDD ledger — Stripe provider-dialect tolerance foundation

| Slice | Red | Green | Refactor / safety proof |
| --- | --- | --- | --- |
| Source-cited Stripe GET/DELETE identities | **Red (2026-08-27):** `go test -timeout 20m ./cmd/connectorgen -run '^(TestSourceImportStripeReferenceDepthOperationLocal|TestSourceImportRejectsUnsafeReferences)$' -count=1` exited 1 before the resolver change: `preflight source grammar: path item "/v1/accounts/{account}": get operation: response "200": reference depth limit exceeded`. The connector-wide preflight prevented the retained `GetAccount` GET and `DeleteAccountsAccount` DELETE descriptors from being emitted. | **Green:** the same focused command exits 0. The two unaffected descriptors preserve their operation IDs, method/path, source URL, and operation location, and the nested Stripe GET resolves its exact string schema when within the configured depth. | The fixture is hermetic and preserves evidence fields from the retained lock only; it does not fetch or store credentials. |
| Nested `$ref` depth is operation-local | **Red:** the command above demonstrates that one nested response traversal aborted all three source operations at source-document preflight time. | **Green:** bounded `GetAccountsAccount` is retained with `cli-source-descriptor-foundation-r1`, exact source location, a merge-blocking gap, and empty responses/output success rather than fabricated fields; its unaffected siblings remain complete. | A typed depth error is the sole conversion path; limits remain bounded and the descriptor's gap is tested as source-projection blocking. |
| Unsafe references remain hard failures | **Red:** the pre-change global preflight did not distinguish a retainable depth exhaustion from unsafe reference failures at the exact operation boundary. | **Green:** the focused suite rejects external, malformed, ambiguous, cyclic, and reference-count-exhausting inputs; `TestSourceReferenceResolverCachesNormalizedTargetsWithoutBypassingCounts` proves canonical target reuse still counts both traversals. Existing changed-package bounds coverage retains structural/byte budget checks. | Normalization and pointer-target memoization are source-document-local; reference counting, target-kind validation, cycle detection, and expansion reservations run before any cached target is used. |
| No materialized command claim | **Red:** a retained incomplete descriptor had no dedicated `source_descriptor` foundation condition. | **Green:** the nested gap is asserted by `sourceProjectionHasBlockingGap`; no Stripe bundle, action, CLI binding, credential path, or provider I/O is added. | No `pm stripe` command is added or exercised with credentials. |
| CI repair: current-main safety preflight remains bounded | **Red (2026-08-27, after merging `origin/main@cf29d302c`):** `go test -timeout 20m ./cmd/connectorgen -count=1` exited 1 after 172.636s. Its retained source-grammar preflight rejected operation-local reference depth before descriptors were emitted; the prior incompatible repair also changed request-media/inbound/resource error ownership. | **Green (2026-08-27):** `go test -timeout 20m ./cmd/connectorgen -run '^(TestSourceImportPreflightsUnusedGrammarObjects|TestSourceImportDoesNotTreatDepthAsDocumentSafetyExemption|TestSourceImportReservesRequestMediaExpansionBeforeCloning|TestSourceImportReservesInboundExpansionBeforeEventConstruction|TestSourceImportReservesCallbackReferenceChainsBeforeCloning|TestSourceImportProviderDialectContracts|TestSourceImportReservesResolvedResponseChildrenBeforeCloning|TestSourceImportRejectsUnsafeOrRetainsMalformedSourceForms|TestSourceReferenceResolverCachesNormalizedTargetsWithoutBypassingCounts)$' -count=1` exits 0 (`ok polymetrics.ai/cmd/connectorgen 1.215s`), and the clean full package suite exits 0 (`ok polymetrics.ai/cmd/connectorgen 176.351s`). | Current-main's full grammar preflight remains authoritative for malformed, external, dynamic, cyclic, ambiguous, and resource-exhausting input. Only typed finite depth errors in a lock that permits source-contract gaps bypass document-wide refusal; a later unsafe component is still rejected. |
| Audit repair: exact retained Stripe corpus | **Red (2026-08-27):** the audit found no test exercising the immutable 7,967,776-byte Stripe artifact or the full 589-entry source lock; current `main` had no checked-in Stripe `sources/` evidence, so the synthetic three-operation fixture could not prove complete corpus retention. | **Green:** `TestSourceImportRetainedStripeCorpus` uses the connector-owned retained-artifact reader and asserts the artifact byte count/digest-bound lock, all 589 unique source IDs, known GET/DELETE identities, and one actual response-reference depth gap bound to every exact locked source operation. `TestSourceImportLockReferenceDepthLimitOnlyNarrows` proves the lock-local cap cannot enlarge the invocation safety budget. | The fixture is live repository evidence from Batch 1 retention, never a network fetch; it only reads a content-addressed source file under `internal/connectors/defs/stripe/sources/`. |
| Audit repair: unused over-depth source data is closed | **Red (2026-08-27):** `preflightOpenAPIComponents` deliberately continued after a retainable depth error, so an unreferenced component could disappear after import with neither error nor source-cited disposition. | **Green:** `TestSourceImportRetainsUnusedDepthDisposition` asserts a normal source operation remains complete and the unreferenced over-depth reference is retained as a deterministic source-document gap (or is rejected if unsafe); it cannot vanish. | Only the typed finite-depth condition receives this disposition. External, malformed, ambiguous, cyclic, and resource exhaustion errors still terminate import. |
| Audit repair: projection reaches registered preflight before I/O | **Red (2026-08-27):** the synthetic importer test stopped at a descriptor gap and never proved the generated source foundation survives projection, engine load/registry construction, and commandrunner preflight. | **Green:** `TestSourceProjectionRetainedStripeDepthGapStopsAtRegistryPreflight` projects one actual retained Stripe gap into a declaration-owned temporary bundle, registers the engine-backed connector through `connectors.NewEmptyRegistry`, and asserts the exact `missing_foundation=cli-source-descriptor-foundation-r1` refusal with zero credentials and no executor/provider dispatch. | The test exercises a blocked declaration before any credential or I/O boundary; it neither adds an executable Stripe route nor resolves a credential. |
