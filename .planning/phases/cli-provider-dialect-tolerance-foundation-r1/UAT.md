# UAT — Stripe provider-dialect tolerance foundation

Automated coverage passed the delivered foundation behaviors:

1. The exact retained 7,967,776-byte Stripe artifact imports all 589 locked
   operations with unique source identity, citation URL/location, method, and
   path. `GetAccount` and `DeleteAccountsAccount` remain visible as exact GET
   and DELETE source rows.
2. The lock-local reference cap converts each exact unresolved Stripe response
   chain into a merge-blocking `cli-source-descriptor-foundation-r1` condition
   on that source operation. The descriptor contains no invented response or
   output contract. The synthetic counterpart proves a complete descriptor is
   retained whenever its exact bounded source contract resolves.
3. An unused over-depth component yields a deterministic source-cited document
   gap. Hidden external, dynamic, cyclic, malformed, ambiguous, and resource
   failures remain terminal rather than becoming an operation-local escape.
4. A real retained Stripe condition survives projection through the registry
   and stops at `commandrunner.Preflight` with the exact missing-foundation
   marker before credentials, executor dispatch, or provider I/O.

No human visual or runtime UAT is applicable. This foundation adds no
user-facing Stripe command and therefore has zero credential-bound runnable
commands. A later connector materialization must supply the exact action/CLI
binding before `error: missing --credential` can be claimed.
