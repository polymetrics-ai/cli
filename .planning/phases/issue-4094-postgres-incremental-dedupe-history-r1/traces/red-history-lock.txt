Command:

    go test -timeout 20m -count=1 ./internal/connectors/native/postgres -run 'TestPostgresHistoryModeRequiresManagedTableLock'

Result before implementation:

    --- FAIL: TestPostgresHistoryModeRequiresManagedTableLock
        incremental_dedupe_history did not require the managed-table lock

The existing PostgreSQL driver had no transaction/table-lock admission for
history writes, so it could not atomically close a current validity window and
publish the next version.
