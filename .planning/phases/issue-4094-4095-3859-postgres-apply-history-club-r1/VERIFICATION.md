# Verification — PostgreSQL apply/history club

Status: pending execution.

The final verification must fill every evidence-table row from `CONTEXT.md`,
record the live PostgreSQL observable state, distinguish the known #4158 base
failure, and list every command result. A green exit without row/receipt state
is not accepted as evidence.
