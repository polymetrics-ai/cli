# Discussion log — Zoom continuation

Inline/manual `discuss-phase` record. The captain's relaunch directive, the task contract, and
#4304 decide every material behavior; no unresolved product choice remains.

- The former definition-composition gap is resolved by the exact temporary #4304 stacked base
  (`d814875a902be684cb2a38b94f7a8077f66b70b1`). The captain separately approved an isolated
  rehearsal of Foundation SHA `c3f83cbf6eabbae00219566fb02719ca2d6c480d` for the optional
  record-query behavior. It passed without changing connector ancestry, but final application-level
  deployability and merge readiness remain gated on Foundation landing on `main`.
- The destination is narrow and declaration-selected. It is not a generic writer and does not
  authorize a provider mutation.
- The required users `id` → normalized action `user_id` mapping (interpolated into the provider's
  `{userId}` path) is the only exact existing source/action field pairing. Other actions remain
  commands but are not fabricated as transports.
- The seven-surface report distinguishes implementation, automated live safety, fixture proof, and
  certification. A lack of live proof never makes a faithful command unreachable.
