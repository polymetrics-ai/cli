# TDD ledger — DB → API PostgreSQL → GitHub route R1

## Execution evidence

- **Red:** `go test -timeout 20m -run '^TestOpenSelectsPostgresIssueLabelDestinationTransport$' ./internal/app` initially failed: the existing dispatch retained a same-connector gate and did not select the valid PostgreSQL-to-GitHub transport for either eligible mode.
- **Green:** dispatch now asks the destination descriptor for an exact admitted source binding, maps rows through that declaration-owned contract, and both the deterministic container test and the separately opt-in real-GitHub proof pass through a fresh `pm` binary.
- **Manual GSD fallback:** the task runner cannot provide the canonical compatible isolated workers and the single-worker contract forbids role spawning. `discuss-phase`, `plan-phase --tdd`, `execute-phase`, `verify-work`, and `code-review` were resolved with `scripts/gsd prompt` and completed inline in this phase record.

| ID | Class | Behavior and observable assertion | Red | Green |
| --- | --- | --- | --- | --- |
| R1 | Happy / simulated binary | A PostgreSQL polling-watermark row crosses the warehouse and makes `pm` issue an exact row-derived `add_issue_labels` request. | `go test -timeout 20m -run '^TestOpenSelectsPostgresIssueLabelDestinationTransport$' ./internal/app` failed both declared cases: `shouldRunTransport` rejected the preflight-valid PostgreSQL→GitHub route at the old same-definition gate. | `TestOpenSelectsPostgresIssueLabelDestinationTransport` now selects the closed route. The Docker-PostgreSQL + `httptest` GitHub binary proof asserts exact `pm-db-api-add` labels, durable receipt artifacts, acknowledgement GET, and checkpoint. |
| R2 | Happy / live binary | The PostgreSQL row shape drives declaration-owned `set_issue_labels` in keyed `incremental_upsert`, and a rerun preserves the exact replacement label set. | Route selection red from R1 covered both `full_append` and `incremental_upsert`. | `TestPMBinaryExecutesLivePostgresWarehouseGitHubIssueLabels` ran against real GitHub: retained issue #2 independently read as exactly `[pm-db-api-live-set]` after the original apply and keyed replay. |
| R3 | Bad / unit + simulated boundary | Unsupported action, ineligible mode, and a malformed PostgreSQL row return typed errors before provider write I/O. | Tests were added before mapper implementation. | `TestPostgresIssueLabelTransportRefusesBadInputsBeforeProviderWrite` asserts `IssueLabelTransportUnsupportedActionError`, `*synccontract.ModeNotExecutableError`, `IssueLabelTransportRowMappingError`, and `IssueLabelTransportDeletesUnavailableError`, each with zero fake writes. |
| R4 | Edge / simulated boundary | Zero rows, `NULL` mapped columns, keyed replay, interruption/watermark resume, and `deletes: not_available` preserve their named provider/checkpoint invariant. | Edge cases were specified before the binary harness addition. | Docker PostgreSQL + faithful local GitHub test proves zero-row checkpoint, null-row pre-write refusal, keyed `PUT` replay without corruption, killed second page then resumed watermark, and app-level tombstone refusal. It is intentionally simulated at the GitHub HTTP boundary. |
| R5 | Live controlled provider | The shipped binary applies controlled PostgreSQL rows to controlled GitHub issues; a separate GitHub read returns the exact labels. | The first binary proof deliberately stopped at `httptest`; it was retained as deterministic coverage and not accepted as live proof. | With `POLYMETRICS_GITHUB_ISSUE_LABEL_LIVE_PROOF=1`, a Colima Docker PostgreSQL source and real GitHub HTTPS produced issue #1 exactly `[pm-db-api-live-add]` and issue #2 exactly `[pm-db-api-live-set]` in retained `karthik-sivadas/pm-parity-proof-db-to-api`; a fresh authenticated labels API client asserted each value. |
| R6 | Bad / definition admission | An unlisted source executor is refused by the destination transport definition before any source read, warehouse staging, plan, apply, or checkpoint side effect. | `go test -timeout 20m -run '^TestPreflightReturnsTypedDestinationSourceIneligibleErrorBeforeExecutorAccess$' ./internal/synctransport` initially failed to compile because `DestinationSourceBinding`, `SourceRecordMapping`, and `DestinationSourceIneligibleError` did not exist. | `source_bindings` now names exact executor/stream/mapping contracts in the destination definition. The green test receives typed `DestinationSourceIneligibleError` and asserts zero source, warehouse, destination, and checkpoint calls; the existing PostgreSQL binary test proves its `input_fields` binding reaches the two declared label actions. |
