# Run state — DB → API PostgreSQL → GitHub route R1

- `discuss-phase`: complete inline. The definition-owned mapping and proof boundary were fixed before code.
- `plan-phase --tdd`: complete inline. `PLAN.md` and `TDD-LEDGER.md` carry the red/green contract.
- `execute-phase`: complete inline. Dispatch, declaration-derived mapper, approval binding, binary proof, docs, and generated transcript are implemented.
- `verify-work`: complete inline. `VERIFICATION.md` records the real GitHub proof, deterministic test boundary, and each local gate.
- `code-review`: complete inline. The diff stays in the DB→API quadrant and continues to invoke only `add_issue_labels` and `set_issue_labels` on the live destination path.

Manual fallback reason: this runner has no compatible isolated canonical Pi worker runtime, and the project single-worker contract forbids role spawning. The resolved `scripts/gsd prompt` lifecycle was therefore executed and evidenced inline.
