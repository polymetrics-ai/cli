# Observability

Status distinguishes plan observation, planning, issue materialization, plan publication, AgentSession
verification, command failure, and parent readiness. Evidence records stable digests and bounded summaries,
not prompts containing secrets or raw command output. A missing required command is an explicit failure.
