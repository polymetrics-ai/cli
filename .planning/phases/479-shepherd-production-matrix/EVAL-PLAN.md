# Evaluation plan

Deterministic fixtures score: plan bootstrapped from issue facts, exact returned child numbers, zero calls
for existing plans, fail-closed malformed/ambiguous facts, exact AgentSession model/thinking route, every
planned verification ID backed by host evidence, real Go test execution, cancellation/join, and equal-second
ready reconciliation. Release requires all focused cases plus the complete Shepherd regression suite.
