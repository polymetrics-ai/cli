# Release notes

Production Shepherd now creates its execution plan from an existing GitHub issue when no plan file exists,
delegates immutable verification selection to a Pi AgentSession while retaining host execution authority,
gives implementation/correction sessions the same ID-only RED→GREEN test capability, supports trusted-local
Go/Node/Make quality-gate verification, and recovers GitHub ready changes that occur within the same timestamp second.
