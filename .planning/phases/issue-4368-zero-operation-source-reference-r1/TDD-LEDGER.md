# TDD LEDGER — issue #4368 zero-operation source-reference foundation

| ID | Enforcement | RED evidence | GREEN evidence | Refactor/verification |
| --- | --- | --- | --- | --- |
| R1 | An intentionally empty rendered coverage document needs an explicit closed marker and all normal retained provenance/identity evidence. | `go test -timeout 20m ./cmd/connectorgen -run '^TestSourceImportV3RenderedReferenceCoverageOnlyRequiresExplicitClosedProof$' -count=1` failed before production edits: `json: unknown field "coverage_only"`. | The same test passes after `coverage_only` is admitted only on a rendered reference with an explicit empty `operations` array. | The full package then identified its pre-existing rendered-reference evidence-only sidecar; it now declares the same explicit marker and passes its unchanged projection assertion. |
| R2 | Missing, malformed, duplicate, unverified, accidental-empty, and mixed-invalid documents fail closed at the offending document/location. | The RED fixture begins with the same unknown-field refusal. | `TestSourceImportV3RenderedReferenceCoverageOnlyRequiresExplicitClosedProof`, `...RetainsAndVerifiesLockedBytes`, and `...RejectsMalformedAndDuplicateJSONMembers` cover absent inventory/manifest, malformed discriminator, duplicate JSON member/document ID, retained-byte tamper, accidental empty, and a mixed document list. | Validation happens before descriptor/projection; the retained reader rejects a changed artifact against its manifest and lock. |
| R3 | A valid coverage-only document is integrity-checked but emits no executable descriptor operation. | The initial unknown-field RED test could not parse it. | The valid fixture reads the hash-pinned retained artifact through the production manifest reader and returns zero operations/events; descriptor validation accepts exactly that empty result. | Deterministic ordering and invalid-peer refusal remain on the existing v3 validator path. |
| R4 | Amplitude 187, Dremio 49, Ashby 193, Workable 84, and HiBob 207 reconcile to 720 exact source-cited deferred rows. | Exact branch inspection shows 187 + 49 + 193 + 84 + 207 operations and 15 zero-operation documents, but no retained-artifact manifests/files. | Firstmate explicitly authorized a scoped foundation-only PR on 2026-08-27. The 720-row exercise stays an explicit source-evidence gap until the pre-pinned bytes and matching manifests are supplied; this PR does not fabricate them or substitute live provider responses. | Prove one source identity, citation, provider ID, lane, and `missing_foundation` disposition per row; no fabricated action. |
| R5 | Deferred registry commands return structured missing-foundation before credential/provider work, while untouched runnable commands retain missing-credential behavior. | Pending: commandrunner boundary test. | Pending. | Preflight uses the real registry and records zero transport/record/mutation witnesses. |
