# Refs #4303 — TDD ledger

| Slice | Red evidence | Green evidence | Refactor/verification | Status |
| --- | --- | --- | --- | --- |
| Generic synthetic destination | `go test -count=1 -timeout 20m ./internal/app -run '^TestDefinitionTransportFactoriesRunTypedDestinationFromDefinition$'` failed: declared executor `declarative_typed_destination` was not registered, before provider I/O | Same command passed after the definition-selected typed-action factory and action-schema validation landed. | `gofmt`; focused `internal/app` run passed. | green |
| Distinct evidence and fail-closed declarations | The second synthetic bundle could not be admitted while the exact executor was absent. | `TestDefinitionTransportFactoriesSelectDistinctTypedDestinationEvidence` proves both conformance references are collected by one exact factory; it also refuses missing approval during planning. `TestDefinitionTransportFactoriesRefuseTypedDestinationDeclarationsBeforeIO` rejects unknown executor, missing action, wrong mapping role, and destination `change_capture`. `TestDeclarativeTypedDestinationRefusesInvalidWorksetsBeforeProviderWrite` rejects an incomplete mapped action record and tombstone deletes with zero provider writes. | `go test -count=1 -timeout 20m ./internal/app -run '^Test(DefinitionTransportFactories(RunTypedDestinationFromDefinition|SelectDistinctTypedDestinationEvidence|RefuseTypedDestinationDeclarationsBeforeIO)|DeclarativeTypedDestinationRefusesInvalidWorksetsBeforeProviderWrite)$'` passed. | green |
| GitHub neutral-path parity | GitHub previously had a bespoke factory branch beside source evidence collection. | The common `declarativeDestinationAdapters` loop now collects definition-owned evidence and installs GitHub's exact typed issue-label adapter. `TestDefinitionTransportFactoriesSelectDeclaredEvidence` builds it from that shared loop. | `TestPMBinaryExecutesLivePostgresWarehouseGitHubIssueLabels` passed against the dedicated proof repository: add, set, keyed replay, independent API read-back, acknowledgement, and checkpoint proof all passed. | green |
| Documentation | The document did not name the reusable typed-destination executor or its action/mapping/acknowledgement boundary. | `docs/sync-transport-definition.md` names the executor and the mechanical declaration rules. | `make verify` passed, including `docs-check`, `connectorgen validate`, and `connectorgen surface-sync --check`. | green |
| Persisted multi-action selection | `go test -count=1 -timeout 20m ./internal/app -run '^TestPersistedConnectionSelectsDeclarativeTypedDestinationAction$'` initially failed to compile because `StreamConfig.DestinationAction`, multi-action descriptor lookup, and generic App plan/preview dispatch did not exist. The first completed run then failed at the engine gate: the structural plan evidence did not match the concrete prepared write. | `DestinationAction` is persisted and action-aware preflight resolves exactly one `(mode, action)`. `TestPersistedConnectionSelectsDeclarativeTypedDestinationAction` proves append/replace actions in one synthetic connector plus a third connector action all plan, preview, approve, execute, acknowledge, read back, checkpoint, and retain their own endpoint. It also proves missing, foreign, unlisted, and stale persisted selections fail before I/O. Durable authorization issues fresh typed evidence per reopened workset, so a destructive named action still passes the engine's exact prepared-write gate without accepting an arbitrary request. | `go test -count=1 -timeout 20m ./internal/connectors -run '^TestDestinationTransportDescriptorSelectsPersistedActionWithinMode$'`; `go test -count=1 -timeout 20m ./internal/app -run '^TestPersistedConnectionSelectsDeclarativeTypedDestinationAction$'`; `go test -count=1 -timeout 20m ./internal/cli -run '^(TestETLTransportBareAndLeafHelpAreContextual|TestDeclarativeTypedDestinationTransportRejectsCallerActionBeforeProjectIO|TestGoldenTranscripts)$'` — green. Full verification remains pending. | green |
| Exact selected action schema field spelling | **Red:** `go test -count=1 -timeout 20m ./internal/app -run '^TestDeclarativeTypedDestinationSourceBindingsUseExactSelectedActionSchemaFields$'` failed because `input_fields` rejected the schema-valid camelCase `targetId` before the selected action schema was inspected. | **Green:** selected-action preflight is now the sole field authority: raw non-empty names are matched byte-for-byte to the selected action's top-level schema properties, including provider-owned spellings such as `http`; required properties must all be mapped. Whitespace, malformed, unknown, cross-action, and undeclared names fail before I/O without normalization. `pm etl transport … --action` remains an unknown flag before project I/O. | Focused remediation command in `VERIFICATION.md` passed. | green |
| Complete persisted reverse result | **Red:** `go test -count=1 -timeout 20m ./internal/app -run '^TestPersistedConnectionSelectsDeclarativeTypedDestinationAction$'` failed to compile because the persisted App `Run` had no `DestinationResults` field. The revised `TestOperationDirectWriteHonorsDeclaredJSONAndNoneResponsePolicies` then failed because `output_policy: none` silently returned a nil response body. | **Green:** the selected typed action now carries its complete engine response into a durable acknowledgement, `synctransport.Result`, persisted App run, and the unmodified CLI `ETLRun` envelope. The provider-shaped synthetic response proves ordinary status/header/body fields, exact large numeric fields, nested rare fields, enterprise-tier fields, and credential-equal provider bytes survive verbatim. System-generated plans, logs, request diagnostics, and synthetic errors remain secret-taint-safe. Regular reverse runs persist `destination_result`; operation-direct-write results retain their complete provider response. `output_policy` now controls parsing only, so it cannot suppress a successful ordinary result. | `go test -count=1 -timeout 20m ./internal/app -run '^TestPersistedConnectionSelectsDeclarativeTypedDestinationAction$'`; `go test -count=1 -timeout 20m ./internal/connectors -run 'TestSanitize.*Output'`; `go test -count=1 -timeout 20m ./internal/connectors/engine -run '^TestOperationDirectWriteHonorsDeclaredJSONAndNoneResponsePolicies$'`; `go test -count=1 -timeout 20m ./internal/app -run '^TestDirectWriteCommandHonorsDeclaredJSONAndNoneResponsePolicies$'` — green. Full verification remains pending. | green |
| Review remediation: complete mappings, overwrite, duplicate definitions, and bodyless responses | **Red:** review reproduced a normalized field name reaching later work, an incomplete required mapping reaching validation after staging, a declared `full_overwrite` lacking its protocol, duplicate action names resolving differently by consumer, and `none` 204 responses failing after mutation. | **Green:** exact raw property matching and required-field coverage now run at the selected-action preflight boundary; the generic adapter rejects `full_overwrite`; the loader rejects duplicate action names; and bodyless `none` responses retain their completed status and provider receipt header. | `go test -count=1 -timeout 20m ./internal/connectors/engine ./internal/app -run '^(TestBundleLoadRejectsDuplicateWriteActionNames|TestOperationDirectWriteHonorsDeclaredJSONAndNoneResponsePolicies|TestValidateRecordSchemaFieldMappingRequiresExactCompleteFields|TestDeclarativeTypedDestinationSourceBindingsUseExactSelectedActionSchemaFields|TestDeclarativeTypedDestinationPreflightRejectsIncompleteMappingAndFullOverwriteBeforeIO|TestDirectWriteCommandHonorsDeclaredJSONAndNoneResponsePolicies)$'` — passed. | green |

## Follow-up foundation r1

| Slice | Red evidence | Green evidence | Refactor/verification | Status |
| --- | --- | --- | --- | --- |
| Per-action source binding identity | `go test -count=1 -timeout 20m ./internal/app -run '^TestDeclarativeTypedDestinationRequiresActionOwnedSourceBindings$'` failed: a synthetic three-action generic destination accepted one destination-wide source binding, so its distinct action mappings were not representable. The assertion stops before any provider I/O. | `go test -count=1 -timeout 20m ./internal/app -run '^(TestDeclarativeTypedDestinationRequiresActionOwnedSourceBindings|TestDeclarativeTypedDestinationBindsThreeActionsAndSeparateConnectorBeforeIO)$'` passed: three mappings sharing one source and a second connector resolve only their persisted action; cross-selected actions refuse in preflight before transport I/O. | Focused schema, connector, synctransport, App, and persisted CLI tests. | green |
| Closed batch receipts and resume | Existing red-to-green engine/orchestrator receipt tests remain the baseline; this follow-up adds the declaration boundary rather than a second provider writer. | `go test -count=1 -timeout 20m ./internal/app -run '^(TestDeclarativeTypedDestinationBatchLimitIsDefinitionOwned|TestDeclarativeTypedDestinationPersistsPartialProviderResultsOnFailedApply)$'` passed: caller batch 99 is clamped to definition-owned 2, oversized records and tombstones refuse before I/O, and the existing two-record 2xx/5xx path retains complete ordered partial provider receipts. | Engine and orchestration tests plus changed-package `-race`. | green |
| Tombstone delete separation | `go test -count=1 -timeout 20m ./internal/app -run '^TestDeclarativeTypedDestinationTombstonesRequireDeclaredDeleteAction$'` failed while loading the synthetic declaration: `/destination_transport/tombstone_read_back: additional property not allowed`. The existing adapter could only reject tombstones (`deletes:not_available`), so it had no declaration-owned delete/read-back vocabulary. | `go test -count=1 -timeout 20m ./internal/app -run '^(TestDeclarativeTypedDestinationTombstonesRequireDeclaredDeleteAction|TestDeclarativeTypedDestinationTombstoneAppliesOnlyDeclaredDeleteAndReadsBackAbsence)$'` passed: persisted plan/preview/approval applies only the declared delete action and confirms provider absence with its sealed receipt. | App/orchestrator/read-back tests plus persisted resume proof. | green |
| Default persisted action admission | **Red:** the full `go test -count=1 -timeout 20m ./internal/app` suite failed in `TestDeclarativeTypedDestination_ReadBackProviderStateBeforeCheckpoint`, `TestDefinitionTransportFactoriesSelectDistinctTypedDestinationEvidence`, and `TestDeclarativeTypedDestinationRefusesInvalidWorksetsBeforeProviderWrite`: an omitted `DestinationAction` was checked as `""` before the descriptor selected its one declared action. It refused before provider I/O but made valid legacy one-action definitions unreachable. | **Green:** `Registry.Preflight` now resolves `ApplyStrategyForAction` before action-owned source admission and passes the resolved declaration action to `SourceBindingForAction`. The targeted regression run passed, followed by the full App package run (`255.389s`). | The ordering keeps exact caller selections closed while preserving defaulted single-action persisted paths. | green |
| Real persisted application/CLI path | `TestPersistedConnectionSelectsDeclarativeTypedDestinationAction` and the CLI rejection test exercise the stored connection path, not a request-shaping CLI override. | `go test -count=1 -timeout 20m ./internal/cli -run '^(TestETLTransportBareAndLeafHelpAreContextual|TestDeclarativeTypedDestinationTransportRejectsCallerActionBeforeProjectIO)$'` passed. The App test proves plan, preview, approval, apply, acknowledgement, provider read-back, checkpoint, terminal persistence, and resume for stored selections. | Help remains contextual and a caller-supplied action remains refused before project or transport I/O. | green |
