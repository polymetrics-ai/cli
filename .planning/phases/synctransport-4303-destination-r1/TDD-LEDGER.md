# Refs #4303 — TDD ledger

| Slice | Red evidence | Green evidence | Refactor/verification | Status |
| --- | --- | --- | --- | --- |
| Generic synthetic destination | `go test -count=1 -timeout 20m ./internal/app -run '^TestDefinitionTransportFactoriesRunTypedDestinationFromDefinition$'` failed: declared executor `declarative_typed_destination` was not registered, before provider I/O | Same command passed after the definition-selected typed-action factory and action-schema validation landed. | `gofmt`; focused `internal/app` run passed. | green |
| Distinct evidence and fail-closed declarations | The second synthetic bundle could not be admitted while the exact executor was absent. | `TestDefinitionTransportFactoriesSelectDistinctTypedDestinationEvidence` proves both conformance references are collected by one exact factory; it also refuses missing approval during planning. `TestDefinitionTransportFactoriesRefuseTypedDestinationDeclarationsBeforeIO` rejects unknown executor, missing action, wrong mapping role, and destination `change_capture`. `TestDeclarativeTypedDestinationRefusesInvalidWorksetsBeforeProviderWrite` rejects an incomplete mapped action record and tombstone deletes with zero provider writes. | `go test -count=1 -timeout 20m ./internal/app -run '^Test(DefinitionTransportFactories(RunTypedDestinationFromDefinition|SelectDistinctTypedDestinationEvidence|RefuseTypedDestinationDeclarationsBeforeIO)|DeclarativeTypedDestinationRefusesInvalidWorksetsBeforeProviderWrite)$'` passed. | green |
| GitHub neutral-path parity | GitHub previously had a bespoke factory branch beside source evidence collection. | The common `declarativeDestinationAdapters` loop now collects definition-owned evidence and installs GitHub's exact typed issue-label adapter. `TestDefinitionTransportFactoriesSelectDeclaredEvidence` builds it from that shared loop. | `TestPMBinaryExecutesLivePostgresWarehouseGitHubIssueLabels` passed against the dedicated proof repository: add, set, keyed replay, independent API read-back, acknowledgement, and checkpoint proof all passed. | green |
| Documentation | The document did not name the reusable typed-destination executor or its action/mapping/acknowledgement boundary. | `docs/sync-transport-definition.md` names the executor and the mechanical declaration rules. | `make verify` passed, including `docs-check`, `connectorgen validate`, and `connectorgen surface-sync --check`. | green |
| Persisted multi-action selection | `go test -count=1 -timeout 20m ./internal/app -run '^TestPersistedConnectionSelectsDeclarativeTypedDestinationAction$'` initially failed to compile because `StreamConfig.DestinationAction`, multi-action descriptor lookup, and generic App plan/preview dispatch did not exist. The first completed run then failed at the engine gate: the structural plan evidence did not match the concrete prepared write. | `DestinationAction` is persisted and action-aware preflight resolves exactly one `(mode, action)`. `TestPersistedConnectionSelectsDeclarativeTypedDestinationAction` proves append/replace actions in one synthetic connector plus a third connector action all plan, preview, approve, execute, acknowledge, read back, checkpoint, and retain their own endpoint. It also proves missing, foreign, unlisted, and stale persisted selections fail before I/O. Durable authorization issues fresh typed evidence per reopened workset, so a destructive named action still passes the engine's exact prepared-write gate without accepting an arbitrary request. | `go test -count=1 -timeout 20m ./internal/connectors -run '^TestDestinationTransportDescriptorSelectsPersistedActionWithinMode$'`; `go test -count=1 -timeout 20m ./internal/app -run '^TestPersistedConnectionSelectsDeclarativeTypedDestinationAction$'`; `go test -count=1 -timeout 20m ./internal/cli -run '^(TestETLTransportBareAndLeafHelpAreContextual|TestDeclarativeTypedDestinationTransportRejectsCallerActionBeforeProjectIO|TestGoldenTranscripts)$'` — green. Full verification remains pending. | green |
| Exact selected action schema field spelling | **Red:** `go test -count=1 -timeout 20m ./internal/app -run '^TestDeclarativeTypedDestinationSourceBindingsUseExactSelectedActionSchemaFields$'` failed because `input_fields` rejected the schema-valid camelCase `targetId` before the selected action schema was inspected. | **Green:** source field syntax now permits exact camelCase spelling while retaining the reserved generic/shell/http boundary. The selected `writes.json` action's concrete top-level schema properties are checked during connection/run preflight and planning, so snake_case and camelCase work only on their own action; cross-action/connector, empty, malformed, generic/shell/http, and undeclared names refuse before I/O. `pm etl transport … --action` remains an unknown flag before project I/O. | `go test -count=1 -timeout 20m ./internal/app -run '^TestDeclarativeTypedDestinationSourceBindingsUseExactSelectedActionSchemaFields$'`; `go test -count=1 -timeout 20m ./internal/app -run '^(TestPersistedConnectionSelectsDeclarativeTypedDestinationAction|TestDeclarativeTypedDestinationSourceBindingsUseExactSelectedActionSchemaFields|TestDefinitionTransportFactories(RunTypedDestinationFromDefinition|SelectDistinctTypedDestinationEvidence|RefuseTypedDestinationDeclarationsBeforeIO)|TestDeclarativeTypedDestinationRefusesInvalidWorksetsBeforeProviderWrite)$'`; `go test -count=1 -timeout 20m ./internal/connectors ./internal/connectors/engine ./internal/synctransport` — green. | green |
| Complete persisted reverse result | **Red:** `go test -count=1 -timeout 20m ./internal/app -run '^TestPersistedConnectionSelectsDeclarativeTypedDestinationAction$'` failed to compile because the persisted App `Run` had no `DestinationResults` field. The revised `TestOperationDirectWriteHonorsDeclaredJSONAndNoneResponsePolicies` then failed because `output_policy: none` silently returned a nil response body. | **Green:** the selected typed action now carries its complete engine response into a durable acknowledgement, `synctransport.Result`, persisted App run, and the unmodified CLI `ETLRun` envelope. The provider-shaped synthetic response proves ordinary status/header/body fields, exact large numeric fields, nested rare fields, and an enterprise-tier field survive; authorization and configured-secret echoes are in-place `{ "masked": true }` markers. Regular reverse runs persist `destination_result`; operation-direct-write results retain headers and redact declaration-owned response-secret fields in place. `output_policy` now controls parsing only, so it cannot suppress a successful ordinary result. | `go test -count=1 -timeout 20m ./internal/app -run '^TestPersistedConnectionSelectsDeclarativeTypedDestinationAction$'`; `go test -count=1 -timeout 20m ./internal/connectors -run 'TestSanitize.*Output'`; `go test -count=1 -timeout 20m ./internal/connectors/engine -run '^TestOperationDirectWriteHonorsDeclaredJSONAndNoneResponsePolicies$'`; `go test -count=1 -timeout 20m ./internal/app -run '^TestDirectWriteCommandHonorsDeclaredJSONAndNoneResponsePolicies$'` — green. Full verification remains pending. | green |
