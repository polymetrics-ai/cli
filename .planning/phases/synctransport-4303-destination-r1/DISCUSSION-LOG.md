# Refs #4303 — Discussion log

The delivery brief is an explicit decision record. No product question remains open: use the merged #4286 source-composition shape for the destination role, preserve the closed typed-action boundary, keep GitHub behavior identical, and write the destination authoring contract. The inline/manual fallback is permitted because compatible isolated GSD workers are unavailable and the canonical contract forbids role spawning.
