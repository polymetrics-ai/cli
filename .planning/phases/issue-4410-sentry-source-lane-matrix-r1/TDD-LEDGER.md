# TDD ledger — #4410 Sentry source-to-seven-lane matrix

| Stage | Evidence | Result |
| --- | --- | --- |
| Red | `go test ./internal/connectors/defs/sentry -run TestSentrySourceLaneMatrix -count=1` after adding the test but before creating the matrix. | Expected failure: all nine focused tests failed only because `sources/sentry-source-lane-matrix.json` was absent. |
| Green | Materialize source-lock-bound matrix and validate all source rows/cells/facts/backlinks. | Pass: `TestSentrySourceLaneMatrixContract` validates all 223 rows and 1,561 cells. |
| Edge | Mutate decoded matrix in memory for hidden IDs, duplicate IDs, invalid backlinks, missing source facts, pageable/extractable ETL, mutation reverse-ETL, and count mismatch. | Pass: all adversarial tests reject the altered in-memory matrix. |
| Refactor | Normalize citations/reasons, add source URL to each cited cell, tighten backlink-gap coverage, and gofmt. | Pass: every cell records pinned URL and source location; all facts and reasons are source-only. |
| Red — SE-R1-001 | Replace the method/array-derived lane expectation with source-summary action, documented-continuation, and webhook-registration predicates while leaving the frozen matrix unchanged. | Passed as expected: `go test ./internal/connectors/defs/sentry -run 'TestSentrySourceLaneMatrix|TestSentrySemanticLanePredicatesUseSourceContracts' -count=1` failed because the old matrix had no semantic `action` fact. |
| Green — SE-R1-001 | Reconcile only the Sentry matrix facts/cells and artifact backlinks to the semantic source predicates. | Pass: 45 ETL candidates from documented continuation (43 cursor plus two SCIM `startIndex` facts); one webhook-registration sync candidate; all 103 semantic mutations retain direct-write plus reverse-ETL. |
| Edge — SE-R1-001 | Exercise non-GET semantic reads, semantic mutations, structural continuation independent of parameter/schema names, JSON-array/page-size negatives, and registration-vs-list sync behavior. | Pass: local in-memory contract mutations reject hidden IDs, duplicates, missing action evidence, array/page-size ETL promotion, pagination-to-sync promotion, missing registration sync, non-required callback/event fields, missing mutation reverse ETL, invalid backlinks, and count drift without modifying the source lock or shared runtime. |
