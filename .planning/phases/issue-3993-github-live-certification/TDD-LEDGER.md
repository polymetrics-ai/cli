# TDD ledger — Issue #3993

| Slice | Red evidence | Green evidence | Refactor / notes |
| --- | --- | --- | --- |
| Boundary parameterization | `scripts/tests/github-live-cases.test.mjs` specified that only one immutable, run-owned org/repository pair can drive output; it failed before the frozen owner/repo constants were removed. | Green: `node --test scripts/tests/github-live-cases.test.mjs` passes; the generator requires `--boundary` and `--baseline`, records immutable IDs, and reports reason-family movement. | Inputs remain default-deny through `validateLabBoundary`; case execution now rejects a slug or immutable-ID mismatch. |
| Concurrent whole-surface runner | `scripts/tests/github-live-proof-sweep.test.mjs` specified that all operations start before any can finish; it failed before the barrier runner existed. | Green: the focused Node suite passes and `executeBarrier` queues every applicable operation before one shared release; the report records `single_barrier_release`. | No #3990 coordinator is implemented here: the credentialed result records admission pressure as evidence rather than silently serializing it. |
| Terminal process bound | `scripts/tests/github-live-proof-sweep.test.mjs` starts a non-terminating child and requires the runner to terminate it at its supplied bound. | Green: the focused proof-sweep suite passes; each spawned `pm` process now has the certification 45,000 ms terminal bound, with a 1,000 ms forced-kill grace period. | Timeout is a terminal, reportable failure and does not retain subprocess output. |
| Write-scope config bypass ([#4020](https://github.com/polymetrics-ai/cli/issues/4020)) | The proof-sweep regression supplied `--config owner=outside-the-dedicated-repository` and `--config repo=outside-the-dedicated-repository` to a write case with a valid read-back. Before the guard, validation advanced far enough to invoke the fake `pm` credential inspection instead of rejecting the out-of-bound target. | Green: `validateWriteRepositoryTarget` now checks direct owner/repository aliases and their `--config` forms before any `pm` child can start; the focused regression passes with no marker created. | This is correction loop 1 of the five permitted by the final gate. It is a #3993 boundary defect, not a #3990 concurrency/rate-policy change. |
| Manifest / bootstrap integrity | Tests required every current `availability: implemented` command rather than the stale 957 historical pre-skips; both checked-in `--check` artifacts initially failed on drift. | Green: `node scripts/github-live-lab-manifest.mjs --check` and `node scripts/github-live-bootstrap-probes.mjs --check` now pass with 1,521 rows and source-derived cohorts. | The archived case ledger is retained only as archival provenance, never as the current command set or current pre-skip policy. |
| Live provider evidence | Not a unit-test red step: the first 665-way barrier result was 0 proven and all 665 bounded out at 45,000 ms. | Green evidence for the harness boundary/auth path: an App-authenticated repository read and a 16-way simultaneous control both returned data; the full sanitized R2 report records 0 proven / 665 failed / 856 untestable and separate REST/GraphQL snapshots. | The all-at-once child launch saturated locally before rate policy was meaningfully reached; no #3990 coordinator was implemented or implied. The revoked fine-grained token was untouched. |
| Warehouse inbound proof | A first disposable destination path wrote a Parquet file but `pm query` correctly refused it because the query surface does not own that root. | Green: a one-step `pm flow` sync against the canonical local warehouse read 1 GitHub record, wrote 1 Parquet record, and `pm query` (DuckDB) read 1 row back. | This is GitHub → warehouse only. Outbound write stays deferred to #3994/#3992. |
| Pre-barrier fail-closed correction ([#4027](https://github.com/polymetrics-ai/cli/issues/4027)) | Red: `permits only boundary-root reads and the installation repository preflight`, `requires the canonical origin and a fully paginated App installation repository identity`, and `rejects unsafe boundary and report scalars without retaining their contents` were added before the correction. | Green: `node --test scripts/tests/github-live-cases.test.mjs scripts/tests/github-live-lab.test.mjs scripts/tests/github-live-proof-sweep.test.mjs` passed 42 tests. | Round 3 of 5 preserves the historical 0 proven / 665 failed / 856 untestable result, the 45,000 ms bound, the 16/16 control, and the 1→1→1 inbound warehouse proof; it adds no #3990/#3994/#3992 behavior. |
| Canonical classifier-to-evidence correction ([#4039](https://github.com/polymetrics-ai/cli/issues/4039)) | Red: `keeps normalized GET organization PAT-governance paths untestable`, `keeps targetless account bootstrap probes untestable`, `derives the production classifier, digest, and static terminal movement`, `derives manifest readiness from the canonical case classifier`, `binds ordered case artifacts to the canonical classifier before dispatch`, and `rejects compact JOSE, embedded GitHub tokens, encoded private-key armor, and Unicode line separators from reports` were added before implementation. | Green: `node --test scripts/tests/github-live-cases.test.mjs scripts/tests/github-live-lab.test.mjs scripts/tests/github-live-proof-sweep.test.mjs` passed 47 tests. | Round 4 of 5 must bind every case/report artifact to the exact ordered classifier output, retain 0 proven / 665 failed / 856 untestable only as the historical terminal measurement, and add no #3990/#3994/#3992 behavior. |
| Historical rate-proof retirement and final artifact guard ([#4050](https://github.com/polymetrics-ai/cli/issues/4050)) | Red observed before production edits: `retires the legacy rate proof without current certification semantics`, `rejects an unsafe bootstrap manifest before inventory hashing`, and `rejects unsafe supplied bootstrap inventories before drift validation` each failed. | Green: `node --test scripts/tests/github-live-cases.test.mjs scripts/tests/github-live-lab.test.mjs scripts/tests/github-live-proof-sweep.test.mjs scripts/tests/github-live-rate-limit-proof.test.mjs` passed 50 tests. | Correction loop 5 of 5 is complete after GREEN; it preserves the historical case-file binding without presenting `rate-limit get` as current attemptable evidence. |

## Final-gate correction history

`1/#4020 -> 2/#4022 -> 3/#4027 -> 4/#4039 -> 5/#4050` is complete after
the focused GREEN suite. The historical terminal measurement remains 0 proven /
665 failed / 856 untestable at 45,000 ms; it is not a current certification.

## Fresh current-SHA lineage (captain-authorized 1/5)

This section is deliberately separate from the completed correction history.
The fresh line is governed by `3993-01-PLAN.md`; it must record a new executable
RED result before each new behavior change and its corresponding GREEN result
afterwards. It cannot reuse historical tests or results as its RED/GREEN proof.

| Fresh slice | Red evidence | Green evidence | Notes |
| --- | --- | --- | --- |
| One-process provenance and exact-head scan repair | **Red:** 2026-08-11, `node --test --test-name-pattern='execution model|config value' scripts/tests/github-live-proof-sweep.test.mjs` exited 1: the new forged external-runner `credentialed_live` report failed its expected provenance assertion because the current validator reported only `proof report contains an unsupported field`. The config-redaction case passed. No credential, provider, or child process was invoked. | **Green:** 2026-08-11, the same focused command passed 2/2 and `node --test scripts/tests/github-live-proof-sweep.test.mjs` passed 15/15. `validateProofReport` requires the closed model; external blockers require `external_pm_per_operation`; credentialed-live reports require `built_pm_in_process`; `executeLive` fails before credential/provider dispatch; `rg -n '^\s*buildCases,' scripts/github-live-proof-sweep.mjs` returned no match. | Fresh lineage **1/5**; the external-process runner cannot certify shared in-process coordination. |
| Bounded built-binary live closure | Pending only after focused gates, approved App credential inventory, and explicit heavy-validation-window announcement. | Pending | Missing safe authority is `needs-decision`, never a synthetic pass. |
