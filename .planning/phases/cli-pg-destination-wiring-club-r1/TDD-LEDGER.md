# TDD ledger — PostgreSQL production transport wiring club

| ID | Criterion / edge | Red evidence to capture | Green observable evidence | Status |
| --- | --- | --- | --- | --- |
| R1 | #3982 destination registered | `TestOpenRegistersDefinitionOwnedProductionTransports` originally failed because App preflight reported no PostgreSQL destination declaration/factory. | Exact bundle reference builds/registers the native destination and both PostgreSQL and GitHub sources preflight to it; public write remains false. | green |
| R2 | #3983 warehouse workset route | Production transport had no `ChangeDeliveryWorkset`/executor constructor call. | `TestPMBinaryExecutesPostgresWarehousePostgres` and `TestPostgresManagedTargetWorksetDeliveryLive` query exact insert/update/unchanged/tombstone rows, durable receipt, baseline, and checkpoint after stage-owned Parquet delivery. | green |
| R3 | #3979 bootstrap bridge | App incremental route never invoked `BootstrapCDC`. | Binary proof observes snapshot, post-barrier multi-row update/insert/delete transaction, manifests, target rows, LSN checkpoint, kill/restart, and resumed row; focused live bootstrap tests prove before/during/after and failed-snapshot rebootstrap. | green |
| R4 | Missing/stale/replayed approval | RED tests admitted a forged/reused approval to destination preparation. | `TestManagedTargetTransportDestinationRefusesBeforeSideEffects`, `TestPostgresManagedTargetStaleApprovalIsTypedAndStateFree`, and binary token replay assert typed refusal plus unchanged target/delivery/checkpoint. | green |
| R5 | Cancellation mid-operation | Cancellation could strand or advance undurable state. | Destination unit cancellation leaves no project artifacts; live driver cancellation rolls back rows and delivery evidence; App acknowledged-page cancellation retains only the durable checkpoint. | green |
| R6 | Connection/process death | Source or target death could yield silent success or blind retry. | Live driver disconnect reports unknown outcome with unchanged delivery evidence; binary process kill resumes after the last acknowledged LSN. Exact production-route target commit-death remains the pre-existing forbidden #4158 boundary and is disclosed, not claimed. | green-with-disclosed-boundary |
| R7 | Empty/single/large | Zero rows could omit state and a page boundary could drop/duplicate rows. | Binary empty source advances a zero-row checkpoint without a target relation; authenticated GitHub supplies one row; 1,001 PostgreSQL rows cross a 1,000-row boundary with exact target count. | green |
| R8 | Duplicate/out-of-order | Replayed or late items could double-write or regress state. | Fresh approved binary replay preserves exact rows and delivery ID; consumed-token replay refuses with no effects; stable per-key baseline windows and ordered CDC tombstones keep source order. Exact live managed-driver out-of-order control assertion is #4158 and disclosed. | green-with-disclosed-boundary |
| R9 | Schema drift | Changed source or replaced target could auto-evolve/adopt. | Binary source drift returns typed stale approval with unchanged rows/delivery/checkpoint; live driver tests cover OID replacement, target schema, owner and unreadable-control refusals without mutation. | green |
| R10 | Auth/permission refusal | Bad credentials or unreadable control state could partially provision. | Binary missing-user and restricted-role runs return safe typed failures with unchanged business counts and checkpoint; no secret appears. | green |
| R11 | Concurrent same target | Two runs could race approval, provision, baseline, or checkpoint. | App stale-writer/state-CAS tests retain the winning acknowledged checkpoint; managed target table locks and one-shot approval serialize mutation; concurrent destination baselines remain owner-isolated. | green |
| R12 | Resume interruption | Durable pages could be reread or skipped after restart. | Fresh binary resumes the sealed slot/barrier/LSN after process death and applies the next committed row once; failed initial snapshot requires explicit rebootstrap instead of reuse. | green |
| R13 | Binary and command surface | Component-only tests repeated the audited reachability defect. | Built `pm` plan/preview/approval/run reaches real authenticated GitHub and PostgreSQL sources, warehouse artifacts, real PostgreSQL target, receipts/read-back/checkpoints; help/docs/goldens are generated from the same surface. | green |

## Red / Green protocol

Red output is captured before each production slice exists and must fail for the missing behavior, not for a broken fixture. Green output records the focused command and observable assertions. Live refusal cases snapshot target rows, managed control/ledger, baseline, and source checkpoint before and after.
