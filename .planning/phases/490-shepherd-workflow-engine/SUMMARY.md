# Summary: #490

Status: implementation in progress.

The planning decision is partial adoption: exact `pi-workflow-engine@0.12.0` is the project-local bounded analysis/review orchestrator, while the existing Shepherd `ProductionAgentSessionPort` and all durable authority remain unchanged. RED/GREEN, verification, exact-head review, PR, and issue comments are pending.
