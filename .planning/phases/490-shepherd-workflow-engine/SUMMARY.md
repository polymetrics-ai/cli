# Summary: #490

Status: PR #491 is published against `feat/471-pi-agent-session-shepherd`; the one bounded correction pass is locally verified and awaiting exact-final-head disposition verification and CI.

At the start of this pass, the clean local worktree, remote branch, and GitHub PR all pointed to `dceef7ffc4193b9251e2b2159abd03c876174b39`. PR #491 is open, non-draft, mergeable/CLEAN, and all reported checks pass. Its body references #490 and #471 and records the current test and 17-row preservation evidence. Publication is complete; the remaining gates are this one independent exact-head Codex review round and parent integration. The review is not human approval, and neither this child PR nor the parent PR may merge to `main` without the separate human gate.

The decision remains partial adoption: exact `pi-workflow-engine@0.12.0` is project-local developer analysis/review tooling, while Shepherd's `ProductionAgentSessionPort` and every durable authority remain unchanged. Production Shepherd does not import the package, routes only `pm-shepherd`, and cannot invoke workflow-engine built-ins. A developer may explicitly invoke those built-ins with normal Pi coding-agent authority, but their commands, generic-shell access, journals, patches, worktrees, and run IDs are outside Shepherd production and never Shepherd verification evidence. The bounded review path receives host-captured exact material with no tools or GitHub mutation authority.

The two-lane exact-diff review at `7c5f927f0d970f1b38536f94b61e857bf7e51a68` found no CI/delivery blocker and confirmed the developer-tool boundary, but identified two related P1 terminal-snapshot blockers: text-only completion discarded final stop status and substituted configured session route for the actual producing route. Both were corrected in the one allowed pass. Event-agnostic completion now validates the final assistant message from Pi's documented public `session.messages` state after `prompt()` settles, requires `stopReason: "stop"` and the exact producing provider/model, reconciles its text with `getLastAssistantText()`, and only then parses the typed handoff; raw event order remains non-authoritative.

Correction evidence is green: the intended RED was 1 control pass / 6 status-route failures; selected GREEN is 9/9; complete affected runtime suites are 163/163; strict Pi 0.80.10 source typecheck and the family/provenance/offline RPC verifiers pass; and the one final sequential complete Shepherd suite passed 1718 tests, failed 0, and skipped 1 intentional test in 159.6 seconds. All 17 matrix rows remain preserved. The pre-correction `7c5f927f` GitHub Shepherd run had one scheduler-timing assertion failure; the final correction push must rerun CI before readiness is declared.
