# Summary: #490

Status: local implementation and verification complete; PR publication pending.

The decision is partial adoption: exact `pi-workflow-engine@0.12.0` is project-local bounded analysis/review orchestration, while Shepherd's `ProductionAgentSessionPort` and every durable authority remain unchanged. Pi compatibility is narrowed to stable `>=0.80.10 <0.80.11`; prompt settlement plus a validated bound terminal handoff is authoritative and unknown raw events are inert bounded telemetry. Exact package provenance and isolated/co-load RPC are reproducible in CI, workflow run records are ignored, and approved engine agents receive host-captured material with no tools. The one substantive xhigh exact-head review is fully dispositioned. Focused tests, strict typecheck, family/provenance/RPC verifiers, and both complete Shepherd suites pass with all 17 matrix rows preserved.
