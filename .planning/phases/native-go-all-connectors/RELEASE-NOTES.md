# Release Notes

- Enabled native Go runtime bindings for all 647 catalog connectors.
- Added fixture-backed conformance reports for every connector.
- Added direct `pm etl check`, `pm etl catalog`, and `pm etl read` commands.
- Added generic native destination receipts for approval-gated reverse ETL.
- Updated generated connector docs to show enabled native status and runtime capabilities.
