# Design Direction

This phase has no frontend UI. The user-facing design surface is the CLI manual and JSON contract.

## CLI UX

- Bare command groups continue to show man-style documentation.
- Direct ETL commands are explicit verbs: `check`, `catalog`, `read`.
- JSON output remains stable for agents.
- Mutations continue to use reverse ETL plan, preview, approval, run.
