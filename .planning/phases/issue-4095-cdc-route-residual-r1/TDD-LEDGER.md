# TDD ledger — Issue 4095 residual route

| ID | Guarantee | Red | Green | Status |
| --- | --- | --- | --- | --- |
| R4-live | A real pgoutput insert/update/delete reaches a live PostgreSQL keyed apply/history target through durable stage, receipt, workset, and mapping. | The first no-restart run returned `postgres CDC: persist durable checkpoint: polling apply page exceeds declared record limit`: the new fixture incorrectly passed its three-event committed transaction to a borrowed two-record target page. Before the reader result was observed, that test mistake appeared as a wait. | The fixture declares a three-record committed target page and the reader wait fails immediately if `ReadCDC` returns an error. A real PostgreSQL 16 pgoutput source transaction now writes the receipt/workset, applies to a live target, restarts source and target, and replays with independent SQL read-back. | pass |
| Refusal-R1 | The R1 source/destination `change_capture` pairing has a typed pre-I/O refusal. | Added the concrete GitHub -> GitHub table row against actual declaration bundles. | `errors.As` identifies `*synccontract.ModeNotExecutableError` with the connection-warehouse reason and every operation counter remains zero. | pass |
| Refusal-R2 | The R2 source/destination `change_capture` pairing has a typed pre-I/O refusal. | Added the concrete GitHub -> PostgreSQL table row against actual declaration bundles. | `errors.As` identifies `*synccontract.ModeNotExecutableError` with the connection-warehouse reason and every operation counter remains zero. | pass |
| Refusal-destinations | Every destination-side `change_capture` cell receives its own row. | Added individual PostgreSQL -> GitHub (R3) and PostgreSQL -> PostgreSQL (R4) rows as well as R1/R2; there is no catch-all. | Every row asserts the concrete typed error and zero `Check`, `Catalog`, `Read`, `ReadCDC`, and `Write` operations. | pass |
| R3 | PostgreSQL CDC → API is not executable with current declarations. | N/A — do not create a destination action merely to test it. | Record `non_executable`: GitHub's destination declares neither a CDC source binding nor delete availability; `change_capture` is source-only to its connection-owned local warehouse. | non_executable |

## Red/green record

- **Red (2026-08-17):** `POLYMETRICS_DATABASE_INTEGRATION=1 POLYMETRICS_CONTAINER_RUNTIME=docker POLYMETRICS_CONTAINER_ENDPOINT=unix:///Users/karthiksivadas/.colima/default/docker.sock go test -timeout 20m -tags=databaseintegration ./internal/connectors/native/postgres -run '^TestPostgresCDCToManagedTargetHistoryRouteRestartCounterfactualsLive$/^no_restart$' -count=1 -v` failed with `postgres CDC: persist durable checkpoint: polling apply page exceeds declared record limit`. This was the test fixture's two-record target declaration against its own three-event transaction, not a restart product defect.
- **Green (2026-08-17):** The same command passed after declaring the committed three-event target page, and the full no-restart/source-only/target-only counterfactual suite passed. The all-restart R4 proof passed with a real PostgreSQL 16 container. An environment skip is still never treated as live proof.
