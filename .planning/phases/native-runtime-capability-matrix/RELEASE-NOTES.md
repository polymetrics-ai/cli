# Release Notes

- Added generated runtime capability metadata to every connector catalog entry.
- Added CLI/manual documentation for check, catalog, read, write, query, ETL, and reverse ETL support.
- Kept planned native ports disabled for data-plane operations until native Go conformance tests exist.
