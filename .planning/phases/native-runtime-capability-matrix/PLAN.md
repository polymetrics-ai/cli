# Native Runtime Capability Matrix Plan

1. Add red tests for catalog capability coverage and CLI/manual rendering.
2. Add `RuntimeCapabilities` to connector catalog definitions.
3. Update catalog generator to populate capability fields deterministically.
4. Render capability information in catalog manuals, skills, markdown, and JSON.
5. Regenerate embedded catalog and connector docs.
6. Run GSD TDD gate and local verification.
