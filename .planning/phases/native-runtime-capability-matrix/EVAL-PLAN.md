# Eval Plan

- Inspect `source-github` as JSON and confirm enabled native read/write capabilities.
- Inspect `destination-postgres` as JSON and confirm metadata-only support with an unsupported reason.
- Inspect `destination-postgres` manual and confirm humans see runtime capability details before config fields.
- Validate generated docs/skills so agents receive the same policy.
