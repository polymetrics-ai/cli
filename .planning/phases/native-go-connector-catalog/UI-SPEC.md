# UI Spec

This phase has no graphical UI. The user interface is the `pm` CLI:

- Human output remains man-style text with stable sections.
- Agent output uses JSON envelopes.
- Catalog-only connector inspection must show implementation status before configuration details.
