# Native Go Connector Catalog Observability

Catalog operations are local metadata reads. Runtime observability is unchanged. Future native ports must add connector-level latency, page count, record count, retry, and error class metrics before being marked enabled.
