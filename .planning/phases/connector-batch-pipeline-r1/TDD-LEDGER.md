# TDD ledger — connector batch pipeline r1

| ID | Enforcement | RED evidence | GREEN evidence | Refactor / verification |
| --- | --- | --- | --- | --- |
| B1 | A candidate cannot enter a batch without complete measured evidence | `go test ./cmd/connectorgen -run '^TestBatchPlan' -count=1` failed on the baseline: `batch` was an unknown subcommand, so no retrieval-date validation existed | `TestBatchPlanRejectsMissingRetrievalDate` now rejects the selected record with a `retrieved_at` evidence error | Included in focused package run (passed) |
| B2 | Manifest output is deterministic and preserves ledger counts/citations | The same baseline run failed because `connectorgen batch plan` did not exist | `TestBatchPlanWritesDeterministicEvidenceManifest` proves byte-identical manifests preserve operation total and artifact URL/version/retrieval date | Included in focused package run (passed) |
| B3 | A failed connector is a named drop, not a batch abort or silent omission | `go test ./cmd/connectorgen -run '^TestBatchGate' -count=1` failed because `batch gate` did not exist and no report was written | `TestBatchGateContinuesAfterConnectorFailure` reports the malformed sibling as a `validate` drop while the valid connector remains included | Included in focused package run (passed) |
| B4 | Executable claims pass the real runtime preflight rather than a generator copy | The same baseline run reported `connectorgen batch: unknown subcommand "gate"`; no runtime-preflight count existed | `TestBatchGateUsesRuntimePreflightForEveryImplementedCommand` proves the gate records both executable paths after calling the runtime preflight entry point | Included in focused package run (passed) |
| B5 | Surface metadata remains derived from `operations.json` | The same baseline run did not produce a surface-sync drop report | `TestBatchGateDropsSurfaceSyncDrift` proves a path-flag mapping that only `surface-sync` can derive becomes a named `surface_sync` drop; it uses a binary download fixture and no redaction policy | Included in focused package run (passed) |
| B6 | The batch cannot mint a redacting declaration | `TestBatchGateDropsRedactingOutputPolicy` first failed because `json_redacted` was included; `TestBatchGateDropsWriteRedactFields` then failed because a `writes.json` `redact_fields` declaration was included | Both now record an `output_policy` drop; the gate rejects policy labels containing `redact`, legacy repository-content policies, and `redact_fields` in write, operation, or command declarations | Focused red then green runs, then full package run (passed) |
| B7 | A cited OpenAPI artifact can only materialize complete v2 provenance and executable CLI declarations | `go test ./cmd/connectorgen -run '^TestBatchMaterializeGeneratesV2ProvenanceAndReachableSurface$' -count=1` failed before the command existed: `connectorgen batch: unknown subcommand "materialize"` | `TestBatchMaterializeGeneratesV2ProvenanceAndReachableSurface` proves artifact URL/date/SHA evidence on every v2 endpoint, explicit classification of a non-executable deletion, an empty operation-executor catalog, and implemented stream/write commands with matching API-surface rows; the companion missing-coverage test proves a named drop leaves the bundle untouched | `go test ./cmd/connectorgen -count=1` passed after the GREEN implementation |
| B8 | Valid provider YAML cannot be dropped merely because response status keys are numeric | `TestParseBatchOpenAPIArtifactAcceptsYAMLResponseStatusKeys` failed against Docker Hub-shaped YAML: `normalize YAML artifact: json: unsupported type: map[interface {}]interface {}` | The parser now reads only OpenAPI path-item nodes from `yaml.Node`, preserving response-status-key independence; the regression and focused materializer suite pass | The actual Docker Hub artifact materialized after the parser fix |
| B9 | V2 materialization cannot retain legacy exclusions or advertise a write whose required record contract cannot be expressed by CLI flags | `TestBatchMaterializeConvertsLegacyExclusionsAndDerivesWriteFlags` failed with both `uses legacy excluded in operation_ledger_version mode` and missing `record.name` mapping | The materializer converts v1 exclusions into blocked v2 `disallowed`/`deprecated` operation rows and derives only representable required scalar/string-array write flags; structured required objects remain generic reverse-ETL-only | Focused test and all five individual bundle validations passed |
| B10 | A worker can run real runtime preflight per manifest candidate | `TestBatchGateSelectsOneManifestCandidateForIndividualPreflight` failed before support existed: `connectorgen batch gate: unknown flag "--connector"` | `batch gate --connector <name>` selects a manifest record, writes a one-candidate report, and still calls real `commandrunner.Preflight` | Five individual reports each include one connector and zero drops |
| B11 | A newly reachable connector namespace updates the canonical root-help transcript | `go test ./internal/cli -count=1` failed in `TestGoldenTranscripts` because the root help fixture did not list the five newly reachable namespaces | `POLYMETRICS_UPDATE_GOLDEN_TRANSCRIPTS=1 go test ./internal/cli -run '^TestGoldenTranscripts$' -count=1` regenerated the authoritative fixture; the same focused test then passed without the update variable | The fixture deliberately changes expected CLI behavior; no test was weakened or skipped |
| B12 | Public artifact retrieval and inventory are fail-closed | Review established that HTTPS-only checks permit private DNS destinations, redirect pivots, query-bearing provenance, local path-item references, TRACE, and silently skipped operation containers | Focused batch tests cover strict artifact URL/destination guards, local `$ref` plus TRACE inventory, and explicit unsupported-container drops | Included in the recorded `go test ./cmd/connectorgen -count=1` package pass; see `VERIFICATION.md`. |
| B13 | Batch candidates require owned output and at least one runtime-reachable command | Review established that a planned-only CLI surface passes with `commands_checked: 0` and materialization overwrites pre-existing directories | Focused batch tests cover zero-preflight drops and collision-safe source-to-new-destination materialization | Included in the recorded `go test ./cmd/connectorgen -count=1` package pass; see `VERIFICATION.md`. |
| B14 | A ranged or partial HTTP artifact cannot become a complete operation inventory | Review identified that the broad 2xx acceptance could parse a truncated 206 or `Content-Range` response as a smaller valid artifact | Focused table-driven response tests require `artifact_inventory_unknown` for both HTTP 206 and a `Content-Range` header before the body is parsed | Included in the recorded `go test ./cmd/connectorgen -count=1` package pass; see `VERIFICATION.md`. |
| B15 | The batch command namespace has normal contextual-help behavior | Review identified that bare `connectorgen batch` wrote help to stderr and exited 2 | Focused CLI tests require bare namespace help on stdout with exit 0 while invalid subcommands still return usage errors | Included in the recorded `go test ./cmd/connectorgen -count=1` package pass; see `VERIFICATION.md`. |
| B16 | Executable coverage retains exact provider method/path provenance | Review identified canonical matching that accepted slash and method-case differences between a source binding and cited artifact | Focused materialization tests reject trailing-slash and method-case variants while retaining only an exact endpoint match | Included in the recorded `go test ./cmd/connectorgen -count=1` package pass; see `VERIFICATION.md`. |
| B17 | TRACE and OPTIONS remain counted protocol-metadata exclusions | Review identified that the default mutation classifier mislabeled both methods as high-risk disallowed operations | Focused materialization tests retain both endpoints, record method-specific non-data metadata, and report them as excluded rather than executable or provider-blocked | Included in the recorded `go test ./cmd/connectorgen -count=1` package pass; see `VERIFICATION.md`. |
| B18 | Direct batch gates reject protocol metadata as executable coverage | Review identified that `covered_by` was classified before TRACE/OPTIONS metadata semantics, allowing direct gate runs to count either method executable | Focused direct-gate tests reject covered TRACE/OPTIONS and retain valid method-specific exclusions in the declared total and exclusion split | Included in the recorded `go test ./cmd/connectorgen -count=1` package pass; see `VERIFICATION.md`. |
| B19 | Batch gates admit only manifest-matched complete v2 provenance | Review identified that legacy, incomplete, and URL-mismatched surfaces reached runtime preflight and inclusion because generic validation preserves legacy compatibility | Focused direct-gate tests refuse legacy/missing/mismatched provenance, admit matched v2 evidence, and aggregate provenance-refusal counts | Included in the recorded `go test ./cmd/connectorgen -count=1` package pass; see `VERIFICATION.md`. |

## Test-first rule

Each named test is added and run against the baseline before its production
implementation. The observed failure is retained here; tests are not weakened,
skipped, or deleted to satisfy the batch report.
