# TDD LEDGER — issue #3715 Pi clean project-only workers

| ID | Enforcement | RED evidence | GREEN evidence | Refactor/verification |
| --- | --- | --- | --- |
| R1 | A clean worker cannot discover bundled, global, or retained legacy project agents. | `bash scripts/tests/pi-clean-project-agents.sh` failed: `ambient-global` (user), nine extension-bundled roles, and retained `.pi/agents` roles were all discoverable. | The same executable test now passes against the real extension entry point; clean discovery and default execution list only the two generated project workers. | Loader is fail-closed on the canonical contract and exact generated filenames; legacy files remain untouched. |
| R2 | A child worker cannot receive the `subagent` tool or recursively delegate. | Existing policy was preserved rather than expanded; the new executable isolation test exercises the child-policy module. | The test proves `subagent` is stripped, depth `1` is rejected, and every child invocation includes `--no-extensions`. | `child-policy.ts` keeps the bounded parent/agent intersection and depth guard isolated from extension loading. |
| R3 | Pi worker files are complete generated projections, not hand-written wrappers/blocks. | `go test ./internal/agentcontract -run 'Test(PiProjectionsAreRequired|SyncCreatesRequiredPiProjections)$' -count=1` failed because Pi targets were optional and `SyncProjections` could not create absent files. | `go test ./internal/agentcontract -count=1` passes, including required creation and whole-file drift rejection; `go run ./cmd/agentcontractgen check` passes. | Complete Pi files are generated by `sync`; existing block projections remain supported for later waves. |
| R4 | Canonical Pi configuration permits exactly the base + connector roles and bounded tools. | The required role/tool invariants were absent from the Wave 1 contract schema. | `TestCanonicalContractRequiredInvariants` rejects scope changes, an extra role, and `subagent` in the child list. | The exact ordered role pair and built-in bounded tool list are validated before any projection is rendered. |

No production extension, generator, canonical-source, or generated-agent edit is permitted before
the recorded R1/R3 RED runs.
