# TDD ledger — issue 4364 deferred visibility/admission bridge

| Behavior | Red evidence | Green evidence | Refactor / safety note |
| --- | --- | --- | --- |
| 4,341-row manifest admits exactly 1,910 concrete deferred rows | **Red (2026-08-27):** `go test -count=1 -timeout 20m ./cmd/connectorgen -run '^TestBatch1DeferredManifest'` fails to build because no manifest decoder, closed model, or published-census reconciliation exists. | **Green (2026-08-27):** the same focused command passes after a closed manifest decoder reconciles source records against the published 4,341 / 846 / 1,585 / 1,910 invariant. | The test derives totals from source records and asserts the published invariant; it must not bless the obsolete 1,908 matrix. |
| Deferred row preserves exact citation, lane, stable CLI path, target, and one foundation | **Red (2026-08-27):** the same focused test fails before it can exercise its duplicate, missing citation, generic target, policy-only, and multiple-foundation fixture table because `batch1DeferredManifest` is absent. | **Green (2026-08-27):** the same focused command passes its duplicate, missing citation, generic target, policy-only, and multiple-foundation cases. | Reuse existing closed citation, endpoint, and foundation-component validators. |
| Generated deferred declaration has no fabricated typed executor | **Red (2026-08-27):** `go test -count=1 -timeout 20m ./cmd/connectorgen -run '^TestBatch1DeferredCatalogsPreserveEveryDeferredSourceIdentity$'` fails to build because the source-lock-backed catalog builder is absent. | **Green (2026-08-27):** the same focused command passes: 1,910 exact source-lock rows produce one inventory, source, and deferred command-binding declaration each, with no stream/write/operation binding. | A later typed-lane PR, not this bridge, owns promotion. |
| Public command returns exact missing foundation before I/O | Pending: request/credential-counting commandrunner/app/CLI regression against a generated command. | Pending. | Keep the typed terminal separate from `missing --credential`; it is visibility proof, not runnable proof. |
| All source classes remain visible and evidence rolls up exactly | Pending: per-provider/lane/component/semantic count regression. | Pending. | Generated operation evidence must state visibility and runnable delta separately. |
