# TDD ledger — #4344

| Slice | Red | Green | Refactor / verification | Status |
| --- | --- | --- | --- | --- |
| Generated parameterized command identity | `GOFLAGS='-p=3' go test -count=1 -timeout 20m ./cmd/connectorgen -run '^TestSourceProjectionGeneratedParameterizedCommandIsRuntimeValidAndStable$'` failed after deliberately restoring the raw source ID: `generated command path retained a raw source parameter` | Operation-derived hex identity plus invalid-legacy-only migration made the focused test pass | Final full `./cmd/connectorgen` suite passed in 248.518s; test also proves flags bind into the planned record, idempotence, and non-collision for `{a}`, `{b}`, and a literal | complete |
| Checked-in surface reconciliation | No affected source descriptor exists on this base; `source-import bitbucket --check` fails before projection because its `sources/` directory is absent | No generated artifact was fabricated or changed; future import regenerations use the new identity | `connectorgen validate` passed (552/0); `surface-sync --check` passed (552, zero drift); operation evidence and runtime preflight passed | complete — no applicable checked-in artifact |
| Credential-free binary reachability | Raw form is rejected by the behavioral projection test before command resolution | Built `pm` in an isolated project; current Bitbucket `repositories list`, `repositories create`, and `repositories delete` all returned exactly `error: missing --credential` | The requested 50-command/28-path Bitbucket sweep remains an integration check against the reviewed batch-1 descriptor, unavailable on this base; it is not counted as complete here | partial — dependency recorded |
