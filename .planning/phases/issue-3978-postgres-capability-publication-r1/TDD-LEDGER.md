# TDD ledger — Issue #3978: final PostgreSQL certification and publication

| ID | Red guarantee | Green implementation / evidence | Status |
| --- | --- | --- | --- |
| PGFINAL-1 | `TestCertificationMatrixPromotesPostgresManagedWriteOnlyWithDeclaredLiveTransportProof` initially found the metadata/matrix claim at `write=false`, while `cdc=true` had no accepted capability proof. | The PostgreSQL capability cells now require definition-owned evidence: one aggregate `capability:write` record from a complete six-mode profile, plus the six exact destination-mode records retained only on mode cells; `cdc` has one receipt-backed real-binary capability record. `TestPostgresPublishedWriteAndCDCMatchLiveCertification` binds both public flags to those live cells; `query` remains false. | Green: `go test -timeout 20m ./cmd/connectorgen -count=1` |
| PGFINAL-2 | A declaration-only `write=true`, or even all twelve `sync_mode` records without a capability record, must not make managed transport executable. | `TestCertificationMatrixKeepsPostgresManagedWriteUnimplementedWithoutCapabilityScopedLiveProof` removes the capability record while retaining all 12 mode records and requires false. The direct `Connector.Write` refusal test remains unchanged. | Green: focused `cmd/connectorgen` tests |
| PGFINAL-3 | A schema-valid but runtime-invalid profile could otherwise look certified. | Scratch `sslmode=bananas` passed `connectorgen validate` but the real built-binary profile exited 2. It was restored to `disabled`, then the same real-binary profile completed green. | Red and restoration recorded in `traces/red-runtime-profile.txt` |
| PGFINAL-4 | A CDC acknowledgement could be confused with a slot's pre-existing startup LSN or advance ahead of the connection-owned receipt. | The fresh binary test now captures the committed post-insert LSN, independently reads Parquet, requires a receipt with no pending transaction manifests and a full checkpoint, then accepts source acknowledgement only at/after that LSN after receipt observation. Its opt-in importer wrote two redacted records. | Green: tagged `TestPMBinaryDispatchesPostgresChangeCaptureToWarehouse` |
| PGFINAL-5 | The stale issue wording could reject executable history, or imply CDC→API/destination passing. | The generated matrix records all six declared target modes, including `incremental_dedupe_history`, as live; `change_capture` is one database-read-into-warehouse pass. API primitives and destination change capture remain concrete non-pass/N/A cells with reasons. | Green: `connectorgen certification-matrix --check` |
| PGFINAL-6 | A shared generator change could regress another connector or generated docs could drift. | The global matrix gate caught a GitHub write regression during this task; the native-database guard was narrowed and GitHub returned byte-identical. PostgreSQL docs/catalog/website projections were regenerated; `gen:docs` ran twice and produced no second-pass diff. | Green: repository, consumer, docs, website, and boundary gates recorded in `VERIFICATION.md` |
| PGFINAL-7 | `TestCertificationMatrixPromotesPostgresManagedWriteOnlyWithDeclaredLiveTransportProof` went red after the certification gate halted the attempted promotion: a `sync_mode` record was attached to `capability:write`, which the agent contract correctly rejected as a scope mismatch. | A fresh PostgreSQL 16 profile emitted `postgres_transport_r1-capability-write.json` at `scope=capability`, after its importer revalidated all six declared destination modes. The matrix binds only that record to `capability:write`; mode records remain separate. `TestCertificationGateEnforcesEveryProtectedTransition` and `TestCertificationGateCurrentBaselineRejectsWithoutBreakingStructuralContractCheck` stay green, so protected transitions and a bad baseline still refuse. | Green: live profile plus `go test -timeout 20m ./internal/agentcontract -run '^(TestCertificationGateEnforcesEveryProtectedTransition|TestCertificationGateCurrentBaselineRejectsWithoutBreakingStructuralContractCheck|TestEvaluateCertificationGateGitHubBaselineAndGreenFixture)$' -count=1 -v` |
