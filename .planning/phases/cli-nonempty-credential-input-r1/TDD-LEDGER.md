# TDD ledger — provider-neutral non-empty credentials

| Slice | Red | Green | Refactor / evidence |
| --- | --- | --- | --- |
| Stdin transport | **Red:** `TestCredentialsAddStdinPreservesSingleTerminalDelimiterAndRoundTrips` reduced a two-terminal-newline synthetic canary from 769 to 768 bytes; empty, LF-only, and CRLF-only stdin exited 0 and persisted. | **Green:** `NormalizeStdin` removes exactly one final LF or CRLF; CLI rejects the resulting empty value with `*credential.EmptySecretError` before `App.AddCredential`. | Assertions expose only length and SHA-256 fingerprint. |
| App persistence | **Red:** `go test -timeout 20m ./internal/app -run '^TestAddCredentialRejectsEmptySecretBeforeVaultPersistence$' -count=1` failed because direct `App.AddCredential` accepted an empty map value; `RuntimeConfigSecretStore.PutSecret` also accepted an empty replacement. | **Green:** `App.AddCredential` and `vault.Put` both call `RequirePersistentValues`; secret-store writes retain existing non-empty data after an empty replacement is refused. | Existing secretless connector credentials remain allowed. |
| Auth emission | **Red:** connsdk and engine focused tests showed blank bearer, basic, API-key-header, and API-key-query routes were selected/applied successfully; the adjacent OAuth form routes were included in the same required-auth audit before Green. | **Green:** selected required bearer/basic/API-key header/API-key query/client-credentials/refresh-token routes reject blank material before request or token-form mutation; engine rejects resolved empty values before constructor selection. | Optional declaration-selected `none` and the documented optional refresh-grant client secret remain valid. |
