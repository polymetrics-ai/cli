# #3864 TDD ledger

| ID | Requirement | RED evidence | GREEN evidence | Status |
| --- | --- | --- | --- | --- |
| T1 | Canonical `full_append` reaches registered transports rather than the pre-resolution hard stop. | `go test -timeout 20m ./internal/app -run '^TestRunETLCanonicalFullAppendUsesRegisteredTransports$'` failed: `sync mode "full_append" is not executable: no matching native executor has completed the shared conformance corpus`. | `go test -timeout 20m ./internal/app -run '^TestRunETLCanonicalFullAppendUsesRegisteredTransports$'` passed; fake registered source → warehouse stage → descriptor-declared destination strategy completed and persisted its acknowledged checkpoint without legacy `Read`/`Write`. | Green |
| T2 | Registry preflight rejects missing executor, type/family mismatch, unsupported mode, unsafe acknowledgement, missing strategy, and unavailable conformance verifier before source read. | `go test -timeout 20m ./internal/synctransport -run '^TestPreflightRejectsClosedAdmissionFailuresBeforeSourceRead$'` could not compile because the closed registry/preflight types do not exist. | `go test -timeout 20m ./internal/synctransport -run '^TestPreflightRejectsClosedAdmissionFailuresBeforeSourceRead$'` passed for each rejection, asserting zero source-executor reads. | Green |
| T3 | Destination plan receives the descriptor-resolved strategy, not `upsert`. | `go test -timeout 20m ./internal/connectors -run '^TestSyncTransportDescriptor'` could not compile because descriptors and strategy resolution do not exist. | `go test -timeout 20m ./internal/connectors -run '^TestSyncTransportDescriptor'` passed; `full_append` resolves only the descriptor's `stage_append` / `append` strategy. | Green |
| T4 | One orchestrator handles fake API→API, API→database, database→API, database→database through warehouse-stage mediation without pair branches. | `go test -timeout 20m ./internal/synctransport -run '^TestOrchestratorDispatchesFourClosedPairingsWithoutPairBranches$'` could not compile because the orchestrator does not exist. | `go test -timeout 20m ./internal/synctransport -run '^TestOrchestratorDispatchesFourClosedPairingsWithoutPairBranches$'` passed for all four fake pairings, each invoking source → stage → destination once. | Green |
| T5 | Candidate checkpoint commits only after durable destination acknowledgement; failed/unsafe acknowledgement leaves it uncommitted. | `go test -timeout 20m ./internal/synctransport -run '^TestOrchestratorCommitsOnlyAfterDurableAcknowledgement$'` could not compile because the orchestrator does not exist. | `go test -timeout 20m ./internal/synctransport -run '^TestOrchestratorCommitsOnlyAfterDurableAcknowledgement$'` passed; an invalid acknowledgement returns the #3810 error and commits zero checkpoints. | Green |
| T6 | Source/destination context cancellation stops without an acknowledgement or checkpoint commit. | `go test -timeout 20m ./internal/synctransport -run '^TestOrchestratorStopsOnCancellationBetweenWarehouseStageAndApply$'` could not compile because the orchestrator does not exist. | `go test -timeout 20m ./internal/synctransport -run '^TestOrchestratorStopsOnCancellationBetweenWarehouseStageAndApply$'` passed; cancellation after stage calls neither apply nor commit. | Green |
| T7 | `connectors inspect` JSON/manual explicitly projects descriptor eligibility or unsupported roles without credential resolution. | `go test -timeout 20m ./internal/cli -run '^TestConnectorInspectProjectsUnsupportedSyncTransport$'` failed with empty source/destination eligibility because inspect emitted no `sync_transport` projection. | `go test -timeout 20m ./internal/cli -run '^TestConnectorInspectProjectsUnsupportedSyncTransport$'` and `go test -timeout 20m ./internal/connectors -run '^TestSyncTransportGuideProjectsDeclaredRolesWithoutCertificationClaim$'` passed. | Green |
| T8 | Registry lookup remains safe while a closed executor registration races with preflight. | Not separately red: this is a synchronization property of the new registry. | `go test -race -timeout 20m ./internal/synctransport -run '^TestRegistryPreflightIsRaceSafeDuringRegistration$'` passed. | Green |
| T9 | A stage cannot mutate nested provider payload through a shallow copied record. | `go test -timeout 20m ./internal/synctransport -run '^TestOrchestratorStagesDeepCopyOfProviderRecords$'` failed: the test stage inserted `staged=true` into the original nested provider map. | The same command passed after recursive composite-value copying; the full `-race` transport package test also passed. | Green |
| T10 | App-level preflight is not masked by an unconfigured warehouse stage. | `go test -timeout 20m ./internal/app -run '^TestRunETLTransportPreflightRejectsMissingExecutorBeforeSourceRead$'` failed with `warehouse stage is required for transport dispatch` instead of the missing source executor. | The same command passed after registry preflight moved before stage/committer dependency validation. | Green |
| T11 | An authored-but-invalid empty descriptor cannot fall back to the legacy/no-descriptor route. | `go test -timeout 20m ./internal/app -run '^TestHasDeclaredSyncTransportRoutesInvalidDescriptorToPreflight$'` failed because role lookup treated it as absent. | The same command passed after app dispatch checks for any authored transport descriptor and lets preflight reject invalid roles. This is correction-loop 1/5, tracked by #4021. | Green |
| T12 | A destination cannot declare an apply strategy for a sync mode it does not declare. | `go test -timeout 20m ./internal/connectors -run '^TestDestinationTransportDescriptorRejectsStrategyOutsideDeclaredModes$'` failed because validation accepted `full_overwrite` strategy beside a `full_append`-only declaration. | The same command passed after descriptor validation rejects an apply-strategy mode outside `DestinationTransportDescriptor.Modes`. | Green |
| T13 | Generic executor references cannot evade the closed dispatch boundary through hyphen spelling. | `go test -timeout 20m ./internal/connectors -run '^TestSyncTransportDescriptorRejectsHyphenatedGenericExecutorReference$'` failed because `generic-http` passed validation. | The same command passed after identifier validation normalizes hyphens before rejecting generic HTTP/SQL/shell executor references. This is correction-loop 2/5, tracked by #4023. | Green |
| T14 | Inspection preserves a structurally valid declared destination with `acknowledgement: none`, while runtime preflight remains durable-warehouse-only. | `go test -v -timeout 20m ./internal/connectors -run '^TestSyncTransportEligibilityProjectsDeclaredNoneAcknowledgement$'` failed: the `none` subtest received destination status `unsupported`, not `declared`. | `go test -v -timeout 20m ./internal/connectors ./internal/synctransport -run '^(TestSyncTransportEligibilityProjectsDeclaredNoneAcknowledgement|TestPreflightRejectsClosedAdmissionFailuresBeforeSourceRead)$'` passed after eligibility projected every structurally valid destination; the existing unsafe-acknowledgement preflight rejection also passed. This is correction-loop 3/5, tracked by #4029. | Green |
| T15 | Every closed mode persists only its active stream's acknowledged interim checkpoint and generation before a source error, cancellation, or final state-save failure; it does not claim successful completion. | `go test -count=1 -timeout 20m ./internal/app -run '^(TestRunETLTransportPersistsActiveCheckpointBeforeSourceFailureForAllModes|TestRunETLTransportCommitsAcknowledgedPageBeforeCancellation|TestRunETLTransportRetainsInterimCheckpointWhenFinalStateSaveFails|TestRunETLTransportTreatsIndeterminateCheckpointPersistenceAsFailure)$'` failed: all seven source-error cases retained `previous` instead of `current`; cancellation retained no checkpoint; final save did not fail at the post-commit boundary; indeterminate persistence claimed a successful run. | `POLYMETRICS_UPDATE_GOLDEN_TRANSCRIPTS=1 go test -count=1 -timeout 20m ./internal/app ./internal/connectors ./internal/synctransport ./internal/cli -run '^(TestRunETLTransportPersistsActiveCheckpointBeforeSourceFailureForAllModes|TestRunETLTransportCommitsAcknowledgedPageBeforeCancellation|TestRunETLTransportRetainsInterimCheckpointWhenFinalStateSaveFails|TestRunETLTransportTreatsIndeterminateCheckpointPersistenceAsFailure|TestSyncTransportEligibilityProjectsDeclaredNoneAcknowledgement|TestSyncTransportEligibilityProjectsValidRolesIndependently|TestPreflightRejectsClosedAdmissionFailuresBeforeSourceRead|TestOrchestratorRejectsInvalidSiblingDescriptorBeforeProviderAccess|TestNewRegistryFailsClosedForTypedNilConformanceVerifier|TestCloneRecordCopiesBinaryValuesAtEveryNestingLevel|TestOrchestratorCommitsAcknowledgedPageBeforeReturningCancellation|TestConnectorsHelpExplainsDeclaredNoneInspectionPolicy|TestGoldenDocsGenerateMatchesTrackedCLIManuals|TestGoldenTranscripts|TestConnectorInspectProjectsUnsupportedSyncTransport)$'` passed; all seven modes persisted only their active interim checkpoint, cancellation committed its acknowledged page first, and definite/indeterminate state failures did not claim completion. | Green |
| T16 | Inspection validates/projects each role independently, while runtime full-validates every selected connector descriptor before provider access. | `go test -count=1 -timeout 20m ./internal/connectors ./internal/synctransport -run '^(TestSyncTransportEligibilityProjectsValidRolesIndependently|TestOrchestratorRejectsInvalidSiblingDescriptorBeforeProviderAccess)$'` failed: valid siblings were `unsupported`, and both malformed-sibling descriptors reached provider dispatch. | The T15 focused GREEN command passed; valid siblings project independently, malformed sibling roles reject before plan/read/apply, and the existing durable-acknowledgement rejection remains covered. | Green |
| T17 | A typed-nil external conformance verifier fails closed at registry construction/preflight without method invocation. | `go test -count=1 -timeout 20m ./internal/synctransport -run '^TestNewRegistryFailsClosedForTypedNilConformanceVerifier$'` failed with the typed-nil verifier panic. | The T15 focused GREEN command passed `TestNewRegistryFailsClosedForTypedNilConformanceVerifier`; `NewRegistry` and preflight both use the typed-nil guard and return verifier unavailable. | Green |
| T18 | Binary record values are independently copied at scalar, nested-map, and nested-list positions. | `go test -count=1 -timeout 20m ./internal/synctransport -run '^TestCloneRecordCopiesBinaryValuesAtEveryNestingLevel$'` failed: the original bytes became `11`, `12`, and `13`. | The T15 focused GREEN command passed `TestCloneRecordCopiesBinaryValuesAtEveryNestingLevel`; only byte backing storage is cloned. | Green |
| T19 | Runtime connector help and generated manual state the declared-`none` inspection policy. | `go test -count=1 -timeout 20m ./internal/cli -run '^TestConnectorsHelpExplainsDeclaredNoneInspectionPolicy$'` failed because `connectorsHelp` omitted `acknowledgement=none remains declared`. | The T15 focused GREEN command passed the runtime-help, tracked-manual, and golden-transcript assertions after `pm docs generate` regenerated `docs/cli`. | Green |
| T20 | An acknowledgement-stamped interim checkpoint must match the active source identity and generation before state persistence. | `go test -count=1 -timeout 20m ./internal/app -run '^TestRunETLTransportRejectsAcknowledgedCheckpointWithIncompatibleResume$'` failed: checkpoints for another credential, another stream, and another generation all returned success instead of their typed rebootstrap outcome. | `go test -count=1 -timeout 20m ./internal/app -run '^(TestRunETLTransportRejectsAcknowledgedCheckpointWithIncompatibleResume|TestRunETLTransportPersistsActiveCheckpointBeforeSourceFailureForAllModes|TestRunETLTransportAdvancesInterimCheckpointAcrossPages|TestRunETLTransportPreservesUnrelatedStateDuringInterimCheckpointCommit|TestRunETLTransportRejectsStaleCheckpointWriter|TestRunETLTransportDistinguishesMissingAndPresentStreamState|TestRunETLTransportCommitsAcknowledgedPageBeforeCancellation|TestRunETLTransportRetainsInterimCheckpointWhenFinalStateSaveFails|TestRunETLTransportTreatsIndeterminateCheckpointPersistenceAsFailure)$'` passed; another credential/stream returns source-identity rebootstrap, a changed generation returns source-generation rebootstrap, no mismatch writes a stream checkpoint, and the matched control completes. | Green |
| T21 | Interim state writes must compare-and-swap only the captured stream entry, preserve its completion metadata, and never overwrite a newer or merely present entry. | `go test -count=1 -timeout 20m ./internal/app -run '^(TestRunETLTransportPersistsActiveCheckpointBeforeSourceFailureForAllModes|TestRunETLTransportAdvancesInterimCheckpointAcrossPages|TestRunETLTransportPreservesUnrelatedStateDuringInterimCheckpointCommit|TestRunETLTransportRejectsStaleCheckpointWriter|TestRunETLTransportDistinguishesMissingAndPresentStreamState)$'` failed: all seven modes dropped prior completion metadata, the multi-page checkpoint did likewise, and stale/present-state writers completed instead of rejecting; the unrelated-update control already passed. | The T20 focused GREEN command passed: all seven modes retain prior metadata on interim persistence, expected state advances across binary checkpoint pages, unrelated stream/project updates survive, a stale two-app writer cannot complete or overwrite the winner, and an absent entry differs from a present zero entry. | Green |

T20 and T21 RED evidence was recorded before correction-loop 5 production changes.
