# TDD ledger — issue 4329

| Slice | Red evidence | Green evidence | Refactor / scope evidence |
| --- | --- | --- | --- |
| Closed `read_only` model | `go test -count=1 -timeout 20m ./cmd/connectorgen -run '^(TestSourceProjectionRequiresExplicitReadOnlyMutationDeclaration|TestOperationEvidenceSeparatesDeclaredReadOnlyFromFoundations)$'` failed to compile because the evidence fields were absent; after implementation it failed loading the new model because the enum was closed. | `GOFLAGS=-p=3 go test -count=1 -timeout 20m ./internal/connectors/engine -run '^TestParseAPISurfaceOperationModelEnumRemainsClosedWithReadOnly$'` passed. | Exactly one member was added. The test round-trips every prior member byte-for-byte and rejects an unknown member. |
| Explicit non-mutating source refusal | The original POST acceptance test was deliberately invalidated by the architecture decision: a POST marker must now fail. | `GOFLAGS=-p=3 go test -count=1 -timeout 20m ./cmd/connectorgen -run '^TestSourceProjectionRequiresExplicitReadOnlyNonMutationDeclaration$'` passed. | Only the exact source-cited GET plus named policy is accepted; an undeclared GET still fails and POST is rejected. |
| Contradictory executable declaration | The real GitHub evidence-path test first failed because the chosen source row had a partial CLI, then because it had a stream target; both failures were the intended conflict guard. | `GOFLAGS=-p=3 go test -count=1 -timeout 20m ./cmd/connectorgen -run '^TestOperationEvidenceSeparatesDeclaredReadOnlyFromFoundations$'` passed after the fixture removed the real endpoint's executable coverage. | An implemented GET route or any runtime/CLI/website evidence makes the declaration contradictory. |
| Separate evidence accounting | The red test initially referenced the missing `ReadOnly`/rollup fields. | The same focused operation-evidence test proves the real source-lock workspace emits `read_only` plus `intentionally_read_only`, with no foundation/gap rollup. | Read-only rows are visible, counted, and distinct from `missing_foundations`. |
| Sentry/Vercel mutation handoff | Historical source evidence shows 34 current Sentry POST/PATCH and 159 Vercel POST/PATCH mutations without current method/path actions. | Recorded in the status handoff for `cli-mutation-disposition-foundation-r1`; no read-only declaration suppresses them. | They remain visible mutation coverage findings, outside this issue. |
