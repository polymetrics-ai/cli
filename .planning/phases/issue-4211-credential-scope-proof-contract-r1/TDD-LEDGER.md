# TDD ledger — Issue 4211 provable credential-scope contract

| Slice | Red | Green | Evidence |
| --- | --- | --- | --- |
| Full-parity guard | A passed report with no `full_parity` stage cannot call the full-parity writer; the current boolean-based writer would have accepted it. | Full scope is derived only by a passed `Report.FullParityVerified()` result and serializes `full_parity_stage`. | `TestCertificationFullParityScopeRequiresPassedReportStage` first refuses the direct-read-like report, then accepts a passed `full_parity` stage. |
| Bounded publication | **Red observed:** `go test -timeout 20m ./cmd/connectorgen -run '^TestCertificationBoundedScopePublishesObservedOperations$' -count=1` failed: `completed live test did not use a full-parity credential`. | The default construction path publishes `observed_operations` from its validated protocol transcript, with `protocol_exchanges` proof and an explicit no-broader-scope note. | The same focused command passed after the implementation; `TestCertificationPublishesNarrowCredentialEvidence` preserves the former test's case under the newly required honest behavior. |
| Validator integrity | A v2 full-parity JSON record missing or mismatching its proof discriminator is accepted by the previous string-only check. | The existing exact full-parity validator remains active and the new validator requires the matching discriminator for every v2 claim. | `TestCertificationFullParityScopeRequiresPassedReportStage` mutates the discriminator and receives a specific validation error; `internal/agentcontract` independently rejects mismatched pointers. |
| PostgreSQL re-issue | Existing records expose `full_parity` despite source reports produced without a full-parity stage. | A fresh database integration run writes all fourteen records as bounded `observed_operations` proofs and matrix generation is byte-stable. | Both opt-in database integration tests passed; the record and matrix field audit is recorded in `VERIFICATION.md`. |
