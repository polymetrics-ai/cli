# CP18 GitLab TDD ledger — 267

| ID | Requirement | Red evidence (immutable) | Green required before increment | Status |
| --- | --- | --- | --- | --- |
| G1 | Scoped query inputs join into runtime requests without opening arbitrary injection. | `TEST-ACCOUNTING-252-265.json`, `remaining-cli252/output.jsonl` SHA-256 `b926573612277887cab36f0582c3e04f4d45cb2e98c2d77c5c0ef1260607f379`: 1,344 retained read failures. | Fresh exact query/auth wire assertions plus undeclared-input zero-I/O refusal. | Green — source lock `00511a5a`; 203 exact scalar joins and three fake-provider wires pass; `RUN-267.md` G1 receipt. |
| G2 | Inclusive source numeric bounds survive lock → execution → runtime. | Fresh `g2-red-267-d1aad9f8/RECEIPT.json`: all four declarations missed their bounds and all eight below/above values planned, with zero provider sends. Retained `PROVISIONAL-INPUT-BOUNDS-252.json` remains the broader 8 direct + 6 cascading record. | Fresh lower/upper/interior requests in independent state, zero plan/HTTP for invalid values. | Green for the four source-backed bounds: source lock `a15eece1`; admitted `writes.json` `f77ad705`; `TestBatch1CP18GitLabG2SourceBounds` passes fresh. Broader retained G2 cohort remains to reconcile. |
| G3 | Completed-send ambiguity follows declared retry/approval/receipt semantics. | Retained `PROVISIONAL-AMBIGUOUS-REPLAY-252.json`; 164 outcomes; fresh policy-oracle counterexample is retained in `repair-266/g3-red-267-9ed6624d/RECEIPT.json`. | Source decision table and direct/saved send, approval, receipt, ack controls. | In progress — RFC-9110-consistent declared-idempotent policy is GREEN for the attached 313/328 exact leaves (five physical sends after a close-before-response, terminal failure, no success receipt; spent approval adds zero sends). The precise `deleteApiV4AdminCiVariablesKey` four-leaf direct/reverse selected runtime receipt was rerun in clean R1 after the derived registry update and passes; raw hash `9a5f751d05019bf0ebb65e14d94276ee5006b02d7567b504fa8fb95767744b8e`, published at `f8828537216d31454a9c440bc0a5e259c913e63e`. A focused proof-coordinate RED (`af22232238e48f5caaef87ba286f58dae8dab3831490fa0a8dc842758882dec3`) showed that the verifier refused the source-lane producer's own canonical `sha256:` generation. Its green compatibility control is `031bd219067344da1cbd876d88f9ce283995edfa783184da68d8a1df9b6ad307`; the resulting report is valid (SHA-256 `57aaa1ee394eccc5979c26c8c3a078132741c4d37fcaea5c21d22ab5dc822516`) and leaves both proposed proof rows unreviewed/unadmitted. It does not increase or replace the 313/328 batch accounting. Fifteen leaves, formal proof admission, independent review, and admission remain open and unwaived. |
| G4 | JSON union arm selection preserves source scalar type. | Retained `PROVISIONAL-SCALAR-UNION-252.json`; 14 outcomes. | Number/string/bigint/null body-byte and plan-fingerprint controls. | Open |
| G5 | Fixtures prove source semantics rather than accepting generic placeholders. | `FIXTURE-SEMANTICS-252.json`; invalid group role/missing selector recorded. | Negative controls for invalid type/value/count/durable stage. | Open |
| G6 | Structured-query cohort preserves source serialization distinctions. | `QUERY-PREREQUISITES-252.json`; cohort not completed. | Matrix-driven direct/saved/continuation wire pairs. | Open |
| G7 | Older dispatch and binary contracts remain executable. | `FINDINGS/CP18-240-*.md`; excluded from 252 batch. | HEAD/204/binary/Conan exact production-path controls. | Open |
| G8 | Saved modes, source classes, and stopped selection are reconciled. | `PAUSED-265.md`, `INCOMPLETE-SELECTION-265.json`: 553 unstarted + 1 interrupted; MLflow and source-class packages not started. | Mode-correct MLflow, source class 1752+2, explicit membership and lane proofs. | Open |

## Firstmate277 G3 admission-closure subledger

| ID | Obligation | Red evidence to retain | Green required before the candidate can be frozen |
| --- | --- | --- | --- |
| G3-A | Canonical coordinates | The retained candidate source-lane receipt records `canonical_generation_mismatch` and `execution_generation_mismatch`; it is diagnostic, not overwritten. | A focused isolated canonical producer/check demonstrates a matching source → canonical → execution coordinate for the exact action and both lane bindings. |
| G3-B | `key` source provenance | The original target added an unsupported `maxLength:8192` to the bare source `key`; the retained pre-correction source-lane result records its incompatible comparison. | Green — the source-derived request schema now retains the exact bare string contract; `lock-render`, `--check`, and definitions validation pass with no invented provider bound. |
| G3-C | 200 typed response | The candidate action has no typed response declaration despite retained source response `APIEntitiesCiVariable`; source lanes report `target_response_contract_unverified`. | Public direct and saved write consumers prove typed success decoding plus source-relevant error and malformed-response rejection. |
| G3-D | Independent review intake | The production source-lane producer hard-codes an empty review catalog, so it cannot consume a normal external review record. | Test-scoped external review catalog fixtures show valid acceptance and missing, stale, or coordinate-mismatched rejection; production has no synthetic review. |
| G3-E | Formal proof proposals | No source-lane executable proof is yet admitted for either candidate command. | Two complete portable proposals bind the direct-write and reverse-ETL selected leaves to exact identities, receipts, target coordinates, coverage, and limitations; a frozen reviewer-output contract is documented. |
| G3-F | Cross-role typed-write reconciliation | `SOURCE-LANES-RESPONSE-BINDING-AFTER-IDENTITY-277-281.json` reports `target_schema_unverified` for the request-side `$schema` document and re-reports `target_parameter_mismatch` / `target_path_projection_mismatch` for the response-role binding despite its paired request binding. | Green — `TestBatch1CP18GitLabAdmission277CrossRoleWriteProjection` passes fresh, including its missing-map refusal control; the exact post-correction report is valid with zero errors and retains only `proof_unavailable` for direct write and reverse ETL. |

No historical RED is relabelled as a fresh post-repair RED. Every repair records command, selected tests, input/source hashes, candidate SHA, exit/result, and a fresh `-count=1` result in `RUN-267.md` before it is called Green.
