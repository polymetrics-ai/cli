# CP18 TDD ledger

## Baseline
On production base 45c235dda353dc9e30838e860acac0aa5ebd2e5c, `go test -json -count=1 -timeout 20m ./internal/cli ./internal/connectors/defs/gitlab -run '^TestGitLab'` exited 0. Receipt baseline-gitlab.jsonl includes 18 passing test/subtest events, no fail/skip. This is limited existing coverage, not full connector certification.

## MLflow second-page retrieval failure — CP18-05/09/12
Red: `go test -json -count=1 -timeout 20m ./internal/connectors/defs/gitlab -run '^TestGitLabMLflowMetricsHistoryETL(SecondPageFailureDoesNotMaterialize|MaterializesThroughDuckDB)$'` exited 1. Healthy two-page control passes; new regression reaches successful page one, then page_token=page-two 502. Run fails but QueryTable returns the first metric (`accuracy`, step 1, timestamp 1000, value 0.5). Raw original receipt mlflow-second-page-original.jsonl is retained in allocated task evidence. Shared runtime unchanged.
Green: absent. Shared warehouse/transport owner must reconcile required non-materialization with current per-batch full_append commit semantics. The regression remains intentionally failing, not skipped or rewritten.

The healthy control was subsequently strengthened to compare every metric field and exact membership (sorted canonical JSON), replacing count/key-presence assertions. `go test -json -count=1 -timeout 20m ./internal/connectors/defs/gitlab -run '^TestGitLabMLflowMetricsHistoryETLMaterializesThroughDuckDB$'` exits 0 in mlflow-exact-control.jsonl. This is stronger existing-behavior proof, not repaired GREEN for the failing case. Original failing receipt remains bound to the preceding test revision.

## Authoring consistency
Unchanged schema4 lock rendered in retained isolated authoring root and `lock-render --check` exited 0. All 399 integrity-listed files verified by size and SHA256: 394 execution files byte-identical to corpus, five publication/provenance members not copied. Integrity file itself is additional publication control. No shared `gen`, execution transfer or source mutation occurred.

## Independent existing-behavior witnesses — Firstmate196/198

No production repair is claimed for these new coverage tests. Red: the original MLflow page-two regression above remains unresolved; no unchanged failing case was rerun. Green: existing supported behavior passes the following fresh, uncached test executions using the warm Go build cache:

- `go test -json -count=1 -timeout 20m ./internal/cli -run '^TestBatch1CP18GitLab(GroupsWarehouseExactPages|GroupOracleRejectsWrongReadableRows)$'`: exit 0; exact 51 independently retained groups across 50+1 records and two source requests, queried through reopened App/DuckDB. Oracle controls reject omitted members, wrong IDs at equal count, wrong path bytes, duplicate substitution and extra rows; permitted row reordering/numeric representations pass. Receipt: groups-exact-pages-196.jsonl.
- `go test -json -count=1 -timeout 20m ./internal/cli -run '^TestBatch1CP18GitLabDirectLinkPagesPreserveWire$'`: exit 0; real generated command/parser, exact GET /api/v4 bridge, Bearer header, empty body, search/sort query, two returned rows plus one continuation row, complete/has_more/cursor state, and zero extra requests for refused page-number/malformed-cursor navigation. Receipt: direct-link-198.jsonl. This does not prove all optional flags or auth modes.

Raw JSONL and command/exit/hash receipts remain in the allocated private task directory. The binary fixture is only synthetic preparation, not binary execution evidence. Source observations record retained HEAD body/query conflict and Conan missing structured body without choosing shared lowering semantics.

## Firstmate214 mode decision and planned witnesses
Firstmate212/214 binds CP18-05 to explicit legacy full_refresh_overwrite. The original full_append assertion/RED remains at adf31f10 and mlflow-second-page-original.jsonl, dispositioned as mode-expectation mismatch, not a runtime repair. Before running revised evidence: canonical append must retain exactly acknowledged page one and ordinal-one committed checkpoint across reopen; explicit overwrite must preserve sentinel Parquet bytes and stream state after reached page-two502, create no table on first failure, and publish exact two healthy metrics. New tests are contract reconciliation/coverage, not manufactured RED/GREEN. Firstmate215 permits diagnosed local setup recovery without treating it as product evidence.

First214 attempt receipts: mode-witness-214.jsonl and corrected/state-diagnostic/final variants are retained, including failed setup expectations. Legacy overwrite adds three metadata fields (local_warehouse.go), unlike canonical append. Its seeded legacy state is normalized by migrateLegacyTransportReceiptAssociation (stream_state.go); Open defers writing this normalization until the next state mutation. The test explicitly accounts for that exact version1/legacy-checkpoint migration while comparing every other state field. An intermediate fence increment expectation was incorrect and removed after inspecting the exact serialized before/after states and legacy route. These are oracle corrections, not product RED/GREEN or runtime changes. Canonical append and exact healthy controls passed throughout; final overwrite result is recorded separately.

Verified overwrite: `go test -json -count=1 -timeout 20m ./internal/connectors/defs/gitlab -run '^TestGitLabMLflowMetricsHistoryETLExplicitOverwritePublication$'` exits0, both first-run and seeded subtests pass; raw mode-witness-214-overwrite-verified.jsonl and hash receipt retained. This is fresh selected-mode proof, not a runtime fix. The broader preceding mode-witness-214-final.jsonl retains passing append/healthy/first-page/preflight members plus its then-failed seeded expectation; no whole-run GREEN is invented.

## Firstmate240/248 passing test checkpoint

This increment adds production-path coverage, with no runtime repair or source promotion. Red: original discovery-cli240 retains missing-cursor fixture failures and misplaced early source-mode refusal expectations; they are setup/oracle defects, not product regressions. Corrected fixture timestamps use each actual declared stream cursor; unsupported source modes are checked at RunETL, and incremental_append remains explicit legacy compatibility. Green: corrected-fixture240 passed20 reached two-page ETL cases and one legacy admission case (23 events including two parents). Four literal source routes bind only their own stream, source-to-warehouse role and explicit legacy full_refresh_overwrite mode. Healthy IDs are101,202,303; each failure reaches page two, refuses foreign/third sends, and leaves no queryable new table.

The current passing file separates unchanged pending status/HEAD declaration witnesses. Firstmate248 requests independent passing increments; the focused passing-checkpoint248 run verifies the resulting test-file boundary, all14 source/destination mode cases,20 ETL cases and six receipt-oracle cases. Pending mapping tests stay uncommitted. The mode admission successes are not execution proof. No previously observed mapping RED is replayed. The helper falsification checks wrong status, wrong plan, missing response, failed record and invented body against a positive control; these controls do not certify any source cell.
