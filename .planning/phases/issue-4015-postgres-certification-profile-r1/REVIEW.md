# Inline code review — PostgreSQL certification profile

## Fallback

The GSD code-review prompt was resolved with `scripts/gsd prompt code-review 4015 --auto`. Its role runtime is unavailable in this isolated worker, so review was performed inline, as recorded by the phase plan's manual-GSD fallback.

## Review scope

- terminal certificate status classification and unexecutable declared transport handling;
- exact PostgreSQL polling-watermark to managed-target live adapter and approval route;
- definition-owned dynamic relation selection, direct-write no-inventory behavior, and all six mode proofs;
- evidence importer/matrix promotion, including absence of an inferred implementation without live proof;
- redaction and environment-only secret use in the evidence path;
- CLI/manual/website/golden parity and generated artifacts.

## Findings and disposition

1. **Warning, fixed:** the first boundary scan found connector-name branches/imports in shared generator/certification code. The repaired selector and importer now derive native-database details from the loaded descriptor, and the final whole-tree boundary scan reports zero findings.
2. **Warning, fixed:** the changed connector manual made the tracked golden transcript stale. It was regenerated by the project test helper and the full CLI suite passed.
3. **No open critical, warning, or info findings.** The final evidence importer refuses incomplete modes before output, and the final matrix uses an evidence-backed implementation mark rather than declaration-only promotion.

## Review result

Ready for direct PR. The exact PostgreSQL adapter remains connector-local in behavior; shared code contains only definition-selected, fail-closed selection and evidence validation.
