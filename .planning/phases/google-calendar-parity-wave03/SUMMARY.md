# Google Calendar parity resume — locally verified write-action slice

Recovered PR #3554 is now active through the declarative engine rather than shadowed by the legacy native connector. Google Calendar API v3 Discovery revision `20260731` documents 38 operations exactly: 11 GET, 15 POST, 4 PATCH, 4 PUT, and 4 DELETE.

All 38 operations are genuinely reachable: 11 fixture-backed GET streams, the bounded typed `freeBusy.query` direct read, and 26 record-driven `writes.json` reverse-ETL actions. The 2026-08-05 shared-contract correction established that these record-shaped mutations execute through the existing write executor, so none is `rest_write`-blocked or `planned`. Each write action has a schema, path/query/body mapping, risk and confirmation metadata, an API-surface mapping, an implemented CLI command, and a fixture under `fixtures/writes/`.

Citation coverage is 149/149 declared request-field uses (27 read/direct-read and 122 writes) and 38/38 operation-level sources. Focused connector validation, surface synchronization, fixture-backed conformance, command preflight, Google Calendar hook regressions, targeted CLI transcripts/manual generation, build, vet, generated docs/website typecheck, lint, smoke, boundary, and release-workflow gates pass. The updated root CLI golden transcripts now advertise the write surface. CI then exposed a stale engine transition guard that assumed no shipped connector used `batchable:false`; it now locks Google Calendar's exact ten intentional non-batchable actions while requiring every other shipped action to remain batchable. Focused engine, app enforcement, hook, conformance, validation, vet, formatting, and diff checks pass; the outer no-mistakes executor owns the next full CI run. No credentialed provider write was executed.
