# Independent CP15 rate and route proof

Firstmate166 freezes the diagnostic slice while its bounded contract decision runs. This file records independent CP15-05/06 evidence; no diagnostic source or bound test is changed.

Existing connsdk admitRequesterSend calls request-epoch admission, then the shared caller SendAdmission, then provider admission, before client.Do. Both buffered and streamed entry points use it. A new baseline matrix will observe actual local-server arrival and exact returned bytes for JSON/form/multipart/stream with caller refusal, provider refusal and allowed send. It verifies order, cause identity and absence of physical server arrival on refusal. It is a baseline preservation proof unless a real production failure occurs, not invented RED.

Retain the documented safe-read transport replay exception: these gates count logical Requester attempts, while strict mutations disable transport replay. Separate existing tests cover net/http replay, redirect and retry behavior. Do not describe this initial-send matrix as proving every physical retransmission is newly admitted.

The immutable pre-edit390-row/1039-complement ledger is now portable at LEGACY-ROUTE-INVENTORY.json, byte-identical to private inventory-frozen.json; LEGACY-INVENTORY-SOURCE-PINS.json retains all947 original Go file pins. Row line coordinates refer to29b790d7, not later diagnostic edits. Behavioral status remains pending until the complete proof map is attached.

## Current baseline evidence

- Caller SendAdmission precedes both generic provider Admission and route-specific RouteRateLimits on JSON/form/multipart/stream initial sends. Caller/provider/route refusals retain exact causes, avoid server arrival and return no bytes; allowed sends return the complete two-record byte fixture. New tests add evidence only; no connsdk production change.
- Existing Requester/DoStream retry, allowed redirect, auth replay suppression, strict mutation transport replay limits and terminal RateLimitError cases pass. Historical113-event results precede route-stage matrix expansion; completed normal117-event result binds the final matrix.
- Every pre-edit49 hook factory is checked for exact ID/connector membership and actually constructed; all7 protected native/compatibility constructors are checked against real embedded bundles, closed adapter ownership and unknown selection rejection. No database or provider execution is attempted. Same-count wrong-identity controls disconfirm count-only oracles.
- Selected GitHub hook declared-route/follow-up/auth-budget, shared engine route admission and App parked/resumed fixtures pass46 events. Tests preserve actual cause/route/counter or committed checkpoint/destination-result observations; production shared-coordinator integration tests remain build-tagged and unexecuted.

Original commands, outputs, event counting and exact pins are retained by each receipt named in RATE-PROOF-RECEIPTS-165.json. These counts include parent events, never operation coverage denominators. Final race/source reconciliation and remaining semantic inventory consumers are pending.

Final169 reconciliation: original541 production AST call occurrences equal the current541 as a path/function/callee multiset; none added or removed. Current961 Go pins versus947 original include new diagnostic tests/helpers. INVENTORY-RECONCILIATION-169.json retains current coordinates and changed pins without rewriting390 original rows/1039 lexical dispositions. Independent preservation race209/209 (cp15-independent-preservation-race-166-01, rawc7fd491563c9cc200216336e994833d62e555d9822a38fe984809c14c95445c0) is now included in the receipt map. Requester/hook/factory production owners are unchanged; later shared diagnostic changes are covered by current producer normal/race. No new transport or provider capability is inferred from census equality.
