# CP15 pre-edit route inventory

Structural census at29b790d7 is frozen in the private cp15-inventory-165/ast-calls.json and inventory-initial.json. It parses all947 tracked Go files under cmd/internal, retains1424 matched call observations (tests tagged separately), all49 generated hook IDs, all7 protected factory IDs plus api_engine, all embedded CLI availability counts and actual rate declarations. Lexical fixture/origin/compatibility complements retain false positives for explicit disposition. This is not yet semantic inventory completion.

No production changes may begin until grouped trace/disposition below is complete. Existing protected factories, authored origin configurations and auth exchange paths are not automatically legacy escape defects. Runtime legacy metadata currently stays nonexecuting; exact schema/runner refusal scope must preserve existing declarations until generator/source migration authority is proven.

Initial structural rows: 390; lexical observations: 1039. Exact expected membership/oracle checks will accompany closure, not counts alone.

## Pre-edit ownership/disposition freeze

Full row ledger: Firstmate home data/cli-batch1-pi-takeover/cp15-inventory-165/inventory-frozen.json. Every390 structural observation and1039 lexical complement observation has a source path and disposition; comments and sync.Once calls remain explicit nonexecution false positives. Exact file/call membership is retained in the AST snapshot. Behavioral proof remains pending; a retained route is not newly certified executable.

| Complete category | Source-backed disposition / owner |
| --- | --- |
| api_engine.v1 | Migrated shared execution, selected exact entry through bundleregistry.ExecutorFactories; no fallback. |
| native_database/{dynamodb,mysql,postgres}.v1 | Protected retained database contracts, no deletion or customer DB work. |
| closed_typed/{bing-ads,faker,hubspot,tally-prime}.v1 | Protected compatibility factories selected by exact executor; no automatic shadow registration; preserve fixture branches until replacement/removal authority. |
| All49 hook factories | Generated hookset complete ID list; bundleregistry validates selected extension/connector. Retain explicit extensions. No new hook transport is introduced. |
| Declared rate corpus | github declared; postgres not_applicable. All other source files absent are absent, not fabricated declaration support. Source-owned auth exchanges and old hooks lacking declarations remain explicit retained compatibility/configuration; new policy adoption must prove their route. |
| GitHub nine write send sites and installation-token exchange | githubWriteRequester → Runtime.RequesterFor; auth DeclaredRouteRequester also selects RequesterFor. Follow-ups retain their own declared route; actual final physical boundary is connsdk admitRequesterSend → client.Do. |
| Shared requester wrappers | Do/DoLimited/form/multipart/binary/JSON/status/stream all trace common policy helpers; SendAdmission precedes provider Admission/AdmitRoute. Redirect callback re-admits; strict writes use no-replay/no-redirect client policies. Existing tests to be source-bound below. |
| Other direct hook HTTP/auth sites | No current declared-rate file. Retained explicit hook routes, not eligible proof of future rate-declared capability. Base requester endpoint guard refuses an unselected rate route; no inference of an auth limiter from metadata. |
| Fixtures/origins | Complete lexical complement includes protected Bing Ads/DynamoDB/Postgres/Tally fixture branches, typed source-owned HTTP base/host configuration and SDK redirect allowlists. Protected factory scope overrides old bulk-delete text. No arbitrary runtime selector is added. |
| Legacy admission vocabulary | Schema/engine deferred helper and runner recognize deferred/unsupported_with_provider_evidence plus associated fields. Current corpus has exactly two Amazon SQS unsupported evidence commands and zero deferred; other availability states remain independently counted. These are nonexecuting metadata, not a fallback executor. CP15 must prove malformed states stay input errors and eliminate unsafe admission recategorization without hand-editing source-derived declarations. |
| Authored admission rows | declaration_admissions.json schema2 contains one GitHub implemented reverse_etl delete-label row; authoring only. No runtime file reader or second registry. |

The inventory's whole-tracked-Go scan includes command/App/CLI consumers in the raw source census, with connector-call rows plus lexical complements grouped here. Downstream selected errors, parked/resumed state and typed failure rendering are separately covered by the PLAN matrix. Names alone do not certify the route: behavioral proof and an exact-identity false-positive oracle remain mandatory before final readiness.

Frozen ledger SHA256 15feff4d70bf9515ea4a41c84a566936492493584d2d444961f79f995de0a978. Original initial inventory retained unchanged.
