# TDD Ledger — Refs #4093

| Slice | Red: test and observed failure | Green: implementation and passing assertion | Refactor / status |
| --- | --- | --- | --- |
| Neutral source | `go test -count=1 -timeout 20m ./internal/synctransport -run '^TestDefinitionConformanceVerifierAcceptsEvidenceSelectedByEachDefinition$'` failed to compile because `DefinitionFactory` has no `AcceptedSourceEvidences`; the historical shape can admit only one fixed evidence reference. `TestDefinitionTransportFactoriesRegisterSharedSourceOnce` then failed with `source transport executor "declarative_stream_source" is registered more than once`. | The conformance test, `TestDefinitionTransportFactoriesSelectDeclaredEvidence`, and `TestDefinitionTransportFactoriesRegisterSharedSourceOnce` now pass. A shared exact source executor is registered once and dispatches via `SourceRequest.Connector`; each evidence value comes from an owning declaration. | Green: focused commands passed. |
| Typed destination | `go test -count=1 -timeout 20m ./internal/app -run '^TestDefinitionTransportFactoriesSelectDeclaredEvidence$'` failed to compile because production has no descriptor-derived factory constructor. | Descriptor-derived factories now carry each destination declaration's evidence, and the typed action executor reads the preflighted destination from apply/read-back requests rather than retaining its first registered connector. | Green: focused production composition test passed. |
| Fail closed | `TestRegisterDeclaredTransportsRefusesBeforeAnyRegistration` begins with an unknown executor and leaves build/registration/read/plan/apply counters at zero. `TestBundleLoadSyncTransportRefusesUnknownOrUnsafeDeclarations` rejects malformed members. | `go test -count=1 -timeout 20m ./internal/connectors/engine ./internal/connectors ./internal/synctransport -run '^(TestBundleLoadSyncTransportRefusesUnknownOrUnsafeDeclarations|TestRegisterDeclaredTransportsRefusesBeforeAnyRegistration)$'` passed. | Green: refusal happens before adapter construction or I/O. |
| Mode validation | `go test -count=1 -timeout 20m ./internal/connectors -run '^TestDestinationTransportDescriptorRefusesChangeCaptureDestinationMode'` failed with `Validate() = <nil>` before the role guard existed. | `TestDestinationTransportDescriptorRefusesChangeCaptureDestinationMode`, `TestTransportFamilyHalfPathConformanceRefusesChangeCaptureDestinationBeforeIO`, and `TestRunETLTransportRefusesDeclaredChangeCaptureDestinationBeforeIO` pass. | Green: destination `change_capture` is forbidden; the source-only connection-warehouse route remains separate. |
| Commit reconciliation | Existing `TestOrchestratorRetiresOwnedStageOnlyAfterCheckpointCommit` repeats the post-commit uncertainty path and asserts only the committed owned receipt is retired. | `go test -count=1 -timeout 20m ./internal/synctransport -run '^TestOrchestratorRetiresOwnedStageOnlyAfterCheckpointCommit$'` passed. | Green: retry reconciliation consumes durable acknowledgement state before replay. |
| Stage bound / cleanup | Existing `TestDeferredReconciliationRetiresOnlyCommittedConnectionOwnedTransportStages` covers mixed owner/generation receipts. | `go test -count=1 -timeout 20m ./internal/app -run '^TestDeferredReconciliationRetiresOnlyCommittedConnectionOwnedTransportStages$'` passed. | Green: cleanup is receipt-owned and bounded to committed connection stages. |

Every Green entry must cite the exact focused `go test` command and observable counter/state assertion. No red result will be recorded as a pass.
