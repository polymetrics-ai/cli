# Refs #4093 — Discussion log

Inline/manual `discuss-phase` fallback. The task brief resolves the otherwise material choices:

- Source evidence is selected by each definition and admitted by a generic factory/verifier.
- Destination execution is a closed, typed action contract; a generic write surface is forbidden.
- The old GitHub App composition is deleted only after equivalent neutral-path parity.
- Crash recovery and transient-stage ownership are mandatory correctness requirements, not optional operational enhancements.
