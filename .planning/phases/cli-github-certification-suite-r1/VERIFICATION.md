# Verification checklist — GitHub certification suite r1

- [x] Fresh red test for exhaustive surface-derived sweep generation is recorded: `go test -timeout 20m ./cmd/connectorgen -run 'TestCertificationSweep' -count=1` initially failed because the generator/model was absent.
- [x] Green command test proves 1,571 unique commands each receive exactly one status/reason and totals reconcile; it also verifies that stream, typed/enum flags, and API surface come from `cli_surface.json`.
- [x] Generated GitHub sweep passes deterministic `--check`: `go run ./cmd/connectorgen certification-sweep --connector github --check` and `make connectorgen-certification-sweep` both pass.
- [x] Product defects include `releases assets view` (`asset_id` versus non-required `--asset-id`); provider refusals remain a separate non-pass type requiring a concrete HTTP status. The generated ledger reports 92 product defects and the named measured HTTP 422 refusal for `actions fork-pr-contributor-approval view`.
- [x] Scratch post-schema assertion sabotage made `direct_read_sweep_repo_read_file` red with `declared output at /response/name does not match`; exact source restoration and rebuild passed all 23 declaration-owned direct-read stages.
- [x] The live proof was serial and read-only, used only the disposable token through `--from-env`, asserted produced values, and used `Polymetrics-Cert/pm-cert-3993-20260810-wz0fru`. The isolated temporary project was moved to Trash after the report was checked; no provider resource was created.
- [x] No accepted evidence is emitted while #4198 is open; the blocked `http_exchanges` reason is recorded in `RUN-STATE.md`.
- [x] `go test -timeout 20m ./cmd/connectorgen -count=1` passes as the required consumer package.
- [x] Local commands passed: `go test -timeout 20m ./internal/connectors/certify -count=1`; `go test -timeout 20m ./internal/cli -count=1`; `go vet ./...`; `make tidy-check`; `make lint`; `make docs-check`; `pnpm --dir website run gen:docs` twice plus `git diff --exit-code -- website`; `make smoke-no-build`; `make agent-contract-check`; `make connectorgen-validate`; `make connectorgen-surface-sync`; `make github-parity-artifacts-check`; `make connectorgen-certification-matrix`; `make connectorgen-certification-sweep`; `make connector-boundary`; `make connector-canon-check`; `make release-workflow-check`; `go build ./cmd/pm`; and `git diff --check`.
- [x] The complete `go test -timeout 20m ./...` suite is intentionally left to CI: repository `AGENTS.md` forbids a single full-suite command under this per-command-timeout worker because its 550+ connector suite routinely exceeds the command window. The changed packages and consumer `internal/cli` ran separately with the mandatory 20-minute timeout.
- [x] Inline code review has no unresolved actionable finding; see `REVIEW.md`.
- [x] PR #4206 base/head pair was read back through `gh-axi pr list --state open --base integration/4015-mvp-flat-r1 --head fm/cli-github-certification-suite-r1 --limit 1`; the API-filtered result contains exactly this PR.
