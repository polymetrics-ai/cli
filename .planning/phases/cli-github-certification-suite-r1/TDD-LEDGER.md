# TDD ledger — GitHub certification suite r1

| Slice | Red evidence | Green evidence | Refactor / result |
| --- | --- | --- | --- |
| Surface-driven sweep generation | **Red observed:** `go test -timeout 20m ./cmd/connectorgen -run 'TestCertificationSweep' -count=1` failed to compile because `buildCertificationSweep`, its command model, and `validateCertificationSweep` are absent. | **Green observed:** focused generator tests pass; `go run ./cmd/connectorgen certification-sweep --connector github` writes 1,571 unique CLI-surface rows and a second `--check` is byte-stable. | The command loads the bundle surface, not a second hand-authored command list. The only retained 23-entry data is a named `certification.json` produced-value assertion overlay, not candidate identity. |
| Product-defect classification | **Red observed:** the same missing generator leaves no product-defect list, so it cannot surface `releases assets view` and required REST `asset_id` versus non-required `--asset-id`. | **Green observed:** `TestCertificationSweepSeparatesProductDefectsAndProviderRefusals` finds `releases assets view` with non-required `--asset-id` and reports no provider refusal before a provider response exists. The current static surface has 92 product-defect rows. | The comparison is generic over loaded operation metadata; shared Go never names GitHub. |
| Non-pass/provider-refusal contract | **Red observed:** no sweep validator exists to reject an unexecuted `pass`, a missing reason, a duplicate path, or a provider refusal without concrete provider status. | **Green observed:** focused tests prove all 1,571 rows have exactly one concrete non-pass classification and reject a provider-refused command without a 100–599 provider status record. | All unexecuted rows remain non-pass with machine-readable reasons. |
| Certified assertion failure proof | **Red observed after `connectorgen validate`:** scratch-changing `direct_read_sweep_repo_read_file`'s declared `/response/name` value makes that stage report `passed=false` with `declared output at /response/name does not match`. | **Green observed after exact restoration and rebuild:** the same stage passes, all 23 declaration-owned direct-read stages pass, and the report states `pass: 23 declaration-owned direct-read candidates; no whole connector command or stream surface claim`. | This read-only run used only the disposable `polymetrics-cli-cert` token against `Polymetrics-Cert/pm-cert-3993-20260810-wz0fru`, ran serially, and used produced values rather than the process exit status as proof. Scratch state was restored before continuing. |
| Evidence integration | Blocked: #4198 is open and `http_exchanges` is not available. | Deferred until its capture contract lands. | No accepted evidence record is fabricated. |

No existing test will be weakened, skipped, or mode-excluded.
