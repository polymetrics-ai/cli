# GitHub Certification Sweep Findings

Generated by `go run ./cmd/connectorgen certification-sweep --connector github`
from `internal/connectors/defs/github/cli_surface.json`.

## Product defects

The generated ledger records 92 declaration/runtime disagreements. They are
product findings, not provider responses, and remain non-`pass` until their
command declarations are corrected and independently certified.

The first finding is `releases assets view`: its required REST path parameter
`asset_id` maps to `--asset-id`, but the CLI surface declares that flag as not
required. That declaration reaches runtime as `missing path variable` when a
caller omits the flag. The full, machine-readable finding list is
`internal/connectors/defs/github/certification-sweep.json` under
`product_defects`.

## Provider refusals

The deterministic generation phase makes no provider request, so it has no
provider-refusal records. A live sweep may add one only with the provider's
concrete HTTP status and reason; it must not be reclassified as a product
defect merely because the operation did not pass.

## Accounting snapshot

The generated artifact reconciles all 1,571 declared commands: 20
`eligible_pending_live`, 1,409 `fixture_required`, 92 `product_defect`, and 50
`not_applicable`. No generated command has an affirmative `pass` status.
