# Prompt Snapshot — Windows signing foundation

## Kickoff

Promotion instruction: continue `cli-windows-signpath-onboarding-r1` as a ship task and implement one provider-inert foundation PR for #554/#555.

## Downstream artifact

- PLAN.md: created before production edits.
- TDD-LEDGER.md: created before production edits.
- VERIFICATION.md: created before production edits.
- SUMMARY.md: created before production edits.
- RUN-STATE.json: created before production edits.

## Verification result

Pending implementation.

## GSD adapter status

`scripts/gsd prompt programming-loop init --phase issue-554-555 --dry-run` returned `unknown GSD command: programming-loop`; manual-GSD fallback is recorded in PLAN.md and RUN-STATE.json.
