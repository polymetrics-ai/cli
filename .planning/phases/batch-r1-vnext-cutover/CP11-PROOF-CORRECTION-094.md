# CP11 Firstmate094 current proof correction

This additive companion supersedes only the current proof/reference projection of the immutable `CP11-ACCEPTANCE.json`. It preserves all 119 original row IDs, contracts and historical evidence. It does not accept CP11. Complete audit091 was committed at `ede1950bcbfbfff5906e604ca8008f0ad579e34c` before corrective test edits; actual oracle RED was committed at `393c0e2a997fa87d9ecb5aad73f2203cbb544af0`. Production and dependencies remain those of `8aadc1cef19ea1a0b329ec898feb07e68e96913e`; original review starts at `36e4d980de0d51d92fe74a68306845643596a6cb`.

The JSON companion binds exact source hashes, symbols/assertion locations, all historical row/group links, immutable audit/ledger/manifest hashes and current governing policy paths/hashes. Its source-location inventory is navigation, not proof by itself. The semantic inspection below and original execution receipts provide the implementation evidence. Independent review must challenge both.

## Contract-to-evidence inspection

Notation: P = independently derived old fixture pointer; N = independently derived desired new fixture pointer; JP = serialized prepared JOURNAL(P,N); JC = committed JOURNAL(P,N). Ordinary successors have exactly capture_intent → captured → selected → committed terminal, attempt1. Pending prepared interruption has zero phases. Base preparation has no predecessor and prior/intended anchors share the existing inode. New identities are acquired only after desired payload/shape comparison. Existing control and history identities are immutable expectations.

| Actual caller / phase | Independent desired state and permitted transitions | Completed effect, ownership and identity acquisition | Actual verdict / proof |
|---|---|---|---|
| Empty/nonempty prepared-new-selected Recover | Before setup Publish: CURRENT=P, JOURNAL absent. JOURNAL→JP, CURRENT→N. Recovery cleanup refusal retains JP and N. | Setup has selected N before AfterCommitSync crash. Stage acquired before rename; transaction/anchors before prepared write; descriptor-bound prepared/phase records at Write; capture directory before capture_intent. Test actor moves actual lease A and creates regular B at cleanup identity hook. | DM empty row and PT prepared row use ExpectedPublish(prepared), composed writer/recovery hooks, leaseDelta, CaptureExpectedCut, exact post-refusal tree and explicit restoration/recovery. |
| Empty/nonempty committed-new-selected Recover | Same independent setup plus JOURNAL JP→JC. Cleanup refusal retains JC and N. | Committed journal exists before cleanup; expected graph comes from explicit three transitions, never observed CURRENT. Actual A/B retained at named generation lease identity cut. | DM committed rows: ExpectedPublish(committed), wantedNew/wantedCurrent, CaptureExpectedCut, exact control/authority/root preservation. |
| Successful Publish final prune, empty/nonempty | Same three transitions as committed setup, desired N supplied before Publish; no JOURNAL clear before refused prune. | Writer actually advances P→N before stale generation lease substitution; validated N artifact bytes and integrity are bound before rename. | DM successful-final-prune uses desired new fixture and FinalPruneVerdict; empty helper requires explicit nonnil desired current. Four-control suite's correctN and coherentQ use same actual caller/verdict. |
| Fresh rejected-new Recover, empty/nonempty lease B | JOURNAL absent→JP only; CURRENT stays P. N is derived before initial Publish. | AfterStageRename crash leaves valid N plus JP. Fresh recovery reaches rejected-new cleanup before any control successor. Original N lease A and actor B update only their explicit fixture entries. | DM FreshRejectedNewRecovery: ExpectedPublish(rejected), same plan spans initial Publish and fresh Recover, journal New compared to desired N; CaptureExpectedCut before restoration. |
| Immediate rollback, empty/nonempty lease B | JOURNAL→JP, CURRENT P→N, CURRENT N→P; no committed journal. | Real active validation rejects broken fixture after staging/selection; rollback has restored P before rejected-N lease cut. | DM ImmediateRollback: ExpectedPublish(rollback), three explicit transitions, fixture-derived broken N, exact expected graph/tree before restoration. |
| Held reader Publish / Recover / pure Prune | Publish B→C permits JP/selectedC/JC/clear and pruning only known unheld B. Recover A→B permits JP/selectedB/clear. Pure Prune has no control delta. | Held generation A remains tied to actual original lease/root witness. New C/B identities are acquired through composed hooks. Explicit pruning removes only B's expected subtree; actor A/B changes are separate from publisher transitions. | PT HeldGenerationUsesStableCleanupLock: plan.check whole tree, plan.controls, immutable prior history and heldExpected; independently desired B/C compared to returned pointers. No overwritten observed-control snapshots. |
| Static Prune / no-journal Recover / Open / Publish initial recovery | Explicit CURRENT from known fixture, JOURNAL absent, zero control transitions. | Late replacement occurs in initial cleanup, so no final-prune claim. Prior tree/history retained and actual lease A→retained/B delta recorded. | PT static public cleanup callers and DM static empty rows: empty ExpectedPlan, leaseDelta, CaptureExpectedCut, exact refusal tree/control checks. |
| Early staged Recover, then Check | Zero control delta. Original owned stage A marker/sentinel from fixture; actual relative identity/type/bytes/membership retained before operation. | BeforeStageRemoval moves A outside connector and creates B; stageDelta validates moved A against premove witness and fixture-valid B. No cleanup mutation is allowed after identity refusal. | PT RefusesReplacedValidatedStageCleanup uses EarlyStageVerdict for both connector cut and external A after Recover, then again after nonmutating Check, before restoration/retry. Two external-A counterexamples call that same nonfatal verdict. |
| Late staged Recover | Zero control delta; original stage A and actor-created empty directory B remain distinct. | Existing late hook moves A; replacement B is checked to be exactly one directory member; stageDelta preserves A and expected B separately. | PT RefusesLateReplacedValidatedStageCleanup uses composed empty plan and exact validated cut, existing restoration/Recover/Publish/Check path retained. |
| F03A preparation-frontier matrix (seven real Write/Sync/Close/post-record frontiers) | Explicit requested JOURNAL absent→prepared(New=P), CURRENT P→P, JOURNAL prepared→absent. Exactly one new prepared transaction, zero phases at interruption. | Anchor acquisition precedes prepared write. Full writes with failing completion retain owned prepared/anchors; short unexposed write keeps original proven-owned cleanup assertions instead of claiming durable prepared. | G2 PreparationFrontierMatrix supplies pure desiredOld/control bytes before writer call. PendingPreparedVerdict first checks independent plan, then retains decoder/identity/coherence checks, old history and fresh recovery/retry. |
| F03A base present→present, CURRENT / JOURNAL | Desired existing fixture payload→same payload; no predecessor and same inode for both anchors; zero phases at cut. | Fixture removes graph before constructor; retained existing public inode anchors are compared before new prepared Write. | G2 BasePresentPresentAuthorityRecovers supplies base:true plan, exact graph/member/phase checks, recovery and retry. |
| Remaining control states | CURRENT absent→P, P→absent; JOURNAL prepared→committed(New=P), absent→absent. Every target/prior/intended byte string is declared before operation. | Real prepared Sync completes and returns injected error; new prepared retained without phases. Absent→absent is still an actual prepared transaction. | RR OwnershipRemainingControlStates supplies plan to the third and final pending-prepared helper consumer, validates full authority/history and fresh recovery. |
| Nonmutating Check siblings | No transition or new lease claim. | Check only validates current graph/public selection; pending/divergent state is refused without cleanup. | Existing CheckIsReadOnly and CheckRefusesPendingJournalWithoutRecovery remain in the focused closure and original full-range coverage. |

Source abbreviations resolve to JSON source bindings: E=`vnext_publication_expected_test.go`; DM=`vnext_publication_durable_matrix_test.go`; PT=`vnext_publication_test.go`; G2=`vnext_publication_group2_original_test.go`; RR=`vnext_publication_record_result_test.go`; all under `cmd/connectorgen/`.

## New helper/state/edge inspection

The representation is test-only and limited to the audited schedules. FixturePointer uses existing pure generation/integrity transforms on cloned inputs. NewExpectedPlan checks both declared public heads, records the complete prior connector tree and validated prior graph heads, then clones ordered desired transitions. ExpectedPublish enumerates prepared, committed, complete, rejected and rollback schedules; observed transition counts never select the schedule.

BeforeControlRepairRecord acquires exactly one new transaction with exactly the expected anchor membership and payload; prior/base anchors retain the expected inode. ControlRecord.Write compares exact independently built prepared/phase JSON before binding the opened record descriptor. Capture acquisition requires one empty directory at attempt1. Phase references bind expected bytes plus acquired identity; predecessor and selected state come from the validated prior head and independent step. Final comparison rejects missing/extra authority, changed history, wrong payloads, roots and membership. Whole connector expected state includes legitimate generation creation and explicitly permitted pruning; it is not merely an authority subset.

BeforeStageRename validates every staged fixture file, integrity, owner marker, regular empty lease, nested directories and exact membership before binding actual stage identities and rebasing to the desired generation name. leaseDelta validates retained actual A and actor B type/distinct identity; stageDelta compares moved A to premove evidence and imports only the explicitly validated B fixture. EarlyStageVerdict separately covers external A, which cannot be inferred from connector-only snapshots.

An oracle disagreement is recorded in plan.problem but never returned into production execution by the composed hook. Original fault callbacks and actual Write still run. This preserves the claimed actual failure cut for counterexamples. CaptureExpectedCut rejects a nil plan and validates the independent plan before taking the snapshot used for subsequent exact-preservation checks. PendingPreparedVerdict has no presence-only fallback: all three production-proof callers plus the negative control supply explicit plans.

Owner inspected the migrated independent inputs, acquisition hooks, caller order and final assertions above against the final test source. Existing first-effect ownership/error-classification contracts are unchanged production at8aadc1ce and remain linked through all119 original rows. Their previous independent dispositions are preserved, while the required final whole-package tests and independent original-range review remain outstanding. This inspection is not an independent review.

## RED, correction and GREEN chronology

Original RED receipt `receipts-094/oracle-red-receipt.json` records four missing rejections through actual caller verdicts, Go exit1, wall9.645836875s, with correctN/correctpayload and genuine movedA positives. The extraction diff and test hashes distinguish that extraction from production repair. Those bytes are committed393c0e2a; this is a test-oracle counterexample, not a demonstrated production corruption.

The first complete migrated normal development closure passed in51.207315292s. Tightening negative assertions then produced a preserved focused failure (`focused-normal-receipt.json`, wall50.330411083s): Q was correctly rejected by the earlier JOURNAL replacement intent check, while the new assertion incorrectly expected later stage detection. The assertion was corrected to require that precise earlier JOURNAL semantic mismatch. No production/test oracle semantics were changed for this reason adjustment. The subsequent test-inclusive lint found QF1003 in the new helper: a label if/else chain was mechanically changed to a tagged switch without changing its cases. Those earlier green receipts remain historical. Candidate-ready focused normal/race receipts use `focused-*-ready-receipt.json`; final whole-package receipts must separately bind the committed candidate.

## Findings and policy disposition

CP11-089-01: complete caller migration and actual coherent-Q / well-formed wrong-prepared-payload controls implemented; independent review pending. CP11-089-02: exact external A witness and changed-nonempty / same-byte-different-identity controls implemented; independent review pending. Both retain low demonstrated direct impact and mandatory proof status until acceptance. No new production defect was found by091.

CP11-089-03: current immutable087/088 ENGINEERING/QUALITY and VERSION bindings are in the JSON companion. The unresolved original080 hashes remain verbatim in the original manifest and audit. Current policy copies are not recovered080 bytes. This is an evidence-only current-reference correction, with the historical limitation retained.

## Validation and scope

Focused closure is the22 selected top-level tests from the eleven cut groups, existing oracle controls, all three pending-prepared consumers and post-record coherence, plus the four new actual-caller controls. Receipts record actual run/pass/fail inventory, uncached command, start/wall/exit, raw hashes, source hashes and unchanged-input comparison. Final package normal/race, test-inclusive vet and original-base introduced lint are required. Atlas revision43 adds only the four oracle proof references; existing physical publication guarantee mappings remain unchanged.

GSD discuss/design authority is complete091 as adopted094. Discuss/plan --tdd/execute/verify-work were resolved through installed scripts/gsd sources/prompt. Execution remains the recorded inline non-Pi single-writer fallback; prompt generation alone is not verification. New acceptance is reserved to Firstmate after independent exact-SHA review. No CP12, pipeline, publication, provider-live, Linux-runtime, power-loss or whole-programme certification is implied. Historical CP29 boundary-package and global lint debts remain distinct; runtime-preflight remains explicitly cached. Supplementary evidence may be reused only with an exact unchanged-input mapping and owning-gate permission.
