---
phase: cp14-deferred-visibility
type: tdd
status: candidate_ready_for_independent_review
source_base: 029c1af4968231f027e1c7f91ef75f2a744f8cc2
---
# CP14 typed deferred visibility and carried corrections

## Task Delivery Header

- Primary issue: https://github.com/polymetrics-ai/cli/issues/4364; parent https://github.com/polymetrics-ai/cli/issues/4325.
- Existing delivery: https://github.com/polymetrics-ai/cli/pull/4294; API verified base main/813f457a925f7ee3fe3bea101a43e445992c8552 and head R1/029c1af4968231f027e1c7f91ef75f2a744f8cc2 after normal push.
- Working branch: fm/cli-top100-declaration-batch-r1-cp14; merges into fm/cli-top100-declaration-batch-r1, never main under worker authority.
- Integrated starting HEAD029c1af4968231f027e1c7f91ef75f2a744f8cc2/tree043b034f664439948ac22c58c2307d0d5a81874e. Integration is not acceptance: CP13 has two required open corrections, CR-158-01 and WR-158-01. CP11/12 acceptance148 remains scoped to b41c6eaa.
- Sole owner: cli-batch1-pi-takeover, Codex gpt-6-astra/medium, native01a07bcc-454f-75a3-89f3-8bbb91572206. Reviewer158 and architect150/154 are terminal; no active owned validation or competing writer observed. Preserve all protected cache/untracked/ignored/history.
- Authority: Firstmate156/158 transition; current Firstmate-authored CP14 brief and complete carry ledger in Firstmate home. All seven original CP14 items remain mandatory. No new runtime foundation, receiver, credential/provider/database exercise or generic routing.
- Delivery gate: coherent implementation/behavioral evidence, exact-source independent Astra/xhigh review supplied by Firstmate, then156 integration/carry policy. Full programme CI/no-mistakes remains at its assigned boundary.

## Single obligation manifest

| ID | Required outcome | Initial relevant seam / evidence to establish |
| --- | --- | --- |
| CP14-01 | Discovery/preflight from generated safe metadata/resolver results; no runtime authoring-source reader | Existing command surface, bundle metadata, registry, source projection/generation. Trace actual public consumers before selecting minimal bridge. |
| CP14-02 | Exact source ID/lane/cited fact/stable reason/named capability for every deferred cell | Independently enumerate current source identities and retained generated dispositions, not historical6790count. |
| CP14-03 | Mapped-unproven, recognized incompatible and missing-foundation remain distinct; malformed selected data remains error | Real resolver/load/preflight consumers; valid, deferred and malformed controls. Certification cannot gate valid execution. |
| CP14-04 | CLI/App same typed reason before credentials, transport construction or provider work | Actual CLI and App callers with reached boundary counters and legitimate supported positive control. |
| CP14-05 | Reject hidden writes/deletes/removes, unknown capability/citation omissions/duplicates/fabricated execution | Exact expected membership, same-count wrong-key substitution and source-supported mutation/remove fixtures. |
| CP14-06 | Missing-foundation visibility cannot dispatch through invented actions/generic HTTP/legacy fallback | Real runner/engine target refusal; valid supported declaration reaches correct approval/credential boundary. |
| CP14-07 | Exact-SHA behavioral evidence and independent review | Meaningful RED/GREEN, full required identity union, affected tests/race/schema/generation/docs; all ten lenses/five evidence safeguards. |
| CR-158-01 | Add independent admission-derived required cells to emitted K | SourceFoundationRegisterCurrent plus completed admission witness. Permanent real-consumer outside-baseline regression; preserve citations/pins/baseline flags/unrelated rows. |
| WR-158-01 | Prove valid same-file second-operation cannot borrow body/auth fit | Two actual canonical operations, independently fixed source/route/schema/mechanism facts, valid positive and reached sibling negative through requirement/register/CLI/adopters; intended/local branch as applicable. |

## GSD method and source reconciliation

Resolved/read scripts/gsd sources discuss-phase and generated discuss-phase cp14-deferred-visibility, official20297a8ff941378b8615a5d3e8629e52c10a0f9d. Execute inline under the established non-Pi single-writer fallback; no invented programming-loop command or additional role. Adapter was already checked; reviewer158 retained the existing unrelated issue122 scaffold doctor failure without relabeling it passed. Plan-phase --tdd follows actual source reconciliation before production changes; execute-phase/verify-work/gaps/code-review retain their installed sources and evidence.

Read live issue4364, full current prepared CP14 assignment and advisory142 report. The report's657ff243 source,6790historical count and missing decision-summary are not current runtime proof. No .codegraph directory exists. Initial read confirms existing CommandFoundation/Unsupported metadata and exact-target deferred engine preflight; commandrunner distinguishes deferred/unsupported/implemented but actual full source/lane/citation coverage and public parity remain to reconcile. The existing App deferred test reaches missing-binding refusal, not yet a complete valid deferred zero-I/O control.

Use accepted Atlas owner/constraint/proof lookup before proposing a foundation change. Reuse the existing generated metadata/closed resolver/runner architecture; no separate runtime registry. Escalate only a concrete consequential contract contradiction under a Firstmate-authored Astra/xhigh scope. Do not treat absent certification as proof of absent implementation.

## Required skills and parity

Reuse already-read current Go how-to, CLI, testing, error-handling, security, safety, design-patterns, structs/interfaces, context/concurrency, documentation/lint; connector-lane-build-order, GSD adapter and exhaustive-review evidence requirements. Refreshed how-to, required routing and CLI parity for this intake. Load additional applicable design skills only if website implementation requires them.

CLI/help/manual/website/generated discovery parity is an explicit CP14 obligation if those surfaces change; no automatic N/A inherited from authoring-only CP13. Determine actual help topic/namespace/output consumers before edits, then verify contextual help, invalid actions, JSON/stdout/stderr and canonical docs links. No docs/provider I/O or dependencies.

## Verification boundaries

No CP14 RED/GREEN yet. Initial inspection and inherited passing receipts are not new behavioral proof. Capture actual source/test/dependency/configuration pins, command, full output, selected identities and terminal exit. Failing regressions must reach the real production consumer with valid setup; meaningful helper-internal side effects and independent expected identities/bytes/counters are required. Preserve setup failures, compound causes and all failed attempts. Use timeout20m for Go tests, measured partitions with exact parent/event unions, and internal/cli separately. Broaden only for changed dependencies. Keep CP15 cleanup and CP16 vertical implementation out of CP14.

## Firstmate159 adoption

Full159 assigns this complete CP14 scope after verified integration; it preserves the existing intake. Both carried corrections are part of this checkpoint. CR-158-01 requires a permanent pre-fix actual-consumer RED then same-expectation GREEN; WR-158-01 may already be correct production and must be labeled a coverage extension unless a genuine defect is reached. No manufactured RED, extra CP13-only review or bulk202-receipt replay. Commit this obligation/review ledger before behavioral edits.

## Executable carry slice: CR-158-01

Plan-phase --tdd and execute-phase sources/prompts resolved and read at the same official adapter version above. Inline fallback remains sole-owner, non-Pi execution. No new dependency or runtime foundation.

1. Add permanent actual source-demands regression using the complete retained fixture and a separately pinned baseline without the admission-required cell. Establish positive control, then assert exact known-cell membership, observed source citations/document pins, baseline overlap deduplication and preserved baseline flags. Expected identities come from the independent fixture/control, not authored assessments. Capture genuine pre-fix failure with unchanged production.
2. In buildSourceFoundationRegisterCurrent, union completed admission affected identities into the existing independent required-cell selection. Reuse current observed source rows for state, citations and document pins; baseline rows keep their flags. Do not consult assessment membership or intended argv.
3. Rerun unchanged expectations, focused adjacent admission/register controls and race. Preserve every failed attempt and raw receipt. Update TDD/verification; correction remains pending the combined CP14 independent review.

Inline plan check: this changes only authoring known-obligation output; existing reader custody must complete before the union. It does not alter U/A, requirements, source lane authority or runtime execution. The fixture must prove the admitted cell is outside baseline and the command reaches complete admission, so a parser/setup refusal cannot count as RED. WR-158-01 and all seven CP14 items remain separate pending tasks in the single manifest.

## Executable carry slice: WR-158-01

Extend the retained combined fixture with an opt-in second canonical DELETE operation at /other in the same operations.json; its independently retained source has no request body and explicitly no auth requirement. Keep the original structured POST/auth positive. Establish exact source/canonical binding validity for both operations before attempting a borrowed body/auth claim. Test actual requirement/register and source-demands rejection with valid sibling IDs/pointers/hashes, plus materialized and missing-CLI intended paths where applicable. The negative must produce no adopter output; existing legitimate positive tuples stay bound to operation0. This is a coverage extension unless a real production failure appears; first successful test run is not relabeled RED. No production mechanism change planned.

Firstmate160 read/handled and blocker cp14-wr158-fixture-159 resolved through its authored continuation. The final opt-in sibling has a real DELETE command and exact unresolved local companion on CLI absence. Source a retains its four literal diagnostics; original source b GET has one direct-read proof deficit. In the two-operation fixture source b independently contributes direct-write proof deficit, operations/1 generation error, commands/1 absent deficit and reverse-ETL proof deficit. Exact ordered tuples determine totals (original1error/4deficits; sibling2errors/6deficits). Original profiles preserve semantics. Materialized and canonical-intended negatives now reach exact body/auth mechanism errors in requirements, register and actual command; no production mechanism changed. Run cp14-real-sibling-160-01 passes all10events including original control; not RED.

## CP14-03 selected-data error slice

Current command entry paths runMaybeConnectorCommandWithRegistry and App.PlanConnectorCommand use Registry.Get, which throws away Resolve errors and reports unknown/not-found. Plan actual-consumer RED using the real engine loader over an embedded valid connector with only operations.json malformed, plus a compound sentinel cause; preserve exact selected-data error through CLI/App before credential work. Positive controls load the original valid bundle and reach real help/required-input preflight. Replace those selected-command Get calls with context-aware Resolve and wrapped causes. Existing unknown-name semantics and cancellation remain tested. This slice does not choose a new deferred source selector or metadata representation and does not implement CP15 diagnostics cleanup. Full CP14 source-only selection bridge remains to reconcile because all30401current source cells have no admitted references.

## Firstmate162 adopted source visibility implementation

Firstmate162 resolves cp14-source-cell-selection-160 and adopts complete decision161 (54081 bytes, SHA256 e5da3565118803f2f008b05df27aea4f7bf28c470d1beb5a626c57dc005b5fee). Continue existing inline GSD execution; unchanged required skills reused, routing/parity and execute/verify/review command sources refreshed. Architect161 is terminal and ineligible for final independent review. No new writer.

Use generated immutable source payloads attached to the existing manifest/registry, with selected-only strict decode and an Atlas-derived closed metadata enum. Runtime never reads authoring inputs. Exact inspect tuple is connector/inventory/source ID/lane; discovery and inspection are informational, preflight returns typed mapping/foundation/source-lane incompatibility. Preserve all4343 source keys and seven lanes, identity citations for every operation, explicitly unresolved lane evidence, independent tuple equality, and32MiB per connector/64MiB total budgets. Valid ordinary commands remain independent of source metadata. Include normal inspect/help/man/docs selected-loader cause preservation under CP14-03 with actual loader regression, cancellation and unknown controls.

The single nine-row obligation manifest above remains authoritative. Its initial seams are now resolved by the full adopted design; the following complete matrix binds implementation and evidence. No design assertion is GREEN.

## Executable acceptance matrix and real TDD order

This table specifies tests to implement and execute after adoption; none ran here. Use real `connectorgen` public command entry and real `cli.Run` routing, plus exported App consumers. When a new Go type does not exist before implementation, avoid presenting a compile failure as RED: begin with existing CLI/generator entry points and independently expected JSON/identity assertions, then add App/type tests once the interface compiles. A public-generator assertion that the current output lacks required source cells is semantic RED; an import/setup failure is not.

| Case / independently owned oracle | Actual consumer and required assertion | Frontier / positive control | Evidence treatment |
| --- | --- | --- | --- |
| Full cohort union, all seven lanes, supplements and NA | Real generator → generated metadata → CLI `--sources` and App ConnectorSources. Compare full tuple set and canonical lane set against retained cohort **and separately maintained** expected-ID fixture. Check values/citations/state, not merely totals. | Zero execution resolver loads for discovery; existing root metadata/help still works. | New missing projection/discovery is expected semantic RED. Current snapshot equality is only an inspection control. |
| Same-count wrong key and duplicates | Mutate one actual fixture source key in emitted projection to a different valid key, preserving counts; also duplicate an exact tuple. The generator must name missing/unexpected or duplicate identities against its independent expected set. Runtime selected validation must reject the key-digest mismatch or duplicate; a digest alone cannot name the missing key, so do not claim that it can. | Unchanged fixture emits the full expected union. | Existing source-lanes same-count controls are already-correct coverage; new projection consumer falsification is distinct. Never regenerate expected keys from the mutated output. |
| Mutation/remove retention | Real Asana removeCustomFieldSettingForGoal and independently selected DELETE/write identities survive generation, JSON, text and App selection for all relevant lanes. Include supplement binary IDs by full inventory. | GET/read and NA entries also survive. | New public visibility RED; literal spelling coverage supplements complete identity equality, not a replacement. |
| Mapping unresolved, including no lane FactRefs | Real facts_unresolved cell selected through CLI `--preflight` and App method returns identical mapped error, exact identity citation, explicitly absent lane proof, nullable capability ID and stable reason. | Direct-write Asana example with positive lane facts has same mapping class for a different factual reason; ordinary supported command control reaches its boundary. | New behavioral RED; never inject a dummy resolver that always returns the desired error. |
| Existing scoped foundation gap | Real Vercel createWebhook/sync_transport tuple returns missing_foundation, exact existing gap and available owner Atlas ID, retained citations/owners; no executable fields. | A current valid normal command still preflights under the same registry. | New public source selection RED; no receiver test/service. |
| Recognized incompatible source/lane | Real retained mutation selected as direct_read returns source_lane_not_applicable / scope source_lane with source exclusion citation. Same source direct_write returns mapped, not incompatible. | Known source applicable lane provides the real positive classification control. | New public incompatible case, not a malformed sync plan disguised as C3. |
| Existing sync C3/C4 distinction | Real syncplan.Resolve valid/exact incompatible/foundation-gap fixtures; real synctransport.Preflight allowlist refusal and valid descriptor/registered executor counterpart. Assert exact variant/cause, no relabeling. | Retain actual valid executable-plan control. Do not use invalid contract setup as the incompatible oracle. | Coverage controls in existing owning packages unless an actual changed path fails semantically. Not evidence of a new CLI/App sync-plan selector. |
| Unknown capability, mismatched cited capability/gap, missing identity citation | Mutate one real projection/authoring fixture through real generator or selected catalog decoder. Unknown actual/candidate IDs, forged known-gap ID/predicate, wrong document/pointer/hash and missing operation citation fail. A lane citation to an unrelated operation also fails generation. | Exact original bytes pass; null unresolved ID passes and does not claim a missing executor. | New strict-consumer RED where absent. Original source validation may already reject some authoring cases; label them controls. |
| Empty lane citation versus required citation | Real facts_unresolved permits identity-only citation with explicit scope. Removing cited source exclusion or known-gap lane facts is invalid. | All4,343 operation identity refs are independently checked; do not require fabricated lane citations for6,127 unresolved cells. | Contract test from ISSUE-161-01; no false fail-everything validator. |
| Hidden operations by filter/proof failure | Remove a source, delete/remove spelling, supplement or NA cell; vary proof deficits without changing retained membership. Public catalog must reject missing identity or keep member visible respectively. | Complete untouched generation passes with current proof deficits. | New projection RED; distinguish malformed source consistency from missing proof. |
| Fabricated executable artifact/route | Mutate payload with action/body/URL override, `runnable=true`, fake admitted refs or implemented-with-empty-refs. Strict schema/gen reject; normal parser refuses invented source-ID-as-command path. | Actual embedded implemented command uses existing runner, not visibility metadata. | New schema/public negative tests; no mocked `Execute` boolean assertion. |
| Malformed selected source payload and compound cause | Take a real generated payload, corrupt its JSON or a required cell/citation; actual CLI source selection and App method preserve SourceVisibilityDataError. Wrap underlying real decode error with errors.Join(sentinel,cause) at the relevant test seam and assert errors.Is/As. | Untouched payload selected through same entry succeeds in inspection and yields its honest preflight disposition. No all-connector eager parsing. | New consumer RED; do not return a stub error without invoking the real decoder. |
| Malformed selected execution bundle | Retain actual engine.Load over embedded GitHub with only operations.json malformed and a compound cause, through existing ordinary CLI/App command callers. Assert cause and file-specific error, not unknown/deferred. | Unmodified bundle's real selected command reaches required-input/credential boundary; unknown and cancellation remain zero-load. |172cf0cc already locally green. Re-run only appropriate affected controls; no invented new RED, no carry-review restart. |
| Invalid/ambiguous flags and cancellation | Actual Run with incomplete/repeated/bare/unknown selector flags, unknown tuple, missing inventory, controls and cancelled context; App invalid selection/cancelled context. Assert stable errors and exact zero counts. | Complete quoted/equal-form tuple works; distinct inventories never alias. | New parser/consumer tests, retaining existing unknown/cancel controls. |
| No side effects on source selection | Actual CLI route with failing/counting App opener, credential reader, stdin, transport factory, provider RoundTripper, state/warehouse writers; App method on registry-only fixture with nil vault/state plus counters at helpers that could do I/O. Assert every frontier count zero on deferred/incompatible/invalid/data-error. | Separate actual normal command using the same production router/registry reaches the intended credential or approval boundary, and a controlled fake-server supported read returns expected records where that existing harness is available. Prove witness counters can fire. | New public boundary RED where route absent; nil fields alone are not sufficient positive evidence. No credentials/provider-live needed. |
| No source-proof gate on supported execution | Corrupt/unavailable source diagnostic payload in one registry fixture while keeping a real rendered supported command valid. Ordinary command still reaches its existing credential/approval frontier; only source catalog selection fails. | Real bundle and real runner, no alternative fake executor. Assert record count/body for fake-server control, not exit0. | New anti-gating control; existing command support is not new implementation proof. |
| Help/manual/JSON/website parity | Actual `pm help connectors`, bare `pm connectors`, `pm connectors inspect --help`, `pm connectors inspect asana --sources --help`; exact source discovery/error JSON; regenerated docs and exposed website copy. | Namespace/help exit0 with no source selection and no I/O. Unknown action remains usage error. | New surface tests and deterministic doc checks. Do not count printed command text as reachability. |

Implementation order after adoption:

1. Amend the existing CP14 plan/obligation manifest and verification with this exact scope, expected tuple fixture, citation distinction, typed contract and acceptance cases. Resolve the installed discuss→plan --tdd→execute→verify→code-review commands per current adapter. Carry existing findings without inventing their RED history.
2. Record real pre-change CLI/generator semantic RED using existing public entries for missing full source metadata/preflight. Preserve original source, test, dependency/config bytes and selected test names/results. Establish untouched actual-source fixture controls first.
3. Implement the shared safe schema/projection and strict validation, with independent complete-key/citation/gap oracles and generated whitelist. Generate from fresh validated inputs; verify deterministic bytes and actual payload budget. Retain all source deficits as observations. Green the same public-generator expectations.
4. Extend immutable registry metadata and source discovery; prove selected-only decoding and zero connector construction. Add the real CLI/App preflight consumers/error schema and corresponding RED/GREEN, then human/JSON/help/docs parity. Keep ordinary dispatch isolated from source metadata validation.
5. Run focused tests with `-timeout 20m`, appropriate race/vet/lint for affected packages and actual selected-name/result accounting. Cover `cmd/connectorgen`, connectors/manifestindex/bundleregistry/commandrunner and App; run `internal/cli` separately under project timing rules. Execute relevant source/generation/schema/help checks because their inputs changed. Reuse old receipts only when source/test/dependency/config identity and meaning still match. No blanket full-suite run per small fix; owning CI/cumulative CP16 gates remain.
6. Freeze one coherent complete candidate including both CP13 carries and all seven CP14 items. Firstmate supplies one independent Astra/xhigh CP14 review; this architect is ineligible. Apply current156 review/integrate/advance policy. This design needs adoption, not a standalone independent design review or per-slice review cycle.


## Final Atlas and verification scope

Atlas48 maintenance is a constrained extension of existing authoring.source-lock-vnext.v1 and runtime.direct-execution.v1 metadata/selection seams; existing execution contracts and stable IDs remain. Source inspection adds no shared executor or receiver. Original proof Atlas/registry pins are intentionally unchanged and current-corpus observations must report their staleness. Historical-proof fixture positives use hash-checked original bytes only in disposable roots. Saved source-manifest operations remain byte-semantically identical; only current input pins changed. Assessment maintenance updates observed Atlas pins only, never a requirement class, contract occurrence hash or proof acceptance.

Final output completion check within CP14-02/04/07: propagate every new source text writer error through the actual CLI return, and reject short nil-error writes through a narrow source-output writer guard. Establish real Run RED first, preserving the known-good full output and fault-prefix witness; no atomic stdout or new durable guarantee.
