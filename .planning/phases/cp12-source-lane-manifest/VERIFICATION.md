# CP12 verification — in progress

CP12 is incomplete and unaccepted. This is the current obligation/evidence table, not a completed `verify-work` gate or independent review. No CP13 advance, provider certification, final candidate, publication or programme-completion claim is made.

Current recorded implementation HEAD: `61d674d4f9499c683b382bf3ae14b33b479b68ec`, tree `8c51ed685f18f8b79e40441864564d3285249c46`, on `fm/cli-top100-declaration-batch-r1-cp12`. The099F parent type/schema/equality dependency is committed and the same099D specialist has resumed complete binding work from it. The complete accepted unit plan is PLAN.md; original chronology and exact raw-result hashes are in TDD-LEDGER.md. FINAL099E report SHA25646254c46d0d5a931bda32fa78bea84f069b709cd4a73db27c9810d7b07194eee supplies the selected contract. Later evidence-only changes do not alter the tested behavior; new source/test deltas require their own receipts.

Authority: `/Users/karthiksivadas/pm-cli-agent-workspace/data/batch-one-execution/REVIEW-EVIDENCE-REQUIREMENTS.md` and ENGINEERING-EXECUTION-POLICY.md. Receipts below are under `/Users/karthiksivadas/pm-cli-agent-workspace/data/cli-batch1-pi-takeover/`. Exact per-command HEAD/tree plus dirty source/test/dependency hashes bind the tested implementation; a pre-commit HEAD alone is not a complete test-source identity. A receipt is original, noncached execution unless explicitly stated otherwise. Do not relabel earlier implementations as final merged proof.

## Required obligations

| ID / invariant | Source owner, public phase and completed effect | Owner / permitted transition | Actual witness and original receipt | Disposition / remaining gate |
| --- | --- | --- | --- | --- |
| E01 exact membership | `source_inventory.go:loadRetainedSourceInventory`, called by manifest builder after roster validation; input bytes may be read | Loader owns read handles and anchored row map; duplicate/missing/unexpected input cannot alter accepted keys | `TestSourceInventoryExactSet`, `MembershipCounterexample`, `InvalidAnchor`; `receipts-cp12-097/unit04-document-node-final-controls`26/26, all exact IDs checked; separate full corpus uses retained independent4343-key fixture | Implemented bounded tests pass; final merged candidate/source binding remains required |
| E02 historical source | Same loader, envelope/artifact read and hash stage | Existing execution384/970 counts cannot replace source249/1752; source failure preserves anchored unobserved rows | `MissingSourceNoExecutionFallback`, `RawArtifactIntegrity`, `HistoricalRestoration`, `RetainsRawDocumentBinding` in same original26-event receipt | Implemented; restored immutable source history/pins preserved; no network/source refresh claimed |
| E03 seven lanes | `classifySourceLanes` and `validateSourceLaneManifest`; source membership exists before classification | Every anchored row retains seven ordered cells; artifact/proof absence cannot remove one | `ManifestExactValidation`, `ArrayEncoding`, `RetainedCorpus`; `unit04-retained-fact-omission-green`49/49 includes4343 keys/30401cells | Implemented existing cases; final merged binding failure/sibling matrix remains |
| E04 retained facts | `normalizeSourceFacts`, `validateSourceLaneFactCitations`, `validateSourceLaneRequiredFactGroups`; actual retained JSON/YAML/HTML roots exist | Builder owns immutable document/copy projections. Source facts and citations must agree; unavailable facts are explicit, never invented | Literal source fact tests recorded in TDD; `FactCitationOracle` wrong-readable response/method/hash/citation and joint omissions; `EffectiveParameterOracle` literal required query limit versus changed location/name/schema/omission. Original RED and whole49/49 GREEN receipts in TDD | Copied-group/operation/parameter plus effective security/shared schemes/webhooks/bridge/event/batch/raw path-parameter omission controls green: shared-fact-omission-green31/31 includes full corpus; raw edge controls31/31. Final source-specific paging/schema-scope reconciliation remains; no blanket all-schema claim |
| E05 applicability | `source_lane_rules.go:classifySourceLanes`; source semantics before execution availability | Preserve POST read, finite collection, mutation, binary and unresolved registration demand; Sentry is not a new receiver gap | Source-rule/literal counterexamples and `unit02-receiver-visibility-green`/`unit02-receiver-clause-controls`; retained11nonVercel+Sentry history distinguished from CP13 owner reconciliation | Bounded implemented evidence preserved; final selected rules/source checks and independent review pending |
| E06 exact references | `source_lane_bindings.go:resolveSourceLaneBindings`; canonical graph/rendered bytes and loaded bundle may exist before a failing join | Collector/builder own in-memory observations; known wrong identity cannot become accepted. No publication | Terminal099D commitsbd685ecf..50103066: same-route wrong provider operation and same-byte wrong artifact kind; original46-event helper GREEN; merged53/53 `unit03-099d-identity-integrated-green` | **Incomplete:** source-schema/projection/template/GraphQL/canonical/schema/sync joins are being implemented by the resumed099D under authored099F/FINAL099E. Existing good identity control remains response-unverified, not full accepted binding |
| E07 absence vs invalid / orphans | Binding reducer and global manifest membership | Absent intended stays unproven; materialized missing/present contradictory claims fail; output cannot add source rows; accepted siblings survive unrelated failure | Existing binding claim/identity tests and exact unexpected-key manifest control | **Incomplete:** valid complete accepted refs, all required scopes, orphan-output reconciliation and compound accepted-sibling evidence after099D completion |
| E08 failure preservation / bounds | Loader/token scanner, normalizer, collector and proof result consumers; earlier reads and normalization may complete | Expected keys/cells persist; no fixture hook at an earlier phase can certify render/admission. Local tokens/arrays are authoring resources, no durable recovery promise | Actual after-normalization observer plus original `normalization-stage-red/green` and state controls. Document-node original RED→26/26 final controls:1M/document8Mtotal, actual14documents1272066total302723max; exact literal7-node and cancellation-before-scan controls | **Partial:** actual import/schema-admission/execution-load control and failed-sibling manifest preservation now pass collector-stage-preservation-proof5/5. Existing proof failure preservation remains covered by parent-source-lane-controls. Render failure schedule still needs reconciliation. Per-document ref/output/collector aggregate boundaries remain open |
| E09 proof semantics | `source_lane_proof.go:loadSourceLaneProofs`, `assessSourceLaneProof`; receipt/input files may be read, no commands run from records | Loader owns bounded root/file observations; only reviewed exact source/lane/target/assertion evidence can promote | Complete terminal099C report and receipts,92/92 final helper and126/126 merged scope;4097distinct valid records, shared closure and two-pass file observations; original RED/edge chronology retained | Bounded099C plus099F structural equality implemented; parent-source-lane-controls290/290 includes the changed loader/reducer and proof-deficit controls. Empty trusted production catalog promotes no real provider. Final merged proof regression pending |
| E10 supplements | Inventory class and key validation |4341primary plus2distinct GitLab supplements; equivalence never deletes primary row | `SourceInventoryPrimarySupplement` and full independent census; original26/49event receipts above | Implemented; final generated artifact must retain1752+2 and separate classes |
| E11 deterministic bytes | `runSourceLanesContext` encoding/check path | Same immutable anchors/bytes produce same complete compact JSON +newline; rewritten source bytes are separately repinned, not claimed byte-identical | Real `SourceLaneCLIReadOnlyRoundTrip`, current `unit04-source-input-bytes-green`12/12 and historical compact/full candidate records | **Incomplete:** final two independent full generations, final schema/default-check and owned tracked candidate-save receipts have not run. Historical99MB candidates are not final |
| E12 read-only / no partial success | CLI input/build/buffer/stdout/check; all reads/encoding may have completed | Caller owns stdout; command never publishes files. Original tracked tree/candidate remains unchanged on failed check; short writer cannot succeed | CLI pre-retained tree/byte snapshots and candidate inode; wrong-readable same-count substitution; actual one-write full/short/partial-error/full-completion-error byte-prefix witnesses and early cancellation; `unit04-cli-output-boundary-proof`8/8 plus12/12 round-trip | Existing bounded checks pass; final output-budget review and safe successful candidate save pending. Read-only Root teardown alone adds no unsupported durable contract |
| E13 runtime isolation | Existing runtime embed allowlist, PM parser/runner untouched | Source evidence and generated report remain authoring-only; no new runtime source reader, transport route or receiver | Existing embed/source separation checks and exact changed-path audit recorded in TDD/plan | Original execution-only embed check at81e63f passed1/1;099F changed authoring files only. Final integrated changed-path check remains; no provider/live/credential/database work authorized or claimed |

## First effect, classifications and named schedules

- Inventory input opens are confined by `os.Root`, component checks and final no-follow/nonblocking descriptor open. `readSourceInput` checks the opened descriptor is regular, bounds bytes, and joins meaningful read/Close failures. A failed read never supplies a successful source payload or execution fallback. The inventory holds the preallocated key set across failures. Harmless read-only root teardown is not a new durable recovery guarantee.
- JSON token accounting executes on actual retained envelope bytes before graph decoding; raw JSON artifacts are counted before canonical decoding. YAML keeps its existing bounded alias conversion, then the converted payload is charged. The YAML parser itself remains bounded by input bytes, not a claimed preallocation node guard. Malformed input and cancellation keep distinct diagnostic classes. No file publication occurs.
- Normalization observer tests call the real normalization first, then inject the disclosed error. They prove normalization only. Render, semantic-admission and proof stage obligations remain separate until actual reached-stage evidence is recorded.
- Proof099C's completed physical-read, stale-order, failed-read charging, final identity/content checks and final cancellation results retain their original scope. A new target-reference representation needs equality/duplicate/required-target tests; it cannot inherit comparability by assumption.
- Canonical rendering/admission are in-memory observations here, not CP11 publication. Rename/lease/recovery schedules are N/A to CP12 generation/check because no publication call is made. The separately owned final artifact-save operation needs its own old/new byte custody record.

## Remaining completion boundary

Complete the resumed099D implementation on the committed099F dependency, reconcile all rows above, and update this single table with final exact source/test/dependency receipt identities. Finish generated schema/byte parity and safe artifact save, proportional required GSD verification, impact/changed-path review, and Firstmate-assigned independent CP12 review/correction. Passing current tests does not accept CP12 or CP11, and no no-mistakes/publication gate has started.
