# CP12 source and lane manifest — local candidate

The authoring command now retains the complete Batch One source universe before
execution availability, projects seven separate lanes, validates supported typed
source-to-target joins, and exposes missing materialization and proof honestly.
The generated product is `data/connector-canon/batch1-source-lane-manifest.json`.
Runtime consumes none of these report, annotation, cohort or proof inputs.

CP12 is **unaccepted**. Firstmate100 owns the independently assigned review and
any ensuing corrections. CP13 has not started. CP11's final independent-review
carry-forward to CP16 remains separate. No publication, no-mistakes, live
provider, credential, database or receiver work was performed here.

## Immutable implementation and evidence

- Base: `9c642b873b6a1ba3a4fe55fd7509715050c5b71c`.
- Source/test candidate: `462a447d7ad8e65f8b8b2452e2abcbf8af7ae1e4`, tree
  `1fb03b13b52ea50af92be8e51081545f2b324c31`.
- Branch: `fm/cli-top100-declaration-batch-r1-cp12`; integration target remains
  https://github.com/polymetrics-ai/cli/pull/4294 under parent
  https://github.com/polymetrics-ai/cli/issues/4325.
- Terminal099F was normally merged at94b05566 with its entire18-commit range,
  retaining parent changes. The subsequent parent change adds the real builder
  integration proof. No production/test edits follow462a447d.
- Product: **99,825,627 bytes**, SHA256
  `4a270dcbe037c67f0368b57bb06a68ae5f47d0ae3aabc259f9a75251d358a6ab`.
  Two independently invoked complete generations compare byte-for-byte equal.
  Candidate validation preceded exclusive creation of the previously absent
  tracked path. Actual default check preserved its pre-retained identity,
  type, timestamp and bytes.

The original command/result/source bindings are in
`/Users/karthiksivadas/pm-cli-agent-workspace/data/cli-batch1-pi-takeover/receipts-cp12-final-110/`.
The source freeze inventory enumerates every changed path and hash. The final
supervisor handoff binds the eventual documentation/artifact commit and tree;
that later commit must not be substituted for the original test-source identity.
[VERIFICATION.md](VERIFICATION.md) owns the E01–E13 contract/evidence table.
[TDD-LEDGER.md](TDD-LEDGER.md) preserves original chronology and complete finding
dispositions, including setup failures and later proof that was not historical RED.

## Delivered local behavior

| Obligation | Product and evidence |
| --- | --- |
| CP12-01 | Existing authoring path extended with `source-lanes`; one CLI/runtime architecture retained. |
| CP12-02 | All ten exact source sets: Asana249, GitLab1752+2separate supplements, Bitbucket297, CircleCI111, DockerHub54, Jira617, Notion49, Sentry223, Stripe589, Vercel400. |
| CP12-03 | Full generated JSON product, closed schema and independent exact-key comparison:4343rows/30401ordered cells. |
| CP12-04 | Retained facts/citations, requiredness and supported typed schema/field/template/GraphQL/canonical/transport joins; unsupported consumers stay explicit. |
| CP12-05 | Seven-lane applicability precedes availability; missing inputs/proof cannot suppress anchored source rows. |
| CP12-06 | Global/orphan errors and absent-versus-invalid targets remain distinct; valid fixture siblings survive. No target or receipt is promoted on existence alone. |
| CP12-07 | Original RED/GREEN/edge/refactor evidence, independent oracle counterexamples and actual builder failure preservation. Renderer-return and resource limits remain explicit below. |
| CP12-08 | Two full equal generations, schema/exact-corpus validation, candidate check, safe owned save and default read-only check pass. Final race result is recorded separately in VERIFICATION.md. |
| CP12-09 | Complete candidate/evidence handoff for Firstmate100; independent review and acceptance remain outstanding. |

Actual report states are **0 implemented**,10,835mapped-unproven,
1missing-foundation and19,565not-applicable;0validation errors and17,926deficits.
The real corpus contains0intended bindings,0accepted target references and0proof
references. Positive binding/proof controls are hermetic fixtures; neither those
controls nor the exact denominator establishes executable connector parity.
`artifact-census.json` enumerates every lane/state, diagnostic class and unresolved
owner count. Receiver and exposure decisions keep their existing owners.

## Verification and boundaries

Merged normal tests passed461/461 events; the added integration test passed5/5.
Final-source semantic admission passed7/7 and runtime embed isolation1/1. Both
binaries built, package vet and agentcontract passed, changed Go files are
gofmt-clean, and the complete source diffcheck passed. Final Atlas revision47
and its newly registered owners/proofs pass20/20selector tests; Atlas/manifest/
proof schemas validate. The final-source race package timed out after20minutes:186events passed,
full-corpus race was interrupted after16m44s. All45unreached tests subsequently
passed279events, so the exact union covers465of466expected events. The remaining
full-corpus test is running alone with the unchanged20m limit; its race proof
remains unresolved until that original terminal result. No skipped/zero-event command substitutes
for these behavior witnesses.

The actual render-failure probe failed strict import before rendering; it is a
setup failure, not RED or render coverage. The real schema-mismatch fixture
reaches rendering/loading before semantic rejection. No independently reachable
serializer-return failure was established, and no fake phase hook was introduced.
The concrete trace and original probe receipt remain in VERIFICATION.md for
independent adjudication under110.

Retained inventory, proof loading and source-schema traversal have their named
limits. Collector execution files are bounded individually, without a separate
aggregate/schema-file-count ceiling; generation buffers the complete output
without its own byte ceiling, while check input is capped512MiB. No total heap
bound, atomic multi-file snapshot or CP11-style durable publication is claimed.
Unsupported direct-only response/binary consumers, external/ambiguous schema
ownership, SDL compilation, dynamic transforms, multiple-media encoders and sync
delivery authority remain visible. These limitations are not silently accepted
as new runtime guarantees or implemented provider capabilities.

PM help/manual/website/completion parity is not applicable: PM's parser, commands,
execution artifacts and runtime sources are unchanged. Authoring help and canon
documentation are updated. The existing x/net version is merely promoted from
indirect to direct for retained HTML parsing; no dependency version was added.
Whole-programme CP29/CP31 checks, carried historical debts and final publication
remain separate. All protected prior candidates, receipts, history and `.cache/`
remain preserved.

## Workflow custody

The installed `scripts/gsd sources verify-work` and generated
`scripts/gsd prompt verify-work cp12-source-lane-manifest` resolve to the pinned
GSD Core20297a8f adapter. The canonical non-Pi worker executes local automated
verification inline; no additional role or pipeline is authorized by110.
Generating that prompt is not itself a passed gate. All existing native children
are terminal. Final local UAT and command custody are reconciled before handoff;
Firstmate remains the only acceptance and next-checkpoint authority.
