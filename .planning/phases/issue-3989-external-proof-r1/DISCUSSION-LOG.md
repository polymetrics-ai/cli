# #3989 discussion log

Mode: `--auto` / autonomous task brief.

- [auto] Ephemeral intake: selected environment/stdin-only, fingerprint-first values; no vault or persisted profile.
- [auto] Evidence boundary: selected a freshly built external `pm` invocation with observed child output and protocol exchanges.
- [auto] Safety: selected exact-value substitution before serialization, bounded bodies, and rejection on any incomplete evidence.
- [auto] Live smoke: selected opt-in execution only when a configured secret already exists; no credential request or disclosure.

No scope expansion was accepted.

## Residual discussion — 2026-08-17

Mode: autonomous residual brief. No product choice remains open.

- [locked] The disposable `polymetrics-ai-certification` identity is the only live GitHub identity. The token is obtained directly into the named environment variable and is never rendered in command output, evidence, fixture, planning artifact, commit, or PR body.
- [locked] The existing local TLS child test remains the deterministic harness. The new OS evidence observes a confirmed live child while its provider response is deliberately held, then scans only command-list output and paths scoped to this test's root/external-build prefix; it does not inspect child environments because the environment is the intentionally narrow secret delivery boundary.
- [locked] Opaque bodies and dual-credential semantics are test evidence over the existing exact-value substitution implementation; no connector, engine, stage-source, GitHub direct-read, or live-write work is in this residual.
- [locked] CLI help/manual/website surfaces are not changed: `--external-proof` behavior and help were delivered in #4159. The parity commands are therefore verification-only and the PR will explicitly mark documentation changes not applicable.
