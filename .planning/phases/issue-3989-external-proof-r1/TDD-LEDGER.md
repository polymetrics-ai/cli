# #3989 TDD ledger

| Slice | Red | Green | Refactor | Evidence |
| --- | --- | --- | --- | --- |
| External ephemeral run | **Red:** `go test -v -count=1 -run '^TestCertificationEphemeralCredentials' ./internal/app` failed because `app.BeginCertificationEphemeralCredentials` and `app.EphemeralCredential` were undefined. | **Green:** `go test -v -count=1 -run '^TestCertificationEphemeralCredentials' ./internal/app` passed. The test resolves the in-memory secret and an overridden config through the normal runtime path while asserting zero persisted credentials plus the absence of `.polymetrics/vault` and `vault/key`. `TestPrepareExternalCertifyStdinCredentialUsesChildMemoryOnly` then proves the `--value-stdin` value is removed from child argv, transformed to a child-only environment reference, and retained solely as the prepared in-memory value. | Reviewed | The tests assert absent canary and present fingerprints. |
| Evidence refusal boundary | **Red:** `go test -count=1 -run '^TestWriteExternalProof' ./internal/connectors/certify` failed because the external-proof input, writer, and transcript verifier were undefined. | **Green:** `go test -v -count=1 -run '^TestWriteExternalProof' ./internal/connectors/certify` passed. It proves the exact observed TLS process transcript verifies, source/replay fingerprints remain stable per project and differ in another project, canaries are absent from the artifact, and a truncated response produces zero certification artifact writes. | Reviewed | Each refusal asserts zero artifact writes. |
| Bounded protocol observation | **Red:** `go test -timeout 20m ./internal/connectors/certify/... -run '^TestObservedTransport' -count=1` failed: `undefined: certify.NewObservedTransport` in both canary and bounded-response tests. | **Green:** `go test -v -count=1 -run '^TestObservedTransport' ./internal/connectors/certify` passed. It asserts one TLS exchange's exact request/response values and a 65-byte error body retained as a 32-byte marked truncation while the caller still receives all 65 bytes. | Reviewed | Assert count, bytes, truncation, and preserved child response. |
| Artifact/smoke | **Red:** `TestExternalProofFreshChildRefusesNoHTTPSWithoutArtifact` first failed because the spawned command omitted `certify`, so no child report was created. | **Green (local):** `go test -v -count=1 -run '^TestExternalProofFreshChildRefusesNoHTTPSWithoutArtifact$' ./internal/cli` passed after fixing the command. It builds and runs a fresh external binary, observes its report write, proves no proof directory is created without HTTPS exchanges, and asserts the credential canary is absent from both process streams. `TestExternalProofGitHubSmoke` is the opt-in real GitHub smoke; it verifies exact stdout/stderr against the artifact then scans every artifact for token absence and a missing vault. It skipped locally because its three GitHub environment variables were unavailable. | Reviewed | Assert observed process output equality and no vault key/profile. |
| External TLS child acceptance and relay | **Red:** `go test -count=1 -run '^TestRelayExternalCertifyChildOutputRefusesCredentialWithoutWrites$' ./internal/cli` failed because `relayExternalCertifyChildOutput` was undefined. The first fresh-child HTTPS acceptance used Freshchat and exposed its unrelated `updated_time`/`updated_at` catalog mismatch, so the proof moved to Recurly whose default certification stream declares `updated_at`. | **Green:** the relay test passes after the parent scans both child streams before writing either. `TestExternalProofFreshChildCapturesCompleteHTTPSProviderTranscript` builds a fresh `pm`, calls a TLS Recurly-compatible provider with an ephemeral Basic credential, records nonzero child traffic, verifies exact stdout/stderr against the artifact, and scans every project artifact for the token. | The scoped observer honors an explicit `SSL_CERT_FILE` only during observed certification so a standard test/enterprise CA can establish a real TLS session without globally changing normal CLI transport behavior. | A secret in either child stream produces zero parent writes; a successful fresh HTTPS process produces exactly one attested evidence file with zero token occurrences. |
| Bounded traversal and flow references | **Red:** the new assertions are designed to fail if a redirect is invisible, if a seventeenth retry/error exchange is accepted, or if accepted evidence omits any flow read-back stage. | **Green:** `TestObservedTransportCapturesRedirectChain` records distinct complete 307 and 200 exchanges. `TestWriteExternalProofRefusesExchangesBeyondRedirectRetryBoundWithoutWrites` and `TestWriteExternalProofRefusesMissingFlowReferencesWithoutArtifactWrites` both assert no certification artifact directory. The positive artifact assertion requires the exact safe argv plus `flow_plan`, `flow_preview`, `flow_run`, and `flow_status`. | None. | Redirect source/final state, bounded refusal, and flow reference presence are observable in the captured exchange/artifact rather than inferred from a zero exit. |

No production edit occurred before the original ledger. The following residual evidence-only slices were planned before their test edits.

| Slice | Red | Green | Refactor | Evidence |
| --- | --- | --- | --- | --- |
| Opaque request/response substitutions | **Red:** `go test -count=1 -run '^TestWriteExternalProofFingerprintsOpaqueBodiesAndSeparatesCredentials$' ./internal/connectors/certify` failed because the asserted opaque-proof semantic helper did not exist. | **Green:** the same command passes. A single accepted proof holds non-JSON request and response bodies containing A/B credential canaries; the test asserts raw A, B, and root salt are absent, both bodies are `opaque`, and their markers match the exact prepared-value HMACs. | The original truncated-opaque-body refusal remains separate so it continues to prove the bounded failure path. | A local deterministic proof writer is the required fake: production only accepts complete opaque captures, and this test reads its actual serialized artifact. |
| Same-run A/B fingerprint semantics | **Red:** the same new opaque-proof test could not compile before its structural marker assertion was added. | **Green:** `TestWriteExternalProofFingerprintsOpaqueBodiesAndSeparatesCredentials` proves repeated A appears twice in the request and once in the response under one marker, while B has a different marker in both; `credential_fingerprints` contains exactly those two markers and never the root salt. | The pre-existing different-root replay test remains the independent salt-rotation contract. | One complete artifact supplies all three claims rather than inferring them across isolated runs. |
| Initial OS process/temporary-artifact proof (superseded) | **Red:** `go test -count=1 -run '^TestExternalProofFreshChildHidesCredentialFromProcessListAndTemporaryArtifacts$' ./internal/cli` failed because the process-list and temporary-artifact inspection helpers were undefined. | **Green:** the same command originally passed after its TLS handler held a fresh external child at the provider response while the parent inspected `ps -A -o command=` and test-owned temporary locations. | The scanner streamed files in 64 KiB windows and only walked the test's `TMPDIR` and root; it neither wrote nor rendered the canary. | PR #4198 CI then exposed that parent-side lifecycle observation was racy. The later child-side inversion row is the current proof and supersedes this intermediate design. |
| OS proof child-side observation inversion | **Red:** PR #4198 then failed with all parent-side settlement observables false (`child_exited`, `report_complete`, and `temporary_artifacts_settled`). This is a third distinct CI failure: the held-response design was observing an external child from outside and racing scheduler progress, not proving a child-side security boundary. After rebasing the current certification roll-up, the test's Recurly child correctly exited 1: the one-route TLS fixture authenticates `/accounts` but cannot certify Recurly's declared write surface. | **Green:** the fresh child now reads its own `ps` entry and `os.Args` exactly after resolving the credential into memory, scans its project, runner workdir, and fresh-binary directory, and writes only a redacted boolean-state artifact. The parent requires the real `connectors certify recurly` process entry, its authenticated request, the strict incomplete-parity exit, safe argv, two test-owned temporary locations, at least one scanned temporary file, and false credential-presence indicators. | No held HTTP response, polling loop, cancellation handoff, timeout expansion, certification weakening, or CI skip is retained. The secret-free artifact path requests evidence but cannot skip the proof. Any detected raw value is redacted before serialization and recorded only as a boolean. | The snapshot occurs at the credential-live boundary inside the child, making the process-list/argv/temporary-state assertion deterministic and stronger than an outside observer's timing window while preserving the stricter certification result. |
| CLI package capacity | **Red:** CI's `go test -timeout 20m ./internal/cli` exceeded its package deadline while displaying an unrelated Bahmni test. The verbose local baseline passed in 706.417s and confirms aggregate capacity: its slowest tests are the two external-child proofs (118.770s and 118.210s), then Bahmni (39.140s); the 17,800-variant dynamic-help sweep is 22.500s. | **Green:** `TestEveryDynamicConnectorLeafHelpRendersWithoutDispatch` materializes the same connector × command × help-flag cases, then starts only those immutable/manual-render cases with `t.Parallel()`. A verbose focused run still logs 17,800 variants; it passes in 6.137s total, the focused `-race` run passes in 61.753s, and the complete package passes under its unchanged 20-minute limit in 694.432s. | No timeout increase, test deletion, sampling, package split, shared mutable output, or shared mutable registry state is permitted. | The final count still covers every discovered command × both help flags; individual assertion failures retain their connector, command, and flag name. |
| Disposable live GitHub proof | Not a unit red state: this is an opt-in real-provider evidence run that was previously skipped for absent environment inputs. The live test then ran with the brief-designated disposable identity and first failed before provider traffic because its non-secret `rate_limit_account` coordination subject was omitted. | **Not green:** the test now supplies the designated disposable account as the declared rate-limit subject and runs through full parity, but the live report remains non-passing. Two final runs reached distinct failed local/live stages (`schedule_create`, then `resume`) and created no accepted proof. The exact commands and safe result facts are recorded in `VERIFICATION.md`; no credential, report, or proof was retained. | Do not weaken full-parity, rate coordination, or accepted-proof admission to make this pass. | This is a real, non-skipped provider-path result, but it cannot satisfy #3989 acceptance until the unstable full-parity result is resolved. |
| Live failure diagnostic | **Red:** `go test -count=1 -run '^TestRedactExternalProofDiagnosticFingerprintsSecrets$' ./internal/connectors/certify` failed at compile time because `certify.RedactExternalProofDiagnostic` was undefined. | **Green:** that command now passes, as does `go test -count=1 -run '^TestRelayExternalCertifyChildOutputRefusesCredentialWithoutWrites$' ./internal/cli`. Exact, base64, and URL-escaped planted-secret forms are replaced with the existing `{{pmcertfp:v1:<hash>}}` marker while the surrounding provider reason stays readable; the relay returns only that checked diagnostic and writes neither raw child stream. | No artifact, root salt, or raw child stream is written for diagnosis; a redaction error returns a generic unavailable message. | This closes the post-rebase observability gap without weakening the credential boundary. |
| Failed flow-stage reason | **Red:** `go test -count=1 -run '^TestCertificationFlowRoundTripReferencesFingerprintsFailedStageDiagnostic$' ./internal/cli` failed at compile time until the reference gate accepted prepared values for redaction. | **Green:** that command passes. A planted secret in `flow_plan`'s stage error is fingerprinted, while the stage name, status, exit code, kind, and non-secret provider reason remain readable. | The child prints the checked fingerprint diagnostic only; a redaction failure reports no stage detail. | This differentiates an environmental or provider failure from a proof-boundary failure without sending credential material to a terminal. |
| Absent flow-stage aggregate reason | **Red:** `go test -count=1 -run '^TestCertificationFlowRoundTripReferencesFingerprintsAbsentStageAggregateAndETLPredecessor$' ./internal/cli` failed because an omitted `flow_plan` reported only an opaque absence. | **Green:** that command passes. When `flow_plan` has no stage record, the gate safely reports the `flow_roundtrip` aggregate plus its failed `etl_full_refresh_append` prerequisite, with every planted secret form fingerprinted. | The aggregate diagnostic uses the same in-memory redactor and never renders the raw report or child stream. | The live rebase failure reported no `flow_plan` record, so the aggregate and predecessor distinguish a missing capture from a failed flow command. |
| Universal typed-stage diagnostic | **Red:** `go test -count=1 -run '^TestAssertKindFingerprintsErrorEnvelopeDiagnostic$' ./internal/connectors/certify` failed because `assertKind` returned only the kind/exit mismatch and discarded its typed error envelope. | **Green:** that command passes. Every failed `assertKind` now appends only the fingerprint-redacted error `category`, `code`, and message; a planted credential is absent while the non-secret reason remains readable. | The earlier stage-specific additions collapse into the common assertion boundary, so a future failing stage cannot silently fall back to an opaque kind mismatch. | The last authorized live run failed at `resume` after the schedule stage passed; the common boundary is the safe diagnostic required for whichever stage fails next, without another blind retry. |
