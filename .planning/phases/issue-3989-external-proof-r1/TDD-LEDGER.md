# #3989 TDD ledger

| Slice | Red | Green | Refactor | Evidence |
| --- | --- | --- | --- | --- |
| External ephemeral run | **Red:** `go test -v -count=1 -run '^TestCertificationEphemeralCredentials' ./internal/app` failed because `app.BeginCertificationEphemeralCredentials` and `app.EphemeralCredential` were undefined. | **Green:** `go test -v -count=1 -run '^TestCertificationEphemeralCredentials' ./internal/app` passed. The test resolves the in-memory secret and an overridden config through the normal runtime path while asserting zero persisted credentials plus the absence of `.polymetrics/vault` and `vault/key`. `TestPrepareExternalCertifyStdinCredentialUsesChildMemoryOnly` then proves the `--value-stdin` value is removed from child argv, transformed to a child-only environment reference, and retained solely as the prepared in-memory value. | Reviewed | The tests assert absent canary and present fingerprints. |
| Evidence refusal boundary | **Red:** `go test -count=1 -run '^TestWriteExternalProof' ./internal/connectors/certify` failed because the external-proof input, writer, and transcript verifier were undefined. | **Green:** `go test -v -count=1 -run '^TestWriteExternalProof' ./internal/connectors/certify` passed. It proves the exact observed TLS process transcript verifies, source/replay fingerprints remain stable per project and differ in another project, canaries are absent from the artifact, and a truncated response produces zero certification artifact writes. | Reviewed | Each refusal asserts zero artifact writes. |
| Bounded protocol observation | **Red:** `go test -timeout 20m ./internal/connectors/certify/... -run '^TestObservedTransport' -count=1` failed: `undefined: certify.NewObservedTransport` in both canary and bounded-response tests. | **Green:** `go test -v -count=1 -run '^TestObservedTransport' ./internal/connectors/certify` passed. It asserts one TLS exchange's exact request/response values and a 65-byte error body retained as a 32-byte marked truncation while the caller still receives all 65 bytes. | Reviewed | Assert count, bytes, truncation, and preserved child response. |
| Artifact/smoke | **Red:** `TestExternalProofFreshChildRefusesNoHTTPSWithoutArtifact` first failed because the spawned command omitted `certify`, so no child report was created. | **Green (local):** `go test -v -count=1 -run '^TestExternalProofFreshChildRefusesNoHTTPSWithoutArtifact$' ./internal/cli` passed after fixing the command. It builds and runs a fresh external binary, observes its report write, proves no proof directory is created without HTTPS exchanges, and asserts the credential canary is absent from both process streams. `TestExternalProofGitHubSmoke` is the opt-in real GitHub smoke; it verifies exact stdout/stderr against the artifact then scans every artifact for token absence and a missing vault. It skipped locally because its three GitHub environment variables were unavailable. | Reviewed | Assert observed process output equality and no vault key/profile. |
| External TLS child acceptance and relay | **Red:** `go test -count=1 -run '^TestRelayExternalCertifyChildOutputRefusesCredentialWithoutWrites$' ./internal/cli` failed because `relayExternalCertifyChildOutput` was undefined. The first fresh-child HTTPS acceptance used Freshchat and exposed its unrelated `updated_time`/`updated_at` catalog mismatch, so the proof moved to Recurly whose default certification stream declares `updated_at`. | **Green:** the relay test passes after the parent scans both child streams before writing either. `TestExternalProofFreshChildCapturesCompleteHTTPSProviderTranscript` builds a fresh `pm`, calls a TLS Recurly-compatible provider with an ephemeral Basic credential, records nonzero child traffic, verifies exact stdout/stderr against the artifact, and scans every project artifact for the token. | The scoped observer honors an explicit `SSL_CERT_FILE` only during observed certification so a standard test/enterprise CA can establish a real TLS session without globally changing normal CLI transport behavior. | A secret in either child stream produces zero parent writes; a successful fresh HTTPS process produces exactly one attested evidence file with zero token occurrences. |
| Bounded traversal and flow references | **Red:** the new assertions are designed to fail if a redirect is invisible, if a seventeenth retry/error exchange is accepted, or if accepted evidence omits any flow read-back stage. | **Green:** `TestObservedTransportCapturesRedirectChain` records distinct complete 307 and 200 exchanges. `TestWriteExternalProofRefusesExchangesBeyondRedirectRetryBoundWithoutWrites` and `TestWriteExternalProofRefusesMissingFlowReferencesWithoutArtifactWrites` both assert no certification artifact directory. The positive artifact assertion requires the exact safe argv plus `flow_plan`, `flow_preview`, `flow_run`, and `flow_status`. | None. | Redirect source/final state, bounded refusal, and flow reference presence are observable in the captured exchange/artifact rather than inferred from a zero exit. |

No production edit has occurred before this ledger.
