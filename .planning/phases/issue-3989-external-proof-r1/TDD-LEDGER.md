# #3989 TDD ledger

| Slice | Red | Green | Refactor | Evidence |
| --- | --- | --- | --- | --- |
| External ephemeral run | **Red:** `go test -v -count=1 -run '^TestCertificationEphemeralCredentials' ./internal/app` failed because `app.BeginCertificationEphemeralCredentials` and `app.EphemeralCredential` were undefined. | **Green:** `go test -v -count=1 -run '^TestCertificationEphemeralCredentials' ./internal/app` passed. The test resolves the in-memory secret and an overridden config through the normal runtime path while asserting zero persisted credentials plus the absence of `.polymetrics/vault` and `vault/key`. | Pending | Must assert absent canary and present fingerprints. |
| Evidence refusal boundary | **Red:** `go test -count=1 -run '^TestWriteExternalProof' ./internal/connectors/certify` failed because the external-proof input, writer, and transcript verifier were undefined. | **Green:** `go test -v -count=1 -run '^TestWriteExternalProof' ./internal/connectors/certify` passed. It proves the exact observed TLS process transcript verifies, source/replay fingerprints remain stable per project and differ in another project, canaries are absent from the artifact, and a truncated response produces zero certification artifact writes. | Pending | Each refusal asserts zero artifact writes. |
| Bounded protocol observation | **Red:** `go test -timeout 20m ./internal/connectors/certify/... -run '^TestObservedTransport' -count=1` failed: `undefined: certify.NewObservedTransport` in both canary and bounded-response tests. | **Green:** `go test -v -count=1 -run '^TestObservedTransport' ./internal/connectors/certify` passed. It asserts one TLS exchange's exact request/response values and a 65-byte error body retained as a 32-byte marked truncation while the caller still receives all 65 bytes. | Pending | Assert count, bytes, truncation, and preserved child response. |
| Artifact/smoke | **Red:** `TestExternalProofFreshChildRefusesNoHTTPSWithoutArtifact` first failed because the spawned command omitted `certify`, so no child report was created. | **Green (local):** `go test -v -count=1 -run '^TestExternalProofFreshChildRefusesNoHTTPSWithoutArtifact$' ./internal/cli` passed after fixing the command. It builds and runs a fresh external binary, observes its report write, proves no proof directory is created without HTTPS exchanges, and asserts the credential canary is absent from both process streams. `TestExternalProofGitHubSmoke` is the opt-in real GitHub smoke; it verifies exact stdout/stderr against the artifact then scans every artifact for token absence and a missing vault. It skipped locally because its three GitHub environment variables were unavailable. | Pending | Assert observed process output equality and no vault key/profile. |

No production edit has occurred before this ledger.
