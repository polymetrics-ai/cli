---
phase: cp13-foundation-demand-register
type: tdd
source_base: 2e364954a6253442f50d86e0eb5e755b30b1978b
---
# CP13 plan139

## Task Delivery Header

- Issue: Refs https://github.com/polymetrics-ai/cli/issues/4400; parent https://github.com/polymetrics-ai/cli/issues/4325.
- Base branch: main for existing https://github.com/polymetrics-ai/cli/pull/4294; no new pull request opened.
- Merges into: fm/cli-top100-declaration-batch-r1-cp13 → fm/cli-top100-declaration-batch-r1. Default/main merge remains unauthorized.
- Delivery: all three carried corrections and CP13-01..09 implemented, verified, independently reviewed; then captain-authorized normal working-branch integration/push with ancestry evidence.
- Working branch: fm/cli-top100-declaration-batch-r1-cp13.
- Starting source: 2e364954a6253442f50d86e0eb5e755b30b1978b / tree24d78d74914da61cea7e70ff3b2d93dcb2a8b137, tracked clean,199 frozen paths/nine inputs matched.
- Ownership: sole Codex gpt-6-astra/medium worker. Firstmate authors specialist prompts and final combined review; no new writer or generic audit.
- Verification: actual permanent pre-edit RED and same-assertion GREEN, affected consumer/race/schema/Atlas/census/check preservation, explicit historical dependency reuse and fresh combined judgment beforeCP14.

## Coherent sequence

1. Commit complete carried-findings/TDD ledger and nine-item obligation plan before any test/schema/production behavior edit.
2. CR-12: enumerate shape/scopes/interpretation/coverage/no-body/proof consumers; permanent retained-builder and independent-validator desired RED on preserved production. One closed success-key rule accepts200–299/2XX, keeps malformed/default/absent/empty and mixed siblings honest under existing semantics; same assertions GREEN. Cover collection/scalar/binary and actual binding/proof consumers, not only a helper.
3. WR-138A-01: permanent deterministic real producer/reader schedule RED across all three handshake owners, valid complete controls, cleanup/join on failure. Completed-before-visible publication in test owner; retain LOCK_NB, exact identity and signal assertions. Same tests GREEN; update canonical current assertion references without destroying119IDs/28groups/history.
4. WR-138B-01: actual published-schema validator and real Go annotation reader parity matrix RED (absent/null/empty/valid/malformed/unknown fields), then closed-object-or-null schema GREEN. No runtime GraphQL extension.
5. CP13 register: inspect19 Atlas owners/constraints/proofs/adopters and exact receiver candidates; connect each of the nine obligations below to existing authoring seams. Commit concrete schema/consumer assessment plan before new feature behavior. Exercise missing register/invalid reconciliation via existing manifest/builder/validator, not unknown-command/compiler/stub failures. Keep source/lane/proof and register assessment separate; no runtime registry. Relevant corrected outputs are prerequisites for final CP13 generation.
6. Meaningful fast focused tests, then dependency-selected final normal/race/lint/schema/Atlas/artifact census/determinism/check preservation. Capture actual command/source/test/input/raw/result at completion; never relabel137 or a discovery probe as new pre-edit RED.
7. Freeze complete combined candidate and full changed/affected-consumer evidence; Firstmate supplies fresh combined independent review. No mutation during review. Separate checkpoint acceptance beforeCP14; then authorized normal working-branch integration/push with actual remote/ancestry/checks evidence.

## CP13 original obligation matrix

| ID | Contract | Existing owner/input to inspect and reuse | Expected positive/negative evidence |
| --- | --- | --- | --- |
| CP13-01 | Source-ID×lane register; prior real Atlas lookup per assessed demand | Existing source inventory/fact/manifest pipeline plus source_lane_demand.go and catalog.json | Exact keys/seven lanes survive; omitted row, skipped lookup, duplicate/conflicting demand rejected |
| CP13-02 | Concrete owner symbols/tests/artifacts/adopters/evidence/next owner | Atlas owner/selection/proof/adopter records joined to retained source | Actual AST/test selectors and declared artifacts resolve; missing owner/adopter or stale pin rejected |
| CP13-03 | Four separate classifications | Existing Atlas constraints plus source-cited assessment | Valid reuse/local/provider/shared cases; ambiguity and uncited limitation rejected |
| CP13-04 | Executable evidence for existing capabilities | Existing proof policy and Atlas registered test selectors | Actual nonzero selected passing proof with bound assertions; skip/zero/unrelated/stale/unknown selector refused |
| CP13-05 | MIME/auth/body/paging reconciliation | Current Atlas owners/constraints, typed source facts and existing admission | Each selected shape traces existing owner; unsupported mismatch stays visible rather than invented foundation |
| CP13-06 | Twelve receiver candidates and Sentry distinction | Retained Bitbucket4,CircleCI2,Jira1,Vercel1,GitLab3,Stripe1 source keys | Reconcile each against current manifest; preserve Sentry mapped-unproven and one existing confirmed Vercel gap; copied names/counts not authority |
| CP13-07 | Existing receiver decision owner | cli-batch1-vercel-inbound-sync-decision-r1 | Exact existing reference preserved; no duplicate decision/live-endpoint claim or receiver implementation |
| CP13-08 | Separate conditional exposure boundary | cli-plan-webhook-receiver-foundation-r1-decision-webhook-exposure-product-boundary | Retain distinct condition/owner and existing answers; no implicit hosted relay/tunnel |
| CP13-09 | Independent work proceeds; unresolved C4 visible | Source/configuration register and explicit pending state | Pending decision cannot erase source rows or manufacture runnable placeholder |

## Required skills and parity

Applied GSD installed sources/prompts and non-Pi inline sole-owner execution; Go how-to, testing, error-handling, safety, security, context, concurrency, design-patterns, structs-interfaces, CLI, documentation, lint; connector-lane-build-order and exhaustive review gate. No new runtime route or PM/App command is planned. Any authoring CLI/schema/docs output change requires its own help/examples/validator parity; runtime help/manual/website N/A only if actual dependency inspection confirms no change.

## Contract-to-evidence / limits

Every new changed ownership/error/preservation row must identify invariant, immutable source, actual caller/phase, first completed side effect, resource owner, pre/post expected identities/bytes/permitted transitions, regression assertion, receipt and disposition. Current plan has no executed139 evidence. Hermetic retained provider fixtures are fake provider inputs because live-provider/credentials/customer DB scope is not authorized; real file/builder/schema/admission/reader operations and their returned outputs are exercised. CP13 authored-register tests must assert exact entries/classifications/refs/refusals, not a success exit.

No runtime foundation/receiver/credential/product change, dependency addition, force/discard or default-branch merge. Existing actor/cache/unlanded material stays untouched. New consequential ambiguity returns toFirstmate; routine authored139 corrections proceed.

## Firstmate141 implementation continuation at29da90a3

Decision cli-batch1-cp13-foundation-proof-admission-139 resolved by Firstmate141 (inbox143). Adopt complete140 report SHA2565248b30eff2b0a7ff8df586d0bf89fd0a2a897d8719d44e75dbf7457e3ccea0d and141 prompt SHA25690b72133f1fe3a138c0223274e58d388e8f60b87bca3d393ede9c5f6e84a3e34. This extends the existing139 plan, not another planning cycle. Native architect completed and is ineligible for final independent review. All three carried corrections remain committed at29da90a3; no acceptance transfer.

Separate foundation proof/assessment from unchanged lane authority. Reuse only bounded file/result/strict decoder/hash/Atlas resolver mechanics. Canonical catalog remains sole capability authority; source-owned reviewed record allowlist is assertion admission, not a runtime registry. Exact U/A/K/complement and full35 example reconciliation remain required. Current26 independently sealed seed cells are obligations, not receiver approval.

Installed scripts/gsd sources execute-phase and prompt execute-phase cp13-foundation-demand-register resolved and read. Continue inline under canonical single-writer restriction; prior139 discuss/plan authority retained. Required Go/connector/review skills remain recorded above; Go how-to/testing/error/security/safety/context and GSD adapter guidance refreshed. No spawned implementation roles.

## 7. Executable acceptance matrix

The owner should implement these as permanent real-consumer tests. Exact names below are proposed test names, not claims of existing tests or executions. Use at least two independently specified retained source keys and their complete fourteen lane cells for integration controls, plus the full bound corpus for final generation. Real captures and synthetic negative fixtures remain distinguishable.

| Case / proposed assertion family | Intended real consumer/frontier | Independently expected result |
| --- | --- | --- |
| FoundationTopLevelProof | Actual confined Atlas/record/capture/output/input loaders and shared result parser; actual Mode test receipt | Exact top-level run/pass accepted for vocabulary only. No fabricated slash, source key or target. |
| FoundationSubtestProof | Same pipeline with the actual Check child and parent events | Exact child and parent accepted for the stated refusal. Parent-only or another child cannot substitute. |
| FoundationScopeDoesNotWiden | Demand assessment requiring executor selection versus vocabulary-only proof | Requirement remains unresolved; proof of the mode set does not satisfy a registered executor/mode intersection or Vercel receiver. |
| FoundationLookupRequired | Actual Atlas lookup and emitted-register validator | Missing lookup, an empty unexamined candidate set, unknown ID, wrong owner/symbol/test or stale Atlas pin is rejected. Valid lookup/control survives. |
| FoundationContractOccurrence | Actual Atlas ID→relative pointer resolution | Same text at another foundation/contract cannot borrow authority. Missing/index-shifted/mismatched pointer/hash is refused. |
| FoundationReceiptIdentity | Real metadata/output reader and exact record review binding | Wrong raw digest, wrong capture digest, command/package/input map mismatch and internally consistent but unreviewed forged record never become current. |
| FoundationResultEvents | Shared parser then foundation selector adapter | Skip/fail/zero/missing run/duplicate run/duplicate pass/wrong package/cached no-run/parent-only results fail. Genuine top-level and child controls pass. |
| FoundationInputClosure | Actual inputs, file pins and review-required closure | Omitted required helper/fixture/go.mod/go.sum or wrong role/digest fails. A copied owner-symbol string cannot stand in for source coverage. |
| FoundationCaptureLargeMetadata | Actual complete captured metadata larger than4MiB, bounded below64MiB | Reads successfully with the typed metadata allowance; oversized metadata is refused before unbounded allocation. No input-map truncation. |
| FoundationFileReplacement | Real read followed by disclosed replacement/cancellation observer; final cache revalidation | Capture/input/result identity or content changes revoke tentative proof. Independently retained before-bytes/inode prove the comparison; no arbitrary sleep. |
| FoundationAttemptBudget | Real confined reader failure after an actual read attempt | Charge failed I/O and final rehash; no per-hit multiplication or free final read. Preserve original lane limits. |
| FoundationDuplicateAndClosedFields | Both orderings of duplicate ID/contract-test tuple; null/unknown/target/key fields | Conflicts stay invalid in both orders; no last-writer wins or scope smuggling. |
| DemandExactUniverse | Real validated source manifest + independently anchored cohort | Same-count substituted key, duplicate/missing lane and forged summary fail. Both independent keys and all ordered lanes remain present. |
| DemandKnownOmission | Real K construction from current CP13/gap ownership and separately pinned historical obligations | Removing a required cell and recomputing authored counts still fails K⊆A. Include a current extra update/header row, not only the original twelve. |
| DemandComplement | Real source U and sparse A | U\A is exact; one missing or extra unassessed key/digest fails independent validation. Missing assessment never becomes no-demand or foundation absence. |
| DemandMultipleAspects | Source body/auth requirement using a shared owner plus a local declaration deficit | Keep both requirement results under one source/lane; cannot erase one by choosing a single whole-cell class. |
| DemandLocalConfiguration | Actual retained request/response/config selector versus existing owner/proof | Emits connector-local work and exact affected JSON/fixture paths; no new shared foundation. Valid existing config provides the positive control. |
| DemandProviderLimitation | Actual retained clause pointer/section and value hash | Cited constraint retained; uncited URL/text or copied unrelated operation clause is rejected. |
| DemandActualGap | Actual examined Atlas candidates/constraints + source requirement | Named genuine mismatch and original decision owner retained. Missing proof or a stale example does not satisfy the gap predicate. |
| DemandExternalExamples | All literal Atlas example references with actual path/pin reconciliation | Exact external references retained without fabricated cohort keys. Missing Asana/GitLab paths remain example_unresolved with owner, not dropped or promoted to capability gaps. |
| DemandReceiverReconciliation | Twelve historical keys plus Sentry and the current26 seed through actual source facts | Eleven historical mapped/unproven and one Vercel gap are observed starting states; Sentry registration remains mapped/unproven absent new source. No counts-as-authority inference. |
| DemandPendingDecisions | Existing receiver owner and conditional exposure owner | Preserve both meanings; no claimed live endpoint, automatic product approval or blocked unrelated source/configuration work. |
| DemandNoLanePromotion | Same source input built with no foundation proofs, valid proof, unrelated proof and invalid proof | Byte/structured comparison of original source/lane cells shows no changed applicability, state, accepted refs or lane ProofRefs. Include one separately valid existing lane-proof fixture as a control that the old reducer still works. |
| DemandCheckAndOutput | Actual new authoring command, full-byte output and check path | Deterministic two-run output; genuine short/partial/full-error writer cases fail; complete write succeeds. Check preserves pre-call full bytes and identity. |
| DemandOracleFalsification | Independent expected-spec validator on coherent forged register and agreeing forged summary | Reject the specific wrong source key, omitted known adopter, broadened assertion or implemented-lane mutation. A self-consistent comparator must not certify its own input. |

For the file-custody rows, record the exact first completed read/open, the original resource owner, the observed fault phase, cancellation/finalization effects and allowed terminal state. All handles remain owned by the invocation and close on every exit. There is no durable publication/recovery contract to add: production generation writes only its final stdout once; check mode is read-only. If a future implementation chooses to write files directly, that is additional behavior requiring its own actual write/close/preservation evidence, not implied by this decision.

### Honest initial RED seams and ordering

The frozen29da source has no CP13 demand consumer or foundation-proof adapter. A compile error for a new symbol, unknown source-demands command or absent output file is not the semantic RED required here. Do not claim otherwise.

Start with a compileable real projection path which loads the existing validated universe/Atlas and preserves source state through the current real readers; build and test its ordinary non-authorizing projection behavior. Any interface/bootstrap failures are recorded as setup, not desired-behavior RED. The first foundation-admission/demand-assessment expectations must then run through that actual path **before implementing their acceptance/classification branches**. In particular, capture failure to account for the independently known cell, failure to produce the intended reviewed top-level proof relation and failure to refuse a coherent forged assessment at the actual read/projection frontier. An unconditional error/no-op stub is not an acceptable baseline.

Keep fixture and assertion bytes fixed across each missing-behavior RED→GREEN. Preserve valid source keys/positive controls so reject-everything cannot pass. Establish the parser/lookup/receipt frontier explicitly before interpreting the failure. A record which fails earlier because its capture is malformed does not prove unrelated assertion rejection. A two-source fixture that never enters a valid Atlas owner or actual target-independent proof loader does not prove foundation admission.

For new code with no meaningful prior consumer, explicitly label that first completed projection as new infrastructure and the subsequent witnessed RED as a regression against that source snapshot; never backdate it to29da. The immutable source/test/input capture must show exactly which source implemented which frontier. The owner can obtain genuine assertion RED without weakening any existing lane-proof shape or manufacturing a provider/subtest. Later oracle-falsification tests remain later evidence if they were not part of the original RED.

## 8. Implementation slices and proportionate verification

| Slice | Files / ownership | Completion evidence |
| --- | --- | --- |
| 1. Pin contracts and inputs | Existing CP13 PLAN/TDD/VERIFICATION and canonical review records; owner only | Adopt this decision, exact139 carried scope, sparse-assessment schemas, trusted review boundary, K sources and dependency map before behavior edits. |
| 2. Reuse Atlas and evidence mechanics | New cmd/connectorgen/source_foundation_atlas.go and source_proof_files.go/source_proof_result.go; mechanical moves/adapters in source_lane_proof.go and vnext_lock_test.go | Existing lane-proof and Atlas tests remain protective. Same exact lane shape/catalog/reducer outputs. New parser adapter admits a real top-level selection without relaxing the lane adapter. |
| 3. Foundation-specific admission | New source_foundation_proof.go, source_foundation_proof_reviews.go and focused tests; docs/connector-canon/foundations/proofs.json plus schema and original copied receipts | Positive real top-level/subtest records, source/command/result/input/semantic review checks, unrelated/forged/no-selection negatives, bounded custody and zero lane promotion. |
| 4. Source demand assessment and coverage | New source_foundation_demand.go / source_foundation_demand_test.go; authored sparse assessment input and closed demand schema | Exact U/A/complement and independent K validation, all known relations, local/shared/provider/gap distinctions, receiver reconciliation, exact pending owners. Keep existing source_lane_demand.go Vercel authority. |
| 5. Authoring command and artifacts | New source_foundation_cli.go/tests; main.go dispatch/help; generated register; authoring docs/Atlas maintenance as actually affected | Actual end-to-end stdout/check behavior, deterministic regeneration, schema/census and per-cell no-promotion comparison. Source-lanes proof/runtime remain unchanged. |
| 6. Freeze and independent combined judgment | Existing owner handoff/receipts; fresh Firstmate-assigned reviewer | Full CP13 unit, all three carried corrections and affected CP11/CP12 contracts; terminal relevant checks and distinct checkpoint outcomes. This architect cannot supply that judgment. |

Use the current20m Go timeout. Initial tests are the actual new consumer tests and affected existing proof/cache/Atlas families. Extracting a shared file/result helper invalidates the prior execution evidence for its existing lane-proof callers, so run their complete parent tests normally and with race where their behavior/concurrent cancellation requires it. It does not automatically invalidate all unrelated CP11 publication tests or demand a repeat of the prior whole1558-event package run after every slice.

When registering a foundation record, execute its actual selected owner test(s) against the captured source and review its input closure. Do not execute every proof_tests entry in all19 Atlas foundations merely to populate an unused catalogue. Reuse a previously completed selected assertion only with its original receipt and actual unchanged dependency/input scope; record historical reuse as such. A test helper or owner changed by this phase invalidates the corresponding reused evidence.

Run new full demand-register validation, closed schema/reader parity, exact U/A/K/complement census and independent source/citation checks on the complete current corpus. Generate A/B through the actual authoring CLI and compare full bytes. Test both candidate and default check-mode preservation with retained pre-call bytes and device/inode/mode/mtime. Rebuild the source manifest after139's schema input-pin change, and after any actual Atlas input change, once per coherent final state; compare every source/lane delta explicitly. Reuse the validated manifest as a pinned input during repeated demand-only checks rather than rebuilding it for each small record test.

Run original-range introduced lint, vet and connectorgen build for the changed package, plus relevant Atlas/authoring-doc checks. PM/runtime/help/release/agentcontract evidence can remain historical only when the actual dependency exclusions hold. No new runtime code means no provider/database/container/live credentials check. If extraction alters existing source-lanes code, include its CLI/result boundary tests; if main.go dispatch changes, include actual source-lanes and new command usage/invalid-action tests. CI/cumulative integration gates remain with the owner.

Keep the14 correction commands' historical failures and focused PASS receipts intact. No correction suite was run as140 design preparation. Final combined testing is selected from the actual complete changed-input/consumer map, not from the age of a receipt or a desire for another timestamp.

## Firstmate150 coordinated correction plan before behavior edits

Scope: complete frozen review148 ledger at b41c6eaa, CR-148-01/02/03 (BLOCKER), WR-148-01 (mandatory evidence WARNING), WR-148-02 (nonblocking WARNING to correct). No findings are waived. Preserve all9 CP13 items and25 original matrix rows above, including exact4343×7 universe, A/K26 and complement30375,35 examples and two receiver/exposure meanings. CP11/CP12 are separately accepted at b41c6eaa; only affected accepted contracts enter regression work.

Stage A: one fresh read-only Astra/xhigh bounded architect under the exact Firstmate150 specialist prompt. No production edits until complete design and this frozen ledger are available. The sole Astra/medium owner executes Stage B inline using existing GSD lifecycle; no spawned writer. Existing discuss/plan contract remains, with installed sources plan-phase/execute-phase resolved for gaps. Required Go how-to, testing, error-handling, safety, security, context/concurrency, design/structs, CLI/documentation/lint and connector/review skills remain applied.

| Group / findings | Real consumer and independent RED oracle | Planned GREEN and protection |
| --- | --- | --- |
| Proof observation/custody: CR-148-01/02 | Permanent actual-reader permutations and input→owner/test overlaps; same14-cell real register/CLI healthy control then missing document/record/dependency or stale dependency; independent full rows/owner/reason and no resolved reuse | Same assertions pass with order-independent bounded declaration acquisition and typed non-authorizing unavailable observations; unsafe/malformed/cancel/in-flight replacement still refuses; shared CP12 cache/lane callers remain correct |
| Exact fit and combined evidence: CR-148-03/WR-148-01 | Same-file unrelated Check/widgets negative through actual requirement/adopter builder; real exact mechanism positive; retained auth/body same-cell shared result plus separate local deficit; dropping a requirement or borrowing proof falsifies output | Exact selected mechanism/source/config join; no file-membership authority; both independent auth/body/local outcomes with actual original mechanism execution and reviewed closure |
| Required wire: WR-148-02 | Actual schema and Go reader: explicit empty condition passes, absent/null must refuse; conditional owner controls | Required presence/non-null with closed fields and unchanged pending/conditional meanings |

Capture permanent semantic RED before each production group, unchanged assertions GREEN afterwards. Preserve reviewer probes as discovery, including unavailable raw-output archival limit, earlier selector setup failure, and147's narrow status1/1 normal/race as later coverage only. No compile/setup/unknown-command failure labeled RED. Design report resolves mechanisms and final exact selectors before dependent edits.

Final verification: retain original263-event selection and later147 test, add new regressions; predeclare normal/race disjoint partitions and exact expected union before execution, full corpus separate,20m unchanged. Shared cache/reader changes require affected CP12 caller tests. Run relevant vet/new-range lint/build/help/schema parity, full source/lane census and no-promotion, full demand A/B byte equality and real default/explicit saved-file bytes/inode/mode/mtime preservation. Regenerate dependent source pins coherently; never hand-edit derived authority. Only independently unchanged dependency scope may reuse earlier evidence. All original117 captures/32 failures and protected work retained.

After complete coherent GREEN wave: update exact finding/9-item/25-matrix dispositions, new functions/states/edges and input maps; freeze code SHA/clean tracked state; return full original receipts and design/report pins for Firstmate-authored fresh independent final review. No self-acceptance/integration/push/CP14 from this plan.

## Firstmate155 adopted admission amendment

Final154 DECISION.md41251bytes SHA2560a327f4e0c605ddd93f47215d2dd0ab3f70688faf9ca57fd052d1f07cc601057 read in full and unchanged hash verified;155 adopts all conjunctive predicates and resolves cp13-153-canonical-intended-universe-admission. Continue existing GSD correction execution: separate exact-global-auth citation RED/control → narrow citation repair/materialized positive → honest missing-CLI universe/register/CLI RED → observed-source admission/custody/companion/output → full154 matrix/affected suite/corpus/schema/exact candidate. Original9items25families/five148findings remain. No source-lanes or internal runtime behavior edits. The prior fixture source-lanes exit0 expectation changes explicitly to invalid JSON/exit1 for missing CLI. Retain materialized baseline and literal14-cell expectations independently of candidate output.

### Firstmate156 scheduling adoption

Read the complete instruction156 (SHA256 f6729c1c58815e944edb345a3f8d80aac4e7b2d2330872ef1e98a13370c785df). The captain permits CP13–16 integration after complete final review with 0 corrections, or 1–4 explicitly open corrections carried with original IDs/instances/evidence and named ownership into the next checkpoint. Five or more retains the coherent correction process. This supersedes strict-zero advancement scheduling only; implementation, complete independent review, evidence and acceptance obligations remain. Current155 correction and validation continue without early review or self-acceptance. A carry ledger will be published if the final review supplies carryover; the original five148 findings are not that final result. No transition, integration or acceptance has occurred here.
