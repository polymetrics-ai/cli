# Prompt snapshots — cli-engine-secret-input-r1

## Manual GSD fallback

The project adapter was available (`scripts/gsd doctor` passed), but did not register
`programming-loop`; the official lifecycle is therefore executed inline. The task prompt and the
firstmate decision key `secret-storage-seam-collision` are the controlling inputs. No secret values
are recorded in this trace.
