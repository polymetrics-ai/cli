Command:
  go test -timeout 20m ./internal/cli -run '^TestFlowSourceConnectionSelectorsReadOnlyOwningRows$' -count=1

Result: expected RED (exit 1)

Both subtests created two structurally connection-owned Parquet tables named
`records` through warehouse.Location.EnsureOwnership and warehouse.WriteTable.
Both declared selectors then failed because the flow adapter passed an unscoped
SQL query to DuckDB:

  query selector reads only acme rows:
    flow: step failed: step query-records: execute query: Catalog Error:
    Table with name records does not exist!

  action source selector reads only globex rows:
    flow: step failed: step act-on-records: execute query: Catalog Error:
    Table with name records does not exist!

DuckDB suggested a qualified owner view. That is evidence the two owners were
present and that the declared flow selectors were not propagated. No network
call, provider mutation, approval lifecycle, or generic HTTP action occurred.
