# TDD LEDGER — issue-3013-shopify-parity

| Slice | Red / preflight evidence | Green evidence | Notes |
| --- | --- | --- | --- |
| Shopify bundle absence | `go run ./cmd/connectorgen validate internal/connectors/defs/shopify` exited non-zero before scaffold (target dir absent), proving red/preflight. | Generated connector-local Shopify bundle and then used a temp defs root for focused validation because `connectorgen validate` expects a directory containing connector subdirectories. | Expected failure proved production bundle was absent/incomplete. |
| Policy addendum | Prepared/edited #3013 and #3014-#3020 with idempotent marker `captain-policy-shopify-destructive-in-scope-r1` via `gh-axi issue edit --body-file`. | Green: marker appended; existing count tables preserved. | Destructive/delete operations are in scope only behind typed destructive confirmation and plan -> preview -> explicit approval -> execute. |
| Ledger/bundle validation | Initial validation failed while shape was incomplete; removed unsupported `additionalProperties` and omitted fabricated `operations.json` execution blocks. | `go run ./cmd/connectorgen validate internal/connectors/defs` => `550 connector(s) checked, 0 findings`. Focused temp-root validation also passed for Shopify only. | Ledger contains 1103 official-source rows: 33 covered rows and 1070 blocked/planned rows. |
| Conformance | Pre-scaffold Shopify conformance could not load the missing connector. | `go test ./internal/connectors/conformance -run 'TestConformance/shopify' -count=1` passed. | Fixture-only evidence remains uncertified; no live Shopify calls. |
| CLI/docs/catalog parity | Adding Shopify caused expected catalog/help golden drift (`553` -> `554`, `549` -> `550`). | Regenerated CLI golden transcripts/docs and updated catalog count expectation; `go test ./internal/cli -run 'Connector|Dynamic|Golden' -count=1` passed. | Connector help now exposes Shopify command groups without raw API passthrough. |
