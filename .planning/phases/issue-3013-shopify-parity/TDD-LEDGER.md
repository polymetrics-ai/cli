# TDD LEDGER — issue-3013-shopify-parity

| Slice | Red / preflight evidence | Green evidence | Notes |
| --- | --- | --- | --- |
| Shopify bundle absence | `go run ./cmd/connectorgen validate internal/connectors/defs/shopify` exited non-zero before scaffold (target dir absent), proving red/preflight. | Generated connector-local Shopify bundle and then used a temp defs root for focused validation because `connectorgen validate` expects a directory containing connector subdirectories. | Expected failure proved production bundle was absent/incomplete. |
| Policy addendum | Prepared/edited #3013 and #3014-#3020 with idempotent marker `captain-policy-shopify-destructive-in-scope-r1` via `gh-axi issue edit --body-file`. | Green: marker appended; existing count tables preserved. | Destructive/delete operations are in scope only behind typed destructive confirmation and plan -> preview -> explicit approval -> execute. |
| Ledger/bundle validation | Initial validation failed while shape was incomplete; removed unsupported `additionalProperties` and omitted fabricated `operations.json` execution blocks. | `go run ./cmd/connectorgen validate internal/connectors/defs` => `550 connector(s) checked, 0 findings`. Focused temp-root validation also passed for Shopify only. | Historical pre-rebuild evidence; its 1,103-row ledger snapshot is superseded by the published-reference rebuild below. |
| Conformance | Pre-scaffold Shopify conformance could not load the missing connector. | `go test ./internal/connectors/conformance -run 'TestConformance/shopify' -count=1` passed. | Fixture-only evidence remains uncertified; no live Shopify calls. |
| CLI/docs/catalog parity | Adding Shopify caused expected catalog/help golden drift (`553` -> `554`, `549` -> `550`). | Regenerated CLI golden transcripts/docs and updated catalog count expectation; `go test ./internal/cli -run 'Connector|Dynamic|Golden' -count=1` passed. | Connector help now exposes Shopify command groups without raw API passthrough. |
| Published-reference rebuild (2026-08-06) | Fresh public Markdown extraction found the prior 1,166-row claim does not match Shopify's current published reference: GraphQL remains 287 queries + 518 mutations, while 67 current REST resource pages yield 152 GET + 73 POST + 35 PUT + 33 DELETE = 293 REST rows, or 1,098 total. The AccessScope resource page itself contains the access-scope GET endpoint, so the usage guide is not a second operation. The prior ledger contains retired REST rows, including ten owner/global Metafield DELETEs. | Green: rebuilt `api_surface.json`, per-row cited `source_inventory.json`, 33 typed `rest_write` DELETE operations, 136 current destructive GraphQL mutation ledger rows with per-row typed-confirmation requirements, 1,098 static command declarations, and 33 fixtures; `go test ./internal/connectors/defs/shopify -count=1`, `connectorgen validate`, `surface-sync --check`, and Shopify conformance passed. | Every command maps through an exact `source_cli_path`. Planned commands deliberately omit executable `api_surface` bindings because the shared validator reserves those for executable coverage. The typed DELETE operations remain planned only because #3852's shared command schema cannot represent the captain-required non-redacting `json` policy. The other destructive mutation rows remain planned until their operation-specific fixed GraphQL contracts exist. |
| Admin API host allow-list | `go test ./internal/connectors/defs/shopify -run TestShopDomainUsesCanonicalAdminHost -count=1` rejected `fixture-shop.invalid.example` before the spec pattern changed. | The same connector-local test now passes for rejection with no value echo and acceptance of `fixture-shop.myshopify.com`; the app-level regression table and canonical-host acceptance test are present. | Captain decision `admin-api-host-allowlist`: canonical lowercase `*.myshopify.com` only; no reviewed custom-domain allow-list. App-level execution remains blocked by missing generated Shopify icon coverage. |
| Shopify generated icon coverage | `PM_ICON_REGISTRY_SOURCE` was unset. The documented public Airbyte OSS registry was then supplied to `make icons-generate`. | After the connector-local rebuild, `go test ./internal/connectors/commandrunner -run TestEveryImplementedCommandPassesRuntimePreflight -count=1` reached the expected global registration boundary and panicked for the same missing Shopify icon entry. | #3809 owns the generator defect. No retry, workaround, or hand edit of `internal/connectors/icon_data.json` was made. The shared fix is required before Shopify's generated icon row, app credential-boundary test, generated docs, and global command-runner preflight can run. |
