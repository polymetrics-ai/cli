# TDD LEDGER — issue #3995 shared connector-certification Shepherd gate

| ID | Enforcement | RED evidence | GREEN evidence | Refactor/verification |
| --- | --- | --- | --- | --- |
| R1 | A GitHub `integrate_sub_pr` gate must expose the exact missing live-evidence cell and an all-green generated fixture must proceed. | `go test -timeout 20m ./internal/agentcontract -run '^TestEvaluateCertificationGateGitHubBaselineAndGreenFixture$' -count=1` failed before production evaluator source: `CertificationGateRequest`, `EvaluateCertificationGate`, `CertificationGateRetry`, `CertificationGateProceed`, and `CertificationGateVerdict` are undefined. The test already loads the checked-in GitHub input and writes a temporary generated all-green capability/workflow/sync/flow/evidence fixture. | The same focused test passes: current GitHub is `RETRY` and contains `capability/github/capability:check/live_evidence`; the all-green fixture is `PROCEED`. | Parser, artifact loading, and state-boundary enforcement are separate; verdict failures sort by stable ID. |
| R2 | Every applicable capability, workflow, sync-mode primitive, and flow pair needs declared, implemented, fixture-tested, live-tested, and validated live evidence. | `TestCertificationGateBindingCriteriaYieldExactCellIDs` was added before the evaluator rules existed; its five fixture variations had no implementation to return their exact IDs. | Focused tests pass for isolated `declared`, `implemented`, `fixture_tested`, `live_tested`, and `live_evidence` defects; the implemented/reachable/file-present fixture still returns `RETRY` without live proof. A missing sidecar returns `HALT` with both `cell_id` and `evidence_id`. | Exact IDs are category/cell coordinates; a sidecar mismatch cannot be reduced to generic prose. |
| R3 | Unknown/missing schemas, unknown fields, invalid evidence, and omitted gate fields fail closed rather than silently passing. | Strict-decoder tests were added for absent schema version, unknown matrix/request field, unknown proof strategy, and missing adapter evidence-directory field. | `TestCertificationGateFailsClosedForSchemaAndAdapterInputDrift` passes, producing `HALT` for each mutation; `TestCertificationGateRejectsUnredactedProofBody` proves a non-fingerprint scalar cannot be consumed. | `DisallowUnknownFields`, trailing-value rejection, version checks, canonical path equality, and recursive proof-body sanitization prevent #3989 proof-schema guessing or raw evidence promotion. |
| R4 | Claude, Codex, Pi, and OpenCode receive equivalent generated gate input/verdict instructions, and drift/missing registered projections fail. | `go test -timeout 20m ./internal/agentcontract -run '^TestOpenCodeProjectionsAreRequiredGeneratedGateInputs$' -count=1` failed with `unknown projection render mode "opencode_markdown_yaml_frontmatter"`. | The same test passes after canonical OpenCode registration/rendering. `TestRegisteredHarnessesEmbedByteEquivalentCertificationGateInputs` and `TestProjectionCheckRejectsCertificationGateDriftForEveryHarness` pass; `agentcontractgen sync` generated eight registered projections and `check` passes. | One renderer owns the marked gate block; projection-specific frontmatter is separately strict-validated. |
| R5 | The current zero-certified GitHub baseline rejects at a transition while generic contract validation stays structural and read-only. | Current baseline had no gate evaluator, so it could not distinguish structural contract validity from transition certification. | `TestCertificationGateCurrentBaselineRejectsWithoutBreakingStructuralContractCheck` and `TestCertificationGateEvaluationIsReadOnly` pass. The former runs real `CheckRoot` then gets GitHub `RETRY`; the latter snapshots temporary artifacts/evidence before/after a `PROCEED` evaluation. | The evaluator accepts no provider client or write operation and reads only canonical relative paths. |
| R6 | Empty proof values, semantically reordered bodies, incomplete matrix topology, escaped/non-regular inputs, producer-valid delivery limitations, and missing executable transition invocation must not weaken certification. | The six reported correction tests were authored before their production changes. Per this review phase's one-focused-verification constraint, they were not separately run RED; each assertion records the prior weakness and expected fail-closed or `PROCEED` result. | `go test -timeout 20m ./internal/agentcontract ./cmd/agentcontractgen -count=1` passes, including the correction tests and real checked-in GitHub `RETRY` command. | Root-bound reads, topology closure, semantic proof comparison, producer-equivalent limitations, and canonical projection argv remove the six review gaps without provider or credential actions. |
| R7 | The consumer must accept only the shared producer flow-kind catalog, derive all completion/status reports from matched evidence, retain safe malformed-pointer coordinates, and require an explicit non-symlink gate root. | Before production edits, RED coverage was added for omitted/added/remapped flow kinds, false capability/status/baseline reports, malformed pointers, `live_tested` without evidence, all four transition roots, and help without `PROCEED`; these assertions expose the former `PROCEED`, generic HALT, or zero-exit paths. | `go test -timeout 20m ./internal/agentcontract ./cmd/agentcontractgen -count=1`, focused `cmd/connectorgen` certification tests, `certification-matrix --check`, projection sync/check, and explicit all-four command tests pass. | The catalog, aggregate derivation, coordinate-safe validation, and strict command root are verified without invoking providers, credentials, evidence creation, or an outer pipeline phase. |
| R8 | Flow overrides may add only live-evidence facts; raw pair-set pointers must bind before override resolution, proof-number comparisons must preserve exact JSON numbers, and consumer flow-kind data must be generated from the producer matrix. | Before production edits, tests were authored for immutable override promotion, safe-missing/mismatched/wrong-coordinate masked base records, unequal 64-bit `original_bytes` values, and catalog sync/check drift. The review-phase boundary deferred their execution until the single focused Green run. | `go test -timeout 20m ./internal/agentcontract ./cmd/agentcontractgen -count=1`, focused unchanged-producer certification tests, `connectorgen certification-matrix --check`, and `agentcontractgen sync/check` pass. | The allowed generated catalog replaces the forbidden direct producer import; no `cmd/connectorgen/certification*.go` change is retained. |
| R9 | Every flow pair must match its endpoint-role facts, and the generated flow-kind catalog must fail closed yet allow sync to bootstrap it when its generated data is absent. | Before production edits, `TestCertificationGateRejectsFlowPairsThatDisagreeWithEndpointRoles` and `TestFlowKindsFailsClosedWithoutValidGeneratedData` were authored. Per this review phase’s one-focused-verification boundary, they were not run separately; the first describes the previously independent green pair/role path and the second requires the planned stable catalog backing state. | `go run ./cmd/agentcontractgen sync --root "$PWD"` rebuilt an intentionally absent `flow_gen.go`, then `go test -timeout 20m ./internal/certificationcatalog ./internal/agentcontract ./cmd/agentcontractgen -count=1`, focused unchanged-producer certification tests, matrix check, projection check, and forbidden-path audit passed. | The pair invariant derives only applicability, declared/implemented conjunctions, and exact not-applicable facts; fixture/live evidence remains independently bound. |

No production evaluator, generator, canonical-source, or generated-projection edit is permitted
before the recorded R1 RED run.
