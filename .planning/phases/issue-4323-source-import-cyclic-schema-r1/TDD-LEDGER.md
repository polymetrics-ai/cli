# TDD ledger — issue 4323

| Slice | Red evidence | Green evidence | Refactor / scope evidence |
| --- | --- | --- | --- |
| Recursive source schemas | **Red (2026-08-23):** `go test -count=1 -timeout 20m ./cmd/connectorgen -run '^TestSourceImportRetainsRecursiveSchemaReferencesAsSourceBoundGaps$'` failed for direct, mutual, and deeply nested fixtures. Each stopped at `preflight source grammar … response "200": reference cycle at "#/components/schemas/Folder"`; no operation descriptor was retained. | **Green (2026-08-23):** the same test passed direct self-reference, mutual recursion, deeply nested recursion, and a non-cyclic control. Each recursive operation retained its canonical `folders/get` source mapping and `$ref`, with `runtime.merge_blocked=true` and a `cli-recursive-schema-foundation-r1` gap naming `#/components/schemas/Folder`; the control emitted no cycle gap. | The importer retains the exact source `$ref` at the first recursive edge; it never expands the cycle to a depth. v3 fields and provenance model are untouched. |
| Non-recursive regression | The red suite included the finite `Folder.name` control. | **Green (2026-08-23):** the control passed with no recursive-schema gap and `merge_blocked=false`. | Assertions observe descriptor output and source mapping, rather than private resolver state. |
| Unreferenced grammar schema | **Red (2026-08-23):** `TestSourceImportPreflightsUnusedGrammarObjects` required an unused self-reference to fail as a cycle. | **Green (2026-08-23):** the updated behavioral test imports it and verifies the existing top-level descriptor `gaps`/`merge_blocked` reporting path contains the recursive schema pointer. | This preserves grammar preflight coverage for external/dynamic/unresolved inputs; only ordinary schema recursion changed semantics. |
| Real affected connector | The current public Grafana OpenAPI source contains recursive `Folder`, `RouteExport`, and `TimeInterval` schemas. | **Green (2026-08-23):** temporary in-worktree source lock pinned `public/openapi3.json` at 790,237 bytes / SHA-256 `8d54e6ceb1aeef72ef8fe31213eed910d82d264b0d8328b7e53670ce4c88b46c`; `go run ./cmd/connectorgen source-import grafana --defs .tmp-source-import-grafana/defs --cache-dir .tmp-source-import-grafana/cache --out .tmp-source-import-grafana/grafana-operation-descriptor.json` imported 314 operations. The descriptor contains 52 recursive-schema gaps, including `#/components/schemas/Folder`, without credentials or connector-definition changes. | The temporary v2 lock permits Grafana's separate unbounded-input gaps; it exercises the real shared importer and is removed before commit. GitHub frozen artifacts remain separate checks. |
| OpenAPI 3.0 bounded reference siblings (#4326) | **Red (2026-08-23):** `go test -count=1 -v -timeout 20m ./cmd/connectorgen -run '^TestSourceImportAllowsOnlyDescriptiveOpenAPI30ReferenceSiblings$'` failed for both `description` and `summary` with `preflight source grammar ... ambiguous response reference with sibling field`. After the initial descriptive green slice, `readOnly` failed at the same schema-ref guard. The explicit non-equivalent `type` source-bound-gap test then failed at `schema reference type sibling conflicts with referenced schema`; the new unused-schema case initially produced no descriptor gap. | **Green (2026-08-23):** `go test -count=1 -v -timeout 20m ./cmd/connectorgen -run '^TestSourceImport(RetainsBoundedOpenAPI30ReferenceSiblings|PreflightsUnusedGrammarObjects|RetainsRecursiveSchemaReferencesAsSourceBoundGaps)$'` passes `description`, `summary`, `x-` extension, `readOnly`, equivalent `type`, a non-equivalent `type` retained as `cli-openapi30-reference-sibling-foundation-r1` with the `#/components/schemas/identifier` pointer, an unused component emitted through top-level descriptor gaps, and rejection of response `content`. | Only OpenAPI 3.0 `description`, `summary`, schema `readOnly`, and schema `type` are admitted. A type differing from the resolved target is retained exactly but makes the source operation non-mergeable through the established runtime-gap path; unused evidence uses the existing descriptor-level gap path. Other semantic siblings remain rejected. |
| Real #4326 source documents | **Asana green (2026-08-23):** the provider-pinned artifact (`3,066,750` bytes / `cb3b90f4e0af56035eab0c648974f625b942a28a7144aa6c2326e38ca0bb3d56`) imports all 249 locked operations and `--check` verifies; the two differing `type` overlays are explicit source-bound gaps, including `#/components/schemas/MembershipResponse` for `POST /memberships`. **GitLab boundary:** the live `master` source no longer matches its historical strict lock (`source-lock refresh required: fetched artifact does not match locked bytes and SHA-256`). **Docker Hub boundary:** its exact pinned `148,322` byte / `99d9d53c2d93656a3c66d604885abd153dc5df285abc0ecb13802a3bc53d0756` artifact advances past the original descriptive response sibling, then fails preflight on its dangling schema reference `#/components/responses/team_repo`. | **Green for #4326 itself:** Asana exercises the shared affected import path with no connector-local mutation. GitLab refresh and Docker Hub's malformed schema reference are recorded external source boundaries, not suppressed or repaired in this resolver-only PR. | No rendered-reference provenance, query identity, source-lock refresh, or provider-document repair was added. |
