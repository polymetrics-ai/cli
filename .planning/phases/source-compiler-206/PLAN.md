# Source compiler206 — issue4325

Firstmate206 selects architecture196A (REPORT SHA256 fba1262220b9b109bea5b926a50a4dbe06fb9414ee3b19aef234557b72b91839). Baseline6f9366b4904ad7c54914ca14cbbab21e0b5e35f6. Sole shared writer; immutable205 archive preserved. No push or acceptance.

GSD: resolved scripts/gsd sources and generated/read discuss-phase, plan-phase --tdd, execute-phase source-compiler-206 at official20297a8f. Inline execution follows canonical sole-writer contract forbidding extra implementation roles; selected196A report and Firstmate206 supply discussion decisions. No new architecture or dependency.

Skills retained from current session: golang-how-to, cli, testing, error-handling, security, safety, design-patterns, structs-interfaces, context, concurrency, documentation, connector-lane-build-order and pinned exhaustive-review.

## Obligation manifest

| Unit | Required behavior | Status / proof plan |
|---|---|---|
| U1 | Closed source_projection v1 in schema4; pinned inventories and coordinates; legacy compatibility; archived inherited parameters | In progress: actual normalizer raw/archive inherited/override/duplicate cases before repair; strict grammar and mixed-arm rejection next |
| U2 | Pure lowering into sole canonical graph; exact joins, stable compatibility identities, types/requiredness | Pending: CircleCI query, DockerHub inherited paths, Asana typed mutation source-only admitted local wire/records; literal independent oracle |
| U3 | Runtime bridge selected by196B | Firstmate212 selects final196B; implementation pending, no alternative runtime semantics |
| U4 | Loaded selected runtime typed batchable saved action predicate and App preflight | Pending; zero-I/O refusal, legacy nil/default and valid nonbatchable direct controls |
| U5 | Full retained closure locked reread, deterministic generation and execution-only transfer | Pending; source replacement beforeCURRENT, cold/warm identity, exact set corruption and destination hashes |

All U1–U5 and original309 obligations remain. Interface checkpoint after U1 and first ordinary U2 green is not acceptance. Retain source4343/lane30401 accounting separately from execution proof. No per-provider importer, source runtime reader or authored MapsTo.

## Verification and documentation

Sequential original receipt runner captures unique206 names, -count=1 focused Go tests -timeout20m. Preserve failing attempts and raw input/source pins. Independent literal expected wire/header/record values, exact negative admission and no-send evidence required; parent/child counts never substitute contracts. Broaden affected suites after coherent source changes. Final unit requires five REVIEW-EVIDENCE safeguards, all ten review lenses and independent complete judgment.

Same-change SOURCE-LOCK-VNEXT, IMPLEMENTATION-PROCEDURE, connector skill and Atlas documentation only for implemented shapes. Existing lock-render help and command/check/transfer recipe must match actual behavior; runtime namespace/docs/website parity evaluated for actual public surface changes. No new command currently planned.

## Firstmate210 B — diagnostic source coordinates

Root owns existing vnext_graph/admission typed diagnostic mapping regression. Preserve original invalid graph test. Red: current actual lock-render admission must retain /operations/0/stream; capture before repair. Green: consume BundleLocation through wrapped errors, translate rendered ordering to source operation/command and preserve sanitized suffix; add reordered/wrapped/unlocated controls. Test-only210 child owns A/C/D/E/F/G in isolated branch, never these production files.

## Firstmate212 runtime contract adoption

196B final REPORT SHA256 f62758665455985c149e3cd886fc08ec91ac8436e8820ae1a51f3fa22e7bd81a read in full. U3 now authorized: typed query/body placement, strict source-selected completion, next-link ownership, complete Notion/Sentry declaration migration, one typed direct/saved/prepared form encoder, explicit terminal-node CodeQL traversal. E1–E4/E6/E7 remain coherent dependent units with separate named RED/GREEN groups; no tiny-unit review gates.

CP18-05 no-materialization uses explicit existing full_refresh_overwrite. Preserve original full_append assertion as mode mismatch and add acknowledged page-one/checkpoint/reopen compatibility. Preserve legacy full_refresh_append nontransactional failed-attempt WAL behavior; proposed exact retry counts are not accepted mode contracts. No WAL deletion, implicit mode switch or new E5 implementation solely for that mismatch. Exact original mode obligations remain audited.

209 receives source-bound multipart envelope cap addendum once; no new design context.

## Firstmate217 selected209 and parallel218

Completed209 report and combined handoff read in full; SHA256 baf83b88f453634f069bd13a8106c0ceecc2f1365ca8313783546753c09e00c9. B0/B1 selected within shared206/212: optional file-only identity/url_percent_utf8 policy, actual serializer metadata measurement, separately enforced65536 PM metadata budget and104857600 file bound deriving104923136 wire cap for adopted Asana profile, shared prepared approval identity and unchanged omitted legacy fields. Required B7 source/header/content/approval/overflow/cleanup matrix precedes implementation completion; no shared multipart edit yet. Independent218 owns selected209 A0/A1/A2 and WR-205-01 in isolated210 branch; parent retains normal integration and symbol overlap reconciliation. No extra review or provider-live scope.

## U2 first source-derived scoped string inputs

Before lowerer edits, extend the real publication-to-engine fixture with archived inherited required namespace/repository path inputs, an operation-level override, optional owner-id query omitted/present, and missing required path zero-send. Expected routes/query/record IDs are literal and independent. This first slice uses source-declared string inputs through existing saved-stream config/template bindings, with copied source schema and conflict refusal; it does not claim direct CLI flags, integer/nullable/union projection, compatibility aliases or complete provider fixture parity. Those remain required subsequent slices.

Direct consumer follow-through: before production projection, require the same inherited path and optional query fixtures to produce api/widgets in the actual commandrunner.Preflight and Run path. Request flags contain literal provider names; the direct call receives no stream config. Assert full returned JSON and literal second request route/query, not generated MapsTo as the expected oracle. Explicit root lane remains authoritative; no direct projection when declared unsupported. Existing default bounded direct-read runtime policy is reused, not a fabricated source limit.

Typed mutation next: strict source write semantics require explicit mutation effect, one_request delivery, explicit batchability, bounded risk and single_attempt retry. Test valid true/false and missing/null/unknown/conflicting semantics through real decoder before adding grammar. Follow with source body/schema lowering and loaded saved-action predicate; grammar alone does not imply executable write support.

Retained envelope identity: actual CircleCI source stores operation_id outside source_operation. Before production correction, test valid envelope-only identity and conflicting inner/outer identity at actual publication; preserve exact source key and refuse disagreement. Stable normalization/compatibility remains a separate required naming slice.

## U4 loaded saved lane causal regression

Use legacy canonical roots with a concrete typed POST action and no reverse CLI command: implemented reverse must admit; unsupported reverse must refuse when the selected action is eligible. Separate explicit nonbatchable true/false lane controls. This reaches current real canonical lane heuristic before adding the selected engine preflight seam. Then move only reverse consistency to loaded selected runtime and reuse the same typed seam in App; preserve schema-less/hollow/hook and direct-only distinctions. All affected legacy root reconciliation remains required, not a hidden lane promotion.

App saved consumer regression: selected declarative runtime overrides the optional saved preflight with an independent sentinel; actual PlanReverseETL must propagate it before trying a deliberately absent warehouse table. Existing nonbatchable/direct command controls remain separate. Extend the existing shared bulk guard, whose callers cover plan/preview/execute/standing authorization; do not edit concurrent218 App lifecycle constructors.

First typed mutation lowerer regression uses a pinned schema2 POST body with closed nested data/name, explicit single-attempt batchable semantics, actual canonical publication/loaded saved preflight, invalid required field refusal, zero-send preview and literal local POST/body plus returned records-written outcome. This generic source fixture is not full Asana/allOf/compatibility or App approval proof; those remain mandatory. Retry policy maps only to the existing unkeyed non-delete writeRequester single-attempt seam.

Mutation boundary extension before correction: optional request-body presence is a distinct unaccounted source arm and must refuse rather than require a record body silently; test actual CURRENT preservation/no sends. Literal201 returned provider body and503 single physical attempt augment exact request validation. Full optional-body variant support remains required; refusal marks current unsupported shape, not programme completion.

U4 saved Asana follow-through: use the actual selected registry create_task with only local HTTP endpoint and synthetic auth; two independent nested data rows from disposable warehouse. Plan and preview send zero requests; absent approval refuses; reopened approved execution sends exact bodies once, reports two returned outcomes, and replay sends none. Existing execution artifact is the witness here, not yet the source-only Asana lowerer.

Next U2 scalar input slice: actual source-only publication and both saved/direct consumers for integer, boolean and >2^53 exact integer bounds. Preserve provider page_size wire spelling while using existing safe identifier/flag ownership. Literal wire and records, lower/upper/malformed direct no-send refusals precede production changes. Existing ExactNumber parameter/flag bounds are reused; no float round trip or paging-strategy inference.

217 B0/B1 starts in existing connsdk MultipartForm/File and shared encoder. Add only inert typed policy fields first, then capture real local-wire desired failures before encoder changes: independent raw ordinary filename headers; original source basename survives snapshot; policy/unsafe-name refusal; actual serialized metadata partition one-byte boundary, separate from file bytes. Existing Asana source-backed filename/exact-file-limit receipts remain original. Later engine admission/prepared identity and both real Asana aliases remain required before completion.

230 A1 documentary closure: add separately pinned non-inventory documents and raw byte-span citations under the same retained reader/revalidation budget. Before reading behavior changes, actual publication fixtures must show wrong document bytes/span hash still admitted; healthy source IDs/three-row wire remain unchanged. Strict identity/format/path/selector conflicts, byte bounds and unchanged CURRENT refusal follow. This slice does not implement A2 amendments or claim citation semantic relevance.

234 selects final231 REPORT SHA25600e6c4075cbc23d6688bedde2d91687ae44d4ef99ebdd1c07443adb459d95ec0. Implement request_inputs/input schema role and exact shared codec, selected immutable plans, all public Read guards and App pre-vault guard; preserve legacy configuration.233 child owns body pagination merge only. A minimal inert Go type/accessor checkpoint precedes loader/consumer RED; it grants no admitted capability by itself. Private StreamSpec per-invocation body carrier and immutable inputPlan allow233 to compile against the selected shared envelope without a second BodySchema declaration.


## Firstmate248 remaining234 integration batch

The basic scalar/doc230 checkpoint43be3ebf7 is preserved locally; merged233/R1 d4eb8e3a is not published. Integration receipt basic234-integration-248-01 ran143 events,142 passed, then TestBodyPagingSaved233 panicked because the historical injected compiled plan lacks the full schema now consumed by234. This is an integration fixture/contract boundary to classify with the complete body-input batch, not a provider failure or current full GREEN. No failed receipt is reused as passing evidence.

Complete remaining selected231 family before next discovery sweep:
- Inclusive exact integer/bigint/decimal bounds; exponent/work/aggregate budgets; native legacy scalar controls; every previously masked invalid case gets its own subtest.
- Absent versus explicit empty/zero/false/default, required path and optional wire omission; exact lexemes after config/endpoint/query overrides.
- Stream-local repeated names, unrelated required config, explicit aliases across path/query/header/body, escaped names, case-folded header identity, repeated value rejection.
- Real loaded Input schema role, absent/extra/wrong bindings, unused enum/pattern/format/default/bounds mismatch and canonical/source provenance correspondence. Source exclusions remain explicit.
- Real App metadata validation and counting vault/auth/rate/requester/WAL frontier, anonymous healthy execution, no public secret values. Optional-interface-only fixture is insufficient for full claim.
- Body233 integration: valid full compiled plan via actual loader, absent/corrupt private plan refuses safely, caller body/default/template preparation, engine-owned required fields after composition, final whole-envelope validation, path/query/header identity and no post-validation merge. Preserve233 literal bodies/IDs and path/cursor controls, adapting injected plans to the actual selected contract without weakening their assertions.
- Effective invalid initial and later paging values before physical send;229 signed URL ownership; prior acknowledged progress and cap/window limits remain honest.
- Execution-only cold/warm reload without source/proof files, order-independent generation/check, source-only current-generation joins, unaffected legacy configuration behavior.

Each family gets an anchored production-consumer selection and explicit healthy/falsifier cases. Existing unchanged original REDs remain retained; new fixture failures are labelled separately. Run one complete targeted discovery after writing the remaining cases, group repairs, then run failed/dependency-affected GREEN. Full suite/race/review later.243 approval identity and FC238-26 proof catalog remain separate established coherent families, with their complete prescribed matrices. No new architecture/agents.

Firstmate250 adds CP18-248-05 to the same compiler batch: source_projection.inventory currently hardcodes primary and cannot declare supplement class. Preserve legacy-compatible explicit generic source class and exact1752+2 identity; no GitLab special case. Existing real TestBatch1CP18GitLabSourceIdentity248 RED and healthy1752 control/source-identity248 original snapshot at worker d547497d are reused, not rerun unchanged. Full finding remains at the CP18 lane-proof-240/FINDINGS/CP18-248-05.md source.

248 pre-secret proof instrumentation: App currently holds a concrete private *vault.Vault, so a counting/failing adapter cannot be substituted. Extract only its existing Get/Put/Delete private interface, retaining Open’s concrete vault and an explicit compile-time assertion. This changes no routing or public capability; no invented behavioral RED for the mechanical seam. Real loaded-engine App tests will count the actual private storage call boundary and include a bypassed-validator fault control. This is the selected231 required proof adapter, not a new storage architecture.

248 aggregate resource closure uses the existing16MiB direct/body request budget as a PM aggregate scalar-preparation bound, separate from each1MiB scalar bound and provider schema constraints. Exactly16 one-MiB values are the preparation control;17 are refused by actual ValidateReadInputs before any network client. Final serialized body/query accounting remains independently required; do not interpret preparation control as successful16MiB HTTP execution.

Firstmate255 / CP18-252-07 is adopted as a separate required typed structured-query obligation: source inspection identifies37 source identities/67 variants requiring bracket array/hash serialization; this is prerequisite evidence, not executed RED. Current Query map[string]string/scalar materialization cannot claim it. Reuse existing238/source220/224 selected context for the smallest explicit shared contract and real generated flags/loader/engine/wire RED before implementation. No raw-query/JSON-string escape or provider branch. Publish the passing234 scalar/body corrections first; this extension and full GitLab completion remain pending. Original finding: operational-home data/cli-batch1-cp18-gitlab/lane-proof-240/FINDINGS/CP18-252-07.md.


## CP18-248-06 canonical local overwrite — execution addendum

Firstmate248/254 selects this remaining shared port after published234/class250.
Base6cf2272596d5f3c14c65da7644017504d0a44fce. Discuss: existing
synctransport.FullOverwriteDestination/FullOverwriteRun is the declared seam;
warehouse.stage-etl.v1 names local_warehouse_transport.go, local_warehouse.go,
warehouse/layout.go and parquet.go. Extend that implementation; no new runtime
selection, source declaration, receiver, SQL path, dependency or agent.
GSD sources discuss/plan/execute/verify resolved; generated discuss-phase,
plan-phase --tdd and execute-phase read. Existing inline/manual fallback remains:
Firstmate248 forbids resuming internal specialists; parent owns implementation.

TDD plan, before production edits:
1. Real loaded GitLab groups → App.RunETL → registered local warehouse: seed
   an existing table through full_append, then canonical full_overwrite with
   more records than batch size. Check exact IDs in durable Parquet and JSONL,
   request pages, completed counters, fresh App reads and committed checkpoint.
   Group empty/source404/malformed-later-page/cancel controls; prior WAL/table
   bytes and prior checkpoint must survive every prepublication source failure.
2. Run-scoped adapter negative matrix: mode/binding/generation/stream/workset
   identity/counts/tombstones/aggregate mismatch/canceled calls/repeated terminal
   calls. Per-batch private JSONL remains bounded and fsynced; single complete
   materialization/publish; idempotent abort only removes owned private files.
3. Real read-back verifies bytes/counts and original acknowledgement binding;
   empty durable recovery uses existing EmptyPublicationReadBackDestination
   and derives current structural paths from bound identity, never receipt paths.
4. Reuse existing JSONL WAL then atomic single-Parquet publication order.
   Do not claim atomic WAL/table pair or physical power-loss proof. Test failures
   around publication, directory durability/read-back and postpublish reconciliation;
   no checkpoint precedes successful durable acknowledgement/read-back.
5. Update same Atlas entry/docs and affected App/warehouse/synctransport/CLI
   expectations coherently. GitLab worker's four historical refusal cases remain
   original evidence until updated against the actual new candidate. No source C2
   or full connector acceptance follows from this shared behavioral slice.

Red: pending actual production consumer run. Green: pending implementation.
Remaining234 resource/authority closure, structured query255, approval243 and
completion/proof244 remain independent required obligations.


## CP18-252-07 typed query execution plan — Firstmate255

Scope remains37 exact source identities/67 variants in the original gap file;
QUERY-255-context.json pins it and the retained REST179–224/groups463–467
provider text. Inspection only; no executed RED or per-source serialization
acceptance yet. Source arrays/hashes cannot pass through scalar stringification.

Reuse preserved235 typed_form.go at0a6886973 as the bounded pair serializer;
keep its form write activation and243 approval repair separate. Preserve the
original implementation and receipts. Extend its explicit array dialect for
bracket-suffixed repeated scalar keys; repeated unsuffixed and indexed arrays
remain distinct. No generic flattening or JSON-looking string success claim.

Before production consumer changes, write an independent generated/source →
loader → commandrunner/engine/App matrix. Source schema remains derived from
actual parameters; reviewed query encoding semantics describe only noninferable
wire dialect and PM resource limits, never maps_to or source membership. The
existing selected request-input envelope owns query schema, aliases, defaults
and final validation. A typed query-value channel preserves object/array values
and exact numbers; legacy scalar Query remains unchanged. Conflicting channels,
undeclared parameters, wrong types and unsupported encoding refuse pre-I/O.

Required complete grouped cases: scalar legacy healthy control; ordered duplicate
scalar arrays with bracket/repeated/indexed distinction; deterministic named/map
objects; indexed array-of-objects; explicit empty/null/absence policies; exact
big integers/decimals; UTF8 and + & = / escaping; ambiguous bracket-bearing map
keys and encoded-coordinate collisions; depth/member/item/pair/input/final-wire
budgets; source/command/schema binding mismatch; cold/warm loaded execution;
caller mutation isolation; saved metadata validation before vault; direct one-page
and saved pagination effective-query identity, later-link ownership and zero
foreign sends. Literal expected bytes/pairs and returned IDs are independent of
the serializer. Blocked downstream cases are labelled at their actual frontier.

GSD discuss/plan/execute continues inline under248's no-new-helper policy. No
provider-specific executor, live credential, new foundation architecture or source
capability promotion. Red: pending actual production path. Green: pending.

Firstmate261 adds provisional running252 scalar input groups to this same work: per_page200 source identities, max_results2, limit1 have reached undeclared-query refusal before I/O; scope3 is currently join inspection, not another observed guard. Frozen84f6d456/remaining-cli252 continues untouched. Preserve original prefix7299939 bytes SHA256fe819e0b5955652c24d8ac017f2a772866f3cdad206705b4d986825a1e2b5a07 and exact offsets in PROVISIONAL-FINDINGS-252.md. Assess existing source/parameter render seam and declared paging ownership without loosening query guards. Helper second_400 delta remains worker-owned pending normal terminal merge (HELPER-CUSTODY-259.patch).

Firstmate262: four source identities have actual invalid-planning acceptance after healthy controls: pagination_limit 1..100, expires_in 1..43200, rollback/rollout percentage 0..100. Preserve eight original observed cases and original raw offsets in PROVISIONAL-INPUT-BOUNDS-252.json; later contaminated snapshot comparisons are not independent defects. Locate ordinary declaration/schema loss before adding runtime behavior. Running252 remains frozen; focused causal source/consumer proof belongs to this parent. Red: original connector observations retained; new causal regression pending. Green: pending.

Firstmate263 adopted: original running252 observed five sends after complete approved DELETE/connection close across direct and saved roles. Existing writeRequester intentionally permits explicitly idempotent deletes; existing TestDeclarativeWriteRetryPolicy requires that behavior. This is a policy/declaration reconciliation frontier, not automatic authority to remove idempotent retries globally. Determine selected source single_attempt mapping and preserve provider-key/read retry siblings. Query255/261 and bounds262 remain required. No new runtime regression or repair yet.

## Source bounds262 scoped normalization slice

Implement source-owned nullable scalar lowering for mutation schemas through the existing source projection/schema compiler, retaining exact minimum/maximum and all other constraints. This does not repair legacy authored omissions by itself. Before production edits extend the real source lock-render → admitted generation → fresh engine loader → ValidateWrite/DryRunWrite/Write fixture with nonnullable and nullable bounded integer cases, null/endpoint healthy controls and below/above/wrong-type refusals with zero sends. Nonnullable baseline success is disconfirmation of a universal compiler bounds-loss claim; nullable admission refusal is the expected new capability frontier, not the original invalid-planning symptom. Preserve raw numeric precision and refuse unsupported schema compositions; keep scalar read input scope unchanged. Red: pending fresh original capture. Green: pending.

## Typed query255 first production-path matrix

Before query production edits, exercise retained explicit query serialization through the existing source lock-render/publisher/loader/commandrunner and saved read helper. Start with source-declared deepObject named string properties (the same bracket coordinate behavior as retained GitLab custom_attributes) and source-declared form/explode repeated arrays; literal request bytes distinguish JSON text from encoded pairs. These representative source fixtures are not a declaration that all37 GitLab identities select either dialect. Original bracket-suffixed-array semantics and source-specific joins remain required. Each source-only admission failure blocks downstream wire assertions and is labelled accordingly. Red: pending. Green: pending.

Firstmate264 adopted while255 remains active: ten provisional numeric-arm wire mismatches/six source identities (integer|string unions) remain a separate next typed-input obligation; preserve allow_bare_string compatibility and distinguish typed warehouse values. PROVISIONAL-SCALAR-UNION-252.json pins raw prefix11963715 SHA256781ff35bcb3b1569dc4d7051723ffcb5b7b485d2d018f6c7b0d0802f61fed6df. Exact four-schema/eight-bound legacy drafts render/check privately under draft-bound-constraints252; no runtime GREEN/transfer yet. Nullable15098cee does not close those legacy omissions. No running GitLab changes.
