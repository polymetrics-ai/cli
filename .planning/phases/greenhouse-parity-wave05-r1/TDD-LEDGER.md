# TDD ledger — Greenhouse parity wave05 r1

| Slice | Red / baseline | Green result | Evidence |
| --- | --- | --- | --- |
| Official ledger reconciliation | Current `api_surface.json` had 129 rows vs parent 134 target; fresh official Harvest HTML parsing found 135 documented operation headings including `DELETE: Destroy a candidate tag`. Existing rows missed v2 user/job-post/tag-delete operations and used 3 `excluded` rows. | `api_surface.json` now has 135 duplicate-free rows: 69 streams, 64 typed writes, 2 blocked binary attachment upload operations, 0 exclusions. | `go run ./cmd/connectorgen validate internal/connectors/defs` -> 550 connectors, 0 findings; inventory script counts match. |
| New/changed writes | Missing write actions/fixtures for v2 user, job-post, scheduled-interview v2, job-opening destroy, and candidate-tag destroy; hiring-team write paths drifted from official docs. | Added 9 fixture-backed typed writes; corrected hiring-team writes to official `/v1/jobs/{id}`; removed unsupported attachment writes. | `go test ./internal/connectors/conformance -run 'TestConformance/greenhouse' -count=1` passed. |
| Unsupported operations | Attachment uploads were exposed as plain JSON writes despite official docs allowing base64 content or external URL, with no byte-bound enforcement. | Both attachment upload operations are blocked by default with official-source evidence; no generic file/path/body escape hatch added. | `api_surface.json` blocked rows for `/v1/applications/{application_id}/attachments` and `/v1/candidates/{candidate_id}/attachments`. |
| Destructive/admin write safety | Existing destructive schemas were additionalProperties-permissive. | Touched destructive/admin actions now use closed schemas; destructive actions keep `confirm: destructive`; idempotency notes documented where Greenhouse docs state enable/disable are safe to replay. | `write_request_shape` conformance passed for every Greenhouse write fixture. |
| CLI/docs/generated surfaces | Greenhouse docs/catalog generated surfaces were stale after definition changes. | Regenerated and retained Greenhouse manual, skill, and catalog count changes; internal `docs.md` rewritten with final counts and blocked-operation evidence. | `go run ./cmd/pm docs validate --connectors-dir docs/connectors` passed. |
