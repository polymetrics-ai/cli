# Issue #397 PM First-Round Review System TDD Ledger

**Status:** captain-authorized advisory F1–F6 correction full GREEN on the uncommitted candidate; exact clean commit + self-review pending; correction budget remains 2/5

**Lifecycle:** active `/pm-orchestrate` owner because `programming-loop` is absent from the 69-command registry

**Stable lineage:** `0f8c964ba9cfbe1b1eec8e7998eacf4158ef0e20...pm-first-round-review-system-r1`
**Correction budget:** 2/5; provider/packet/OAuth/WebSocket/context retries are separate operational attempts and do not consume rounds

| ID | Risk / required behavior | RED evidence | GREEN target | State |
|---|---|---|---|---|
| A495/M495/STATE/PACKET/CLOSURE/LINEAGE/OWNER/SHEPHERD/MEASURE/RESEARCH/IMPACT-1..9/LAB-1..6/CONTRACT-2/R1-A..O/R2-A..O/N1..6/SCOPE-1..2 | historical round-0/1/2 risks (readiness gate, disposition parsing, authority/closure/lineage, packet coverage/bounds, typed bidirectional impact graph, disposable labs, one-owner synthesis, downstream Shepherd, measured fixtures, exact-head identity, immutable manifest, route/authority/state parity) | committed RED per round | green; full RED->GREEN detail in git history and `REVIEW-R1-DISPOSITION.md`/`REVIEW-R2-DISPOSITION.md` | green |

| A495/M495/STATE/PACKET/CLOSURE/LINEAGE/OWNER/SHEPHERD/MEASURE/RESEARCH/IMPACT-1..9/LAB-1..6/CONTRACT-2/R1-A..O/R2-A..O/N1..6/SCOPE-1..2 | historical round-0/1/2 risks (readiness gate, disposition parsing, authority/closure/lineage, packet coverage/bounds, typed bidirectional impact graph, disposable labs, one-owner synthesis, downstream Shepherd, measured fixtures, exact-head identity, immutable manifest, route/authority/state parity) | committed RED per round | green; full RED->GREEN detail in git history and `REVIEW-R1-DISPOSITION.md`/`REVIEW-R2-DISPOSITION.md` | green |
| F1 | accepted `combined`/`impact_graph` classification can silently omit changed files while compile reports ready | focused RED: accepted `combined` probe exited 0 instead of named nonzero `packet_coverage` block | data-driven classified-domain loop plus closing per-file assignment and exact diff-byte postcondition; violating compile blocks with `packet_coverage` | green focused |
| F1 | accepted `combined`/`impact_graph` classification can silently omit changed files while compile reports ready | focused RED: accepted `combined` probe exited 0 instead of named nonzero `packet_coverage` block | data-driven classified-domain loop plus closing per-file assignment and exact diff-byte postcondition; violating compile blocks with `packet_coverage` | green focused |
| F1-BYTES | normal complete range must prove packed changed-slice bytes equal each exact `git diff base...head -- path` payload | permanent byte-level assertion added for every changed file; execution initially stopped first on missing F5 field | exact byte equality for every changed file under the unchanged limits | green focused |
| F1-BYTES | normal complete range must prove packed changed-slice bytes equal each exact `git diff base...head -- path` payload | permanent byte-level assertion added for every changed file; execution initially stopped first on missing F5 field | exact byte equality for every changed file under the unchanged limits | green focused |
| F2 | prompt delegates to contracts absent from most rendered packets | focused renderer RED rejected the old delegated contract instruction | remove dishonest delegation and state the exact supplied/assumed reviewer contract plainly | green focused |
| F2 | prompt delegates to contracts absent from most rendered packets | focused renderer RED rejected the old delegated contract instruction | remove dishonest delegation and state the exact supplied/assumed reviewer contract plainly | green focused |
| F3 | compressed prompt deleted the actionable-vs-residual-risk criterion | focused renderer RED could not find the actionability clause | restore candidate-introduced/activated/worsened/relied-on criterion within unchanged byte cap | green focused |
| F3 | compressed prompt deleted the actionable-vs-residual-risk criterion | focused renderer RED could not find the actionability clause | restore candidate-introduced/activated/worsened/relied-on criterion within unchanged byte cap | green focused |
| F4 | impact anchors do not disclose per-file provided/total byte coverage | focused compile/response RED found no coverage rows and no synthesis-bound echo | deterministic per-impact-file provided/total bytes in manifest/packet reviewer input with exact synthesis echo | green focused |
| F4 | impact anchors do not disclose per-file provided/total byte coverage | focused compile/response RED found no coverage rows and no synthesis-bound echo | deterministic per-impact-file provided/total bytes in manifest/packet reviewer input with exact synthesis echo | green focused |
| F5 | comment claims two packets of headroom while achieved margin is one and manifest omits metric | focused compile RED raised `KeyError: packet_headroom` | truthful comment and first-class arithmetic headroom metric; no behavior change | green focused |
| F5 | comment claims two packets of headroom while achieved margin is one and manifest omits metric | focused compile RED raised `KeyError: packet_headroom` | truthful comment and first-class arithmetic headroom metric; no behavior change | green focused |
| F6 | replay re-appends deterministic occurrence id | focused replay RED duplicated and changed occurrence lists for four roots | set-union occurrence ids and idempotent replay regression | green focused |
| F6 | replay re-appends deterministic occurrence id | focused replay RED duplicated and changed occurrence lists for four roots | set-union occurrence ids and idempotent replay regression | green focused |
| PERF-F1 | corrections must not trade detection quality or hard bounds for speed | before: 14.24s, 285,360,128-byte max RSS, 63 packets, max 29,994 bytes, 1.018025x duplication, derived headroom 1 | no regression in packet/bound/duplication metrics; three deterministic compiles; take only measured safe performance wins | green: after ~14.6s, 63 packets, dup 1.017594 (improved), headroom 1, max 30000 (≤ hard cap 30000), 0 overflow, stable across 3 compiles |
| PERF-F1 | corrections must not trade detection quality or hard bounds for speed | before: 14.24s, 285,360,128-byte max RSS, 63 packets, max 29,994 bytes, 1.018025x duplication, derived headroom 1 | no regression in packet/bound/duplication metrics; three deterministic compiles; take only measured safe performance wins | green: after ~14.6s, 63 packets, dup 1.017594 (improved), headroom 1, max 30000 (≤ hard cap 30000), 0 overflow, stable across 3 compiles |

## Required measurement fields

- first-round true positives, false positives, false negatives;
- recall, precision, escaped-defect rate, false-positive rate;
- exact-version invalidations;
- review rounds and context overflows;
- wall-clock latency;
- input/output tokens and cost when available, otherwise explicit `unavailable` with reason;
- historical, held-out fixture, and prospective observation scopes separated.

## Evidence log

- 2026-07-23/24 (condensed; full detail in git history and REVIEW-R1/R2 artifacts): post-#495 base `0f8c964ba9c...` verified clean; GSD doctor/list passed with no `programming-loop`; round-0 corpus frozen (`inputs.json` `21048975...`, `oracle.json` `5fc03b6e...`) and RED→GREEN reached at head `7f1b2d8fe`. Captain graph/lab requirements: `ALGORITHM-RESEARCH.md` benchmark, correction corpus frozen (`8b78b7ce...`/`93bef77f...`), RED→GREEN at `0e210d18`, full verification at `e4ca19ce`. Codex round 1 at `b1d869732`/tree `ec828909` (17/17, 89 findings, `REVIEW-R1-DISPOSITION.md`, round 1/5), committed RED `58df3909`, focused GREEN `89c86dd5` (41 packets, max 29,982/30,000), full verification `528dcc68` (`internal/cli` 462.828s). Recurrence round 2 at `92ce5e6a1`/tree `0a3c3eda` (41/41, 141 findings, `REVIEW-R2-DISPOSITION.md`, lineage 2/5); committed RED `d61799ad9`; route/docs/state and fresh-runtime lab-fixture recovery closed; combined full verification passed (`internal/cli` 467.908s, make verify, 547 connectors, safe reverse ETL). Exact-commit RED `1e640f9a4` exposed the 77/64 packet block; bounded-efficiency GREEN reached 63 packets ≤30,000 bytes; bounded replacement full GREEN and committed `c28126bb3` reconfirmed clean.
- 2026-07-25 advisory correction authorization: captain halted the partial prospective R3 at `c28126bb3`; no synthesis or Shepherd verdict exists. The independent advisory F1–F6 report is not PM coverage and does not consume a correction round. Captain authorized the six smallest-safe corrections with permanent tests, leaving usage at 2/5. GSD doctor/list passed; `programming-loop` remains absent, so manual GSD fallback is recorded. Baseline trust-bound compile: ready/0 findings, 63 packets, max 29,994 rendered bytes, 1.018025x slice duplication, one derived packet of headroom, 14.24s wall time, and 285,360,128-byte maximum RSS. Production edits had not begun.
- 2026-07-25 advisory correction RED: after planning/state/checklist edits and permanent test additions, shell syntax, Shellcheck, and diff checks passed. `bash scripts/tests/pm-review-system.sh` exited 1 with five independent groups: missing `packet_headroom`; accepted `combined` rule did not block; renderer lacked honest-contract/actionability text; packets/responses lacked per-impact-file byte disclosure; and dedup replay duplicated deterministic occurrence ids across four roots. The all-changed-file exact diff-byte assertion is frozen in the same test. No production treatment had started.
- 2026-07-25 advisory correction focused GREEN: `bash scripts/tests/pm-review-system.sh` passed. The classified-domain loop is data-driven with reserved split-role classifications caught by a closing assignment/diff-byte postcondition; valid changed slices use deterministic exact-accounted cross-role best-fit. Packet/response contracts now expose exact per-impact-file provided/available bytes and synthesis rejects alteration. The prompt states its held role/schema assumption, restores the actionability criterion, and labels low ratios as anchors. Replay deduplicates occurrence ids; the manifest reports arithmetic packet headroom and the coalescing comment reports a target rather than a guarantee.
- 2026-07-25 packet-budget honesty note: the F2/F3 correction requires a self-contained honest prompt (~1,163 bytes) that no longer cites unprovided contracts. Because the prompt is charged once per packet, plus legitimate content growth (corrected compiler, new tests, larger impact graph), the accumulated candidate initially compiled at 64 packets. CI (Linux) produced a slightly larger impact graph than local (macOS), tipping 64→65 (> the 64 hard cap) and blocking the `verify` job — the fragility of zero headroom. To restore a robust margin, superseded process narration in `TDD-LEDGER.md`, `VERIFICATION.md`, `PLAN.md`, and `ALGORITHM-RESEARCH.md` was condensed (frozen R1/R2 dispositions and the other workstream's orchestration-state files were left untouched), returning the candidate to **63 packets, headroom 1, ready**, every packet ≤ 30,000 rendered bytes, 0 overflow, within the unchanged 64-packet hard cap. No limit, bound, gate, quality check, or disclosure was weakened. The focused fixture asserts ≤64 packets, consistent headroom, 0 overflow, max ≤30,000 bytes, and ≤1.02x duplication.
- 2026-07-25 bounded performance pass GREEN: three safe output-preserving optimizations were added — a cheap lower-bound prefilter before the changed-slice best-fit deep copy, an `exact_edge_slices` cache keyed by sorted edge ids, and an `account_packet` fixed-point early stop. An isolated equivalence probe compiled the perf-optimized and perf-reverted compilers against the same frozen `c28126bb3` tree and proved byte-identical packets and coverage. Wall clock returned to ~14.6s from a ~15.8s pre-optimization regression; three independent compiles produce one stable semantic-manifest digest. Baseline/after evidence is in `data/cli-first-round-review-audit-r1/f1-correction-{baseline-c28126bb33,after-uncommitted}/measurement.json`.
- 2026-07-25 packet-budget content condensation: to keep the candidate within the hard 64-packet cap after adding the required honest prompt and F4 disclosure, the superseded process narration in `TDD-LEDGER.md` (76 historical risk rows plus round-0/1/2 evidence entries), `VERIFICATION.md` (historical checklists), and `PLAN.md` were condensed to compact summaries. Exact SHAs, correction lineage, F1–F6 records, and pointers to `REVIEW-R1/R2-DISPOSITION.md` are preserved; no test, gate, bound, or code behavior changed.
- 2026-07-25 advisory correction full GREEN (uncommitted candidate): `git diff --check`, `gofmt -l cmd internal` (clean), `go vet ./...`, `go test -timeout 20m ./...` (all pass), `go build ./cmd/pm`, `go mod verify`, `go mod tidy -diff`, and `make verify` all passed. `make verify` reported 0 golangci-lint issues, 547 connector definitions, the PM review-system/orchestrator/Pi-routing/Shepherd stall/watchdog/verdict suites, and safe sample reverse ETL in plan → preview → approval → execute order. Static Python/shell/Shellcheck/JSON/YAML/schema checks passed.
- 2026-07-25 CI-robustness follow-up: the first correction commit (`0933611bd`) plus self-review docs (`d5d5fb7b0`) compiled at 64/headroom-0 locally but CI's `verify` blocked because the Linux impact graph tipped it to 65. Condensing process narration in PLAN/ALGORITHM-RESEARCH (only docs; no code/test change) restored 63/headroom-1. Focused PM/orchestrator/routing suites remain green; Go build/test/vet are unaffected (only docs changed). This is the CI-safe candidate.
