# TDD Ledger

| Slice | RED evidence | GREEN evidence | Status |
| --- | --- | --- | --- |
| #373 qualification | `new-milestone` returned with M001 still in `pre-planning`; unknown `plan` became a quick task; first actual Sol session exposed thinking `off` | Governed query works; settings mismatch fails admission; a disposable governed session observed `openai-codex/gpt-5.6-sol` with `thinkingLevel: high` and stopped at the real depth gate | PARTIAL |
| #374 workflow contract | Missing four-field dispatch, forbidden typed tool, unsafe scope, and overlong handoff tests failed first | Project preference/hooks, agents, skill, contracts, schemas, and negative tests pass | GREEN |
| #375 domain/store | Transition, fencing, outbox, and module-isolation tests failed before packages existed | Typed domain, SQLite/WAL grant/lease/outbox store, and root isolation pass | GREEN |
| #376 runtime | Missing runner plus malformed/oversized event, silent-process, blocked-exit, and premature-success tests | Supervised process tree, bounded events, heartbeat, query, and early-exit reconciliation pass; fire-and-forget UI misclassification found and fixed by canary | GREEN |
| #377 authority | Stale-head/model/expiry, duplicate effect, missing grant, and merge-denial tests | Exact-head attestation/recheck and grant-gated idempotent outbox pass; credentialed publisher remains disabled | PARTIAL |
| #378 telemetry | Raw payload, duplicate, and torn-tail tests | Fsynced normalized JSONL spool with recovery and 0600 files passes; analytics exporters remain pending | PARTIAL |
| #379 replay/canary | Named failure-class guards failed against absent controller | Core incident guard matrix and intake canary pass; issue-to-draft-PR canary and legacy cutover remain pending | PARTIAL |
| adversarial runtime hardening | Tests exposed synchronous human-gate waits, missing response timeout, timestamp run IDs, unbound issue context, valid `skip` rejection, fictional `complete`, tool-error loss, project thinking override, symlink escape, and outbox key collision | Human questions are asynchronous with <=15-second heartbeats and controller-first cancellation; stable issue leases, typed JSON intake, immutable milestone binding, persistent attempts/human resume, compact model/thinking events, real 1.11 query shapes, no generic recover/auto, minimal child environment, protected external state, clean-head recording, and collision checks pass unit/race/vet | GREEN |
| protected canary continuation | The adopted milestone's first fenced `next` returned exit 10 before any agent event because GSD's DB held an unplanned milestone that its markdown projector cannot render | Correctly persisted `blocked`, released the controller lease, reported zero observed tool/heartbeat events, and refused implicit retry; interactive GSD repair plus explicit human resume is required | BLOCKED |
| Podman image qualification | `go test ./internal/gsd -run TestContainerImageReusesBaseNonRootIdentity -count=1` failed because `node:24.13.1-bookworm-slim` already owns UID 1000 while the Containerfile attempted to create another UID 1000 account; the real `podman build` failed at the same `useradd` step | Reused the base UID by renaming `node` to `shepherd`; the image built and probes reported UID/GID 1000, non-root writable home, `USER shepherd`, and GSD 1.11.0 | GREEN |
| Podman version admission | The real image emitted bare `1.11.0`, while `ValidatePinnedContainer` required `GSD v1.11.0`; `go test ./internal/gsd -run TestValidateContainerVersionOutputRequiresExactPinnedVersion -count=1` failed before the exact-output validator existed | Exact trimmed equality accepts the real output and rejects wrong, decorated, and substring-spoofed versions; real Shepherd admission passed | GREEN |
| Podman GSD startup | The governed query passed version/mount admission, then real GSD exited because the slim Node image has no `git` on `PATH` | After approved dependency installation, the same governed query completed in Podman and reported isolated `pre-planning`/DB-unavailable state without importing host `.planning` | GREEN |
| Podman runtime packages | Official GSD 1.11 prerequisites and packaged preflight require Node >=22, npm, Git >=2.20, and provider credentials; Node/npm/auth were present, while Git and the base image's CA bundle were missing. `go test ./internal/gsd -run TestContainerImageInstallsRequiredGSDRuntimePackages -count=1` failed on the absent apt setup | Installed only `git` and `ca-certificates` with no recommends; probes passed for Node 24.13.1, npm 11.8.0, Git 2.39.5, GSD 1.11.0, CA trust, non-root identity, writable home, and empty apt lists | GREEN |
| governed agent toolchain | Policy tests failed before Go/Make/jq/ripgrep, pinned browser CLI, and read-only fetch existed. Real builds exposed absent Linux ARM64 Chrome, npm `ECONNRESET`, and Debian login-shell PATH reset hiding the copied Go binary. | The image builds and probes report GSD 1.11.0, Go 1.25.12, Make, Git, jq, ripgrep, agent-browser 0.31.1, Chromium, bounded GET, rejected POST, no `gh`, and successful nested-module plus CLI package tests. Browser content boundaries appeared and `eval` was denied. | GREEN |
| trusted research services | Tests failed before controller-owned Context7 configuration and a private SearXNG sidecar existed. First SearXNG start refused the default secret and restart-looped. | Shepherd writes only the trusted Context7 HTTP MCP URL into protected state. SearXNG now uses an exact image digest, generates its secret inside the container at runtime, publishes no host port, and returned a valid JSON search over the private network. | GREEN |
| local programming-loop command | `scripts/gsd prompt programming-loop ...` failed because only the upstream registry was loaded and the Pi extension mirrored the same incomplete registry. | A repo-local registry is merged by both compatibility surfaces; the prompt renders and `scripts/gsd doctor` reports 70 commands. Official GSD Pi remains the actual workflow runtime. | GREEN |
| fast-exit event drain | Final verification reproduced a race where a fast exit-10 child let `cmd.Wait` close `StdoutPipe` before the scanner observed EOF, misclassifying blocked as error. | Wait is sequenced after event scanning finishes, and the blocked-exit regression now runs 25 consecutive attempts. | GREEN |
| host-only official GSD Pi migration | Existing host execution had no delivery-scoped `GSD_STATE_DIR`, omitted governed Git identity, and had not proved that tracked `.gsd` policy survives a real local GSD run. The first focused test failed because the runner had no `StateDir` contract. | The runner now rejects state inside the project, exports a delivery-scoped official `GSD_STATE_DIR`, supplies safe-directory and fixed Git identity configuration, and passes unit/race/vet/build gates. Real Asana acceptance and Podman removal remain pending. | PARTIAL |
| headless pre-planning advance | Real Asana M001 remained in `pre-planning`: official `headless next` attempted an interactive menu unavailable in RPC mode and exited 10. New tests first failed because Shepherd rejected `discuss` and reconciliation treated the next pending unit as an incomplete current unit. | Shepherd permits only a targeted canonical `discuss-milestone`, derives its M-ID from the fenced pre-run query, and accepts the resulting next dispatch as one completed unit. Real Asana M001 advanced to `research-milestone` after its context write. | GREEN |
| requested terminal event projection | The real targeted Asana discuss reached `agent_end`, but Shepherd failed because its CLI requested that event while the projector omitted it from the allowlist. A focused regression test reproduced `event type "agent_end" is not allowlisted`. | The projector accepts `agent_end`, retains only compact model identity, and drops message content. Multiple real resumed Asana rounds completed with `openai-codex/gpt-5.6-sol`. | GREEN |
| multi-turn local discussion continuity | After answering a real Asana interview round, official GSD ended that agent turn successfully while M001 correctly remained `discuss-milestone`; a fresh process would otherwise start a new session and lose the prior answers. Tests first failed because no worktree-bound session resolver existed and resumed discuss arguments were rejected. | `--continue-unit` resumes only the newest valid Pi session header whose `cwd` exactly matches the configured worktree. Real Asana context retained earlier answers across three resumed rounds and reached the depth/context write. | GREEN |
| bounded upstream terminal diagnostics | The first continuation exited before agent events, but Shepherd reported only `exit status 1`, hiding the actionable official GSD reason. A focused test first failed because no bounded diagnostic projector existed. | Terminal errors now include only the final non-empty stderr line, stripped of control bytes and capped at 512 bytes; the real retry exposed the precise session lookup mismatch. | GREEN |
| official/Pi session path compatibility | Real continuation selected a valid exact-worktree session from `$GSD_HOME/agent/sessions`, while official headless resume independently searched `$GSD_HOME/sessions` and reported no match. A focused bridge test failed against the missing path. | Local runner admission creates `$GSD_HOME/sessions -> agent/sessions` only inside the isolated controlled home and rejects conflicting or unexpected targets. Real Asana continuation subsequently resumed the correct session. | GREEN |
| decision provenance and PR summary | Interactive responses were visible only on the terminal and could be mistaken for direct human decisions; the official exact `depth_verification_M001_confirm` ID also failed the narrow auto-approval matcher. Tests first failed for the absent ledger/renderer and exact ID. | A fsynced protected ledger records bounded decision labels with actor (`human`, `shepherd`, or `contract`), basis, unit, execution, and time; `shepherd decisions` renders a deterministic PR-ready table. The depth matcher accepts exact or suffixed official IDs. PR publication and real-ledger qualification remain pending. | PARTIAL |
| step-mode false-green research | Real Asana `headless next` left `research-milestone/M001` canonical and its runtime record `crashed`, but returned exit 0 after reporting step-mode start. Reconciliation tests first accepted the unchanged before/after snapshot, and the runner rejected the exact official alias. | Reconciliation rejects successful `next` or `research-milestone` runs that do not advance canonical phase/dispatch state. Shepherd allowlists the exact alias only when the fenced pre-query identifies it as next; real direct dispatch advanced Asana to planning. | GREEN |
| official headless in-flight tool timer | The direct Asana research retry reached five concurrent `fetch_page` calls; official GSD 1.11.0 armed its 15-second idle timer despite those tools being active, killed the session, and exited 0 without a research artifact. | A pinned exact-shape compatibility patch tracks every tool ID and arms idle completion only after all tools end. A real 16-second fetch survived, research wrote a 21.6 KB artifact, and canonical state advanced to `plan-milestone`. Runtime admission applies the patch idempotently and rejects unknown bytes/version. | GREEN |
| exact canonical unit dispatch | Generic `next` entered the flawed step-mode wrapper even though query exposed an exact unit type; only `research-milestone` was initially allowlisted as a canary correction. | Shepherd now allowlists the official direct aliases for research/plan/execute/complete/validate units, requires each alias to equal the fenced query's next unit type, and rejects exit-zero without a canonical transition. | GREEN |
| automatic PR decision summary | `shepherd decisions` could render the durable ledger, but the controller only instructed an operator to copy it. Focused tests failed before a GitHub publisher, PR binding, and append-then-publish gate existed. | Shepherd now requires repository/PR binding, fsyncs each attributed answer, updates one delivery-marker-owned PR comment through bounded `gh api` transport, and fails the gate if publication fails while retaining the local record. `decisions --publish` reconciles interruptions; tests cover create, update, ambiguous ownership, unsafe targets, body-via-stdin, and retained records on publication failure. The real Asana ledger was synchronized idempotently into PR #388 comment `4962823285`. | GREEN |
| nested-agent lifecycle envelope bound | The healthy S01 planning subagent wrote continuously for about 20 minutes and advanced GSD to `execute-task/M001/S01/T01`, but its compact tool-return event arrived in a 1,714,076-byte JSON line. The 256 KiB scanner bound rejected the line and marked Shepherd attempt 17 failed even though canonical planning completed. A prior interrupted return measured 3,933,413 bytes. The new default-bound test failed at 262,144 bytes. | The raw lifecycle envelope ceiling is now 8 MiB, which admits both measured returns while remaining bounded. The allowlisted projector still retains only lifecycle metadata and discards raw subagent payloads. The active Asana config was updated, and nested tests, race tests, vet, and build pass. | GREEN |
| scoped implementation checkpoint | Real Asana T01 completed through `gsd_task_complete` and advanced canonical state to T02, leaving four tracked source/test files changed inside the issue contract. Shepherd attempt 18 then failed solely because its post-unit invariant required a clean tree, making successful coding units impossible unless the untrusted runtime committed internally. Focused tests initially failed because no scoped checkpoint or protected-context loader existed. | Shepherd atomically stores the validated issue context outside the worker tree, verifies its authority hash before every run, checks all `execute-task` paths against immutable `write_scope`, disables commit hooks, and creates a local checkpoint before the next clean dispatch. Tests cover allowed commit, idempotent clean state, out-of-scope rejection without staging, immutable context, and hash mismatch. The interrupted T01 diff passed focused tests and was recovered as commit `07625b09`. | GREEN |
| primary terminal-cause preservation | T02 produced its intended failing regression test but did not call `gsd_task_complete`; canonical state correctly remained T02. Reconciliation detected non-advancement, but the later dirty-tree check overwrote that cause with only `worktree must be clean`, obscuring the distinction between incomplete and completed work. The focused join test first failed because no preservation helper existed. | Terminal finalization now joins the dirty-tree error to the primary reconciliation error instead of replacing it. The T02 test was independently confirmed RED and checkpointed as `8c59cf30` before retry. | GREEN |
| exact-session runtime identity | The T02 retry reached `gsd_task_complete`, advanced canonical state to T03, and its latest session contained 39 assistant records identifying `openai-codex/gpt-5.6-sol` plus `thinking_level_change=high`. Identity fallback nevertheless failed because it walked every historical session and aborted on an older 3.9 MiB line under a 1 MiB scanner limit. The regression test first failed against the old one-argument all-session reader. | Identity fallback now selects only the newest valid session header whose `cwd` exactly matches the configured worktree, scans that session with the bounded 8 MiB ceiling, and projects only model/thinking metadata. Tests cover an older oversized same-worktree session. The interrupted T02 changes passed focused tests and were recovered as `9b0ccc7a`. | GREEN |

## Rules

- A production behavior is not added before its failing test or capability probe exists.
- Focused package tests are progress evidence; full nested-module and root gates remain required.
- Failed or incomplete verification is recorded as failed, never inferred from partial success.
- `scripts/gsd doctor` and `scripts/gsd prompt programming-loop ...` pass. The host-runtime
  migration uses the repository programming-loop contract directly.
