# TDD ledger — #3987 four-path warehouse conformance

| Slice | Red | Green | Refactor | Status |
| --- | --- | --- | --- | --- |
| Exact four-direction contracts | `go test -count=1 -timeout 20m ./internal/app -run '^TestWarehouseMediatedFourPathConformance$'` failed to build: `undefined: warehouseMediatedFlowConformanceContracts`. | `go test -count=1 -timeout 20m ./internal/app ./internal/synctransport -run 'TestWarehouseMediated|TestOrchestratorDispatchesFourClosedPairingsWithoutPairBranches'` passed. The matrix derives each generated flow ID and resolves its exact GitHub/PostgreSQL executor pair. | The map is one-to-one with `certificationcatalog.FlowKinds`; a missing, added, or role-mismatched ID now fails closed. | green |
| Warehouse mediation and current mode matrix | The initial green implementation exposed that a GitHub destination does not select dispatch from an unpersisted `Connection{}`; API destination routes must first resolve the source-bound persisted connection. | `go test -count=1 -timeout 20m ./internal/app ./internal/synctransport` passed. The orchestration proof records `destination-plan → source-read → warehouse-stage → warehouse-reopen → destination-apply → destination-readback → checkpoint`, plus the staged connection owner, sealed receipt, and reopened workset ID. | The matrix now persists the route selection fixture before calling `shouldRunTransport`, matching production dispatch rather than assuming preflight alone starts a destination. | green |
| Change-capture source-only restriction | The mode test first had no contract helper, so the same compiler RED above prevented a false source/destination interpretation. | `TestWarehouseMediatedModeConformance` passes all six currently executable transport modes, proves PostgreSQL's implemented logical-replication source declaration, and records `change_capture` as a concrete pre-I/O `refused` result because the PostgreSQL destination omits it. | Only a `pass` result contributes to the mode pass count; `change_capture` has a non-pass reason and is excluded. The existing `TestLocalWarehouseDestinationExecutorAppliesChangeCaptureTombstones` remains the durable derived-workset materialization proof. | green |
| Direction-specific failure demonstration | After changing only GitHub's API destination source binding from `issues` to `pull_requests`, `go run ./cmd/connectorgen validate internal/connectors/defs` still passed, then `go test -count=1 -v -timeout 20m ./internal/app -run '^TestWarehouseMediatedFourPathConformance$/^api_to_api$'` failed with `destination transport does not admit source executor "declarative_stream_source" for stream "issues"`. | Restored the exact `issues` binding. `go run ./cmd/connectorgen validate internal/connectors/defs && go test -count=1 -v -timeout 20m ./internal/app -run '^TestWarehouseMediatedFourPathConformance$/^api_to_api$'` passed. | N/A. | green |

The implementation records only observed red/green output. No skipped database or provider check is counted as a pass.
