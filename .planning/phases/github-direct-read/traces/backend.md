# Backend Trace

- Added direct-read request/result types and connector interface.
- Added engine direct-read execution with fixed endpoint validation and safe path/query resolution.
- Added commandrunner direct-read routing and CLI direct-read output.
- Wired GitHub `repo read-file` and `repo read-dir` to the contents endpoint.
