# SUMMARY: Reader Notes And Header Clarity

## Delivered

- Replaced flattened `@mention` replies with a recursive direct-parent thread tree.
- Added durable click/keyboard-pinned note cards with complete message text, Close, Escape, and Open Thread actions.
- Preserved lightweight hover previews while making pointer movement safe after pinning.
- Reworked the comments sheet hierarchy, labels, reply composer, thread rails, and mobile width.
- Removed the navbar's phantom symmetric grid column and verified the first search-visible desktop breakpoint.
- Added pure unit coverage and database-free Playwright coverage for the repaired interactions.

## Design Outcome

The result keeps the existing square emerald identity while shifting the annotation layer toward a restrained editorial conversation surface: messages lead, metadata supports, actions are explicit, and thread ancestry is visible at a glance.
