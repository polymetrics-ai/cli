# TDD LEDGER — issue #3628 Email IMAP/SMTP connector

| ID | Enforcement | RED evidence | GREEN evidence | Refactor/verification |
| --- | --- | --- | --- | --- |
| R1 | Bundle declares client-like constrained configuration and executable protocol command coverage | `go test ./internal/connectors/native/email -run '^TestConnectorDeclaresExecutableProtocolCommandSurface$'` failed before implementation: `undefined: New` | Same command passes with the embedded bundle and native factory. | `go test ./internal/connectors/native/email -count=1` |
| R2 | IMAP mailbox/message reads are bounded and expose the promised fields | The local-IMAP test was introduced with this slice; the pre-native absence is captured by R1. | `TestMessagesReadUsesUIDValidityCursorAndBoundedBodyParts` passes against a local `imapmemserver`, including a 1 MiB body-part bound. | `go test ./internal/connectors/native/email -count=1` |
| R3 | Incremental state uses mailbox + UIDVALIDITY + UID, never dates; hard deletes remain undetectable | The cursor/parser contract was introduced with the native reader; R1 captures the missing-executor red state. | Local fixture emits UID 2 after the UID 1 cursor; cross-mailbox state is rejected. Docs explicitly state that hard deletes produce no tombstone. | `go test ./internal/connectors/native/email -count=1` |
| R4 | SMTP sender creates actual envelope/MIME payload but does not send during preview | The preview/send fixture contract was introduced with the native writer; R1 captures the missing-executor red state. | `TestSendPreviewIsUnmaskedAndAttachmentDriftCannotDispatch` verifies envelope/body/attachment visibility, BCC header exclusion, and zero local SMTP dispatch during preview. | `go test ./internal/connectors/native/email -count=1` |
| R5 | Send is non-batchable and needs preview approval plus typed destructive confirmation | The write-action contract was introduced with the native writer; R1 captures the missing-executor red state. | `TestSendRequiresTypedApprovalBeforeSMTPDispatch` and `TestMessageSendCommandBuildsTypedUnmaskedPreview` pass against a local SMTP double. | `go test ./internal/connectors/native/email -count=1` |
| R6 | Secret-bearing configuration errors do not echo supplied values | The configuration assertion was introduced with the connector; R1 captures the missing-executor red state. | `TestCursorRejectsDifferentMailboxAndPortErrorsDoNotEchoValues` and the app credential test pass without a supplied value in an error. | `go test ./internal/connectors/native/email ./internal/app -count=1` |
| R7 | Every declared implemented Email command passes real runtime preflight | `TestConnectorDeclaresExecutableProtocolCommandSurface` was red before the bundle/factory existed (R1). | The specific Email preflight and the fleet-wide real preflight sweep both pass. | `go test ./internal/connectors/commandrunner -run '^TestEveryImplementedCommandPassesRuntimePreflight$' -count=1` |
| R8 | Saved JSON command plans retain typed recipient arrays | `go test ./internal/connectors/native/email -run '^TestSendMessageAcceptsPersistedJSONArrays$' -count=1` failed: `email send_message to must be an array of strings`. | The native decoder accepts JSON-decoded string arrays while rejecting non-string members. | `go test ./internal/connectors/native/email -run '^TestSendMessageAcceptsPersistedJSONArrays$' -count=1` |
| R9 | Mailbox list honors the stream record limit | `go test ./internal/connectors/native/email -run '^TestMailboxListHonorsRequestedLimit$' -count=1` failed: `mailboxes Read emitted 2 records with limit=1, want 1`. | The streaming IMAP LIST loop emits at most the requested/default bounded limit and drains the protocol command safely. | `go test ./internal/connectors/native/email -run '^TestMailboxListHonorsRequestedLimit$' -count=1` |

## Red-test rule

Each behavior-changing slice must first run a named focused test against the pre-fix state and
record its expected failure here. Tests remain as the permanent contract; no test is weakened,
skipped, or deleted to claim completion.
