# TDD ledger — Twenty CRM

| Slice | Red | Green | Refactor / evidence |
| --- | --- | --- | --- |
| Recovery command contract | Before import, `go test -timeout 20m ./internal/connectors/defs/twenty` failed as expected: `load bundle twenty: missing required file metadata.json`. After importing the historical bundle, the same test failed as expected on 28 `get` commands (`availability=planned`, no direct-read operation) and 28 batch writes (`availability=partial`). | `go test -timeout 20m ./internal/connectors/defs/twenty` now passes: all 168 commands preflight as executable and retain the expected 28 ETL / 28 direct-read / 112 reverse-ETL split. | `connectorgen validate` and conformance already passed on the stale bundle, proving those checks alone do not establish runtime reachability. No provider call or secret involved. |
| Read and pagination | Focused preflight test exposed stale planned direct reads, absent operation binding, and 18 stale hyphenated paths where Twenty publishes camel-cased REST object paths. | `TestOperationReadAndStructuredWriteSafetyContracts` proves an exact provider route succeeds and the stale hyphenated route is rejected; `go run ./cmd/connectorgen surface-sync` regenerated the operation endpoint ledger. | The test is no-network and preserves the real instance proof for the next slice. |
| Write/delete safety | Recovered batch commands were `partial` because no safe structured input existed; delete actions used a legacy `confirm` field. | All 28 batches now use their closed-schema `records` JSON array with `maxItems: 60`; all 28 deletes use `{ "kind": "destructive" }`. The focused test accepts only `records`, rejects a generic `raw_body`, and asserts the 60-item bound. | `go test -timeout 20m ./internal/connectors/conformance -run 'TestConformance/twenty'` passes without credentials. |
| Live round trip | A pinned disposable Twenty image reached `/healthz`; GraphQL introspection was explicitly disabled and `workspace:seed:dev --light` supplied a real seeded workspace. The first Keychain experiment was red: its interactive `-w` prompt truncated a 300-byte non-secret control to 128 bytes, so a 128-byte fragment was correctly rejected with provider 401. | Firstmate's direct-stream decision produced green evidence: the image's API-key emitter piped directly to `pm credentials add --connector twenty --value-stdin api_key`; the full token measured 444 bytes at the pipe boundary. The built binary then authenticated `companies list`, paged companies to 600 records, swept bounded list/get across all 28 declared object groups, created one company, read it back, updated it, read the changed field back, deleted it through preview + typed confirmation, and observed list absence plus get 404. Each live mutation run reported `records_staged=1`, `records_succeeded=1`, and `records_failed=0`. | Firstmate, not the captain, selected the headless seed/key route. The captain's standing requirement is the real-instance, real-data gate. No API key, approval token, password, connection string, or record identifier is recorded. Keychain is intentionally not used in the completed proof. |
| CLI catalog and golden projections | After Twenty entered the connector registry, the full `go test -timeout 20m ./internal/cli` run failed as expected: catalog JSON still asserted count 556, and the root help variants lacked the Twenty command line. | `POLYMETRICS_UPDATE_GOLDEN_TRANSCRIPTS=1 POLYMETRICS_GOLDEN_TRANSCRIPT_NAMES='root_bare_manual,root_long_help,root_short_help,root_help_command,root_man_command,root_json_help,root_late_json_help,root_equals_form,root_space_form' go test -timeout 20m ./internal/cli -run '^TestGoldenTranscripts$' -count=1` passed and regenerated exactly the nine root-help inputs; the catalog assertion now expects 557. A clean `go test -timeout 20m ./internal/cli -run '^TestGoldenTranscripts$' -count=1` then passed in 17.729s, and the final full package run passed in 1184.608s. | The generated diff contains only the expected Twenty help line in each root transcript; all non-root transcript inputs remain byte-identical. |
| Bundle registry projection | CI `Verify` run 32274541836 provided the red observation after the recovered connector was added: `TestNewLoadsDeclarativeBundlesWithHooksAndNativeOverrides` loaded 553 bundles while its stale assertion expected 552. | `go test -timeout 20m ./internal/connectors/bundleregistry` passes with the derived registry expectation updated to 553. | This is the bundle-level counterpart to the CLI catalog baseline (557 command registrations); it does not alter connector runtime behavior. |
| Typed destination declaration | After the #4304 foundation merge, `go test -count=1 -timeout 20m ./internal/connectors/defs/twenty -run '^TestTypedDestinationDeclaration$'` failed with `SyncTransport = (*connectors.SyncTransportDescriptor)(nil)`, proving Twenty had not declared a source or connector-owned destination route. | `sync_transport.json` declares every executable source stream plus the closed `create_companies` full-append destination. The green focused test asserts the exact executor, all 28 streams, keyed/no-tombstone delivery, durable acknowledgement, strategy, and `name` → `name` mapping. | `go test -count=1 -timeout 20m ./internal/connectors/defs/twenty`, `go run ./cmd/connectorgen validate internal/connectors/defs/twenty`, `go run ./cmd/connectorgen surface-sync --check`, and `go test -count=1 -timeout 20m ./internal/app -run '^TestDefinitionTransportFactories'` pass. The adapter intentionally excludes tombstones; it does not remove any of the 28 typed-delete CLI commands. |
| Typed-write eligibility | `go test -count=1 -timeout 20m ./internal/connectors/defs/twenty -run '^TestEveryTypedWriteHasEligibilityDisposition$'` failed with `read write eligibility ledger: open write_eligibility.json: no such file or directory`, proving the initial one-action destination did not account for the remaining typed actions. | The same command passes with `write_eligibility.json`: 1 currently bound action, 55 record-shaped actions eligible pending exact-action multiplicity in #4304, 28 exact array-envelope semantic incompatibilities, and 28 exact tombstone-workset semantic incompatibilities. | Every entry is independently checked against `writes.json` and an implemented `reverse_etl` CLI command. Safety, privilege, and destructive classifications are refused as eligibility reasons; they continue to govern execution, not reachability. |
| Persisted per-action destinations (pending refreshed #4304) | Before changing the Twenty declaration, add a focused test that fails on the current foundation: all 56 record-shaped actions must have one persisted action selection with the exact provider-sourced `input_fields`; missing action, mismatched field, and duplicate action cases must fail. | After normally merging the updated #4304 head, make the focused test green with connector-owned declarations for `create_companies` plus all 55 required eligible actions. The test must also prove the installed App path selects the requested action rather than defaulting by sync mode. | This is intentionally deferred, not waived: current `c6f03c937` can select only one action per mode/source mapping. The captain authorized the required all-55 completion through the forthcoming #4304 persisted multi-action foundation; no connector workaround is allowed. |
| Post-foundation live App/CLI certification (pending refreshed #4304) | A fresh attempt starts from no credential in the test project and fails closed without `--credential`; an invalid/missing input binding must fail before provider mutation. | The newly built binary and persisted App path authenticate only to the dedicated disposable Twenty instance, then pass list/get with pagination; lane-owned create/read-back/update/delete/verified cleanup; ETL source; reverse-ETL plan/apply/acknowledgement plus independent API read-back; and a binary round-trip only if the declared provider surface contains one. | The secret is sourced from the registered `pm-twenty` Keychain/secret-store reference only at stdin injection. Historical direct-stream proof is retained for provenance but is not acceptance evidence for this slice. |
| Live bearer authorization audit | The actual built `pm twenty companies list` against the isolated loopback Twenty endpoint returned HTTP 403 with an encrypted credential; this is the red live gate. | Before any live retry, a local in-process receiver must show the App-resolved vault secret and the engine-emitted bearer value have equal length and SHA-256, an API-key-shaped non-expired token claim set, and exactly the `Bearer` scheme. A connector-owned discrepancy requires a focused failing test before its fix; a matching header instead directs repair to the provider API-key role/configuration path without regenerating the key. | The diagnostic prints only booleans, lengths, hashes, and HTTP/status classes. It does not contact production, persist the credential, print a token/header/identifier/payload, or alter the isolated stack. |
| Empty stdin credential ingestion | The read-only vault-to-engine audit selected one Twenty credential with one declared `api_key` field, but its decrypted value was length zero; the engine sent a bearer scheme without a token. This proves the built CLI's prior `credentials add --value-stdin` accepted empty stdin and explains the HTTP 403. | Blocked outside this connector lane: a provider-neutral CLI change must reject empty input after line-ending normalization and before vault persistence. Its required tests are happy non-empty stdin, bad empty stdin, and edge newline-only stdin. Only after that foundation repair and approved issuance of a new disposable key may the HTTP-200 gate retry. | Existing `runCredentials` reads stdin and persists it without an emptiness check. The Twenty bundle has the correct `bearer` / `api_key` contract; source-backed Twenty role, expiry, workspace, and self-host checks are non-blocking. No generic code, token replacement, or live retry occurred here. |
| Dynamic workspace-schema six-surface certification (pending foundations) | Red: a static declaration, fixture, or provider documentation audit without an authenticated inventory of the disposable workspace must fail the gate. Missing source/schema trace, absent CLI/help/website route, disabled destructive/tiered operation, inferred binary route, or a CRUD write lacking independent provider read-back/cleanup are separate red cases. | Green: after the corrected non-empty credential path and final #4304/#4305 heads are present, record the dynamic provider inventory and reconcile every discovered operation to its provider source/schema trace, definition, executable CLI command, generated help/manual, and website documentation across ETL, reverse ETL, direct read, direct write, binary download, and binary upload. Prove every applicable supported CRUD/application operation on lane-owned records with independent provider read-back and cleanup. | Happy coverage is each applicable provider capability completing through the installed binary and persisted App path; bad coverage is an invalid/unauthorized/confirmation-required operation failing closed without mutation; edge coverage is an empty workspace/object or non-transfer attachment metadata retaining its exact source fact without inventing a binary route. Tier/scope/destructive/safety metadata may warn or require authorization, never disable a discovered operation. |
