---
phase: 600
plan: 02
type: tdd
wave: 2
depends_on:
  - 600-01
files_modified:
  - cmd/connectorgen/certificationmatrix.go
  - cmd/connectorgen/certificationflow.go
  - cmd/connectorgen/certificationmatrix_test.go
  - internal/connectors/certifications/flow-matrix.json
  - internal/connectors/certifications/status.json
  - Makefile
  - docs/architecture/connector-certification-design.md
autonomous: true
requirements:
  - FLOW-01 explicit API/database endpoint roles
  - FLOW-02 source/destination pair-scoped flow cells
  - FLOW-03 independent-readback evidence
  - FLOW-04 final all-or-nothing connector certification
  - WORKFLOW-01 ETL, reverse ETL, flow authoring, and schedule completeness
  - SYNC-01 stable warehouse-facing primitive and sync-mode scoreboard
must_haves:
  truths:
    - "D-07: flow applicability is explicit for all API/database roles and a flow identity includes source and destination."
    - "D-08: a pair is live-tested only after an end-to-end pipeline and independent destination readback, never by endpoint inference."
    - "D-10: the flow artifact is generated and committed separately with a separate baseline."
    - "A connector is finally certified only when every applicable function, workflow, sync-mode, and pair-flow cell is complete."
    - "Each sync mode is evaluated against database-read-into-warehouse, database-write-from-warehouse, api-read-into-warehouse, and api-write-from-warehouse; all inapplicable cells retain a named reason."
  artifacts:
    - path: "internal/connectors/certifications/flow-matrix.json"
      provides: "compact pair-scoped flow, workflow, warehouse-facing sync-mode, and final connector-certification baseline"
    - path: "cmd/connectorgen/certificationmatrix.go"
      provides: "role/workflow/sync generation, pair-set resolver, strict flow evidence, and final aggregation"
    - path: "cmd/connectorgen/certificationmatrix_test.go"
      provides: "executable proof for durable admission, pair identity, sync applicability, readback, and aggregate certification"
  key_links:
    - from: "cmd/connectorgen/certificationmatrix.go"
      to: "internal/synccontract/commit.go"
      via: "DurableETLDestination admission defines generic destination implementation"
    - from: "cmd/connectorgen/certificationmatrix.go"
      to: "internal/app/app.go"
      via: "local warehouse materialization is a distinct executable destination path"
    - from: "internal/connectors/certifications/flow-matrix.json"
      to: "internal/connectors/certifications/capability-matrix.json"
      via: "final connector certification combines capability, workflow, sync-mode, and pair-flow completeness"
    - from: "cmd/connectorgen/certificationflow.go"
      to: "internal/synccontract/mode.go"
      via: "the source of truth for every scored sync mode"
---

# Plan 600-02: Add workflow, sync-mode, and pair-scoped flow certification

## Objective

Extend the generated baseline with workflow cells, a stable warehouse-facing
sync-mode scoreboard, and compact source/destination pair cells. Certification
cannot treat individually capable endpoints or individually executable commands
as compositional proof.

## Captain correction — workflow and sync-mode scope

Certification covers the whole workflow set: ETL, reverse ETL, flow authoring,
and schedule. A connector whose commands resolve but cannot be scheduled or
expressed as a flow remains a community build.

Every source connector has every mode from internal/synccontract/mode.go crossed
with exactly four stable warehouse-facing primitives:
database_read_into_warehouse, database_write_from_warehouse,
api_read_into_warehouse, and api_write_from_warehouse. change_capture is
applicable only to database_read_into_warehouse; every other inapplicable cell
carries a machine-checkable reason. No generic write evidence promotes a mode:
database write is red until a durable runtime path exists.

## Captain clarification — mandatory warehouse-mediated round trips

Every generated flow is a three-stage pipeline, never a direct connector hop:
`source connector -> local Parquet warehouse -> destination connector`. This
applies to all four pair classes, including database source/destination pairs.
The warehouse is a required, explicit mediator in both the generated identity
and the evidence contract. A source and destination may be the same connector:
GitHub-to-GitHub is a legitimate pair and must not be filtered from a product.

`live_tested=true` for a flow means one real `pm` binary ran the complete round
trip and independently read the delivered data back from the destination. It
cannot be inferred from successful individual ETL/reverse-ETL legs. Accepted
flow evidence must cite the complete run, an independent warehouse readback,
and an independent destination readback.

Delivery guarantees are separate from whether that round trip worked. A passed
flow cell carries an explicit delivery contract for `resumable`,
`receipt_backed`, `checkpointed`, `replay_identity`, and
`provider_idempotency_key`; each false guarantee has a named limitation. This
allows a one-shot GitHub reverse leg to be honestly recorded as working while
remaining explicitly non-resumable and receipt-unbacked, rather than advertising
guarantees the implementation does not provide.

<threat_model>
## Threat model

| Threat | Boundary | Mitigation | Verification |
|---|---|---|---|
| Endpoint evidence is reused as flow proof | capability records to pair certification | Flow evidence keys both source and destination and requires destination readback | pair evidence validation test |
| Successful ETL/reverse legs are mislabeled as an end-to-end flow | two-leg pipeline to certification | Require a single real-`pm` round-trip record plus warehouse and destination independent readbacks | round-trip evidence validation test |
| A same-connector round trip is silently omitted | pair construction | Preserve source==destination products, including GitHub-to-GitHub | pair resolver test |
| A working one-shot reverse leg is advertised as resumable | evidence contract to report | Serialize each delivery guarantee and named false-guarantee limitation independently of `live_tested` | delivery-contract validation test |
| API sink claims are accepted despite no durable acknowledgement | app flow admission to matrix | Derive implementation from `DurableETLDestination` / warehouse materializer runtime contracts | API destination red-baseline test |
| Non-applicability is hidden by omitted pair data | role facts to matrix consumer | Generate all four endpoint roles for each connector with named reasons; serialize candidate pair products plus exact overrides | role reason and pair resolver tests |
| Output explodes into an unreviewable cartesian product | hundreds of connectors to committed JSON | Encode disjoint source/destination sets with a default pair cell and pair-key overrides; report exact pair counts | deterministic compact rendering test |
| A connector's commands work but scheduling or flow authoring does not | command surface to certification | Score each required workflow independently and require all applicable workflows | workflow completeness test |
| A sync mode is claimed in the abstract | mode name to executable primitive | Cross every mode with the four warehouse-facing primitives and retain named N/A reasons | primitive and CDC applicability tests |
| A connector becomes certified while an applicable flow is false | capability and flow aggregation | Require capability completeness and every involved applicable pair cell before final `certified=true` | aggregation test |
</threat_model>

<tasks>

<task type="tdd">
  <name>Task 1: Establish red pair-flow contracts</name>
  <files>cmd/connectorgen/certificationmatrix_test.go</files>
  <read_first>
    600-RESEARCH.md
    internal/app/app.go
    internal/synccontract/commit.go
    internal/connectors/connectors.go
  </read_first>
  <action>
    Add RED tests for generated ETL, reverse-ETL, flow-authoring, and schedule
    workflow cells; every sync mode with the four stable warehouse-facing
    primitives; generated API/database source/destination roles; named
    integration-type mismatch reasons, a pair identity that includes source,
    mandatory local warehouse mediator, and destination (including a same
    source/destination pair), API destination rejection without durable
    acknowledgement, a real-pm round-trip plus both independent readbacks as
    mandatory passed-flow evidence, explicit non-resumable/non-receipt-backed
    delivery contract validation, compact pair-set resolution, and
    flow-incomplete connectors remaining uncertified.
    Run the focused test command and record its RED output in `TDD-LEDGER.md`.
  </action>
  <acceptance_criteria>
    Tests prove workflow and sync applicability plus D-07 and D-08 without
    contacting a provider or inferring pair proof from individual cells or
    successful individual pipeline legs.
  </acceptance_criteria>
  <verify>go test -timeout 20m ./cmd/connectorgen -run 'Test.*Flow'</verify>
  <done>Focused tests fail for absent role, pair, durable-admission, and readback behaviour; the RED output is recorded in TDD-LEDGER.md.</done>
</task>

<task type="tdd">
  <name>Task 2: Implement workflows, sync cells, roles, pairs, and final aggregation</name>
  <files>
    cmd/connectorgen/certificationmatrix.go
    cmd/connectorgen/certificationmatrix_test.go
  </files>
  <read_first>
    600-01-PLAN.md
    cmd/connectorgen/certificationmatrix.go
    internal/app/app.go
    internal/synccontract/commit.go
  </read_first>
  <action>
    Discover workflow annotations from the actual CLI entry points and modes
    from synccontract.AllModes(). Generate the exact four stable
    warehouse-facing sync primitives and their named N/A reasons, then
    generate four explicit endpoint roles for each connector and form
    API-to-API, API-to-database, database-to-API, and database-to-database
    pair cells from applicable role products. Every cell fixes the local
    warehouse as an explicit mediator and retains products where source equals
    destination. Group identical defaults into non-overlapping
    source/destination sets, preserve an exact three-stage pair key for
    overrides, require strict real-pm round-trip evidence with independent
    warehouse and destination readbacks, serialize delivery guarantees and
    named limitations, and calculate final connector certification from
    capability, workflow, sync-mode, and flow cells.
  </action>
  <acceptance_criteria>
    API-only connectors state why database-destination does not apply. Every
    unsupported sync primitive or mode is explicit; CDC only applies to
    database read into the warehouse. API
    destinations lacking durability are not implemented flows. No flow has
    live-tested=true without a real-pm round-trip plus warehouse and
    pair-specific destination readback pointers. No connector is finally
    certified at the baseline, and a passed one-shot flow can state exactly
    which replay/delivery guarantees remain false.
  </acceptance_criteria>
  <verify>go test -timeout 20m ./cmd/connectorgen -run 'Test.*Flow'</verify>
  <done>Pair-scoped flow tests are green and no test invokes a real provider.</done>
</task>

<task type="tdd">
  <name>Task 3: Commit flow artifact, separate baseline, and final gate</name>
  <files>
    internal/connectors/certifications/flow-matrix.json
    Makefile
    docs/architecture/connector-certification-design.md
  </files>
  <read_first>
    cmd/connectorgen/certificationmatrix.go
    internal/connectors/certifications/capability-matrix.json
    docs/architecture/connector-certification-design.md
  </read_first>
  <action>
    Regenerate and commit the pair-flow artifact in its own coherent commit.
    Make the certification generator check cover both artifacts, document the
    compact pair representation, workflow/sync scoreboard, and pair-evidence
    rule, and record distinct capability and flow baseline totals in
    SUMMARY.md.
  </action>
  <acceptance_criteria>
    `connectorgen certification-matrix --check` validates both artifacts from
    a clean checkout. Baseline reports connector and capability totals apart
    from workflow, sync-mode, and flow-kind pair totals and shows zero final certifications unless
    accepted evidence proves otherwise.
  </acceptance_criteria>
  <verify>go run ./cmd/connectorgen certification-matrix --check &amp;&amp; make connectorgen-certification-matrix</verify>
  <done>The flow artifact is committed separately and generated summaries state both baselines.</done>
</task>

</tasks>

## TDD evidence

- **Red:** Task 1 workflow/sync/pair/evidence tests fail before their generator
  contracts exist.
- **Green:** the matrix resolves workflow, sync, and exact pair identities,
  requires readback, and makes focused tests plus the joint drift check pass.
- **Refactor:** validate disjoint set products and deterministic group ordering
  after the first green run.

## Definition of done

The flow artifact is committed separately from the capability layer where
delivery sequencing permits, final certification is workflow-, sync-, and
flow-aware and all-or-nothing, and separate baseline totals are generated from
source.
