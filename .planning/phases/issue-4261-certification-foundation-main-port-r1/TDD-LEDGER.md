# TDD ledger — issue 4261

| Slice | Red evidence | Green evidence | Refactor / scope evidence |
| --- | --- | --- | --- |
| Certification projection | **Red (2026-08-19):** `go test -timeout 20m ./cmd/connectorgen -run 'TestCertification'` failed to build because `publishPreparedAcceptedEvidence`, `preparedAcceptedEvidence`, `DeclaredRows`, and the parity classifier are absent on main. | **Green (2026-08-19):** after porting the foundation and regenerating, `go test -timeout 20m ./cmd/connectorgen -run 'TestCertification'` passed (`ok`, 102.189s). | Confirm only foundation paths differ from `origin/main`; no `sync_modes.go`. |
| Generated sweep | The old main artifact did not contain the new projection fields exercised by the red test. | **Green (2026-08-19):** `go run ./cmd/connectorgen certification-sweep . --connector github` wrote 1,575 rows / 1,571 commands; its SHA-1 is identical to `ac2944115` after regeneration from this branch. | Retain the main-derived result; equality confirms no hidden definition drift, not a copied artifact. |
| Safe live evidence | The main-base runner lacks the ported safe draft/import/scoped-check ordering. | **Green (2026-08-19):** the authorized Keychain-backed read-only run executed one candidate and reported `executed=1 certified=1 no_object=0 wrong_credential=0 entitlement=0 product_defect=0`; it published `github-direct_read_sweep_repo_read_file-github-1787112681540-1fc3dacd63e6.json`. The scoped matrix check accepted the record. | The committed record contains only schema-v2 salted fingerprints and a 200 response; credential values are not inspected or recorded. |
| Delivery guard | **Red (2026-08-19):** the first guarded push failed in `internal/connectors/boundary`: inherited hook `GIT_DIR` made nested temporary-repository `git add .` report “must be run in a work tree.” | **Green (2026-08-19):** with the hook clearing the inherited Git variables, the same hook was exercised under that environment and its `make verify` run passed: full tests (including `internal/connectors/boundary`), build, docs, smoke, lint, generator, certification, boundary, canon, and release-policy gates. | Run final merge-base assertions and the actual guarded branch push. |
