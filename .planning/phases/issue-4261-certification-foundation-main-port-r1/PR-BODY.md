## Intent

Port the certification foundation from `ac2944115` to the current `main` base
without merging the squash-drifted integration branch.

Refs #4261 — feat(certification): port foundation parity projection to main

Base: `main`.

## What Changed

- Added the source-derived parity classifier and 1,575-row GitHub sweep
  projection, including closed operation-kind/class identities and delete
  action classification.
- Made accepted evidence publication staged, validated, durable, and
  no-replace; the live runner now imports a draft then regenerates and checks
  only its connector shard.
- Regenerated `github/certification-sweep.json` from this branch and added one
  read-only GitHub schema-v2 proof record from the authorized live run.
- Added architecture and AGENTS guidance plus a worktree-local pre-push
  `make verify` guard that clears hook-local Git variables before nested
  temporary-repository tests run.

## Deliberate Exclusions

- No integration-branch merge; no `internal/app/sync_modes.go` change.
- No `CHANGELOG.md`, `.release-please-manifest.json`, or historical planning
  trace change.
- No global certification `--all` refresh.

## Red / Green / Refactor Evidence

- **Red:** after importing the focused upstream test deltas, `go test -timeout
  20m ./cmd/connectorgen -run 'TestCertification'` failed because the main
  base lacked the projection and prepared-evidence symbols.
- **Green:** after porting the foundation and regenerating the sweep, the same
  command passed in 102.189s; full changed-package tests also passed.
- **Refactor:** generated bytes were compared to `ac2944115` only after
  regeneration from this branch; they are identical because the underlying
  main definitions have no relevant drift.

## Live GitHub Proof

One declared, read-only `repo read-file` candidate ran through the project
runner with the authorized Keychain credential passed only by environment
variable reference. It reported one execution and one certification, with no
non-pass outcome, published a redacted schema-v2 record, regenerated GitHub's
scoped matrix shard, and passed its scoped check. No credential value appears
in committed files, logs, this PR body, or command arguments.

## GSD / Skills / Parity

- Lifecycle: `scripts/gsd doctor`; sources and generated prompts for
  `discuss-phase`, `plan-phase --tdd`, `execute-phase`, `verify-work`, and
  `code-review`; all executed inline under the canonical no-delegation
  fallback.
- Skills: `golang-how-to`, `golang-cli`, `golang-testing`,
  `golang-error-handling`, `golang-security`, `golang-safety`,
  `golang-documentation`, `golang-design-patterns`,
  `golang-structs-interfaces`, and `golang-lint`.
- CLI/docs parity: no `pm` user command, namespace, flag, generated manual, or
  website page changes. The applicable architecture documentation and
  generator help/tests are updated and verified.

## Verification

- `go test -count=1 -timeout 20m ./cmd/connectorgen` — PASS (90.215s)
- `go test -count=1 -timeout 20m ./internal/cli` — PASS (527.280s)
- `go run ./cmd/connectorgen certification-sweep --connector github --check`
  — PASS (1,575 rows; 1,571 commands)
- `go run ./cmd/connectorgen certification-candidates --connector github --check`
  — PASS
- `go run ./cmd/connectorgen certification-matrix --connector github --check`
  and global `--check` — PASS
- `go run ./cmd/connectorgen surface-sync --check`, `validate`,
  `go run ./cmd/agentcontractgen check`, and `go vet ./...` — PASS
- `make verify` — PASS (format, tidy, full tests, build, docs, smoke, lint,
  validation, generated checks, boundary, canon, and release-policy gates)
- `git diff --check` — PASS

## Automated Review

Route: `claude_auto` on this trusted-owner, non-draft PR. Copilot is fallback
only if Claude is unavailable; no review finding exists at PR creation.
