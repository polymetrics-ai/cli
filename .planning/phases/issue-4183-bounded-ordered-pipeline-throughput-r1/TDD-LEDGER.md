# TDD ledger — bounded ordered pipeline throughput R1

| ID | Class | Required observable assertion | Red | Green |
| --- | --- | --- | --- | --- |
| R1 | Bad/fake | An endpoint missing ordered-pipeline declaration returns the typed error before source/destination method counters change. | `RunRequest` and descriptor capability fields did not exist; the initial test compile failed. | `TestOrchestratorArrowFullOverwriteRefusesUndeclaredPipelineBeforeIO` and app pre-dispatch test pass; counters remain zero. |
| R2 | Edge/fake | Depth `1` does not permit source page N+1 to start before COPY of N finishes and retains one final publication/checkpoint. | The existing synchronous controller is the baseline serial behavior. | `TestOrchestratorArrowFullOverwriteDepthOneKeepsSerialEmitBehavior` holds COPY 1, observes no page 2 extraction, then verifies one publish/read-back and final checkpoint. Depth one remains the unchanged controller branch. |
| R3 | Happy/fake | With depth `2`, blocked COPY page N observes source extraction of N+1 before COPY release; consumer applies immutable receipts in source order. | The initial capability/controller symbols were absent. | `TestOrchestratorArrowFullOverwriteOverlapsNextExtractionWithPreviousCopy` proves page 2 overlaps COPY 1, page 3 cannot begin before capacity advances, output `[1,2,3]` ordering, and a 64-byte peak for two 32-byte credit leases. |
| R4 | Edge/fake | Queue admission never exceeds the configured count or byte credit; cancellation/failure leaves no retained Arrow record or goroutine. | A serial callback cannot retain queued work. | Depth-two credit assertion plus `go test -race -count=1 -timeout 20m ./internal/synctransport` pass. Cancellation-after-page-two test returns `context.Canceled`, drains, and aborts. |
| R5 | Bad/fake | A failure while a later batch is queued aborts the run and performs neither publish/read-back nor checkpoint. | No pipeline failure seam existed before the controller. | `TestOrchestratorArrowFullOverwritePipelineFailureAbortsBeforePublicationOrCheckpoint` injects COPY 2 failure and proves `publish/read-back/checkpoint = 0/0/0`, `abort = 1`. |
| R6 | Happy/live | The two-page PostgreSQL full-overwrite binary path independently reads every target row, exactly one receipt, and a final checkpoint after read-back. | Existing #4183 regression retained. | Tagged `TestPMBinaryPostgresFullOverwriteRetainsEverySourcePage` and `TestPMBinaryPostgresTransformedFullOverwriteUsesArrowCOPY` pass after the pipeline default is admitted. |
| R7 | CLI bad/edge | Invalid `--max-in-flight-batches` and `--target-copy-workers` fail before persistence/I/O; defaults and upper bounds are observable in stored connection and plan/preview. | `TestChangedCLICommandHelpIsExecutable` first failed: `pm etl run --help` attempted I/O and `pm connections create --help` parsed an empty endpoint. Parsers and policy were absent before the slice. | Parser bounds, declaration-derived default/range/typed refusal, plan+preview output, and executable subcommand help tests pass. |
| R8 | Performance/live | The immutable baseline remains adjacent to a new quiet-host 5 GB report with identical logical-byte definition and a truthful gate result. | Baseline remains exactly 111.85 MB/s / 48.000221500 s, unchanged. | **PENDING by captain decision.** The benchmark is not a merge gate. A post-change report requires a quiet host, recorded pre-run load, the existing explicit database/performance opt-in, and the same logical-byte definition; no speedup is claimed. |
| R9 | CLI regression/gap | The two changed leaf-help commands exit zero and print their documented flags from a directory with no `.polymetrics`; help performs no project, connection, or credential resolution. | `go test -count=1 -timeout 20m ./internal/cli -run '^TestChangedCLICommandHelpIsExecutable$'` failed after `t.Chdir(t.TempDir())`: both paths returned `open project at .polymetrics`. | The Cobra legacy router recognizes valid `etl run` and `connections create` help before `withApp`; the strengthened test passes with exit zero, expected flag text, and empty stderr. |

No test is skipped, weakened, or reclassified as stale to satisfy this plan.
