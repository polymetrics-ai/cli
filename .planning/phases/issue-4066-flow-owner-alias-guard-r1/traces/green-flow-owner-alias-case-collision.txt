# #4066 ASCII-equivalent collision GREEN evidence

Command:

`go test -race -timeout 20m ./internal/cli -run '^(TestFlowOmittedConnectionRejectsGeneratedOwnerAliases|TestFlowGeneratedOwnerAliasCollision|TestFlowGeneratedOwnerAliasCaseVariantCollision|TestFlowSourceConnectionSelectorsReadOnlyOwningRows|TestFlowSourceConnectionSelectorRefusesOmissionAndAcceptsUnattributed|TestFlowActionSourceReadsAllSelectedConnectionRows)$' -count=1`

Result: PASS.

Coverage:

- omitted flows reject quoted and unquoted lowercase and uppercase forms that
  are ASCII-equivalent to a generated owner alias as a typed ambiguity for
  `records` with the existing flow connection remedy;
- generic queries return the legitimate uppercase collision table for every
  spelling, while the existing non-colliding alias still returns its selected
  owner;
- existing explicit selection, bare ambiguity, `_unattributed`, `SELECT 1`,
  and action-source coverage remains green under the race detector.
