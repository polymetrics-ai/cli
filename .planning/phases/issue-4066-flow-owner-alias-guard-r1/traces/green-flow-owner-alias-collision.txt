# #4066 collision GREEN evidence

Command:

`go test -race -timeout 20m ./internal/cli -run '^(TestFlowOmittedConnectionRejectsGeneratedOwnerAliases|TestFlowGeneratedOwnerAliasCollision|TestFlowSourceConnectionSelectorsReadOnlyOwningRows|TestFlowSourceConnectionSelectorRefusesOmissionAndAcceptsUnattributed|TestFlowActionSourceReadsAllSelectedConnectionRows)$' -count=1`

Result: PASS.

Coverage:

- omitted flows reject quoted and unquoted identifiers that collide with a
  generated owner alias as a typed ambiguity for `records` with the existing
  flow connection remedy;
- generic quoted and unquoted queries return the legitimate real collision
  table, while the existing non-colliding alias still returns its selected
  owner;
- existing explicit selection, bare ambiguity, `_unattributed`, `SELECT 1`,
  and action-source coverage remains green under the race detector.
