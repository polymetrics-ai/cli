# #4066 collision RED evidence

Production head: `bda85b778f89f4320760b8d83826ac9d393b0220`

Command:

`go test -timeout 20m ./internal/cli -run '^TestFlowGeneratedOwnerAliasCollision$' -count=1`

Result: expected RED, exit 1.

- Omitted-flow unquoted and quoted `records__<connection-id>` queries both
  returned a successful real-table result where a typed `records` ambiguity was
  required.
- Generic unquoted and quoted controls both failed with DuckDB's duplicate-view
  error because the generated alias and real catalog table used the same name.
