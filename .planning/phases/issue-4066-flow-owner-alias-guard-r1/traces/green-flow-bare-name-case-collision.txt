# #4066 case-equivalent bare-name GREEN evidence

Command:

`go test -race -timeout 20m ./internal/cli -run '^(TestFlowOmittedConnectionRejectsGeneratedOwnerAliases|TestFlowGeneratedOwnerAliasCollision|TestFlowGeneratedOwnerAliasCaseVariantCollision|TestFlowCaseVariantBareTableAmbiguity|TestFlowSourceConnectionSelectorsReadOnlyOwningRows|TestFlowSourceConnectionSelectorRefusesOmissionAndAcceptsUnattributed|TestFlowActionSourceReadsAllSelectedConnectionRows)$' -count=1`

Result: PASS.

Coverage:

- omitted flows reject quoted and unquoted lowercase and uppercase bare-name
  variants as a typed ambiguity for `records` with the existing flow connection
  remedy;
- generic queries retain access to the legitimate uppercase `RECORDS` table;
- non-colliding aliases, explicit selection, bare ambiguity, `_unattributed`,
  `SELECT 1`, and action-source coverage remain green under the race detector.
