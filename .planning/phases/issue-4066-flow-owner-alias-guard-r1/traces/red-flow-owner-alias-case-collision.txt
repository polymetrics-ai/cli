# #4066 ASCII-equivalent collision RED evidence

Production head: `08acb08a8521ae7485152092810d4318ced29086`

Command:

`go test -timeout 20m ./internal/cli -run '^TestFlowGeneratedOwnerAliasCaseVariantCollision$' -count=1`

Result: expected RED, exit 1.

- All quoted and unquoted lowercase and uppercase forms returned successful
  unscoped-flow reads from the real uppercase collision table.
- The corresponding generic controls failed with DuckDB's duplicate-view error
  because the exact-string snapshot policy did not recognize the collision.
