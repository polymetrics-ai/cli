# Capability reconciliation — captain correction 021

The captain supersedes the historical merge-base partition. Source mapping is
capability-based: a provider operation is implemented in the narrow executor
that the shared runtime can prove, not held planned to preserve a count.

| Source lane | Count | Reconciliation |
| --- | ---: | --- |
| GET reads | 119 | 12 existing record-and-pagination-backed streams remain ETL. 106 remaining JSON GET operations have the shared fixed, bounded direct-read executor and must materialize with their locked identity/method/path and typed scalar inputs. `asana.rest.getMembership` remains deferred on `cli-openapi30-reference-sibling-foundation-r1`, its actual typed-contract gap. |
| POST/PUT/DELETE mutations | 130 | 73 already use the declared reverse-ETL plan/preview/approval executor. 69 are implemented partial contracts with their existing source-cited missing-foundation dispositions (65 `cli-request-schema-foundation-r1`, 4 `source-path-parameter-alias-foundation-r1`); 21 have no action and retain `source-cited-non-executable-mutation-foundation-r1`. |
| Generic batch wrapper | 1 | `/batch` remains `unsupported_api`: it is a generic subrequest escape hatch, not a missing executor. |
| Binary/download/upload | 0 source-backed Asana commands | No Asana source operation in this retained lock has a complete declared binary contract; do not invent one. |

Red state is the former projection behavior: it only seals reads already marked
implemented, leaving an otherwise complete planned `rest_read` unavailable.
Green state must promote that complete direct read, preserve ETL only where a
named stream proves records and pagination, and leave only the named real
foundation above deferred. Retained-artifact hash/byte/capture integrity is
verified separately and never decides this reconciliation.
