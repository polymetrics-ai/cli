# GSD programming-loop prompt attempt

Command:

```bash
scripts/gsd prompt programming-loop "Issue #155 Chatwoot sensitive/admin/destructive policy: implement typed reverse-ETL coverage for remaining safe official mutation operations"
```

Result:

```text
scripts/gsd: unknown GSD command: programming-loop
```

Manual fallback recorded in `PLAN.md` and `RUN-STATE.json`.
