# Prompts — Jira Parity Wave 01 R1

## Kickoff snapshot

Task: implement Jira connector parity for parent #81 and subissues #104-#110 within connector-owned files.

GSD prompt paths:

- `scripts/gsd prompt plan-phase jira-parity-wave01-r1 --skip-research` rendered successfully.
- `scripts/gsd prompt programming-loop init --phase jira-parity-wave01-r1 --dry-run` is unavailable in this checkout (`unknown GSD command: programming-loop`), so manual GSD fallback is active.

Downstream artifact: `.planning/phases/jira-parity-wave01-r1/PLAN.md`

Verification result: local focused gates passed; see `VERIFICATION.md`.
