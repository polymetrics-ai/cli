# GitLab #4394 semantic POST mapping repair — TDD ledger

| Slice | Red evidence | Green evidence | Edge/no-shortcut evidence |
| --- | --- | --- | --- |
| Conan semantic POST reads | `GOCACHE=/private/tmp/gocache-gitlab-4394-r4 go test -count=1 ./internal/connectors/defs/gitlab -run '^TestGitLabSourceLaneMatrixRetainsEveryLockedOperationAndLane$'` failed on the exact base at the new assertion: the first Conan row had `source_semantic_post_read_candidate`, not the required untyped-JSON-body classification. | The same focused command passed after all four lock-derived Conan rows remained `mapped_unproven` with the exact typed-body gap reason. | Mutated `implemented`, `no_request_body`, and `raw_body` promotions are rejected by deterministic matrix reconciliation; no synthetic body is sent. |
| Status-only semantic POST reads | The new regression initially had no dedicated guard against a future direct-read JSON artifact. | The guard proves the four selected rows retain no success media and no implemented direct-read declaration in `operations.json` or `cli_surface.json`. | A direct-read `json_redacted` policy mapping for an empty-media row is rejected; `none` is not claimed as execution without a source-backed request form and runtime proof. Existing method-partition writes are preserved, not reclassified. |
| Denominator and scope | `GOCACHE=/private/tmp/gocache-gitlab-4394-r4 go run ./cmd/connectorgen validate --connector gitlab` on exact base returned `1 connector(s) checked, 0 findings`. | Matrix reconciliation retains source lock/descriptor identity, all 1,754 rows, and seven lanes per row; the focused normal test passed in 2.943s and scoped race test in 27.094s. | JSON parse, `go vet ./internal/connectors/defs/gitlab`, `agentcontractgen check`, and path-scoped diff checks passed; locks, descriptors, Atlas, runtime artifacts, and all provider I/O remain untouched. |

No provider test is appropriate: this is a source-lock/matrix repair. The tests
read retained source bytes only and perform no credential or provider I/O.
