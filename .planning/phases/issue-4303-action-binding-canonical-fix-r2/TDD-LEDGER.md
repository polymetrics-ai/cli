# Refs #4303 — Canonical repair R2 TDD ledger

Every production change begins with a focused production-shaped test added in the same group. `Red:` and `Green:` entries are filled with the exact command and result as work proceeds.

| IDs | Red proof | Green proof | Status |
| --- | --- | --- | --- |
| AB-B01 | Red: `go test -count=1 -timeout 20m ./internal/app -run '^TestDeclarativeTypedDestinationTombstoneAppliesOnlyDeclaredDeleteAndReadsBackAbsence$'` failed because persisted plan JSON exposed `apply_widget` only, while the test's later apply sent `delete_widget`. | Green: the same production-shaped plan now exposes a destructive `transport_physical_actions` set containing both actions; the plan binding, authorization, and destination plan all recheck the exact set. CLI plan and preview render the same safe set in human and JSON forms. | green — commit `3a72b6ab4c0f3811b31b32da1b15423a68780e36` pushed |
| AB-B02 | Red: `go test -count=1 -timeout 20m ./internal/app -run '^TestDeclarativeTypedDestinationRequiresActionOwnedReadBackPolicy$'` failed because a descriptor-wide policy was admitted. | Green: the same command passes with action-owned policy, and action policy field compatibility is validated from the selected mapping before I/O. | green — commit `3a72b6ab4c0f3811b31b32da1b15423a68780e36` pushed |
| AB-B03 | Red: the production-shaped tombstone test with two distinct events mapped to one destination identity failed at read-back with `destination tombstone workset contains duplicate read-back identity`, although both declared missing-ok DELETEs had completed. | Green: `<code>go test -count=1 -timeout 20m ./internal/app -run '^(TestDeclarativeTypedDestinationTombstoneAppliesOnlyDeclaredDeleteAndReadsBackAbsence&#124;TestDeclarativeTypedDestinationReopensUncommittedWorksetAfterLocalReceiptFailure)$'</code>` passed; each mapped tombstone is validated, duplicate expected identities are treated as an absence set, and one acknowledged checkpoint follows two missing-ok DELETEs. | green — review remediation; outer executor owns commit |
| AB-B04 | Red: `go test -count=1 -timeout 20m ./internal/app -run '^TestDeclarativeTypedDestinationBatchLimitIncludesActionReadBack$'` derived an effective unit of 2 despite action read-back max_records=1. | Green: the same command derives one before any provider I/O; workset validation independently checks action-owned read-back maxima. | green — commit pending |
| AB-B05 | Red: `go test -count=1 -timeout 20m ./internal/app -run '^TestDeclarativeTypedDestinationRefusesInvalidWorksetsBeforeProviderWrite$'` sent 33 provider writes for legal 256-byte NUL receipts that overflow after escaping. | Green: the same test observes zero provider writes; exact worst-case escaped receipt sizing refuses the unit before I/O. | green — commit pending |
| AB-B06 | The AB-B05 red path established post-write receipt overflow; its paired-action variant would also overflow the composite envelope. | Green: `go test -count=1 -timeout 20m ./internal/app -run '^TestDeclarativeTypedDestinationReceiptBudgetReservesCompositeAcknowledgement$'` proves two 256-byte bounded receipt parts admit two records, refuses three-by-three before I/O, and rejects an over-limit composite. | green — composite proof added with the AB-B05 receipt-budget repair |
| AB-B07 | The prior helper accepted up to 16 KiB while acknowledgement attachment allowed 8 KiB; this was the same post-write capacity class as AB-B05 and AB-B06. | Green: `TestDeclarativeTypedDestinationReceiptBudgetReservesCompositeAcknowledgement` plus `go test -count=1 -timeout 20m ./internal/synccontract` prove all constructors, composition, parsing, and attachment use `synccontract.MaxPrivateReceiptBytes` (8 KiB). | green — canonical-bound proof added with the receipt-budget repair |
| AB-B08 | Red: `go test -count=1 -timeout 20m ./internal/app -run '^TestDeclarativeTypedDestinationPersistsProviderResultsAfterPostSuccessLocalFailure$'` returned a post-success receipt failure with zero persisted destination results. | Green: the same test fails locally with no checkpoint but retains both ordered sanitized provider successes; secret echoes are masked and ordinary provider values survive. | green — commit pending |
| AB-B09 | Red: after provider success followed by a local private-receipt failure, retrying the same source page created a new staged receipt and provider idempotency key, replaying the mutation. | Green: `<code>go test -count=1 -timeout 20m ./internal/app -run '^(TestDeclarativeTypedDestinationTombstoneAppliesOnlyDeclaredDeleteAndReadsBackAbsence&#124;TestDeclarativeTypedDestinationReopensRepeatedFullAppendWorksetAfterLocalReceiptFailure)$'</code>` passed; an earlier committed full-append workset remains distinct, while the later failed workset reopens after a fresh plan and approval with its original provider key across restart. | green — review remediation; outer executor owns commit |
| AB-W01 | Red: `go test -count=1 -timeout 20m ./internal/connectors -run '^TestDestinationSourceBindingJSONOmitsAbsentBatch$'` failed because `omitempty` emitted `batch:{disposition:"",max_records:0}`. | Green: same command passed after `Batch` became presence-aware and clone-safe. | green — commit `4e8b2506d` pushed |
| AB-W02 | Red: `go test -count=1 -timeout 20m ./internal/connectors/engine -run '^TestCompileSchemaEnforcesOneOf$'` failed because `oneOf` was not in the supported schema dialect. | Green: same command passes and rejects neither/both while accepting exactly one mapping. | green — commit `4e8b2506d` pushed |
| AB-W03 | Red: `go test -count=1 -timeout 20m ./internal/connectors -run '^TestDestinationTransportDescriptorRequiresExactActionClosure$'` accepted `ghost_action`. | Green: same command passes; every eligible action has a reachable strategy before selection. | green — commit `4e8b2506d` pushed |
| AB-W04 | Red: same action-closure test admitted a source binding for the unreachable `ghost_action`. | Green: same command passes; action-owned bindings must name a reachable declared action (typed contract separately validates its writes.json action). | green — commit `4e8b2506d` pushed |
| AB-W05 | Red: `go test -count=1 -timeout 20m ./internal/cli -run '^TestETLTransportBareAndLeafHelpAreContextual$'` first missed `clamp --batch-size` from typed leaf help, then exposed the same omission from bare `pm etl transport` manual help. | Green: the same exact test passes for typed leaf and bare manual text; generator output, embedded `pm help etl`, `docs/cli/etl.md`, website, and definition docs carry the matching semantics. | green — commit pending |

## B09 exact receipt association

- Red: an earlier committed full-append page and a later uncommitted repeated page differed only by observation time, so normalized checkpoint matching retired the later receipt before retry and minted a new provider key.
- Green: `<code>go test -count=1 -timeout 20m ./internal/app -run '^(TestConnectionWarehouseStageRefusesUnreadableReceiptOnRetry&#124;TestDeferredReconciliationRetiresOnlyCommittedConnectionOwnedTransportStages&#124;TestDeferredReconciliationKeepsUncommittedRepeatedFullAppendReceipt&#124;TestDeclarativeTypedDestinationReopensRepeatedFullAppendWorksetAfterLocalReceiptFailure)$'</code>` passed. Checkpoint persistence now records each exact receipt identity, while reconciliation and retry recovery act only on that durable association.
