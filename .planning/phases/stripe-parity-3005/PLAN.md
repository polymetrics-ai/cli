# Stripe connector parity plan (#3005)

## Scope

Implement connector-local Stripe official API parity evidence for parent #3005 and subissues #3006-#3012 on branch `fm/cli-stripe-parity-wave01-r1`.

Allowed production paths for this slice:

- `internal/connectors/defs/stripe/**`
- Stripe connector-owned fixtures under `internal/connectors/defs/stripe/fixtures/**`
- Stripe connector-owned generated/manual docs and command metadata (for example `docs.md`, `cli_surface.json`, `certification.json`, `operations.json` when definition-owned)
- Review-fix exception already applied by the pipeline: `internal/connectors/engine/write.go` and `internal/connectors/engine/write_test.go` for generic write default materialization preserving defaulted `base_url` overrides.

Do not make further shared runtime/engine/CLI Go edits in this slice. If another shared foundation is missing, record the exact dependency in Stripe-owned docs/ledger and continue without claiming implementation completion.

## GSD command path and fallback

- Ran `scripts/gsd doctor`: passed.
- Ran `scripts/gsd list`: passed, 69 commands listed.
- Attempted required implementation command `scripts/gsd prompt programming-loop init --phase stripe-parity-3005 --dry-run`: failed with `scripts/gsd: unknown GSD command: programming-loop`.
- Manual GSD fallback is active for this phase using `.pi/prompts/pm-gsd-loop.md`, `.agents/agentic-delivery/workflows/gsd-universal-runtime-loop.md`, and `docs/prompts/universal-programming-loop-prompts.md`.

## Required skills loaded

- `gsd-core`
- `golang-how-to`
- `golang-design-patterns`
- `golang-structs-interfaces`
- `golang-error-handling`
- `golang-security`
- `golang-safety`
- `golang-testing`
- `golang-cli`
- `golang-documentation`
- CLI help/docs/website parity reference

## Source inventory

Official source for this slice:

- Stripe OpenAPI spec v3: `https://raw.githubusercontent.com/stripe/openapi/master/openapi/spec3.json`
- Current fetched evidence: OpenAPI `3.0.0`, `info.version=2026-07-29.dahlia`, 416 paths, 589 HTTP operations (`GET=263`, `POST=294`, `DELETE=32`).

Parent issue counts remain authoritative until regenerated by exact op-level rows. Do not fabricate implemented/fixture/certified counts.

## Implementation slices

1. **Issue policy addendum**
   - Append idempotent captain-policy addendum to #3005 and #3006-#3012 through `gh-axi`.
   - Preserve existing bodies and counts.

2. **Operation ledger parity**
   - Replace the partial Stripe `api_surface.json` ledger with a source-linked operation ledger covering all 589 official operations exactly once.
   - Use `operation_ledger_version: 1` for blocked/planned rows.
   - Keep already implemented stream/write rows as `covered_by` entries.
   - Classify unimplemented destructive and DELETE operations as in-scope blocked/planned destructive actions, not blanket unsafe exclusions.

3. **Connector-local write safety metadata**
   - Ensure implemented destructive write actions use `confirm: "destructive"` when any are added.
   - If the shared typed destructive confirmation runtime is present, reference it; if missing, document the dependency instead of claiming execution parity.

4. **Bounded command/direct-read metadata**
   - Add or update Stripe-owned `cli_surface.json` only for implemented, bounded, fixed-target command metadata.
   - Avoid raw provider query/path/body passthrough.

5. **Fixtures and conformance evidence**
   - Keep fixture data synthetic and secret-free.
   - Add fixture coverage for any newly implemented stream/write action.
   - Do not run live provider calls or credentialed checks.

6. **Docs/certification truthfulness**
   - Update `docs.md` and optional certification metadata to state fixture-only, uncertified status unless live-safe certification is separately approved.
   - Mention destructive/delete policy correction without changing counts.

## Orchestration decision

`local_critical_path`: the task assigns one connector directory and seven coupled subissues to this isolated worker branch. Mutating subagents were not spawned because the write scope is a single connector-owned tree and additional mutating workers would collide in the same worktree. Read-only recon may be run inline or through non-mutating tools.

## Human gates / non-goals

- No secrets, credential requests, live Stripe calls, provider writes, certification claims, VPS/Thaalam work, merges, or pushes to default branch.
- No new dependencies.
- No further shared runtime edits beyond the write-default materialization fix already applied by the pipeline.
- Reverse ETL remains plan → preview → explicit approval → execute.

