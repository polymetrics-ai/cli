# TDD ledger — issue 4264

| Slice | Red evidence | Green evidence | Refactor / scope evidence |
| --- | --- | --- | --- |
| Operation-backed action kind | **Red (2026-08-19):** `go test -timeout 20m ./cmd/connectorgen -run 'TestCertificationParityClassifier(ProjectsOnlyTheNormalizedTaxonomy|RefusesUndeterminableOperationWriteActionKind)$'` failed: DELETE and update operations projected `WriteActionKind:""`; an indeterminate POST returned nil error. | **Green (2026-08-19):** the same focused command passed after the shared operation classifier derived `delete`, `update`, `create`, `upsert`, and GraphQL `custom` kinds. | Derivation remains in `certificationsweep.go`; no connector paths or provider-specific operation-name rules changed. |
| Existing write action derivation | The existing `direct write delete action` table case is run in the red command and asserts a `writes.json` action projects `delete`. | **Green (2026-08-19):** the same test still passes because the `writes.json` branch is evaluated before operation derivation. | `writes.json` remains the first declarative source. |
| Indeterminate mutation | **Red (2026-08-19):** a POST `rest_write` with only `mutation_class: secret` had an empty action kind and no error. | **Green (2026-08-19):** the focused test asserts a contextual error and verifies the sweep converts it to `product_defect`. | No fallback assigns `custom` to ambiguous REST mutations. |
| Real generated artifacts | **Red (2026-08-19):** `go run ./cmd/connectorgen certification-sweep . --connector github --check` reported artifact drift after the classifier behavior changed. | **Green (2026-08-19):** regenerated GitHub sweep passes `--check`; `TestCertificationSweepCommandChecksGeneratedGitHubArtifact` passes. Remote Zoom parity source has 18 REST DELETE operation IDs intersecting exactly 18 direct-write commands, all on the shared DELETE derivation path. | GitHub artifact was generator-written. Zoom definitions are absent from this `main` base and owned by its concurrent lane, so no Zoom definition/artifact was edited here. |
