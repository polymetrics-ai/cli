# TDD ledger — issue #4291

| Slice | Red | Green | Refactor / evidence |
| --- | --- | --- | --- |
| Batch 6 map | `test ! -f` for each batch-6 source lock and disposition ledger: pass before implementation, proving the acceptance artifact does not exist | Run the real ledger invariant checker against the ten generated connector directories | Inspect source URLs, one-to-one endpoint identity, documented DELETE count, six-class count, and reason vocabulary |
| Batch 7 map | `test ! -f` for each batch-7 source lock and disposition ledger: pass before implementation, proving the acceptance artifact does not exist | Run the same real checker against the remaining ten directories | Repeat provenance and coverage inspection; run connector generator validation and surface drift checks |
| Transport truthfulness | Before implementation, no issue-local ledger can evidence the corrected reverse-ETL state | Checker requires typed write actions to remain enabled `direct_write` while their `declaration.reverse_etl` attribute carries `generic-typed-destination-executor`, `internal/app/issue_label_warehouse_transport.go:85-95`, and the prescribed minimal change | Confirm no `transport_binding` or connector action is invented |
| Reachability truthfulness | The provisional map enabled existing streams even where no API-surface command or typed action bound the source operation | Checker requires every `enabled` row to carry `covered_by.direct_read`, `covered_by.direct_reads`, or `covered_by.write`; streams without such a binding are `declaration-pending` | Report per connector documented / enabled / commands / writes / deletes / ENABLED% |
| Source-lock completeness recovery | The captain's Salesloft spot-check proves the initial 12-operation inventory and `declared_percent: 100` are false evidence of complete provider coverage | Each suspect connector has a complete official-spec or complete-rendered-reference lock with `counts.total`, per-method counts, `operations_found`, and explicit coverage-confidence basis; its API surface and every ledger row are regenerated from that inventory | Reject landing-page/overview artifacts, self-referential percentages, an operation array whose size differs from the provider count, and preserving the legacy API-surface boundary during recovery |
| Segment provider-shape migration | The pinned Segment Public API OpenAPI 3.0.3 contains `GET /` (`getWorkspace`) and no `GET /workspaces`; the legacy paginated stream made `connectorgen validate` fail `surface_incomplete` after remapping | Declare a non-paginated singleton `workspace` stream at `GET /`, with `records.path=data.workspace` and `single_object=true`; make the check and fixtures exercise that exact request/response shape | Preserve `GET /workspaces` only as an explicit `REMOVED` legacy declaration in the source lock and disposition ledger with reason `not present in the provider's authoritative OpenAPI`; `connectorgen validate` must become green without inventing a `/workspaces` source row |
| Zoho Bigin complete-reference recovery | The 8KB Bigin landing-page lock reports only 50 operations, has no `counts.total` / `operations_found`, and cannot prove the ordinary or bulk endpoint surface; the first crawler parser also fails closed when it sees markup-split method/URL nodes or an omitted bulk base | Resume the persisted 98-page sitemap crawl until every page is retrieved; normalize the provider's rendered operation templates to 75 unique paths, then regenerate the source lock, API surface, and ledger at 75/75/75 with explicit complete-rendered confidence | Keep parser corrections as crawl-cache invalidations rather than source-lock results, retain only exact typed-write bindings as enabled, and confirm every direct-write row carries the neutral typed-destination foundation gap without a transport binding |
| ServiceNow fixed-template recovery | The legacy 22-row surface expands six Table API templates over three selected tables, presenting a finite count for an instance-defined schema | Pin only the official fixed list/get/insert/replace/patch/delete templates with `counts.total: 6`, while recording table names, fields, ACLs, and customer tables as an explicit dynamic-schema basis with no instance total | Use duplicate API-surface coverage projections only to retain real existing stream/action bindings; do not add source operations or increase the six-template denominator, and keep all direct writes in `direct_write` with the neutral destination gap |
| ActiveCampaign embedded-OpenAPI recovery | The legacy 61-row landing-page inventory cannot prove the full current v3 surface | Parse the complete OpenAPI 3.1.0 document embedded in the official reference renderer and regenerate the source lock, API surface, and disposition ledger at 296 source operations | Preserve no enabled operation without an exact command/action binding; every recovered mutation is `direct_write` with the neutral destination gap as an attribute |

The map is a declarative inventory artifact, not a change to runtime behaviour. The red/green checks therefore assert the observable artifact state that is absent before the map and present after it.

## Relaunch reconciliation ledger — 2026-08-20

| Slice | Red | Green | Refactor / evidence |
| --- | --- | --- | --- |
| Seven-surface baseline | `READINESS-BASELINE.json` reports zero declared source/destination transports, 1,343 direct reads without an exact command binding, 1,745 direct writes without an exact typed-action binding, and two executable binary commands whose ledger class is not binary. | Every source-locked provider operation is bound to its exact user-reachable command or typed action, or carries a precise named technical contract gap; safety, scope, rarity, binary form, and live-certification state are rejected as an unreachability reason. | Preserve a separate `provider_live_certification: pending` value and executable safety metadata. |
| Declarative typed destinations | Before foundation `609f23bb3861ba7bc2ef1f7bc5246f5751cf9e57`, generic persisted App/CLI dispatch, exact action selection, and schema-known camelCase inputs were unavailable. | Each declared `declarative_typed_destination` has exact named actions, input-field source bindings, keyed delivery, durable acknowledgement, a strategy per mode, connector-owned evidence, and passing no-credential preflight. | Use `docs/sync-transport-definition.md`; never add a generic HTTP writer or `transport_binding` to the selected action. |
| Generated surface parity | The two existing binary commands demonstrate ledger/class drift, and the failed CI run shows stale Gorgias blocked-row metadata. | `surface-sync --check`, generated docs/website-data check, and the targeted Gorgias source-ledger test pass with regenerated projection artifacts committed. | Manual review confirms every generated CLI command traces to a pinned source operation and its safety/approval policy. |
| Gorgias destination proof and legacy read | RED: `GET /api/tickets/{ticket_id}/messages` and the documented binary download were source-locked but disabled in the disposition/API ledger; no Gorgias sync transport existed. The public provider artifact hashed to the lock but `params-import` refused its valid union parameter declaration at `/api/jobs`, so the test could not be generated from the importer. | GREEN: a source-backed bounded `list_ticket_messages` command, binary ledger classification, `tickets → update_ticket` source/destination declaration, and the generated endpoint ledger validate. `TestGorgiasAPISurfaceOperationLedger`, runtime preflight sweep, and the installed no-credential binary all pass/reach expected preflight. | Foundation `609f23bb3861ba7bc2ef1f7bc5246f5751cf9e57` supplies App/CLI dispatch and action selection. All 61 typed actions are explicit: one bound proof, 59 declaration-pending exact mapping/conformance, and a CLI-reachable multipart semantic exclusion. |
| Chatwoot destination proof | RED: Chatwoot had seven executable ETL streams and 60 typed actions but no source/destination declaration; every typed action still named the retired generic-destination gap. | GREEN: declare all seven sources and exact `contacts(id,blocked) → update_contact` binding with keyed delivery, durable acknowledgement, complete mode strategies, and fixture/dry evidence. Definition validation and runtime preflight sweep pass. | Foundation `609f23bb3861ba7bc2ef1f7bc5246f5751cf9e57` supplies App/CLI dispatch and action selection. All 60 actions are explicit: one bound proof and 59 declaration-pending exact mapping/conformance. |
| Customer.io destination proof | RED: Customer.io had 16 fixture-backed streams and ten source-bound typed actions without a source/destination declaration; the other 58 mutations mislabelled unauthored connector contracts as generic foundation gaps. | GREEN: declare all 16 sources and exact `snippets(name,value) → update_snippet` binding with keyed delivery, durable acknowledgement, complete mode strategies, and fixture/dry evidence. Definition validation and runtime preflight sweep pass. | Foundation `609f23bb3861ba7bc2ef1f7bc5246f5751cf9e57` supplies App/CLI dispatch and action selection. The other nine typed actions are declaration-pending exact mapping/conformance; the other 58 mutations remain `declaration-pending`, retaining direct CLI reachability as the required follow-on rather than calling them an engine gap. |
| Close destination proof | RED: Close had 14 fixture-backed streams and 12 source-bound typed actions without a source/destination declaration; its other 151 direct writes mislabelled unauthored connector contracts as generic foundation gaps. | GREEN: declare all 14 sources and exact `leads(id,name,description,url,status_id) → update_lead` binding with keyed delivery, durable acknowledgement, complete mode strategies, and fixture/dry evidence. Definition validation and runtime preflight sweep pass. | Foundation `609f23bb3861ba7bc2ef1f7bc5246f5751cf9e57` supplies App/CLI dispatch and action selection. The other 11 typed actions are declaration-pending exact mapping/conformance; the other 151 direct writes are `declaration-pending`, retaining exact connector contracts and CLI reachability as the required follow-on. |
| Outreach destination proof | RED: Outreach had 96 fixture-backed streams and 163 source-bound typed actions without a source/destination declaration; every typed action still named the retired generic-destination gap. | GREEN: declare all 96 sources and exact `sequences(id) → activate_sequence` binding with keyed delivery, durable acknowledgement, complete mode strategies, and fixture/dry evidence. Definition validation and runtime preflight sweep pass. | Foundation `609f23bb3861ba7bc2ef1f7bc5246f5751cf9e57` supplies App/CLI dispatch and action selection. The other 162 typed actions are declaration-pending exact mapping/conformance. Mutation safety remains approval metadata, never an ineligibility reason. |
| Zoho Bigin destination proof | RED: Zoho Bigin had 13 fixture-backed streams and six source-bound typed actions without a source/destination declaration; its other 37 mutations mislabelled unauthored connector contracts as generic foundation gaps. | GREEN: declare all 13 sources and exact `records(id) → delete_record` binding with keyed delivery, durable acknowledgement, complete mode strategies, and fixture/dry evidence. Definition validation and runtime preflight sweep pass. | Foundation `609f23bb3861ba7bc2ef1f7bc5246f5751cf9e57` supplies App/CLI dispatch and action selection. The other five typed actions are declaration-pending exact mapping/conformance; the other 37 direct writes are `declaration-pending`; delete safety stays confirmation/approval metadata. |
| Chargebee destination proof | RED: Chargebee had 32 fixture-backed streams and 36 source-bound typed actions without a source/destination declaration; its other 331 mutations mislabelled unauthored connector contracts as generic foundation gaps. | GREEN: declare all 32 sources and exact `customers(id) → update_customer` binding with keyed delivery, durable acknowledgement, complete mode strategies, and fixture/dry evidence. Definition validation and runtime preflight sweep pass. | Foundation `609f23bb3861ba7bc2ef1f7bc5246f5751cf9e57` supplies App/CLI dispatch and action selection. The other 35 typed actions are declaration-pending exact mapping/conformance; the other 331 direct writes are `declaration-pending`; finance/destructive safety remains approval metadata. |
| ServiceNow destination proof | RED: the fixed public Table API exposes dynamic table templates, so only two action/source projections existed and a naive expansion would invent customer table schemas. | GREEN: declare all three fixture streams and exact `incidents(sys_id) → update_incident` binding with keyed delivery, durable acknowledgement, complete modes, and fixture/dry evidence. | Foundation `609f23bb3861ba7bc2ef1f7bc5246f5751cf9e57` supplies App/CLI dispatch and action selection. A separate eligibility ledger accounts for its two source-backed typed actions: one bound and one declaration-pending exact mapping/conformance. Preserve the fixed-template provider boundary. |
| Braze destination proof | RED: Braze had 21 fixture streams and 29 typed actions without source/destination declarations; its public operation inventory remains unproven. | GREEN: declare the existing streams and exact `content_blocks(content_block_id) → update_content_block` proof with keyed delivery, acknowledgement, closed modes, and fixture/dry evidence. | Foundation `609f23bb3861ba7bc2ef1f7bc5246f5751cf9e57` supplies App/CLI dispatch and action selection. All 29 typed actions are explicit (one bound, 28 declaration-pending exact mapping/conformance); preserve the unproven inventory flag instead of claiming complete provider coverage. |
| Help Scout destination proof | RED: Help Scout had 24 fixture streams and 65 source-bound typed actions without source/destination declarations; its provider inventory remains unproven. | GREEN: foundation `609f23bb3861ba7bc2ef1f7bc5246f5751cf9e57` accepts the exact closed-schema `conversations(id) → update_conversation(conversationId)` declaration with keyed delivery, acknowledgement, closed modes, generated projections, and fixture/preflight evidence. | All 65 typed actions are explicit: one is fixture/preflight bound, `update_customer` is not enabled by the open `declarative-typed-destination-action-specific-source-bindings` gap, and 63 are eligible but declaration-pending exact mapping/conformance. The five source-locked v3 reads are separately `foundation-gap` blocked on `cli-operation-route-override-foundation-r1`; preserve the unproven inventory and existing binary-ledger defect rather than claiming completeness. |
| Help Scout v3 route selection | RED: the configured `https://api.helpscout.net/v2` base plus a source-locked `/v3/...` operation constructs `/v2/v3/...` under `internal/connectors/engine/direct_read.go:701-717` and `direct_read_paginate.go:499-504`; changing stored defaults would break existing v2 connections. | GREEN: pending `cli-operation-route-override-foundation-r1`. Its closed declaration route must be exercised across direct read/write, binary download/upload, ETL, and reverse ETL with all arbitrary caller URLs, undeclared routes, and silent fallback refused before I/O. | Keep all five v3 records source-locked and `foundation-gap` blocked; no Help Scout-specific override or generic HTTP escape is authorized. |
| Captain zero-omission pre-merge gate | RED: a connector or provider operation is missing source URL/version/hash, canonical mapping, runtime reachability, CLI/website projection, or fixture/conformance evidence; a surface is silently omitted or marked N/A without provider proof. | GREEN: a twenty-connector, operation-level evidence matrix has a separately classified ETL, reverse ETL, direct read/write, binary-download, and binary-upload cell for each provider operation. | Reject safety/tier/scope/destructive classification as an N/A or disabled reason; require it as typed runtime/confirmation metadata instead. |
| Stale typed-destination gap, increment 1 | RED: after #4304, 1,111 direct-write ledger rows in ten untouched connectors still named `generic-typed-destination-executor`, a retired factory limitation, despite having no connector-owned typed action. | GREEN: `salesloft`, `copper`, `klaviyo`, `intercom`, and `freshdesk` replace 499 of those rows with explicit `declaration-pending` reverse-ETL eligibility and a connector-owned destination declaration TODO. | The generated operation record preserves the five remaining connectors as an observable follow-up, never as a hidden or resolved gap. |
| Stale typed-destination gap, increment 2 | RED: the operation record retains 612 stale generic-gap rows for `segment`, `activecampaign`, `iterable`, `square`, and `braintree`. | GREEN: each direct-write eligibility is `declaration-pending` because no typed action exists; each destination transport is a connector-owned declaration TODO. The record has no `generic-typed-destination-executor` row. | Preserve only the two real Help Scout foundation gaps, with their source-traced fan-out and `merge_ready: false`. |
| Rendered-reference lock v3, increment 1 | RED: `go run ./cmd/connectorgen validate internal/connectors/defs/iterable` rejects legacy per-operation `source_url`, and `go run ./cmd/connectorgen operation-evidence . --check` detects nine duplicate Iterable provider identities spanning 29 routes. | GREEN: the 29 Iterable identities are route-qualified in both its lock and disposition crosswalk; operation-evidence regeneration passes at 5,457 rows. Close, Salesloft, Copper, Zoho Bigin, and Klaviyo each parse as a v3 document-owned source lock, advancing the validator to the missing-descriptor check. | The reproducible issue-local mapper preserves locked bytes/SHA-256, source operation identity, and rendered citations. It refuses a cross-origin rendered citation rather than reassigning its provenance. |
| Close source descriptor import | RED: after Close's v3 lock reaches the descriptor phase, the descriptor is absent. The retained hash-pinned OpenAPI import now reaches `GET /activity/` parameter `id__in` and refuses its documented `anyOf: [array<string>, null]` request schema. | A shared source importer change must faithfully normalize only the OpenAPI nullable-null union into the equivalent optional non-null input, or emit a source-bound per-operation gap for every other union; it must never silently select an arbitrary union arm. | The refusing loop is `validateBoundedRequestSchemaWithinEnum` in `cmd/connectorgen/sourceimport.go:7175-7178`. `#4345` has already moved the former missing-numeric-bound case out of this terminal path. No connector-local schema may invent or collapse a general union. |
| Main foundation reconciliation | RED: the Help Scout source disposition still lists five `declarative-operation-route-override` rows and one `declarative-typed-destination-action-specific-source-bindings` row even though `origin/main` supplies both contracts. | The shipped Help Scout bundle must load its two action-owned source bindings, and the five v3 reads must reach the declared `/v3` routes through the actual executor without a provider credential. | Record the stale ledger as evidence debt only until source-lock schema v3 can be projected by `operation-evidence`; do not retain a closed foundation gap or add a connector-local routing shim. |
| V3 source retention increments | RED: `source-retain` must refuse an HTTP redirect, bot/authorization response, 404, or bytes that differ from the immutable source lock rather than replacing a provider contract. | GREEN: only a source response matching the lock's exact SHA-256 and byte count creates a connector-owned artifact and provenance manifest; the subsequent offline import must expose the next exact source-contract refusal, if any. | Record `undetermined` only where the pinned lock establishes no license/terms information. Drift, redirects, bot blocks, and missing pages remain source-evidence exceptions, never re-pins or inferred schemas. |
