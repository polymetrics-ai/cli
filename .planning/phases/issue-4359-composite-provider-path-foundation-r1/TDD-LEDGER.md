# Issue #4359 — TDD ledger

## Red

Pending. The red test will exercise `ResolveImplementedCommandBinding` with the actual CircleCI source identity and the existing runtime templates, then reproduce the eleven binding failures against the immutable Batch-1 declaration shape. It must fail before production implementation is changed.

## Green

Pending.

## Refactor / review

Pending. The implementation must remain a closed equivalence proof; no request interpolation, execution, parser, method, body, or credential logic may change.
