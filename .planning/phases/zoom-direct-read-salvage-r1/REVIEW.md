# Code review — Zoom direct-read salvage

## Scope

- Zoom-local JSON declarations, fixtures, docs, and command-surface tests
- Derived `internal/connectors/defs/operation_endpoint_ledger.json`
- Wave #4267 GSD/TDD evidence

## Manual review result

No critical, warning, or informational finding.

The salvage transform selects only `rest_read` declarations and `direct_read` command rows from PR #3951. It excludes every write, reverse-ETL, binary, AI Services, authentication, engine, allowlist, and certification-status change. The endpoint ledger is generated by the repository command rather than hand-edited. The fixture test verifies fixed declared routes, path substitution, bearer transport, bounded responses, and output-policy execution against a loopback server; the cohort test independently preflights every real command path. The SCIM2 non-default origin is described explicitly and not overstated as live-ready.

## Review fallback

`scripts/gsd prompt code-review zoom-direct-read-salvage-r1` was resolved. The canonical contract forbids the reviewer-role spawn expected by the official workflow, so this file records the required inline/manual review and its disposition.
