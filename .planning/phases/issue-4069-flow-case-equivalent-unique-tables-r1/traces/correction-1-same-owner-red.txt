# #4069 correction 1 / 5 — strict RED record

Canonical finish-plan snapshot SHA-256:

```text
939f14f61defd993f8ad0335a5aeb617d97083c9f73a6a75259d0e312ae8f408
```

Planning checkpoint before this RED: `465e02911`.

## App inventory, pre-mutation, SQL, and direct-read controls

```text
go test -timeout 20m ./internal/app -run 'Test(CreateConnectionRejectsSameOwnerCaseEquivalentDestinationTables|LegacySameOwnerCaseEquivalent)' -count=1
exit 1
```

Observed expected failures:

```text
TestCreateConnectionRejectsSameOwnerCaseEquivalentDestinationTables:
  CreateConnection() accepted case-equivalent destination tables for one warehouse owner
TestLegacySameOwnerCaseEquivalentInventorySurvivesOpenAndFailsBeforeMutation:
  RunETL() accepted legacy same-owner case-equivalent destination tables
TestLegacySameOwnerCaseEquivalentSQLUsesDeclaredInventory:
  generic/selected bare and quoted references returned nil;
  generated alias returned an ordinary missing-table error, not the typed collision
```

`TestLegacySameOwnerCaseEquivalentDirectReadsUseOnlyPhysicalExactNames` passed
as a RED control: test-owned Parquet files were resolved by their actual host
filesystem spellings; query/action/reverse reads did not alias a missing case
variant.

## Flow and schedule re-entry boundary

```text
go test -timeout 20m ./internal/cli -run '^TestFlowLegacySameOwnerCaseEquivalentInventoryStopsAtTheTypedBoundary$' -count=1
exit 1
```

Observed expected failures:

```text
omitted bare: expected an error, got nil
omitted quoted: expected an error, got nil
selected bare: expected an error, got nil
selected quoted: expected an error, got nil
```

The omitted-bare case invokes the same flow twice with a shared checkpoint
store, representing scheduler re-entry through `pm flow run`; both attempts
currently complete instead of failing at the typed collision boundary.

All fixture data is temporary test-owned local Parquet/DuckDB state. No
provider call, credential value, transport registration, or production wiring
was used. This RED commit contains tests and evidence only; implementation
begins in the next commit.
