# #4069 correction 2 / 5 — broad local and inline GSD verification

Canonical finish-plan SHA-256:
`939f14f61defd993f8ad0335a5aeb617d97083c9f73a6a75259d0e312ae8f408`

All commands below ran locally on
`fix/4069-flow-case-equivalent-unique-tables-r1` after the minimum GREEN
commit `f9b5654c8`. No credential, provider, transport, PR, workflow rerun,
or dependency-install command was used.

```text
go test -timeout 20m ./internal/cli -run '^(TestGoldenDocsGenerateMatchesTrackedCLIManuals|TestGoldenTranscripts|TestDocsGenerateAndValidateConnectorDocs|TestDocsGenerateIncludesConnectorCatalog)$' -count=1
exit 0

go test -timeout 20m ./internal/cli -count=1
exit 0 (161.636s)

go vet ./...
exit 0

golangci-lint run ./internal/cli
exit 0 (0 issues)

make lint
exit 0 (0 issues)

gofmt -d internal/cli/docs.go
exit 0 (no output)

go build ./cmd/pm
exit 0

make docs-check-no-build
exit 0

go run ./cmd/agentcontractgen check
exit 0

go run ./cmd/connectorgen surface-sync --check
exit 0 (552 connector definitions scanned; 0 issues)

go run ./cmd/connectorgen validate internal/connectors/defs
exit 0 (552 connector definitions scanned; 0 issues)

scripts/verify-gsd-workflow
exit 0 (implementation files have GSD/TDD evidence against origin/main)
```

Generator/idempotence and help evidence:

```text
go run ./cmd/pm docs generate --dir docs/cli --connectors-dir docs/connectors
exit 0 twice; no docs/cli or docs/connectors diff

POLYMETRICS_UPDATE_GOLDEN_TRANSCRIPTS=1 go test -timeout 20m ./internal/cli -run '^TestGoldenTranscripts$' -count=1
exit 0 twice; the first pass updated only six expected flow/connections help,
bare-manual, and JSON-manual transcript values; the second pass was clean

cd website && npm run gen:website-data
exit 0 twice; no website source or generated-output diff

go run ./cmd/pm help flow
go run ./cmd/pm flow
go run ./cmd/pm flow --json
each exited 0 and contains the approved three-line fail-closed wording
```

No-install Website checks:

```text
cd website && npm run lint
exit 0; 13 pre-existing warnings in untouched files, no errors

cd website && npm run typecheck
exit 0

cd website && npm run test:unit
exit 0 (12 files, 80 tests)

cd website && npm run test:scripts
exit 0 (27 tests)

cd website && npm run build
exit 0; existing non-fatal default-auth diagnostic only, no secret supplied
```

Scoped-diff proof:

```text
git diff --check 678e294568a8a010a460ecb05fe11a42e1eb40f2..HEAD
exit 0

git diff --name-only 678e294568a8a010a460ecb05fe11a42e1eb40f2..HEAD
only #4069 phase evidence, internal/cli/docs.go, and
internal/cli/testdata/golden_transcripts.json
```

Inline `verify-work` and `code-review` were performed from the resolved GSD
prompts under the documented single-worker fallback. Both PASS: the acceptance
criteria are covered, the source owns the generated manual, and no candidate
finding remains. PR/no-mistakes/CI state is intentionally excluded from this
local record.
