# #4069 correction 2 / 5 — targeted GREEN

Canonical finish-plan SHA-256:
`939f14f61defd993f8ad0335a5aeb617d97083c9f73a6a75259d0e312ae8f408`

Authoritative source change only:

- `internal/cli/docs.go:flowHelp` now contains the three approved
  case-equivalent fail-closed lines.
- The same source's `connectionsHelp` now uses the already-tracked, accurate
  `any local sync` wording instead of `its local sync`.

Derived-artifact commands and results:

```text
go run ./cmd/pm docs generate --dir docs/cli --connectors-dir docs/connectors
exit 0; generated CLI manuals match tracked output; no docs/cli or docs/connectors diff

POLYMETRICS_UPDATE_GOLDEN_TRANSCRIPTS=1 go test -timeout 20m ./internal/cli -run '^TestGoldenTranscripts$' -count=1
exit 0; updated only six flow/connections help, bare-manual, and JSON-manual fixture values

cd website && npm run gen:website-data
exit 0; no website source or generated output diff
```

Each generator was invoked a second time with the same result. The candidate
generated diff is exactly:

```text
internal/cli/docs.go
internal/cli/testdata/golden_transcripts.json
```

Focused verification passed:

```text
go test -timeout 20m ./internal/cli -run '^(TestGoldenDocsGenerateMatchesTrackedCLIManuals|TestGoldenTranscripts|TestDocsGenerateAndValidateConnectorDocs|TestDocsGenerateIncludesConnectorCatalog)$' -count=1
go run ./cmd/pm help flow
go run ./cmd/pm flow
go run ./cmd/pm flow --json
```

The three approved lines appear in each flow manual form. No runtime logic,
credential, connector, transport, website source, workflow, PR, or GitHub
metadata changed.
