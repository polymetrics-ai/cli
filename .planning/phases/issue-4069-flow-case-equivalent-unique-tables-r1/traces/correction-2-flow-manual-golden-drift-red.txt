# #4069 correction 2 / 5 — committed RED

Canonical finish-plan SHA-256:
`939f14f61defd993f8ad0335a5aeb617d97083c9f73a6a75259d0e312ae8f408`

Command, run before an `internal/cli/docs.go` source edit:

```text
go test -timeout 20m ./internal/cli -run '^TestGoldenDocsGenerateMatchesTrackedCLIManuals$' -count=1
```

Result: exit `1`, as required.

The second independent invocation exactly reproduced the GitHub Verify failure:

```text
--- FAIL: TestGoldenDocsGenerateMatchesTrackedCLIManuals
generated docs drift for flow.md
```

The tracked `docs/cli/flow.md` contains these lines while generated `flowHelp`
output omits them:

```text
  A case-equivalent spelling whose owner cannot be decided also fails closed;
  set "connection" to a known healthy owner rather than relying on an
  unscoped query.
```

The first independent invocation exited `1` at another map entry first:

```text
generated docs drift for connections.md
want: ... any local sync refuses ...
got:  ... its local sync refuses ...
```

`TestGoldenDocsGenerateMatchesTrackedCLIManuals` iterates the documentation
map and stops at the first mismatch. Git provenance shows both mismatches are
in the #4069 pipeline document range: `678e294568` changed the tracked
connection wording but not the embedded source, and added the tracked flow
wording while `flowHelp` omitted it. GREEN updates only those authoritative
embedded strings, then regenerates derived artifacts. No runtime code,
behavior, credential, transport, GitHub workflow, or PR state is changed.
