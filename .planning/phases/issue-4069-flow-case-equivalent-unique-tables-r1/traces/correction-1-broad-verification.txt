# #4069 correction 1 / 5 — broad local verification

Canonical private finish-plan SHA-256:
`939f14f61defd993f8ad0335a5aeb617d97083c9f73a6a75259d0e312ae8f408`

Audited starting head: `d9022359e7b7bc2f7eb262c16177b52010681192`

This record covers the correction commits through `bb7c2458c` plus a
format-only cleanup of the same-owner test source. It contains no credentials,
provider calls, transport activity, production wiring, or certification claim.

## Targeted and affected Go coverage — PASS

```text
go test -timeout 20m ./internal/app -run '^TestLegacySameOwnerCaseEquivalentSQLUsesDeclaredInventory$' -count=1
go test -timeout 20m ./internal/cli -run '^(TestGoldenTranscripts|TestGoldenDocsGenerateMatchesTrackedCLIManuals)$' -count=1
go test -count=1 -timeout 20m ./internal/app
go test -count=1 -timeout 20m ./internal/cli
go test -count=1 -timeout 20m ./internal/flow
go test -count=1 -timeout 20m ./internal/warehouse
go test -count=1 -timeout 20m ./internal/schedule
go test -race -count=1 -timeout 20m ./internal/app -run 'Test(CreateConnectionRejectsSameOwnerCaseEquivalentDestinationTables|LegacySameOwnerCaseEquivalent)'
go test -race -count=1 -timeout 20m ./internal/cli -run 'TestFlow(CaseEquivalentUniqueTablesPreserveGenericSQLAndTypedAmbiguity|LegacySameOwnerCaseEquivalentInventoryStopsAtTheTypedBoundary|OmittedConnectionRejectsGeneratedOwnerAliases|GeneratedOwnerAliasCollision|GeneratedOwnerAliasCaseVariantCollision|CaseVariantBareTableAmbiguity|SourceConnectionSelectorRefusesOmissionAndAcceptsUnattributed)'
```

All commands passed. The real-table control writes
`RECORDS__<owner-id>` into the legacy same-owner fixture and proves that a
resolver-visible physical table wins over an invented alias while the collided
base identifier remains typed and unavailable to SQL.

## Quality and repository gates — PASS

```text
gofmt -d internal/app/app.go internal/app/types.go internal/app/query_engine_duckdb.go internal/app/warehouse_destination_collision.go internal/app/warehouse_connection_isolation_test.go internal/warehouse/layout.go internal/cli/flow_cli_test.go internal/cli/docs.go
git diff --check d9022359e7b7bc2f7eb262c16177b52010681192..HEAD
go vet ./...
make lint
make tidy-check
go build -o pm ./cmd/pm
make smoke-no-build
make agent-contract-check connectorgen-validate connectorgen-surface-sync github-parity-artifacts-check connectorgen-certification-matrix connector-boundary connector-canon-check release-workflow-check
scripts/verify-gsd-workflow
go run ./cmd/prissueguard --title 'feat(flow): scope warehouse reads and guard table collisions' --body $'Refs #4069\nRefs #3897\nParent #4060'
```

All commands passed. The issue guard reported three linked issues. The live
draft PR #4071 was read through `gh-axi`; it remains titled
`feat(flow): scope warehouse reads and guard table collisions`, contains
`Refs #4069` and `Refs #3897`, identifies parent #4060, and remains a draft.
No PR metadata was changed.

## CLI, manuals, and website parity — PASS

```text
go run ./cmd/pm docs generate --dir docs/cli --connectors-dir docs/connectors
POLYMETRICS_UPDATE_GOLDEN_TRANSCRIPTS=1 go test -timeout 20m ./internal/cli -run '^TestGoldenTranscripts$' -count=1
make docs-check-no-build
npm run typecheck
npm run lint
npm run build
```

Generated `connections` and `query` manuals match their embedded help source,
and golden transcripts cover `pm help connections`, `pm connections`,
`pm help query`, and `pm query` forms. The built binary was also used to
inspect the updated connections and query manuals. The existing dispatcher
opens a project before `connections create --help` and `query run --help`, so
those bare-root invocations keep their inherited missing-project behavior;
this correction adds no command or flag and the generated/manual transcript
tests are the applicable help contract.

Website typecheck and production build passed. Website lint completed with 13
warnings in pre-existing component files outside this correction; no warning
references either changed MDX document. The build emitted its existing default
local-auth configuration diagnostic but completed successfully; no secret was
provided or recorded.

## Manual GSD fallback — PASS

```text
scripts/gsd doctor
scripts/gsd sources verify-work
scripts/gsd sources code-review
scripts/gsd prompt verify-work
scripts/gsd prompt code-review
```

The repository-local Pi adapter is healthy. This issue is not a numbered
ROADMAP phase and the canonical contract forbids role spawning, so verify-work
and standard code review were performed inline against the changed app,
warehouse, flow, CLI, docs, and evidence files. No acceptance gap or candidate
finding remains. The manual fallback is recorded in RUN-STATE.json,
VERIFICATION.md, and REVIEW.md.
