# Verification — Twenty S7 fixtures/certify

Status: corrective VERIFY-TURN59 pass in progress. Original S7 fixture gates were green, but final certify gate stayed red; current correction owns only `internal/connectors/certify/**` plus these S7 artifacts.

## Passed gates
- `find internal/connectors/defs/twenty/fixtures .planning/phases/twenty-s7-fixtures-certify -name '*.json' -type f -print0 | xargs -0 -n1 jq -e . >/tmp/twenty-s7-jq.log` — PASS.
- `go run ./cmd/connectorgen validate internal/connectors/defs --json` — PASS: `findings=0`, `warnings=0`.
- `make connectorgen-validate` — PASS: `connectorgen validate: 548 connector(s) checked, 0 findings`.
- `go test ./internal/connectors/conformance -run TestTwentyFixturesCoverAllStreamsAndWrites -count=1` — PASS.
- `go test ./internal/connectors/conformance -run 'TestConformance/twenty' -count=1` — PASS.
- `go test ./internal/connectors/conformance -run 'TestTwentyFixturesCoverAllStreamsAndWrites|TestConformance/twenty' -count=1` — PASS: `ok polymetrics.ai/internal/connectors/conformance 1.317s`.
- `gofmt -w cmd internal` and `gofmt -l cmd internal` — PASS, no formatted diff outside intended docs/test state.
- `go vet ./...` — PASS.
- `go test ./...` — PASS (slowest package `internal/connectors/certify` ~340s).
- `go build ./cmd/pm` — PASS.
- `./pm help connectors` — PASS, metadata/help only; no credentials read.
- `./pm help connectors certify` — PASS/help fallback to connectors manual; no credentials read.
- `make docs-check-no-build` — PASS: `Validated connector docs in docs/connectors`.
- `cd website && pnpm run gen:website-data` — PASS.
- Website idempotency: captured generated data diff, reran `pnpm run gen:website-data`, compared before/after diffs with `cmp -s` — PASS: `website gen idempotent`.
- `git diff --check` — PASS.

## Certify result
- Credential-free local replay server used; no live `api.twenty.com` calls; placeholder env value only.
- Historical non-full command: `PM_TWENTY_CERT_TOKEN=fixture_token_placeholder ./pm connectors certify twenty --stream attachments --limit 1 --config base_url=http://127.0.0.1:<local-port> --from-env api_key=PM_TWENTY_CERT_TOKEN --json` — FAIL exit 2. Failure: `etl_full_refresh_append` -> `record is missing cursor field "updated_at"`; certify creates the bootstrap connection before catalog discovery, so it defaults to `updated_at` while Twenty schemas use `updatedAt`.
- Historical full command: `PM_TWENTY_CERT_TOKEN=fixture_token_placeholder ./pm connectors certify twenty --stream attachments --full --skip write --limit 1 --config base_url=http://127.0.0.1:<local-port> --from-env api_key=PM_TWENTY_CERT_TOKEN --json` — FAIL exit 2. Surface inventory passed (`endpoints=168`, `covered=168`, `blocked=0`; `stream=56`, `write=112`), read/sync/flow/schedule capabilities passed, but `etl_full_refresh_overwrite_deduped` failed for `message_channel_message_association_message_folders` with `file name too long` in `_pm_raw/...jsonl.tmp` generated by certify/full-sweep naming.
- 2026-07-11 red refresh before code edits:
  - `./pm help connectors certify` — PASS/exit 0; help/manual rendered; no credentials read.
  - Non-full localhost command with `--keep-workdir` — FAIL/exit 2; kept-workdir manual `./pm etl run --connection cert_live --stream attachments --json --root <kept-workdir>` — FAIL/exit 1 with JSON error `record is missing cursor field "updated_at"`.
  - Full localhost command with `--full --skip write --keep-workdir` — FAIL/exit 2; current run still dominated by cursor failure, so long-path blocker will be pinned by focused unit test before the code fix.
- Corrective disposition: fixed certify harness only; no connector defs, docs CLI surface, dependencies, or parent artifacts changed.

## Correction gates — 2026-07-11
- Red unit gate: `go test ./internal/connectors/certify -run 'TestInspectStreamSpecsSeedBootstrapCursor|TestFullSweepArtifactNamesAreBoundedForLongStream' -count=1` — FAIL/build failed before production code (`undefined: streamSpecsFromInspectEnvelope`; missing bounded-name helpers).
- Green focused unit gate: `go test ./internal/connectors/certify -run 'TestInspectStreamSpecsSeedBootstrapCursor|TestFullSweepArtifactNamesAreBoundedForLongStream' -count=1` — PASS: `ok polymetrics.ai/internal/connectors/certify 0.525s`.
- Broader focused certify package gate: `go test ./internal/connectors/certify -run 'TestInspectStreamSpecsSeedBootstrapCursor|TestFullSweepArtifactNamesAreBoundedForLongStream|TestCatalogStreamSpecsFromStreams|TestFullSweepNamesAreStreamScoped|TestFullSweepSourceStagesAgainstSample|TestSourceStagesAgainstSample' -count=1` — PASS: `ok polymetrics.ai/internal/connectors/certify 46.341s`.
- `go test ./internal/connectors/conformance -run 'TestConformance/twenty|TestTwentyFixturesCoverAllStreamsAndWrites' -count=1` — PASS: `ok polymetrics.ai/internal/connectors/conformance 1.819s`.
- `go run ./cmd/connectorgen validate internal/connectors/defs --json` — PASS: `findings=0`, `warnings=0`.
- `go vet ./...` — PASS, no output.
- `go build ./cmd/pm` — PASS, no output.
- `gofmt -w cmd internal` then `gofmt -l cmd internal` — PASS/clean (`gofmt clean`).
- Localhost fixture server: `/tmp/twenty_fixture_all_server.py` served checked-in Twenty stream fixtures on `127.0.0.1:<local-port>`; no live `api.twenty.com` calls.
- Non-full certify: `PM_TWENTY_CERT_TOKEN=fixture_token_placeholder ./pm connectors certify twenty --stream attachments --limit 1 --config base_url=http://127.0.0.1:<local-port> --from-env api_key=PM_TWENTY_CERT_TOKEN --json` — PASS/exit 0; `.report.passed=true`.
- Full certify: `PM_TWENTY_CERT_TOKEN=fixture_token_placeholder ./pm connectors certify twenty --stream attachments --full --skip write --limit 1 --config base_url=http://127.0.0.1:<local-port> --from-env api_key=PM_TWENTY_CERT_TOKEN --json` — PASS/exit 0; `.report.passed=true`.

## Not run
- `make verify` — NOT RUN. Reason: `verify` depends on `smoke`, and `smoke-no-build` runs `./pm reverse run "$PLAN_ID" --approve "$APPROVAL"`; this correction forbids reverse ETL execution and destructive/write execution. Relied on focused local gates plus existing PR CI verify context.

## Safety notes
- No live Twenty API contacted.
- No real credentials requested, printed, stored, or summarized.
- Temp certify workdirs created with placeholder env were removed.
- No reverse ETL execution against Twenty; write fixtures are replay/capture request-shape only.
- No new dependencies.
