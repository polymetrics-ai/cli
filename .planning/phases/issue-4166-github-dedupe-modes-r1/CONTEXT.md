# Context: GitHub dedupe modes r1

The Firstmate launch brief, the binding truthfulness shared context, and F3 of
`/Users/karthiksivadas/karthik-agent-workspace/data/cli-mvp-truthfulness-sweep-r1/report.md`
define this non-interactive corrective phase.

Locked decisions:

- The target connector is GitHub. The assigned scope also includes the shared
  certification-matrix generator because it manufactures the false claims for
  every connector; correcting generated output alone is forbidden.
- Mode-cell capability facts are derived by intersecting the connector-level
  capability with the source transport's actually admitted modes. A generated
  claim may change in any connector when that intersection says it must.
- GitHub must make `incremental_dedupe` and `incremental_dedupe_history`
  truthful through the production warehouse-mediated ETL path. The modes must
  be admitted by the source transport, find registered destination/apply
  support during preflight, and show their dedupe semantics in a real `pm`
  run against a private retained GitHub repository.
- `incremental_append`, PostgreSQL history-mode work, certification `pass`
  semantics, and `--full-parity` remain outside this phase.
- The live proof uses an existing GitHub CLI identity through a shell command
  substitution only. No credential value is written, displayed, committed, or
  included in artifacts.

The phase uses the required inline/manual GSD fallback: `scripts/gsd prompt`
resolved `discuss-phase`, `plan-phase --tdd`, `execute-phase`, `verify-work`,
and `code-review`; this runtime has no compatible isolated Pi worker and the
task explicitly requires one autonomous worker. That is execution mechanics,
not a waiver of the lifecycle.
