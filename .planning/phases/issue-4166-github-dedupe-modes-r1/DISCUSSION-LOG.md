# Discussion Log: GitHub dedupe modes r1

## 2026-08-16 — non-interactive issue-authorized discussion

- Generator truth comes first: certification-matrix mode cells must follow
  transport admission, not a connector-wide coarse `read` bit.
- The resulting cross-connector generated changes are required consequences of
  the shared generator correction, not unrelated connector edits.
- GitHub must add the two named modes only if the real warehouse apply path can
  preserve the required repeated-key and replay behavior. A missing honest
  key/cursor is a `needs-decision` finding, never a synthetic mode.
- Live evidence must use a private retained repository, a built `pm` binary,
  and an independent GitHub REST read-back. Happy, bad, and edge evidence are
  named in the TDD ledger and future PR body.
- Exclusions are `incremental_append`, PostgreSQL history mode, certification
  result semantics, `--full-parity`, and destination action expansion.
