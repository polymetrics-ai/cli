# Verification — scoped operation-surface reconciliation, R1

- [x] Plan recorded before shared-tooling edits.
- [x] RED command captured before implementation: unknown `--notes-contains` flag.
- [x] Focused selector test passed after implementation.
- [x] Full `go test -count=1 ./cmd/connectorgen` passed.
- [x] Existing `--reason-contains` unit callers retain an explicit empty notes selector.
- [x] The selector is additive and conjunctive; no unselected ledger row is rewritten.
- [ ] Healthcare consumer rerun recorded in its own phase evidence.
