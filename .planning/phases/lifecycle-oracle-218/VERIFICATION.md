# Verification218 — implementation evidence, independent review pending

Source custody: isolated fm/cli-batch1-ci-tests210 after e38476e3a7a2783e0ac874564d4ccd690381cbf6; P 0edfc10c, A0 3c224e32, A1 8da2d962e9678e8ea5036ff918e1eb5275151173. A2 exact dirty input is pinned in lifecycle-oracle-218/a2-selection-green/receipt.json with source.diff; final committed source is pinned in private INPUTS.json. Every receipt has identical before/after source pins, exact argv, dependencies, elapsed, output hash, and named events. Counts below include parent events and must not be summed as independent leaves.

| Contract / public consumer | Owner and phase | Evidence / outcome |
|---|---|---|
| Strict complete LSN advancement after physical resume barrier | postgresCheckpointAdvanced173 / parsePostgresCheckpointLSN218, test-only | p-red: 84 run, 69 pass, 15 fail; p-green and p-race: identical84/84, zero skips. All29 original events retained, literal205 five malformed cases plus full syntax siblings/carry/max/equality controls. p-vet passes. Physical199 never rerun/relabelled. |
| Supplied registry chosen before constructor; standalone fallback once; fresh trees/flags | cobraRegistrySource legacy/manual-help methods, newRootCmd, public Run | a0-red actual counts legacy2/help1/supplied43/standalone44/public43. Green counts0/0/0/1/0. a0-green18/18 and a0-race19/19, identity/help/public differing args verified; final race includes direct manual-help wrapper. |
| Optional borrowed shared admission, unchanged key/budget/context semantics | RuntimeConfig.SharedRateLimits; engine resolver; Registry.ResolveRateLimit | TestBorrowedSharedRateConsumers218 exercises actual Check/saved Read/DirectRead/typed write against HTTP fixture. Healthy local/shared exactly1 send; nil/unavailable/cancel/deadline/closed/malformed handles0 sends. Final a1-handles-green race41/41 =4 consumers×10 modes+root. Nil-handle panic was an intermediate218 edge caught and fixed, not attributed to6f. Existing GitHub tests migrated to explicit config; a1-race76/76 across5 packages. |
| Lazy owned client, closed once, no cross-invocation closure | NewSharedRateLimitScope / ResolveRateLimit / Close; Registry adapter | Constructor/Close races,32 parallel callers, repeated availability contexts, partial constructor and Close errors, same/different address barriers. Actual client identity/key/budget/observer control uses ping hook without service. Bounded fresh child correlates4 real redis.Client identities/4 Close calls with maintenance loops0→4→0. a1-coordination9/9; a1-final-race48/48 includes revised controls. |
| Runtime injected before due resume; cancel/join before App returns closed | openWithRegistryWithStat; RuntimeOptions; App.Close | Actual App construction uses durable literal parked checkpoint and controlled scheduler; callback sees capability during Start, Close waits active callback release, constructor cancellation closes, durable run/checkpoint preserved. TestAppParkingLifecycle218 / TestAppBorrowedRuntime218; a1-app-03 4/4; race coverage; existing parking/recovery/process-kill/constructor controls84/84. |
| Production Apps owned; custom Apps/capabilities borrowed; provider result preserved | CLI run/appOpeners/four withApp helpers; direct anonymous ExecutableRuntime | Fresh public Run resource RED:3 versions leak0→3 maintenance loops; GREEN original3 +4 public controls0→0. Failure ownership tests registry/App/credential/command and cleanup error; generic sanitized warning preserves result. Two real concurrent CLI→App→Check fixture invocations use barriers and exactly2 sends; supplied App remains usable after Run. a1-cli-02 9/9, final race covers corrected cleanup barriers. |
| Only selected fresh sweep leaves project fixtures; declared membership unchanged | TestProductionRegistryBuiltPaths173 | a2-selection-red:3 healthy leaf commands but root fails prepared1868/selected3. Green moves projection inside callback before Parallel: prepared3/selected3; race11/11 includes both173 and178 original3 selected leaves. Both original13856 declaration guards remain. No partition, cap, verdict cache or full-union execution claim. |

A2 measurement: literal dockerhub/repositories list, github/autolinks view, asana/access-requests create-access-request. a2-measure4/4. BuildTransportPM existing sync.Once preserved: warm build-cache build21.478s, first fresh version2.978s (includes minimal command, not pure startup). Registry metadata0.868–3.953ms; selected bundle acquisition6.753/228.088/61.099ms; fixtures0.012/0.004/54.836ms; Preflight0.100/1.249/2.445ms; App open0.253–0.321ms, Close0.001–0.012ms. CLI in-process0.610/4.895/4.464ms; fresh process244.281/464.257/322.712ms. Fresh process internal phases were not separately instrumented. Measurements overlap a race run, use translated darwin/amd64 Go1.26.6, and cannot predict Linux full-suite time. Both sweeps still acquire each connector to enumerate authoritative commands; no independent pre-acquisition membership representation was designed. Parent can evaluate that separately with exact-union verification if needed.

Static checks: P vet and final five-package vet pass. Initial lint84 findings included owned test/style corrections; final lint77 findings15 files, no218 filename finding. lint-baseline-218.json identifies identical6f file hashes or unchanged diagnostic source lines; this is source comparison, not a separately executed full baseline lint. No suppression. Original21020 findings8 files preserved separately, and Linux publication witness remains unverified.

Failures retained: unused import compile/setup; first two App fixture attempts lacked valid checkpoint invariants (then read complete validators and corrected explicit fixture); CLI barrier closed twice (actual consumer resolves twice; Once correction); these are setup failures, never behavioral RED. All raw receipts remain. Older untracked test variants have hashes without full byte snapshots in some early receipts; final committed files and tracked deltas are retained, later receipts snapshot untracked bytes. This limitation is explicit rather than silently substituting final test bytes into early evidence.

GSD: sources/prompts discuss, plan --tdd, execute, verify-work, code-review resolved/read. Inline implementation/verification fallback because delegation is forbidden. No self-review; parent owns independent review. Help/command spelling/output contracts unchanged; constructor/public help controls passed; manual/website generator changes not applicable. Lifecycle architecture docs updated; Atlas aggregate belongs to parent integration. No Linux, live Redis/provider/database, full Verify, full13856 unions, CodeQL, CP16 or programme acceptance claimed.210 HANDOFF prose saying arm64 is corrected here:210 INPUTS already correctly records darwin/amd64; original evidence remains immutable.
