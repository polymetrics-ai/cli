# TDD ledger218

P Red: frozen205 independent source6f9366b4 probe16 events/10pass/6fail; fresh integrated p-red84run/69pass/15fail/0skip,22.92s, same29 original events plus55 syntax events. Five retained205 failures plus whitespace/suffix/hex-prefix siblings fail at actual advancement helper, not setup.
P Green: p-green84/84,13.12s; p-race84/84,37.04s; identical exact selected names and unmodified regression bytes between RED/GREEN. p-vet tagged CLI9.82s exit0. Strict ASCII two-halves (1..8 digits each) parser in test integration helper only. No production parser/dependency or physical run.
A0/A1/A2 pending. Existing210 lint20/8 baseline and Linux publication gap unchanged.

A0 Red: a0-red counts actual instance factory calls at unchanged eager fallback behavior: legacy2,help1,supplied tree43,standalone44,actual run43; expected0,0,0,1,0. Instance factory seam introduced without correcting eager behavior, no mutable global factory/cache.
A0 Green: a0-green18/18 events; a0-race19/19 including supplied metadata identity/no acquisition and public consecutive differing argv; standalone exactly1, supplied/helper/run0. Both fallback branches changed. Original fresh tree/flags/hidden help controls retained.
A1 Red: fresh child public Run3cycles observed maintenance loops0→3 after bounded2s quiescence; no Redis dial. a1-resource-red preserves command/source/raw output. A1 implementation pending.

A1 Green: a1-resource-green-02 normal fixes original three-Run maintenance0→3 RED to0→0; a1-final-race expands three version cycles with help, metadata, source-only and missing-credential controls (zero retained loops). a1-coordination9/9 with actual Redis fresh child4 distinct live IDs/4 Close, loops0→4→0; ownership, first-allocation/Close races and partial-constructor+Close errors preserved. a1-app-03 ordinary/reverse due-start injection and cancellation/join/durable checkpoint controls4/4. a1-cli-02 actual two concurrent CLI→App.TestCredential→engine.Check consumers9/9 including late registry/project/credential/command errors and sanitized cleanup result preservation. a1-engine49/49 covers Check/saved read/direct read/typed write and existing relevant hook/refusal controls. a1-race76/76; a1-final-race48/48 after test cleanup strengthening. Existing parking/process recovery84/84,77.14s. Vet exit0.

A1 fix-created edge: deliberately malformed borrowed capability returning nil handles panicked in engine admission (a1-handles-red, first child and root fail; incomplete remainder, not4 executed consumers). Added typed refusal before admission and independent missing-admission/missing-observer siblings. a1-handles-green race41/41 with all10 states across4 actual engine consumers; zero sends/records on refusals. No deployed or original6f production defect is inferred from this new-interface counterexample.

Setup history: a1-resource-green unused engine import (zero selected); a1-app/a1-app-02 invalid literal checkpoint (partitions, then timestamp); a1-cli barrier closed twice because actual Check resolves twice. All raw attempts retained.215 recovery reads strict checkpoint validator and adds synchronous fixture validation; scheduler/once barriers corrected without product changes.

A1 lint: initial84 issues included7 new test cleanup/style findings; corrected those. Final scoped lint77 locations in15 files, no218-file finding; diagnostic source lines all present in6f9366b4. Original210 lint20/8 separately retained. This is not a lint-green claim.

Toolchain erratum: PATH Go is1.26.6 darwin/amd64 on arm64 host, as210 INPUTS.json already records.210 HANDOFF prose incorrectly said arm64; originals retained and corrected here.218 timings are translated-amd64 observations, not native-arm64/Linux performance.
