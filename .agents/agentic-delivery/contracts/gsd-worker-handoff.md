# GSD worker handoff

The machine record retains exact safe command names, exit status, SHAs, evidence hashes, and gate
results. The rendered human digest is at most 40 lines and never includes secrets or raw output.
Completion means current-head evidence exists; it never means merge approval.

