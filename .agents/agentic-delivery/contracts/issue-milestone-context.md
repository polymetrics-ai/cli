# Issue milestone context

Each implementation issue maps to one native GSD milestone, isolated worktree, branch, and primary
PR. Intake validates `schemas/issue-milestone-context.schema.json` before GSD starts. The context
contains issue/parent identity, objective, scope/non-goals, acceptance, dependencies, write scope,
required reading/skills, TDD, verification, safety, human gates, branch/PR topology, and sources.
