# GSD unit dispatch

Every unit envelope validates against `schemas/gsd-unit-dispatch.schema.json`. Its four required
policy fields are `objective`, `output_format`, `tool_guidance`, and `boundaries`. Identity, base
head, worktree, write scope, required skills, TDD requirements, verification, deadlines, and typed
effect intents are machine fields rather than prose conventions.

