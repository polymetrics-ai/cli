{
  "base": {
    "url": "{{ config.base_url }}",
    "user_agent": "whenclausevalid/1.0",
    "headers": {},
    "auth": [
      { "mode": "bearer", "token": "{{ secrets.token }}", "when": "{{ config.auth_type == 'token' }}" },
      { "mode": "none", "when": "{{ config.auth_type in ['public', 'none', 'anonymous'] }}" }
    ],
    "pagination": { "type": "none" },
    "check": { "method": "GET", "path": "/ping" },
    "error_map": []
  },
  "streams": [
    {
      "name": "widgets",
      "path": "/widgets",
      "records": { "path": "data" },
      "incremental": { "cursor_field": "updated_at" },
      "schema": "schemas/widgets.json"
    }
  ]
}
