# Overview

Badconn is a synthetic connector used to seed connectorgen validate's default-type-mismatch rule.

## Auth setup

Provide a bearer token via the `token` secret.

## Streams notes

`widgets` is incremental on `updated_at`.

## Write actions & risks

None; read-only.

## Known limits

None; this is test fixture data.
