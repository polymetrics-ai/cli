version: "2"

# Scope note: this config owns the minimal, non-weakened linter set. The
# Makefile `lint` target owns the package scope.

run:
  timeout: 5m

linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - ineffassign
    - unused
    - misspell

  exclusions:
    generated: strict
    paths:
      - '(^|/)[^/]+_gen\.go$'
